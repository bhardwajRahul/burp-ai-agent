---
phase: 28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is
plan: 08
type: execute
wave: 5
depends_on: ["28-07"]
gap_closure: true
autonomous: true
requirements: [PRIV-05]
files_modified:
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
  - .planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-VERIFICATION.md

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "THE TEMPORAL BOUND IS NAMED IN THE CARRIER REGISTRY — `ISSUE_DETAIL_CARRIER_DISPOSITION`'s STILL OPEN clause carries the canonical literal `WRITE-TIME/READ-TIME BOUND`, states that both controls decide once at issue construction and bake the result into an immutable `AuditIssue.detail()`, and names `AiScanCheck.consolidateIssues`'s `KEEP_EXISTING` as why a re-scan does not repair it. HEAD: that literal occurs **0** times in `CookieCarrierInventoryTest.kt`."
    - "THE TEMPORAL BOUND IS NAMED IN THE SECURITY REGISTER — `26-SECURITY.md` row 315 gains a third dated marker that amends clause (d) 'WHAT THIS AMENDMENT DOES NOT COVER' by ADDING to its enumeration. HEAD: row 315 carries exactly two markers, `AMENDED 2026-08-27 by plan 28-03` and `AMENDED 2026-08-27 by plan 28-06`, and the literal occurs **0** times in the row."
    - "BOTH EDITS ARE APPEND-AND-AMEND, PROVEN BY DIGEST AND NOT BY ASSERTION — after the register edit the 8693-byte prefix of row 315 still hashes to `8dc326ac23204becce687deeba867740eb2d4dde21346c58d7da9595d137ae2e` and the 16071-byte prefix ending at the column separator still hashes to `5316a97149017ae824d162b72b99d954cb0fa25b28b0c3a8214c01e42390ed72`. Both digests are taken BEFORE the edit and re-asserted after; neither is reconstructed from the post-edit file."
    - "THE DISPOSITION IS RECORDED WITH ITS AUTHORITY AND ITS CONDITIONS — both records name D-28-09's acceptance as a HUMAN maintainer answer on 2026-08-28, not an auto-advance default, and map each of D-28-10's four conditions to the artifact that discharged it. `AR-27-08` STAYS OPEN in both."
    - "THE ROUND'S OWN TWO CORRECTIONS ARE RECORDED AS CORRECTIONS — the register and the registry both state that the 28-06 block named `AiScanCheckDetailCookieCarrierTest` as the committed probe for detail line (4) while that file asserted nothing about that line (`grep -o \"Payload Used\"` returned **0** at that commit), and that the route-2 gate's fail-open set was unrecorded. Both are named as prose written ahead of the tree, with the plan that repaired each."
    - "THE ASYMMETRY IS NOT SMOOTHED IN THE RECORD — detail line (4) on route 2 is recorded as DEFENCE IN DEPTH, not as a measured carrier, in both records. Adding assertions over it made a claim true; it did not close a leak, and neither record may say otherwise."
    - "THE OVERRIDE IS LAST AND CONDITIONAL — `28-VERIFICATION.md`'s frontmatter gains the `overrides:` block for SC1 with `accepted_by` naming the maintainer's 2026-08-28 answer and `accepted_at` set, and `overrides_applied` moves 0 -> 1. It is applied only after all six preconditions measure green; if any is unmet the task halts without writing."
    - "THE VERIFIER'S OWN FINDINGS SURVIVE THE OVERRIDE — `status`, `score`, `gaps`, `deferred` and the body's fenced template block in `28-VERIFICATION.md` are byte-unchanged. An override records an accepted must-have; it does not re-verify anything, and rewriting the verifier's account would be this phase's own named failure mode."
    - "PRIV-05 STAYS `- [ ]` — `.planning/REQUIREMENTS.md` is byte-unchanged at sha256 `9b3219662ec0d007c1c82d64eed3ef2698bd306ce69f01205ac9bbc3f42fcfb4`, PRIV-05 on line 23 still an unchecked box. The gate runs twice under distinct headings with raw output recorded both times."
    - "[edge:ordering] THE INSERTION POSITION IS ORDER-BEARING AND MACHINE-CHECKED — each new marker is appended AFTER every existing marker and BEFORE the ` | Measured by plan 27-08` column separator, so the prior text stays a contiguous byte-prefix and the three markers read top-to-bottom in date order. In `ISSUE_DETAIL_CARRIER_DISPOSITION` the same rule makes the append purely additive: exactly ONE existing source line is modified (the concatenation's final line gains a continuation), proven by `git diff -U0 "$BASE" | grep -cE '^-[^-]'`, pinned to the pre-plan commit because `execute-plan.md` commits per task."
    - "[edge:adjacency] THREE DATED MARKERS NOW TOUCH IN ONE CELL AND THREE IN ONE CONSTANT, AND THEY MUST NOT MERGE — each carries its own date and plan id and states explicitly what it amends or supersedes and what it does NOT. The third marker withdraws nothing: it EXTENDS an enumeration. Separately, `overrides:` appears twice in `28-VERIFICATION.md` — once in frontmatter (written by this plan) and once inside a fenced template block in the body (line 282 at HEAD, never edited); the frontmatter-scoped extraction keeps the two from colliding."
    - "[edge:encoding] WHOSE DEFINITION OF LENGTH APPLIES IS PINNED — row 315 carries em-dashes and backticks, so `substr`/`index` over it are BYTE operations only under `LC_ALL=C`; under a UTF-8 locale the same expression takes CHARACTERS and the digests silently disagree. Every offset, digest and byte-count command in this plan is run with `LC_ALL=C`, and each digest assertion is paired with its byte count (8693, 16071) so a character-vs-byte reading is caught rather than absorbed."
    - statement: "`threats_open` is RECOMPUTED by re-running the awk documented at the top of `26-SECURITY.md` over the Threat Register rather than restated, and its raw output plus the number of rows scanned is recorded. `AR-` rows sit outside that counter's population at any severity — the counter scans rows whose id begins `T-26-` and nothing else — so an OPEN `AR-27-08` beneath a counter reading 0 is documented behaviour, not a contradiction."
      verification: backstop
  prohibitions:
    - "MUST NOT apply the override before its conditions measure green. It is the last task for exactly that reason; applying it first would convert a CONDITIONAL acceptance into an unconditional one, which is not the acceptance the maintainer gave."
    - "MUST NOT rewrite, soften or delete the verifier's own `status`, `score`, `gaps` or `deferred` in `28-VERIFICATION.md`, and MUST NOT edit the fenced template block in its body. The override is an addition to frontmatter and nothing else."
    - "MUST NOT tick PRIV-05 or edit `.planning/REQUIREMENTS.md` at all. D-28-04 stands and the verifier judged it MORE consistent with the code after gap 1, not less; the sha256 gate is the enforcement."
    - "MUST NOT claim any control was added this round. Round 3 changes no runtime behaviour. A record that reads as if a leak was closed here is the precise failure this round exists to repair, and it would be the fifth time in this series."
    - "MUST NOT re-open the SC1 disposition or insert a checkpoint asking about it. D-28-09 was put to the maintainer and answered on 2026-08-28; re-asking a settled one-way decision is checkpoint fatigue, and the answer is already the plan's input."
  artifacts:
    - "src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt — third supersession on `ISSUE_DETAIL_CARRIER_DISPOSITION`; the class KDoc's marker count amended in place by append"
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md — row 315 third dated marker amending clause (d); `threats_open` recomputed"
    - ".planning/phases/28-.../28-VERIFICATION.md — frontmatter `overrides:` block for SC1; `overrides_applied` 0 -> 1"
    - ".planning/phases/28-.../28-08-SUMMARY.md — the two pre-edit digests, the two post-edit digests, the awk raw output, the six override preconditions with their raw outputs, and the PRIV-05 gate run twice"
  key_links:
    - "D-28-09's acceptance -> D-28-10's four conditions -> the four artifacts that discharge them (`consolidateIssues` KDoc and `PRIVACY_MODE_TOOLTIP` from plan 28-07; `ISSUE_DETAIL_CARRIER_DISPOSITION` and row 315 clause (d) from this plan) -> the override's precondition gate"
    - "the 8693-byte and 16071-byte prefix digests -> the append point at the ` | Measured by plan 27-08` column separator -> the append-and-amend protocol plans 28-03 and 28-06 established"
    - "plan 28-07's four `**Payload Used:**` assertions -> the corrected probe claim in both records -> D-28-11's first half discharged"
    - "plan 28-07's `theRouteTwoGateIsFailOpenForTheseCookieCapableTypes` -> the named fail-open set in both records -> D-28-11's second half discharged"
---

<objective>
Write the record round 3 exists to repair, then apply the override it earns — in that order, and
only in that order.

Plan 28-07 changed the tree: it named the bound at the privacy-mode selector and at
`AiScanCheck.consolidateIssues`, made the `**Payload Used:**` probe claim true, corrected the false
`type()` KDoc premise, and named the route-2 fail-open set with a pinned tripwire. This plan writes
that down in the two enumerations that drifted a step behind the mechanism, and then — last, gated,
and unreachable if any condition is unmet — applies the SC1 override the maintainer conditionally
granted on 2026-08-28.

Purpose: the verifier's own diagnosis was that this round measured its controls rigorously and did
not re-measure its record against them. The mechanism work is the strongest in the phase. Two
records describe it, and both currently claim slightly more than the tree supports. Closing that
distance is the entire remaining job.

Output: an honest carrier registry, an honest register cell, a recomputed counter, and a conditional
acceptance whose conditions are demonstrably met before it is written.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-CONTEXT.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-PATTERNS.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-07-SUMMARY.md
</context>

<measured_facts>
Measured at HEAD by this plan's author, immediately before writing. Use them; do not re-derive them
by reading whole files. Where a fact is a line number or an anchor, RE-DERIVE it at execution time —
plans 28-05 and 28-06 both broke on stale ranges in these exact files.

1. **`26-SECURITY.md` IS 266,063 BYTES OVER 1,799 LINES AND ROW 315 IS A SINGLE 16,191-BYTE LINE.**
   Reading it whole terminates the executor mid-run; this has happened before in this phase.
   Targeted extraction ONLY: `LC_ALL=C awk 'NR==315{…}'` and `grep -n`, never `cat`, never an
   unbounded `Read`. Line 316 is a further ~10,400 bytes, so do not widen a `sed` range "to be safe".

2. **THE ANCHOR IN THE ROUND-3 BRIEF IS STALE — MEASURED, NOT SUSPECTED.** The brief names the
   unique file-wide anchor as the full literal `not an omission. | Measured by plan 27-08`. At HEAD
   that literal occurs **0** times: plan 28-06's own amendment replaced the sentence that preceded
   the separator. Measured at HEAD:
   - `LC_ALL=C grep -o "not an omission. | Measured by plan 27-08" 26-SECURITY.md | wc -l` -> **0**
   - `LC_ALL=C grep -o "not a contradiction. | Measured by plan 27-08" 26-SECURITY.md | wc -l` -> **1**  (THE UNIQUE ANCHOR AT HEAD)
   - `LC_ALL=C grep -c "| Measured by plan 27-08" 26-SECURITY.md` -> **2** (lines 314 and 315) — the
     short form is NOT unique, exactly as the brief warns.
   This is itself an instance of the failure the round is repairing, and it is why step one of the
   register task is to re-derive the anchor rather than paste one.

3. **THE TWO PREFIX DIGESTS, both under `LC_ALL=C`.**
   - `LC_ALL=C awk 'NR==315{printf "%s", substr($0,1,8693)}' … | shasum -a 256` ->
     `8dc326ac23204becce687deeba867740eb2d4dde21346c58d7da9595d137ae2e` (the digest D-28-10 names)
   - `LC_ALL=C awk 'NR==315{i=index($0,"| Measured by plan 27-08"); printf "%s", substr($0,1,i-1)}' … | wc -c`
     -> **16071**, and the same piped to `shasum -a 256` ->
     `5316a97149017ae824d162b72b99d954cb0fa25b28b0c3a8214c01e42390ed72` (the full pre-edit prefix)

4. **ROW 315'S INTERNAL LAYOUT, by byte offset under `LC_ALL=C`.** Row head at offset 1 reads
   `| AR-27-08 | T-26-02-01 | **NEW, OPEN, severity MEDIUM …`. `AMENDED 2026-08-27 by plan 28-03`
   begins at **3427**. `AMENDED 2026-08-27 by plan 28-06` begins at **8696** — which is why the
   8693-byte prefix is the text before it. `**(d) WHAT THIS AMENDMENT DOES NOT COVER` begins at
   **12747**, i.e. clause (d) belongs to the 28-06 amendment. The column separator begins at
   **16072**. Clauses (e), (f) and (g) follow (d): (e) the enforcement gap / `WR-01` / D-28-06,
   (f) the disposition and its authority, (g) PRIV-05's status.

5. **THE ROW'S MARKER VOCABULARY, which must not gain a synonym.** Both existing markers are spelled
   `**AMENDED <ISO date> by plan 28-NN (phase 28) — …**`. `LC_ALL=C awk 'NR==315' … | grep -o
   "AMENDED 2026-08-27 by plan 28-0[36]" | wc -l` -> **2**.
   `grep -c "^threats_open: 0" 26-SECURITY.md` -> **1**, at line 198.

6. **`CookieCarrierInventoryTest.kt` IS 769 LINES.** `ISSUE_DETAIL_CARRIER_DISPOSITION` begins at
   `:700` (RE-DERIVE). Its concatenation's FINAL source line is
   `"a third would be caught by nothing."` — `grep -c` on that literal -> **1**. The constant carries
   ONE measurement (2026-08-25) and TWO supersessions, spelled ` SUPERSEDED 2026-08-27 (phase 28,
   plan 28-01)` and ` SUPERSEDED AGAIN 2026-08-27 (phase 28, plan 28-06)`, and its STILL OPEN clause
   currently names only `AR-28-01` and `AR-27-08`. The class KDoc above it contains the literal
   `THIS CONSTANT NOW CARRIES TWO` exactly **1** time — a sentence that becomes false the moment a
   third supersession lands, and which therefore has to be amended in the same pass rather than left
   to drift. `grep -o "WRITE-TIME/READ-TIME BOUND" | wc -l` on this file -> **0**.

7. **`28-VERIFICATION.md`.** Frontmatter is lines 1-99, closed by the second `^---$`.
   `status: gaps_found` at line 4, `score: 5/6 must-haves verified` at line 5,
   `overrides_applied: 0` at line 7, `gaps:` at line 22, `deferred: []` at line 96. There is NO
   `overrides:` key in frontmatter. **There IS an `overrides:` at line 282 — inside a fenced yaml
   block in the body**, under "Human Verification Required", which is the verifier's own template
   for this exact override. That block is a template and is NOT edited by this plan; a bare
   `grep -c "^overrides:"` would match both, which is why every override criterion below extracts
   the frontmatter first.

8. **PRIV-05 GATE BASELINE.** `shasum -a 256 .planning/REQUIREMENTS.md` ->
   `9b3219662ec0d007c1c82d64eed3ef2698bd306ce69f01205ac9bbc3f42fcfb4`; line 23 is
   `- [ ] **PRIV-05** (Finding 2, **high**): …`.

9. **SUITE AND TOOLING.** HEAD baseline before plan 28-07: 1298 tests, 0 failures, 0 errors, 1
   skipped (a pre-existing `@Disabled` in `ExternalMcpClientManagerTest`), 180 classes. The suite
   takes about 19 minutes — background it; a 600s foreground cap is not enough.
   `RedactionTest > windowedScanRedactsJsonPairAcrossEveryBoundaryAlignment()` is a known wall-clock
   flake under CPU load: fail-closed, not leak-open, and it passes in isolation.
   **`./gradlew check` is NOT a gate** — RED at HEAD for a maintainer-accepted coverage-floor reason.
   `JAVA_HOME=$(/usr/libexec/java_home -v 21)` is required; the default JDK 25 breaks Gradle 8.12.1.

10. **`grep -c` COUNTS LINES, NOT OCCURRENCES,** and caps at 1 on single-line input — which row 315
    is. Every occurrence count below uses `grep -o … | wc -l` for that reason. This exact defect
    blocked round 2's first plan-check.
</measured_facts>

<probe_edge_accounting>
Four PRIV-05 probe edges were surfaced, all `unresolved`, and all four are AUTHORED as explicit
`must_haves.truths` predicates across the two round-3 plans. No silent drops; no auto-dismissals.

| Edge | Authored in | Where |
|---|---|---|
| `empty` | 28-07 | `[edge:empty]` — the absent-`type()` arm and its corrected premise; the empty-`baseValue()` arm stays pinned |
| `ordering` | 28-08 | `[edge:ordering]` — insertion position as a byte-prefix invariant; the purely-additive diff count |
| `adjacency` | 28-08 | `[edge:adjacency]` — three touching dated markers that must not merge; the two `overrides:` occurrences that must not collide |
| `encoding` | 28-08 | `[edge:encoding]` — bytes vs characters over an em-dash-bearing row; `LC_ALL=C` plus paired byte counts |

4 surfaced == 4 authored + 0 flagged.
</probe_edge_accounting>

<reversibility_gates>
All three tasks carry a `reversibility` element rated one-way, and NONE is preceded by a
`checkpoint:decision`. That is deliberate, and `verify plan-structure` will warn about it three
times — the warnings are expected output, not defects to fix.

`planner-reversibility.md` names this exact case under **"Gating a decision already made"**: when the
phase CONTEXT.md records the human choosing this option, the door is already walked through; keep the
rating for the record and do NOT insert a checkpoint to re-ask. `D-28-09` and `D-28-10` were put to
the maintainer at the `verify_phase_goal` gate and answered on 2026-08-28, and the round-3 contract
states in terms that the disposition must not be re-opened and no checkpoint may ask about it again.

The ratings are kept because the doors are real — an append-only register cell, and an override that
unblocks `phase.complete 28` — and a future reader is entitled to see which doors this round walked
through. What replaces the checkpoint is the six-condition precondition gate on task 3: the
acceptance was CONDITIONAL, so the conditions are machine-checked instead of re-litigated.
</reversibility_gates>

<tasks>

<task type="auto">
  <name>Task 1: The third supersession on `ISSUE_DETAIL_CARRIER_DISPOSITION` — the temporal bound named in the STILL OPEN clause</name>

  <precondition>`grep -c '"a third would be caught by nothing."' src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt` prints `1` and `grep -o "SUPERSEDED AGAIN 2026-08-27 (phase 28, plan 28-06)" <same file> | wc -l` prints `1` — assert both before editing. If either moved, the constant was amended by something outside this round and its append point must be re-derived before a byte is written.</precondition>

  <reversibility rating="one-way">The register discipline is supersession, never deletion: a claim written into this constant can only be withdrawn by another append. D-28-09 and D-28-10 were put to the maintainer and answered on 2026-08-28, so the door is already walked through — no checkpoint re-asks it.</reversibility>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt` lines 40-80 (the class KDoc, including the `THIS CONSTANT NOW CARRIES TWO` sentence) and 690-769 (the constant's own KDoc and its full concatenation). RE-DERIVE both ranges; do not trust measured fact 6's numbers.
    - `.planning/phases/28-.../28-07-SUMMARY.md` — the symbols, test names and raw grep outputs plan 28-07 actually produced. Cite them by the names that SUMMARY uses. Do NOT invent an assertion name; if a name in this plan's prose disagrees with the SUMMARY, the SUMMARY wins and the disagreement is recorded.
    - `28-PATTERNS.md` → "Append-and-amend record correction" — the protocol, and `Redaction.kt:600-620` as the in-KDoc analog: prior text kept verbatim under a dated marker, nothing rewritten.
  </read_first>

  <files>
    src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt
  </files>

  <action>
BEFORE ANY EDIT IN THIS PLAN, PIN THE BASE COMMIT AND RECORD IT IN THE SUMMARY:
`BASE=$(git rev-parse HEAD)`. `execute-plan.md` commits per task, so a bare `git diff` goes empty the
moment task 1 is committed and every later diff gate would then pass by measuring nothing. Every
`git diff` acceptance criterion in this plan is written against `"$BASE"` for that reason. If the
shell is lost between tasks, re-derive it as the commit immediately preceding this plan's first
commit and record how it was re-derived.

Append a THIRD supersession block to `ISSUE_DETAIL_CARRIER_DISPOSITION`, in the constant's own
established vocabulary. The two existing markers are spelled ` SUPERSEDED 2026-08-27 (phase 28, plan
28-01)` and ` SUPERSEDED AGAIN 2026-08-27 (phase 28, plan 28-06)`. Write the third in the same form —
` SUPERSEDED A THIRD TIME <today's date, ISO> (phase 28, plan 28-08)` — keeping the marker word
identical. Do not introduce a synonym: a constant whose markers are spelled three different ways
cannot be enumerated by any reader or any grep, and inventing a token so a check passes is the
behaviour this constant exists to record against.

The new block's FIRST sentence must state what it does and does not do, because three dated markers
now sit adjacent in one string and a reader must not merge them: this one WITHDRAWS NOTHING. It
EXTENDS the STILL OPEN clause of the 2026-08-27 (plan 28-06) block. The 2026-08-25 measurement and
both prior supersessions stand, byte-exact, and are kept for the reason the first one gave — they are
the evidence the controls were needed.

Then write, in this order:

  (a) THE `WRITE-TIME/READ-TIME BOUND`, spelled with that exact literal because plan 28-07 used it at
      `AiScanCheck.consolidateIssues` and `26-SECURITY.md` will use it in task 2. All four controls
      decide ONCE, at issue construction, and bake the result into `AuditIssue.detail()` — an
      immutable string Burp stores and the `scanner_issues` MCP tool replays. There is no read-time
      pass. An issue built while `privacyMode` was `OFF` therefore still emits the raw cookie value
      on a later STRICT read. `AiScanCheck.consolidateIssues` returns `KEEP_EXISTING` on a matching
      canonical name and normalized URL, so a re-scan under STRICT does not repair the site map, and
      plan 28-05's own red probe recorded the sentinel surviving STRICT redaction verbatim whenever
      the write gate does not fire — `Redaction.apply` provably cannot rescue it downstream.

  (b) THE DISPOSITION AND ITS AUTHORITY. ACCEPTED as a NAMED RESIDUAL by a HUMAN maintainer answer on
      2026-08-28 (`D-28-09`, recorded in `28-CONTEXT.md`), not an auto-advance default — the question
      was put and answered. State the reason the maintainer gave rather than only the outcome: every
      issue PRODUCED under STRICT or BALANCED — the entire default posture, `AgentSettings.kt:493`
      defaults to `BALANCED` — is measurably clean across all four detail lines of both producers,
      and the residual requires a deliberate OFF scan followed by a mode switch, the same latent,
      opt-in reachability profile that put `AR-27-08` at MEDIUM rather than high. Say that a
      read-time fix is new architecture on the emission path and belongs to its own phase; a reader
      who cannot see the reason will re-litigate the decision.

  (c) WHERE THE BOUND IS NOW NAMED, by identifier, because `D-28-10` made the acceptance conditional
      on exactly this: `AiScanCheck.consolidateIssues`'s KDoc and `SettingsPanelInit`'s
      `PRIVACY_MODE_TOOLTIP` (both plan 28-07), `26-SECURITY.md` row 315 clause (d) (task 2 of this
      plan), and here.

  (d) THE PROBE CLAIM FOR DETAIL LINE (4) WAS FALSE WHEN THIS CONSTANT MADE IT, AND IS TRUE NOW.
      The 28-06 block named `AiScanCheckDetailCookieCarrierTest` as the committed probe for line (4),
      the `**Payload Used:**` line; at that commit `grep -o "Payload Used"` on that file returned
      **0**, KDoc included — the class asserted nothing about that line. Plan 28-07 added the named
      assertions; cite them by the names `28-07-SUMMARY.md` records. State the correction as prose
      written ahead of the tree rather than as a mistake to be apologised for — that is the failure
      mode being tracked, and naming it precisely is worth more than softening it.
      THE ASYMMETRY IS UNCHANGED BY THE REPAIR: line (4) on route 2 remains DEFENCE IN DEPTH and not
      a measured carrier, because `AiScanCheck` sources payloads from the static `getQuickPayloads`
      table and interpolates no insertion-point value. Adding assertions made a claim true; it did
      not close a leak. Say so in those terms.

  (e) THE ROUTE-2 GATE'S FAIL-OPEN SET, previously unrecorded (`D-28-11`). `isCookieInsertionPoint`
      is an identity compare against `AuditInsertionPointType.PARAM_COOKIE`, and four members of that
      17-member enum can carry a cookie value while not being `PARAM_COOKIE`: `HEADER`,
      `USER_PROVIDED`, `EXTENSION_PROVIDED` and `UNKNOWN`. Contrast it with route 1, whose
      `InjectionType` has exactly one cookie-capable member and it IS `COOKIE` — which is what made
      D-28-01's pass-through safe BY CONSTRUCTION. Record that widening was CONSIDERED AND NOT TAKEN,
      with plan 28-07's four reasons, and name the two tests that pin the residual and its enum
      population. Also record the corrected KDoc premise: the claim that a real Burp implementation
      may return null from `type()` was FALSE against the shipped jar — `javap` on
      `montoya-api-2026.2.jar` shows a DEFAULT method whose entire body is
      `getstatic AuditInsertionPointType.EXTENSION_PROVIDED; areturn`.

  (f) WHAT IS STILL OPEN AFTER THIS ROUND, so the constant cannot be read as a closure: `AR-28-01`
      (the `Evidence:` line in the same blob, MEDIUM, maintainer-accepted at 28-03's blocking
      checkpoint and deliberately not reopened); `AR-27-08` itself, which STAYS OPEN in
      `26-SECURITY.md`; the write-time/read-time bound as the named residual of (a); the route-2
      fail-open set of (e); and the continued absence of any repository-wide detail-producer gate —
      `WR-01` is not closed by anything written here and `D-28-06` records building one as CONSIDERED
      AND NOT TAKEN. State plainly that ROUND 3 ADDED NO CONTROL: it changed no runtime behaviour,
      and a reader who takes this block as evidence of a new control has misread it.

THEN AMEND THE CLASS KDoc IN THE SAME PASS. Measured: the KDoc above the constant contains the
sentence beginning `THIS CONSTANT NOW CARRIES TWO SUPERSESSIONS AND ONE MEASUREMENT`, which becomes
false the instant the third marker lands. Do NOT rewrite it — that is the protocol this file
enforces. Append a new dated paragraph before the KDoc's closing delimiter stating that the constant
now carries THREE supersessions and one measurement, that the preceding sentence was true at plan
28-06's commit, and that the third supersession extends rather than withdraws. Leaving the old count
in place uncorrected would be, in miniature, exactly the drift this round exists to close.

Do not touch any other entry, map or assertion in this file. Do not edit `.planning/REQUIREMENTS.md`.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.CookieCarrierInventoryTest' --console=plain</automated>
  </verify>

  <acceptance_criteria>
    - `grep -o "WRITE-TIME/READ-TIME BOUND" src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt | wc -l` prints `1` or more. HEAD value: **0**.
    - `grep -o "SUPERSEDED A THIRD TIME" src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt | wc -l` prints `1`. HEAD value: **0**.
    - BOTH PRIOR MARKERS INTACT: `grep -o "SUPERSEDED 2026-08-27 (phase 28, plan 28-01)" <same file> | wc -l` prints `1` and `grep -o "SUPERSEDED AGAIN 2026-08-27 (phase 28, plan 28-06)" <same file> | wc -l` prints `1`. HEAD values: **1** and **1**.
    - `grep -o "DEFENCE IN DEPTH" src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt | wc -l` prints `1` or more — the asymmetry survives into the record. HEAD value: **0**.
    - `grep -o "EXTENSION_PROVIDED" src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt | wc -l` prints `2` or more — the fail-open set is named, not summarised. HEAD value: **0**.
    - `grep -o "D-28-09" src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt | wc -l` prints `1` or more, and the same for `D-28-11`. HEAD values: **0** and **0**.
    - [edge:ordering] PURELY ADDITIVE, base-pinned so it holds whether or not the task is already committed: `git diff -U0 "$BASE" -- src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt | grep -cE '^-[^-]'` prints `1` — the concatenation's former final line, which gains a continuation. Every other changed line is an addition.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.CookieCarrierInventoryTest' --console=plain` exits 0, and `build/test-results/test/TEST-com.six2dez.burp.aiagent.redact.CookieCarrierInventoryTest.xml` reports `failures="0" errors="0"`.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt --console=plain` exits 0 and `git diff --stat -- detekt-baseline.xml` prints nothing.
  </acceptance_criteria>

  <done>The carrier registry names the write-time/read-time bound, its human authority, the two claims round 3 had to repair and the five things still open — appended under a third dated marker with both prior blocks byte-exact and exactly one source line modified.</done>
</task>

<task type="auto">
  <name>Task 2: `26-SECURITY.md` row 315 — a third dated marker amending clause (d), and `threats_open` recomputed</name>

  <precondition>All four of these print their measured values BEFORE any edit, under `LC_ALL=C`: the 8693-byte prefix digest is `8dc326ac23204becce687deeba867740eb2d4dde21346c58d7da9595d137ae2e`; the prefix ending at the column separator is 16071 bytes and digests to `5316a97149017ae824d162b72b99d954cb0fa25b28b0c3a8214c01e42390ed72`; the anchor `not a contradiction. | Measured by plan 27-08` occurs exactly once file-wide; and row 315 still begins `| AR-27-08 | T-26-02-01 | **NEW, OPEN, severity MEDIUM`. If ANY of the four differs, STOP and re-measure — the row moved and every offset in this task is void.</precondition>

  <reversibility rating="one-way">The security register is append-only by its own protocol: a disposition written into this cell can only be superseded by another append, never withdrawn, and downstream milestone audits read it as evidence. D-28-09 and D-28-10 were answered by the maintainer on 2026-08-28, so no checkpoint re-asks the door this task walks through.</reversibility>

  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` — **TARGETED EXTRACTION ONLY. NEVER READ THIS FILE WHOLE**: it is ~266 KB over 1,799 lines and line 315 alone is a single 16,191-byte row, with line 316 a further ~10,400 bytes. A full read terminates the executor mid-task; this has already happened in this phase. Use exactly these: `LC_ALL=C awk 'NR==315{print substr($0,1,700)}'` for the row head; `LC_ALL=C awk 'NR==315{print substr($0,12747,1600)}'` for clause (d) — RE-DERIVE that offset with `index($0,"**(d) WHAT THIS AMENDMENT DOES NOT COVER")` rather than trusting it; `LC_ALL=C awk 'NR==315{n=length($0); print substr($0,n-1500)}'` for the tail and the append point; and `sed -n '1,20p'` plus `sed -n '185,200p'` for the front-matter counter and its documented population rule. Do not widen any range "to be safe".
    - `.planning/phases/28-.../28-07-SUMMARY.md` — the symbols, test names and raw grep outputs to cite. The SUMMARY's names win over this plan's prose if they disagree.
    - `28-PATTERNS.md` → "Append-and-amend record correction" — the protocol plans 28-03 and 28-06 used, whose byte-prefix guarantee SC5 verified by digest.
  </read_first>

  <files>
    .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
  </files>

  <action>
STEP ONE IS RE-DERIVATION, NOT EDITING, AND IT IS NOT OPTIONAL. The round-3 brief hands over an
anchor literal that is STALE: it names `not an omission. | Measured by plan 27-08`, which occurs
**0** times at HEAD because plan 28-06's own amendment replaced the preceding sentence. The measured
unique anchor at HEAD is `not a contradiction. | Measured by plan 27-08`. Re-derive it yourself
rather than pasting either one: run `LC_ALL=C awk 'NR==315{i=index($0,"| Measured by plan 27-08");
print substr($0,i-40,64)}'` to read the 40 bytes preceding the separator, then confirm the resulting
literal occurs exactly once file-wide with `grep -o … | wc -l`. Record both the derived anchor and
the stale one in the SUMMARY. This is itself an instance of the drift the round is repairing, and
recording it is part of the work.

`LC_ALL=C` IS LOAD-BEARING ON EVERY COMMAND IN THIS TASK. Row 315 carries em-dashes and backticks.
Under a UTF-8 locale `substr` and `index` count CHARACTERS, and the digests will disagree with the
measured values for a reason that looks like tampering and is not. Pair every digest with its byte
count — 8693 and 16071 — so a character-vs-byte reading is caught rather than absorbed.

RECORD BOTH PRE-EDIT DIGESTS AND PASTE THEIR RAW OUTPUT INTO THE SUMMARY BEFORE TOUCHING THE FILE.
They must print the values in this task's `<precondition>`. A digest reconstructed from the post-edit
file proves nothing, which is why the order is fixed.

Then amend the `AR-27-08` cell by INSERTING new text immediately before the ` | Measured by plan
27-08` column separator on line 315, using a scoped, anchored edit against the re-derived unique
literal. Never a whole-row retype, never a whole-file rewrite. Nothing before the insertion point may
change by one byte, and the file's line count must not change — the row is one line and stays one
line.

Open the appended text with a dated marker in THIS ROW'S OWN vocabulary. Both existing markers read
`**AMENDED <ISO date> by plan 28-NN (phase 28) — …**`. Write the third the same way:
`**AMENDED <today's date, ISO> by plan 28-08 (phase 28) — …**`. Do not coin a synonym for the marker
word; a cell whose markers are spelled three ways cannot be enumerated by any reader or any grep.

The marker's first sentence must state, explicitly, that this amendment **ADDS TO CLAUSE (d)**
"WHAT THIS AMENDMENT DOES NOT COVER" of the 2026-08-27 (plan 28-06) amendment and WITHDRAWS NOTHING —
neither the 2026-08-25 measurement nor either prior amendment. Three dated markers now sit adjacent
in one cell; each must state what it touches so a reader cannot merge them or mistake which is being
amended. Say why the addition is appended at the tail rather than spliced into clause (d) in place:
the protocol's byte-prefix guarantee is what makes the record auditable, and honouring it costs one
cross-reference while breaking it would cost the guarantee.

Then write, in this order — the same five items task 1 wrote into the carrier registry, in register
language, so the two records cannot drift into two accounts of one fact:

  (a) THE `WRITE-TIME/READ-TIME BOUND`, spelled with that exact literal. Both producers decide once
      at issue construction and bake the result into an immutable `AuditIssue.detail()` that Burp
      stores and `scanner_issues` replays; an issue built under `OFF` still emits the raw cookie
      value on a later STRICT read; `AiScanCheck.consolidateIssues` returns `KEEP_EXISTING` on a
      matching canonical name and normalized URL so a re-scan does not repair the site map; and plan
      28-05's red probe recorded the sentinel surviving STRICT redaction verbatim whenever the write
      gate does not fire.

  (b) THE DISPOSITION AND ITS AUTHORITY: ACCEPTED as a NAMED RESIDUAL by a HUMAN maintainer answer on
      2026-08-28 (`D-28-09`), not an auto-advance default, with the maintainer's stated reason — the
      entire default posture is measurably clean across all four detail lines of both producers, and
      the residual needs a deliberate OFF scan followed by a mode switch, the same latent opt-in
      profile that set `AR-27-08` at MEDIUM. A read-time fix is new architecture and belongs to its
      own phase. Then map each of `D-28-10`'s conditions to the artifact that discharged it, by
      identifier: the `consolidateIssues` KDoc, `SettingsPanelInit`'s `PRIVACY_MODE_TOOLTIP`,
      `ISSUE_DETAIL_CARRIER_DISPOSITION`'s third supersession, and this clause.

  (c) THE PROBE CLAIM FOR DETAIL LINE (4), corrected: this row named
      `AiScanCheckDetailCookieCarrierTest` as the committed probe for the `**Payload Used:**` line
      while that file asserted nothing about it (`grep -o "Payload Used"` returned **0** at that
      commit). Plan 28-07 supplied the assertions; cite them by the names `28-07-SUMMARY.md` records.
      Keep the asymmetry intact and unsoftened: line (4) on route 2 is DEFENCE IN DEPTH, not a
      measured carrier, because that class sources payloads from the static `getQuickPayloads` table
      and interpolates no insertion-point value. Making a claim true is not closing a leak.

  (d) THE ROUTE-2 FAIL-OPEN SET, previously unrecorded (`D-28-11`): `HEADER`, `USER_PROVIDED`,
      `EXTENSION_PROVIDED` and `UNKNOWN`, four of the 17 `AuditInsertionPointType` members, are
      cookie-capable and are NOT matched by the identity compare. Contrast route 1, whose
      `InjectionType` has exactly one cookie-capable member and it IS `COOKIE` — the property that
      made D-28-01's pass-through safe by construction and which this enum lacks. Record widening as
      CONSIDERED AND NOT TAKEN with plan 28-07's four reasons, name the two tests that pin the
      residual and its enum population, and record the corrected KDoc premise measured by `javap` on
      `montoya-api-2026.2.jar`.

  (e) WHAT THIS AMENDMENT STILL DOES NOT COVER, restating clause (d)'s existing identifiers so the
      enumeration reads whole in one place — `AR-28-01`, `AR-27-04`, `AR-27-07`, `AR-27-10`,
      `AR-27-11` — and adding the two new named residuals from (a) and (d). Re-state that clause (e)
      of the 28-06 amendment is untouched: no repository-wide detail-producer gate exists, `WR-01` is
      not closed by anything written here, and `D-28-06` recorded building one as CONSIDERED AND NOT
      TAKEN. `AR-27-08` **STAYS OPEN**. PRIV-05 stays `- [ ]` and `.planning/REQUIREMENTS.md` stays
      byte-unchanged. And state plainly that ROUND 3 ADDED NO CONTROL AND CHANGED NO RUNTIME
      BEHAVIOUR: it repaired the record and named a residual, and a reader who takes this marker as
      evidence of a new control has misread it.

RECOMPUTE `threats_open` RATHER THAN RESTATING IT. Re-run the awk documented at the top of
`26-SECURITY.md` over the Threat Register after the edit, paste its RAW output and the number of rows
scanned into the SUMMARY, and update the front-matter counter ONLY if the recomputed value differs
from the written one. Record in the SUMMARY that `AR-` rows sit outside that counter's population at
any severity because it matches rows whose id begins `T-26-` and nothing else, so an OPEN `AR-27-08`
beneath a counter reading 0 is documented behaviour rather than a contradiction. This round adds,
amends and reclassifies no `T-26-` row, so the expected raw output is unchanged.

Re-assert BOTH prefix digests after the edit and paste their raw output into the SUMMARY beside the
pre-edit pair. Do not edit `.planning/REQUIREMENTS.md`. Do not touch any other row.
  </action>

  <verify>
    <automated>LC_ALL=C awk 'NR==315{printf "%s", substr($0,1,8693)}' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | shasum -a 256</automated>
  </verify>

  <acceptance_criteria>
    - The command in `<verify>` prints `8dc326ac23204becce687deeba867740eb2d4dde21346c58d7da9595d137ae2e` AFTER the edit. HEAD value: identical — this digest must not move.
    - `LC_ALL=C awk 'NR==315{i=index($0,"| Measured by plan 27-08"); printf "%s", substr($0,1,i-1)}' <file> | wc -c` prints `16071` and the same pipeline into `shasum -a 256` prints `5316a97149017ae824d162b72b99d954cb0fa25b28b0c3a8214c01e42390ed72`. HEAD values: **16071** and that digest — both must be unchanged, which is what proves the edit was an append.
    - `LC_ALL=C awk 'NR==315' <file> | grep -o "AMENDED 2026-08-27 by plan 28-0[36]" | wc -l` prints `2` — both prior markers survive byte-exact. HEAD value: **2**.
    - `LC_ALL=C awk 'NR==315' <file> | grep -o "by plan 28-08 (phase 28)" | wc -l` prints `1`. HEAD value: **0**.
    - `LC_ALL=C awk 'NR==315' <file> | grep -o "WRITE-TIME/READ-TIME BOUND" | wc -l` prints `1` or more. HEAD value: **0**.
    - `LC_ALL=C awk 'NR==315' <file> | grep -o "EXTENSION_PROVIDED" | wc -l` prints `2` or more, and `grep -o "D-28-09" | wc -l` prints `1` or more, and `grep -o "D-28-11" | wc -l` prints `1` or more. HEAD values: **0**, **0**, **0**.
    - `LC_ALL=C awk 'NR==315{print substr($0,1,60)}' <file>` still begins `| AR-27-08 | T-26-02-01 | **NEW, OPEN, severity MEDIUM` — the row stays OPEN.
    - [edge:ordering] The row is still one line and the file still has 1799 lines: `wc -l < <file>` prints `1799`. HEAD value: **1799**. And `git diff -U0 "$BASE" -- <file> | grep -cE '^-[^-]'` prints `1` — row 315 alone. Base-pinned: task 1 is committed by the time this runs, so a bare `git diff` would print `0` and prove nothing.
    - `grep -c "^threats_open: 0" <file>` prints `1` — recomputed and unchanged, with the awk's raw output and row count recorded in the SUMMARY. HEAD value: **1**.
    - `git diff --stat -- .planning/REQUIREMENTS.md` prints nothing.
  </acceptance_criteria>

  <done>Row 315 carries a third dated marker that adds to clause (d) rather than replacing it, names the bound, the authority, the two repaired claims and the two new residuals, keeps `AR-27-08` open, and leaves both measured byte-prefixes identical.</done>
</task>

<task type="auto">
  <name>Task 3: THE OVERRIDE — applied last, and only after all six of its conditions measure green</name>

  <precondition>ALL SIX of the following print their required values before a single byte is written to `28-VERIFICATION.md`. If ANY of them fails, HALT: write nothing, record the failing check and its raw output in the SUMMARY, and leave `overrides_applied` at 0. (1) `grep -o "Applies from now on, not retroactively" src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelInit.kt | wc -l` >= 1. (2) `grep -o "WRITE-TIME/READ-TIME BOUND" src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt | wc -l` >= 1. (3) `grep -o "Payload Used" src/test/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheckDetailCookieCarrierTest.kt | wc -l` >= 4. (4) `grep -o "WRITE-TIME/READ-TIME BOUND" src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt | wc -l` >= 1. (5) `LC_ALL=C awk 'NR==315' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | grep -o "by plan 28-08 (phase 28)" | wc -l` == 1. (6) the full suite reports 0 failures and 0 errors. Every HEAD value for (1)-(5) is 0 except (3), whose HEAD value is also 0 — so no check can pass by accident on an unmodified tree.</precondition>

  <reversibility rating="one-way">Applying the override unblocks `phase.complete 28` and is read downstream as the maintainer's acceptance of an open security residual. The decision itself was put to the maintainer and answered on 2026-08-28 (D-28-09 / D-28-10), so the door is already walked through and no checkpoint re-asks it; the rating is kept so the record shows which door this was.</reversibility>

  <read_first>
    - `/private/tmp/claude-501/-Users-six2dez-Tools-burp-ai-agent/9350b64b-6a1e-458a-8927-6cc4268cc53d/scratchpad/r3/OVERRIDE-BLOCK.yaml` — the literal block to paste. It is 13 lines; read it whole.
    - `.planning/phases/28-.../28-VERIFICATION.md` lines 1-25 only (the frontmatter head through the start of `gaps:`) and lines 276-300 (the fenced template block in the body, so it is recognised and NOT edited). Do not read the file whole — the gap narrative is long and none of it is needed here.
    - `.planning/phases/28-.../28-CONTEXT.md` lines 130-185 (the file is 185 lines; D-28-09 at 136, D-28-10 at 154, D-28-11 at 167 running to the end — a 130-175 range would cut D-28-11 mid-clause) — D-28-09, D-28-10 and D-28-11, for the exact phrasing of the maintainer's answer that `accepted_by` must name.
  </read_first>

  <files>
    .planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-VERIFICATION.md
  </files>

  <action>
RUN THE SIX PRECONDITION CHECKS FIRST AND PASTE THEIR RAW OUTPUT INTO THE SUMMARY UNDER A HEADING
THAT NAMES THEM AS THE OVERRIDE GATE. This task is last for a reason: `D-28-10` made the acceptance
CONDITIONAL, and applying it before its conditions are demonstrably met would silently convert a
conditional acceptance into an unconditional one — which is not the acceptance the maintainer gave.
If any check fails, HALT without writing. A halt here is a correct outcome, not a failure of the
plan; record which check failed and stop.

**THERE ARE TWO `overrides:` IN THIS FILE AND ONLY ONE OF THEM IS YOURS.** Measured at HEAD:
frontmatter is lines 1-99, closed by the second `^---$`, and it has NO `overrides:` key. The body
carries an `overrides:` at line 282 inside a fenced yaml block under "Human Verification Required" —
that is the verifier's own template for this exact override, it is documentation, and it is NOT
edited by this plan. Scope every read and every check to the frontmatter with
`awk 'NR>1 && /^---$/{exit} NR>1' <file>` rather than a bare `grep`, or the two will be confused.

Insert the `overrides:` block from `OVERRIDE-BLOCK.yaml` into the frontmatter, placed immediately
after `overrides_applied` so the count and the list it counts sit together. Copy `must_have` and
`reason` VERBATIM from that file — they are the verifier's own words and the maintainer accepted
those words, not a paraphrase. Fill the two placeholders:
  - `accepted_by` names the maintainer and the answer: a human answer given interactively on
    2026-08-28 at the `verify_phase_goal` gate, recorded as `D-28-09` and `D-28-10` in
    `28-CONTEXT.md`. State that it was NOT an auto-advance default — the same discipline plan 28-06
    applied to its own checkpoint, and the reason that plan recorded the auto-mode flags alongside
    the answer.
  - `accepted_at` is a real ISO-8601 timestamp. Neither placeholder token may survive into the
    frontmatter copy.

Then change `overrides_applied` from `0` to `1`. It is a count of the list immediately below it;
one entry, one applied.

CHANGE NOTHING ELSE IN THE FILE. `status` stays `gaps_found`, `score` stays as written, and `gaps`,
`deferred`, `behavior_unverified_items` and `coincidental_reliance_items` are untouched. An override
records that a named must-have was accepted despite being unmet; it does not re-verify anything and
it does not entitle this plan to edit the verifier's account of what it found. Rewriting a verifier's
findings to match a desired outcome is the failure mode the entire phase-27/28 series exists to
correct, and doing it in the last task of the round would be the most expensive possible place to do
it.

Add nothing to the body. Do not touch the fenced template block.

FINALLY, RUN THE PRIV-05 GATE TWICE, under two distinct headings in the SUMMARY, with raw output
recorded both times: RUN 1 in-task, RUN 2 post-completion, immediately before the phase commit and
after any `phase.complete` step. Both must show `shasum -a 256 .planning/REQUIREMENTS.md` beginning
`9b3219662ec0d007` and `grep -n 'PRIV-05' .planning/REQUIREMENTS.md` returning line 23 as an
unchecked box. If either moves: DO NOT COMMIT. Revert the file, re-run both commands to confirm the
revert, and record the incident as a new `.planning/WINDOWS.md` entry in the style of entry 54.
`phase.complete` has flipped that box once already, as a side effect, while its own warnings said it
was skipping the write.
  </action>

  <verify>
    <automated>awk 'NR>1 && /^---$/{exit} NR>1' .planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-VERIFICATION.md | grep -c '^overrides_applied: 1'</automated>
  </verify>

  <acceptance_criteria>
    - The command in `<verify>` prints `1`. HEAD value: `overrides_applied: 0`, so this criterion prints `0` on an unmodified tree.
    - `awk 'NR>1 && /^---$/{exit} NR>1' <file> | grep -c '^overrides:'` prints `1` — the frontmatter block exists exactly once. HEAD value: **0** (frontmatter), while a bare `grep -c "^overrides:"` over the whole file prints **1** at HEAD from the body template and **2** after; use the frontmatter-scoped form.
    - `awk 'NR>1 && /^---$/{exit} NR>1' <file> | grep -cE '^ +accepted_at: "20'` prints `1` — a real ISO timestamp, not a placeholder. HEAD value: **0**.
    - `awk 'NR>1 && /^---$/{exit} NR>1' <file> | grep -cE '^ +accepted_by: ".*2026-08-28'` prints `1` — the maintainer's answer is named with its date. HEAD value: **0**.
    - `awk 'NR>1 && /^---$/{exit} NR>1' <file> | grep -o "SC1" | wc -l` prints `1` or more — the override names the must-have it covers. HEAD value: **0**.
    - THE VERIFIER'S FINDINGS SURVIVE: `grep -c '^status: gaps_found' <file>` prints `1` and `grep -c '^score: 5/6 must-haves verified' <file>` prints `1`. HEAD values: **1** and **1** — both unchanged.
    - THE BODY TEMPLATE IS UNTOUCHED: `grep -o 'ISO timestamp' <file> | wc -l` prints `1` — the single remaining occurrence is the fenced template's placeholder in the body, never the frontmatter. HEAD value: **1**.
    - PRIV-05 GATE, BOTH RUNS: `shasum -a 256 .planning/REQUIREMENTS.md` prints `9b3219662ec0d007c1c82d64eed3ef2698bd306ce69f01205ac9bbc3f42fcfb4` and `sed -n '23p' .planning/REQUIREMENTS.md` still begins `- [ ] **PRIV-05**`. `git diff --stat -- .planning/REQUIREMENTS.md` prints nothing.
    - FULL SUITE: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --console=plain` reports 0 failures and 0 errors with 1 skipped. HEAD baseline before plan 28-07: 1298 tests / 180 classes; expect roughly 1307 after 28-07. If `RedactionTest > windowedScanRedactsJsonPairAcrossEveryBoundaryAlignment()` is the only failure, re-run it alone and record both outcomes — known wall-clock flake, fail-closed, not a regression.
  </acceptance_criteria>

  <done>The SC1 override sits in `28-VERIFICATION.md`'s frontmatter with the maintainer's 2026-08-28 answer named and a real timestamp, `overrides_applied` reads 1, every one of its six conditions has its raw output recorded, the verifier's own findings are byte-unchanged, and PRIV-05 is still an unchecked box.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Security register -> milestone audit / future phase | `26-SECURITY.md` rows are consumed as evidence of what is and is not controlled. A row that claims more than its controls deliver makes the next reader stop looking. |
| Carrier registry -> future contributor | `CookieCarrierInventoryTest`'s constants are the enumeration a contributor consults before adding a cookie-touching call site. |
| Verification record -> `phase.complete` | An override in this frontmatter unblocks phase completion. Applied before its conditions are met, it converts a conditional acceptance into an unconditional one with no trace of the difference. |
| Requirements file -> release gate | A ticked PRIV-05 would let a release claim a defect closed. `phase.complete` has flipped this box once already as a side effect. |
| Executor -> a 266 KB file with a 16 KB row | Reading it whole terminates the run mid-task. Measured, not hypothetical. |

## STRIDE Threat Register

ASVS level 1; blocking threshold `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-42 | Repudiation | `26-SECURITY.md` row 315 prior text | high | mitigate | Append-and-amend at a RE-DERIVED unique anchor; two prefix digests (8693 and 16071 bytes) recorded before the edit and re-asserted after, each paired with its byte count; file line count pinned at 1799; `git diff` removed-line count pinned at 1. |
| T-28-43 | Tampering | The stale anchor literal supplied in the brief | high | mitigate | Measured at HEAD as 0 occurrences; the task's first step is re-derivation with a uniqueness check, and both the derived and the stale anchor are recorded in the SUMMARY rather than silently swapped. |
| T-28-44 | Repudiation | The override's conditionality | high | mitigate | Six machine-checked preconditions, each with a HEAD value of 0 so none can pass on an unmodified tree, plus a HALT rule that writes nothing when any fails. The task is last in the last plan. |
| T-28-45 | Tampering | The verifier's own `status` / `score` / `gaps` | high | mitigate | Explicit prohibition plus positive criteria pinning `status` and `score` to their HEAD values; the body's fenced template block is identified in `<read_first>` and pinned by the remaining-placeholder count. |
| T-28-46 | Tampering | `.planning/REQUIREMENTS.md` | high | mitigate | Not in `files_modified`. sha256 plus line-23 grep run twice under distinct headings with raw output, plus `git diff --stat` proving no change, plus a documented revert-and-record procedure if it moves. |
| T-28-47 | Denial of Service | Executor context vs `26-SECURITY.md` (266 KB; one 16,191-byte row, one ~10,400-byte row) | high | mitigate | `<read_first>` mandates four bounded `LC_ALL=C awk` extractions and two narrow `sed` ranges; a full read is forbidden in the task text. This has already killed an executor in this phase. |
| T-28-48 | Information Disclosure | The write-time/read-time bound and the route-2 fail-open set | medium | accept | Both accepted as NAMED residuals — D-28-09 by human maintainer answer 2026-08-28, the fail-open set by plan 28-07's stated four-reason decision. Named in both records by this plan, pinned by tests in 28-07, and explicitly excluded from any closure claim. |
| T-28-49 | Spoofing | The absent repository-wide detail-producer gate | medium | accept | `D-28-06` records it CONSIDERED AND NOT TAKEN and `WR-01` stays open. Both records restate that after this round a third producer would be caught by nothing, rather than implying the gap away. |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs nothing and modifies no dependency file. No Package Legitimacy Gate applies. |
</threat_model>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Where |
|---|---|---|
| Third supersession block on `ISSUE_DETAIL_CARRIER_DISPOSITION` | test constant | `redact/CookieCarrierInventoryTest.kt`, appended before the concatenation's end |
| Dated amendment to the class KDoc's marker count | test comment | idem, appended before the KDoc's closing delimiter |
| `AMENDED … by plan 28-08 (phase 28)` marker amending clause (d) | register text | `26-SECURITY.md` line 315, inserted before the column separator |
| Recomputed `threats_open` with raw awk output | record | `26-SECURITY.md` front matter (value unchanged) + `28-08-SUMMARY.md` |
| Frontmatter `overrides:` block for SC1 + `overrides_applied: 1` | record | `28-VERIFICATION.md` frontmatter |
| The six override preconditions with raw output | record | `28-08-SUMMARY.md`, under a heading naming them as the override gate |
| Two pre-edit and two post-edit prefix digests | record | `28-08-SUMMARY.md` |
| PRIV-05 gate RUN 1 and RUN 2 raw outputs | record | `28-08-SUMMARY.md`, two distinct headings |
| `28-08-SUMMARY.md` | record | phase 28 directory |

NOT produced, and named so nobody looks for it: any control, any runtime behaviour change, any code
fix for the write-time/read-time bound (D-28-09 accepted it as a residual precisely because that is
new architecture belonging to its own phase), any widening of the route-2 predicate, a
repository-wide detail-producer gate (`WR-01` / D-28-06 keep it a named residual), a reopened
`AR-28-01`, and a ticked PRIV-05.
</artifacts_this_phase_produces>

<verification>
1. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --console=plain` — 0 failures, 0 errors, 1 skipped. Background the run: the suite takes about 19 minutes and will exceed a 600s foreground cap.
2. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt --console=plain` — both exit 0; `detekt-baseline.xml` byte-unchanged.
3. Do NOT run `./gradlew check` as a gate — RED at HEAD for a maintainer-accepted coverage-floor reason.
4. Both row-315 prefix digests re-asserted after every edit, raw output recorded beside the pre-edit pair: `LC_ALL=C awk 'NR==315{printf "%s", substr($0,1,8693)}' … | shasum -a 256` -> `8dc326ac…`, and the 16071-byte prefix -> `5316a971…`.
5. `wc -l < .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` prints `1799`, and `grep -c "^threats_open: 0"` prints `1` with the documented awk's raw output and row count recorded.
6. The six override preconditions, each with its raw output, recorded in the SUMMARY under a heading naming them as the gate — including a record of any that failed and the resulting halt.
7. PRIV-05 GATE, RUN 1 (in-task) and RUN 2 (post-completion, immediately before the phase commit): `shasum -a 256 .planning/REQUIREMENTS.md` and `grep -n 'PRIV-05' .planning/REQUIREMENTS.md`, raw outputs under two distinct headings. Digest must still begin `9b3219662ec0d007`; line 23 must still be an unchecked box. If either moved: DO NOT COMMIT — revert, re-run both to confirm, and record the incident as a new `.planning/WINDOWS.md` entry in the style of entry 54.
8. Record the re-derived row-315 anchor and the stale one from the brief side by side, so the next round inherits the measurement rather than the paste.
</verification>

<success_criteria>
- The write-time/read-time bound is named, with that exact literal, in `ISSUE_DETAIL_CARRIER_DISPOSITION`'s
  STILL OPEN clause and in `26-SECURITY.md` row 315 — both appended under dated markers with all
  prior text byte-exact and both measured prefix digests unmoved.
- Both records name D-28-09's acceptance as a human maintainer answer on 2026-08-28 and map each
  D-28-10 condition to the artifact that discharged it.
- Both records state the two claims round 3 had to repair — the probe claim for detail line (4) and
  the unrecorded route-2 fail-open set — as prose that ran ahead of the tree, with the plan that
  repaired each and the asymmetry left unsoftened.
- `AR-27-08` stays OPEN, `threats_open` is recomputed with raw output, and the counter's population
  rule is restated so an open `AR-` row beneath a 0 is not read as a contradiction.
- The SC1 override is applied last, after six machine-checked conditions, with the maintainer's
  answer named and a real timestamp; `overrides_applied` reads 1.
- The verifier's own `status`, `score` and `gaps` are byte-unchanged, and the body's fenced template
  block is untouched.
- `.planning/REQUIREMENTS.md` is byte-unchanged and PRIV-05 remains `- [ ]`.
- No artifact written this round claims a control was added, a leak was closed, or a repository-wide
  producer gate exists.
</success_criteria>

<output>
Create `.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-08-SUMMARY.md` when done.
</output>
