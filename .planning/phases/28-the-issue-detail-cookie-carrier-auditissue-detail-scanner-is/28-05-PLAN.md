---
phase: 28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is
plan: 05
type: execute
wave: 2
depends_on: ["28-04"]
gap_closure: true
autonomous: true
requirements: [PRIV-05]
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheckDetailCookieCarrierTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/scanner/CookieRouteDispositionTest.kt

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC1 route 2 — under STRICT and under BALANCED, `AiScanCheck.buildDetail` emits no byte of a `PARAM_COOKIE` insertion point's `baseValue()`, measured through the full `IssueDetails` serialize -> `Redaction.apply` -> `detail` field path. This is the FIRST measurement of the route-2 shape through `Redaction.apply` (`28-VERIFICATION.md` `missing[6]`)."
    - "SC2 for route 2 — under OFF the `baseValue()` still appears, so route 2's control is policy-driven. Before this plan the file read no privacy mode at all and behaved identically in all three modes."
    - "SC3 for route 2 — the red probe is the WRONG-PREDICATE probe: substituting the shared string-name predicate for the enum identity compare leaves the class GREEN on presence and RED on the named absence assertion, and both the named assertion and its verbatim message are recorded. This is the round's single highest-risk item and it is measured, not assumed."
    - "THE TYPE-KEYING CHOICE IS STATED, NOT ASSUMED — route 2 keys on `insertionPoint.type() == AuditInsertionPointType.PARAM_COOKIE`, an identity compare against a member of Burp's closed `AuditInsertionPointType` enum. Option (b), widening `Redaction`'s shared predicate, was considered and NOT taken; the reason is recorded in source."
    - "ONE MARKER VOCABULARY ACROSS ROUTES — `AiScanCheck` writes `ScannerIssueSupport.INJECTION_VALUE_STRIPPED_MARKER` by reference. No second marker constant and no second marker string literal exists in `AiScanCheck.kt`."
    - "THE OLD PREDICATE POPULATION IS UNMOVED — `CookieRouteDispositionTest.exactlyOneCookieTypePredicateExistsInMainSource` still finds exactly ONE hit and still finds it in `redact/Redaction.kt`. The new spelling class is a DIFFERENT population with its own named tripwire, not a widening of that one."
    - "THE NEW SPELLING CLASS IS COVERED — a committed tripwire counts `AuditInsertionPointType.PARAM_COOKIE` identity compares in main source, asserts exactly one, asserts it lives in `scanner/AiScanCheck.kt`, and carries a known-positive fixture so a regex that matches nothing cannot report a clean tree for free."
    - "[edge:encoding] Whose definition of type equality applies is answered and MEASURED: the shared predicate compares `typeName.trim().uppercase() == \"COOKIE\"` and returns FALSE for the Montoya insertion-point constant, whose name is `PARAM_COOKIE`. A committed assertion pins that measurement so a future reader cannot re-derive it wrongly by inspection."
    - "[edge:empty] A `PARAM_COOKIE` insertion point whose `baseValue()` is the empty string still renders the stripped marker; and an insertion point whose `type()` is absent does not throw — it falls to the pass-through branch."
    - "THE `ONLY PRODUCER` CLAIM IS CORRECTED (D-28-08, second site) — `ScannerIssueSupport.kt:74-80` no longer asserts it is the only active-scan detail-line producer in the repository, and no longer asserts a single-producer gate that does not exist."
  prohibitions:
    - "MUST NOT record a closure claim wider than the control this plan ships. After this plan there are TWO controlled producers and STILL no repository-wide enforcement; the correction written into `ScannerIssueSupport.kt` must say exactly that."
    - "MUST NOT claim, imply, or quietly satisfy the repository-wide single-producer gate. D-28-06 records it as CONSIDERED AND NOT TAKEN. The new predicate tripwire this plan builds is a DIFFERENT mechanism over a DIFFERENT population and must not be presented as closing `WR-01`."
    - "MUST NOT remove the operator's own raw copy of their traffic from the same issue's local Burp evidence pane while claiming a privacy win — `AiScanCheck` passes `listOf(baseRequestResponse, markedAttack)` to `AuditIssue.auditIssue` and that list stays byte-unchanged by this control."
  artifacts:
    - "src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt — gains a policy lookup, `isCookieInsertionPoint`, `sanitizeCookiePointText`; `buildDetail` widened to `internal`"
    - "src/test/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheckDetailCookieCarrierTest.kt — NEW; the first test in this repository that fakes a Montoya `AuditInsertionPoint`"
    - "src/test/kotlin/com/six2dez/burp/aiagent/scanner/CookieRouteDispositionTest.kt — gains the insertion-point predicate tripwire and its fixture; `relativePath` made cross-platform"
    - "src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt — the `ONLY PRODUCER` KDoc corrected append-and-amend"
  key_links:
    - "`AiScanCheck.buildDetail` -> `RedactionPolicy.fromMode(settings.privacyMode)` — derived from the `val settings = getSettings()` ALREADY on the function's first line (`AiScanCheck.kt:327`); no parameter is threaded down from `doCheck`"
    - "`AiScanCheck` -> `ScannerIssueSupport.INJECTION_VALUE_STRIPPED_MARKER` / `ORIGINAL_VALUE_MAX_CHARS` / `PAYLOAD_VALUE_MAX_CHARS` — referenced across files, never retyped"
    - "`AiScanCheck.buildDetail` -> `AuditIssue.auditIssue(...)` at `AiScanCheck.kt:250` -> `api.siteMap()` -> `McpToolExecutorImpl.kt:602-618` (`scanner_issues`) — the same tool result route 1 reaches"
    - "route-2 test -> `IssueDetails` / `toolJson.encodeToString` / `Redaction.apply` — the serialization tail borrowed from `IssueDetailCookieCarrierTest`, which is the only existing way to measure this shape through the redactor"
---

<objective>
Close **route 2 of `AR-27-08`** (`CR-02`): `AiScanCheck.buildDetail` is a SECOND active-scan
`AuditIssue` detail producer, live-registered `PER_INSERTION_POINT` at `App.kt:214-215`, that writes
`**Original Value:** ${insertionPoint.baseValue().take(100)}` (`AiScanCheck.kt:353`) with no privacy
control of any kind. `grep -n 'Redaction\|PrivacyMode\|RedactionPolicy\|sanitize\|privacyMode'` over
that file returns NOTHING — it behaves identically in STRICT, BALANCED and OFF, and `doCheck`
applies no insertion-point TYPE filter, so `PARAM_COOKIE` points reach line 353.

Purpose: `AR-27-08` is defined by the ROADMAP as the cookie-value -> `scanner_issues` ->
`AuditIssue.detail()` carrier. `AiScanCheck` IS that carrier. Closing route 1 alone and writing the
register as closed is the overclaim vocabulary phase 27 exists to correct (D-28-05).

Output: a type-keyed control on the second producer, the repository's first `AuditInsertionPoint`
test fixture, a tripwire over the new predicate spelling class, and the corrected `ONLY PRODUCER`
claim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-CONTEXT.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-PATTERNS.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-04-SUMMARY.md
@src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt
@src/test/kotlin/com/six2dez/burp/aiagent/scanner/CookieRouteDispositionTest.kt
</context>

<measured_facts>
Measured at source and re-verified before this plan was written. Do not re-derive; do not contradict
without a new recorded measurement.

1. **Nothing needs threading.** `buildDetail` ALREADY calls `val settings = getSettings()` on its
   first line (`AiScanCheck.kt:327`). The policy is one expression away —
   `RedactionPolicy.fromMode(settings.privacyMode)` — plus one import. A design that threads a
   `RedactionPolicy` parameter down from `doCheck` is over-building; do not write it.
2. **THE TYPE-KEYING DOES NOT TRANSFER LITERALLY BETWEEN ROUTES.** `ScannerIssueSupport` keys on the
   project's own `InjectionType.COOKIE`. `AiScanCheck` holds a Montoya `AuditInsertionPoint`, whose
   `type()` returns `AuditInsertionPointType`, and whose cookie constant is named **`PARAM_COOKIE`**
   — confirmed by `javap` against `montoya-api-2026.2.jar`; the full constant list is `PARAM_URL,
   PARAM_BODY, PARAM_COOKIE, PARAM_XML, PARAM_XML_ATTR, PARAM_MULTIPART_ATTR, PARAM_JSON, PARAM_AMF,
   HEADER, PARAM_NAME_URL, PARAM_NAME_BODY, ENTIRE_BODY, URL_PATH_FILENAME, URL_PATH_FOLDER,
   USER_PROVIDED, EXTENSION_PROVIDED, UNKNOWN`. `Redaction.isCookieParameterType` compares
   `typeName.trim().uppercase(Locale.ROOT) == "COOKIE"` (`Redaction.kt:622`, constant at `:630`), so
   it returns **FALSE** for `PARAM_COOKIE`. Reusing it here ships a control that never fires.
3. **`AuditInsertionPoint` is a plain interface** — `javap` gives `name()`, `baseValue()`,
   `buildHttpRequestWithPayload(ByteArray)`, `issueHighlights(ByteArray)` and a `default type()`.
   Mockito can stub all four, including the default method.
4. **No test analog exists.** `grep -rc 'AuditInsertionPoint' src/test/kotlin/` returns ZERO files.
   Nothing calls `AuditIssue.auditIssue(...)` or `AuditResult.auditResult(...)` successfully — both
   route through `ObjectFactoryLocator.FACTORY`, which is null outside Burp;
   `AiPassiveScanCheckTest.kt:38-45` CATCHES the resulting `NullPointerException` rather than faking
   anything. Consequences this plan adopts: the route-2 test CANNOT go through `createIssue`
   (`AiScanCheck.kt:249-260`); `buildDetail` must widen from `private` to `internal`.
5. **`org.mockito.kotlin:mockito-kotlin:5.4.0` is ALREADY a `testImplementation` dependency**
   (`build.gradle.kts:60`). Do NOT add a dependency. No Package Legitimacy Gate applies to this plan.
6. **Route 2's payload line is NOT a measured carrier today.** `AiScanCheck` sources payloads from
   `payloadGenerator.getQuickPayloads(vulnClass, maxRisk)` (`AiScanCheck.kt:64-67`), and
   `getQuickPayloads` returns entries from the static table with no interpolation of any value
   (`PayloadGenerator.kt:633-639`). Unlike route 1, `:357` does not carry operator traffic at HEAD.
   D-28-05 names `:357` and it IS controlled here — but as DEFENCE IN DEPTH and vocabulary parity,
   and the record must say that rather than claim a measured leak closure.
7. **`.baseValue()` has exactly TWO call sites in main source**, both in `AiScanCheck.kt`: `:114`
   (inside `determineVulnClasses`, used only as a regex subject to pick vuln classes — NON-CARRYING)
   and `:353` (the carrier). This measurement is consumed by plan 28-06.
8. **`.take(100)` at `:353` and `.take(500)` at `:357` equal `ScannerIssueSupport.ORIGINAL_VALUE_MAX_CHARS`
   and `PAYLOAD_VALUE_MAX_CHARS`.** Routing through the named constants is value-preserving. If
   detekt's `MagicNumber` baseline entries for this file become unused, leave `detekt-baseline.xml`
   alone — QUAL-07 forbids GROWING it and unused entries are not an error.
9. `CookieRouteDispositionTest.COOKIE_TYPE_COMPARISONS` matches only STRING comparisons against
   `"COOKIE"` or against `COOKIE_PARAMETER_TYPE_NAME`. An enum identity compare against
   `AuditInsertionPointType.PARAM_COOKIE` matches NONE of the four spellings, so
   `exactlyOneCookieTypePredicateExistsInMainSource` stays green at 1 hit in `Redaction.kt`. That is
   exactly why the new spelling class needs its OWN tripwire — the file's class KDoc states the rule.
10. `build.gradle.kts:285-288` already declares `inputs.dir("src/main/kotlin")` on `tasks.test`. Any
    source-scanning test added here is already covered; do NOT add a per-file `inputs.file`.
11. `git stash` is PROHIBITED in this repository's worktrees. Red probes revert with
    `git checkout HEAD -- <path>`.
</measured_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Control `AiScanCheck.buildDetail` — enum-identity type key, live policy, shared marker</name>

  <precondition>`ScannerIssueSupport.INJECTION_VALUE_STRIPPED_MARKER`, `ORIGINAL_VALUE_MAX_CHARS` and `PAYLOAD_VALUE_MAX_CHARS` are `internal` members of the same Gradle module as `AiScanCheck` (created by plan 28-01, present at HEAD) — verify by reading `ScannerIssueSupport.kt` before writing the reference.</precondition>

  <reversibility rating="costly">Choosing an enum identity compare over widening `Redaction`'s shared predicate establishes a SECOND cookie-type predicate population in main source, with its own tripwire, its own owner and its own KDoc. Undoing it touches the predicate, the new tripwire test, and the amended class KDoc of an existing test. Not a migration and not a broken contract, so not one-way — but the reader should see the weight.</reversibility>

  <read_first>
    - `src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt` lines 1-30 (imports and the constructor's `getSettings` collaborator) and lines 320-370 (`buildDetail` in full).
    - `src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt` lines 6-71 — the three constants and the sibling gate whose discipline this task mirrors across a different enum.
    - `src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt` lines 1237-1246 — the policy-acquisition analog (`RedactionPolicy.fromMode(getSettings().privacyMode)` at the write site).
    - `.planning/phases/28-.../28-PATTERNS.md` section "Route 2 — `AiScanCheck.kt` (`buildDetail`)", including the boxed `TYPE-KEYING DOES NOT TRANSFER LITERALLY` measurement.
  </read_first>

  <files>src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt</files>

  <behavior>
    Written as failing tests in Task 2 before this control is made green. Expectations:
    - A `PARAM_COOKIE` insertion point under STRICT: `buildDetail` renders the shared stripped marker
      in place of `baseValue()`, and the sentinel is absent from the string.
    - Same under BALANCED.
    - Under OFF: `baseValue()` is present verbatim.
    - A `PARAM_URL` insertion point under STRICT: `baseValue()` is present verbatim (attribution).
    - An insertion point whose `type()` is absent: `buildDetail` returns normally and takes the
      pass-through branch. No exception.
  </behavior>

  <action>
Add two imports to `AiScanCheck.kt`: `burp.api.montoya.scanner.audit.insertionpoint.AuditInsertionPointType` and `com.six2dez.burp.aiagent.redact.RedactionPolicy` — the second is the exact import `ActiveAiScanner.kt:14` already carries.

STATE THE TYPE-KEYING CHOICE IN SOURCE, because assuming it is how this control ships dead. Add `internal fun isCookieInsertionPoint(insertionPoint: AuditInsertionPoint): Boolean` whose whole body is an identity comparison of `insertionPoint.type()` against `AuditInsertionPointType.PARAM_COOKIE`. Give it a KDoc that records, in its own words, the MEASUREMENT behind the choice: the shared predicate in `Redaction` compares an uppercased, trimmed parameter-type NAME against the literal `COOKIE`, and the Montoya insertion-point constant's name is `PARAM_COOKIE`, so that predicate returns false here and reusing it would ship a control that never fires. Name the alternative that was NOT taken — widening the shared predicate — and why: widening moves `CookieRouteDispositionTest.exactlyOneCookieTypePredicateExistsInMainSource`, which counts a DIFFERENT population (`HttpParameterType` name comparisons), and merging two Montoya enums under one predicate makes the predicate's own contract ambiguous. Keep D-28-07's discipline sentence applicable: the decision is taken on a member of a CLOSED enum, never on a rendered string.
<!-- planner-discipline-allow: isCookieParameterType -->

Add `internal fun sanitizeCookiePointText(insertionPoint: AuditInsertionPoint, policy: RedactionPolicy, raw: String, maxChars: Int): String` as the single gate both detail lines call. Shape it as a two-arm `when` mirroring `ScannerIssueSupport.sanitizeInjectionPointValue`: the first arm fires on `policy.stripCookies && isCookieInsertionPoint(insertionPoint)` and returns `ScannerIssueSupport.INJECTION_VALUE_STRIPPED_MARKER` by reference; the `else` arm returns `raw.take(maxChars)`, the pre-existing truncation byte-for-byte. Do not declare a marker constant in this file and do not retype the marker's text — D-28-05's one-vocabulary rule makes a second literal here the named failure mode.

In `buildDetail`, immediately after the existing `val settings = getSettings()` at `:327`, derive `val policy = RedactionPolicy.fromMode(settings.privacyMode)`. Do not add a second `getSettings()` call and do not add a parameter.

Route BOTH detail lines through the gate. `**Original Value:**` renders `sanitizeCookiePointText(insertionPoint, policy, insertionPoint.baseValue(), ScannerIssueSupport.ORIGINAL_VALUE_MAX_CHARS)`. The `Payload Used:` fenced block renders `sanitizeCookiePointText(insertionPoint, policy, payload.value, ScannerIssueSupport.PAYLOAD_VALUE_MAX_CHARS)`. Both replace bare numeric truncation bounds with the named constants — a value-preserving change measured in fact 8.

RECORD THE ASYMMETRY HONESTLY in the `buildDetail` KDoc rather than letting a reader infer symmetry with route 1: the `**Original Value:**` line IS a measured carrier — `baseValue()` on a `PARAM_COOKIE` point is the operator's raw cookie value — while the payload line at `:357` is NOT a carrier at HEAD, because this class sources payloads from the static quick-payload table (`AiScanCheck.kt:64-67` -> `PayloadGenerator.kt:633-639`) and never interpolates `baseValue()`. State that the payload line is controlled as defence in depth and for vocabulary parity with route 1, and that calling it a measured leak closure would be the overclaim this phase exists to correct.

Widen `buildDetail` from `private` to `internal`, and record why in one line of its KDoc: `AuditIssue.auditIssue(...)` routes through Burp's `ObjectFactoryLocator.FACTORY`, which is null outside the Burp runtime, so no unit test can reach this function through `createIssue`; `internal` is the same visibility `ScannerIssueSupport`'s controlled functions already use.

Leave the `listOf(baseRequestResponse, markedAttack)` argument to `AuditIssue.auditIssue` untouched. The operator keeps the raw attack request in their own Burp pane; that is the accepted trade route 1 already records, and altering it here would be a different change with a different justification.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew compileKotlin --console=plain</automated>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt --console=plain</automated>
  </verify>

  <acceptance_criteria>
    - The file now reads a privacy mode, which it did not before: `grep -cE 'RedactionPolicy|privacyMode' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt` returns at least 3 (import, policy derivation, gate parameter). At HEAD before this task it returns 0.
    - Exactly one policy lookup: comment lines stripped, `grep -vE '^[[:space:]]*(//|\*|/\*)' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt | grep -c 'RedactionPolicy.fromMode('` returns exactly 1, and that line's argument text is `settings.privacyMode`.
    - Exactly one `getSettings()` call inside `buildDetail`: `awk '/internal fun buildDetail/,/^    private fun mapConfidence/' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt | grep -c 'getSettings()'` returns 1.
    - The enum identity compare exists exactly once: `grep -vE '^[[:space:]]*(//|\*|/\*)' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt | grep -c 'AuditInsertionPointType.PARAM_COOKIE'` returns 1.
    - The shared string-name predicate is NOT reused here: `grep -vE '^[[:space:]]*(//|\*|/\*)' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt | grep -c 'isCookieParameterType'` returns 0.
    - No second marker literal: `grep -c -F '"[STRIPPED]"' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt` returns 0, and `grep -c 'INJECTION_VALUE_STRIPPED_MARKER' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt` returns at least 1.
    - Bare truncation literals are gone from the detail block: `awk '/internal fun buildDetail/,/^    private fun mapConfidence/' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt | grep -cE 'take\(100\)|take\(500\)'` returns 0.
    - `buildDetail` is `internal`: `grep -c 'internal fun buildDetail' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt` returns 1.
    - `compileKotlin`, `ktlintCheck` and `detekt` all exit 0; `git diff --stat -- detekt-baseline.xml` prints nothing.
  </acceptance_criteria>

  <done>
    `AiScanCheck.buildDetail` reads the operator's live privacy mode, keys on the closed Montoya
    insertion-point enum, writes the one shared marker, and states in source both the choice it made
    and the asymmetry between its two controlled lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: The route-2 test — the repository's first `AuditInsertionPoint` fixture, measured through `Redaction.apply`</name>

  <precondition>`buildDetail` is `internal` (task 1) and `org.mockito.kotlin:mockito-kotlin:5.4.0` resolves — confirm with `grep -n 'mockito-kotlin' build.gradle.kts` returning line 60. Add no dependency.</precondition>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/scanner/AiPassiveScanCheckTest.kt` lines 28-49 — the only test in the repository that drives a Burp `ScanCheck`, and the constraint it documents (the Burp object factory is null outside Burp).
    - `src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt` lines 461-482 (the sentinel-distinctness guard), 520-620 (`RAW_REQUEST`, `issueDetailsFor`, `redactedBlobFor`) and 645-712 (`detailFieldOf`, `requestFieldOf`, `fallbackStringField`) — the serialization tail this test reuses and the extractor-pin discipline that makes it non-vacuous.
    - `src/test/kotlin/com/six2dez/burp/aiagent/config/AgentSettingsMigrationTest.kt` lines 203-207 — the deep-stub `MontoyaApi` idiom.
    - `.planning/phases/28-.../28-PATTERNS.md` section "Test — route 2 (`AiScanCheck`)" and the "No Analog Found" table.
  </read_first>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheckDetailCookieCarrierTest.kt</files>

  <action>
Create `AiScanCheckDetailCookieCarrierTest`. It is the first test in this repository to fake a Montoya `AuditInsertionPoint`; write a class KDoc that says so, states WHY the obvious end-to-end shape is unavailable (the Burp object factory is null outside the Burp runtime, so `createIssue` cannot be driven), and states what the file therefore does and does NOT prove.

Fixture. Build the insertion point with `mock<AuditInsertionPoint>()` and stub `name()`, `baseValue()` and `type()`. Use sentinels in the established style of the sibling file: a low-entropy hyphenated word value with no digits, no `=` and no metacharacters, DISTINCT from `IssueDetailCookieCarrierTest`'s sentinels so a cross-class collision cannot make an absence assertion pass for the wrong reason, and not containing the token `cookie` so no header-name rule can bind to it. Build the check with `AiScanCheck(mock<MontoyaApi>(defaultAnswer = Answers.RETURNS_DEEP_STUBS)) { settingsFor(mode) }`, where `settingsFor` returns `AgentSettingsRepository(api).defaultSettings().copy(privacyMode = mode, preferredBackendId = "test-backend")`. If the repository constructor proves unusable against a deep stub, fall back to a locally-constructed `AgentSettings` in the style of `AiPassiveScanCheckTest.testSettings()` — do not weaken the assertions to route around it.

FIXTURE PIN, BEFORE ANY BEHAVIOURAL ASSERTION. Assert the mock actually returns `AuditInsertionPointType.PARAM_COOKIE` from `type()` and the sentinel from `baseValue()`. A mock whose stub silently did not take would make every absence assertion below pass while measuring nothing — the class of vacuity `IssueDetailCookieCarrierTest`'s extractor pins exist to prevent.

Behavioural tests, one named `@Test` per mode in the sibling file's convention rather than a `@ParameterizedTest`, so SC3 can name a single designated red-probe assertion. Every message states the invariant, the mode and the observed value:
- `cookieBaseValueIsStrippedUnderStrict` — DESIGNATE THIS as the red-probe assertion.
- `cookieBaseValueIsStrippedUnderBalanced`.
- `cookieBaseValueSurvivesUnderOff` — SC2 for route 2, and the proof the control is policy-driven where before this round the file read no mode at all.
- `urlParamInsertionPointSurvivesStrict_attributionControl` — a `PARAM_URL`-typed point with the same `baseValue()` keeps it verbatim under STRICT.
- `anEmptyBaseValueStillRendersTheMarkerUnderStrict` — [edge:empty].
- `anAbsentInsertionPointTypeDoesNotThrowAndPassesThrough` — [edge:empty], the null-`type()` arm.
- `theSharedStringNamePredicateDoesNotRecogniseTheInsertionPointCookieConstant` — [edge:encoding]. Assert directly that `Redaction.isCookieParameterType(AuditInsertionPointType.PARAM_COOKIE.name)` is FALSE, with a message explaining that this is WHY route 2 needed its own identity compare. This pins measured fact 2 so a future reader cannot re-derive it wrongly by inspection.

Measurement through the redactor — this is `28-VERIFICATION.md` `missing[6]` and the reason this file exists rather than a string-only assertion. Reuse the sibling file's serialization tail unchanged in shape: wrap the `buildDetail` output as the `detail` field of an `IssueDetails`, include a `requestResponses` entry carrying a real `Cookie:` header as the POSITIVE CONTROL (a value unrelated to the detail sentinel), encode with `toolJson`, pass through `Redaction.apply(blob, RedactionPolicy.fromMode(mode), <fixed salt>)`, and extract the `detail` field. Add:
- `theCookieHeaderPositiveControlFiresInTheSameStrictOutput` — the pre-existing header rule rewrites the `Cookie:` header in the SAME STRICT output in which the detail assertion is made, so a null result inside `detail` is attributable to the new control rather than to a dead pipeline.
- `theSentinelIsNotTheTailOfTheSerializedBlob_nonVacuity` — measured in OFF, the only mode in which the sentinel is present by design.

Clear `Redaction.setCustomPatterns(emptyList())` in `@BeforeEach`: `Redaction` is a singleton object and patterns left by another class in the same JVM could remove a sentinel for the wrong reason. Precedent is the sibling file's own `@BeforeEach`.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.scanner.AiScanCheckDetailCookieCarrierTest' --console=plain</automated>
  </verify>

  <acceptance_criteria>
    - The file exists and all nine named tests from the action exist by exact name and pass; 0 failures, 0 errors.
    - It is the first `AuditInsertionPoint` fixture in the repository: `grep -rl 'AuditInsertionPoint' src/test/kotlin/ | wc -l` returns 1 and that path is this file.
    - No new dependency: `git diff --stat -- build.gradle.kts` prints nothing.
    - The route-2 shape is measured through the redactor, not asserted on a bare string: `grep -c 'Redaction.apply' src/test/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheckDetailCookieCarrierTest.kt` returns at least 1.
    - Sentinel isolation: the sentinel constants in this file do not appear in `IssueDetailCookieCarrierTest.kt` — run `grep -o` for each sentinel literal against the sibling file and get 0 hits.
    - THE WRONG-PREDICATE RED PROBE (SC3, and the round's highest-risk item), recorded in the SUMMARY with a NAMED assertion and its VERBATIM failure message: in the working tree, replace the enum identity compare in `AiScanCheck.isCookieInsertionPoint` with the shared string-name predicate applied to the insertion point's type name, run this class, and record which assertion goes red and what it says. EXPECT `cookieBaseValueIsStrippedUnderStrict` red with the sentinel PRESENT — the control compiles, reads as correct, and does not fire. Revert with `git checkout HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt` (never `git stash`) and confirm `git status --porcelain src/` prints nothing.
    - THE CONTROL-REVERT RED PROBE: restore the untreated `baseValue()` render, run, record the named assertion and its verbatim message, revert, confirm clean.
  </acceptance_criteria>

  <done>
    Route 2 is measured, not inferred: a `PARAM_COOKIE` insertion point's `baseValue()` is absent
    from the redacted `detail` field under STRICT and BALANCED, present under OFF, with a firing
    positive control in the same output — and the wrong-predicate probe proves on the record that
    reusing the shared string-name predicate would have shipped a control that never fires.
  </done>
</task>

<task type="auto">
  <name>Task 3: A tripwire for the new predicate population, and the `ONLY PRODUCER` correction</name>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/scanner/CookieRouteDispositionTest.kt` lines 18-56 (the class KDoc, including the numbered "OUTSIDE that population BY DESIGN" list), 58-108 (the two existing scan tests), 274-300 (`cookieTypeComparisonsIn`, `mainSourceFiles`, `relativePath`, `mainSourceRoot`) and 300-356 (the companion, `COOKIE_TYPE_COMPARISONS` and `SPELLING_FIXTURES`).
    - `src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt` lines 72-104 — the `ONLY PRODUCER` KDoc block.
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` lines 596-620 — the append-and-amend KDoc supersession style to copy.
  </read_first>

  <files>
    src/test/kotlin/com/six2dez/burp/aiagent/scanner/CookieRouteDispositionTest.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt
  </files>

  <action>
`CookieRouteDispositionTest`'s class KDoc states its own extension rule: anyone adding a new spelling class must add it to the enumerated population, or the tripwire silently stops covering their code. Task 1 added a cookie-type predicate in a spelling that population cannot see. Discharge the rule WITHOUT widening the existing count.

Amend the class KDoc's numbered "OUTSIDE that population BY DESIGN" list from two items to three. The new item names the `AuditInsertionPointType.PARAM_COOKIE` identity compare in `scanner/AiScanCheck.kt` and states the measured reason it is a different population: the enumerated spellings compare a Montoya PARAMETER type NAME string against the literal `COOKIE`, whereas the insertion-point constant is a member of a different closed enum whose name that predicate cannot produce. Say explicitly that this is not an exemption — the new population gets its own tripwire below.

Add `exactlyOneInsertionPointCookieTypePredicateExistsInMainSource`, built from the same parts as its sibling so a reader meets one idiom: the same `mainSourceFiles()` walk with the same `MIN_EXPECTED_MAIN_FILES` floor asserted FIRST (a walk that reaches nothing reports a clean tree for the same reason a correct tree does), the same comment-line stripping by line prefix, a new companion pattern list for the identity-compare spelling, and assertions that the hit count is exactly 1 and that the single hit lives in `com/six2dez/burp/aiagent/scanner/AiScanCheck.kt`. Write the failure message in the sibling's register: a SECOND insertion-point cookie predicate is how this control gets bypassed without anyone editing the first, and the fix is to route the new call site through the existing one.

Add `everyInsertionPointSpellingHasAKnownPositive` alongside it, mirroring `everyCookieTypeComparisonSpellingHasAKnownPositive`: one known-positive fixture per pattern, plus the size-equality assertion, so a regex that matches nothing cannot report a clean tree for free.

Assert the OLD population is UNMOVED as part of this task's evidence, not merely as a side effect: `exactlyOneCookieTypePredicateExistsInMainSource` must still find exactly one hit and must still find it in `redact/Redaction.kt`.

Fix `WR-05` while this file is open — it is a real defect against a stated project constraint (Windows is a supported platform) and the rule for `already_known` items is that they are fixed when a plan already touches the file. `relativePath` currently mixes a forward-slash root constant with the platform separator, so on Windows the needle never occurs, the whole absolute path is returned, and the owner assertion fails with a confusing message. Reimplement it as a relativisation against `mainSourceRoot()` followed by an invariant-separator rendering — the sibling `CookieCarrierInventoryTest.kt:238` already does this correctly; copy that. Leave a one-line comment naming `WR-05` so the fix is attributable.

Then correct the SECOND D-28-08 prose site: `ScannerIssueSupport.kt:72-104` asserts it is "THE ONLY PRODUCER OF THE ACTIVE-SCAN ISSUE DETAIL LINES IN THE REPOSITORY" and that a single-producer gate would catch a second one. Both are false and `WR-01` measured the second. Amend append-and-amend in the `Redaction.kt:600-620` style: keep the existing paragraphs verbatim as the historical record, then append a dated supersession marker naming phase 28 plan 28-05 that states three things plainly. (a) There are TWO producers of these lines; the second is `AiScanCheck.buildDetail`, and it is now controlled by that class's own type-keyed gate against a DIFFERENT closed enum. (b) The single-producer gate referred to above DOES NOT EXIST: the assertion it points at filters the list this function itself returned and is structurally incapable of seeing another file. (c) No repository-wide enforcement is added by this round — D-28-06 records that as CONSIDERED AND NOT TAKEN, and after this round there are two CONTROLLED producers and still no gate that would catch a third. Do not soften (c) into an implication that the gate exists.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.scanner.CookieRouteDispositionTest' --tests 'com.six2dez.burp.aiagent.scanner.AiScanCheckDetailCookieCarrierTest' --tests 'com.six2dez.burp.aiagent.scanner.IssueDetailCookieCarrierTest' --console=plain</automated>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt --console=plain</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'exactlyOneInsertionPointCookieTypePredicateExistsInMainSource' src/test/kotlin/com/six2dez/burp/aiagent/scanner/CookieRouteDispositionTest.kt` returns 1, and that test passes.
    - `grep -c 'everyInsertionPointSpellingHasAKnownPositive' src/test/kotlin/com/six2dez/burp/aiagent/scanner/CookieRouteDispositionTest.kt` returns 1, and that test passes.
    - `exactlyOneCookieTypePredicateExistsInMainSource` still passes and still reports its single hit in `redact/Redaction.kt` — assert by running it, not by reading it.
    - NON-VACUITY, recorded in the SUMMARY: add a second identity-compare occurrence to a scratch main-source location, run `exactlyOneInsertionPointCookieTypePredicateExistsInMainSource`, record the NAMED assertion and its VERBATIM failure message showing a hit count of 2 with both paths listed, then revert with `git checkout HEAD -- <path>` and confirm `git status --porcelain src/` prints nothing.
    - WR-05 is fixed: `grep -c 'File.separator' src/test/kotlin/com/six2dez/burp/aiagent/scanner/CookieRouteDispositionTest.kt` returns 0, and `grep -c 'invariantSeparatorsPath' ...` returns at least 1. All five pre-existing tests in the class still pass.
    - The `ONLY PRODUCER` correction is append-and-amend, not a rewrite: the block's prior text survives byte-identically at the front of the block. Prove it — before editing, record `LC_ALL=C awk 'NR>=72 && NR<=104' src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt | shasum -a 256`; after editing, the same byte range of the ORIGINAL text must still appear verbatim as a contiguous prefix of the amended block. Paste both the pre-edit digest and the post-edit verification into the SUMMARY.
    - The correction states the residual: `grep -c 'D-28-06' src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt` returns at least 1.
    - All three named test classes pass together; `ktlintCheck` and `detekt` exit 0; `detekt-baseline.xml` byte-unchanged.
  </acceptance_criteria>

  <done>
    The new predicate spelling class has its own committed, non-vacuous tripwire; the old population
    is proven unmoved; the Windows defect in the file is fixed; and the false single-producer claim
    is corrected without deleting the record of what was believed and without claiming a gate that
    does not exist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Burp-derived insertion point -> `AiScanCheck.buildDetail` | `AuditInsertionPoint.baseValue()` on a `PARAM_COOKIE` point is the operator's raw session cookie, derived by Burp from proxied traffic. |
| `AuditIssue.detail()` -> `scanner_issues` MCP tool result | `AiScanCheck.kt:250` -> `api.siteMap()` -> `McpToolExecutorImpl.kt:602-618`. The same tool result route 1 reaches, by a producer no phase-28 artifact previously named. |
| Type predicate -> control activation | A predicate that compiles, reads as correct, and returns false for the real constant is indistinguishable from a working control by inspection. |
| Plan record -> future reader | The `ONLY PRODUCER` KDoc is consumed as an invariant by later readers; it was false when written. |

## STRIDE Threat Register

ASVS level 1; blocking threshold `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-22 | Information Disclosure | `AiScanCheck.kt:353` (`**Original Value:**`) | high | mitigate | `sanitizeCookiePointText` gated on `policy.stripCookies && isCookieInsertionPoint(...)`, policy derived from the live `getSettings()` already on `buildDetail`'s first line. Held by `cookieBaseValueIsStrippedUnderStrict` / `…UnderBalanced`, measured through `Redaction.apply`. |
| T-28-23 | Spoofing | The cookie-type predicate itself | high | mitigate | Enum identity compare against `AuditInsertionPointType.PARAM_COOKIE`. Reuse of `Redaction`'s string-name predicate is measured FALSE for that constant and is banned by an acceptance-criteria grep; the wrong-predicate red probe proves on the record that reuse ships a control that never fires. |
| T-28-24 | Tampering | The new predicate spelling class | high | mitigate | `exactlyOneInsertionPointCookieTypePredicateExistsInMainSource` plus a known-positive fixture; non-vacuity proven by a recorded second-occurrence probe. Discharges `CookieRouteDispositionTest`'s own stated extension rule instead of silently rotting it. |
| T-28-25 | Information Disclosure | `AiScanCheck.kt:357` (payload line) | medium | mitigate | Controlled by the same gate — but as DEFENCE IN DEPTH, recorded as such. Measured: this class sources payloads from the static quick-payload table and does not interpolate `baseValue()`, so the line is not a carrier at HEAD. Claiming otherwise would be the overclaim this phase corrects. |
| T-28-26 | Elevation of Privilege | `buildDetail` visibility widened `private` -> `internal` | low | accept | `internal` is module-scoped in Kotlin and the shipped artifact is a single fat JAR; it is the same visibility `ScannerIssueSupport`'s controlled functions already use. No external surface is created. |
| T-28-27 | Repudiation | The `ONLY PRODUCER` KDoc correction | high | mitigate | Append-and-amend under a dated supersession marker with a pre-edit byte-range digest recorded; prior text kept verbatim so a reader can see what was believed. States the D-28-06 residual explicitly rather than implying a gate exists. |
| T-28-28 | Denial of Service | `AiScanCheck.doCheck` unchanged behaviour | low | accept | This plan changes only rendering inside `buildDetail`. Rate limiting, scope checks, the `activeAiEnabled` opt-in and the per-point payload cap are untouched. |
| T-28-SC | Tampering | package-manager installs | n/a | accept | This plan installs NOTHING. `mockito-kotlin:5.4.0` is already declared at `build.gradle.kts:60`; `git diff --stat -- build.gradle.kts` printing nothing is an acceptance criterion. No Package Legitimacy Gate applies. |
</threat_model>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Where |
|---|---|---|
| `AiScanCheck.isCookieInsertionPoint(insertionPoint)` | `internal fun` | `scanner/AiScanCheck.kt` — the enum identity compare; the single insertion-point cookie predicate in main source |
| `AiScanCheck.sanitizeCookiePointText(insertionPoint, policy, raw, maxChars)` | `internal fun` | idem — the one gate both detail lines call |
| `AiScanCheck.buildDetail(...)` | visibility change `private` -> `internal` | idem |
| `AiScanCheckDetailCookieCarrierTest` | NEW test class | `src/test/kotlin/.../scanner/` — the repository's first `AuditInsertionPoint` fixture |
| `cookieBaseValueIsStrippedUnderStrict` | `@Test` | idem — SC3 designated red-probe assertion for route 2 |
| `cookieBaseValueIsStrippedUnderBalanced` | `@Test` | idem |
| `cookieBaseValueSurvivesUnderOff` | `@Test` | idem — SC2 for route 2 |
| `urlParamInsertionPointSurvivesStrict_attributionControl` | `@Test` | idem |
| `anEmptyBaseValueStillRendersTheMarkerUnderStrict` | `@Test` | idem — [edge:empty] |
| `anAbsentInsertionPointTypeDoesNotThrowAndPassesThrough` | `@Test` | idem — [edge:empty] |
| `theSharedStringNamePredicateDoesNotRecogniseTheInsertionPointCookieConstant` | `@Test` | idem — [edge:encoding]; pins measured fact 2 |
| `theCookieHeaderPositiveControlFiresInTheSameStrictOutput` | `@Test` | idem |
| `theSentinelIsNotTheTailOfTheSerializedBlob_nonVacuity` | `@Test` | idem |
| `exactlyOneInsertionPointCookieTypePredicateExistsInMainSource` | `@Test` | `scanner/CookieRouteDispositionTest.kt` |
| `everyInsertionPointSpellingHasAKnownPositive` | `@Test` | idem |
| `28-05-SUMMARY.md` | record | phase 28 directory |

REUSED, not created: `ScannerIssueSupport.INJECTION_VALUE_STRIPPED_MARKER`, `ORIGINAL_VALUE_MAX_CHARS`,
`PAYLOAD_VALUE_MAX_CHARS`, `RedactionPolicy.fromMode`, `Redaction.apply`, `Redaction.setCustomPatterns`,
`IssueDetails`, `toolJson`, `AgentSettingsRepository.defaultSettings`.
</artifacts_this_phase_produces>

<verification>
1. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.scanner.AiScanCheckDetailCookieCarrierTest' --tests 'com.six2dez.burp.aiagent.scanner.CookieRouteDispositionTest' --tests 'com.six2dez.burp.aiagent.scanner.IssueDetailCookieCarrierTest' --tests 'com.six2dez.burp.aiagent.scanner.EvidenceTailReachTest' --tests 'com.six2dez.burp.aiagent.scanner.AiPassiveScanCheckTest'` — 0 failures, 0 errors.
2. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt` — both exit 0; `detekt-baseline.xml` byte-unchanged.
3. `git diff --stat -- src/main/kotlin/com/six2dez/burp/aiagent/scanner/AdaptivePayloadEngine.kt` prints nothing (SC4 must not regress).
4. `git diff --stat -- build.gradle.kts .planning/REQUIREMENTS.md` prints nothing.
5. Do NOT run `./gradlew check` as a gate — RED at HEAD for a maintainer-accepted coverage-floor reason.
6. `RedactionTest > windowedScanRedactsJsonPairAcrossEveryBoundaryAlignment()` is a known wall-clock flake; not a regression.
</verification>

<success_criteria>
- A `PARAM_COOKIE` insertion point's `baseValue()` is absent from the redacted `detail` field of an
  `AiScanCheck`-produced issue under STRICT and BALANCED, and present under OFF.
- The type-keying choice is stated in source with its measurement, and the wrong-predicate red probe
  proves on the record what reusing the shared predicate would have shipped.
- The old predicate population is unmoved; the new one has its own non-vacuous tripwire.
- The `ONLY PRODUCER` claim is corrected append-and-amend and names the D-28-06 residual.
- `WR-05` is fixed.
- Nothing in this plan claims a repository-wide producer gate exists or has been built.
</success_criteria>

<output>
Create `.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-05-SUMMARY.md` when done.
</output>
