---
phase: 28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [PRIV-05]
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC1 route 1 — under STRICT and under BALANCED, the `Payload Used:` line of the active-scan issue detail carries no byte of a COOKIE-typed injection point's `originalValue`, measured through the full serialize -> `Redaction.apply` -> `detail` field path, with a payload built the way production builds it."
    - "SC2 preserved for route 1 — under OFF the rendered payload survives verbatim, so the payload control is proven policy-driven and not an unconditional rewrite."
    - "SC3 for route 1 — a red probe reverting the payload control turns a NAMED assertion red and its VERBATIM `org.opentest4j.AssertionFailedError` message is recorded in the SUMMARY; `the suite went red` is not an acceptable record."
    - "The fixture can SEE the route it names — `PAYLOAD.value` is derived at fixture-construction time from `PayloadGenerator.generateContextAwarePayloads(VulnClass.SQLI, DETAIL_SENTINEL, 5)` and a guard asserts the derived value CONTAINS `DETAIL_SENTINEL`, so a generator that stops interpolating turns the fixture red instead of silently un-covering the route."
    - "ONE MARKER VOCABULARY — the payload control writes `ScannerIssueSupport.INJECTION_VALUE_STRIPPED_MARKER`; no second marker constant and no second marker string literal is introduced in main source."
    - "TYPE-KEYED, NEVER SHAPE-KEYED (D-28-07) — the payload gate reads `point.type` and `policy.stripCookies` only. It performs no search, no comparison and no substitution over the TEXT of `point.originalValue` inside `payload.value`."
    - "[edge:adjacency] The detail sentinel and the rendered payload stand in a deliberate CONTAINMENT relation (payload contains sentinel) while remaining non-overlapping with the positive-control cookie value; the fixture guard asserts BOTH relations, so neither an accidental equality nor an accidental overlap can make an absence assertion pass for the wrong reason."
    - "[edge:empty] A COOKIE-typed point whose `originalValue` is the EMPTY STRING still renders the stripped marker on both the `Original Value:` and `Payload Used:` lines under STRICT and BALANCED — the gate is keyed on type and never on the value being non-empty, so no `isNotEmpty()` guard is admitted."
    - "[edge:encoding] Whose definition of equality applies is answered as: NONE. A COOKIE point whose value appears in the payload only in TRANSFORMED form (URL-encoded) is still stripped under STRICT, because the gate never reads the value's text. Asserted by a named test whose payload contains the encoded form and not the raw sentinel."
    - "[edge:ordering] The detail LINE ORDER is unchanged by this round: the STRICT blob equals the OFF blob with exactly the enumerated substitutions applied, and the prediction helper applies the payload-line substitution BEFORE the original-value one so a global sentinel replace cannot corrupt the prediction."
    - statement: "The truncation bounds count UTF-16 code units, not code points: a pass-through value whose Nth boundary falls inside a surrogate pair renders a split fragment. Pre-existing behaviour of `take(N)`, unchanged by this round and not newly introduced by the payload control."
      verification: backstop
  prohibitions:
    - "MUST NOT record, in source comment or SUMMARY, a closure claim wider than the control this plan actually ships. This plan controls ONE of THREE measured detail-line routes; the other two are closed by plans 28-05 and 28-06 and are not this plan's to claim."
    - "MUST NOT claim, imply, or quietly satisfy a repository-wide single-producer gate. `WR-01` / D-28-06 records that gate as a NAMED RESIDUAL that was considered and NOT taken; a helper whose assertion filters the list one producer returned must say so in its own failure message."
    - "MUST NOT remove the operator's own raw copy of their traffic from the same issue's local Burp evidence pane while claiming a privacy win — `requestResponses` stays byte-unchanged by this control."
  artifacts:
    - "src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt — gains `sanitizeRenderedPayload`; `PAYLOAD_VALUE_MAX_CHARS` KDoc corrected"
    - "src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt — production-derived payload fixture, `payloadRenderedFor` helper, and the named per-mode payload tests"
  key_links:
    - "`buildActiveIssueDetailLines` line 121 -> `sanitizeRenderedPayload(point, payload, policy)` — the same `policy` argument line 120 already holds; no new parameter, no new policy lookup"
    - "`sanitizeRenderedPayload` -> `INJECTION_VALUE_STRIPPED_MARKER` — the constant already in the file, referenced not retyped"
    - "`IssueDetailCookieCarrierTest.PAYLOAD` -> `PayloadGenerator.generateContextAwarePayloads` — the fixture is derived from production, not hand-typed"
---

<objective>
Close **route 1 of `AR-27-08`** (`CR-01`): `ScannerIssueSupport.kt:121` writes
`Payload Used: ${payload.value.take(PAYLOAD_VALUE_MAX_CHARS)}` with no policy argument, one line
below the line 120 that plan 28-01 controlled. For a COOKIE-typed point the payload is DERIVED FROM
the cookie value (`PayloadGenerator.kt:782` builds `"$originalValue' AND '1'='1"`), so line 121
re-leaks the exact bytes line 120 just stripped — measured by the verifier with a temporary fixture
mutation whose verbatim failure text is quoted in `28-VERIFICATION.md`.

Purpose: this is the phase's SC1, and it is the criterion the phase goal sentence is about. The
mechanism plan 28-01 built is sound; what failed is its SPAN.

Output: a type-keyed payload control in the same file as its sibling, a fixture that can SEE the
route it names, and a recorded red probe.

**This plan is the phase's TRACER.** It drives ONE COOKIE-typed injection point through every layer
this gap round touches — write site -> `buildActiveIssueDetailLines` -> `IssueUtils.formatIssueDetailHtml`
-> `toolJson.encodeToString(IssueDetails)` -> `Redaction.apply` -> the extracted `detail` field —
end to end, with a real runnable verification, before plan 28-05 expands the same discipline to the
second producer. It is production code written for keeps, not a prototype: there are no stubs in it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-CONTEXT.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-PATTERNS.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-VERIFICATION.md
@src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt
@src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt
</context>

<measured_facts>
Do not re-derive these. They were measured at source and re-verified independently before this plan
was written. Contradicting one requires a new measurement, recorded.

1. `ScannerIssueSupport.buildActiveIssueDetailLines` already receives `policy: RedactionPolicy` as
   its sixth parameter and already uses it at line 120. The payload branch needs **no new
   parameter** and **no new `RedactionPolicy.fromMode(` call anywhere**.
2. `theWriteSiteReadsTheLivePolicy` (`IssueDetailCookieCarrierTest.kt:262-285`) pins the count of
   executable `RedactionPolicy.fromMode(` occurrences in `ActiveAiScanner.kt` at exactly 1. This
   plan adds none to that file, so the pin stays green. Do not widen it. (`WR-04` proposes widening
   it to a property assertion; that is NOT this plan's scope and the pin is non-vacuous today.)
3. `PayloadGenerator` has a no-arg constructor and `generateContextAwarePayloads` is a pure function
   of its arguments — it touches no `ScanKnowledgeBase` and no singleton. It is safe to call from a
   mocks-free test fixture initializer.
4. `generateContextAwarePayloads(VulnClass.SQLI, <non-numeric value>, 5)` returns the STRING-context
   arm: the first element is `"<value>' AND '1'='1"` with `DetectionMethod.BLIND_BOOLEAN` and
   `PayloadRisk.SAFE` (`PayloadGenerator.kt:778-790`). Note the detection method DIFFERS from the
   current hand-typed fixture's `DetectionMethod.REFLECTION`; the `Detection Method:` detail line
   changes text accordingly and that is expected, not a regression.
5. `IssueUtils.formatIssueDetailHtml` escapes only `&`, `<` and `>` (`IssueUtils.kt:50-62`).
   Apostrophes and `=` pass through unescaped, and JSON encoding does not escape them either, so a
   `' AND '1'='1` payload survives into the serialized blob verbatim and is directly greppable.
6. `build.gradle.kts:285-288` already declares `inputs.dir("src/main/kotlin")` on `tasks.test`
   (`mainSourceTreeStructuralInputs`). Any source-scanning test in this round is already covered —
   do NOT add a per-file `inputs.file` declaration.
7. `git stash` is PROHIBITED in this repository's worktrees (`refs/stash` is shared across linked
   worktrees). Red probes revert with `git checkout HEAD -- <path>`.
</measured_facts>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — a COOKIE point's payload-derived value dies at the write site and is absent from the redacted `detail` field</name>

  <precondition>JDK 21 resolves: `/usr/libexec/java_home -v 21` exits 0 and prints a path. Gradle 8.12.1 fails under the default JDK 25.</precondition>

  <reversibility rating="reversible">The whole-payload strip is one `when` branch in one internal function; undoing it is reverting that branch. The cost it incurs — cookie-point payload diagnostics are given up, deliberately, per D-28-07 — is a product trade the maintainer already locked, not a migration.</reversibility>

  <read_first>
    - `src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt` — the WHOLE file (161 lines). Lines 26-34 are the KDoc this task corrects; 36-71 are `sanitizeInjectionPointValue`, the exact analog to copy; 105-131 are `buildActiveIssueDetailLines`.
    - `src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt` — lines 107-200 (the per-mode test convention), 461-482 (the sentinel-distinctness guard), 520-645 (the companion fixtures and helpers).
    - `.planning/phases/28-.../28-PATTERNS.md` — section "Route 1 — `ScannerIssueSupport.kt`" for the verbatim gate shape and the KDoc discipline sentence, and section "Test — route 1 red probe" for the fixture and named-assertion conventions.
    - `src/main/kotlin/com/six2dez/burp/aiagent/scanner/PayloadGenerator.kt` lines 646-660 and 750-800 — `generateContextAwarePayloads` and the string-context SQLI arm.
  </read_first>

  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt
  </files>

  <behavior>
    Write these expectations as failing tests FIRST, against the un-modified `ScannerIssueSupport`,
    and record that they are red before making them green:
    - `cookiePayloadIsStrippedUnderStrict` — the redacted `detail` field for a COOKIE point under
      STRICT contains neither `DETAIL_SENTINEL` nor the rendered payload text, and DOES contain the
      payload prefix followed by the shared stripped marker.
    - `cookiePayloadIsStrippedUnderBalanced` — same under BALANCED.
    - `cookiePayloadSurvivesUnderOff` — under OFF the rendered payload is present verbatim,
      including its `DETAIL_SENTINEL` substring.
    - `urlParamPayloadSurvivesStrict_attributionControl` — a `URL_PARAM`-typed point with the same
      value and the same payload keeps BOTH lines verbatim under STRICT, so a green cookie result is
      attributable to the TYPE gate and not to some unrelated rule eating the sentinel.
    - `thePayloadStrippedMarkerIsNotTruncated` — the payload line's rendered value equals the shared
      marker EXACTLY, not a prefix of it.
    - `aCookiePayloadCarryingOnlyAnEncodedFormOfTheValueIsStillStripped` — [edge:encoding] a payload
      whose text contains a percent-encoded transformation of the sentinel and NOT the raw sentinel
      is still replaced with the marker under STRICT. Proves the gate reads the TYPE, not the text.
    - `anEmptyValuedCookiePointStillRendersTheMarkerOnBothLines` — [edge:empty] a COOKIE point with
      an empty `originalValue` renders the marker on both lines under STRICT rather than an empty
      value or a bare prefix.
  </behavior>

  <action>
Add `internal fun sanitizeRenderedPayload(point: InjectionPoint, payload: Payload, policy: RedactionPolicy): String` to `ScannerIssueSupport`, placed immediately AFTER `sanitizeInjectionPointValue` so the two gates read as siblings. Implement it as a `when` with exactly two arms, mirroring its sibling's shape: the first arm fires on `policy.stripCookies && point.type == InjectionType.COOKIE` and returns `INJECTION_VALUE_STRIPPED_MARKER`; the `else` arm returns `payload.value.take(PAYLOAD_VALUE_MAX_CHARS)`, the pre-existing behaviour byte-for-byte.

D-28-07 binds the treatment and the plan states it explicitly: this is a TYPE-KEYED WHOLE-PAYLOAD strip. The gate must not read, search, compare or substitute over the text of `point.originalValue` inside `payload.value` — the substring-excision alternative sketched in `28-REVIEW.md` CR-01 was CONSIDERED AND REJECTED by D-28-07 because it contradicts the discipline this file's own KDoc states and because any payload that transforms or encodes the value defeats it. Do not add an emptiness guard on `point.originalValue` either: the CR-01 sketch carries one and it would make the control skip an empty-valued cookie point, which must still render the marker.

Reuse `INJECTION_VALUE_STRIPPED_MARKER`. Do NOT declare a second marker constant and do NOT retype the marker's string literal — the CR-01 sketch proposes a separate payload-specific constant and that is rejected under D-28-05's one-vocabulary rule.

Give the new function a KDoc that (a) names `PRIV-05`, `28-04`, `CR-01` and `D-28-07`, (b) quotes the existing discipline sentence from `sanitizeInjectionPointValue`'s KDoc verbatim rather than paraphrasing it — the sentence beginning "TYPE-KEYED, never shape-keyed", (c) states the ACCEPTED TRADE in the same register the sibling uses: cookie-point payload diagnostics are given up deliberately, and the operator retains the raw attack request in the same issue's `requestResponses` pane, and (d) names the reason the payload is operator traffic: `ActiveAiScanner.kt:511-515` passes `target.injectionPoint.originalValue` into `PayloadGenerator.generateContextAwarePayloads`, which interpolates it at `PayloadGenerator.kt:762`, `:771`, `:782` and `:791` with no injection-type filter.

Change `buildActiveIssueDetailLines` line 121 so the payload line renders `sanitizeRenderedPayload(point, payload, policy)`. The `policy` value is already in scope from the sixth parameter — add no parameter and add no policy lookup.

Correct the `PAYLOAD_VALUE_MAX_CHARS` KDoc (currently `ScannerIssueSupport.kt:26-34`), which is the FALSE PREMISE that caused this line to be skipped (D-28-08, first site). Follow the append-and-amend correction style of `Redaction.kt:600-620`: keep the existing paragraph verbatim as the historical record, then append a dated supersession marker naming phase 28 plan 28-04 that states the measured truth — the payload is not unconditionally agent-authored; for a context-aware payload it is built by interpolating operator traffic, and the constant is now the pass-through branch's bound in a function that IS part of the privacy control. Do not delete the prior text; a reader must be able to see what was believed and why it was wrong.

In `IssueDetailCookieCarrierTest`, replace the hand-typed `PAYLOAD` companion value with one DERIVED at fixture-construction time from `PayloadGenerator().generateContextAwarePayloads(VulnClass.SQLI, DETAIL_SENTINEL, 5)`, taking the first element. Keep the fixture a `val` in the companion. This is `28-VERIFICATION.md` `missing[3]`.

Extend `assertSentinelsAreDistinctAndNonOverlapping` (`:461`) with the containment half required by [edge:adjacency]: assert the derived payload value CONTAINS `DETAIL_SENTINEL` — with a message stating that a `PayloadGenerator` that stops interpolating the original value would otherwise leave every payload assertion in this class vacuously green — while keeping the existing assertions that `DETAIL_SENTINEL` and `POSITIVE_CONTROL_COOKIE_VALUE` are distinct and neither contains the other.

Add a `payloadRenderedFor(point, mode)` helper twinning `originalValueRenderedFor` (`:621-637`), reading the payload line from `detailLinesFor(point, mode)` by its rendered prefix. Its `assertEquals(1, matching.size)` message must state its ACTUAL reach honestly: it filters the list `buildActiveIssueDetailLines` itself returned and is structurally incapable of seeing another file. Do not copy the existing helper's repository-wide framing — `WR-01` measured that framing as false and D-28-06 keeps the repo-wide gate as a NAMED RESIDUAL that this round does not build.

Add the seven named tests listed in `<behavior>`, one `@Test` per mode in this file's established convention rather than a `@ParameterizedTest`, so SC3 can name a single designated red-probe assertion. Every assertion carries a message naming the invariant, the mode and the observed value, matching the self-describing failure text this file already produces.

FINALLY, EXPECT A RED AND DO NOT CHASE IT. Two pre-existing tests —
`theStrippedDetailFieldRetainsEverythingAfterTheControlPoint` and
`theOnlyTwoDifferencesBetweenStrictAndOffAreTheEnumeratedControls` — go red the moment this control
ships, for a SPECIFICATION reason on correct code: each predicts the STRICT output by an unqualified
whole-string substitution written when one sentinel occurrence lived inside `detail`, and a
whole-payload strip writes the marker ALONE where the prediction expects marker-plus-suffix. Task 2
of this plan repairs the prediction, and it is Task 2 that carries the ordering reasoning the repair
depends on. Do NOT repair them here, and do NOT soften `sanitizeRenderedPayload` or any new
assertion to fit the old prediction — that is the shape `28-REVIEW.md` WR-04 names as the one a
future round relaxes rather than fixes. This task's `<verify>` is scoped accordingly: the first
command runs only what must be green now, the second asserts those two are still red.
<!-- planner-discipline-allow: theOnlyTwoDifferencesBetweenStrictAndOffAreTheEnumeratedControls -->
<!-- planner-discipline-allow: thirdDifferenceMessage -->
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*IssueDetailCookieCarrierTest.cookie*' --tests '*IssueDetailCookieCarrierTest.urlParam*' --tests '*IssueDetailCookieCarrierTest.sentinelsAreDistinctAndNonOverlapping' --tests '*IssueDetailCookieCarrierTest.thePayloadStrippedMarkerIsNotTruncated' --tests '*IssueDetailCookieCarrierTest.aCookiePayloadCarryingOnlyAnEncodedFormOfTheValueIsStillStripped' --tests '*IssueDetailCookieCarrierTest.anEmptyValuedCookiePointStillRendersTheMarkerOnBothLines' --console=plain</automated>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*IssueDetailCookieCarrierTest.theStrippedDetailFieldRetainsEverythingAfterTheControlPoint' --tests '*IssueDetailCookieCarrierTest.theOnlyTwoDifferencesBetweenStrictAndOffAreTheEnumeratedControls' --console=plain; if [ $? -eq 0 ]; then echo 'UNEXPECTED GREEN — the two difference guards should still predict the OLD output at this boundary; see acceptance criteria before touching anything'; exit 1; else echo 'EXPECTED RED at the task-1 boundary — Task 2 repairs the prediction'; fi</automated>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt --console=plain</automated>
  </verify>

  <acceptance_criteria>
    - THE TASK-1 BOUNDARY IS DELIBERATELY NOT ALL-GREEN. Read this before reacting to any red.
      Exactly TWO tests are EXPECTED RED here and are NOT to be repaired in this task:
      `theStrippedDetailFieldRetainsEverythingAfterTheControlPoint` and
      `theOnlyTwoDifferencesBetweenStrictAndOffAreTheEnumeratedControls`. Both predict the STRICT
      output by an unqualified whole-string substitution written when exactly one sentinel occurrence
      lived inside `detail`; the whole-payload strip falsifies the PREDICTION, not the control. Task 2
      owns the repair and owns the ordering reasoning that makes the repair correct. Weakening either
      new assertion, or weakening `sanitizeRenderedPayload`, to make these two green here is the exact
      failure this plan exists to prevent. Record both names and their verbatim
      `org.opentest4j.AssertionFailedError` messages in the SUMMARY as the task-1 boundary state.
    - The first `<verify>` command is scoped to the twelve tests that MUST be green here — the seven
      new ones plus the five pre-existing route-1 guards (`cookie*` covers the three route-1
      original-value tests and the three new payload tests; `urlParam*` covers both attribution
      controls; plus `sentinelsAreDistinctAndNonOverlapping`). It exits 0. A whole-class run is NOT
      this task's gate; Task 2 owns it, and Task 2's whole-class run must report 0 failures.
    - `grep -c 'sanitizeRenderedPayload' src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt` returns at least 2 (declaration + call site).
    - Line-121 call site: `grep -E 'Payload Used: \$\{sanitizeRenderedPayload\(point, payload, policy\)\}' src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt` matches exactly one line.
    - The gate is type-keyed: `grep -E 'policy\.stripCookies && point\.type == InjectionType\.COOKIE' src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt | wc -l` returns 2 (one per sibling gate).
    - SHAPE-KEYING IS ABSENT, asserted positively and region-scoped to the new function so the rest of the file is unaffected: extract the region with `awk '/internal fun sanitizeRenderedPayload/,/^$/'` over `ScannerIssueSupport.kt`, strip comment lines with `grep -vE '^[[:space:]]*(//|\*|/\*)'`, then `grep -c 'point\.'` returns exactly **1** — the single `point.type` read in the gate. Any text-level treatment of the value would raise that count.
    - No second policy lookup introduced: `grep -vE '^[[:space:]]*(//|\*|/\*)' src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt | grep -c 'RedactionPolicy.fromMode('` still returns exactly 1, and `theWriteSiteReadsTheLivePolicy` passes unchanged.
    - The fixture is production-derived: `grep -c 'generateContextAwarePayloads' src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt` returns at least 1, and `grep -c 'benign-probe-payload' src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt` returns 0.
    - All seven named tests from `<behavior>` exist by exact name and pass. The class now declares 21 `@Test` methods (14 at HEAD, measured, plus these 7). At THIS task's boundary 19 pass and the two named at the top of these criteria are red by design; Task 2 takes the class to 21 passing.
    - `ktlintCheck` and `detekt` both exit 0. `detekt-baseline.xml` is byte-unchanged: `git diff --stat -- detekt-baseline.xml` prints nothing (QUAL-07 forbids growing it).
    - `.planning/REQUIREMENTS.md` byte-unchanged: `shasum -a 256 .planning/REQUIREMENTS.md` still begins `9b3219662ec0d007`.
  </acceptance_criteria>

  <done>
    A COOKIE-typed injection point's `originalValue` is absent from the redacted `detail` field under
    STRICT and BALANCED on BOTH the `Original Value:` and `Payload Used:` lines, with a payload built
    the way production builds it; it survives on both lines under OFF; a `URL_PARAM` point is
    untouched in every mode; and the false KDoc premise that skipped this line is corrected without
    deleting the record of what was believed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repair the difference-enumeration guards the whole-payload strip invalidates</name>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt` lines 341-410 (`theStrippedDetailFieldRetainsEverythingAfterTheControlPoint` and `theOnlyTwoDifferencesBetweenStrictAndOffAreTheEnumeratedControls`) and lines 714-760 (`windowAround`, `contentDestructionMessage`, `thirdDifferenceMessage`).
    - The result of Task 1 in the same file — the derived `PAYLOAD` value and `payloadRenderedFor`.
  </read_first>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt</files>

  <action>
Both existing difference guards were written when exactly ONE occurrence of the sentinel lived inside `detail`, and both predict the STRICT output by an UNQUALIFIED whole-string substitution of `DETAIL_SENTINEL` for the marker. Under a whole-payload strip that prediction is wrong by construction: it predicts a payload line reading marker-plus-suffix, while the shipped control writes the marker alone. Left unrepaired, these two guards go red for a specification reason on correct code — the shape `28-REVIEW.md` WR-04 identifies as the one a future round relaxes rather than fixes.

Repair `theStrippedDetailFieldRetainsEverythingAfterTheControlPoint` so its `expected` value is built from the OFF `detail` by TWO PREFIX-QUALIFIED substitutions instead of one unqualified one: substitute the rendered payload occurrence (its line prefix followed by the OFF-mode rendered payload text) with the same prefix followed by the marker, THEN replace the original-value occurrence (its prefix followed by the sentinel) with that prefix followed by the marker. [edge:ordering] The payload substitution MUST run first: the payload text CONTAINS the sentinel, so an original-value-first order mutates the payload text mid-flight and the prediction becomes unfalsifiable. Write that ordering constraint into a comment on the helper stating WHY, not merely THAT.

Keep both existing extractor pins ahead of the comparison (the OFF `detail` is non-blank; the OFF `detail` contains the sentinel) and add a third: the OFF `detail` contains the rendered payload text. An extractor that silently returned a shorter field would otherwise make the new substitution a no-op and the equality vacuous.

Rename `theOnlyTwoDifferencesBetweenStrictAndOffAreTheEnumeratedControls` to `theOnlyThreeDifferencesBetweenStrictAndOffAreTheEnumeratedControls` and add DIFFERENCE 3 as a first-class named clause in the same register as the two that exist: inside `detail`, the OFF blob carries the rendered payload verbatim and the STRICT blob carries the marker in its place and does not carry the rendered payload. Update the class KDoc sentence that states the count. Extend the whole-blob `predicted` computation with the same two prefix-qualified substitutions in the same order, then the pre-existing header-rule substitution. Rename `thirdDifferenceMessage` to a count-free name such as `unenumeratedDifferenceMessage` so the helper does not have to be renamed again the next time a difference is enumerated, and update its text accordingly.

Update the class KDoc's own description of what this file covers so it names the payload line alongside the original-value line. Do not write any sentence asserting that the repository has one detail-line producer — after this plan there are still two, and plan 28-05 owns the second.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.scanner.IssueDetailCookieCarrierTest' --console=plain</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'theOnlyThreeDifferencesBetweenStrictAndOffAreTheEnumeratedControls' src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt` returns 1 and `grep -c 'theOnlyTwoDifferencesBetweenStrictAndOffAreTheEnumeratedControls' ...` returns 0.
    - `grep -c 'DIFFERENCE 3' src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt` returns at least 1.
    - `grep -c 'thirdDifferenceMessage' src/test/kotlin/com/six2dez/burp/aiagent/scanner/IssueDetailCookieCarrierTest.kt` returns 0.
    - Both repaired tests PASS. THIS TASK OWNS THE WHOLE-CLASS RUN that Task 1's scoped gate deferred: `--tests 'com.six2dez.burp.aiagent.scanner.IssueDetailCookieCarrierTest'` reports 21 tests, 0 failures, 0 errors. The two tests that were red by design at the task-1 boundary are green here, repaired by a corrected prediction and not by a weakened control — state which in the SUMMARY, and quote the new `expected` construction.
    - ORDERING PROOF, recorded in the SUMMARY, not merely asserted: swap the two substitutions into the wrong order in the working tree, run the class, and record the NAMED assertion that goes red plus its VERBATIM failure message. Restore with `git checkout HEAD -- <path>` (never `git stash`) and confirm `git status --porcelain src/` prints nothing.
    - `ktlintCheck` exits 0.
  </acceptance_criteria>

  <done>
    The content-preservation guard and the difference enumeration both predict the STRICT output
    correctly under a whole-payload strip, the enumeration names three differences, and the ordering
    hazard is proven by a recorded red probe rather than asserted in a comment.
  </done>
</task>

<task type="auto">
  <name>Task 3: SC3 — the red probe for the payload control, with a NAMED assertion and its verbatim failure message</name>

  <read_first>
    - `.planning/phases/28-.../28-01-SUMMARY.md` lines 123-305 — the SC3 record shape this task must match: designated assertion named, one heading per mutation, every `org.opentest4j.AssertionFailedError` quoted verbatim, and any mutation detected only by a weaker signal called out as weaker rather than reported as reach it lacks.
    - `.planning/phases/28-.../28-VERIFICATION.md` section "SC1 — the measurement" — the verifier's own probe output, which this probe must reproduce in the opposite direction.
  </read_first>

  <files>.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-04-SUMMARY.md</files>

  <action>
Designate ONE assertion as the red-probe target and name it in the SUMMARY: `cookiePayloadIsStrippedUnderStrict`. Then run three mutations, one at a time, each applied to the working tree, run, and reverted with `git checkout HEAD -- <path>` — `git stash` is prohibited in this repository's worktrees because `refs/stash` lives in the parent `.git` and is shared across every linked worktree.

Mutation A — the control revert. Change the payload line back to render the untreated truncated payload value. EXPECT: `cookiePayloadIsStrippedUnderStrict` and `cookiePayloadIsStrippedUnderBalanced` go red. Record both verbatim messages.

Mutation B — the policy-plumbing revert. Leave the call to the new sanitizer in place but hand it a policy constructed for OFF instead of the `policy` parameter. EXPECT: the same assertions go red, proving the control reads the LIVE policy rather than a constant. Record the verbatim message.

Mutation C — the fixture regression. Restore the hand-typed non-interpolating payload value the fixture carried before this plan. EXPECT: the containment guard added to `assertSentinelsAreDistinctAndNonOverlapping` goes red — this is the assertion whose ABSENCE let 14 green tests say nothing about this route. Record its verbatim message. If any mutation is detected only by a source-text pin rather than by a behavioural assertion, say so and call it weaker; do not report reach the probe does not have.

Write the SUMMARY to `28-04-SUMMARY.md` using the standard template. Include a `## Threat Flags` section reporting on every threat this plan registers — a SUMMARY shipping without that section counts as an executor self-check failure (recorded from phase 23's seal). Include a `## Residuals` section that states, in the plan's own words: this plan closed ONE of THREE measured detail-line routes; `AiScanCheck` is still uncontrolled at this commit and is plan 28-05's; the repository-wide single-producer gate `WR-01` names does NOT exist and was deliberately NOT built (D-28-06); and the evidence tail `AR-28-01` remains an accepted residual by maintainer decision at 28-03's checkpoint and is not reopened.

Do not edit `.planning/REQUIREMENTS.md`. Re-derive its digest before finishing and record the raw output.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.scanner.IssueDetailCookieCarrierTest' --tests 'com.six2dez.burp.aiagent.scanner.CookieRouteDispositionTest' --tests 'com.six2dez.burp.aiagent.scanner.EvidenceTailReachTest' --console=plain</automated>
    <automated>git status --porcelain src/ | wc -l</automated>
    <automated>shasum -a 256 .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `28-04-SUMMARY.md` names `cookiePayloadIsStrippedUnderStrict` as the designated red-probe assertion, verbatim.
    - Three mutation headings exist and each carries at least one quoted `org.opentest4j.AssertionFailedError` line. The literal phrase "the suite went red" appears nowhere in the SUMMARY.
    - After the probes, `git status --porcelain src/` prints nothing (0 lines) — every mutation reverted cleanly.
    - The three phase-28 test classes pass together with 0 failures and 0 errors.
    - `shasum -a 256 .planning/REQUIREMENTS.md` prints a digest beginning `9b3219662ec0d007` and `grep -n 'PRIV-05' .planning/REQUIREMENTS.md` shows line 23 still `- [ ]`. Both raw outputs are pasted into the SUMMARY.
    - The `## Residuals` section names all four residuals listed in the action by their identifiers.
    - The `## Threat Flags` section reports a disposition for each of `T-28-17` through `T-28-20`.
  </acceptance_criteria>

  <done>
    SC3 is discharged for route 1: the control was verified to FAIL before it was verified to pass,
    the designated assertion is named, and every failure message is on the record verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Burp-held proxied traffic -> `AuditIssue.detail()` | A COOKIE-typed injection point's `originalValue` is the operator's real session cookie, captured by the proxy. It crosses into an issue-detail blob here. |
| `AuditIssue.detail()` -> `scanner_issues` MCP tool result | `Serialization.kt:14` (`detail = detail()`) then `McpToolExecutorImpl.kt:602-618`. The blob leaves the extension for an external AI agent. |
| Plan record -> future reader | A closure claim written into source or a SUMMARY is consumed as evidence by a later phase. A claim wider than its control is the failure class this series exists to correct. |

## STRIDE Threat Register

ASVS level 1; blocking threshold `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-17 | Information Disclosure | `ScannerIssueSupport.buildActiveIssueDetailLines` line 121 (`Payload Used:`) | high | mitigate | Type-keyed whole-payload strip in `sanitizeRenderedPayload` on `policy.stripCookies && point.type == InjectionType.COOKIE`, writing `INJECTION_VALUE_STRIPPED_MARKER`. Held by `cookiePayloadIsStrippedUnderStrict` / `…UnderBalanced` measured end-to-end through `Redaction.apply`. |
| T-28-18 | Tampering | `IssueDetailCookieCarrierTest` fixture (`PAYLOAD`) | high | mitigate | The fixture is DERIVED from `PayloadGenerator.generateContextAwarePayloads`, and `assertSentinelsAreDistinctAndNonOverlapping` asserts the derived value contains `DETAIL_SENTINEL`. A generator change that stops interpolating turns the fixture red instead of silently restoring the blind fixture that produced 14 uninformative greens. |
| T-28-19 | Repudiation | SC3 evidence in `28-04-SUMMARY.md` | medium | mitigate | Designated assertion named; three mutations run; every `org.opentest4j.AssertionFailedError` quoted verbatim; reverts by `git checkout HEAD --` with a `git status --porcelain` clean gate, never `git stash` (shared `refs/stash` across worktrees). |
| T-28-20 | Information Disclosure | `Evidence:` line in the same detail block | medium | accept | Owned by `AR-28-01`, filed by plan 28-03 and accepted by the maintainer at that plan's blocking checkpoint. NOT reopened here. Naming it prevents this plan's closure being read as covering it. |
| T-28-21 | Repudiation | This plan's own closure claim | high | mitigate | Prohibition block plus the `## Residuals` requirement in task 3: the plan closes ONE of THREE routes and must say so. `ScannerIssueSupport.kt:74-75`'s single-producer claim is deliberately NOT corrected here — plan 28-05 corrects it, because only that plan can name the second producer's control accurately. |
| T-28-SC | Tampering | package-manager installs | n/a | accept | This plan installs NOTHING. No `npm`/`pip`/`cargo` install task exists; `mockito-kotlin:5.4.0` is already declared at `build.gradle.kts:60` and this plan does not use it. No Package Legitimacy Gate applies. |
</threat_model>

<artifacts_this_phase_produces>
Symbols and files this plan creates or renames. Plan 28-05 and 28-06 consume them by these names.

| Symbol / file | Kind | Where |
|---|---|---|
| `ScannerIssueSupport.sanitizeRenderedPayload(point, payload, policy)` | `internal fun` | `scanner/ScannerIssueSupport.kt`, directly after `sanitizeInjectionPointValue` |
| `IssueDetailCookieCarrierTest.payloadRenderedFor(point, mode)` | test helper | `scanner/IssueDetailCookieCarrierTest.kt` companion |
| `cookiePayloadIsStrippedUnderStrict` | `@Test` | idem — the SC3 designated red-probe assertion |
| `cookiePayloadIsStrippedUnderBalanced` | `@Test` | idem |
| `cookiePayloadSurvivesUnderOff` | `@Test` | idem — SC2 for route 1 |
| `urlParamPayloadSurvivesStrict_attributionControl` | `@Test` | idem |
| `thePayloadStrippedMarkerIsNotTruncated` | `@Test` | idem |
| `aCookiePayloadCarryingOnlyAnEncodedFormOfTheValueIsStillStripped` | `@Test` | idem — [edge:encoding] |
| `anEmptyValuedCookiePointStillRendersTheMarkerOnBothLines` | `@Test` | idem — [edge:empty] |
| `theOnlyThreeDifferencesBetweenStrictAndOffAreTheEnumeratedControls` | `@Test` (renamed) | idem — was `…TwoDifferences…` |
| `unenumeratedDifferenceMessage` | test helper (renamed) | idem — was `thirdDifferenceMessage` |
| `28-04-SUMMARY.md` | record | phase 28 directory |

REUSED, not created — do not redeclare: `INJECTION_VALUE_STRIPPED_MARKER`, `ORIGINAL_VALUE_MAX_CHARS`,
`PAYLOAD_VALUE_MAX_CHARS`, `sanitizeInjectionPointValue`, `buildActiveIssueDetailLines`,
`RedactionPolicy.fromMode`, `detailLinesFor`, `issueDetailsFor`, `redactedBlobFor`, `detailFieldOf`,
`requestFieldOf`, `originalValueRenderedFor`.
</artifacts_this_phase_produces>

<verification>
1. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.scanner.IssueDetailCookieCarrierTest' --tests 'com.six2dez.burp.aiagent.scanner.CookieRouteDispositionTest' --tests 'com.six2dez.burp.aiagent.scanner.EvidenceTailReachTest'` — 0 failures, 0 errors.
2. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt` — both exit 0; `detekt-baseline.xml` byte-unchanged.
3. `git diff --stat -- src/main/kotlin/com/six2dez/burp/aiagent/scanner/AdaptivePayloadEngine.kt` prints nothing (SC4 must not regress).
4. `git diff --stat -- .planning/REQUIREMENTS.md` prints nothing; `shasum -a 256` begins `9b3219662ec0d007`.
5. Do NOT run `./gradlew check` as a gate: it is RED at HEAD for a maintainer-accepted reason (redact BRANCH coverage 0.92784 against a 0.930 floor). Do not adjust the floor.
6. `RedactionTest > windowedScanRedactsJsonPairAcrossEveryBoundaryAlignment()` is a known wall-clock flake under CPU load (SafeRegex 50 ms deadline, fails fail-closed). Not a regression; re-run in isolation if it appears.
</verification>

<success_criteria>
- Under STRICT and BALANCED, neither the `Original Value:` nor the `Payload Used:` line of the
  active-scan issue detail carries any byte of a COOKIE-typed point's `originalValue`, measured
  through `Redaction.apply` with a `PayloadGenerator`-derived payload.
- Under OFF both lines survive verbatim.
- A `URL_PARAM` point is unchanged in every mode.
- The false `PAYLOAD_VALUE_MAX_CHARS` KDoc premise is corrected append-and-amend, prior text intact.
- SC3 is discharged with a named assertion and verbatim failure messages.
- Nothing in this plan claims route 2 is closed or claims a repository-wide producer gate exists.
</success_criteria>

<output>
Create `.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-04-SUMMARY.md` when done.
</output>
