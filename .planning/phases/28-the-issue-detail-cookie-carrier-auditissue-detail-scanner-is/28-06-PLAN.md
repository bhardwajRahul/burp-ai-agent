---
phase: 28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is
plan: 06
type: execute
wave: 3
depends_on: ["28-04", "28-05"]
gap_closure: true
autonomous: false
requirements: [PRIV-05]
files_modified:
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
  - .planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-03-SUMMARY.md
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC5 EXTENDED — `26-SECURITY.md`'s `AR-27-08` row carries a SECOND dated supersession recording that the 2026-08-27 closure claim was premature, what was actually controlled then (one line of one producer), what is controlled now (three lines across two producers), and what remains uncontrolled. Written append-and-amend; the prior 8693-byte prefix of the row is byte-identical before and after."
    - "THE RECORD IS NOT WIDER THAN THE CONTROL — the amended cell enumerates, by name, every route this round did NOT close: `AR-28-01` (evidence tail, accepted by maintainer decision), `AR-27-04`, `AR-27-07`, `AR-27-10`, `AR-27-11`, and the ABSENCE of any repository-wide detail-producer gate (`WR-01` / D-28-06)."
    - "THE NAMED RESIDUAL IS NAMED, NOT SATISFIED — the record states that after this round there are TWO CONTROLLED producers and STILL no repository-wide enforcement, and that D-28-06 recorded that gate as considered and NOT taken. No artifact in this round claims it exists or claims `WR-01` is closed."
    - "`.planning/REQUIREMENTS.md` IS BYTE-UNCHANGED — sha256 `9b3219662ec0d007c1c82d64eed3ef2698bd306ce69f01205ac9bbc3f42fcfb4`, PRIV-05 on line 23 still `- [ ]`. The gate runs TWICE: once in-task, once post-completion, under two distinct headings, with RAW outputs recorded both times."
    - "THE 28-03 RUN 2 RECORDING FAILURE IS DISCHARGED — the block at `28-03-SUMMARY.md:542-580` no longer holds pre-run instructions. It holds the raw `shasum` and `grep` outputs, under an honest note that RUN 2 was executed late, during the gap round, rather than at 28-03's own commit."
    - "THE CARRIER REGISTRY MATCHES THE CODE — `CookieCarrierInventoryTest`'s `INJECTION_EXTRACTOR/PARAMETER_LIST` entry no longer asserts a closure that is false of the block, and `ISSUE_DETAIL_CARRIER_DISPOSITION` carries a second supersession naming both producers and the three controlled lines."
    - "THE ACCESSOR SET GREW BY THE ONE ACCESSOR THAT HID ROUTE 2 — `AuditInsertionPoint.baseValue()` joins `COOKIE_BYTE_ACCESSORS`, producing exactly one new (file, accessor) pair for `scanner/AiScanCheck.kt` with a measured count of 2, whose entry enumerates the carrying and non-carrying call by line. This is the class's own axis-4 blindness, closed for this accessor."
    - "[edge:ordering] The append-and-amend is order-bearing and machine-checked: the amendment is inserted at a named column boundary so the prior text stays a contiguous byte-prefix, and the pre-edit digest is recorded BEFORE the edit, not reconstructed after it."
    - "[edge:adjacency] Two supersession markers now sit adjacent in one register cell. Each carries its own date and plan id, and the second explicitly states that it supersedes the first rather than the original measurement, so a reader cannot merge them into one claim."
    - statement: "`threats_open` is RECOMPUTED by re-running the documented awk over the Threat Register rather than restated, and the recomputation's raw output is recorded. `AR-` rows sit outside that counter's population at any severity — the counter scans rows whose id begins `T-26-`."
      verification: backstop
  prohibitions:
    - "MUST NOT record a closure claim wider than the controls plans 28-04 and 28-05 shipped. Four prior PRIV-05 closures were wrong in exactly this way; this cell has already been written as closed once, on a route that was open."
    - "MUST NOT claim, imply, or quietly satisfy the repository-wide single-producer gate. D-28-06 keeps it a NAMED RESIDUAL; the amended record must say plainly that no such gate exists after this round."
    - "MUST NOT tick PRIV-05 in `.planning/REQUIREMENTS.md`, and MUST NOT edit that file at all. D-28-04 makes the judgement the round's own gate, not a plan-authoring liberty; the sha256 gate is the enforcement."
  artifacts:
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md — `AR-27-08` second supersession; `threats_open` recomputed"
    - ".planning/phases/28-.../28-03-SUMMARY.md — RUN 2 block replaced with raw output"
    - "src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt — corrected entries, second disposition supersession, `baseValue()` accessor, `AiScanCheck.kt` registry entry"
  key_links:
    - "`AR-27-08` cell -> the two controls shipped by 28-04 and 28-05, named by symbol (`ScannerIssueSupport.sanitizeRenderedPayload`, `AiScanCheck.sanitizeCookiePointText`) and by committed probe class"
    - "`COOKIE_BYTE_ACCESSORS` -> `AuditInsertionPoint.baseValue()` -> the `scanner/AiScanCheck.kt` registry entry — the mechanism that would have surfaced route 2 before a verifier had to"
    - "`.planning/REQUIREMENTS.md` -> the twice-run sha256 gate -> two distinct SUMMARY headings"
---

<objective>
Make the RECORD match the CODE, and no wider.

Plans 28-04 and 28-05 controlled three detail lines across two producers. `26-SECURITY.md`'s
`AR-27-08` row and `CookieCarrierInventoryTest`'s registry entries currently assert a closure that
was measured false by `28-VERIFICATION.md` — they were written when one line of one producer was
controlled. D-28-08 requires them corrected; the correction protocol is append-and-amend under a
dated supersession marker with the prior text kept byte-exact, the protocol plan 28-03 used and SC5
verified by digest.

This plan also discharges the second gap `28-VERIFICATION.md` records: the PRIV-05 gate RUN 2 block
in `28-03-SUMMARY.md` still holds pre-run instructions marked OUTSTANDING while the phase commits
landed anyway. That is a recording failure with a named `missing` item, and leaving it unplanned
would be the silent drop this round exists to stop.

Purpose: the failure class this whole phase-27/28 series exists to correct is a record wider than
the control it describes. Getting this plan right is worth more than the closure would have been.

Output: an honest register cell, an honest carrier registry, a discharged gate record, and a
machine-checked accessor set that would have surfaced route 2 without a verifier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-CONTEXT.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-PATTERNS.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-VERIFICATION.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-04-SUMMARY.md
@.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-05-SUMMARY.md
</context>

<measured_facts>
Measured before this plan was written. Use them; do not re-derive them by reading the whole file.

1. **`26-SECURITY.md` IS 259 KB AND LINE 315 IS A SINGLE 8813-BYTE ROW.** Lines 313-317 are
   2085 / 1987 / 8813 / 10400 / 4160 bytes. A full read of this file kills the executor mid-run —
   this has happened. Use `LC_ALL=C awk 'NR==315{...}'` and `grep -n` with context only.
2. **The insertion anchor is unique.** The literal `not an omission. | Measured by plan 27-08`
   occurs EXACTLY ONCE in the whole file. It marks the boundary between the `AR-27-08` description
   cell and the next column. Insert the amendment immediately before the ` | Measured by plan 27-08`
   separator on that line — do not retype the row.
3. **The byte-prefix gate is pre-computed.** At HEAD, the description cell of line 315 up to (not
   including) that separator is **8693 bytes** and its sha256 is
   `8dc326ac23204becce687deeba867740eb2d4dde21346c58d7da9595d137ae2e`. After the amendment the same
   first 8693 bytes of line 315 must still hash to that value. Compute with `LC_ALL=C` — the row
   contains multi-byte em dashes and a char-oriented `substr` gives a different answer.
4. `.baseValue()` has exactly TWO call sites in main source, both in `scanner/AiScanCheck.kt`:
   `:114` inside `determineVulnClasses`, used only as a regex subject to pick vuln classes
   (NON-CARRYING), and `:353`, the carrier. Measured by plan 28-05, fact 7.
5. `CookieCarrierInventoryTest` machine-checks its registry against a source scan. Adding a
   `ROUTED_THROUGH` entry WITHOUT extending `COOKIE_BYTE_ACCESSORS` turns
   `everyCookieByteCarrierSiteIsRoutedOrClassified` red (declared would exceed measured). The two
   edits must land together, and `MEASURED_CARRIER_SITES` plus `EXPECTED_TOTAL_CARRIER_SITES` must
   move with them or `theMeasuredPerFilePerAccessorCountsArePinned` reports the disagreement.
6. `.planning/REQUIREMENTS.md` at HEAD: sha256
   `9b3219662ec0d007c1c82d64eed3ef2698bd306ce69f01205ac9bbc3f42fcfb4`, PRIV-05 on line 23 is `- [ ]`.
7. `28-03-SUMMARY.md:542-580` still carries the pre-run instruction block ending
   `Paste BOTH raw outputs under this heading, replacing this block.` The phase commits landed
   through `723ae59` without it being replaced.
8. `CookieCarrierInventoryTest` anchors: class KDoc axis 3 at `:56-68`; the
   `INJECTION_EXTRACTOR/PARAMETER_LIST` entry at `:406-421`; the `RESPONSE_ANALYZER/HEADER_LIST`
   entry's bound prose at `:539-540`; `ISSUE_DETAIL_CARRIER_DISPOSITION` at `:565`;
   `COOKIE_BYTE_ACCESSORS` at `:289`; `MEASURED_CARRIER_SITES` at `:359`. File is 605 lines.
9. `already_known` defers two items in that file to a plan that already touches it — this is that
   plan. `:539` states the evidence-tail bound as a singular "capped at 80 characters" when the
   derived truth is the multiset `{80, 80, 60}`, and `:540` cites `ActiveAiScanner.kt:1246` when the
   live line is `:1242`.
</measured_facts>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: the `AR-27-08` disposition — a one-way write into an append-only register</name>
  <decision>How is `AR-27-08` dispositioned in `26-SECURITY.md` now that three detail lines across both producers are controlled — CLOSED, or STILL OPEN with a narrowed, named residual?</decision>
  <context>
This register cell has already been written as CLOSED once, on 2026-08-27 by plan 28-01, while two
routes carrying the same bytes to the same tool result were open. `28-VERIFICATION.md` measured that
closure false and recommends: *"`AR-27-08` should stay OPEN with a second append-and-amend recording
why the first closure was premature — that record is worth more than the closure would have been."*

The register is APPEND-ONLY by its own protocol: prior text is never rewritten, only superseded. A
disposition written here cannot be withdrawn, only appended to. Four PRIV-05 closures have been wrong
in this exact way, and the file's own `T-26-02-01` history records the class three times.

What changed since the first closure, stated plainly so the choice is made on facts:
  - CONTROLLED NOW: `ScannerIssueSupport` `Original Value:` (28-01) and `Payload Used:` (28-04);
    `AiScanCheck` `**Original Value:**` and its payload line (28-05). Three measured-carrier lines
    plus one defence-in-depth line, across the two producers that exist.
  - STILL UNCONTROLLED, inside the same detail blob: the `Evidence:` line — `AR-28-01`, MEDIUM,
    accepted as a residual by maintainer decision at 28-03's blocking checkpoint, not reopened here.
  - STILL ABSENT: any repository-wide detail-producer gate. `WR-01` measured the advertised one as
    structurally incapable of seeing another file; D-28-06 records building one as CONSIDERED AND
    NOT TAKEN. A THIRD producer added tomorrow would be caught by nothing.

PRIV-05 itself is NOT part of this decision. D-28-04 keeps `.planning/REQUIREMENTS.md` byte-unchanged
and makes the tick a judgement for the round's own gate. Whichever option is chosen, this plan does
not edit that file, and the sha256 gate enforces it.
  </context>
  <options>
    <option id="stay-open">
      <name>AR-27-08 stays OPEN, with a second supersession narrowing it to the named residual</name>
      <pros>Matches what the code actually guarantees: every producer that EXISTS is controlled, but nothing prevents the next one, and one line of the same blob is still open by accepted decision. It is the verifier's own recommendation. It cannot be wrong in the direction that has been wrong four times. A future reader inherits an accurate bound rather than a closure they must re-measure to trust.</pros>
      <cons>A row that reads OPEN after two rounds of real work can be misread as "nothing was fixed", so the amendment must carry the control inventory explicitly. It leaves an OPEN row in a register a milestone audit will read.</cons>
    </option>
    <option id="close-with-residual">
      <name>AR-27-08 is CLOSED for its stated scope, with `AR-28-01` and the missing gate carried forward as separate open rows</name>
      <pros>The row's own sentence is about a COOKIE-typed point's value reaching `scanner_issues` through `AuditIssue.detail()`, and for every producer in the tree that is now measured false under STRICT and BALANCED with committed probes and recorded red probes. Separating "this finding" from "the enforcement gap" keeps each row about one thing.</pros>
      <cons>It is a closure claim written the same day as the fix, which is the same-day-closure pattern this series has failed on. It relies on "every producer in the tree" being complete, and the only mechanism that would prove that — a repository-wide gate — is precisely what D-28-06 did NOT build. The claim would rest on a grep the round ran once, not on a committed tripwire.</cons>
    </option>
  </options>
  <resume-signal>Reply `stay-open` or `close-with-residual`. If no human answers and the run auto-advances, the executor MUST select `stay-open` — it is the verifier's recorded recommendation and the only option whose failure mode is understating rather than overstating a security control — and MUST record in the SUMMARY that the choice was made by the auto-advance default rather than by a human, naming this line as its authority.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: `AR-27-08`'s second supersession, `threats_open` recomputed, and the RUN 2 recording failure discharged</name>

  <precondition>`26-SECURITY.md` line 315 still begins `| AR-27-08 | T-26-02-01 |` and the literal `not an omission. | Measured by plan 27-08` still occurs exactly once in the file — assert both with `grep -c` before editing; the row may have moved if another phase amended the register.</precondition>

  <reversibility rating="one-way">The register is append-only by its own protocol — prior text is never rewritten. A disposition written into this cell can only be superseded by another append, never withdrawn. The preceding `checkpoint:decision` gates it for that reason.</reversibility>

  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` — **TARGETED READS ONLY.** `LC_ALL=C awk 'NR==315{print substr($0,1,700)}'` for the row's head, `LC_ALL=C awk 'NR==315{n=length($0); print substr($0,n-1400)}'` for its tail, and `sed -n '1,20p'` plus `sed -n '185,200p'` for the front-matter counters and the `threats_open` population statement. DO NOT read the file whole and do not read lines 313-317 in full — line 316 alone is 10400 bytes.
    - `.planning/phases/28-.../28-03-SUMMARY.md` lines 535-585 — the RUN 2 block and the reasoning that made it OUTSTANDING.
    - `.planning/phases/28-.../28-04-SUMMARY.md` and `28-05-SUMMARY.md` — the control symbols, probe class names and red-probe results this amendment cites. Cite them by the names those SUMMARYs actually use; do not invent symbol names.
  </read_first>

  <files>
    .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md,
    .planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-03-SUMMARY.md
  </files>

  <action>
BEFORE ANY EDIT, record the pre-edit byte-prefix digest and paste the raw output into the SUMMARY: `LC_ALL=C awk 'NR==315{i=index($0,"| Measured by plan 27-08"); printf "%s", substr($0,1,i-1)}' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | shasum -a 256`. It must print `8dc326ac23204becce687deeba867740eb2d4dde21346c58d7da9595d137ae2e`. If it does not, STOP — the row moved and this plan's anchors need re-measuring before anything is written.

Amend the `AR-27-08` cell by INSERTING new text immediately before the ` | Measured by plan 27-08` column separator on line 315. Use a scoped, anchored edit against the unique literal from measured fact 2 — never a whole-row retype and never a whole-file rewrite. Nothing before the insertion point may change by one byte.

The appended text opens with a dated supersession marker in THIS ROW'S OWN established vocabulary. Measured at HEAD: the cell carries exactly ONE marker and it reads `**AMENDED 2026-08-27 by plan 28-03 (phase 28) — THE ROUTE IS NOW CONTROLLED. …**`. Write the new one in that same form — `**AMENDED <today's date, ISO> by plan 28-06 (phase 28) — …**` — and do NOT introduce a synonym for the marker word. A cell whose markers are spelled two different ways cannot be enumerated by any reader or any grep, and inventing a token so that a check passes is the exact behaviour this row exists to record against.

The marker must then state, in its own sentence, that it supersedes the marker DATED 2026-08-27 (plan 28-03), and NOT the original 2026-08-25 measurement, which stands. Cite both dates literally — two markers now sit adjacent in one cell and a reader must not merge them, nor mistake which one is being withdrawn.

Then write, in this order:
  (a) WHY THE FIRST CLOSURE WAS PREMATURE, in one sentence that names the mechanism rather than the mistake: the control was applied to ONE of the nine detail lines the same function emits, and the register was amended to say the route was closed. Note that phase 27's own probe output at `27-08-SUMMARY.md:297` had already PRINTED both occurrences side by side.
  (b) THE CONTROL INVENTORY AS IT NOW STANDS, by symbol and by committed probe class, taken from the 28-04 and 28-05 SUMMARYs: the two `ScannerIssueSupport` lines and the two `AiScanCheck` lines, which enum each gate keys on, and which probe class holds each. State that `AiScanCheck` was a producer named in no phase-28 artifact before `28-VERIFICATION.md` found it, and that it read no privacy mode at all.
  (c) THE ASYMMETRY, stated rather than smoothed: route 2's payload line is controlled as defence in depth, not as a measured leak closure — that class sources payloads from a static table and does not interpolate the insertion point's value.
  (d) WHAT IS STILL NOT CONTROLLED, by identifier: `AR-28-01` (the `Evidence:` line in the same blob, MEDIUM, accepted by maintainer decision at 28-03's blocking checkpoint, not reopened), `AR-27-04`, `AR-27-07`, `AR-27-10`, `AR-27-11`.
  (e) THE ENFORCEMENT GAP, in its own sentence and not softened: no repository-wide detail-producer gate exists. The one this row's predecessor implied was measured by `WR-01` as structurally incapable of seeing another file; D-28-06 recorded building one as CONSIDERED AND NOT TAKEN. After this round there are TWO CONTROLLED producers and nothing that would catch a third.
  (f) THE DISPOSITION selected at the preceding checkpoint, stated as the chosen option's own sentence, with the authority for the choice named — a human answer, or the auto-advance default and this plan's resume-signal line.
  (g) PRIV-05's STATUS: still `- [ ]`, `.planning/REQUIREMENTS.md` byte-unchanged, and the reason is the enumeration at `28-03-SUMMARY.md:515-539` with its own premise corrected — that enumeration said "Closing `AR-27-08` closes ONE carrier" while `AR-27-08` was not closed either. Say that the premise is now repaired, not that the count changed.

Recompute `threats_open` rather than restating it: re-run the awk documented at the top of `26-SECURITY.md` over the Threat Register, paste its RAW output and the number of rows scanned into the SUMMARY, and update the front-matter counter only if the recomputed value differs. State in the SUMMARY that `AR-` rows sit outside that counter's population at any severity because it scans rows whose id begins `T-26-`.

Then discharge the RUN 2 recording failure. Replace the instruction block at `28-03-SUMMARY.md:542-580` with the RAW outputs of `shasum -a 256 .planning/REQUIREMENTS.md` and `grep -n 'PRIV-05' .planning/REQUIREMENTS.md`, under a short honest note that RUN 2 was executed LATE — during the phase-28 gap round, not at 28-03's own commit — naming this plan as where it was finally run and stating that the phase commits had already landed without it. Do not delete the reasoning that explains why a distinct heading exists and why RUN 1 does not discharge it; that reasoning is the record's value. Do not write anything that implies the gate ran at 28-03's commit.

TENSE, NOT DELETION — READ THIS BEFORE TOUCHING THE BLOCK, because the preservation instruction above and the acceptance criterion below only LOOK contradictory. Measured: the uppercase token appears on exactly TWO lines inside 535-585, and they are different kinds of sentence.
<!-- planner-discipline-allow: OUTSTANDING -->
  - `:544` is the present-tense banner `**OUTSTANDING — the phase commit is BLOCKED until RUN 2 is recorded here.**`. It is part of the instruction block and is REPLACED, because the commit is no longer blocked and the run is no longer pending.
  - `:548` is a REASON, not a banner: `This heading is therefore left OUTSTANDING deliberately, and its outstanding-ness is stated in the closing narration so the orchestrator executes it.` REWRITE IT IN THE PAST TENSE — that the heading WAS deliberately left open at 28-03's commit, and why (`phase.complete 28` runs after that plan's last task, so its executor had no reachable trigger) — using neutral wording such as "left open" so the rewritten sentence does not itself carry the uppercase token. Do NOT delete it. The reasoning is preserved; only the present-tense claim that an item is still pending is retired.
LEAVE THE REST OF THE FILE ALONE. The same token also appears at `:651`, `:673` and `:722`, OUTSIDE this task's `535-585` scope and outside the acceptance criterion. Those three are 28-03's own record of the state AT ITS OWN COMMIT, where the statement was true; rewriting them would be editing a committed summary's history, which is the opposite of this plan's discipline. Add one sentence to the new block pointing at them so the two readings cannot be merged: they describe 28-03's commit, this block describes the discharge.

Do not edit `.planning/REQUIREMENTS.md`. Do not tick PRIV-05.
  </action>

  <verify>
    <automated>LC_ALL=C awk 'NR==315{i=index($0,"| Measured by plan 27-08"); printf "%s", substr($0,1,8693)}' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | shasum -a 256</automated>
    <automated>shasum -a 256 .planning/REQUIREMENTS.md; grep -n 'PRIV-05' .planning/REQUIREMENTS.md; git diff --stat -- .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - BYTE-PREFIX GATE: the first automated command prints `8dc326ac23204becce687deeba867740eb2d4dde21346c58d7da9595d137ae2e` AFTER the edit. Both the pre-edit and post-edit raw digests are pasted into the SUMMARY.
    - The row grew and did not shrink: `LC_ALL=C awk 'NR==315{print length($0)}'` returns a value strictly greater than 8813.
    - TWO DATED MARKERS NOW EXIST IN THE CELL, AND THE SECOND NAMES THE FIRST — asserted on the vocabulary the row ACTUALLY uses, re-measured at HEAD before this criterion was written. HEAD census of line 315: `AMENDED` x1, `SUPERSEDE`/`SUPERSEDES`/`SUPERSEDED` x0, `2026-08-27` x1, `2026-08-25` x2. Do not manufacture a marker word to satisfy a grep, and do not edit the prior text to insert one — the byte-prefix gate above forbids the second and the singular-marker instruction in the action forbids the first. All four checks run against `LC_ALL=C awk 'NR==315{print}' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md`, piped as shown. Use `grep -o … | wc -l` and NEVER `grep -c`: the row is a SINGLE line, so `grep -c` can only ever return 0 or 1 and would report 1 for a cell with ten markers.
      (i) MARKER COUNT — `| grep -oE 'AMENDED|SUPERSEDE[SD]?' | wc -l` returns at least 2. HEAD: 1.
      (ii) THE NEW MARKER CARRIES THE PROTOCOL FORM AND THIS PLAN'S ID — `| grep -oE 'AMENDED [0-9]{4}-[0-9]{2}-[0-9]{2} by plan 28-06' | wc -l` returns at least 1. HEAD: 0 (the sole existing match is `AMENDED 2026-08-27 by plan 28-03`).
      (iii) IT NAMES THE MARKER IT SUPERSEDES — `| grep -o '2026-08-27' | wc -l` returns at least 2. HEAD: 1 (the marker itself). The second occurrence is the new text citing it by date.
      (iv) IT NAMES THE MEASUREMENT IT DOES **NOT** SUPERSEDE — `| grep -o '2026-08-25' | wc -l` returns at least 3. HEAD: 2 (one inside the protected prefix, one in the row's trailing date column). The third is the new text citing it by date.
    - Every not-closed identifier is named in the cell: for each of `AR-28-01`, `AR-27-04`, `AR-27-07`, `AR-27-10`, `AR-27-11`, `WR-01`, `D-28-06`, `LC_ALL=C awk 'NR==315{print}' … | grep -c '<id>'` returns at least 1.
    - `.planning/REQUIREMENTS.md` untouched: `git diff --stat -- .planning/REQUIREMENTS.md` prints nothing; `shasum -a 256` prints a digest beginning `9b3219662ec0d007`; `grep -n 'PRIV-05'` shows line 23 with an unchecked box.
    - The RUN 2 block no longer holds pre-run instructions: `grep -c 'replacing this block' .planning/phases/28-.../28-03-SUMMARY.md` returns 0.
    - No pending-item claim survives inside the block's scope: `sed -n '535,585p' .planning/phases/28-.../28-03-SUMMARY.md | grep -c 'OUTSTANDING'` returns 0. HEAD: 2 — the `:544` banner (replaced) and the `:548` reason sentence (rewritten in the past tense, per the action, NOT deleted). Re-measure the range before asserting: the block's line numbers shift as the raw output replaces the instructions, so run the count against the block by heading rather than by stale offsets if 535-585 no longer spans it.
    - THE REST OF THE FILE SURVIVES ON THIS AXIS, and that is deliberate, not an oversight: the three later mentions (HEAD `:651`, `:673`, `:722`) are 28-03's record of the state at ITS OWN commit, where the statement was true. Assert survival by content, not by total count — `grep -c 'OUTSTANDING BY DESIGN' .planning/phases/28-.../28-03-SUMMARY.md` still returns 2 and `grep -c 'The one remaining' …` still returns 1 (both measured at HEAD). Deleting or retrospectively rewriting them is a FAILURE of this task, not a cleaner result: they describe a different moment than the discharged block does, and the new block's cross-reference sentence is what keeps the two readings apart.
    - The RUN 2 block holds raw output: within lines 535-600 of `28-03-SUMMARY.md`, a line matching `^9b3219662ec0d007` and a line matching `^23:` both exist.
    - The lateness is stated, not hidden: within that block, a sentence naming plan `28-06` and the word `late` (any case) exists.
    - `threats_open` recomputation raw output and the row-scan count are pasted into the SUMMARY.
    - The chosen disposition and the AUTHORITY for it (human answer, or auto-advance default) are both recorded in the SUMMARY and in the cell.
  </acceptance_criteria>

  <done>
    The register cell states what is controlled, what is not, and by what authority — with the prior
    text byte-identical at the front of the cell, proven by a digest computed before the edit rather
    than reconstructed after it. The RUN 2 gate is on the record with its lateness disclosed.
  </done>
</task>

<task type="auto">
  <name>Task 2: The carrier registry stops asserting a closure that is false of the block, and grows the accessor that hid route 2</name>

  <precondition>Plans 28-04 and 28-05 are committed and their test classes pass at HEAD — the registry text this task writes cites control symbols that must already exist. Confirm with `grep -c 'sanitizeRenderedPayload' src/main/kotlin/com/six2dez/burp/aiagent/scanner/ScannerIssueSupport.kt` and `grep -c 'sanitizeCookiePointText' src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt` both returning at least 1.</precondition>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt` — lines 20-84 (the class KDoc's four named blind axes, especially axis 3 at `:56-68` and axis 4 at `:70-72`), 85-210 (the four machine-checked tests), 264-330 (`AccessorSpec` and `COOKIE_BYTE_ACCESSORS`), 350-380 (`MEASURED_CARRIER_SITES`, `EXPECTED_TOTAL_CARRIER_SITES`), 390-475 (`ROUTED_THROUGH`), and 545-605 (`ISSUE_DETAIL_CARRIER_DISPOSITION`).
    - `.planning/phases/28-.../28-05-SUMMARY.md` — for the exact control symbol names and probe class name to cite.
    - `.planning/phases/28-.../28-VERIFICATION.md` — the `artifacts` entry for this file, which states precisely what `:407` gets wrong.
  </read_first>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt</files>

  <action>
Correct the `INJECTION_EXTRACTOR/PARAMETER_LIST` entry (`:406-421`), D-28-08's third site. Its text asserts "TWO CONSUMERS, BOTH READ, AND BOTH NOW CONTROLLED", which `28-VERIFICATION.md` measured as true of one line and false of the block. Amend it in the file's own supersession-never-deletion discipline: keep the existing reason verbatim, append a dated marker naming plan 28-06, and enumerate the THIRD consumer the entry never named — `payloadGenerator.generateContextAwarePayloads` at `ActiveAiScanner.kt:513` and `:709`, whose output re-enters the same detail blob — and name the control that now covers it. Consumer 1's disposition is unchanged and must be restated as unchanged: `AdaptivePayloadEngine.kt:52` substitutes its own value marker and is deliberately NOT double-redacted.

Append a SECOND supersession to `ISSUE_DETAIL_CARRIER_DISPOSITION` (`:565`), keeping both prior blocks verbatim. It must state: there are TWO producers of these detail lines, not one; the first supersession's control covered one line of one producer; four lines across both producers are controlled now, three of them measured carriers and one defence in depth; the `Evidence:` line in the same blob remains open as `AR-28-01`; and no repository-wide producer gate exists (D-28-06). Cite the committed probe classes by name.

Amend the class KDoc's axis 3 (`:56-68`). It currently reads "Phase 28 CONTROLLED the `AuditIssue.detail()` route at its write site" — true of one write site in one file when written. Correct it to name both producers and both write sites, and KEEP the paragraph's central point, which is still exactly right and is the most valuable sentence in the file: closing one transitive carrier does not teach this inventory to follow the next one, and the axis itself is still open.

Close axis 4 FOR THIS ACCESSOR — the blindness that let route 2 exist unseen. Axis 4 says the accessor set is additive-only and a reader adding one must extend it; `AuditInsertionPoint.baseValue()` returns cookie bytes for a `PARAM_COOKIE` insertion point and is not in the set. Add it:
  - a new accessor key constant alongside `HEADER_LIST` / `PARAMETER_LIST` / `SINGLE_HEADER_LOOKUP` / `RAW_MESSAGE` / `COOKIE_JAR`, named for what it is (an insertion point's base value);
  - an `AccessorSpec` whose `pattern` matches the accessor call, whose `whyItCarriesCookieBytes` states that Burp derives `PARAM_COOKIE` insertion points from the `Cookie` header so the base value IS the cookie value, and whose `positiveFixture` quotes a real line from `AiScanCheck.kt` as it stands AFTER plan 28-05 — a fixture quoting a line that no longer exists stays green while it rots, the drift this file's own KDoc warns about;
  - a `ROUTED_THROUGH` entry for the resulting `(scanner/AiScanCheck.kt, <new accessor>)` pair whose reason ENUMERATES THE SPLIT BY LINE, because the pair covers two calls with different dispositions: `:114` inside `determineVulnClasses` is NON-CARRYING (the value is a regex subject used to choose vuln classes and is never emitted), `:353` is the carrier and is now controlled by `AiScanCheck.sanitizeCookiePointText`. That per-call attribution is prose; what is machine-checked is the COUNT, and the entry should say so in the register the file already uses for `McpToolExecutorImpl`'s split `parameters()` calls;
  - the matching `MEASURED_CARRIER_SITES` and `EXPECTED_TOTAL_CARRIER_SITES` updates. RE-MEASURE these on the tree at execution time rather than copying the number from this plan; the pinned map and the pinned total are cross-checked against each other and against the scan, so all three must move in the same edit.

Fix the two `already_known` items now that this plan touches the file. At `:539` the evidence-tail bound is stated as a singular "capped at 80 characters" while the derived truth is the multiset `{80, 80, 60}` — plan 28-03 measured it and `EvidenceTailReachTest` derives it from source; state the multiset and point at the test rather than restating a number that can drift. At `:540` the citation `ActiveAiScanner.kt:1246` is stale; the live line is `:1242`. Verify the live line against the tree before writing it — do not copy this plan's number blindly.

Write nothing in this file asserting that a repository-wide detail-producer gate exists, and nothing implying `WR-01` is closed.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.CookieCarrierInventoryTest' --console=plain</automated>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.scanner.*' --tests 'com.six2dez.burp.aiagent.redact.*' --console=plain</automated>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt --console=plain</automated>
  </verify>

  <acceptance_criteria>
    - All four tests in `CookieCarrierInventoryTest` pass: `everyCookieByteCarrierSiteIsRoutedOrClassified`, `theMeasuredPerFilePerAccessorCountsArePinned`, `theHeaderValueArgumentMultisetIsPinned`, `theCarrierScanIsNonVacuous`. 0 failures, 0 errors.
    - The accessor set grew by exactly one: `grep -c 'AccessorSpec(' src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt` returns one more than it did at HEAD (record both counts in the SUMMARY).
    - The new registry pair exists and names the file: `grep -c 'AiScanCheck' src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt` returns at least 2 (registry entry plus positive fixture).
    - NON-VACUITY OF THE NEW ACCESSOR, recorded in the SUMMARY with a NAMED assertion and its VERBATIM failure message: add a third call to the new accessor in a scratch main-source location, run the class, and record `theMeasuredPerFilePerAccessorCountsArePinned` going red with both the pinned and measured maps printed. Revert with `git checkout HEAD -- <path>` (never `git stash`) and confirm `git status --porcelain src/` prints nothing. Without this, a pattern that matches nothing would report a clean tree for free.
    - Both prose corrections landed: `grep -c 'capped at 80 characters' …` returns 0 and the multiset appears instead; `grep -c 'ActiveAiScanner.kt:1246' …` returns 0 and the cited line matches the live tree — prove the live line with `grep -n 'confirmation.evidence' src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt` and paste the output.
    - Supersession discipline: the pre-existing reason text for the `INJECTION_EXTRACTOR/PARAMETER_LIST` entry and for `ISSUE_DETAIL_CARRIER_DISPOSITION` both still appear verbatim; nothing was deleted. Prove with `git diff` showing additions only within those two string literals.
    - No gate overclaim: `grep -ci 'repository-wide' src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieCarrierInventoryTest.kt` — every hit, if any, must be a sentence stating such a gate does NOT exist. Quote each hit in the SUMMARY.
    - `ktlintCheck` and `detekt` exit 0; `detekt-baseline.xml` byte-unchanged; the full `scanner` and `redact` package test runs are green apart from the known `RedactionTest` wall-clock flake, which must be re-run in isolation and shown green if it appears.
  </acceptance_criteria>

  <done>
    The carrier registry describes the tree as it is: two producers, four controlled lines with the
    defence-in-depth one labelled as such, one open residual, no repository-wide gate — and the
    accessor whose absence hid route 2 is now machine-checked, proven non-vacuous by a recorded probe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Security register -> milestone audit / future phase | `26-SECURITY.md` rows are consumed as evidence of what is and is not controlled. A row wider than its control makes the next reader stop looking. |
| Carrier registry -> future contributor | `CookieCarrierInventoryTest`'s maps are the enumeration a contributor consults before adding a cookie-touching call site. |
| Requirements file -> release gate | A ticked PRIV-05 would let a release claim a defect closed. `phase.complete` has flipped this box once already, as a side effect, while its own warnings said it was skipping the write. |
| Executor -> a 259 KB file with a 38 KB row | Reading it whole terminates the run mid-task. |

## STRIDE Threat Register

ASVS level 1; blocking threshold `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-29 | Repudiation | `26-SECURITY.md` `AR-27-08` row | high | mitigate | Append-and-amend at a unique named column anchor; the 8693-byte prefix digest `8dc326ac…` recorded BEFORE the edit and re-asserted after. Two dated markers, the second naming which one it supersedes. |
| T-28-30 | Repudiation | The closure claim's SCOPE | high | mitigate | The `checkpoint:decision` gates the disposition; the amended cell must name `AR-28-01`, `AR-27-04`, `AR-27-07`, `AR-27-10`, `AR-27-11`, `WR-01` and `D-28-06` by identifier, each enforced by a grep acceptance criterion. |
| T-28-31 | Tampering | `.planning/REQUIREMENTS.md` | high | mitigate | Not in `files_modified`. sha256 + `grep -n` gate run twice under distinct headings, plus `git diff --stat` proving no change. D-28-04 makes the tick out of scope for any plan. |
| T-28-32 | Denial of Service | Executor context vs `26-SECURITY.md` (259 KB, one 10400-byte and one 8813-byte row) | high | mitigate | `<read_first>` mandates `LC_ALL=C awk 'NR==315{substr…}'` and bounded `sed -n` ranges; a full read is explicitly forbidden. Measured: this has killed an executor before. |
| T-28-33 | Tampering | `CookieCarrierInventoryTest` accessor set | high | mitigate | The new accessor lands together with its `MEASURED_CARRIER_SITES` entry and the total; the class cross-checks pinned map against pinned total against the live scan, and the non-vacuity probe proves the new pattern can go red. |
| T-28-34 | Information Disclosure | `AR-28-01` — the `Evidence:` line | medium | accept | Accepted by maintainer decision at 28-03's blocking checkpoint and explicitly NOT reopened (`28-CONTEXT.md` `already_known`). Named in the amended cell so this round's closure cannot be read as covering it. |
| T-28-35 | Spoofing | The absent repository-wide producer gate | medium | accept | D-28-06 records it CONSIDERED AND NOT TAKEN. Accepted with the residual stated in the register, in `ScannerIssueSupport.kt` (plan 28-05) and in the SUMMARY. A third producer added later is caught by nothing, and that is written down rather than implied away. |
| T-28-SC | Tampering | package-manager installs | n/a | accept | This plan installs NOTHING and modifies no dependency file. No Package Legitimacy Gate applies. |
</threat_model>

<artifacts_this_phase_produces>
| Symbol / file | Kind | Where |
|---|---|---|
| `AR-27-08` second supersession block | register text | `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` line 315, appended before the column separator |
| RUN 2 raw-output block | record | `.planning/phases/28-.../28-03-SUMMARY.md`, replacing the instruction block at `:542-580` |
| A new `COOKIE_BYTE_ACCESSORS` key + `AccessorSpec` for `AuditInsertionPoint.baseValue()` | test constant | `redact/CookieCarrierInventoryTest.kt` |
| A `ROUTED_THROUGH` entry for `(scanner/AiScanCheck.kt, <new accessor>)` | test constant | idem |
| Updated `MEASURED_CARRIER_SITES` + `EXPECTED_TOTAL_CARRIER_SITES` | test constants | idem |
| Second supersession on `ISSUE_DETAIL_CARRIER_DISPOSITION` | test constant | idem |
| `28-06-SUMMARY.md` | record | phase 28 directory |

NOT produced, and named so nobody looks for it: a repository-wide detail-line-producer gate. D-28-06
records it CONSIDERED AND NOT TAKEN; `WR-01` remains OPEN after this round.
</artifacts_this_phase_produces>

<verification>
1. `LC_ALL=C awk 'NR==315{printf "%s", substr($0,1,8693)}' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | shasum -a 256` prints `8dc326ac23204becce687deeba867740eb2d4dde21346c58d7da9595d137ae2e` after the edit.
2. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.*' --tests 'com.six2dez.burp.aiagent.scanner.*'` — green apart from the known `RedactionTest` wall-clock flake, which must pass in isolation.
3. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt` — both exit 0; `detekt-baseline.xml` byte-unchanged.
4. `git diff --stat -- .planning/REQUIREMENTS.md src/main/kotlin/com/six2dez/burp/aiagent/scanner/AdaptivePayloadEngine.kt` prints nothing.
5. PRIV-05 GATE, RUN 1 (in-task) and RUN 2 (post-completion, after `phase.complete 28`, immediately before the phase commit): `shasum -a 256 .planning/REQUIREMENTS.md` and `grep -n 'PRIV-05' .planning/REQUIREMENTS.md`, raw outputs recorded under TWO DISTINCT headings in `28-06-SUMMARY.md`. REQUIRE the digest still begins `9b3219662ec0d007` and line 23 is still an unchecked box. If either moved: DO NOT COMMIT — revert the file, re-run both commands to confirm the revert, and record the incident as a new `.planning/WINDOWS.md` entry in the style of entry 54.
6. Do NOT run `./gradlew check` as a gate — RED at HEAD for a maintainer-accepted coverage-floor reason.
</verification>

<success_criteria>
- `AR-27-08`'s cell states the disposition chosen at the checkpoint, names its authority, inventories
  the controls by symbol and probe, and names every route and gap it does NOT cover.
- The prior 8693-byte prefix of that row is byte-identical, proven by a digest taken before the edit.
- `threats_open` is recomputed with raw output recorded, not restated.
- The RUN 2 gate is discharged with raw outputs and its lateness disclosed.
- The carrier registry no longer asserts a closure that is false of the block; the accessor that hid
  route 2 is machine-checked and proven non-vacuous.
- `.planning/REQUIREMENTS.md` is byte-unchanged and PRIV-05 remains unchecked.
- No artifact in this round claims a repository-wide producer gate exists.
</success_criteria>

<output>
Create `.planning/phases/28-the-issue-detail-cookie-carrier-auditissue-detail-scanner-is/28-06-SUMMARY.md` when done.
</output>
