---
phase: 23-edt-confinement-ui-responsiveness
plan: 05
type: execute
wave: 4
depends_on: [23-01, 23-02, 23-03, 23-04]
files_modified:
  - src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelEdtConfinementTest.kt
  - .planning/phases/23-edt-confinement-ui-responsiveness/23-HUMAN-UAT.md
  - .planning/phases/23-edt-confinement-ui-responsiveness/23-VALIDATION.md
autonomous: true
requirements: [REL-05]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No regression in the REL-01 EDT-confinement guarantees: ChatPanel.assertEdt() (:783-787) and all six of its call sites are byte-identical to HEAD, and the five @GuardedBy(\"EDT\") session maps are still touched only on the EDT. Phase 26 / QUAL-07 owns assertEdt(); its unchanged state is SC5's cheapest and most direct evidence, exactly as Phase 22 produced for the same contract (SC5, AI-SPEC E5)"
    - "The SwingUtilities.invokeLater count in ChatPanel.kt is stated against the measured HEAD baseline of 11 (lines 647, 658, 673, 715, 723, 741, 1909, 1926, 2190, 2268, 2283) with a one-line reason for each addition — the same evidence shape 22-07-SUMMARY.md:95 used (SC5)"
    - "MainTab's existing Thread + SwingUtilities.invokeLater health-check idiom is what this phase reused: no coroutine outside the mcp package, no SwingWorker, no ChatPanel-owned ExecutorService, no new concurrency idiom. The one correction on top of MainTab.kt:193 is that isDaemon is set explicitly, following MontoyaHttpTransport.kt:85 (SC6, D-05, CONVENTIONS.md:95)"
    - "All three new suites — ChatPanelEdtConfinementTest, McpToolExecutorEdtGuardTest, SettingsSaveAsyncTest — actually execute under ./gradlew test -PexcludeHeavyTests=true, which is what .github/workflows/build.yml:47 runs on the macOS/Linux/Windows matrix. A suite carrying one of the five excluded suffixes would ship green having never run on the one platform matrix where an EDT difference could surface"
    - "detekt-baseline.xml is byte-identical to HEAD. It is signature-keyed and pinned at 1096 entries as the v0.10.0 milestone metric Phase 26 improves against; a widened signature invalidates an entry and a regenerated baseline moves the metric the wrong way"
    - "The four residuals this phase cannot assert headlessly are recorded in 23-HUMAN-UAT.md rather than dropped: the D-12 JOptionPane save-failure modal, FLAG-23-01's disabled-Save affordance under Burp's Look-and-Feel, FLAG-23-04's sub-frame Send/Cancel flicker on the auto-approved path, and research assumption A1's live confirmation of Burp's EDT sendRequest exception"
  artifacts:
    - path: ".planning/phases/23-edt-confinement-ui-responsiveness/23-HUMAN-UAT.md"
      provides: "The four live-Burp verification items this phase cannot assert headlessly, each with a concrete instruction and a stated pass condition"
      contains: "FLAG-23-01"
      min_lines: 30
  key_links:
    - from: "src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelEdtConfinementTest.kt"
      to: "src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt"
      via: "the SC5 structural assertion reads ChatPanel.kt from disk, which is already a declared tasks.test input at build.gradle.kts:197-200 so it re-runs when the source text changes"
      pattern: "assertEdt"
---

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<build_invocation>
EVERY Gradle invocation in this plan MUST be prefixed:
`JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew <tasks>`
Gradle 8.12.1 breaks on this machine's default JDK 25. A missing prefix fails for the wrong reason.
</build_invocation>

<objective>
Close SC5 and SC6, record the four residuals, and run the phase gate.

SC5 asks for something unusual: evidence that a file did **not** change. `assertEdt()`
(`ChatPanel.kt:783-787`) and its six call sites staying byte-identical is the cheapest and most direct
proof that REL-01's EDT-confinement contract did not regress while the *work* moved off the EDT —
Phase 22 produced exactly this evidence for the same contract, and Phase 26 / QUAL-07 owns that method,
so this phase deliberately leaves it alone and adds an inverse check elsewhere instead.

SC6 asks that `MainTab`'s existing idiom was reused rather than a new concurrency idiom introduced.
That is a claim about what is absent, and the phase gate is where it gets stated with its evidence.

Purpose: REL-05 / SC5 / SC6 — the two success criteria that are proven by what did not happen, plus the
phase-specific gates the standard `ktlintCheck detekt test` run does not cover.
Output: the SC5 structural assertion; `23-HUMAN-UAT.md`; a completed `23-VALIDATION.md` per-task map;
the phase gate.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-VALIDATION.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-01-SUMMARY.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-02-SUMMARY.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-03-SUMMARY.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
The full phase-level list is in `23-01-PLAN.md` §"Artifacts this phase produces". This plan creates:

- `.planning/phases/23-edt-confinement-ui-responsiveness/23-HUMAN-UAT.md` (new file)

It adds no production symbol.
</artifacts_this_phase_produces>

<multi_source_coverage_audit>
Every item from all four source artifacts, mapped to the plan that covers it. No item is MISSING.

## GOAL — the ROADMAP phase goal

| Item | Status | Plan |
|---|---|---|
| "The Burp UI stays responsive during an agent tool chain" | COVERED | 23-01 (chain), 23-02 (both user sites), 23-04 (exits) |
| "…and during a Settings save" | COVERED | 23-03 |
| SC1 `executeTool` never on the EDT from the chain, asserted by a test, not relying on `-ea` | COVERED | 23-01 (dispatch), 23-02 (throwing guard + `McpToolExecutorEdtGuardTest`) |
| SC2 `sendRequest` and the external `runBlocking` both off the EDT | COVERED | 23-01 (S-02), 23-02 (guard precedes the `ext:` early return) |
| SC3 8-iteration chain leaves the UI repainting; results in order; land on the EDT | COVERED | 23-01 Task 2 |
| SC4 Settings save does not block the EDT on the bounded 10 s wait, nor on `settingsRepo.save()` / `backends.reload()` | COVERED | 23-03 |
| SC5 no REL-01 regression | COVERED | 23-05 Task 1 |
| SC6 reuse `MainTab`'s idiom, no new concurrency idiom | COVERED | 23-01 (implementation), 23-05 Task 2 (stated evidence) |

## REQ — `REQUIREMENTS.md`

| Item | Status | Plan |
|---|---|---|
| REL-05 | COVERED | all five plans carry it in `requirements` |

## RESEARCH — `23-RESEARCH.md` findings, patterns, pitfalls and open questions

| Item | Status | Plan |
|---|---|---|
| F-1 no executor test double (`McpToolExecutor` is an `object`) | COVERED | 23-01 — the observer hangs off `OffEdtDispatch`, not a mock |
| F-2 `McpToolContext` is an immutable EDT snapshot | COVERED | 23-03 Task 2 (E8 reasoning rests on it) |
| F-3 limiter contention is unreachable; E10 inverted | COVERED | 23-04 Task 3 |
| F-4 the guard must precede the `ext:` early return | COVERED | 23-02 Task 2 + the `ext:` guard test |
| F-5 `deleteSession` does not call `cancelInFlightRequest` | COVERED | 23-04 Task 1 |
| F-6 `ToolCallOutcome` needs a fourth value; both consumers fall through | COVERED | 23-01 Task 1 |
| F-7 detekt baseline is signature-keyed and pinned at 1096 | COVERED | 23-01/02/03/04 acceptance criteria + 23-05 phase gate |
| F-8 only four files are declared `tasks.test` inputs | COVERED | 23-03 Task 1 (adds declarations if the fallback fires); 23-04 Task 3 (uses the already-declared `ChatPanel.kt`) |
| F-9 `AuditLogger.registerGlobalEmitter` install/clear precedent | COVERED | 23-01 Task 1, 23-02 Task 3 |
| F-10 `AuditLogger.enabled` is `@Volatile` | COVERED | 23-03 Task 2 (E8 reasoning) |
| P-1 the off-EDT dispatch helper | COVERED | 23-01 Task 1 |
| P-2 the throwing door guard | COVERED | 23-02 Task 2 |
| P-3 the supersede tracker | COVERED | 23-01 Task 1, 23-04 Task 1 |
| P-4 the Settings busy seam, two-layer `finally` | COVERED | 23-03 Task 3, tested in Task 4 |
| Pitfalls 1–10 | COVERED | 1 → 23-05 Task 2; 2 → 23-02 Task 2; 3 → 23-01 Task 1 criterion; 4 → 23-04 Task 3; 5 → 23-04 Task 2; 6 → 23-01 Task 1; 7 → 23-01 Task 2 + 23-03 Task 4; 8 → every plan's baseline criterion; 9 → 23-03 Task 1; 10 → 23-04 Task 1 |
| Open Question 1 the worker-aware await | COVERED | 23-01 Task 1 |
| Open Question 2 deterministic SC3 | COVERED | 23-01 Task 2 |
| Open Question 3 detecting EDT-ness (two capture points) | COVERED | 23-01 Task 1 (call level), 23-02 Task 3 (door level) |
| Open Question 4 settings atomicity / E8 | COVERED | 23-03 Task 2 (checkpoint) + Task 3 + Task 4 |
| Open Question 5 the `BottomTabsPanel` seam and A2 | COVERED | 23-03 Tasks 1 and 3 |
| Open Question 6 wave decomposition and the tracer | COVERED | this plan set follows it |
| A1 Burp's EDT `sendRequest` exception, not live-confirmed | COVERED | 23-01 Task 2 (test does not depend on it) + 23-05 Task 2 (UAT item) |
| A2 `SettingsPanel` headless constructibility | COVERED | 23-03 Task 1 (spike, with a documented fallback) |
| A3 extraction keeps both functions under `LongMethod` | COVERED | 23-01 Task 1, 23-03 Task 3, both with a baseline criterion |
| A4 `EXECUTING` needs no consumer change | COVERED | 23-01 Task 1 (KDoc mitigation) |
| A5 maintainer accepts the E8 residual | COVERED | 23-03 Task 2 (checkpoint) |
| A6 `isEventDispatchThread()` is headless-safe | COVERED | 23-02 Task 2 (stated in the guard's justification comment) |
| Validation Architecture: three suite names, the sampling rates, the Wave 0 list | COVERED | 23-01/02/03 create them; 23-05 Task 2 completes `23-VALIDATION.md` |
| Security Domain: V1, V3, V4, V7, V8 and the eight threat patterns | COVERED | the five `<threat_model>` blocks, T-23-01 … T-23-13 |

## CONTEXT — `23-CONTEXT.md` locked decisions

| Item | Status | Plan |
|---|---|---|
| D-01 all three call sites move | COVERED | 23-01 (chain), 23-02 (dialog + `/tool`) |
| D-02 helper AND door guard | COVERED | 23-01 (helper), 23-02 (guard) |
| D-03 the guard throws | COVERED | 23-02 Tasks 1–2 |
| D-04 guard at `executeToolResult`, same commit as the moves | COVERED | 23-02 Task 2 |
| D-05 daemon `Thread` per call plus `invokeLater`, `isDaemon` explicit, thread named | COVERED | 23-01 Task 1 |
| D-06 panel goes busy for the duration, reusing `setSendingState(true)` | COVERED | 23-01 Task 1 (chain), 23-02 Task 2 (both user sites) |
| D-07 cancel stops the chain and discards; the audit event still fires | COVERED | 23-01 Task 3 |
| D-08 session delete, project change and unload use the same supersede path | COVERED | 23-04 Task 1 |
| D-09 busy state stays global to `ChatPanel` | COVERED | 23-01 Task 1 |
| D-10 Save disables while in flight; every other field editable | COVERED | 23-03 Task 3 |
| D-11 whole body to one worker; the Swing tail marshals back | COVERED | 23-03 Task 3 |
| D-12 failure keeps banner and modal | COVERED | 23-03 Task 3, bounded by Rule T-3 and 23-05's UAT item |
| D-13 one async path, both callers, per-caller copy | COVERED | 23-03 Task 3 |
| D-14 `KtorMcpServerManager.stop()` stays blocking | COVERED | 23-03 (byte-identity criterion) |
| Claude's Discretion — enforcement mechanism vs `assertEdt()` | COVERED | 23-02 (separate check), 23-05 Task 1 (`assertEdt()` byte-identity) |
| Claude's Discretion — deterministic SC3 | COVERED | 23-01 Task 2 |
| Claude's Discretion — transcript between approval and result | COVERED | closed by UI-SPEC §"The Open Question, Answered"; Rule S-4 implemented in 23-02 Task 2 |

## Deferred and out of scope — NOT gaps

`MontoyaHttpTransport.execute`'s offload-and-block; the non-`@Volatile` scanner settings fields; the
dead `isSending` field; `ExternalMcpClientManager`'s `runBlocking` (not EDT-reachable); upgrading
`assertEdt()` (Phase 26 / QUAL-07); the `ChatPanel.kt` mega-file split; a per-call progress indicator;
and the chat-path Burp-scope asymmetry (`McpToolContext.scopeOnly` defaults `false` and
`ChatPanel.buildToolContext` never passes it — a candidate SEC-family requirement, triage only, must
not be fixed inside Phase 23). All are excluded by `23-CONTEXT.md` §Deferred and are not planning gaps.

## Result

**0 MISSING.** Every GOAL, REQ, RESEARCH and CONTEXT item is COVERED by a plan.
</multi_source_coverage_audit>

<tasks>

<task type="auto">
  <name>Task 1: SC5 — prove REL-01 did not regress, with the evidence a diff can carry</name>

  <files>
    src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelEdtConfinementTest.kt
  </files>

  <precondition>Plans 23-01 through 23-04 are merged, so `ChatPanel.kt` is in its final phase state before the counts are taken.</precondition>

  <read_first>
    - `src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` lines 780-790 — `assertEdt()` and its KDoc, which states the method is a no-op in production and that the EDT-confinement test is the real gate.
    - `src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` lines 110-125 — the five `@GuardedBy("EDT")` maps.
    - `src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` lines 318-347 — `functionBody(...)`, the source-text assertion helper this task reuses.
    - `build.gradle.kts` lines 188-201 — the existing `inputs.file` declaration for `ChatPanel.kt`, and the comment explaining the 22-09 stale-cache defect. No new declaration is needed for this task.
    - `.planning/phases/22-agent-tool-call-trust-boundary/22-07-SUMMARY.md` around line 95 — the `invokeLater` count evidence shape Phase 22 produced for the same contract.
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-UI-SPEC.md` §"States Summary" — the measured HEAD baseline of 11 `invokeLater` sites and 6 `assertEdt()` call sites.
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-AI-SPEC.md` §5 dimension E5.
  </read_first>

  <action>
Add the SC5 evidence to `ChatPanelEdtConfinementTest` as assertions, not as prose in a summary, so a
future edit that quietly reopens the contract goes red.

**1. `assertEdt()` byte-identity.** Using the existing `functionBody("private fun assertEdt()")`
helper, assert the method body still contains its `SwingUtilities.isEventDispatchThread()` check and
its REL-01 message about session maps being touched on the EDT only. Assert the call-site count is
still exactly 6. Write the assertion message so it explains *why* an unchanged file is the evidence:
Phase 26 / QUAL-07 owns this method, this phase adds an inverse check elsewhere, and byte-identity is
the cheapest proof that the confinement contract did not move.

`ChatPanel.kt` is already a declared `tasks.test` input (`build.gradle.kts:197-200`), so a source-text
assertion on it re-runs when the source text changes even if the bytecode does not. Do **not** add a
new declaration for this task — and do not read any other main file here, because that would need one.

**2. The `invokeLater` count.** Assert the current count of `SwingUtilities.invokeLater` sites in
`ChatPanel.kt` equals a named constant carrying the phase's final number, and write that constant's
KDoc as the justification ledger: the HEAD baseline was 11 (`:647, 658, 673, 715, 723, 741, 1909,
1926, 2190, 2268, 2283`) and each addition is one line of "what it is for". A bare number with no
reason is not evidence; a number that fails loudly when someone adds an unexplained marshalling point
is.

**3. No worker-side guarded-map access (E5).** Extend the S-01 scenario's thread capture: while the
tool double is executing on the worker, assert from that thread that no read of `sessionPanels`,
`sessionStates`, `sessionsById`, `sessionDrafts` or `pendingDecisions` occurred — the practical form is
to assert that the values the tail uses came from the `ToolCallCapture` record rather than from a map
lookup, plus a source assertion that `finishApprovedToolCall`'s worker-side lambda body references none
of the five map names. Both halves, because the source assertion alone can pass on a renamed field and
the behavioural half alone can pass by luck of timing.

Do not convert any of the five maps to a thread-safe collection. Phase 17's REL-01 explicitly declined
that, and doing it here would replace the contract rather than preserve it.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*ChatPanelEdtConfinementTest' --tests '*ChatPanelToolGateTest'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'assertEdt()' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` returns `6`.
    - `git diff $(git merge-base HEAD main)..HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt | grep -cE '^[-+].*(fun assertEdt|off-EDT access is a data race)'` returns `0`.
    - `grep -c 'assertEdt' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelEdtConfinementTest.kt` returns at least `1`.
    - Behavior: a test asserts the `SwingUtilities.invokeLater` count in `ChatPanel.kt` equals a named constant, and that constant's KDoc lists one justification line per addition above the HEAD baseline of 11.
    - Behavior: a test asserts that `finishApprovedToolCall`'s worker-side lambda body contains none of `sessionPanels`, `sessionStates`, `sessionsById`, `sessionDrafts`, `pendingDecisions`.
    - `grep -cE 'ConcurrentHashMap|Collections.synchronizedMap' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` returns `0` — the guarded maps were not converted.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*ChatPanelEdtConfinementTest'` exits 0.
  </acceptance_criteria>

  <done>
    SC5 is proven by assertion rather than by claim: `assertEdt()` is untouched, the marshalling-point
    count is justified line by line, and no worker reads a guarded map.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — the PR-gate filter, the pinned baseline, the human residuals, and the validation map</name>

  <files>
    .planning/phases/23-edt-confinement-ui-responsiveness/23-HUMAN-UAT.md,
    .planning/phases/23-edt-confinement-ui-responsiveness/23-VALIDATION.md
  </files>

  <read_first>
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-VALIDATION.md` — the whole file, especially the per-task map whose `Task ID` and `Plan` columns are placeholders to be filled in now that the plans exist, and the Validation Sign-Off checklist.
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-VALIDATION.md` §"Manual-Only Verifications" — the four items and their instructions, which `23-HUMAN-UAT.md` restates in runnable form.
    - `.planning/phases/22-agent-tool-call-trust-boundary/22-HUMAN-UAT.md` — the house format for a UAT file: one scenario per item, a concrete instruction, a stated pass condition, and an unchecked box.
    - `.github/workflows/build.yml` lines 38-55 and `build.gradle.kts` lines 201-215 — what CI runs and what the filter excludes.
    - `.planning/STATE.md` lines 50-60 — the pinned milestone baselines (coverage 34% line / 23% branch; detekt baseline 1096 entries).
    - The four plan SUMMARYs, for the recorded decisions this task consolidates: the A2 spike outcome, the E8 answer, the FLAG-23-01 recolor choice, and the FLAG-23-03 model-history choice.
  </read_first>

  <action>
**1. Run the phase gate.** The standard `ktlintCheck detekt test` run does not cover the two properties
that would let this phase ship green while being void:

- `./gradlew test -PexcludeHeavyTests=true` — read the executed-test counts out of the test report and
  confirm all three new suites appear with a non-zero count. This is the direct check for the naming
  trap: `.github/workflows/build.yml:47` passes that flag, `build.gradle.kts:206-213` filters five
  suffixes, and `ChatPanelConcurrencyTest` is already in the excluded set — which makes the wrong name
  the natural one to have reached for. A suite reporting zero here has the wrong name and must be
  renamed, never exempted.
- `git diff --stat detekt-baseline.xml` — must produce no output. It is signature-keyed and pinned at
  1096 entries as the v0.10.0 milestone metric Phase 26 improves against. If detekt reports a new
  finding, extract a function; never regenerate the baseline.

**2. Create `23-HUMAN-UAT.md`** with the four items, in the house format used by
`22-HUMAN-UAT.md` — one scenario each, a concrete instruction, a stated pass condition, an unchecked
box:

- **D-12 — the save-failure modal.** Force a settings save failure in a running Burp. Pass condition:
  both the inline banner and the `JOptionPane` appear, and the banner text matches the modal text.
  Why manual: `JOptionPane.getRootFrame()` throws `HeadlessException`, measured and recorded in
  `CONCERNS.md`.
- **FLAG-23-01 — does the disabled Save button read as disabled?** Trigger a slow save and observe the
  recolored button in both light and dark themes. Pass condition: it reads as visibly inert. Record the
  outcome either way: if plain disabling already read correctly, the recolor may be dropped — but the
  choice must be recorded, not defaulted by omission, and `isEnabled = false` stays mandatory because
  it is what closes the double-save race.
- **FLAG-23-04 — the sub-frame Send/Cancel flicker.** Run an 8-iteration chain with `AUTO`-tier tools
  and watch for a Send-button flash between steps. This is an **observation item, not a pass/fail
  gate** — it is the phase's one accepted unresolved residual (see `23-01-PLAN.md`
  §planner_assumptions), and the fix would restructure `maybeExecuteToolCall` in a file this phase must
  not restructure.
- **A1 — does Burp really throw on an EDT `sendRequest`?** Call `http1_request` from chat against
  pre-fix code and record the actual exception text. Nothing in the automated suite depends on this:
  the S-02 test configures the Montoya *double* to throw, so it pins "we do not call Burp's HTTP API
  from the EDT" regardless. What is at stake is only the framing of SC2 as a defect rather than a
  latency.

**3. Complete `23-VALIDATION.md`.** Fill the `Task ID` and `Plan` columns of the per-task map from the
five plans now that they exist; set `wave_0_complete: true`; set `nyquist_compliant: true` once every
row has an automated command and no three consecutive tasks lack one; tick the Validation Sign-Off
checklist, including the two phase-specific boxes (new suites confirmed to run under
`-PexcludeHeavyTests=true`, and `detekt-baseline.xml` unchanged). Leave `status: draft` — `validate-phase`
sets `validated`.

**4. Record the SC6 statement in the SUMMARY**, with its evidence: no coroutine was introduced outside
the `mcp` package; none of the Swing background-worker classes the anti-pattern table in
`23-PATTERNS.md` names was used; `ChatPanel` still owns no `ExecutorService`; the shape is `MainTab`'s
`Thread` plus `SwingUtilities.invokeLater`, with the one correction that `isDaemon` is set explicitly
because `MainTab.kt:193` does not set it and D-08's unload guarantee depends on it.

**5. Consolidate the decisions** made across the phase into the SUMMARY so a reader does not have to
open four files: the A2 spike outcome, the E8 answer and its residual sentence, the FLAG-23-01 recolor
choice, and the FLAG-23-03 model-history choice.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt test -PexcludeHeavyTests=true</automated>
  </verify>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt test` exits 0.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test -PexcludeHeavyTests=true` exits 0, and the test report shows non-zero executed counts for `ChatPanelEdtConfinementTest`, `McpToolExecutorEdtGuardTest` and `SettingsSaveAsyncTest`.
    - `git diff --stat detekt-baseline.xml` produces no output.
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-HUMAN-UAT.md` exists and contains the four markers `D-12`, `FLAG-23-01`, `FLAG-23-04` and `A1`, each with an instruction line and a pass condition.
    - `grep -c 'TBD' .planning/phases/23-edt-confinement-ui-responsiveness/23-VALIDATION.md` returns `0`.
    - `grep -c 'nyquist_compliant: true' .planning/phases/23-edt-confinement-ui-responsiveness/23-VALIDATION.md` returns `1` and `grep -c 'wave_0_complete: true' <same file>` returns `1`.
    - The SUMMARY states the SC6 evidence (no coroutine outside `mcp`, no `SwingWorker`, no `ChatPanel`-owned executor) and the four consolidated decisions.
    - `grep -c 'SwingWorker' src/main/kotlin/com/six2dez/burp/aiagent/ui/*.kt` returns `0`, and `grep -rc 'kotlinx.coroutines' src/main/kotlin/com/six2dez/burp/aiagent/ui/ | grep -v ':0$'` produces no output.
  </acceptance_criteria>

  <done>
    The phase gate is green, the three new suites are confirmed to run on the platform matrix, the
    milestone metrics are unmoved, the four residuals a headless test cannot reach are recorded rather
    than dropped, and `23-VALIDATION.md` carries a complete per-task map.
  </done>
</task>

</tasks>

<threat_model>
ASVS level 1; block on `high`. Sourced from `23-RESEARCH.md` §"Security Domain".

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EDT ↔ the five `@GuardedBy("EDT")` session maps | REL-01's integrity control (ASVS V3). This phase moves work off the EDT while the maps stay confined; SC5 is the evidence that the boundary held. |
| the PR gate ↔ the shipped artifact | A suite that does not run on the cross-platform matrix is not a control, however green it looks locally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-13 | Tampering | the five `@GuardedBy("EDT")` maps, if a later edit reopens them to worker access | high | mitigate | Task 1 asserts both halves: `finishApprovedToolCall`'s worker-side body references none of the five map names, and `assertEdt()` plus its six call sites are byte-identical. The maps are not converted to concurrent collections — Phase 17's REL-01 declined that explicitly. |

No package-manager install task exists in this plan; `23-RESEARCH.md` §"Package Legitimacy Audit"
records zero packages added, so no supply-chain row is warranted.
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt test` exits 0.
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test -PexcludeHeavyTests=true` exits 0 with all three new suites executing.
- `git diff --stat detekt-baseline.xml` produces no output.
- `grep -c 'assertEdt()' ChatPanel.kt` returns 6 and the method body is byte-identical to HEAD.
- `23-HUMAN-UAT.md` exists with four items; `23-VALIDATION.md` has no remaining `TBD`.
</verification>

<success_criteria>
- SC5 is asserted, not claimed: `assertEdt()` untouched, the `invokeLater` count justified per addition, no worker-side guarded-map access.
- SC6 is stated with its evidence: `MainTab`'s idiom reused, `isDaemon` set explicitly, no new concurrency idiom anywhere.
- All three new suites execute under the PR-gate filter on the cross-platform matrix.
- `detekt-baseline.xml` is byte-identical; the v0.10.0 milestone metric is unmoved.
- The four residuals a headless test cannot reach are in `23-HUMAN-UAT.md`, and the phase's four recorded decisions are consolidated in one place.
</success_criteria>

<output>
Create `.planning/phases/23-edt-confinement-ui-responsiveness/23-05-SUMMARY.md` when done.
</output>
