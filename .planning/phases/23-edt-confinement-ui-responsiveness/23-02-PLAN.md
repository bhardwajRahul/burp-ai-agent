---
phase: 23-edt-confinement-ui-responsiveness
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorEdtGuardTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelEdtConfinementTest.kt
autonomous: false
requirements: [REL-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "All three ChatPanel executeTool call sites — ChatPanel.kt:1010 (ToolCallOrigin.UserDialog), :2319 (ToolCallOrigin.UserSlashCommand) and :2856 (the SEC-06-gated chain) — dispatch through OffEdtDispatch. SC1 names only the chain; D-01 rejects that on the ground that the two user-originated sites are expected to FAIL outright for HTTP tools, so leaving them is leaving a bug"
    - "McpToolExecutor.executeToolResult throws IllegalStateException when entered on the Swing EDT, in a build with -ea OFF. This is the substantive answer to SC1's requirement that the new check must not rely on -ea, since ChatPanel.assertEdt() is assert-based and a no-op in shipped Burp (D-03, D-04, AI-SPEC E2)"
    - "The guard is the FIRST statement of executeToolResult, before canonicalToolId and before the ext: early return at McpToolExecutorImpl.kt:145-148, so routeExternalToolCall's runBlocking at :1126 is covered too. SC2's runBlocking clause rides on that one-line placement (research F-4)"
    - "The guard and all three call-site moves land in ONE commit. ChatPanelToolGateTest.slashCommandPathIsNotDoublePrompted (:350) reaches executeToolResult on the EDT today via ChatPanelTestHarness.sendUserMessage's invokeAndWait (:193): guard-first turns it red, moves-first leaves the guard with nothing to catch (D-04)"
    - "The /tool path echoes its command in the existing 'You' channel before dispatch, so no tool result can appear in the transcript without its request being visible. Today the typed command survives on screen only because the EDT is frozen mid-execution (UI-SPEC Rule S-4, partial-state consideration)"
    - "Both user-originated sites enter UI-SPEC state S3 on the EDT before dispatch. Neither calls setSendingState today, so leaving them idle-and-live would let a user fire /tool and immediately press Send (UI-SPEC Rule S-2)"
    - "A Deny produces zero executeToolResult invocations and starts no worker — the SEC-06 trust boundary is evaluated on the EDT before any dispatch, and this phase changes where an approved call runs, never whether it runs (AI-SPEC E1, S-03)"
    - "openToolDialog keeps its parameterless signature, handleToolCommand keeps its five-parameter Boolean signature, and the SC5 comments at ChatPanel.kt:1008-1010 and :2317-2318 survive verbatim — userDialogPathIsNotDoublePrompted asserts on that source text and detekt-baseline.xml:886/:889 key ReturnCount suppressions off those exact signatures"
  prohibitions:
    - "The EDT guard at the executor door must never be softened to a log-and-proceed or to a debug-time-only check. D-03 rejects both by name: a log nobody reads is not a control, and a check that fires only in tests leaves the field — where the real slow tools live — as the half that never enforces."
  artifacts:
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorEdtGuardTest.kt"
      provides: "Scenario S-10: executeToolResult invoked directly from SwingUtilities.invokeAndWait throws IllegalStateException with -ea off, including the ext:-prefixed variant proving the guard precedes the early return"
      contains: "class McpToolExecutorEdtGuardTest"
      min_lines: 40
  key_links:
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt"
      to: "javax.swing.SwingUtilities"
      via: "check(!SwingUtilities.isEventDispatchThread()) as the first statement of executeToolResult — a deliberate ui-flavoured import into the mcp package, justified in a comment"
      pattern: "isEventDispatchThread"
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt"
      to: "com.six2dez.burp.aiagent.ui.OffEdtDispatch"
      via: "openToolDialog and handleToolCommand's /tool branch each dispatch their own McpToolExecutor.executeTool call through OffEdtDispatch.run, keeping the call lexically inside the function the structural test reads"
      pattern: "OffEdtDispatch\\.run"
---

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<build_invocation>
EVERY Gradle invocation in this plan MUST be prefixed:
`JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew <tasks>`
Gradle 8.12.1 breaks on this machine's default JDK 25. A missing prefix fails for the wrong reason.
</build_invocation>

<objective>
Close SC1 completely. Move the two user-originated `executeTool` call sites off the EDT and install the
throwing door guard at `McpToolExecutor.executeToolResult` — **in one commit**, because D-04's
sequencing constraint makes any other order broken by construction.

D-02 is the shape: *the helper makes it work; the guard makes it stay.* Plan `23-01` delivered the
helper on one path. This plan finishes the moves and locks the door behind them, so a fourth
parse-and-execute path added later cannot silently reintroduce the defect — exactly the gap Phase 22's
SC5 closed for the trust boundary, applied to the threading one.

Purpose: REL-05 / SC1 / SC2 — `executeTool` is never invoked on the EDT from any path, asserted by a
test that fires in shipped Burp rather than by an `-ea`-gated assertion.
Output: the guard as the first statement of `executeToolResult`; `openToolDialog` and
`handleToolCommand`'s `/tool` branch dispatched off-EDT with S3 and the Rule S-4 echo;
`McpToolExecutorEdtGuardTest` (S-10); `slashCommandPathIsNotDoublePrompted` made worker-aware.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-CONTEXT.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-RESEARCH.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-PATTERNS.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-UI-SPEC.md
@.planning/phases/23-edt-confinement-ui-responsiveness/23-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
The full phase-level list is in `23-01-PLAN.md` §"Artifacts this phase produces". This plan creates:

- `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorEdtGuardTest.kt` (new file)
- `McpToolExecutorEdtGuardTest` (new class)

It adds no new production symbol. The guard is a new statement inside an existing function, and both
user-originated call sites reuse `OffEdtDispatch.run` and `setSendingState`, which already exist.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the throwing door guard — a one-way behaviour change for every caller</name>

  <decision>
Install a throwing EDT guard as the first statement of `McpToolExecutor.executeToolResult`
(`McpToolExecutorImpl.kt:137`)?
  </decision>

  <context>
**Why this is rated `one-way` and therefore gated.** `executeToolResult` is the convergence point for
*everything* REL-05 names: the three chat call sites, the MCP-server path
(`McpToolHandlers.kt:129`), `routeExternalToolCall`'s `runBlocking` (`:1126`) and both
`api.http().sendRequest` sites (`:191`, `:237`). A `check(...)` there changes behaviour for **every**
caller, including shipped Burp. If a call site is ever missed — now or in a future phase — a tool that
previously froze the UI instead returns an `IllegalStateException`. The chain site's `runCatching`
turns that into a tool error in the transcript rather than a crash, which bounds the blast radius but
does not remove it.

**What CONTEXT already locks, and is not being re-opened:** D-03 locks *throw*, and rejects both
`logToError`-and-proceed (the no-op assert in a different costume) and throw-in-tests /
log-in-production (a new tunable, two behaviours, and the field is the half that never enforces).
D-04 locks the placement at `executeToolResult` rather than `executeTool`, and locks the
single-commit sequencing.

**What research adds:** the guard must be the **first** statement, before `canonicalToolId` and before
the `ext:` early return at `:145-148`, or SC2's `runBlocking` clause is unmet (F-4). And it imports
`javax.swing.SwingUtilities` into a file under `mcp/` — a deliberate trade, not a layering accident;
`isEventDispatchThread()` is a pure thread comparison with no AWT initialisation, so it is
headless-safe (A6).

**Verified before asking:** exactly one caller reaches `executeToolResult` on the EDT today, and it is
a test — `ChatPanelToolGateTest.slashCommandPathIsNotDoublePrompted` (`:350`), via the harness's
`invokeAndWait` (`ChatPanelTestHarness.kt:193`). No production caller other than the three D-01 sites
was found on the EDT. Task 3 of this plan fixes that test in the same commit.
  </context>

  <options>
    <option id="door-guard">
      <name>A — RECOMMENDED, and what D-03/D-04 lock: a throwing EDT precondition as the first statement of executeToolResult</name>
      <pros>Fires in shipped Burp, where the JVM debug-time assertion facility does nothing. One line covers the chat path, the MCP-server path and the `ext:` branch, because all three converge here. Free for the server path, which already runs on Ktor coroutines off the EDT.</pros>
      <cons>Changes behaviour for every caller. A call site missed now or in a future phase returns a tool error instead of freezing — bounded by the chain site's existing `runCatching`, but real.</cons>
    </option>
    <option id="wrapper-guard">
      <name>B — guard only the chat wrapper executeTool (:1052)</name>
      <pros>Smaller blast radius; the MCP-server path cannot possibly trip it.</pros>
      <cons>Covers only the chat half. `executeTool` is a text-extracting wrapper over `executeToolResult`, so the `ext:` branch, `routeExternalToolCall`'s `runBlocking` and `McpToolHandlers.kt:129` all escape it. Contradicts D-04's verified placement.</cons>
    </option>
    <option id="helper-only">
      <name>C — no guard; rely on the OffEdtDispatch helper alone</name>
      <pros>Zero behaviour change for any existing caller.</pros>
      <cons>Rejected by D-02: nothing stops a fourth parse-and-execute path calling `executeToolResult` directly, and the guarantee reverts from "enforced" to "correct today" — exactly the gap Phase 22's SC5 closed for the trust boundary.</cons>
    </option>
  </options>

  <recommendation>
A. It is what D-03 and D-04 lock, the sequencing constraint is already solved by landing it with Task 2
in one commit, and the alternative to a control a future edit can silently undo is not a smaller
control — it is no control.
  </recommendation>

  <resume-signal>Reply with the option id — `door-guard`, `wrapper-guard` or `helper-only` — or describe a different placement. Execution resumes at Task 2 on the chosen option.</resume-signal>

  <read_first>
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-CONTEXT.md` §decisions D-02, D-03, D-04 in full, including D-04's "confirm before locking" paragraph.
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-RESEARCH.md` §"Pattern P-2" and finding F-4.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt` lines 137-160 and 1040-1075.
  </read_first>

  <verify>
    <automated>test -f .planning/phases/23-edt-confinement-ui-responsiveness/23-02-SUMMARY.md || echo "decision recorded in the task log; SUMMARY records the chosen option and the rationale"</automated>
  </verify>

  <acceptance_criteria>
    - The chosen option id (`door-guard`, `wrapper-guard` or `helper-only`) is recorded verbatim in `23-02-SUMMARY.md` with the date and who chose it.
    - If any option other than `door-guard` is chosen, the SUMMARY records which locked decision (D-03 or D-04) is being overridden and by what reasoning — a locked decision is never silently dropped.
    - No source file is edited by this task.
  </acceptance_criteria>

  <done>
    The guard's placement and failure mode are confirmed by the maintainer and recorded. Task 2 proceeds
    on the confirmed option.
  </done>
</task>

<task type="auto">
  <name>Task 2: The throwing door guard and the two user-originated call sites — one commit</name>

  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt
  </files>

  <precondition>Task 1's decision checkpoint has been answered and recorded; this task implements the confirmed option.</precondition>

  <reversibility rating="one-way">
    The guard changes behaviour for every caller of `executeToolResult`, including the MCP-server path
    and shipped Burp. A missed call site converts a UI freeze into an `IllegalStateException` in the
    field. Gated by Task 1.
  </reversibility>

  <read_first>
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt` lines 1-60 (the import block and the `object McpToolExecutor` declaration at `:45`), 137-160 (`executeToolResult`, the `ext:` early return, the descriptor lookup and the Pro-edition early return), 1040-1075 (`executeTool`, the wrapper, and the `@Suppress` paragraph at `:1045-1051` whose comment voice the guard's justification must match), and 1120-1130 (`routeExternalToolCall`'s `runBlocking`).
    - `src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` lines 960-1020 (`openToolDialog` in full, including the `commandPreview` echo at `:1004-1005` and the SC5 comment at `:1008-1010`), and 2290-2326 (`handleToolCommand` in full, including the `/tool` branch and its SC5 comment at `:2317-2318`).
    - `src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` lines 326-362 — `userDialogPathIsNotDoublePrompted` reads `functionBody("fun openToolDialog()")` and asserts on **source text**: it must still contain `McpToolExecutor.executeTool(` and `ToolCallOrigin.UserDialog`, and must NOT contain `ToolApprovalGate` or `ToolApprovalCard`.
    - `detekt-baseline.xml` lines 884, 886, 889 — the three `ReturnCount` entries keyed off verbatim signatures.
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-UI-SPEC.md` Rules S-2 and S-4, and FLAG-23-03.
    - `.planning/codebase/CONVENTIONS.md` lines 186-192 (`IllegalStateException` for logical precondition failures) and 216-221 (comment style; no temporal language).
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-01-SUMMARY.md` — the `OffEdtDispatch.run` signature as actually landed.
  </read_first>

  <action>
Two edits, one commit. Neither is optional and neither works without the other.

**1. `McpToolExecutorImpl.kt` — the door guard.**

Insert, as the **first statement** of `fun executeToolResult(name, argsJson, context): CallToolResult`
at `:137` — before `val resolvedName = canonicalToolId(name)` and therefore before the `ext:` early
return at `:145-148` — a `check(!SwingUtilities.isEventDispatchThread()) { … }` whose message names
REL-05 and points the caller at `OffEdtDispatch`. `check` produces `IllegalStateException`, which is
what CONVENTIONS.md:190 specifies for logical precondition failures and what D-03 locks.

Placement is load-bearing: `routeExternalToolCall`'s `runBlocking` at `:1126` is reached through that
early return, so a guard placed after it leaves SC2's `runBlocking` clause unmet. Put it first.

Do **not** use the JVM's debug-time assertion statement here. It is disabled without `-ea`, i.e. in
every shipped Burp, and SC1 rules it out by name.

Add a comment above the guard in the voice this file already uses at `:1045-1051` — a short paragraph
explaining why the unusual thing is the right thing. It must cover: why the door and not the wrapper
(`executeTool` at `:1052` merely extracts text from this function's result, and the MCP-server path at
`McpToolHandlers.kt:129` calls this function directly); why it precedes the `ext:` early return; and
why `javax.swing.SwingUtilities` appears in a file under `mcp/` — `isEventDispatchThread()` is a pure
thread comparison that initialises no AWT state, so it is headless-safe, and the door is where the
guarantee belongs. A reviewer must be able to read the import as deliberate.

Add no guard to `executeTool` (`:1052`).

**2. `ChatPanel.kt` — `openToolDialog` (`:969`).**

The `McpToolExecutor.executeTool(invocation.toolId, args, context, ToolCallOrigin.UserDialog)` call
must stay **lexically inside `openToolDialog`'s body** — move it into the `work = { … }` lambda passed
to `OffEdtDispatch.run`, do not extract it into a shared helper.
`ChatPanelToolGateTest.userDialogPathIsNotDoublePrompted` reads this function's source text from disk
and asserts the substrings `McpToolExecutor.executeTool(` and `ToolCallOrigin.UserDialog` are present;
hoisting the call out of the function makes that test red for the wrong reason.

Preserve the three-line SC5 comment at `:1008-1010` **verbatim** — same bytes, same position relative
to the call. It is the comment that records why this path is deliberately ungated.

Before dispatch, on the EDT: keep the existing `commandPreview` echo (`:1004-1005`) exactly as it is,
then call `setSendingState(true)` (UI-SPEC Rule S-2) and register a supersede token with
`runningTool.set(...)` the same way `executeApprovedToolCall` does. Dispatch through
`OffEdtDispatch.run` with `threadName = "burp-ai-tool-exec"` and a label identifying this invocation.

In the `onEdt` tail, on the EDT: check the supersede token; on success append
`panel.addMessage("Tool result: ${invocation.toolId}", result)`, add the assistant `ChatMessage`, set
`state.toolsMode = true`, `state.toolCatalogSent = true`, `refreshSessionList()`, and
`setSendingState(false)`; on failure surface the error through the same `"Tool result: …"` row with the
`Error:` prefix the chain path already uses. Carry the `Result` across the boundary intact.

Keep the signature `fun openToolDialog()` parameterless — `detekt-baseline.xml:886` keys a
`ReturnCount` suppression off it verbatim.

**3. `ChatPanel.kt` — `handleToolCommand`'s `/tool` branch (`:2306-2324`).**

Same shape, plus one addition. Keep the signature
`private fun handleToolCommand(text, sessionId, panel, state, settings): Boolean` byte-identical —
`detekt-baseline.xml:889` keys off the whole verbatim parameter list.

Keep the two SC5 comment lines at `:2317-2318` verbatim, and keep the
`McpToolExecutor.executeTool(toolName, argsJson, context, ToolCallOrigin.UserSlashCommand)` call
lexically inside this function.

**Rule S-4 — echo the command before going async.** The `/tool` branch calls no
`panel.addMessage("You", …)` today, and `sendFromInput` clears `inputArea.text` and returns at
`:525-526` without echoing. Synchronously the typed command survives on screen only because the EDT is
frozen; asynchronously the transcript would be blank until `"Tool result:"` appeared — a result with no
visible request, in a tool whose users must be able to answer "what did I just run?". Add
`panel.addMessage("You", trimmed)` before dispatch, mirroring what `openToolDialog` already does at
`:1004` with its `commandPreview`. Reuse `SessionPanel.addMessage`; add no widget and no string
template.

**FLAG-23-03 is this plan's call, and the answer is yes:** also append the command to
`session.messages` as a user `ChatMessage`, matching `openToolDialog` at `:1005`. That changes what the
model sees, not what the user sees, and it removes a pre-existing asymmetry between the two
user-originated paths rather than preserving it. Record the choice in the SUMMARY.

The strings `"Usage: /tool <name> <json>"` (`:2312`) and `"MCP server is not running."` (`:972`) are
validation and precondition checks. They stay synchronous and stay **before** dispatch; do not move
them into the worker.

**Both edits go in one commit.** Guard first alone turns `slashCommandPathIsNotDoublePrompted` red;
moves first alone leaves the guard with nothing to catch. Task 3 updates that test in the same commit.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*ChatPanelToolGateTest' --tests '*ChatPanelEdtConfinementTest' --tests '*McpToolParityTest'</automated>
  </verify>

  <acceptance_criteria>
    - `awk '/    fun executeToolResult\(/,/^    }$/' src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt | grep -vE '^\s*(//|\*|/\*)' | head -8 | grep -c 'isEventDispatchThread'` returns `1` — the guard is inside the first few non-comment statements.
    - Behavior: the line number of the `isEventDispatchThread` check inside `executeToolResult` is strictly less than the line number of `if (resolvedName.startsWith("ext:"))`, verified with `grep -n` on the two patterns.
    - `awk '/    fun executeToolResult\(/,/^    }$/' src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt | grep -vE '^\s*(//|\*|/\*)' | grep -c 'check('` returns at least `1`.
    - `grep -c 'fun openToolDialog()' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` returns `1`; `grep -c 'private fun handleToolCommand(' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` returns `1`; and `awk '/private fun handleToolCommand\(/,/\): Boolean \{/' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt | grep -cE 'text: String|sessionId: String|panel: SessionPanel|state: ToolSessionState|settings: AgentSettings'` returns `5` — the whole baselined parameter list, asserted positively. The `HEAD~1` diff form this replaces was unreliable: this plan has four tasks and Task 3 commits after Task 2, so `HEAD~1` is not dependably the pre-change commit and an empty diff passed.
    - `awk '/fun openToolDialog\(\)/,/^    }$/' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt | grep -c 'McpToolExecutor.executeTool('` returns `1` and the same range contains `ToolCallOrigin.UserDialog` and contains neither `ToolApprovalGate` nor `ToolApprovalCard`.
    - `awk '/private fun handleToolCommand\(/,/^    }$/' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt | grep -c 'McpToolExecutor.executeTool('` returns `1`, and the same range contains `addMessage("You"` before the `OffEdtDispatch.run` line.
    - `grep -c 'OffEdtDispatch.run' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` returns `3` — one per D-01 call site.
    - The SC5 comment blocks survive: `grep -c 'SC5: user-originated' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` returns `2` — the measured HEAD count, one at `:1007` and one at `:2317`. Positive, so deleting or rewording either goes red; the `HEAD~1` diff form this replaces could not, for the same commit-topology reason as the criterion above.
    - `grep -c '<ID>' detekt-baseline.xml` returns `1096` — the pinned v0.10.0 entry count is unmoved; a regenerated baseline moves it; `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` exits 0.
    - The guard and the two call-site moves appear in a single commit: `git show --stat HEAD` lists both `McpToolExecutorImpl.kt` and `ChatPanel.kt`.
  </acceptance_criteria>

  <done>
    `executeToolResult` refuses to run on the EDT and says so with an `IllegalStateException`; all three
    chat call sites dispatch through `OffEdtDispatch`; both structural SC5 assertions still hold; the
    detekt baseline is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Make the existing SC5 slash-command test worker-aware, and prove the guard fires (S-10)</name>

  <files>
    src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorEdtGuardTest.kt
  </files>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` **as `23-01` left it** — line numbers below are HEAD's and will have shifted. `slashCommandPathIsNotDoublePrompted` at `:348-362`, whose `verify(h.api.proxy(), times(1)).history()` at `:358` fires immediately after `drainEdt()` and becomes racy the moment `/tool` goes async; and the `@BeforeEach`/`@AfterEach` pair at `:638-653`, which `23-01` step 7 already extended with `installSettledObserver()` / `releaseSettledObserver()` beside the `AuditLogger.registerGlobalEmitter` install/clear.
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-01-PLAN.md` §Task 1 step 7 — the enumerated ten-test repair `23-01` landed in this class, so this task can tell what is already done from what is left.
    - `src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt` — `awaitToolSettled`, `installSettledObserver` and `releaseSettledObserver` as landed by plan `23-01`.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt` lines 137-160 and 1120-1130, and `src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt` lines 19-48 — the context data class the guard test must construct.
    - `src/test/kotlin/com/six2dez/burp/aiagent/ui/SettingsDefaultsPersistenceTest.kt` line 68 area — the in-memory `Preferences` fake, if a settings-derived context is needed.
    - `build.gradle.kts` lines 140-165 — `tasks.test`'s `jvmArgs`, specifically whether `-ea` is enabled for the whole test task.
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-RESEARCH.md` Open Question 3 (two capture points, deliberately) and finding F-4.
  </read_first>

  <action>
**1. Fix `slashCommandPathIsNotDoublePrompted` (`:350`) — and only that test — in the same commit as Task 2.**

`installSettledObserver()` and `releaseSettledObserver()` are **already** in this class's `@BeforeEach`
and `@AfterEach`: `23-01` step 7 installed them there together with the repair of the ten tests that
ride the chain call site. Confirm they are present and add no second pair.

Replace the bare `drainEdt()` at `:356` that precedes `verify(h.api.proxy(), times(1)).history()` with
`ChatPanelTestHarness.awaitToolSettled(count = 1)`. This is the one test in the class whose repair had
to wait for this plan, and the reason is narrow: it is the `/tool` site, and the `/tool` site does not
move until Task 2 above. The race it closes is specific and worth naming in a comment: it lives in the
window **between the worker returning and its `invokeLater` tail running**, which `drainEdt` cannot see
because it drains the EDT queue and knows nothing about a daemon worker.

Do not weaken the test's claim. It must still assert `times(1)` on `history()` and still assert
`findApprovalCard` returns null — SC5's "the user typed this call themselves; it must not be gated"
is Phase 22's contract and this phase does not touch it.

**No sweep.** `23-01` enumerated the rest of the class by name and closed it;
`userDialogPathIsNotDoublePrompted` is a pure source-text assertion and needs no change. If some other
test in this class still looks racy after this edit, that is a `23-01` defect to report in the SUMMARY,
not a discretionary fix to make here.

**2. New file `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorEdtGuardTest.kt`.**

The class name is load-bearing: `.github/workflows/build.yml:47` runs
`./gradlew test -PexcludeHeavyTests=true` and `build.gradle.kts:206-213` filters five suffixes out
under that flag. `McpToolExecutorEdtGuardTest` is the approved name and it runs on the PR gate.

Two tests, both calling `McpToolExecutor.executeToolResult` directly from inside
`SwingUtilities.invokeAndWait`, and both asserting the thrown cause is an `IllegalStateException` whose
message names REL-05:

- a **built-in** tool id (for example `proxy_http_history`), proving the guard covers the ordinary path;
- an **`ext:`-prefixed** tool id, proving the guard precedes the early return at `:145-148` and
  therefore covers `routeExternalToolCall`'s `runBlocking` at `:1126`. Without the guard this call
  returns the typed `"External MCP client not available"` string instead of throwing, so the assertion
  distinguishes the two placements rather than passing either way (finding F-4).

`invokeAndWait` wraps a throwing runnable in an `InvocationTargetException`; unwrap `cause` before
asserting the type, following `MontoyaHttpTransport.kt:87-89`'s `throw e.cause ?: e` precedent for the
same JDK behaviour.

Add a third test asserting the same call **off** the EDT does not throw the guard exception — it may
return an error result for an unknown tool, but it must reach past the guard. Without this, a guard
that threw unconditionally would pass the first two.

**Prove the guard fires with `-ea` off**, which is SC1's explicit requirement. Read
`build.gradle.kts`'s `tasks.test` `jvmArgs`: if `-ea` is enabled task-wide, the JUnit assertion alone
does not demonstrate the claim. Either add a `jvmArgs("-da:com.six2dez.burp.aiagent.mcp.tools...")`
scoped disable for this suite, or register a small dedicated `Test` task with assertions disabled that
runs only this class, and document which was chosen and why in the SUMMARY. Do not simply assert and
declare the requirement met — `check(...)` is not the JVM assertion facility, but the phase's whole
argument for it rests on that distinction being demonstrated rather than asserted.

**3. Scenario S-03 in `ChatPanelEdtConfinementTest`** — a `CONFIRM`-tier tool call, card shown, **Deny**
clicked. Assert zero tool executions reached the Montoya double, no worker thread was captured, that
`ChatPanelTestHarness.dispatchedLabels()` carries **no** entry for this call's trace id, and that the
denial result was returned to the model so the conversation continues.

Read the **dispatch** record, not the settle record. `23-01` step 7(b) establishes why and this test
has exactly the same exposure: a settle event is recorded from the EDT tail's `finally`, so in the
world this assertion exists to catch — a denial that dispatched a worker anyway — the settle event is
absent at assert time too, because the worker has not finished. `dispatchedLabels()` is written
synchronously with the dispatch statement, so its absence means no worker was ever started. This is AI-SPEC E1's PASS clause: this phase changes where an approved call runs, never
whether it runs.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolExecutorEdtGuardTest' --tests '*ChatPanelToolGateTest' --tests '*ChatPanelEdtConfinementTest'</automated>
  </verify>

  <acceptance_criteria>
    - `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorEdtGuardTest.kt` exists and `grep -c 'class McpToolExecutorEdtGuardTest' <file>` returns `1`.
    - `grep -c 'ext:' src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorEdtGuardTest.kt` returns at least `1` — the F-4 placement test exists.
    - `grep -c 'IllegalStateException' src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorEdtGuardTest.kt` returns at least `2`.
    - `grep -c 'awaitToolSettled' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` returns `6` — the five `23-01` landed plus this plan's one — and `grep -c 'times(1)).history()' <same file>` still returns `1`.
    - `awk '/fun slashCommandPathIsNotDoublePrompted/,/^    }$/' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt | grep -c 'awaitToolSettled'` returns `1` — the await is inside THIS test, not merely somewhere in the file.
    - `grep -c 'installSettledObserver' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` returns `1` and `grep -c 'releaseSettledObserver' <same file>` returns `1` — still exactly one pair, so this task did not add a second.
    - `grep -c 'dispatchedLabels' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` still returns `5` and `grep -c 'never()).history()' <same file>` still returns `5` — `23-01`'s no-dispatch clauses survived this task's edit.
    - Behavior: the SUMMARY records how the `-ea`-off demonstration was arranged (scoped `-da` argument or a dedicated `Test` task) and shows the command output proving the guard threw with assertions disabled.
    - Behavior: the S-03 Deny test asserts zero interactions with the tool-side Montoya stub and that `dispatchedLabels()` carries no entry for the denied call's trace id — the dispatch record, not the settle record, because an unfinished worker is missing from the settle record too.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test -PexcludeHeavyTests=true --tests '*McpToolExecutorEdtGuardTest'` reports at least 3 executed tests.
    - The full existing suite is green: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` exits 0.
  </acceptance_criteria>

  <done>
    The guard is proven to fire from the EDT with assertions disabled, on both the built-in and the
    `ext:` path; the pre-existing SC5 slash-command test is worker-aware and still makes its full claim;
    a denied call still starts nothing.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wave gate — full suite, PR-gate filter, and the detekt baseline</name>

  <files>
    src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelEdtConfinementTest.kt
  </files>

  <read_first>
    - `.planning/phases/23-edt-confinement-ui-responsiveness/23-VALIDATION.md` §"Sampling Rate" — the per-wave and phase-gate commands.
    - `build.gradle.kts` lines 201-215 — the `excludeHeavyTests` filter.
    - `.github/workflows/build.yml` lines 38-55 — what CI actually runs on the cross-platform matrix.
  </read_first>

  <action>
Close the wave. Run the full local gate and confirm the two phase-specific properties the standard gate
does not cover.

Confirm every new suite really executes under the PR-gate filter by running the test task with
`-PexcludeHeavyTests=true` and reading the executed-test counts out of
`build/reports/tests/test/index.html` (or the XML results under `build/test-results/test/`). A suite
that reports zero executed tests under that flag has a name matching one of the five excluded suffixes
and must be renamed, not exempted.

Confirm `detekt-baseline.xml` is byte-identical to HEAD. If detekt reports a new finding, extract a
function or a helper rather than regenerating the baseline: it is pinned at 1096 entries as the
v0.10.0 milestone metric that Phase 26 improves against (STATE.md:56-58), and `LongMethod`'s threshold
is 80 with `executeApprovedToolCall` at ~83 source lines before this phase's extraction.

Record in the SUMMARY: the current `SwingUtilities.invokeLater` count in `ChatPanel.kt` against the
measured HEAD baseline of 11, with a one-line reason for each addition; and confirmation that
`grep -c 'assertEdt()'` still returns 6 with the method body untouched.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt test</automated>
  </verify>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt test` exits 0.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test -PexcludeHeavyTests=true` exits 0, and the test report shows non-zero executed counts for `ChatPanelEdtConfinementTest` and `McpToolExecutorEdtGuardTest`.
    - `grep -c '<ID>' detekt-baseline.xml` returns `1096` — the pinned v0.10.0 entry count is unmoved; a regenerated baseline moves it.
    - `grep -c 'assertEdt()' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` returns `6`.
    - The SUMMARY states the new `SwingUtilities.invokeLater` count in `ChatPanel.kt` and gives a reason per addition against the HEAD baseline of 11.
  </acceptance_criteria>

  <done>
    Wave 2's chat half is green locally and under the PR-gate filter, with the milestone metrics
    unmoved.
  </done>
</task>

</tasks>

<threat_model>
ASVS level 1; block on `high`. Sourced from `23-RESEARCH.md` §"Security Domain".

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → `ToolApprovalGate` | The SEC-06 control. Evaluated on the EDT before any dispatch; this plan must not move, weaken or re-enter it. |
| any caller → `executeToolResult` | The convergence point for the chat path, the MCP-server path, the `ext:` branch and both `sendRequest` sites. The new guard is a precondition on this boundary. |
| user-typed `/tool` args → the executor | User-originated and deliberately ungated (Phase 22 SC5). Untrusted only in the sense that the args are free text; the user is the author. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04 | Elevation of Privilege | a future fourth parse-and-execute path calling `executeToolResult` directly | high | mitigate | The throwing guard at the door refuses the EDT for every caller, so a new call site cannot silently reintroduce the defect. `McpToolExecutorEdtGuardTest` proves it fires with `-ea` off. |
| T-23-05 | Elevation of Privilege | time-of-check/time-of-use between `ToolApprovalGate` producing `Run` and the worker starting | high | mitigate | `approved.origin`, `.tier` and `.decision` are captured on the EDT before dispatch; the worker re-evaluates nothing. Asserted by S-03 (a Deny starts no worker) and by E1's exactly-once clause. |
| T-23-06 | Denial of Service | a missed call site turning a UI freeze into an `IllegalStateException` in shipped Burp | medium | accept | Accepted cost, recorded in D-03. Bounded by the chain site's existing `runCatching` (`ChatPanel.kt:2856`), which renders it as a tool error in the transcript rather than a crash, and by Task 1's decision checkpoint which puts the trade in front of the maintainer before it lands. |

No package-manager install task exists in this plan; `23-RESEARCH.md` §"Package Legitimacy Audit"
records zero packages added, so no supply-chain row is warranted.
</threat_model>

<verification>
- All three D-01 call sites dispatch through `OffEdtDispatch.run` — `grep -c 'OffEdtDispatch.run' ChatPanel.kt` returns 3.
- The guard is the first non-comment statement of `executeToolResult` and precedes the `ext:` early return.
- `McpToolExecutorEdtGuardTest` passes with assertions disabled, demonstrating SC1's "must not rely on `-ea`" clause rather than asserting it.
- `ChatPanelToolGateTest` is fully green, including `slashCommandPathIsNotDoublePrompted` and `userDialogPathIsNotDoublePrompted`.
- `grep -c '<ID>' detekt-baseline.xml` returns `1096` — the pinned v0.10.0 entry count is unmoved; a regenerated baseline moves it.
</verification>

<success_criteria>
- SC1 is closed: `McpToolExecutor.executeTool` is never invoked on the EDT from any of the three chat paths, and the check that enforces it fires in shipped Burp.
- SC2's `runBlocking` clause is closed by placement, not by hope: the `ext:`-prefixed guard test would fail if the guard moved one line down.
- The `/tool` transcript shows the command before it shows the result.
- Both structural SC5 assertions and all three baselined signatures survive unchanged.
</success_criteria>

<output>
Create `.planning/phases/23-edt-confinement-ui-responsiveness/23-02-SUMMARY.md` when done.
</output>
