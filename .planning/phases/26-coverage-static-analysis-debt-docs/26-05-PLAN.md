---
phase: 26-coverage-static-analysis-debt-docs
plan: 05
type: execute
wave: 2
depends_on: [26-04]
files_modified:
  - SECURITY.md
  - README.md
  - SPEC.md
  - docs/anthropic-backend.md
  - docs/external-mcp-servers.md
  - docs/ui-safety-guide.md
  - build.gradle.kts
  - src/test/kotlin/com/six2dez/burp/aiagent/SecurityDocsTest.kt
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-GITBOOK-HANDOFF.md
autonomous: false
requirements: [DOC-03, QUAL-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC5: `SECURITY.md` carries a `## Security Advisories` section with one entry for SEC-04 and one for PRIV-05. Each names the affected versions as 0.9.0, 0.9.1 and 0.9.2; states the concrete impact in terms of what an affected user's data or endpoint was exposed to; and names 0.10.0 as the fixed version."
    - "SC5: each advisory entry carries a USER ACTION line. PRIV-05's says to treat session cookies that passed through passive AI scanning on an affected version with a third-party backend as disclosed to that backend and to rotate them. SEC-04's says to treat an externally-reachable MCP listener on an affected version as having been unauthenticated."
    - "SC5: no CVE or GHSA identifier is invented. If none has been issued, the advisory says so explicitly rather than leaving a reader to assume one exists."
    - "SC5: `SECURITY.md`'s Supported Versions table no longer claims 0.5.x is the supported line — it names the versions this project actually supports today."
    - "SC6: `README.md` and `SPEC.md` each describe the tool-call confirmation flow — that a tool call parsed out of MODEL OUTPUT does not reach Burp without a user decision, that the three tiers are automatic / confirm-with-session-approval / confirm-every-time, that an unrecognised tool name fails closed to confirm-every-time, and that the decision is audit-logged."
    - "SC6: `README.md` and `SPEC.md` state `SecretCipher`'s at-rest guarantee accurately — the AES-256-GCM master key is stored beside the ciphertext in Burp Preferences, so this is obfuscation against casual inspection of a preferences export, NOT protection against a local attacker who can read those preferences."
    - "SC6: the absolute claim `no plaintext in preferences` no longer appears in `README.md`, because the master key itself is stored there Base64-encoded. The replacement states what is true rather than deleting the sentence."
    - "SC6: `docs/anthropic-backend.md` and `docs/external-mcp-servers.md`, which carry the same absolute at-rest claim, are corrected in the same pass, so the repo does not ship a corrected README beside two uncorrected pages."
    - "SC6: `docs/ui-safety-guide.md` tells the operator what the tool-confirmation card is and what each of its four actions means, because that runbook is where a user looks when the extension asks them to approve something."
    - "SC6: the claims above are pinned by `SecurityDocsTest`, which reads the markdown files from disk, and `build.gradle.kts` declares each of those files as a `tasks.test` input so a documentation-only edit cannot be served from the build cache with the guard never running — the same defect the existing `adrRecord` declaration exists to prevent."
    - "SC6 GitBook half: the out-of-repo `burp-ai-agent-docs` site is handled by a prepared, file-by-file diff in `26-GITBOOK-HANDOFF.md` plus a blocking human-action checkpoint. No plan in this repository writes to, commits to, or pushes that repository."
    - "The handoff names the five GitBook pages that currently carry a stale or absolute claim, quoting the CURRENT text of each so the operator can verify the diff applies to what is actually there."
  artifacts:
    - "SECURITY.md — `## Security Advisories` with the SEC-04 and PRIV-05 entries; corrected Supported Versions table; corrected Security Model at-rest statement"
    - "README.md — corrected at-rest claims plus the tool-call confirmation flow"
    - "SPEC.md — corrected at-rest claims in §4.4 and §9, the confirmation gate in §6 and §9, and an acceptance test for the gate in §10"
    - "docs/anthropic-backend.md, docs/external-mcp-servers.md — corrected at-rest claims"
    - "docs/ui-safety-guide.md — a `## Tool-Call Confirmation` section"
    - "src/test/kotlin/com/six2dez/burp/aiagent/SecurityDocsTest.kt — the automated guard on all of the above"
    - "build.gradle.kts — `tasks.test` input declarations for SECURITY.md, README.md, SPEC.md and the three docs pages"
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-GITBOOK-HANDOFF.md — the prepared out-of-repo diff"
  key_links:
    - "`SECURITY.md` advisory -> the v0.9.0-v0.9.2 users who are the reason DOC-03 exists: the milestone's own framing is that SEC-04 and PRIV-05 were confirmed by RUNNING the shipped code, so the people running it are owed a statement"
    - "`SecurityDocsTest` -> `build.gradle.kts` inputs: a markdown-only edit produces byte-identical compiled output, so without the input declarations the guard is served from cache in exactly the commit that breaks it — measured and recorded in this build for `DECISIONS.md`"
    - "`26-GITBOOK-HANDOFF.md` -> the human-action checkpoint -> `burp-ai-agent-docs`: this repository's plans cannot reach that repository, so the honest shape is a prepared diff handed to the operator, not a commit that silently does not happen"
  prohibitions:
    - statement: "The advisory must not understate impact, hedge the affected version range, or omit the user action. A security advisory whose main effect is to make the maintainer look diligent is worse than no advisory, because it consumes the reader's attention without changing what they do."
      severity: high
    - statement: "Do not invent a CVE, GHSA or any other identifier that has not been issued. A fabricated identifier makes the advisory unverifiable and undermines every true statement beside it."
      severity: high
    - statement: "Do not resolve the overstated at-rest claim by DELETING the sentence. SC6 asks for an accurate statement, not for silence; a removed claim reads to a returning user as though the property still holds and simply went undocumented."
      severity: high
    - statement: "Do not write to, create files in, modify, commit to or push anything under the `burp-ai-agent-docs` checkout. It is a separate repository this phase does not own. Read it to quote its current text; change nothing."
      severity: high
    - statement: "Do not claim in any document a control that does not ship. Every sentence added here must be checkable against a symbol in this repository, and the SUMMARY must name the symbol behind each new claim."
      severity: high
---

<objective>
SC5 and the user-facing half of SC6 (DOC-03, plus QUAL-07's `SecretCipher` clause): publish the
advisory for the two live defects this milestone fixed, and make the shipped documentation describe
the tool-call confirmation flow and the real at-rest guarantee.

Purpose: SEC-04 and PRIV-05 were confirmed by running v0.9.2, not by reading it. Users on 0.9.0-0.9.2
have an unauthenticated MCP listener if they opted into external access, and have sent real session
cookies to a third-party model if they ran passive AI scanning. Nothing in the repository tells them.
Separately, `README.md` currently asserts `no plaintext in preferences` while the AES master key sits
in preferences Base64-encoded beside its own ciphertext, and neither `README.md` nor `SPEC.md`
mentions the SEC-06 confirmation gate that shipped in Phase 22.

Output: an advisory, corrected claims across six in-repo documents, an automated guard on all of it,
and a prepared out-of-repo diff for the GitBook site.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@AGENTS.md
</context>

## Why this plan is in wave 2

`26-04` may edit `build.gradle.kts` (its Option B extends `edtGuardWithoutAssertionsTest`'s filter) and
this plan certainly does (new `tasks.test` input declarations). Two plans editing one file in one wave
is the merge conflict the wave scheme exists to prevent, so this one waits. There is no content
dependency on `26-04`.

## Measured facts this plan is built on

Measured by the planner at commit `4f0ebd7` on 2026-08-22:

- `build.gradle.kts` version is `0.9.2`; `CHANGELOG.md`'s `## [Unreleased]` section is empty. **0.10.0
  is the fixed version and it is not yet released** — the advisory must say so rather than implying a
  published fix exists.
- `SECURITY.md`'s Supported Versions table names `0.5.x` and `< 0.5`. It has not been updated in four
  milestones.
- `README.md:15` states `no plaintext in preferences`; `README.md:211`, `SPEC.md:64` and `SPEC.md:152`
  each assert AES-256-GCM at-rest with a per-install master key and no caveat.
- `docs/anthropic-backend.md` (three occurrences) and `docs/external-mcp-servers.md` (two) carry the
  same absolute claim.
- `grep -rln 'SEC-06|Approve for session|approve_once'` over `docs/`, `README.md` and `SPEC.md`
  returns **nothing**. The confirmation gate shipped in Phase 22 and is documented only in
  `DECISIONS.md` ADR-15, which is a design record, not user documentation.
- `build.gradle.kts` `tasks.test` already declares `inputs.file("DECISIONS.md")` with property name
  `adrRecord`, and carries a comment recording the measured cache defect that declaration prevents.
  This plan follows that exact pattern.
- The GitBook site is a SEPARATE git repository at `~/Tools/burp-ai-agent-doc`
  (GitHub `burp-ai-agent-docs`). It is not this repository's `docs/` directory and no plan here can
  commit to it.

## Artifacts this phase produces

| Symbol / section | File | Note |
|---|---|---|
| `## Security Advisories` | `SECURITY.md` | SEC-04 and PRIV-05 entries |
| `## Tool-Call Confirmation` | `docs/ui-safety-guide.md` | operator-facing description of the card |
| `SecurityDocsTest` | new test file | the automated guard on the doc claims |
| six `inputs.file` declarations | `build.gradle.kts` | makes the guard non-cacheable |
| `26-GITBOOK-HANDOFF.md` | phase directory | prepared out-of-repo diff |

<tasks>

<task type="tracer">
  <name>Task 1: The SEC-04 and PRIV-05 advisory, with its automated guard and build wiring</name>

  <read_first>
    - SECURITY.md — the whole file. Note the Supported Versions table, the Reporting section, the Scope lists and the Security Model section.
    - .planning/REQUIREMENTS.md — the SEC-04 and PRIV-05 entries, which carry the precise technical statement of each defect.
    - .planning/ROADMAP.md, the `## Active Milestone: v0.10.0` preamble — the two bullets that state what was reproduced and how.
    - .planning/STATE.md `## Milestone Origin` — the reproduced observations, including the exact wrong response `POST /message` returned.
    - build.gradle.kts lines 160-200 — the existing `inputs.file("DECISIONS.md")` declaration with property name `adrRecord`, and the comment above it explaining the cache defect. Copy that pattern exactly.
    - src/test/kotlin/com/six2dez/burp/aiagent/DecisionsAdrTest.kt — `readProjectFile` and `adrSection`; the new guard reuses the same slice-then-assert shape and the same message discipline.
    - CHANGELOG.md — confirm that `## [Unreleased]` is still empty before writing anything about a released fix.
  </read_first>

  <files>
    SECURITY.md,
    src/test/kotlin/com/six2dez/burp/aiagent/SecurityDocsTest.kt,
    build.gradle.kts
  </files>

  <action>
    Add a `## Security Advisories` section to `SECURITY.md`, placed after `## Reporting a Vulnerability`
    and before `## Security Model`, containing two entries.

    **SEC-04 — MCP access-control checks did not run on resolved routes.** State the mechanism in one
    sentence: the access-control interceptor was registered after the routing block in Ktor's `Call`
    phase, and Ktor runs same-phase interceptors in registration order, so any route that resolved was
    served before the checks ran. State the observable consequence: with external MCP access enabled,
    an unauthenticated request to the message endpoint and an unauthenticated SSE connect both reached
    the MCP handler instead of being rejected; in local mode the Origin, Host and User-Agent checks and
    the security response headers did not apply to matched routes. State the reproduction that was
    actually observed — an unauthenticated `POST /message` returned a 400 complaining about a missing
    `sessionId` query parameter, which is the handler's own error, rather than a 401. State the
    precondition honestly: the server binds loopback by default, so the unauthenticated-listener
    exposure required the external-access opt-in, while the local-mode gap left the browser-origin
    defences inert. Affected: 0.9.0, 0.9.1, 0.9.2. Fixed in 0.10.0.

    **PRIV-05 — session cookies reached AI backends unredacted in STRICT and BALANCED.** State the
    mechanism: the passive scanner emitted a dedicated cookies section as bare name-and-value pairs,
    dropping the header prefix the redaction rule matched on, and sensitive-key matching was an exact
    match against a fixed list. State the observable consequence: cookie values named `JSESSIONID`,
    `PHPSESSID`, `connect.sid`, `auth_token`, `csrftoken` and `remember_me` were included in the prompt
    sent to the configured backend in both STRICT and BALANCED; only a cookie literally named `session`
    was caught. Affected: 0.9.0, 0.9.1, 0.9.2. Fixed in 0.10.0.

    Give each entry a bolded **User action** line. For PRIV-05: any session cookie that passed through
    passive AI scanning on an affected version with a non-local backend should be treated as disclosed
    to that provider and rotated. For SEC-04: any MCP listener that was reachable beyond loopback on an
    affected version should be treated as having accepted unauthenticated tool calls for as long as it
    was reachable, and the MCP token should be rotated.

    State plainly, once, that 0.10.0 is the fixing release and is not yet published at the time of
    writing, and that no CVE or GHSA identifier has been issued for either finding. Do not invent one.
    Link both entries to the existing private-reporting instructions above them.

    Correct the Supported Versions table: name 0.10.x as supported, 0.9.x as receiving security fixes
    only until 0.10.0 ships, and anything below 0.9 as unsupported. Do not leave 0.5.x in the table.

    Then write `SecurityDocsTest` in `src/test/kotlin/com/six2dez/burp/aiagent/`, beside
    `DecisionsAdrTest`, asserting on the SECURITY.md slice: the advisories section exists; it names
    SEC-04 and PRIV-05; each entry's slice contains the three version strings and the fixed version;
    each entry's slice contains a user-action line; and the Supported Versions table no longer names
    the stale 0.5 line. Every assertion carries a message explaining why the claim matters, in the
    style `DecisionsAdrTest` uses.

    Finally, declare `SECURITY.md` as a `tasks.test` input in `build.gradle.kts`, following the
    `adrRecord` declaration exactly — property name, path sensitivity, and a comment stating the reason
    in one line rather than repeating the whole existing explanation.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*SecurityDocsTest'` is green.
    - `grep -c 'SEC-04' SECURITY.md` and `grep -c 'PRIV-05' SECURITY.md` each return at least 1.
    - `grep -cE '0\.9\.0|0\.9\.1|0\.9\.2' SECURITY.md` returns at least 2.
    - `grep -c '0.10.0' SECURITY.md` returns at least 2.
    - `grep -c '0.5.x' SECURITY.md` returns 0.
    - `grep -c 'adrRecord' build.gradle.kts` returns 1 and `grep -c 'securityPolicy' build.gradle.kts` returns 1 — the new declaration exists beside the old one rather than replacing it.
    - CACHE PROBE, recorded with observed output: run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*SecurityDocsTest'`, then change one asserted word in `SECURITY.md`, re-run WITHOUT `cleanTest`, and record that the task re-executes and turns red rather than being served UP-TO-DATE. Restore with `git checkout HEAD -- SECURITY.md` if committed, otherwise by hand. Never `git stash`.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck` green and `git diff --quiet detekt-baseline.xml` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*SecurityDocsTest' detekt ktlintCheck</automated>
  </verify>

  <done>
    A user on 0.9.0-0.9.2 reading `SECURITY.md` learns what was wrong, what it exposed, which version
    fixes it and what to do now; an automated guard pins those claims; and the build re-runs that guard
    when the file changes, proven by a recorded cache probe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the at-rest claims and document the tool-call confirmation flow</name>

  <read_first>
    - README.md — line 15 (`no plaintext in preferences`), line 135, line 211, and the `### Privacy and Security Notes` and `## Highlights` sections in full.
    - SPEC.md — §4.4 (line 64), §6 (lines 119-126), §9 (line 152) and §10.
    - docs/anthropic-backend.md and docs/external-mcp-servers.md — every occurrence of the at-rest claim.
    - docs/ui-safety-guide.md — the whole file; it is short, and `## MCP Safety` is where the new section sits beside.
    - src/main/kotlin/com/six2dez/burp/aiagent/config/SecretCipher.kt — the class KDoc and `MASTER_KEY_PREF_KEY`. Every sentence written about the at-rest guarantee must be checkable against this file.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/ToolApprovalGate.kt — `tierFor`, `evaluate`, `resolve`, and the `SecTier` values. Every sentence written about the confirmation flow must be checkable against this file and `McpToolCatalog`.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolCatalog.kt — the `SecTier` KDoc and the AUTO definition, so the documented tier meanings match the shipped ones.
    - DECISIONS.md ADR-15 — the design record. Do NOT edit it; plan 26-06 owns that file. Read it for accuracy only.
    - src/test/kotlin/com/six2dez/burp/aiagent/SecurityDocsTest.kt — the class Task 1 created; extend it.
    - Read every file you are about to modify in full before editing it.
  </read_first>

  <files>
    README.md,
    SPEC.md,
    docs/anthropic-backend.md,
    docs/external-mcp-servers.md,
    docs/ui-safety-guide.md,
    src/test/kotlin/com/six2dez/burp/aiagent/SecurityDocsTest.kt,
    build.gradle.kts
  </files>

  <action>
    **At-rest accuracy.** Replace every absolute claim with an accurate one. The true statement is:
    stored secrets are encrypted with AES-256-GCM using a per-install random master key, and that
    master key is itself stored in Burp Preferences beside the ciphertext — so the encryption protects
    against casual inspection of a preferences file or an export, and does NOT protect against an
    attacker or a process that can read those preferences. Say that once, fully, in `README.md`'s
    Privacy and Security Notes and in `SPEC.md` §9, and have the shorter mentions
    (`README.md` Highlights, `SPEC.md` §4.4, both `docs/` pages) state the encryption plus a
    one-clause caveat and point at the full statement. Remove the words `no plaintext in preferences`
    by REPLACING that clause with what is true, not by deleting the bullet.

    **Tool-call confirmation flow.** Add it to `README.md`'s Privacy and Security Notes and to
    `SPEC.md` §6 and §9. The statement to make, all of it checkable against `ToolApprovalGate` and
    `McpToolCatalog`: a tool call that the extension parses out of model output does not execute
    against Burp until the user decides; every tool carries a required security tier; the tiers are run
    automatically, confirm-with-an-approve-for-this-chat option, and confirm-every-time; a tool name
    the catalog does not recognise resolves to confirm-every-time rather than to automatic; external
    `ext:`-namespaced tools always confirm every time; the decision surfaces as a card in the chat
    transcript rather than a modal; and every decision is audit-logged. State also that this tier is
    independent of the Unsafe Mode switch — Unsafe Mode governs whether a tool may ever run, the tier
    governs whether the model may run it without asking. In `SPEC.md` §6, the sentence that currently
    describes tools as gated behind the Unsafe Mode master switch must be amended, not merely
    supplemented, because as written it names one axis where two now exist. Add one line to `SPEC.md`
    §10 acceptance tests covering the gate.

    **Operator runbook.** Add a `## Tool-Call Confirmation` section to `docs/ui-safety-guide.md` beside
    `## MCP Safety`: what the card looks like, what each of its actions means, that approve-for-session
    is scoped to one chat session and is cleared by Clear Chat, that approvals do not survive a Burp
    restart, and that a denial returns a neutral result to the model rather than an error. Keep it to
    the same numbered-list style the rest of that file uses.

    Do not restate ADR-15's threat model in user documentation; link the reasoning to `DECISIONS.md`
    ADR-15 the way `docs/mcp-hardening.md` links to ADR-16.

    Extend `SecurityDocsTest` with assertions over `README.md`, `SPEC.md` and `docs/ui-safety-guide.md`:
    each states the master-key-beside-ciphertext caveat; each describes the confirmation gate; the
    stale absolute phrase is absent from `README.md`. Add the corresponding `tasks.test`
    `inputs.file` declarations for every markdown file this test now reads, following the `adrRecord`
    pattern.

    In the SUMMARY, list each new documentation claim beside the symbol in this repository that makes
    it true. A claim with no symbol behind it is a claim to delete, not to soften.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*SecurityDocsTest'` is green and reports at least 12 tests.
    - `grep -c 'no plaintext in preferences' README.md` returns 0.
    - `grep -ci 'burp preferences' README.md` returns at least 1 and `grep -ci 'burp preferences' SPEC.md` returns at least 1 — the caveat names where the master key lives.
    - `grep -cin 'confirm' README.md SPEC.md docs/ui-safety-guide.md` each return at least 1.
    - `grep -c 'inputs' build.gradle.kts` increases by exactly the number of markdown files the guard now reads, and `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*SecurityDocsTest'` re-runs rather than reporting UP-TO-DATE after a one-word edit to `SPEC.md` (recorded cache probe, restored afterwards).
    - The SUMMARY lists every new documentation claim beside the repository symbol that makes it true.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` reports 0 failures.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck` green and `git diff --quiet detekt-baseline.xml` exits 0.
    - `git diff --name-only | grep -c 'DECISIONS.md'` returns 0.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*SecurityDocsTest' detekt ktlintCheck</automated>
  </verify>

  <done>
    Six in-repo documents state the at-rest guarantee accurately and describe the tool-call
    confirmation flow; every new claim is traceable to a symbol; the guard covers all of them and
    re-runs when they change.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Hand the GitBook site changes to the operator as a prepared diff</name>

  <read_first>
    - ~/Tools/burp-ai-agent-doc/README.md — the `Auto Tool Chaining` bullet and the feature-table row that both describe the model as autonomously chaining up to 8 tool calls. Quote the current text exactly.
    - ~/Tools/burp-ai-agent-doc/developer/data-flow.md — the section stating that tool calls are executed automatically in a loop, and the mermaid block under it.
    - ~/Tools/burp-ai-agent-doc/mcp/security-model.md — the whole page; this is where the confirmation flow belongs on the site.
    - ~/Tools/burp-ai-agent-doc/privacy/limitations.md — the whole page; this is where the `SecretCipher` caveat belongs.
    - ~/Tools/burp-ai-agent-doc/backends/anthropic.md and ~/Tools/burp-ai-agent-doc/mcp/external-servers.md — the absolute at-rest claims.
    - ~/Tools/burp-ai-agent-doc/SUMMARY.md — the site's table of contents, in case a new page is proposed rather than an edit.
    - The in-repo wording this plan just landed in `README.md`, `SPEC.md` and `docs/ui-safety-guide.md` — the site copy must agree with it rather than diverge into a second, slightly different account.
  </read_first>

  <action>
    Write `.planning/phases/26-coverage-static-analysis-debt-docs/26-GITBOOK-HANDOFF.md`.

    For each of the five pages above, give: the absolute path, the CURRENT text quoted verbatim so the
    operator can confirm the diff applies to what is actually on disk, the REPLACEMENT text, and one
    line saying which success criterion or which repository symbol motivates the change. Where a page
    needs an addition rather than a replacement, name the heading it goes under and the surrounding
    anchors.

    Then present a blocking human-action checkpoint. State plainly that `burp-ai-agent-docs` is a
    separate repository this phase cannot commit to, that nothing under `~/Tools/burp-ai-agent-doc` has
    been modified, and that the operator applies the diff and pushes it themselves. Give the exact
    command sequence to review and commit in that repository, and state that the site is published from
    it so the change is live once pushed.

    Do not create, modify or delete a single file under `~/Tools/burp-ai-agent-doc`. Reading it is the
    whole of this task's interaction with it.

    Record in the SUMMARY, under `## SC6 GitBook half`, whether the operator confirmed the diff was
    applied, deferred it, or amended it — and if deferred, that SC6's GitBook clause is OPEN, so the
    phase verifier reports it as an open item rather than as satisfied. Do not mark it satisfied on the
    strength of the handoff document existing.
  </action>

  <instructions>
    Already done for you: every page of the GitBook site that carries a stale or absolute claim has
    been read, its current text quoted verbatim, and a replacement block written into
    `.planning/phases/26-coverage-static-analysis-debt-docs/26-GITBOOK-HANDOFF.md`. The wording has
    been checked against what this phase just landed in this repo's `README.md`, `SPEC.md` and
    `docs/ui-safety-guide.md`, so the two accounts agree. Nothing under `~/Tools/burp-ai-agent-doc`
    has been touched — `git status --porcelain` there prints nothing.

    The ONE thing requiring you: `burp-ai-agent-docs` is a separate git repository that this
    repository's plans cannot commit to or push. Apply the prepared diff there and push it yourself.
    The site at burp-ai-agent.six2dez.com builds from that repository, so the change is live once
    pushed.

    1. Open `.planning/phases/26-coverage-static-analysis-debt-docs/26-GITBOOK-HANDOFF.md`.
    2. For each section, confirm the quoted CURRENT text still matches what is on disk at the named
       path, then apply the REPLACEMENT.
    3. `git -C ~/Tools/burp-ai-agent-doc diff` to review, then commit and push.
    4. Tell me whether you applied it, deferred it, or amended it — and if you amended it, how.
  </instructions>

  <verification>
    After you reply, this task checks: `git -C ~/Tools/burp-ai-agent-doc status --porcelain` still
    prints nothing from THIS plan's side (nothing was written by the executor), the handoff file
    exists and covers every page, and your response is recorded verbatim in the SUMMARY under
    `## SC6 GitBook half`. If you defer, SC6's GitBook clause is recorded as OPEN rather than
    satisfied — the handoff document existing is not the criterion.
  </verification>

  <resume-signal>Reply `applied`, `deferred`, or `amended: <what you changed>`.</resume-signal>

  <acceptance_criteria>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-GITBOOK-HANDOFF.md` exists and contains a section per page for at least the five pages named above, each with a verbatim CURRENT quote and a REPLACEMENT block.
    - `cd ~/Tools/burp-ai-agent-doc && git status --porcelain` prints nothing — the separate repository is untouched by this plan.
    - The handoff's replacement wording for the confirmation flow and the at-rest caveat matches the wording landed in this repo's `README.md` and `SPEC.md`; the SUMMARY states that it was compared, not assumed.
    - The SUMMARY carries a `## SC6 GitBook half` section recording the operator's response as applied, deferred or amended.
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/26-coverage-static-analysis-debt-docs/26-GITBOOK-HANDOFF.md && [ -z "$(git -C "$HOME/Tools/burp-ai-agent-doc" status --porcelain)" ]</automated>
    <ordering>Run the command above BEFORE the operator applies the prepared diff to `burp-ai-agent-docs`. It asserts this executor never wrote to that repository; once the operator legitimately applies the diff, a dirty tree there is expected and this check no longer means what it is here to mean.</ordering>
    <human-check>The operator has reviewed the prepared diff and stated whether it was applied to `burp-ai-agent-docs`, deferred, or amended.</human-check>
  </verify>

  <done>
    A file-by-file diff for the five affected GitBook pages exists and is quoted against their current
    text; the separate repository is provably unmodified; and the operator's decision is on the record,
    with SC6's GitBook clause left OPEN unless they confirm it was applied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project documentation → user's security decisions | An overstated at-rest claim causes a user to store a credential they would otherwise have kept elsewhere. Documentation is a control surface here, not decoration. |
| Advisory → affected user's remediation | What the advisory says determines whether a user rotates a leaked session cookie or does not. |
| This repository → `burp-ai-agent-docs` | A separate git repository. Anything written there by this phase would be an unrequested side effect in a repository the phase does not own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-05-01 | Repudiation | missing advisory | high | mitigate | `SECURITY.md` gains explicit SEC-04 and PRIV-05 entries with affected versions, impact, fixed version and a user action, pinned by `SecurityDocsTest` so a later edit cannot quietly remove them. |
| T-26-05-02 | Information Disclosure | overstated at-rest claim | high | mitigate | Every absolute at-rest claim in six documents is replaced by an accurate statement naming where the master key lives, so a user's threat model for storing an API key matches what ships. |
| T-26-05-03 | Spoofing | fabricated advisory identifier | medium | mitigate | Prohibition plus an explicit sentence stating that no CVE or GHSA has been issued. An invented identifier would make the advisory unverifiable. |
| T-26-05-04 | Tampering | out-of-repo writes | high | mitigate | Prohibition plus an acceptance criterion that `git status --porcelain` in the GitBook checkout prints nothing. The site change is a prepared diff and a human action, never an automated cross-repository write. |
| T-26-05-05 | Repudiation | cache-served documentation guard | high | mitigate | Every markdown file the guard reads is declared as a `tasks.test` input, following the existing `adrRecord` declaration, and a recorded cache probe proves the task re-runs on a documentation-only edit. Without this the guard is green in exactly the commit that breaks it. |
| T-26-05-06 | Repudiation | claiming a control that does not ship | high | mitigate | Every new documentation claim is required to name the repository symbol behind it in the SUMMARY; a claim with no symbol is deleted rather than softened. |
| T-26-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package and adds no Gradle dependency; the `build.gradle.kts` edits are `inputs.file` declarations only. No package-legitimacy audit is required because there is no install task. |
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck` all green.
- `git diff --stat detekt-baseline.xml` prints nothing; `git diff --name-only` contains no `DECISIONS.md`.
- `git -C ~/Tools/burp-ai-agent-doc status --porcelain` prints nothing.
- Two cache probes recorded with observed output (SECURITY.md and SPEC.md).
- The `dependencies` block in `build.gradle.kts` is unchanged.
</verification>

<success_criteria>
- SC5 satisfied: an advisory naming affected versions, impact, fixed version and user action for both SEC-04 and PRIV-05.
- SC6 in-repo half satisfied across `README.md`, `SPEC.md` and three `docs/` pages, with the `DECISIONS.md` half owned by plan 26-06.
- SC6 GitBook half handled honestly: a prepared diff, an untouched separate repository, and an operator decision on the record.
</success_criteria>

<output>
Create `.planning/phases/26-coverage-static-analysis-debt-docs/26-05-SUMMARY.md` when done, with a
`## SC6 GitBook half` section recording the operator's response.
</output>
