---
phase: 26-coverage-static-analysis-debt-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/util/SsrfGuard.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/util/SsrfGuardTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/util/SsrfGuardNoResolutionTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/config/McpSettingsTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/config/McpTokenStrengthTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/config/SecretCipherTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/EntropyTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionPolicyTest.kt
autonomous: true
requirements: [QUAL-06, QUAL-07]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "W-2 / 25-REVIEW WR-02: `SsrfGuard.isPrivateOrLinkLocal(\"http://[::ffff:169.254.169.254]/\")` returns true. Before this plan it returned false while the hex spelling of the identical address returned true — the same notation-evasion class Phase 25's SC3 closed for IPv4."
    - "W-2: the IPv4-mapped IPv6 forms `http://[::ffff:192.168.1.10]/`, `http://[0:0:0:0:0:ffff:192.168.1.10]/` and the unbracketed `http://::ffff:192.168.1.10/` all return true, and each agrees with its hex spelling (`[::ffff:a9fe:a9fe]`, `[::ffff:c0a8:10a]`) — the classification is notation-independent, which is the property the object KDoc claims."
    - "W-2: `http://[::ffff:127.0.0.1]/` returns false, because loopback stays excluded under every spelling (D-01: Ollama and LM Studio local use must not start warning)."
    - "W-2: the guard still performs ZERO name resolution. `SsrfGuardNoResolutionTest`'s corpus gains the IPv4-mapped forms and the JVM-wide `CountingInetAddressResolverProvider` still counts 0 lookups across it, with the installation control test still red-able."
    - "W-2: `SsrfGuard`'s object KDoc states that the IPv4-mapped IPv6 notation is classified identically to its hex spelling, so the accuracy claim the review flagged as overstated becomes true rather than being softened."
    - "W-1b / 25-REVIEW WR-01: `McpSettings.isTokenWeak(token)` is a pure, AWT-free predicate — true for a token shorter than `Defaults.MCP_MIN_TOKEN_LENGTH` after trimming, false at exactly that length and above, and true for a blank token."
    - "W-1b: `Defaults.MCP_MIN_TOKEN_LENGTH` is 32, and `McpSettings.generateToken()`'s output is NOT weak by that floor — the default 32-random-byte token is 43 Base64URL characters, so the floor never fires against the value the product itself generates."
    - "W-1b: `SettingsPanelMcpTabs.refreshMcpNotice` calls `McpSettings.isTokenWeak` and adds a RISK-level notice item when MCP is enabled with a non-blank but weak token. Asserted structurally by reading the source file, because `SettingsPanel` cannot be constructed headlessly."
    - "SC2: line coverage on `com.six2dez.burp.aiagent.config` is >= 96.2% and branch coverage >= 91.0%, against the 95.57% (1014/1061) line and 88.98% (541/608) branch recorded at 4f0ebd7 on 2026-08-22."
    - "SC2: line coverage on `com.six2dez.burp.aiagent.redact` is >= 97.5% and branch coverage >= 93.0%, against the 96.13% (422/439) line and 89.69% (174/194) branch recorded at 4f0ebd7."
    - "`McpSettings.parseToolToggles` handles a null, a blank, a malformed-JSON, a non-object-JSON, a boolean-valued, a string-valued and a mixed-type payload without throwing, and `serializeToolToggles` returns the empty-object literal when serialisation fails."
    - "`SecretCipher.decrypt` returns a non-prefixed input unchanged (plaintext migration path) and returns the empty string for a prefixed value whose GCM tag does not authenticate — the fail-soft contract is asserted as an observable outcome, not read from the KDoc."
    - "`RedactionPolicy.default()` is exercised and asserted to enable all three flags, so the constructor the tripwire path uses is not the only unexecuted policy factory in the package."
    - "`SafeRegex.isPatternSafe` rejects a syntactically invalid pattern, rejects a zero-width-matching pattern, and accepts a bounded literal pattern — all three arms asserted."
    - "`detekt-baseline.xml` is byte-identical to its state at 4f0ebd7 after this plan — `./gradlew detekt` is green with no entry added."
  artifacts:
    - "src/main/kotlin/com/six2dez/burp/aiagent/util/SsrfGuard.kt — `IPV6_REGEX` accepts the dot, and the object KDoc records the mapped-notation coverage"
    - "src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt — `MCP_MIN_TOKEN_LENGTH`"
    - "src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt — `McpSettings.Companion.isTokenWeak(token: String): Boolean`"
    - "src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt — the weak-token RISK item in `refreshMcpNotice`"
    - "src/test/kotlin/com/six2dez/burp/aiagent/config/McpTokenStrengthTest.kt — the predicate's behaviour plus the structural assertion that the panel consumes it"
    - "src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionPolicyTest.kt — `RedactionPolicy.default`, `Redaction.anonymizeHost` and the HMAC branches"
  key_links:
    - "`IPV6_REGEX` -> `resolveIpv6Literal` -> `InetAddress.getByName`: widening the literal gate must NOT widen what reaches the resolving call; the `host.contains(':')` conjunct is what keeps hostnames away and it is unchanged"
    - "`Defaults.MCP_MIN_TOKEN_LENGTH` -> `McpSettings.isTokenWeak` -> `SettingsPanelMcpTabs.refreshMcpNotice`: the floor is only a mitigation if the operator is told; the predicate is pure so it is testable, the notice is where it becomes visible"
    - "`McpSettings.isTokenWeak` -> ADR-16's seventh residual (plan 26-06): the residual bullet claims a mitigation exists; this plan is what makes that claim true, so 26-06 must not be written as if the floor were absent"
  prohibitions:
    - statement: "The weak-token notice must remain ADVISORY. Do not block saving, do not refuse to start the MCP server, and do not overwrite the operator's token. A control that silently rewrites a user's credential is worse than the weak credential it replaced."
      severity: high
    - statement: "Do not raise `Defaults.MCP_MIN_TOKEN_LENGTH` above the length of what `McpSettings.generateToken()` produces. A floor that flags the product's own default token trains the operator to dismiss the notice, which is how a safety control stops being one."
      severity: high
    - statement: "No test in this plan may execute production code without asserting on its result."
      severity: high
    - statement: "No existing test may be deleted, renamed, `@Disabled` or narrowed to make this plan's suite green. `RedactionTest` in particular must not be touched — it carries a recorded wall-clock flake class and is not this plan's target."
      severity: high
    - statement: "No wall-clock threshold and no fixed `Thread.sleep` in any assertion."
      severity: medium
---

<objective>
Two Phase-25 follow-ups plus the `redact` and `config` halves of SC2.

Purpose: `redact` (96.13% line) and `config` (95.57% line) are already saturated against SC2's stated
2026-08-05 bar of 34% line / 23% branch, so the honest work here is not bulk coverage — it is closing
the specific branches that are still dark and closing two review findings that live in the same files.
W-2 is the notation-evasion hole Phase 25's own review reproduced against the compiled `SsrfGuard`:
the dotted IPv4-mapped IPv6 spelling of the cloud-metadata address is not flagged while its hex
spelling is, and the rewritten KDoc reads as though it were. W-1b is the entropy floor that makes
ADR-16's forthcoming seventh residual a real mitigation instead of an advisory sentence.

Output: a notation-independent `SsrfGuard`, an MCP token-strength predicate with a visible advisory,
and `redact` / `config` at their measured floors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-secondary-hardening/25-REVIEW.md
@.planning/phases/25-secondary-hardening/25-VERIFICATION.md
@CLAUDE.md
@AGENTS.md
</context>

## Coverage baseline (measured, not assumed)

Measured by the planner on 2026-08-22 at commit `4f0ebd7`, whole suite, no filter:

```
JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport --continue
```

BUILD SUCCESSFUL, 880 tests / 127 classes / 0 failures / 1 pre-existing skip.

| scope | line covered/total | line % | branch covered/total | branch % |
|---|---|---|---|---|
| `redact` | 422/439 | **96.13%** | 174/194 | **89.69%** |
| `config` | 1014/1061 | **95.57%** | 541/608 | **88.98%** |
| `util` | 228/298 | 76.51% | 146/228 | 64.04% |
| project-wide | 14138/25096 | 56.34% | 3705/10860 | 34.11% |

The 2026-08-05 milestone bar recorded in `STATE.md` is 34% line / 23% branch project-wide. Both of
this plan's packages cleared it by more than 60 points before this phase started, which is why the
floors here are set against the *post-Phase-25* measurement instead. Where the remaining dark lines
sit, from the same report:

- `redact` — 17 missed lines, of which `RedactionPolicy.default` (5), `SecretTripwire.GateDecision.<init>` (3), `SafeRegex.isPatternSafe` (2) and four one-line accessors are directly reachable; 20 missed branches, of which `SafeRegex.isPatternSafe` (4), `Redaction.splitPoint` (3), `Entropy` (3), `Redaction.hmacSha256` (2), `Redaction.windowEnd` (2) and `Redaction.pairMayBeInFlightAt` (2) are reachable.
- `config` — 47 missed lines and 67 missed branches, of which `McpSettings.Companion` holds 16 lines / 19 branches and `SecretCipher` holds 9 lines / 6 branches.

## Artifacts this phase produces

| Symbol | File | Note |
|---|---|---|
| `Defaults.MCP_MIN_TOKEN_LENGTH` | `config/Defaults.kt` | `const val`, value 32 |
| `McpSettings.Companion.isTokenWeak(token: String): Boolean` | `config/McpSettings.kt` | pure, AWT-free |
| weak-token RISK item | `ui/SettingsPanelMcpTabs.kt` `refreshMcpNotice` | advisory only |
| `McpTokenStrengthTest` | new test file | behaviour + structural wiring assertion |
| `RedactionPolicyTest` | new test file | `RedactionPolicy.default`, host anonymisation, HMAC branches |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Close the IPv4-mapped IPv6 notation gap end-to-end (W-2)</name>

  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/util/SsrfGuard.kt — the whole file. The object KDoc's "'Literal' means every `inet_aton` notation" paragraph, `IPV6_REGEX`, the `when` in `isPrivateOrLinkLocal`, `resolveIpv6Literal`, `isIpv6Ula` and `extractAuthorityHost`.
    - src/main/kotlin/com/six2dez/burp/aiagent/util/Ipv4Literal.kt — so you can see which notations the IPv4 arm already handles and do not duplicate them on the IPv6 arm.
    - src/test/kotlin/com/six2dez/burp/aiagent/util/SsrfGuardTest.kt — the existing 17-case corpus and its assertion style.
    - src/test/kotlin/com/six2dez/burp/aiagent/util/SsrfGuardNoResolutionTest.kt — the 15-URL corpus, the `@Order(1)` installation control, and why the control must stay red-able.
    - src/test/kotlin/com/six2dez/burp/aiagent/util/CountingInetAddressResolverProvider.kt — how the JVM-wide counter works; do not change it.
    - .planning/phases/25-secondary-hardening/25-REVIEW.md section WR-02 — the measured table of what is and is not flagged today. Reproduce it before changing anything.
  </read_first>

  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/util/SsrfGuard.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/util/SsrfGuardTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/util/SsrfGuardNoResolutionTest.kt
  </files>

  <behavior>
    Failing tests first, added to `SsrfGuardTest`:
    - `http://[::ffff:169.254.169.254]/` returns true, and equals the verdict for `http://[::ffff:a9fe:a9fe]/`.
    - `http://[::ffff:192.168.1.10]/` returns true, and equals the verdict for `http://[::ffff:c0a8:10a]/`.
    - `http://[0:0:0:0:0:ffff:192.168.1.10]/` returns true.
    - The unbracketed `http://::ffff:192.168.1.10/` returns true (the `extractAuthorityHost` fallback path).
    - `http://[::ffff:127.0.0.1]/` returns false — loopback stays excluded under the mapped spelling too.
    - `http://[::ffff:8.8.8.8]/` returns false — a public address in mapped form is still public.
    - A hostname containing a dot and no colon (`http://example.com/`) still returns false and still reaches no literal branch.
    - A colon-bearing string that is not a valid IPv6 literal and now also contains a dot (`http://1.2:3/`) returns false without throwing.
    Added to `SsrfGuardNoResolutionTest`: the mapped forms above join the corpus, and the JVM-wide lookup count across the corpus is still 0.
  </behavior>

  <action>
    Widen `IPV6_REGEX`'s character class by one character — the ASCII full stop — so an IPv6 literal
    carrying a dotted IPv4 suffix passes the literal gate instead of falling through to the
    not-private default. Leave the `host.contains(':')` conjunct in `isPrivateOrLinkLocal` exactly as
    it is: that conjunct, not the character class, is what keeps hostnames away from the one resolving
    call, and Phase 25's review measured that a dot does not weaken it.

    Correct the object KDoc. The existing paragraph claims notation-independence; extend it to state
    explicitly that the IPv4-mapped IPv6 forms are classified identically to their hex spelling, and
    name why the widening is safe (the colon conjunct is the resolver gate, not the character class).
    Do NOT soften the existing claim — the point of the change is to make the claim true.

    Preserve the KDoc's existing instruction about the resolving method name appearing exactly once in
    this file; do not name it again anywhere you edit.

    Extend both corpora. In `SsrfGuardNoResolutionTest` add the mapped forms to the existing corpus
    list only — do not restructure the counter, the `@Order(1)` installation control, or the
    service-provider file, all of which Phase 25's verification proved non-vacuous.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*SsrfGuardTest' --tests '*SsrfGuardNoResolutionTest' --tests '*Ipv4LiteralTest'` is green.
    - RED PROBE, recorded with its observed output: run the new `SsrfGuardTest` cases against the unmodified `SsrfGuard.kt` and record that the mapped-form assertions fail with the observed verdict `false`. Restore with `git checkout HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/util/SsrfGuard.kt` — never `git stash`, which shares `refs/stash` across linked worktrees.
    - `grep -c 'getByName' src/main/kotlin/com/six2dez/burp/aiagent/util/SsrfGuard.kt` still returns exactly 1 — the Phase 25 invariant that the IPv4 branch resolves nothing and the IPv6 branch was not deleted is unchanged by this edit.
    - `SsrfGuardNoResolutionTest` reports a corpus of at least 19 URLs and asserts a lookup count of 0.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` green and `git diff --quiet detekt-baseline.xml` exits 0.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck` green.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*SsrfGuard*' --tests '*Ipv4LiteralTest' detekt ktlintCheck</automated>
  </verify>

  <done>
    An IPv4-mapped IPv6 literal is classified identically to its hex spelling in both directions,
    loopback stays excluded under the mapped spelling, the guard still resolves nothing, and the
    object KDoc's notation-independence claim is now true rather than overstated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP token-strength floor and the config coverage floors</name>

  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt — `generateToken`, `parseToolToggles`, `serializeToolToggles`, `parseUnsafeToolSet`, `serializeUnsafeToolSet`, `parseAllowedOrigins`, `serializeAllowedOrigins`.
    - src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt — the `const val` style used for scalar defaults.
    - src/main/kotlin/com/six2dez/burp/aiagent/config/SecretCipher.kt — `encrypt`, `decrypt`, `loadOrCreateMasterKey`, `deriveKey`, `SecretCipherException`, and the "ENC1:" envelope contract.
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt — `refreshMcpNotice` in full: the `Item` accumulator, the `SubtleNotice.Level` values it uses, and the existing `external && tokenBlank` RISK item this one sits beside.
    - src/test/kotlin/com/six2dez/burp/aiagent/config/McpSettingsTest.kt and SecretCipherTest.kt — extend these, do not create parallel classes for the same production files.
    - .planning/phases/25-secondary-hardening/25-REVIEW.md section WR-01 — the offline-guessing property this floor mitigates, and the measured fact that `isBlank()` is the only token validation in the repo today.
    - Read every file you are about to modify in full before editing it.
  </read_first>

  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/config/McpSettings.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/config/McpTokenStrengthTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/config/McpSettingsTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/config/SecretCipherTest.kt
  </files>

  <behavior>
    Failing tests first.

    `McpTokenStrengthTest`:
    - `isTokenWeak` is true for the empty string, for a whitespace-only string, and for a string one character shorter than `Defaults.MCP_MIN_TOKEN_LENGTH`.
    - `isTokenWeak` is false at exactly `Defaults.MCP_MIN_TOKEN_LENGTH` characters and above.
    - `isTokenWeak` trims before measuring, so a padded short token is still weak.
    - `isTokenWeak(McpSettings.generateToken())` is false, asserted over at least 50 generated tokens so the floor is proven never to fire against the product's own default.
    - `Defaults.MCP_MIN_TOKEN_LENGTH` is at most the length of `McpSettings.generateToken()`'s output — asserted as a relation between the two, so raising one without the other fails.
    - Structural: reading `src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt` from disk, `refreshMcpNotice`'s slice contains a call to `isTokenWeak` and a `SubtleNotice.Level.RISK` item.

    `McpSettingsTest` additions:
    - `parseToolToggles` for null, blank, malformed JSON, a JSON array rather than an object, boolean values, `"true"`/`"false"` string values, a non-boolean non-string value, and a blank key — none throws, and each returns the documented shape.
    - `serializeToolToggles` round-trips a populated map and returns the empty-object literal on a failure.
    - `parseUnsafeToolSet` and `serializeUnsafeToolSet` for null, blank, each of the three separators, whitespace padding, duplicates, and the sorted-output guarantee.
    - `parseAllowedOrigins` and `serializeAllowedOrigins` for the blank-entry filter and the distinct filter.

    `SecretCipherTest` additions:
    - `decrypt` returns a non-prefixed input unchanged.
    - `decrypt` returns the empty string for an "ENC1:"-prefixed value whose payload does not authenticate.
    - `decrypt` returns the empty string for an "ENC1:"-prefixed value that is not valid Base64.
    - the master key is created once and reused: two `encrypt` calls on the same instance both decrypt, and a second `SecretCipher` over the same `Preferences` decrypts the first one's output.
  </behavior>

  <action>
    Add `const val MCP_MIN_TOKEN_LENGTH = 32` to `Defaults`, with a one-line comment naming WR-01 and
    stating that the value must never exceed the length of `McpSettings.generateToken()`'s output
    (32 random bytes Base64URL-encoded without padding is 43 characters).

    Add `fun isTokenWeak(token: String): Boolean` to `McpSettings.Companion`: trim, then compare the
    length against `Defaults.MCP_MIN_TOKEN_LENGTH`. Give it a KDoc naming the threat it mitigates —
    the takeover proof's HMAC message is fully known to a squatter, which makes a captured proof an
    offline verifier for the token — and stating that the check is advisory. Keep it AWT-free and
    dependency-free so it stays unit-testable.

    Wire it into `refreshMcpNotice` beside the existing `external && tokenBlank` item: when MCP is on
    and the token is not blank but is weak, append a RISK item whose HTML names the weakness and
    points the operator at the existing Regenerate-token action. Do NOT gate the item on external
    mode — the bind-conflict takeover path runs in local mode too, which is precisely the mode WR-01
    is about. Change nothing else in that function, and do not block or rewrite anything.

    Then extend the two existing config test classes for the coverage floors. Compute expectations
    from the production constants where a bound is involved.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpTokenStrengthTest' --tests '*McpSettingsTest' --tests '*SecretCipherTest'` is green.
    - `grep -c 'isTokenWeak' src/main/kotlin/com/six2dez/burp/aiagent/ui/SettingsPanelMcpTabs.kt` returns at least 1.
    - `grep -c 'MCP_MIN_TOKEN_LENGTH' src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt` returns at least 1.
    - RED PROBE, recorded with its observed output: temporarily raise `MCP_MIN_TOKEN_LENGTH` above 43 and record that the generated-token test turns red, proving the relation assertion is falsifiable. Restore with `git checkout HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt` after the probe if the file is committed, otherwise restore the value by hand and re-run.
    - After `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport`, package `com/six2dez/burp/aiagent/config` in `build/reports/jacoco/test/jacocoTestReport.xml`: LINE covered/(covered+missed) >= 0.962 and BRANCH >= 0.910. Recorded pre-phase: 1014/1061 = 0.9557 line, 541/608 = 0.8898 branch.
    - Both numbers written into the SUMMARY as covered/total pairs beside their pre-phase pairs.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` green and `git diff --quiet detekt-baseline.xml` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport detekt ktlintCheck</automated>
  </verify>

  <done>
    A pure, tested token-strength predicate exists, the MCP tab shows an advisory RISK item for a weak
    token in every mode the takeover path runs in, the floor provably never fires against the
    product's own generated token, and `config` is at or above 96.2% line / 91.0% branch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close the reachable dark branches in the redact package</name>

  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt — `RedactionPolicy` and its companion, `anonymizeHost`, `hmacSha256`, `budgetExceededMarker`, `setCustomPatterns`. Do not read the whole 1831-line file end to end; grep for those symbols and read their ranges.
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/SafeRegex.kt — `isPatternSafe` and the `DeadlineCharSequence.subSequence` override.
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/Entropy.kt — `qualifyingEntropy` (private, reached through `maxQualifyingTokenEntropy`), and the hex vs base64 threshold split.
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/SecretShapes.kt — the `shapes` accessor.
    - src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt and EntropyTest.kt — extend these.
    - Do NOT open or modify `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt`. It carries a recorded wall-clock flake class (the SafeRegex 50 ms deadline under CPU load) and is out of scope.
  </read_first>

  <files>
    src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionPolicyTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/redact/SafeRegexTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/redact/EntropyTest.kt
  </files>

  <behavior>
    Failing tests first.

    `RedactionPolicyTest` (new):
    - `RedactionPolicy.default()` returns a policy with all three flags enabled.
    - `RedactionPolicy.fromMode` returns the documented flag triple for each of the three `PrivacyMode` values, asserted per flag rather than by data-class equality so a failure names the flag.
    - `Redaction.anonymizeHost` is deterministic for a fixed salt, differs across salts, differs across hosts under one salt, and returns a stable form for an empty host and for a host that is already an IP literal — assert the observed behaviour and name it in the test, do not assume.
    - `Redaction.budgetExceededMarker` and `SecretShapes.shapes` are read and asserted non-empty / well-formed.

    `SafeRegexTest` additions:
    - a syntactically invalid pattern is rejected;
    - a pattern that matches the empty string is rejected;
    - a bounded literal pattern is accepted;
    - `DeadlineCharSequence.subSequence` is exercised through a pattern whose evaluation slices the input.

    `EntropyTest` additions:
    - a token below the minimum length scores zero regardless of its entropy;
    - a hex token above the hex threshold qualifies and a hex token below it does not;
    - a base64 token above the base64 threshold qualifies and one below it does not;
    - a token that is neither hex nor base64 never qualifies however high its Shannon entropy;
    - the dot-joined second pass qualifies a dotted base64url candidate whose segments are individually too short, and does NOT qualify a dotted hex run.
  </behavior>

  <action>
    Create `RedactionPolicyTest` in the existing redact test package and extend `SafeRegexTest` and
    `EntropyTest` in place.

    Where a threshold is involved — the entropy thresholds, the minimum token length, the SafeRegex
    deadline — import the production constant and build the fixture relative to it. Never re-type a
    threshold as a literal in the test, and never assert a wall-clock duration: for the SafeRegex
    cases assert the boolean verdict, not how long it took.

    If a branch turns out to be genuinely unreachable from outside the package, say so in the SUMMARY
    with the reason rather than reaching it by reflection or by widening a private symbol. The floors
    below are set with enough margin that two or three unreachable branches do not put them at risk.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*RedactionPolicyTest' --tests '*SafeRegexTest' --tests '*EntropyTest'` is green.
    - After `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport`, package `com/six2dez/burp/aiagent/redact`: LINE covered/(covered+missed) >= 0.975 and BRANCH >= 0.930. Recorded pre-phase: 422/439 = 0.9613 line, 174/194 = 0.8969 branch.
    - Both numbers written into the SUMMARY as covered/total pairs beside their pre-phase pairs.
    - `git diff --name-only | grep -c 'RedactionTest.kt'` returns 0 — the flake-carrying class was not touched.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` reports 0 failures across the whole suite.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck` green and `git diff --quiet detekt-baseline.xml` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport detekt ktlintCheck</automated>
  </verify>

  <done>
    `redact` is at or above 97.5% line and 93.0% branch, the policy factory and the regex-safety arms
    are asserted, and `RedactionTest` is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-typed / imported backend base URL → `SsrfGuard` | The URL comes from settings the user typed or imported; the guard's verdict is what the user is warned by. Advisory and non-blocking per D-01. |
| Operator-typed MCP token → takeover proof HMAC key | A short token turns a captured proof into an offline verifier (25-REVIEW WR-01). |
| Burp Preferences → `SecretCipher` | Ciphertext and its master key sit side by side in Preferences; the boundary this plan touches is only the fail-soft decrypt contract. |
| Traffic → redaction policy | `RedactionPolicy` decides what leaves the host per privacy mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03-01 | Spoofing | `SsrfGuard.IPV6_REGEX` | medium | mitigate | The IPv4-mapped IPv6 spelling of a private or metadata address is classified identically to its hex spelling, so the advisory warning can no longer be sidestepped by notation on the IPv6 arm. Asserted in both directions with a recorded red probe. |
| T-26-03-02 | Information Disclosure | `SsrfGuard.resolveIpv6Literal` | high | mitigate | Widening the character class must not widen what reaches the one resolving call. The `host.contains(':')` conjunct is unchanged, `SsrfGuardNoResolutionTest`'s JVM-wide counter still asserts zero lookups over an enlarged corpus, and the single-occurrence grep invariant on the resolving method name is re-asserted. |
| T-26-03-03 | Denial of Service | `SsrfGuard` loopback exclusion | medium | mitigate | `http://[::ffff:127.0.0.1]/` is asserted false, so local Ollama and LM Studio users do not start seeing a warning they will learn to ignore. |
| T-26-03-04 | Spoofing | weak MCP token vs takeover proof | high | mitigate | `isTokenWeak` plus the RISK notice make the offline-guessing residual visible to the operator in every mode the takeover path runs in. Advisory by design — see the prohibitions; a control that rewrites the operator's credential is out of bounds. |
| T-26-03-05 | Denial of Service | over-strict token floor | medium | mitigate | The floor is asserted never to fire against `McpSettings.generateToken()`'s own output, over at least 50 samples, and the relation between the two is itself asserted so raising the constant alone fails. Notice fatigue is a real failure mode for a safety control. |
| T-26-03-06 | Information Disclosure | `SecretCipher.decrypt` fail-soft | medium | mitigate | The empty-string-on-authentication-failure contract is asserted as an observable outcome, so a future change that returns the raw ciphertext or the undecrypted payload fails loudly. |
| T-26-03-07 | Information Disclosure | `RedactionPolicy.fromMode` | high | mitigate | The flag triple per privacy mode is asserted per flag; this is the table that `sanitizeHeaders` (plan 26-02) and `Redaction.redact` both branch on, and a silent flip in it is the PRIV-05 shape. |
| T-26-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package and adds no Gradle dependency. No package-legitimacy audit is required because there is no install task. |
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck` all green.
- `git diff --stat detekt-baseline.xml` prints nothing.
- `git diff --stat build.gradle.kts` prints nothing.
- `redact` and `config` floors read out of `build/reports/jacoco/test/jacocoTestReport.xml` and recorded as covered/total pairs.
- Two red probes recorded with observed output: the SsrfGuard mapped-form probe and the token-floor relation probe.
</verification>

<success_criteria>
- W-2 is closed: notation-independent classification on the IPv6 arm, zero resolution, accurate KDoc.
- W-1b is closed: a tested token-strength floor with a visible advisory, so ADR-16's seventh residual (plan 26-06) describes a real mitigation.
- `redact` >= 97.5% line / 93.0% branch; `config` >= 96.2% line / 91.0% branch.
- No new detekt baseline entry; no new Gradle dependency.
</success_criteria>

<output>
Create `.planning/phases/26-coverage-static-analysis-debt-docs/26-03-SUMMARY.md` when done. Record the
W-1b outcome under a heading `## W-1b outcome` in wording plan 26-06 can copy verbatim into ADR-16's
seventh `Residual:` bullet.
</output>
