---
phase: 26-coverage-static-analysis-debt-docs
plan: 07
type: execute
wave: 3
depends_on: [26-01, 26-02, 26-03, 26-04, 26-05, 26-06]
files_modified:
  - detekt-baseline.xml
  - src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/design/DesignTokens.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/design/Components.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/McpHelpPanel.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/BackendConfigPanel.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/PrivacyPill.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ToolInvocationDialog.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/context/ContextCollector.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/AdaptivePayloadEngine.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ResponseAnalyzer.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeuristics.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackend.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/backends/ollama/OllamaBackend.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackend.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/KtorMcpServerManager.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/config/SecretCipherTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/audit/AiRequestLoggerTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/ProxyHistoryListenerPortFilterTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/AiGateMcpToolTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/AiGateMcpToolTest.kt
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-COVERAGE.md
autonomous: true
requirements: [QUAL-06, QUAL-07]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC3 clause 1: `detekt-baseline.xml` contains at most 1045 `<ID>` entries, against the 1096 recorded at 4f0ebd7 — at least 51 fewer. The planned set is 62 removals across eleven named rule categories; 1045 is the gate, 1034 the target, and the gap is the margin for entries that turn out unsafe to fix."
    - "SC3 clause 1, anti-hollow: at least 40 of the removals correspond to a finding that was FIXED in source, not merely to a stale entry that no longer matched. The stale-entry count is reported separately from the fixed-finding count so the shrink cannot be read as larger than the work behind it."
    - "SC3 clause 2: no finding introduced by Phases 20-25 was added to the baseline. Proven by one command — `git log --oneline ab567fb..HEAD -- detekt-baseline.xml` returns nothing, where ab567fb (2026-07-29) is the last commit that touched the file and predates Phase 20's first commit ecc69bc (2026-08-05)."
    - "SC3, direction: `git diff --stat detekt-baseline.xml` for this phase shows removals only. No `<ID>` line is added by any commit in Phase 26, including this plan's."
    - "`detekt.yml` is byte-identical to its state at 4f0ebd7. No rule is deactivated, no threshold is relaxed, and no `excludes` glob is broadened — a finding count that falls because the rule stopped firing is not a smaller baseline, it is a smaller detector."
    - "No blanket `@Suppress` is introduced to make a finding disappear. Where a suppression is genuinely correct it is inline, narrowly scoped, and carries its reason in the same line or the line above — the pattern Phase 23 established for `LargeClass`."
    - "`./gradlew detekt` is green at the end with zero findings outside the baseline, and `./gradlew ktlintCheck` is green."
    - "The whole suite is green: `./gradlew test` reports 0 failures and exactly 1 skip — the same skip count as the 4f0ebd7 baseline. No test was deleted, renamed, `@Disabled` or narrowed to reach any number in this plan."
    - "SC2, phase seal: project-wide line coverage is >= 57.0% and branch coverage is >= 34.8%, against the 56.34% (14138/25096) line and 34.11% (3705/10860) branch measured at 4f0ebd7, and against the 34% line / 23% branch recorded as the 2026-08-05 milestone baseline in STATE.md."
    - "SC2, phase seal: `26-COVERAGE.md` exists and records the pre-phase and post-phase per-package tables side by side, each with the exact command and commit that produced it, so the SC2 numbers have a provenance rather than being asserted."
    - "SC2, phase seal: every per-package and per-file floor claimed by plans 26-01, 26-02 and 26-03 is re-verified on the MERGED tree and reported as met or missed. A floor met in an isolated worktree and missed after merge is a floor that was not met."
  artifacts:
    - "detekt-baseline.xml — at most 1045 `<ID>` entries, removals only"
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-COVERAGE.md — the phase's coverage provenance record"
    - "the eleven categories' source fixes across the files listed in `files_modified`"
  key_links:
    - "each fixed finding -> its removed baseline entry: a fix whose entry stays is invisible, and an entry removed without a fix makes detekt red — the two halves must land in one commit per category"
    - "`ab567fb` (2026-07-29, the last commit touching the baseline) -> HEAD: the empty log between them is the whole evidence for SC3's second clause, and it is one command rather than an argument"
    - "`detekt.yml` unchanged -> the finding count means what it says: a baseline that shrinks because a rule was switched off measures nothing"
    - "`26-COVERAGE.md` -> the phase verifier: SC2's target was derived from a measurement taken at plan time, so the seal records both endpoints rather than asking the verifier to trust one"
  prohibitions:
    - statement: "Do not add any `<ID>` entry to `detekt-baseline.xml`. This phase's goal is that the file shrinks; appending to it inverts the goal, and `git diff --stat` showing an addition is a failure of the plan regardless of what else passed."
      severity: high
    - statement: "Do not modify `detekt.yml`. Deactivating a rule, raising a threshold or broadening an `excludes` glob makes findings disappear without fixing anything, and produces a number that looks like progress and is not."
      severity: high
    - statement: "Do not introduce a file-level or class-level blanket `@Suppress` to clear a category. A suppression is acceptable only where it is genuinely correct, narrowly scoped to the one declaration, and carries its reason."
      severity: high
    - statement: "Do not delete, rename, `@Disabled` or narrow any test to reach a coverage or finding number. The suite's failure count and skip count are both asserted against the 4f0ebd7 baseline precisely so this cannot pass unnoticed."
      severity: high
    - statement: "Do not edit any file outside the `files_modified` list. The trim is bounded by eleven named rule categories; a fix that requires touching an undeclared file is a fix to defer and record, not to smuggle in."
      severity: medium
    - statement: "Do not use `git stash` at any point. `refs/stash` lives in the parent `.git` and is shared across every linked worktree, and executors run in parallel worktrees. Use `git checkout <ref> -- <path>` and `/bin/cp -f` instead — this shell aliases `cp` and `rm` to interactive mode, so a bare `cp` in a script silently no-ops."
      severity: high
---

<objective>
SC3 (QUAL-07) and the phase's coverage seal: shrink the detekt baseline by fixing real findings in
named categories, prove no Phase 20-25 finding was ever added to it, and record the phase's coverage
endpoints with their provenance.

Purpose: "fewer than 1096" is satisfiable by deleting one entry, which would be a hollow pass. The
baseline was grouped by rule ID at plan time and the cheap, safe, genuinely-fixable categories were
enumerated; this plan goes after those rather than an arbitrary count. Separately, SC2's target was
derived from a measurement taken at plan time against the post-Phase-25 tree, and that derivation is
worth nothing unless both endpoints are on the record.

Output: a baseline at or below 1045 entries with a removals-only diff, `detekt.yml` untouched, and a
committed coverage provenance record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
</context>

## Why this plan is in wave 3

Every other plan in this phase adds source. Each one gates on `./gradlew detekt` being green with an
unchanged baseline, so by the time this plan runs the tree should already be finding-free outside the
baseline — Task 1 verifies that rather than assuming it. This plan also edits `CliBackend.kt`,
`ChatPanel.kt`, `McpToolHelpers.kt` and `AgentSettings.kt`, which wave-1 plans own, so it must run
after them. It is the last plan in the phase, which is also why it carries the seal.

## Measured baseline composition (grouped at plan time)

Measured by the planner at commit `4f0ebd7` on 2026-08-22. `detekt-baseline.xml` holds **1096**
`<ID>` entries, none of them duplicated, grouped by rule:

| Rule | Count | | Rule | Count |
|---|---|---|---|---|
| MagicNumber | 651 | | UnusedPrivateProperty | 13 |
| ReturnCount | 133 | | SwallowedException | 9 |
| CyclomaticComplexMethod | 57 | | SpreadOperator | 9 |
| LongMethod | 42 | | LargeClass | 9 |
| TooGenericExceptionCaught | 40 | | MayBeConst | 8 |
| TooManyFunctions | 32 | | UseCheckOrError | 6 |
| LoopWithTooManyJumpStatements | 20 | | UseRequire | 4 |
| NestedBlockDepth | 16 | | UnusedPrivateMember | 3 |
| LongParameterList | 14 | | TooGenericExceptionThrown | 3 |
| FunctionOnlyReturningConstant | 14 | | MatchingDeclarationName | 3 |
| | | | ImplicitDefaultLocale | 3 |
| | | | ComplexCondition | 3 |
| | | | UnusedParameter | 2 |
| | | | InvalidPackageDeclaration | 1 |
| | | | EmptyFunctionBlock | 1 |

Also measured, by backing the file up, running `detektBaseline`, diffing and restoring (the tree was
verified byte-identical afterwards):

- **7 entries were STALE** — present in the committed baseline but not produced by any current
  finding. Detekt ignores unmatched entries, so removing them is free and risk-free. They were
  `CyclomaticComplexMethod:SettingsPanelActions.kt`, `LongMethod:MainTab.kt`,
  `LongMethod:SettingsPanelSettingsIO.kt`, two `ReturnCount:ChatPanel.kt` entries,
  `ReturnCount:KtorMcpServerManager.kt` and `TooManyFunctions:KtorMcpServerManager.kt`. **Recompute
  this set at execution time** — waves 1 and 2 changed `ChatPanel.kt`, `McpSupervisor.kt` and
  `CliBackend.kt`, so the stale set will differ from the seven above.
- **0 entries were MISSING** — every current finding is covered, i.e. `./gradlew detekt` reports zero
  findings today (confirmed: `build/reports/detekt/detekt.md` reads `0 number of total code smells`).

### The eleven categories this plan attacks, and the five it does not

**In scope — 62 planned removals:**

| Category | Count | Fix | Files |
|---|---|---|---|
| stale entries | ~7 | delete the entry; no source change | — |
| `FunctionOnlyReturningConstant` | 14 | `private fun defaultXxx(): String` → a `private const val`, call sites updated | `AgentSettings.kt` |
| `UnusedPrivateProperty` | 13 | delete the dead declaration | `AdaptivePayloadEngine.kt`, `AiRequestLoggerTest.kt`, `Components.kt`, `ContextCollector.kt`, `McpHelpPanel.kt`, `OpenAiCompatibleBackend.kt`, `PassiveAiScanner.kt`, `PassiveAiScannerHeuristics.kt` (3), `PrivacyPill.kt`, `ProxyHistoryListenerPortFilterTest.kt`, `ResponseAnalyzer.kt` |
| `MayBeConst` | 8 | `val` → `const val` | `DesignTokens.kt` (7), `RedactionTest.kt` (1) |
| `UseCheckOrError` | 6 | bare `throw IllegalStateException(m)` → `error(m)`; same exception, same message | `CliBackend.kt` (2), `KtorMcpServerManager.kt` (3), `McpToolExecutorImpl.kt` (1) |
| `UseRequire` | 4 | guarded `throw IllegalArgumentException(m)` → `require(cond) { m }` | `McpToolHelpers.kt` (2), `ToolInvocationDialog.kt` (2) |
| `UnusedPrivateMember` | 3 | delete the dead private function after proving zero references | `ActiveAiScanner.kt`, `BackendConfigPanel.kt`, `ChatPanel.kt` |
| `ImplicitDefaultLocale` | 3 | pass `Locale.ROOT` to `String.format` | `ChatPanel.kt` (2), `OllamaBackend.kt` (1) |
| `UnusedParameter` | 2 | remove the parameter, or suppress narrowly if it satisfies an interface | `AiScanCheck.kt`, `ResponseAnalyzer.kt` |
| `EmptyFunctionBlock` | 1 | give the empty block a body or a stated reason | `SecretCipherTest.kt` |
| `InvalidPackageDeclaration` | 1 | move the file so its path matches its `package` line | `AiGateMcpToolTest.kt` → `mcp/tools/` |

**Out of scope, with reasons — recorded so they are decisions, not omissions:**

| Category | Count | Why not |
|---|---|---|
| `MagicNumber` | 651 | The bulk of these are inline layout literals in `ui/`. Replacing them with `DesignTokens` references is a large UI refactor with real regression surface, and this phase is not a UI phase. The single largest available win, deliberately left for a dedicated effort. |
| `SwallowedException` | 9 | Fixing means making an exception observable or rethrowing it — a behaviour change on error paths. This repo already carries a deliberate `INTENTIONAL:` comment convention on those catch blocks. Not a static-analysis cleanup. |
| `MatchingDeclarationName` | 3 | The fix is renaming `McpToolExecutorImpl.kt`, `McpToolHandlers.kt` and `Components.kt`. Those filenames are cited by ADR-15, by the Phase 22-25 records and by `docs/`; renaming them churns far more than three entries are worth. |
| `ReturnCount`, `CyclomaticComplexMethod`, `LongMethod`, `NestedBlockDepth`, `LoopWithTooManyJumpStatements` | 268 | Every fix is a structural refactor of a method this milestone's security work just rewrote. Refactoring them now would obscure exactly the diffs the milestone exists to make legible — the same reasoning that put the mega-file split out of v0.10.0 scope. |
| `LargeClass`, `TooManyFunctions`, `LongParameterList`, `SpreadOperator`, `TooGenericExceptionCaught`, `TooGenericExceptionThrown`, `ComplexCondition` | 110 | Same class of objection; several are already answered by inline `@Suppress` with a stated reason where that was the right call. |

## Coverage baseline for the seal

Measured by the planner on 2026-08-22 at commit `4f0ebd7`:

```
JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport --continue
```

BUILD SUCCESSFUL, 880 tests / 127 classes / 0 failures / 1 skip.

| scope | line covered/total | line % | branch covered/total | branch % |
|---|---|---|---|---|
| `redact` | 422/439 | 96.13% | 174/194 | 89.69% |
| `config` | 1014/1061 | 95.57% | 541/608 | 88.98% |
| `mcp` (leaf) | 1326/1436 | 92.34% | 374/477 | 78.41% |
| `mcp/tools` | 956/2221 | 43.04% | 370/1089 | 33.98% |
| `mcp/schema` | 69/142 | 48.59% | 35/68 | 51.47% |
| `mcp/external` | 137/225 | 60.89% | 13/66 | 19.70% |
| `mcp` tree (4 packages) | 2488/4024 | 61.83% | 792/1700 | 46.59% |
| `backends/cli` | 215/699 | 30.76% | 75/488 | 15.37% |
| `util` | 228/298 | 76.51% | 146/228 | 64.04% |
| **project** | **14138/25096** | **56.34%** | **3705/10860** | **34.11%** |

The 2026-08-05 milestone baseline recorded in `STATE.md` is 34% line / 23% branch project-wide.

## Artifacts this phase produces

| Artifact | Note |
|---|---|
| `detekt-baseline.xml` at <= 1045 entries | removals only |
| `26-COVERAGE.md` | pre- and post-phase tables with commands and commits |
| `AgentSettings` `DEFAULT_*` constants | replacing the fourteen constant-returning private functions |

<tasks>

<task type="tracer">
  <name>Task 1: Prove SC3 clause 2, recompute the stale set, and run the whole trim loop on one category</name>

  <read_first>
    - detekt-baseline.xml — the file's structure: the `<ID>` entry format is `RuleName:File.kt$Owner$signature`, so an entry is matched by exact string. Skim, do not read all 1096.
    - build.gradle.kts, the `detekt { }` block — `buildUponDefaultConfig`, `allRules = false`, the `baseline` property and the comment naming `./gradlew detektBaseline` as the generator.
    - detekt.yml — the whole file. It is four small override blocks. You must NOT change it; read it so you can assert that.
    - src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt — the `AgentSettingsRepository.Companion` block holding the fourteen `private fun defaultXxx()` declarations, and every call site of each.
    - build/reports/detekt/detekt.md if present — the current findings total.
  </read_first>

  <files>
    detekt-baseline.xml,
    src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt
  </files>

  <action>
    **Step 1 — SC3 clause 2, one command.** Run
    `git log --oneline ab567fb..HEAD -- detekt-baseline.xml` and record the output. `ab567fb`
    (2026-07-29, `chore(detekt): refresh baseline for v0.9.1 changes`) is the last commit that touched
    the file, and it predates `ecc69bc` (2026-08-05), Phase 20's first commit. Empty output is the
    whole proof that no Phase 20-25 finding was added. Quote the command and its output in the SUMMARY;
    if it is NOT empty, stop and report — the criterion is false and the phase needs to know.

    **Step 2 — recompute the live-finding set.** Back the baseline up to a path OUTSIDE the repository
    with `/bin/cp -f` (this shell aliases `cp` to interactive mode, so a bare `cp` in a script silently
    no-ops). Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detektBaseline`, which OVERWRITES
    `detekt-baseline.xml` with the current findings. Extract the `<ID>` set from the regenerated file,
    extract it from the backup, and compute two differences: entries in the committed file that are not
    current findings (STALE — free to remove) and entries that are current findings but not in the
    committed file (MISSING — must be zero; if any appear, a wave-1 or wave-2 plan added a finding and
    that is a blocker, not something to baseline). Then restore the backup with `/bin/cp -f` and verify
    `git diff --quiet detekt-baseline.xml` exits 0 before doing anything else. Record both counts.

    Do not skip the restore-and-verify step. A `detektBaseline` run left in place would re-baseline
    every finding this plan is about to fix, which is the single worst outcome available here.

    **Step 3 — the trim loop on one category.** Fix `FunctionOnlyReturningConstant` in
    `AgentSettings.kt`: convert each of the fourteen `private fun defaultXxx(): T` declarations into a
    `private const val` (or a `private val` where the initialiser is not a compile-time constant) in
    the same companion, update every call site, and keep the values byte-identical. Then remove those
    fourteen entries and the recomputed stale entries from `detekt-baseline.xml` by deleting their
    `<ID>` lines — do not regenerate the file.

    **Step 4 — close the loop.** Run detekt, confirm green with zero findings outside the baseline,
    count the remaining entries, and confirm `git diff detekt-baseline.xml` contains only deletions.
    Commit the source fix and the entry removals TOGETHER: a fix whose entry stays is invisible, and an
    entry removed without its fix makes detekt red.
  </action>

  <acceptance_criteria>
    - `git log --oneline ab567fb..HEAD -- detekt-baseline.xml` output is quoted in the SUMMARY and is empty.
    - The recomputed STALE and MISSING counts are both recorded in the SUMMARY. MISSING is 0; if it is not, the task stops and reports.
    - `git diff --quiet detekt-baseline.xml` exited 0 immediately after the `detektBaseline` probe was restored, verified before any real edit — stated in the SUMMARY.
    - `grep -c '<ID>' detekt-baseline.xml` returns at most 1075 after this task, and the exact number is recorded.
    - `git diff -U0 detekt-baseline.xml | grep -c '^+.*<ID>'` returns 0 — no entry was added.
    - `git diff --quiet detekt.yml` exits 0.
    - `grep -c 'private fun default' src/main/kotlin/com/six2dez/burp/aiagent/config/AgentSettings.kt` returns 0.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` is green and reports zero findings.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*AgentSettings*'` is green.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck` is green.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck test --tests '*AgentSettings*'</automated>
  </verify>

  <done>
    SC3's second clause is proven by one command with its output on the record; the stale and missing
    sets are recomputed against the merged tree rather than inherited from the plan; one category is
    fixed in source with its entries removed in the same commit; and the whole trim loop — fix, remove,
    verify green, verify removals-only — is demonstrated working before it is applied at volume.
  </done>
</task>

<task type="auto">
  <name>Task 2: The dead-code and constant categories</name>

  <read_first>
    - detekt-baseline.xml — the `UnusedPrivateProperty`, `UnusedPrivateMember`, `UnusedParameter`, `MayBeConst`, `EmptyFunctionBlock` and `InvalidPackageDeclaration` entries. Each names its file and its declaration; read the declaration in context before deleting it.
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/design/DesignTokens.kt — the seven `Spacing` values and their KDoc; `const val` inside a nested `object` is legal and source-compatible for every call site.
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeuristics.kt — the three unused private constants. Read their KDoc/comments: a constant kept as documentation of a threshold is a different case from a leftover, and the two are handled differently.
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt — `cancelCurrentRequest`, as wave 1 left it. Plan 26-04 edited this file; do not assume its pre-wave shape.
    - src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt — whether the flagged parameter is required by an interface it implements. A parameter that satisfies a Montoya interface is suppressed narrowly, not deleted.
    - src/test/kotlin/com/six2dez/burp/aiagent/mcp/AiGateMcpToolTest.kt — its `package` line versus its directory.
    - Read each file you are about to modify before editing it.
  </read_first>

  <files>
    detekt-baseline.xml,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/design/DesignTokens.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/design/Components.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/components/PrivacyPill.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/McpHelpPanel.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/panels/BackendConfigPanel.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/context/ContextCollector.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/scanner/AdaptivePayloadEngine.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/scanner/ResponseAnalyzer.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScanner.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeuristics.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/scanner/ActiveAiScanner.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/scanner/AiScanCheck.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/backends/openai/OpenAiCompatibleBackend.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/config/SecretCipherTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/audit/AiRequestLoggerTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/ProxyHistoryListenerPortFilterTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/mcp/AiGateMcpToolTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/AiGateMcpToolTest.kt
  </files>

  <action>
    Work one category at a time, and for each: fix the source, delete the matching `<ID>` lines, run
    detekt, confirm green and removals-only, then move on. Batching all six categories before running
    detekt once turns a single mistake into a bisect.

    **`UnusedPrivateProperty` and `UnusedPrivateMember`.** Before deleting any declaration, grep the
    whole `src/` tree for its name and record the reference count. Delete only where the count proves
    it is genuinely unreferenced. Where a declaration is unused but is documentation of a threshold —
    the `PassiveAiScannerHeuristics` constants are the likely case — do not delete it: either wire it
    into the code that re-derives the same number, or convert it into a comment stating the threshold,
    and record which you did and why. `ChatPanel.cancelCurrentRequest` is the one to be most careful
    with: it neighbours `cancelInFlightRequest`, which plan 26-04 just touched, and deleting the wrong
    one of the two is a real hazard. Prove the reference count before removing it.

    **`MayBeConst`.** Convert the seven `DesignTokens.Spacing` values to `const val`, keeping their
    KDoc verbatim. For the `RedactionTest.kt` entry, convert if the declaration's container permits
    `const`; if it does not — `const val` is only legal at top level or inside an `object` — leave it
    and record the entry as not removable, deducting it from this task's expected count rather than
    forcing it.

    **`UnusedParameter`.** Remove the parameter and update call sites, UNLESS it is required by an
    interface or an override, in which case add a narrowly-scoped inline `@Suppress` on that one
    declaration with its reason on the same line or the line above. Record which of the two applied.

    **`EmptyFunctionBlock`.** Give the empty block in `SecretCipherTest.kt` either a real body or a
    comment stating why it is deliberately empty, following this repo's existing `INTENTIONAL:` comment
    convention for empty catch blocks.

    **`InvalidPackageDeclaration`.** `AiGateMcpToolTest.kt` declares `package …mcp.tools` while sitting
    in the `…mcp` test directory. Move the file with `git mv` so the path matches the package. Do not
    change the package line — the package is correct and the location is wrong, and other tests in that
    package resolve it unqualified.

    Where a fix turns out to be unsafe or larger than described, SKIP it, leave its baseline entry in
    place, and record the skip with the reason. The gate is 1045 with 62 planned; the margin exists for
    exactly this.
  </action>

  <acceptance_criteria>
    - `grep -c '<ID>' detekt-baseline.xml` returns at most 1048 after this task; the exact number is recorded, along with a per-category count of removed and skipped entries.
    - `git diff -U0 detekt-baseline.xml | grep -c '^+.*<ID>'` returns 0.
    - `git diff --quiet detekt.yml` exits 0.
    - `git status --porcelain` shows the `AiGateMcpToolTest.kt` move as a rename, not as a delete plus an add.
    - No new file-level or class-level `@Suppress` annotation: `git diff -U0 src/ | grep '^+' | grep -c '@Suppress'` is at most 2, and every one of them is on a single declaration with a stated reason. The SUMMARY quotes each.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` green with zero findings.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` reports 0 failures and exactly 1 skip.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck` green.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck test</automated>
  </verify>

  <done>
    Six categories are cleared or explicitly skipped with reasons, the baseline is at or below 1048
    entries with a removals-only diff, `detekt.yml` is untouched, and the suite is green at the
    baseline failure and skip counts.
  </done>
</task>

<task type="auto">
  <name>Task 3: The idiom categories, and the phase coverage seal</name>

  <read_first>
    - detekt-baseline.xml — the `UseCheckOrError`, `UseRequire` and `ImplicitDefaultLocale` entries; each quotes the exact `throw` or `String.format` expression.
    - src/main/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackend.kt, mcp/KtorMcpServerManager.kt, mcp/tools/McpToolExecutorImpl.kt — the six bare `throw IllegalStateException(...)` sites, as waves 1 and 2 left them.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt — `resolveReportPath`'s two guards. Plan 26-02 asserts BOTH of them behaviourally; the conversion must preserve the exception type and the message text or those tests turn red.
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ToolInvocationDialog.kt — the two JSON-editor guards.
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt and backends/ollama/OllamaBackend.kt — the three `String.format` sites.
    - .planning/phases/26-coverage-static-analysis-debt-docs/26-01-SUMMARY.md, 26-02-SUMMARY.md, 26-03-SUMMARY.md — every coverage floor those plans claimed, with the covered/total pairs they recorded. The seal re-verifies each on the merged tree.
  </read_first>

  <files>
    detekt-baseline.xml,
    src/main/kotlin/com/six2dez/burp/aiagent/backends/cli/CliBackend.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/KtorMcpServerManager.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/components/ToolInvocationDialog.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt,
    src/main/kotlin/com/six2dez/burp/aiagent/backends/ollama/OllamaBackend.kt,
    .planning/phases/26-coverage-static-analysis-debt-docs/26-COVERAGE.md
  </files>

  <action>
    **`UseCheckOrError`.** Each of the six is a bare `throw IllegalStateException(message)`. Replace
    with `error(message)`, which throws the same type with the same message. Where the throw already
    sits inside an `if`, prefer `check(condition) { message }` with the condition inverted, which is
    what the rule is actually asking for. Do not change any message string: `KtorMcpServerManager`'s
    three messages are asserted by existing tests, and `McpToolExecutorImpl`'s Pro-edition message is
    surfaced to the user.

    **`UseRequire`.** Each of the four is a guarded `throw IllegalArgumentException(message)`. Convert
    to `require(condition) { message }` with the condition inverted. `McpToolHelpers.resolveReportPath`
    is the sensitive one — plan 26-02 asserts both of its rejections behaviourally, including the
    path-containment guard, so the exception type and the message must survive the conversion
    unchanged. Run 26-02's test class immediately after converting it, before moving on.

    **`ImplicitDefaultLocale`.** Add `Locale.ROOT` as the first argument to the three `String.format`
    calls. This is a behaviour change under a Turkish or Arabic-digit locale, which is the point of the
    rule; note it in the SUMMARY rather than presenting it as purely cosmetic.

    Remove the thirteen matching `<ID>` lines in the same commits as their fixes.

    **The seal.** Run the whole suite with coverage:
    `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport`. From
    `build/reports/jacoco/test/jacocoTestReport.xml`, build the post-phase per-package table for the
    same nine scopes the pre-phase table above lists, plus the four per-file floors plans 26-01 through
    26-03 claimed. Write
    `.planning/phases/26-coverage-static-analysis-debt-docs/26-COVERAGE.md` containing: the pre-phase
    table exactly as recorded above with its command and commit `4f0ebd7`; the post-phase table with
    its command and the current commit; a per-floor MET/MISSED column against every floor claimed by
    plans 26-01, 26-02 and 26-03; and a short paragraph stating where the SC2 target numbers came from
    and why they were set against the post-Phase-25 measurement rather than the 2026-08-05 milestone
    figure.

    If a floor claimed in an isolated worktree is MISSED after merge, say so plainly in
    `26-COVERAGE.md` and in the SUMMARY. Do not adjust the floor to match the outcome — a floor that
    moves to meet the result measures nothing.
  </action>

  <acceptance_criteria>
    - `grep -c '<ID>' detekt-baseline.xml` returns at most 1045. The exact final number is recorded in the SUMMARY beside the 1096 starting count and the arithmetic that connects them.
    - The SUMMARY reports the removals split into stale entries and fixed findings, and the fixed-finding count is at least 40.
    - `git diff -U0 ab567fb..HEAD -- detekt-baseline.xml | grep -c '^+.*<ID>'` returns 0 — across the ENTIRE phase, not just this plan, no entry was added.
    - `git diff --quiet detekt.yml` exits 0.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` green with zero findings; `./gradlew ktlintCheck` green.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` reports 0 failures and exactly 1 skip.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest'` is green after the `resolveReportPath` conversion — run separately and recorded.
    - `26-COVERAGE.md` exists and contains both tables, both commands, both commits, and a MET/MISSED column covering every floor claimed by plans 26-01, 26-02 and 26-03.
    - From the post-phase jacoco XML, project-wide LINE covered/(covered+missed) >= 0.570 and BRANCH >= 0.348. Pre-phase: 14138/25096 = 0.5634 line, 3705/10860 = 0.3411 branch.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew build` completes — the shadowJar path still builds after the source edits.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport detekt ktlintCheck</automated>
  </verify>

  <done>
    The baseline is at or below 1045 entries with at least 40 of the removals backed by a source fix,
    the phase's whole-history diff on that file adds nothing, `detekt.yml` is untouched, and
    `26-COVERAGE.md` records both coverage endpoints with their provenance and an honest MET/MISSED
    verdict on every floor this phase claimed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static-analysis configuration → the finding count | `detekt.yml` and `detekt-baseline.xml` together determine what is reported. Either can be edited to make findings vanish without any code improving. |
| a deleted "unused" declaration → runtime behaviour | Detekt's unused-symbol rules are static; a declaration reached only reflectively, by a service loader or by a Kotlin synthetic path can be flagged and still be load-bearing. |
| idiom conversion → an asserted exception contract | Two of the `UseRequire` sites are security guards that plan 26-02 asserts by exception type and message. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-07-01 | Repudiation | `detekt.yml` weakening | high | mitigate | Prohibition plus an acceptance criterion on every task that `git diff --quiet detekt.yml` exits 0. A count that falls because a rule stopped firing is not progress and would misreport QUAL-07. |
| T-26-07-02 | Repudiation | a hollow shrink | medium | mitigate | The removals are split into stale entries and fixed findings, and at least 40 must be fixed findings. The eleven target categories are named up front, and the five excluded ones are named with reasons, so the shape of the work is on the record rather than inferred from a number. |
| T-26-07-03 | Repudiation | re-baselining a fixed finding | high | mitigate | `git diff -U0 ab567fb..HEAD -- detekt-baseline.xml | grep -c '^+.*<ID>'` must return 0 across the WHOLE phase. The `detektBaseline` probe in Task 1 must be restored and verified before any real edit, because a probe left in place would re-baseline everything this plan is about to fix. |
| T-26-07-04 | Tampering | deleting a load-bearing "unused" declaration | high | mitigate | Every deletion is preceded by a recorded whole-tree reference-count grep, the full suite must stay green at the baseline failure and skip counts, and `./gradlew build` must still produce the shadowJar. `ChatPanel.cancelCurrentRequest` is called out by name because it neighbours a function plan 26-04 just edited. |
| T-26-07-05 | Elevation of Privilege | `UseRequire` conversion on a path-containment guard | high | mitigate | `McpToolHelpers.resolveReportPath`'s two guards are converted with the exception type and message preserved, and plan 26-02's test class is re-run immediately afterwards and recorded separately from the whole-suite run. |
| T-26-07-06 | Tampering | mass `@Suppress` as a shortcut | high | mitigate | At most two new `@Suppress` annotations across the whole plan, each on one declaration with a stated reason, each quoted in the SUMMARY. |
| T-26-07-07 | Denial of Service | `ImplicitDefaultLocale` conversion | low | accept | Adding `Locale.ROOT` changes formatting under a Turkish or Arabic-digit locale — which is the defect the rule names. Recorded as a deliberate behaviour change rather than presented as cosmetic. |
| T-26-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package and adds no Gradle dependency; the `dependencies` block is not touched. No package-legitimacy audit is required because there is no install task. |
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew build test detekt ktlintCheck` all green; 0 failures, 1 skip.
- `grep -c '<ID>' detekt-baseline.xml` <= 1045.
- `git diff -U0 ab567fb..HEAD -- detekt-baseline.xml | grep -c '^+.*<ID>'` returns 0.
- `git log --oneline ab567fb..HEAD -- detekt-baseline.xml` output quoted in the SUMMARY.
- `git diff --quiet detekt.yml` exits 0.
- `git diff --quiet build.gradle.kts` — this plan makes no build-file change.
- `26-COVERAGE.md` committed with both endpoints, their commands and their commits.
</verification>

<success_criteria>
- SC3 satisfied on both clauses, with the second proven by one command.
- At least 51 entries removed, at least 40 of them backed by a source fix; `detekt.yml` untouched.
- SC2 sealed: project-wide line >= 57.0%, branch >= 34.8%, and every per-package floor this phase claimed re-verified on the merged tree with an honest MET/MISSED verdict.
- The suite is green at the 4f0ebd7 failure and skip counts, and the shadowJar still builds.
</success_criteria>

<output>
Create `.planning/phases/26-coverage-static-analysis-debt-docs/26-07-SUMMARY.md` when done.
</output>
