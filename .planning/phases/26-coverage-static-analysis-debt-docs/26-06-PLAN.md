---
phase: 26-coverage-static-analysis-debt-docs
plan: 06
type: execute
wave: 2
depends_on: [26-03, 26-04]
files_modified:
  - DECISIONS.md
  - src/test/kotlin/com/six2dez/burp/aiagent/DecisionsAdrTest.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisor.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisorConnectionTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpTestServerSupport.kt
  - docs/mcp-hardening.md
autonomous: true
requirements: [DOC-03, QUAL-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "W-1a / 25-REVIEW WR-01: ADR-16 carries a SEVENTH `Residual:` bullet recording that the takeover proof's HMAC message is fully known to a squatter, which makes a captured proof an offline verifier for the MCP token, and that the mitigation is the token-strength floor plan 26-03 landed — not an aspiration."
    - "W-1a / 25-REVIEW IN-02: `MIN_ADR16_RESIDUALS` equals the number of `Residual:` bullets ADR-16 actually ships (7), raised in the SAME commit as the bullet, so the guard can catch the deletion of exactly one bullet — which at 5-against-6 it could not."
    - "The residual guard is proven falsifiable: deleting exactly one `Residual:` bullet from ADR-16 turns `DecisionsAdrTest` red, recorded as a red probe with its observed output. At the pre-plan bound it stayed green, which is the defect being closed."
    - "SC4 / SC6: `DECISIONS.md` carries `## ADR-17` recording QUAL-07's three dispositions — the direction of the detekt baseline, the disposition of `ChatPanel`'s EDT enforcement (copied VERBATIM from `26-04-SUMMARY.md` § `SC4 decision`, in the same way ADR-16 clause 1 copies 25-01's), and the accurate statement of `SecretCipher`'s at-rest guarantee."
    - "ADR-17's `SecretCipher` clause states that the AES-256-GCM master key is stored Base64-encoded in Burp Preferences beside the ciphertext it protects, so the property is obfuscation against casual inspection of a preferences file or export and not protection against a local attacker who can read those preferences."
    - "`DecisionsAdrTest` is updated in the same commit as ADR-17: its highest-ADR assertion now requires `## ADR-17` to exist and `## ADR-18` not to, so the NEXT phase's author must claim a number deliberately — the property that assertion exists for."
    - "ADR-15's final `Residual:` bullet, which records `assertEdt()` as a production no-op, is corrected to match what plan 26-04 actually shipped. Whether it becomes a closed residual or a restated one depends on 26-04's selection; either way it no longer describes a state that has changed."
    - "W-3 / 25-REVIEW WR-03: when TLS is enabled and the MCP server is bound to a NON-loopback host, `McpSupervisor.openConnection` emits a diagnostic naming the actual reason — certificate pinning is applied to loopback hosts only and this server is bound elsewhere — instead of leaving the operator with the `no compatible MCP server was detected` line, which is the opposite of what happened."
    - "W-3: `docs/mcp-hardening.md` item 3 no longer promises a diagnostic that a non-loopback deployment never sees. It states the loopback scope of the pin and names the line a non-loopback operator actually gets."
    - "W-3: the non-loopback TLS path has a test. `McpSupervisorConnectionTest` asserts BOTH that no TLS override is installed for a non-loopback host AND that the honest diagnostic is emitted — the review measured that every Phase 25 fixture pinned 127.0.0.1, so this path had zero coverage."
    - "W-3 scope, stated rather than left implicit: this plan takes the review's MINIMUM remedy (an honest diagnostic) and NOT its broader one (dropping the loopback gate so external-mode TLS takeover works). ADR-16 records the wider gap as an accepted residual with the reason, so it is a decision on the record rather than an omission."
    - "No `.kt:NNN` line-number citation pointing at CURRENT code appears anywhere added to `DECISIONS.md`. Cross-references name symbols; a line number is admissible only where it pins historical evidence at a named commit (ADR-15's own correction)."
    - "`detekt-baseline.xml` is byte-identical to its state at 4f0ebd7 after this plan."
  artifacts:
    - "DECISIONS.md — ADR-16's seventh `Residual:` bullet and its W-3 residual; ADR-17 in full; ADR-15's corrected final residual"
    - "src/test/kotlin/com/six2dez/burp/aiagent/DecisionsAdrTest.kt — `MIN_ADR16_RESIDUALS` at 7, the highest-ADR assertion moved to ADR-17, and a guard on ADR-17's own clauses"
    - "src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisor.kt — the non-loopback TLS diagnostic"
    - "src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisorConnectionTest.kt — the non-loopback diagnostic assertion"
    - "src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpTestServerSupport.kt — a non-loopback TLS settings fixture, which the review measured as absent"
    - "docs/mcp-hardening.md — a corrected item 3"
  key_links:
    - "`26-03-SUMMARY.md` § W-1b outcome -> ADR-16's seventh residual: the bullet claims a mitigation, and 26-03 is what makes that claim true. If 26-03 did not land the floor, the bullet must say the mitigation is absent rather than assert one that does not ship."
    - "`26-04-SUMMARY.md` § SC4 decision -> ADR-17 clause 2, verbatim: the ADR copies the developer's selection rather than paraphrasing it, exactly as ADR-16 clause 1 copies 25-01's"
    - "`26-04-SUMMARY.md` § Handover to 26-06 -> ADR-15's final residual bullet: 26-04 identified it as stale and deliberately did not edit `DECISIONS.md`; this plan owns that file"
    - "`McpSupervisor.openConnection`'s host gate -> `docs/mcp-hardening.md` item 3: the runbook promises a diagnostic the shipped code cannot produce for a non-loopback bind; the code and the runbook must be corrected together or the mismatch just moves"
  prohibitions:
    - statement: "No `.kt:NNN` citation pointing at current code may be added to `DECISIONS.md`. ADR-15 records that eighteen such citations in one phase were every one of them wrong within that same phase. Name symbols; a line number is admissible only when pinning historical evidence at a named commit."
      severity: high
    - statement: "Do not paraphrase the developer's SC4 selection. Copy it verbatim from `26-04-SUMMARY.md`. A paraphrased decision is a second decision made by whoever paraphrased it."
      severity: high
    - statement: "Do not write ADR-16's seventh residual as though the token-strength floor exists if plan 26-03 did not land it. Read `26-03-SUMMARY.md` first. A residual bullet that claims a mitigation which does not ship is the exact failure T-25-17 exists to prevent, inverted."
      severity: high
    - statement: "Do not lower `MIN_ADR16_RESIDUALS` below the number of bullets ADR-16 actually ships, and do not raise it above. The bound must EQUAL the shipped count or the guard means something other than what its own comment says."
      severity: high
    - statement: "Do not remove the loopback gate from `McpSupervisor.openConnection`'s TLS branch in this plan. That is a security-behaviour change to when the extension will shut down a remote listener, on a path with no non-loopback test coverage before this plan, in a phase scoped to coverage, static-analysis debt and documentation. Record it as a residual and a backlog item instead."
      severity: high
    - statement: "No wall-clock threshold and no fixed `Thread.sleep` in any assertion."
      severity: medium
---

<objective>
The `DECISIONS.md` half of SC6, the ADR record for SC4's outcome, and the two Phase-25 review items
that are documentation-accuracy defects: ADR-16's missing residual with its too-loose guard (W-1a /
IN-02), and the non-loopback TLS diagnostic that contradicts the runbook Phase 25 shipped (W-3).

Purpose: Phase 25's verification passed 5/5 while recording three warnings, all of which reduce to the
same shape — something is true of the shipped code that the written record does not say, or says
wrongly. That is precisely what a phase titled "Docs" is for. ADR-16's residual guard is bounded one
below the count it protects, so it cannot catch the deletion it exists to catch. The takeover runbook
promises a diagnostic that a non-loopback deployment never sees. And `assert()`-based EDT enforcement
is recorded in ADR-15 as a production no-op, which plan 26-04 may have just changed.

Output: ADR-16 amended and its guard tightened, ADR-17 written, ADR-15 corrected, and the non-loopback
TLS path given an honest diagnostic, a test and a runbook that matches it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-secondary-hardening/25-REVIEW.md
@.planning/phases/25-secondary-hardening/25-VERIFICATION.md
@.planning/phases/26-coverage-static-analysis-debt-docs/26-03-SUMMARY.md
@.planning/phases/26-coverage-static-analysis-debt-docs/26-04-SUMMARY.md
@CLAUDE.md
@AGENTS.md
</context>

## Why this plan is in wave 2

Two content dependencies, not merge friction. ADR-16's seventh residual asserts a mitigation that plan
`26-03` ships, and ADR-17 clause 2 copies plan `26-04`'s blocking-checkpoint selection verbatim.
Neither SUMMARY exists until wave 1 completes. This is the same shape as Phase 25, where `25-03` was
forced into wave 2 because its ADR-16 clause copied `25-01`'s decision out of a SUMMARY that did not
yet exist.

## Measured facts this plan is built on

Measured by the planner at commit `4f0ebd7` on 2026-08-22:

- `DECISIONS.md` runs to `## ADR-16`; ADR-16's section contains exactly **6** `Residual:` bullets.
- `DecisionsAdrTest` declares `MIN_ADR16_RESIDUALS = 5`. Six shipped against a bound of five means
  deleting exactly one bullet leaves the suite green — the review's IN-02, confirmed by the verifier
  as measured fact rather than inference.
- `DecisionsAdrTest.adr16ExistsAndIsTheHighestNumberedAdr` asserts that no line starts `## ADR-17`.
  **Adding ADR-17 without updating that test turns the suite red**, which is the assertion working as
  designed and must be handled in the same commit.
- `build.gradle.kts` `tasks.test` already declares `inputs.file("DECISIONS.md")` as `adrRecord`, so
  this plan needs no build-file change.
- `McpSupervisor.openConnection`'s TLS branch is gated on `isLoopbackUrlHost(url.host)`, and
  `isLoopbackUrlHost` compares against three literal strings. For any other host no override is
  installed, the JDK default trust store rejects the extension's own `CN=burp-mcp` certificate,
  `probeExistingServer` returns false and `handleBindFailure` takes the no-compatible-server limb —
  which schedules no retry and tells the operator the opposite of what happened.
- `docs/mcp-hardening.md` item 3 promises the operator a specific Output line about an unreadable
  keystore. On a non-loopback bind that line never appears however readable the keystore is.
- Every Phase 25 fixture in `McpTestServerSupport` pins `host = "127.0.0.1"`, including
  `externalTlsSettings`. The non-loopback path has no test at all.

## Artifacts this phase produces

| Symbol / section | File | Note |
|---|---|---|
| ADR-16 seventh `Residual:` bullet | `DECISIONS.md` | W-1a |
| ADR-16 non-loopback residual | `DECISIONS.md` | W-3 scope statement |
| `## ADR-17` | `DECISIONS.md` | QUAL-07's three dispositions |
| `MIN_ADR16_RESIDUALS = 7` | `DecisionsAdrTest.kt` | IN-02 |
| ADR-17 guard assertions | `DecisionsAdrTest.kt` | highest-ADR moves to 17 |
| non-loopback TLS diagnostic | `McpSupervisor.kt` | W-3 |
| non-loopback TLS settings fixture | `McpTestServerSupport.kt` | first in the repo |

<tasks>

<task type="tracer">
  <name>Task 1: ADR-16's seventh residual and a guard bound that can catch its deletion</name>

  <read_first>
    - DECISIONS.md `## ADR-16` in full — all three decision clauses and all six existing `Residual:` bullets, so the new one matches their shape and does not restate one of them.
    - src/test/kotlin/com/six2dez/burp/aiagent/DecisionsAdrTest.kt — `MIN_ADR16_RESIDUALS`, its KDoc, `adrSection`, and `adr16RecordsEveryResidualItAccepts` with its comment enumerating the five residuals it believed were shipped.
    - .planning/phases/25-secondary-hardening/25-REVIEW.md WR-01 and IN-02 — the residual's exact technical content and the measured guard defect.
    - .planning/phases/26-coverage-static-analysis-debt-docs/26-03-SUMMARY.md § `W-1b outcome` — whether the token-strength floor actually landed, and in what form. Read this BEFORE writing the bullet.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpTakeoverProof.kt — `forTarget`'s message construction, so the bullet describes the real HMAC input rather than a remembered one.
  </read_first>

  <files>DECISIONS.md, src/test/kotlin/com/six2dez/burp/aiagent/DecisionsAdrTest.kt</files>

  <action>
    Append a seventh `Residual:` bullet to ADR-16, in the same shape as the six above it. Its content:
    the proof's HMAC message is composed entirely of values a squatting process already holds — the
    domain-separated prefix, the host and port it is being dialled on, and a time-window index derived
    from the wall clock — so the value it receives is an offline verifier for the MCP token itself, one
    HMAC evaluation per guess, with no interaction and no rate limit. Recovering the token restores the
    Finding-7 outcome, because the bearer form still authenticates. State that this is not a regression
    — the pre-phase behaviour handed over the plaintext token at any entropy — and that it is infeasible
    against the 32-random-byte token `McpSettings.generateToken` produces by default.

    Then state the mitigation as it ACTUALLY is, having read `26-03-SUMMARY.md`. If the token-strength
    floor landed, name `McpSettings.isTokenWeak` and `Defaults.MCP_MIN_TOKEN_LENGTH` and say that the
    check is advisory — it surfaces a notice and does not refuse a weak token. If it did not land, say
    that no floor is enforced and that the only validation is the blank check. Do not write the bullet
    from this plan's expectation of what 26-03 did.

    Raise `MIN_ADR16_RESIDUALS` to 7 in the same commit, and rewrite the constant's KDoc and the
    assertion's comment so the enumeration matches the bullets actually shipped. The comment currently
    enumerates five; leaving it stale would reproduce, in the guard itself, the drift the guard exists
    to catch.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*DecisionsAdrTest'` is green.
    - The count of lines containing `Residual:` inside the `## ADR-16` slice equals 7, and `MIN_ADR16_RESIDUALS` equals 7. Both numbers are stated in the SUMMARY.
    - RED PROBE, recorded with observed output: delete exactly ONE `Residual:` bullet from ADR-16 and record that `DecisionsAdrTest` turns red. Then restore, re-run, and record green. Restore with `git checkout HEAD -- DECISIONS.md` if committed, otherwise by hand. Never `git stash` — `refs/stash` is shared across linked worktrees.
    - COUNTERFACTUAL, recorded: state in the SUMMARY that the same single-bullet deletion left the suite GREEN at the pre-plan bound of 5, and say whether that was verified by running it or reasoned from the arithmetic.
    - `grep -cE '\.kt:[0-9]+' DECISIONS.md` does not increase relative to `git show HEAD:DECISIONS.md` — no new line-number citation into current code (ADR-15's rule).
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck` green and `git diff --quiet detekt-baseline.xml` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*DecisionsAdrTest' detekt ktlintCheck</automated>
  </verify>

  <done>
    ADR-16 records the offline-guessing residual with an accurate statement of its mitigation, and the
    guard's bound equals the shipped count so it can catch the deletion of exactly one bullet — proven
    by a red probe that was green before this task.
  </done>
</task>

<task type="auto">
  <name>Task 2: ADR-17 — QUAL-07's three dispositions, and ADR-15's stale residual</name>

  <read_first>
    - .planning/phases/26-coverage-static-analysis-debt-docs/26-04-SUMMARY.md § `SC4 decision` and § `Handover to 26-06` — the verbatim selection and the replacement wording for ADR-15's stale bullet.
    - DECISIONS.md ADR-16 in full — the structural template for a new ADR in this repo: Context, Decision (numbered clauses), Consequences with `Residual:` bullets, and the verbatim-quotation style ADR-16 clause 1 uses for a checkpoint selection.
    - DECISIONS.md ADR-15's final `Residual:` bullet — the one recording `assertEdt()` as a production no-op — and ADR-15's cross-reference rule about symbols versus line numbers.
    - src/test/kotlin/com/six2dez/burp/aiagent/DecisionsAdrTest.kt — `adr16ExistsAndIsTheHighestNumberedAdr`, which currently asserts `## ADR-17` does NOT exist.
    - src/main/kotlin/com/six2dez/burp/aiagent/config/SecretCipher.kt — the class KDoc and `MASTER_KEY_PREF_KEY`, so the at-rest clause describes the real storage rather than a remembered one.
    - README.md and SPEC.md as plan 26-05 left them — ADR-17's at-rest clause must agree with the user-facing wording, not compete with it.
    - detekt-baseline.xml and the plan `26-07-PLAN.md` — for the baseline-direction clause, so ADR-17 states the rule the next phase inherits rather than this phase's numbers.
  </read_first>

  <files>DECISIONS.md, src/test/kotlin/com/six2dez/burp/aiagent/DecisionsAdrTest.kt</files>

  <action>
    Write `## ADR-17` at the end of `DECISIONS.md`, following ADR-16's structure, with three decision
    clauses.

    **Clause 1 — the detekt baseline shrinks and is never appended to.** State the rule a future phase
    inherits: a finding introduced by new work is fixed, not baselined; the baseline is a record of
    debt that predates the milestone, and a diff to it that adds an entry inverts what it is for.
    Record the measured fact that the file has been unchanged since 2026-07-29, i.e. across the whole
    of Phases 20-25, and name the one-command check that proves it. State also that a baseline entry
    which no longer corresponds to a live finding is dead weight and may be pruned freely, because
    detekt ignores unmatched entries.

    **Clause 2 — the disposition of `ChatPanel`'s EDT enforcement.** Quote the developer's selection
    VERBATIM from `26-04-SUMMARY.md`, in the blockquote form ADR-16 clause 1 uses. Then state, in your
    own words, what shipped as a consequence, name the other two options and why each was not taken,
    and — if an upgrade option was chosen — name the CI gate that proves the mechanism fires with
    assertions disabled, and why a green `-ea` suite would not have proven it. Do not paraphrase the
    selection itself.

    **Clause 3 — `SecretCipher`'s at-rest guarantee, stated accurately.** The master key is a
    per-install random 256-bit key stored Base64-encoded in a dedicated Burp Preferences entry, beside
    the ciphertext it protects. Therefore the property is: an API key or token does not appear in
    cleartext in a preferences file or export, and a reader of that file cannot recover it without also
    reading the master-key entry. It is NOT: protection against an attacker or a process that can read
    Burp Preferences. State why the design is still the right one — no passphrase prompt, works
    headless, zero user friction — so the ADR records a trade-off rather than an apology, and note that
    ADR-16's filesystem-read residual already depends on exactly this property.

    Under Consequences, record ADR-16's cross-link and add a `Residual:` bullet for W-3's scope: the
    certificate pin remains gated on a loopback host, so an external-mode deployment on a non-loopback
    bind installs no override and cannot take over its own listener; this phase makes the diagnostic
    honest and leaves the wider change — dropping the host gate, which would make external-mode TLS
    takeover work for the first time — as accepted, out of scope for a coverage-and-documentation phase,
    and carried to the backlog.

    Then correct ADR-15's final `Residual:` bullet using 26-04's handover wording. If the enforcement
    was upgraded, the bullet becomes a record that it WAS a no-op and is no longer, pointing at ADR-17
    clause 2. If it was documented as test-only, the bullet stays but points at ADR-17 clause 2 for the
    decision that made it explicit. Either way it must not still read as an open item that nothing
    addressed.

    Update `DecisionsAdrTest` in the same commit: the highest-ADR assertion now requires `## ADR-17` to
    exist and `## ADR-18` not to, keeping the existing requirement that ADR-15 and ADR-16 both survive.
    Add one guard on ADR-17's own content — at minimum that its slice names all three clause subjects —
    so a future edit cannot hollow it out while leaving the heading.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*DecisionsAdrTest'` is green.
    - `grep -c '^## ADR-17' DECISIONS.md` returns 1 and `grep -c '^## ADR-18' DECISIONS.md` returns 0.
    - The verbatim SC4 selection string from `26-04-SUMMARY.md` § `SC4 decision` appears inside the ADR-17 slice; the SUMMARY quotes both and states they were compared character for character.
    - `grep -ci 'burp preferences' DECISIONS.md` returns at least 1 within the ADR-17 slice.
    - RED PROBE, recorded with observed output: delete the `## ADR-17` heading line and record that `DecisionsAdrTest` turns red on the highest-ADR assertion; restore and re-run green.
    - `grep -cE '\.kt:[0-9]+' DECISIONS.md` does not increase relative to `git show HEAD:DECISIONS.md`.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck` green and `git diff --quiet detekt-baseline.xml` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*DecisionsAdrTest' detekt ktlintCheck</automated>
  </verify>

  <done>
    ADR-17 records all three QUAL-07 dispositions with the SC4 selection quoted verbatim, ADR-15's
    stale residual is corrected, and the ADR guard has moved forward so the next phase's author must
    claim ADR-18 deliberately.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: An honest non-loopback TLS diagnostic, its first test, and a runbook that matches</name>

  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisor.kt — `openConnection` with its full KDoc, `isLoopbackUrlHost`, `probeExistingServer`, `requestRemoteShutdown` and `handleBindFailure` including the no-compatible-server limb and what it schedules.
    - src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisorConnectionTest.kt — the four-row truth table, `invokeOpenConnection`, `FakeHttpsURLConnection`, and `McpTestServerSupport.deepStubApi()`.
    - src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpTestServerSupport.kt — `localSettings`, `localTlsSettings`, `externalTlsSettings`; note that all three pin `127.0.0.1`.
    - docs/mcp-hardening.md `## Takeover on a Bind Conflict`, items 1 through 4 and the closing ADR-16 pointer.
    - .planning/phases/25-secondary-hardening/25-REVIEW.md WR-03 — the full argument, including the wider remedy this plan deliberately does not take.
  </read_first>

  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisor.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisorConnectionTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpTestServerSupport.kt,
    docs/mcp-hardening.md
  </files>

  <behavior>
    Failing tests first, in `McpSupervisorConnectionTest`:
    - With TLS enabled and a non-loopback host, `openConnection` installs neither a socket factory
      override nor a hostname verifier — the existing assertion, retained.
    - With the same settings, exactly one Output line is emitted, and it names the configured host and
      states that certificate pinning applies to loopback hosts only. Asserted by verifying the
      interaction on the deep-stubbed logging seam, matching the value against a predicate rather than
      against a whole hard-coded sentence, so a wording improvement does not break the test while a
      change of meaning does.
    - The existing loopback rows still emit their own lines and not this one — the diagnostic is
      specific to the branch it describes.
    - With TLS DISABLED and a non-loopback host, no TLS diagnostic is emitted at all, because nothing
      about TLS applies.
  </behavior>

  <action>
    Add a non-loopback TLS settings factory to `McpTestServerSupport` — the review measured that no
    fixture in the repo binds anything but `127.0.0.1`, which is why this branch has never been
    exercised. Give it a name that says what it is for.

    In `McpSupervisor.openConnection`, restructure the TLS branch so the non-loopback case is an
    explicit limb rather than a silent fall-through: when TLS is on and the connection is an HTTPS
    connection but the host is not loopback, log one Output line naming the host and stating that
    certificate pinning is applied to loopback hosts only, so no TLS override was installed and the
    takeover was not attempted; tell the operator to free the port manually. Keep the existing
    loopback limbs — the pin branch and the no-pin fail-closed branch — byte-for-byte in behaviour.

    Do NOT remove `isLoopbackUrlHost` from the condition. That is the review's wider remedy and it
    changes when this extension will shut down a listener on a non-loopback host, on a path with no
    prior test coverage, in a phase scoped to coverage, static analysis and documentation. It is
    recorded as an accepted residual in ADR-17 by Task 2 and belongs in the backlog. Say so in the
    SUMMARY as a deliberate exclusion, not as an oversight.

    Correct `docs/mcp-hardening.md` item 3. It currently promises the unreadable-keystore diagnostic
    without qualification; make it state that the pin, and that diagnostic, apply to a loopback bind,
    and name the line a non-loopback operator gets instead. Keep the item's existing shape and its
    "treat that line as an action item" framing. Do not restate ADR-16's reasoning in the runbook —
    the closing pointer already carries that division.

    While in `McpSupervisor.kt`, note but do NOT fix `isLoopbackUrlHost`'s unreachable IPv6 arm
    (25-REVIEW IN-07): `java.net.URL.getHost()` returns the bracketed form, so that comparison can
    never match. It is a dead condition, not a defect, it is pre-existing, and touching it widens this
    task into the host-gate change the prohibition above excludes. Record it in the SUMMARY as a
    known, unfixed, non-blocking item so the next reader does not have to rediscover it.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpSupervisorConnectionTest' --tests '*McpTakeover*' --tests '*McpSupervisorProbeTest'` is green.
    - `McpSupervisorConnectionTest` reports at least 6 tests, up from the 4 recorded at 4f0ebd7.
    - RED PROBE, recorded with observed output: remove the new diagnostic line, run the class, and record that the non-loopback diagnostic test turns red while the three pre-existing rows stay green. Restore with `git checkout HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpSupervisor.kt`; never `git stash`.
    - `grep -ci 'loopback' docs/mcp-hardening.md` returns at least 1 within the `## Takeover on a Bind Conflict` section, and item 3 names both the loopback scope and the non-loopback line.
    - The SUMMARY records two deliberate exclusions with reasons: the host-gate change (WR-03's wider remedy) and the unreachable IPv6 arm (IN-07).
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` reports 0 failures.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck` green and `git diff --quiet detekt-baseline.xml` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck</automated>
  </verify>

  <done>
    A non-loopback TLS bind produces a diagnostic that names the real reason, that branch has its first
    test and its first non-loopback fixture, the runbook agrees with the shipped behaviour, and both
    deliberate exclusions are on the record.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Squatting local process → MCP takeover proof | The proof value crosses to a hostile listener by design; what it discloses is the subject of ADR-16's seventh residual. |
| Design record → future contributor's decisions | An ADR that omits an accepted residual, or a guard bounded below the count it protects, is how a residual nobody wrote down becomes a residual nobody noticed. |
| `McpSupervisor` Output lines → operator's remediation | The diagnostic determines whether the operator frees the port or hunts a keystore that was never the problem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-06-01 | Information Disclosure | takeover proof as offline verifier | high | mitigate | Recorded as ADR-16's seventh residual with an accurate statement of its mitigation, read out of `26-03-SUMMARY.md` rather than assumed. The residual is disclosed, bounded (infeasible against the generated 32-byte token) and paired with the advisory floor plan 26-03 landed. |
| T-26-06-02 | Repudiation | residual guard bounded below the shipped count | medium | mitigate | `MIN_ADR16_RESIDUALS` is raised to equal the shipped count in the same commit as the new bullet, and the tightening is proven by a red probe that deletes exactly one bullet — the deletion the old bound could not catch. |
| T-26-06-03 | Repudiation | undocumented QUAL-07 dispositions | medium | mitigate | ADR-17 records all three, with the SC4 selection quoted verbatim from the blocking checkpoint rather than paraphrased. |
| T-26-06-04 | Information Disclosure | overstated at-rest claim in the design record | high | mitigate | ADR-17 clause 3 states that the master key sits in Burp Preferences beside its ciphertext and names what the property therefore is and is not, agreeing with the user-facing wording plan 26-05 landed. |
| T-26-06-05 | Denial of Service | non-loopback bind conflict | medium | mitigate | The operator is told the real reason and what to do, instead of being told no compatible server was found when the listener was their own. The underlying inability to take over a non-loopback listener is unchanged and is recorded as an accepted residual, so it is a decision rather than an omission. |
| T-26-06-06 | Spoofing | removing the loopback gate | high | accept | Explicitly NOT done in this plan. Dropping the gate would extend certificate-pinned takeover to non-loopback hosts — plausibly an improvement, but it changes when this extension shuts down a remote listener, on a path with no prior test coverage, inside a phase scoped to coverage and documentation. Recorded as an ADR-17 residual and a backlog item. |
| T-26-06-07 | Tampering | drifting line-number citations in ADRs | low | mitigate | Prohibition plus a criterion that the count of `.kt:NNN` citations in `DECISIONS.md` does not increase. ADR-15 records that eighteen such citations in one phase were every one of them wrong within that phase. |
| T-26-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package and adds no Gradle dependency. No package-legitimacy audit is required because there is no install task. |
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck` all green.
- `git diff --stat detekt-baseline.xml` and `git diff --stat build.gradle.kts` both print nothing.
- Three red probes recorded with observed output: one `Residual:` bullet deleted, the ADR-17 heading deleted, and the non-loopback diagnostic removed.
- `grep -cE '\.kt:[0-9]+' DECISIONS.md` compared against `git show HEAD:DECISIONS.md` and reported unchanged or lower.

## Explicitly excluded, with reasons

| Item | Source | Decision |
|---|---|---|
| Dropping `isLoopbackUrlHost` from the TLS branch so external-mode takeover works | 25-REVIEW WR-03 "better" remedy | EXCLUDED — security-behaviour change on an untested path, outside a coverage/docs phase. Recorded as an ADR-17 residual and a backlog item. |
| `isLoopbackUrlHost`'s unreachable `"::1"` arm | 25-REVIEW IN-07 | EXCLUDED — a dead condition, pre-existing, and fixing it means touching the host gate excluded above. Recorded in the SUMMARY. |
| `McpTls.pinnedLeafSha256` password-array zeroing | 25-REVIEW IN-01 | EXCLUDED — a hygiene item in `McpTls.kt`, which no plan in this phase owns; belongs to a hardening phase, not a documentation one. Recorded here so it is not silently dropped. |
| `instanceFollowRedirects = false` on the takeover client | 25-REVIEW IN-03 | EXCLUDED — the review itself concluded it is not exploitable beyond what a squatter already has; it is one-line hardening, not documentation, and belongs in the backlog. |
| `McpTakeoverSquatterTest`'s shadowed `SHUTDOWN_PATH` constant | 25-REVIEW IN-05 | EXCLUDED — plan 26-07 owns test-source cleanups; raising it here would put two plans in one test file. Carried to the backlog. |
| `Ipv4LiteralTest`'s unpinned two- and three-part maxima | 25-REVIEW IN-06 | EXCLUDED from this plan — it is coverage work in `util`, and plan 26-03 owns that package. Recorded so the gap is not lost. |
| `SsrfGuardNoResolutionTest`'s process-global counter attribution | 25-REVIEW IN-04 | EXCLUDED — the verifier measured the failure mode as a false RED, not a vacuous green, i.e. the safe direction. Backlog. |
</verification>

<success_criteria>
- ADR-16 carries seven residuals and a guard bound that equals them.
- ADR-17 exists, records QUAL-07's three dispositions, and quotes the SC4 selection verbatim.
- ADR-15's stale residual is corrected.
- The non-loopback TLS path has an honest diagnostic, its first test, its first fixture, and a runbook that matches it.
- Every excluded Phase-25 review item is named with a reason rather than dropped.
</success_criteria>

<output>
Create `.planning/phases/26-coverage-static-analysis-debt-docs/26-06-SUMMARY.md` when done.
</output>
