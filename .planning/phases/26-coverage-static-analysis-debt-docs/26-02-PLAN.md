---
phase: 26-coverage-static-analysis-debt-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolModelsTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/schema/SerializationTest.kt
autonomous: true
requirements: [QUAL-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC2: line coverage on the `com.six2dez.burp.aiagent.mcp` tree (`mcp` + `mcp.tools` + `mcp.schema` + `mcp.external`, summed) is >= 65.0% against the 61.83% (2488/4024) recorded at 4f0ebd7 on 2026-08-22."
    - "SC2: line coverage on package `com/six2dez/burp/aiagent/mcp/tools` is >= 49.0% against the recorded 43.04% (956/2221)."
    - "SC2: line coverage on package `com/six2dez/burp/aiagent/mcp/schema` is >= 70.0% against the recorded 48.59% (69/142)."
    - "SC2: line coverage on sourcefile `McpToolHelpers.kt` is >= 58.0% against the recorded 10.27% (23/224)."
    - "SC2: line coverage on sourcefile `McpToolModels.kt` is >= 50.0% against the recorded 26.35% (83/315)."
    - "SC2: line coverage on sourcefile `Serialization.kt` (mcp/schema) is >= 70.0% against the recorded 35.19% (38/108)."
    - "`resolveReportPath` refuses a path that escapes the user home — a relative path containing parent-directory segments that normalises above `user.home` throws, and the absolute-path form that lands outside `user.home` throws. The guard is asserted as a rejection, not only as an acceptance."
    - "`resolveReportPath` accepts a plain relative name and resolves it under `user.home`, and accepts an absolute path already under `user.home`."
    - "`sanitizeHeaders` replaces the value of `Cookie` and `Set-Cookie` under a policy whose `stripCookies` is set, replaces `Authorization`, `Proxy-Authorization`, `X-API-Key` and `Api-Key` under `redactTokens`, and anonymises `Host` under `anonymizeHosts` — each asserted per privacy mode, and header-name matching is asserted case-insensitively so a real-world `COOKIE:` spelling is caught."
    - "`sanitizeHeaders` preserves the ORIGINAL header-name casing in its output map and preserves input order, so sanitisation does not silently rewrite the header block it is describing."
    - "`maybeAnonymizeUrl` returns its input unchanged outside STRICT, anonymises only the host component in STRICT, preserves scheme/port/path/query/fragment, and returns the input unchanged for a string that is not a parseable URI."
    - "`decodeJwt` returns the fixed invalid-JWT message for a one-part token, decodes a two-part token with an empty signature line, and yields the invalid-header / invalid-payload placeholders for segments that are not valid Base64URL — no exception escapes."
    - "`truncateIfNeeded` is a byte-length bound, not a character-length bound: a multi-byte UTF-8 payload at the boundary truncates on bytes and the returned suffix names both the original byte count and the limit."
    - "`ensureAllowedProxyHistoryCount` returns for a requested count at the limit and throws for one above it."
    - "`countOccurrences`, `diffLines`, `normalizeHashAlgorithm`, `parseHighlightColor`, `applyReplacements`, `resolveAuditConfig`, `withAiIssuePrefix` and `orderedProxyHistory` each have both their taken and their untaken branch asserted."
    - "Every tool-input model asserted in `McpToolModelsTest` is built by DESERIALISING a JSON payload through `kotlinx.serialization`, not by calling its Kotlin constructor — the production path is model-supplied JSON and that is the path the assertion must exercise."
    - "`HttpServiceParams.toMontoyaServiceOrNull` returns null for a blank hostname and for a port that is zero or negative, and returns a service for a valid pair. The guard is asserted as a rejection."
    - "`detekt-baseline.xml` is byte-identical to its state at 4f0ebd7 after this plan — `./gradlew detekt` is green with no entry added, including for the three new test classes."
  artifacts:
    - "src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt — the fourteen pure/near-pure helpers in `McpToolHelpers.kt`"
    - "src/test/kotlin/com/six2dez/burp/aiagent/mcp/schema/SerializationTest.kt — new test source directory `mcp/schema/`; the four `toSerializableForm` extensions and the schema data classes"
    - "src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolModelsTest.kt — JSON deserialisation of the tool-input models and the `HttpServiceParams` guard"
  key_links:
    - "`McpToolHelpers.sanitizeHeaders` -> `RedactionPolicy.fromMode` -> `Redaction.anonymizeHost`: the header sanitiser is a second, independent redaction path beside `Redaction.redact`, and until this plan nothing asserted it — a privacy control with no test is what PRIV-05 was"
    - "`McpToolHelpers.resolveReportPath` -> `java.nio.file.Path.normalize` -> `startsWith(home)`: the containment check is the only thing between a model-supplied report path and an arbitrary filesystem write location"
    - "model-supplied JSON -> `kotlinx.serialization` -> tool-input data class -> `toMontoyaServiceOrNull`: the blank-host / non-positive-port guard is the last check before a Montoya `HttpService` is built from attacker-influenceable fields"
  prohibitions:
    - statement: "No test in this plan may execute production code without asserting on its result. Deserialising sixty data classes to move a jacoco counter, with no assertion about defaults or guards, converts SC2 into a vanity metric and is explicitly out of bounds."
      severity: high
    - statement: "No existing test may be deleted, renamed, `@Disabled` or narrowed to make this plan's suite green."
      severity: high
    - statement: "No new test class may be named `*IntegrationTest`, `*ConcurrencyTest`, `*BackpressureTest`, `*RestartPolicyTest` or `*SupervisionTest` — those five suffixes are excluded from the fast PR gate by `build.gradle.kts`, so a security assertion carrying one silently stops running in CI."
      severity: medium
    - statement: "No wall-clock threshold and no fixed `Thread.sleep` in any assertion."
      severity: medium
    - statement: "No production source file may be modified by this plan. Every target is already `internal` or public; if one is not, cover a different one and record the exclusion rather than widening a symbol another wave's plan also edits."
      severity: medium
---

<objective>
SC2 (QUAL-06) for the `mcp` tree: raise line coverage on the MCP packages from the measured 61.83% to
at least 65.0%, with the new tests concentrated on the paths that carry privacy and trust decisions —
header sanitisation, URL anonymisation, report-path containment, and the JSON deserialisation of
model-supplied tool inputs.

Purpose: SC2 names `redact`, `mcp` and `config`. Measured on the post-Phase-25 tree, `redact` (96.1%)
and `config` (95.6%) are already saturated; the entire remaining headroom in the criterion's own
package list is the `mcp` tree, and inside it three files hold 503 of the 1536 uncovered lines. Two of
those files hold privacy controls that nothing currently asserts.

Output: three new test classes and a measured `mcp`-tree line coverage at or above 65.0%.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
</context>

## Coverage baseline (measured, not assumed)

Measured by the planner on 2026-08-22 at commit `4f0ebd7`, whole suite, no filter:

```
JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport --continue
```

BUILD SUCCESSFUL, 880 tests / 127 classes / 0 failures / 1 pre-existing skip. Read out of
`build/reports/jacoco/test/jacocoTestReport.xml`:

| scope | line covered/total | line % | branch covered/total | branch % |
|---|---|---|---|---|
| `mcp` (leaf package) | 1326/1436 | 92.34% | 374/477 | 78.41% |
| `mcp/tools` | 956/2221 | **43.04%** | 370/1089 | 33.98% |
| `mcp/schema` | 69/142 | **48.59%** | 35/68 | 51.47% |
| `mcp/external` | 137/225 | 60.89% | 13/66 | 19.70% |
| **`mcp` tree (4 packages summed)** | **2488/4024** | **61.83%** | 792/1700 | 46.59% |
| ↳ `McpToolHelpers.kt` | 23/224 | **10.27%** | 15/149 | 10.07% |
| ↳ `McpToolModels.kt` | 83/315 | **26.35%** | 0/16 | 0.00% |
| ↳ `Serialization.kt` | 38/108 | **35.19%** | 13/38 | 34.21% |

Per-method detail from the same report — the fourteen pure/near-pure helpers in `McpToolHelpers.kt`
hold **111** of that file's 201 missed lines:

`withAiIssuePrefix` 4 · `truncateIfNeeded` 5 · `ensureAllowedProxyHistoryCount` 2 ·
`orderedProxyHistory` 1 · `decodeJwt` 11 · `normalizeHashAlgorithm` 6 · `diffLines` 14 ·
`countOccurrences` 8 · `parseHighlightColor` 4 · `sanitizeHeaders` 16 · `maybeAnonymizeUrl` 15 ·
`resolveReportPath` 15 · `applyReplacements` 5 · `resolveAuditConfig` 5.

The remaining 90 sit in `executeIssueCreate` (54), `findProxyHistoryMatch` (24), `hasEquivalentIssue`
(5) and `getActiveEditor` (8), which are Montoya-bound or EDT-bound and are deliberately **out of
scope** for this plan — see Task 2's exclusion note.

## Artifacts this phase produces

Symbols created by **this plan** (all in test sources; no production symbol is added or re-scoped):

| Symbol | File | Note |
|---|---|---|
| `McpToolHelpersTest` | `src/test/kotlin/.../mcp/tools/McpToolHelpersTest.kt` | fourteen pure helpers |
| `SerializationTest` | `src/test/kotlin/.../mcp/schema/SerializationTest.kt` | new test package directory |
| `McpToolModelsTest` | `src/test/kotlin/.../mcp/tools/McpToolModelsTest.kt` | JSON-in, guard-out |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The measure-test-remeasure loop, proven on the four privacy/trust helpers</name>

  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt — read the whole file; the four targets are `sanitizeHeaders`, `maybeAnonymizeUrl`, `resolveReportPath` and `applyReplacements`.
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt — `RedactionPolicy.fromMode` and `Redaction.anonymizeHost`, which `sanitizeHeaders` and `maybeAnonymizeUrl` delegate to. Read the policy flags (`stripCookies`, `redactTokens`, `anonymizeHosts`) per `PrivacyMode` before writing an expectation.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt — the required constructor arguments; note `api`, `limiter` and `edition` have no defaults.
    - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpScopeFilterTest.kt — an existing test in this package that builds an `McpToolContext`; copy its construction rather than inventing one.
    - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/NormalizeHttpRequestTest.kt — the existing coverage of `normalizeHttpRequest`; do NOT duplicate it in the new class.
  </read_first>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt</files>

  <behavior>
    Failing tests first. `McpToolHelpersTest` asserts:
    - `sanitizeHeaders` under each of the three `PrivacyMode` values: which header values are replaced and which are passed through, for `Cookie`, `Set-Cookie`, `Authorization`, `Proxy-Authorization`, `X-API-Key`, `Api-Key`, `Host` and one ordinary header. Header names supplied in mixed and upper case to prove the match is case-insensitive.
    - `sanitizeHeaders` returns the input header names with their original casing and in their original order.
    - `maybeAnonymizeUrl`: unchanged outside STRICT; in STRICT the host is replaced and scheme, port, path, query and fragment survive; a non-URI string returns unchanged; a URI with no host returns unchanged.
    - `resolveReportPath`: a relative name resolves under `user.home`; an absolute path already under `user.home` is returned normalised; a relative path whose parent-directory segments normalise above `user.home` throws; an absolute path outside `user.home` throws; a blank string throws. Every expectation is computed from `System.getProperty("user.home")` at runtime, never from a hard-coded home directory.
    - `applyReplacements`: an empty map returns the input identically; a single mapping is applied; multiple mappings are all applied; a mapping whose key is absent leaves the content unchanged.
  </behavior>

  <action>
    Create `McpToolHelpersTest` in the existing `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/`
    directory. Build one shared `McpToolContext` factory inside the test class that takes the privacy
    mode and the host salt and fills the rest from a mockito-kotlin `mock<MontoyaApi>()` plus a real
    `McpRequestLimiter` and a `BurpSuiteEdition` constant, mirroring `McpScopeFilterTest`.

    Build `HttpHeader` fixtures with the Montoya static factory `HttpHeader.httpHeader(name, value)`;
    the `montoya-api` artifact is on `testImplementation`, so no mock is needed for a header.

    For `sanitizeHeaders`, derive the expectation from `RedactionPolicy.fromMode(mode)` rather than
    re-encoding the mode-to-flag table in the test — if the policy table changes, the test must move
    with it instead of asserting a stale copy. Assert the replacement SENTINEL values that
    `sanitizeHeaders` writes, and for the host case assert equality against
    `Redaction.anonymizeHost(value, salt)` computed in the test with the same salt.

    For `resolveReportPath`, use a temporary directory only for the outside-home case; do not create
    files under the real `user.home`.

    Record the pre-state and post-state of the two counters this task moves, and state in the SUMMARY
    that this task exists to prove the measure-test-remeasure loop end to end before Tasks 2 and 3 use
    it at volume.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest'` is green and reports at least 14 tests.
    - After `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport`, the `LINE` counter for sourcefile `McpToolHelpers.kt` in `build/reports/jacoco/test/jacocoTestReport.xml` yields covered/(covered+missed) >= 0.300. Recorded pre-phase value: 23/224 = 0.1027.
    - The SUMMARY records the observed `McpToolHelpers.kt` covered/total pair before and after this task, not a bare percentage.
    - RED PROBE, recorded with its observed output: delete the `startsWith(home)` containment check from `resolveReportPath`, run the new class, and record that the escape-path test turns red. Restore with `git checkout HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt` — never `git stash`.
    - `git diff --quiet src/main/kotlin` exits 0 at the end of this task — the probe was restored and no production file is left modified.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` green and `git diff --quiet detekt-baseline.xml` exits 0.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck` green.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest' jacocoTestReport detekt ktlintCheck</automated>
  </verify>

  <done>
    Four privacy/trust helpers are asserted behaviourally, the report-path containment guard is proven
    falsifiable by a recorded red probe, `McpToolHelpers.kt` line coverage is at or above 30.0%, and
    the measure-test-remeasure recipe the rest of the plan uses is demonstrated working.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover the remaining ten pure helpers and take the McpToolHelpers floor</name>

  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt — the ten remaining targets: `withAiIssuePrefix`, `truncateIfNeeded`, `ensureAllowedProxyHistoryCount`, `orderedProxyHistory`, `decodeJwt`, `normalizeHashAlgorithm`, `diffLines`, `countOccurrences`, `parseHighlightColor`, `resolveAuditConfig`.
    - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt — the class Task 1 created; extend it, do not create a second class for the same file.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt — `determinismMode` and `proxyHistoryNewestFirst`, the two flags `orderedProxyHistory` branches on.
  </read_first>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt</files>

  <behavior>
    Failing tests first, appended to `McpToolHelpersTest`:
    - `withAiIssuePrefix`: an unprefixed name gains the prefix; a name already carrying either recognised prefix is returned trimmed and unchanged; the recognition is case-insensitive; leading and trailing whitespace is trimmed.
    - `truncateIfNeeded`: a payload under the limit returns identically; a payload over the limit returns a value whose suffix names the original byte count and the limit; a multi-byte UTF-8 payload is bounded on BYTES not characters; a limit of zero is coerced up to one rather than producing an empty result.
    - `ensureAllowedProxyHistoryCount`: returns at the limit; throws above it, and the thrown message names both the requested and the allowed count.
    - `orderedProxyHistory`: determinism mode sorts by the supplied key regardless of input order; determinism off with newest-first reverses; determinism off with newest-first unset preserves input order.
    - `decodeJwt`: three-part token yields header, payload and signature lines; two-part token yields an empty signature line; one-part token yields the fixed invalid message; a segment that is not valid Base64URL yields the corresponding placeholder without throwing.
    - `normalizeHashAlgorithm`: each of the three shorthand spellings maps to its hyphenated form; an already-hyphenated value passes through; an unknown value is upper-cased and returned; surrounding whitespace is trimmed.
    - `diffLines`: identical inputs, an added line, a removed line, a changed line, and one empty side.
    - `countOccurrences`: zero, one, many, overlapping candidates, and an empty needle (assert whatever the implementation actually does and state it in the test name — do not assume).
    - `parseHighlightColor`: a valid colour name in lower case, a valid one in upper case, a blank string, a null, and an unrecognised name.
    - `resolveAuditConfig`: each of the six aliases the `when` lists, one canonical enum name reached through the `else` branch, and an unrecognised value (assert the observed failure mode, do not assume it is caught).
  </behavior>

  <action>
    Extend `McpToolHelpersTest` with the ten helpers above. Keep one nested `@Nested` group per helper
    so a failure names the helper without reading the stack.

    Where a helper's behaviour on a degenerate input is not obvious from reading it — the empty needle
    in `countOccurrences` and the unrecognised value in `resolveAuditConfig` are the two known cases —
    run it, observe the behaviour, and pin the observed behaviour with a test name that states it.
    Do not guess an expectation and then bend the production code to match; this plan modifies no
    production source.

    Explicitly EXCLUDE `executeIssueCreate`, `findProxyHistoryMatch`, `hasEquivalentIssue` and
    `getActiveEditor` (90 of the file's 201 missed lines). The first three need a deep `MontoyaApi`
    graph — `siteMap()`, `scanner()`, `proxy()` and their return types — where the mock becomes the
    thing under test; `getActiveEditor` walks `KeyboardFocusManager` and is EDT-bound, which
    `-Djava.awt.headless=true` makes untestable here. Record that exclusion and its reason in the
    SUMMARY rather than leaving the gap unexplained.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest'` is green and reports at least 40 tests.
    - After `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport`, the `LINE` counter for sourcefile `McpToolHelpers.kt` yields >= 0.580. Recorded pre-phase value: 23/224 = 0.1027.
    - The SUMMARY records the observed covered/total pair and names the four excluded functions with the reason for each.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` green and `git diff --quiet detekt-baseline.xml` exits 0.
    - `git diff --quiet src/main/kotlin` exits 0 — this task modifies no production source.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest' jacocoTestReport detekt ktlintCheck</automated>
  </verify>

  <done>
    All fourteen pure/near-pure helpers in `McpToolHelpers.kt` have both branch directions asserted;
    the file is at or above 58.0% line coverage; the four Montoya/EDT-bound exclusions are named with
    reasons.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cover the MCP wire schema and the model-supplied tool inputs; take the tree floor</name>

  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/schema/Serialization.kt — the four `toSerializableForm` extensions, `toSiteMapEntry`, and the ten data classes and three enums below them.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/schema/JsonSchema.kt — read before asserting anything about schema shape.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolModels.kt — the `HttpServiceParams` interface and its `toMontoyaService` / `toMontoyaServiceOrNull` defaults, then the tool-input data classes.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt line 22 — `toolJson`, the `Json { encodeDefaults = true }` instance the production path uses; the test must use the same configuration, not a fresh default `Json`.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolCatalog.kt — the `secTier` classification, to choose which tool-input models carry the most trust weight.
    - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/ResponsePreprocessorTest.kt — how this repo mocks Montoya request/response objects with mockito-kotlin.
  </read_first>

  <files>
    src/test/kotlin/com/six2dez/burp/aiagent/mcp/schema/SerializationTest.kt,
    src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolModelsTest.kt
  </files>

  <behavior>
    Failing tests first.

    `SerializationTest` (new directory `src/test/kotlin/com/six2dez/burp/aiagent/mcp/schema/`):
    - `AuditIssue.toSerializableForm` maps every field it reads — name, detail, remediation, host/port/secure, base URL, severity, confidence, request/response list and collaborator interactions — from a mocked `AuditIssue`, and the severity and confidence enums round-trip by NAME so a Montoya enum rename fails loudly rather than silently mapping to the wrong bucket.
    - `HttpRequestResponse.toSerializableForm` maps request, response and notes, and tolerates a null response.
    - `ProxyWebSocketMessage.toSerializableForm` maps both `Direction` values.
    - `toSiteMapEntry` produces the entry shape for a request/response pair.
    - Each schema data class serialises to JSON and deserialises back to an equal instance under `toolJson`.

    `McpToolModelsTest`:
    - For each selected tool-input model, a realistic JSON payload deserialises into the expected instance, and a payload that omits every optional field yields the documented defaults.
    - `toMontoyaServiceOrNull` returns null for a blank hostname, null for port 0, null for a negative port, and a non-null service for a valid pair. `toMontoyaService` applies the `resolveHost` transform it is given.
    - A payload missing a REQUIRED field fails deserialisation with a `MissingFieldException` rather than silently producing a default — assert the failure, because a silently-defaulted required field on a tool input is how a model-supplied call reaches Burp with something the caller never wrote.
  </behavior>

  <action>
    Create the new test source directory `src/test/kotlin/com/six2dez/burp/aiagent/mcp/schema/` and put
    `SerializationTest` in it with `package com.six2dez.burp.aiagent.mcp.schema` — the package
    declaration must match the directory, because `InvalidPackageDeclaration` is a live detekt rule in
    this repo and plan 26-07 is removing the one existing violation, not adding a second.

    Mock the Montoya interfaces with mockito-kotlin, following `ResponsePreprocessorTest`. Where a
    Montoya interface returns another Montoya interface, stub the minimum depth the extension actually
    reads and no more.

    In `McpToolModelsTest`, select the models whose `secTier` in `McpToolCatalog` is `CONFIRM_EACH` or
    `CONFIRM` first — those are the inputs the trust boundary exists for — and work down. Deserialise
    every fixture through the production `toolJson` instance. Do not construct a model with its Kotlin
    constructor except where the test needs a known-good instance to serialise for a round-trip.

    If a floor is missed after the first pass, add assertions for behaviour that is genuinely
    unasserted — a default, a guard, an enum mapping — rather than adding constructor calls with no
    assertion. State in the SUMMARY which models were covered and which were left, so the gap is a
    recorded decision rather than an accident.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*SerializationTest' --tests '*McpToolModelsTest'` is green.
    - After `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport`, from `build/reports/jacoco/test/jacocoTestReport.xml`:
      - sourcefile `Serialization.kt` in package `com/six2dez/burp/aiagent/mcp/schema`: LINE covered/(covered+missed) >= 0.700 (pre-phase 38/108 = 0.3519).
      - sourcefile `McpToolModels.kt`: LINE >= 0.500 (pre-phase 83/315 = 0.2635).
      - package `com/six2dez/burp/aiagent/mcp/schema`: LINE >= 0.700 (pre-phase 69/142 = 0.4859).
      - package `com/six2dez/burp/aiagent/mcp/tools`: LINE >= 0.490 (pre-phase 956/2221 = 0.4304).
      - the four packages `mcp`, `mcp/tools`, `mcp/schema`, `mcp/external` summed: LINE >= 0.650 (pre-phase 2488/4024 = 0.6183).
    - Each of those five numbers is written into the SUMMARY as a covered/total pair beside its pre-phase pair.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` green and `git diff --quiet detekt-baseline.xml` exits 0.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck` green.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test` reports 0 failures across the whole suite.
    - `git diff --quiet src/main/kotlin` exits 0 — this plan modifies no production source.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test jacocoTestReport detekt ktlintCheck</automated>
  </verify>

  <done>
    The MCP wire schema and the model-supplied tool inputs are asserted through the production JSON
    configuration; all five coverage floors are met and recorded as covered/total pairs; the whole
    suite and both static-analysis gates are green with the detekt baseline untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model output → MCP tool input | A tool call parsed out of model text carries a JSON argument blob. `kotlinx.serialization` turning that blob into a tool-input data class is where attacker-influenceable text becomes typed parameters. |
| Burp data → AI prompt | `sanitizeHeaders` and `maybeAnonymizeUrl` are the redaction path for MCP tool RESULTS, independent of `Redaction.redact`'s prompt path. |
| Tool input → filesystem | `resolveReportPath` turns a model-supplied string into a `java.nio.file.Path` that a report is written to. |
| Tool input → Montoya `HttpService` | `toMontoyaServiceOrNull` turns model-supplied host and port into an outbound target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-02-01 | Information Disclosure | `sanitizeHeaders` | high | mitigate | This plan does not change the control; it makes it falsifiable. Case-insensitive matching of `Cookie`, `Set-Cookie`, `Authorization`, `Proxy-Authorization`, `X-API-Key`, `Api-Key` and `Host` is asserted per privacy mode against `RedactionPolicy.fromMode`, so the PRIV-05 failure mode — a real-world header spelling that the matcher misses — cannot recur unnoticed in this second redaction path. |
| T-26-02-02 | Information Disclosure | `maybeAnonymizeUrl` | medium | mitigate | Asserted that STRICT replaces the host and only the host, and that a malformed URL falls back to returning the input rather than throwing into the tool result. |
| T-26-02-03 | Tampering | `resolveReportPath` | high | mitigate | Path containment above `user.home` is asserted as a REJECTION for both the relative-with-parent-segments form and the absolute-outside-home form, and the guard is proven falsifiable by a recorded red probe that deletes the containment check. |
| T-26-02-04 | Spoofing | `toMontoyaServiceOrNull` | medium | mitigate | Blank hostname and non-positive port are asserted to return null, so a partially-specified model-supplied target cannot be turned into an outbound request destination. |
| T-26-02-05 | Tampering | tool-input deserialisation | medium | mitigate | A payload missing a required field is asserted to FAIL rather than default, so a model-emitted call cannot acquire a parameter value the model never wrote. |
| T-26-02-06 | Denial of Service | `truncateIfNeeded` | low | mitigate | The byte-bound is asserted on a multi-byte UTF-8 payload, so the MCP body cap cannot be silently defeated by a character-vs-byte confusion. |
| T-26-02-07 | Repudiation | assertion-free coverage | medium | mitigate | Prohibition: a test that executes production code without asserting is out of bounds. The floors are stated per FILE as well as per package precisely so they cannot be reached by bulk data-class construction. |
| T-26-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package and adds no Gradle dependency; `mockito-kotlin`, `junit-jupiter` and `montoya-api` are already on `testImplementation`. No package-legitimacy audit is required because there is no install task. |
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck` all green.
- `git diff --stat detekt-baseline.xml` and `git diff --stat src/main/kotlin` both print nothing.
- All five coverage floors computed from `build/reports/jacoco/test/jacocoTestReport.xml` and recorded as covered/total pairs.
</verification>

<success_criteria>
- The `mcp` tree line coverage is at or above 65.0% (from a measured 61.83%).
- The privacy and containment guards in `McpToolHelpers.kt` are asserted, with a recorded red probe on the report-path guard.
- No production source is modified; no detekt baseline entry is added.
</success_criteria>

<output>
Create `.planning/phases/26-coverage-static-analysis-debt-docs/26-02-SUMMARY.md` when done.
</output>
