---
phase: 20-mcp-access-control-correctness
plan: 07
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecision.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecisionTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/KtorMcpServerManagerSecurityTest.kt
autonomous: true
requirements: [SEC-04, SEC-05]
closes_gaps:
  - "20-VERIFICATION gap 1, decision-core half — evaluateLocal skips the DNS-rebinding branch when facts.host == null (McpAccessControlDecision.kt:150), a fail-open in a file whose KDoc claims the fail-closed shape"
  - "20-REVIEW WR-01 — parseAuthority maps an out-of-range port to \"no port\" (:174), and isLoopbackAuthority then skips the port comparison (:190), so isLoopbackAuthority(\"localhost:99999999999\", 9876) returns true"
tags: [security, access-control, sec-04, sec-05, fail-closed, authority-parsing]

must_haves:
  truths:
    - "Over HTTP/1.1, a local-mode request that carries no Host header is denied 403 host_mismatch instead of being allowed. Over HTTP/2 an ABSENT :authority is NOT closable — the h2 connection point coalesces it into the bound socket, making it byte-identical to a legitimate authority equal to that socket; plan 20-08 owns that as its residual 1"
    - "isLoopbackAuthority rejects a loopback hostname whose port is numeric but outside 1..65535 (65536 and 99999999999 both denied)"
    - "Every local-mode ALLOW that a real client can produce still holds: a matching loopback authority, with or without a browser Origin, a loopback Referer, or an Authorization header, is still allowed"
    - "Branch precedence is unchanged: a foreign Origin still reports origin_mismatch and a browser UA without Origin still reports browser_no_origin, even when no authority is present"
    - "External mode is untouched — Host/Origin/Referer/User-Agent are still ignored there"
    - "The D-11 Origin/Referer drift guard in KtorMcpServerManagerSecurityTest still compares an ALLOW against an ALLOW for loopback authorities — it does not degrade into comparing two denials"
  artifacts:
    - path: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecision.kt"
      provides: "Fail-closed authority handling in evaluateLocal and range-validated port parsing in parseAuthority"
      contains: "isLoopbackAuthority(facts.host"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecisionTest.kt"
      provides: "Unit pins for the absent-authority denial and the out-of-range port denial"
      contains: "99999999999"
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/mcp/KtorMcpServerManagerSecurityTest.kt"
      provides: "The D-11 Origin/Referer drift guard, repaired so it still compares a real ALLOW against a real ALLOW"
      contains: "evaluate_treatsOriginAndRefererIdentically"
  key_links:
    - from: "src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecision.kt"
      to: "isLoopbackAuthority"
      via: "evaluateLocal host branch, called unconditionally instead of behind a null guard"
      pattern: "!isLoopbackAuthority\\(facts\\.host"
    - from: "isLoopbackAuthority"
      to: "parseAuthority"
      via: "the single shared authority parser, which now rejects an out-of-range port outright"
      pattern: "MAX_TCP_PORT"
---

<objective>
Close the decision-core half of verification gap 1 and code-review warning WR-01, both of which live in
`McpAccessControlDecision.kt`: an absent authority is currently ALLOWED, and a numeric port that does
not fit an `Int` silently disables the port comparison.

Purpose: `McpAccessControlDecision.kt`'s own KDoc calls its shape "the fail-closed shape". Two inputs
prove otherwise, and both are the single shared predicate behind the `Host`, `Origin` and `Referer`
checks (D-11), so a defect here is a defect in three places at once.

Output: `evaluateLocal` denies `HOST_MISMATCH` when no authority is present (the maintainer's locked
answer to "what does a missing authority mean" — DENY, fail closed); `parseAuthority` distinguishes
"no port was present" from "a port was present but is not a usable port number"; unit pins for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/20-mcp-access-control-correctness/20-VERIFICATION.md
@.planning/phases/20-mcp-access-control-correctness/20-CONTEXT.md

@src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecision.kt
@src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecisionTest.kt
@src/test/kotlin/com/six2dez/burp/aiagent/mcp/KtorMcpServerManagerSecurityTest.kt
</context>

<constraints_verified_against_the_real_source>
Read these before writing a line. Each was checked against the merged tree while this plan was
written; where a line number appears it was verified, but prefer the named symbol as your anchor.

**The real API of `McpAccessControlDecision.kt`.** Do not invent or assume names. The file exports
`evaluate(facts, settings): GateDecision`, `isLoopbackAuthority(authority, expectedPort): Boolean`,
`isLoopbackUrlAuthority(url): Boolean`, `isBrowserUserAgent(userAgent): Boolean`,
`isAuthorizedBearer(authHeader, token): Boolean`, `constantTimeCompare(left, right): Boolean`,
private `evaluateExternal`, private `evaluateLocal`, private `parseAuthority(value): Pair<String, Int?>?`,
`enum class BlockReason`, `data class RequestFacts`, `sealed class GateDecision`, and
`const val HEALTH_PATH`. There is no `isValidHostAuthority`, no `isValidOriginValue`, no
`isValidRefererValue` — those names were dropped in plan `20-01`.

**`ReturnCount` budget is already spent.** `detekt.yml` overrides only `LongMethod`,
`LongParameterList`, `MaxLineLength` and `FunctionNaming`, so `ReturnCount` runs at its default max of
2, and `parseAuthority` already uses both of its returns (the `BARE_IPV6_LOOPBACKS` early exit and the
final expression). Everything else in the file is expression-bodied on purpose. **Do not add a third
`return` to `parseAuthority`.** Express the new outcome inside the existing `let`.

**`detekt-baseline.xml` must stay byte-identical.** QUAL-07 forbids growing it, and it is currently
byte-identical to the milestone start across all six executed plans. There is no baseline entry for any
file this plan touches. A change that needs a new entry is a broken change.

**The port regex already rejects non-numeric ports.** Both `BRACKETED_AUTHORITY_REGEX` and
`PLAIN_AUTHORITY_REGEX` capture the port as `(\d+)`, so `localhost:abc` fails `matchEntire` outright and
is already denied (pinned by `isLoopbackAuthority_failsClosedOnMalformedInput`). The new branch exists
only for values that ARE numeric but are not usable port numbers.

**SIX existing tests depend on the current fail-open.** Five construct local-mode `RequestFacts` with no
`host` and will FAIL. The sixth is worse: it stays GREEN while going vacuous. Task 2 enumerates all six; do
not discover them by running the suite and guessing.

**`MagicNumber` is the sibling budget to `ReturnCount`, and it is active on main source.** `detekt.yml`
does not override it, its `ignoreNumbers` default covers only `-1, 0, 1, 2`, and `detekt-baseline.xml` has
zero entries for `McpAccessControlDecision.kt`. An inline `65_535` therefore fails `detekt`, and the
baseline escape is forbidden. `MagicNumber`'s `ignoreConstantDeclaration` default is `true`, so the value
must live in a top-level `private const val`. A property does not count toward `TooManyFunctions`.
</constraints_verified_against_the_real_source>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseAuthority rejects a port that is not a usable port number</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecision.kt, src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecisionTest.kt</files>
  <behavior>
    - isLoopbackAuthority("localhost:99999999999", 9876) is false (today: true — the WR-01 fail-open)
    - isLoopbackAuthority("localhost:65536", 9876) is false (already true today; keep it pinned)
    - isLoopbackAuthority("localhost:65536", null) is false — a null expectedPort must not rescue an unusable port
    - isLoopbackAuthority("localhost:0", null) is false
    - isLoopbackAuthority("127.0.0.1", null) is still true — an authority with NO port still yields a null port and still passes
    - isLoopbackAuthority("localhost:9876", 9876) is still true
    - isLoopbackAuthority("[::1]:9876", 9876) and isLoopbackAuthority("::1", null) are still true
  </behavior>
  <action>
First add a top-level `private const val MAX_TCP_PORT = 65_535` beside `LOOPBACK_HOSTS`. Do NOT write the
literal inline: `MagicNumber` is active on main source, `detekt.yml` does not override it, and
`detekt-baseline.xml` has no entry for this file, so an inline `65_535` fails `detekt` and this task's own
gate forbids the baseline escape. `MagicNumber`'s `ignoreConstantDeclaration` default of `true` exempts the
constant declaration, and a `val` does not count toward `TooManyFunctions`.

Then, in `parseAuthority`, change only the final expression so the three outcomes are distinct: an empty
port capture group yields a null port (authority accepted, port comparison skipped, unchanged); a non-empty
port capture group that parses to an `Int` in `1..MAX_TCP_PORT` yields that port; a non-empty port capture
group that does not parse or falls outside that range makes the WHOLE authority malformed and yields null,
so `isLoopbackAuthority` denies it. Keep the function to its existing two `return` statements —
compute the outcome inside the `match?.let { ... }` body as a conditional expression rather than adding
an early exit. Do not touch `isLoopbackAuthority` itself: once the parser stops conflating "absent"
with "unusable", the existing `port == null || port == expectedPort` clause is correct.

Rewrite the KDoc paragraph on `parseAuthority` that currently describes `toIntOrNull` as mapping an
empty group to a null port "without an exception handler". It must now state all three outcomes and
name WR-01 as the reason: an authority whose port is numeric but outside the RFC 3986 range used to be
accepted with its port assertion silently disabled. Keep the existing note that this function holds the
file's whole `ReturnCount` budget.

In `McpAccessControlDecisionTest`, add the assertions from `<behavior>` to the existing
`isLoopbackAuthority` section. Put the out-of-range cases in a new `@Test` named for the defect (for
example `isLoopbackAuthority_rejectsAPortThatIsNumericButOutOfRange`) with a comment recording that
`"localhost:99999999999"` returned true before this change while `"localhost:65536"` returned false —
the inconsistency is what made the parser wrong. Leave
`isLoopbackAuthority_failsClosedOnMalformedInput` and every other existing assertion untouched; they
must all still pass.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpAccessControlDecisionTest' &amp;&amp; JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck &amp;&amp; test -z "$(git diff --stat detekt-baseline.xml)"</automated>
  </verify>
  <done>
`build/test-results/test/TEST-com.six2dez.burp.aiagent.mcp.McpAccessControlDecisionTest.xml` reports
zero failures and a test count strictly greater than the pre-task count; every assertion in
`<behavior>` is present in the file; `detekt` and `ktlintCheck` exit 0; `git diff --stat
detekt-baseline.xml` is empty; `parseAuthority` still contains exactly two `return` statements.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a local-mode request with no authority is denied, not allowed</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecision.kt, src/test/kotlin/com/six2dez/burp/aiagent/mcp/McpAccessControlDecisionTest.kt, src/test/kotlin/com/six2dez/burp/aiagent/mcp/KtorMcpServerManagerSecurityTest.kt</files>
  <behavior>
    - evaluate(RequestFacts(path = "/message"), localSettings()) is Deny(Forbidden, HOST_MISMATCH, facts) — today it is Allow
    - evaluate(RequestFacts(path = HEALTH_PATH), localSettings()) is likewise Deny(Forbidden, HOST_MISMATCH, facts) — D-03 gates every path
    - evaluate(RequestFacts(path = "/message", host = "127.0.0.1:9876"), localSettings()) is still Allow
    - A foreign Origin with no host still reports ORIGIN_MISMATCH, not HOST_MISMATCH (Origin branch precedes the host branch)
    - A browser User-Agent with no Origin and no host still reports BROWSER_NO_ORIGIN
    - External mode with host = "evil.example" is still Allow-or-401 purely on the bearer token
  </behavior>
  <action>
In `evaluateLocal`, replace the host branch's guard-and-skip condition with an unconditional
fail-closed check: call `isLoopbackAuthority` on `facts.host.orEmpty()` and deny
`GateDecision.Deny(HttpStatusCode.Forbidden, BlockReason.HOST_MISMATCH, facts)` when it returns false.
The empty string needs no extra predicate — `parseAuthority("")` already yields null because both
authority regexes require at least one host character, and that is pinned today by
`isLoopbackAuthority_failsClosedOnMalformedInput`.

Keep the branch exactly where it is in the `when`: after the `ORIGIN_MISMATCH` and `BROWSER_NO_ORIGIN`
branches and before the `REFERER_MISMATCH` branch. `evaluate_localOriginIsCheckedBeforeHost` pins
Origin-over-Host precedence and must stay green unchanged.

Replace the branch's comment with the reasoning, phrased so a future reader cannot mistake it for an
accident: the maintainer's locked answer is that a request carrying neither an HTTP/1 `Host` header nor
an HTTP/2 `:authority` is DENIED; real MCP clients always send one; and this is what the file's own
"fail-closed shape" KDoc already claimed. Do not write the old guard expression verbatim into the
comment — paraphrase it, following this phase's precedent of paraphrasing identifiers that greps count.

Now repair the SIX existing tests whose local-mode facts carry no `host`. Five of them FAIL after the
change; the sixth stays green while going vacuous, which is worse. These are the complete list — six, not
five — and each is named with the edit it needs:

  1. `evaluate_localLoopbackOriginWithBrowserUserAgentIsAllowed` — add `host = "127.0.0.1:$MCP_PORT"`
     to the facts. Keep its DELIBERATE-NARROWING comment verbatim; that comment guards a separate
     behaviour change and must not be diluted.
  2. `evaluate_localForeignRefererIsForbidden` — add `host = "127.0.0.1:$MCP_PORT"`. Without it the
     host branch now fires first and the test would measure HOST_MISMATCH instead of REFERER_MISMATCH,
     silently destroying the referer coverage.
  3. `evaluate_localLoopbackRefererIsAllowed` — add `host = "127.0.0.1:$MCP_PORT"`.
  4. `evaluate_localAllGatedHeadersAbsentIsAllowed` — this test's premise is inverted by the change.
     Replace it with two tests: one asserting that facts carrying ONLY a matching loopback authority
     are allowed, and one asserting the new denial for facts with no authority at all. Name the second
     for the behaviour it pins and give it a comment recording that this branch was a fail-open closed
     by gap-closure plan 20-07, so nobody "restores" the old permissiveness.
  5. `evaluate_localNeverInspectsAuthorization` — add `host = "127.0.0.1:$MCP_PORT"` so the test still
     measures what its name says rather than tripping the host branch.
  6. **In a different file** — `KtorMcpServerManagerSecurityTest.evaluate_treatsOriginAndRefererIdentically`
     (the D-11 Origin/Referer drift guard, at `KtorMcpServerManagerSecurityTest.kt:109-127`). It loops six
     authority values, evaluating each once as `origin` and once as `referer` against
     `McpTestServerSupport.localSettings(port = 9876)`, and asserts the two decisions agree on allow-vs-deny.
     Both `RequestFacts` are built with no `host`, at `:119-120`. After this change EVERY iteration denies —
     the `referer` variants via the new `HOST_MISMATCH` branch, which sits BEFORE the referer branch, so the
     referer predicate is never consulted at all. The assertion then compares `false` to `false` and stays
     GREEN while measuring nothing. That silently destroys the only above-predicate assertion guarding D-11,
     a locked decision. Add `host = "127.0.0.1:9876"` to both `RequestFacts` at `:119-120` so the loopback
     authorities produce a real ALLOW on both sides again. Do not touch the loop values, the KDoc, or the
     assertion message. This is the same reasoning already applied to `evaluate_localForeignRefererIsForbidden`
     above; it was missed once, so it is spelled out.

Do NOT edit `evaluate_localForeignOriginIsForbidden` or
`evaluate_localBrowserUserAgentWithoutOriginIsForbidden`: their branches precede the host branch, so
they stay green with a null host. Add a one-line comment to each recording that this is deliberate —
their greenness with an absent authority is the proof that branch precedence survived.

Add the acceptance-gate test required by the maintainer: a decision-core unit test pinning the chosen
`facts.host == null` behaviour on both `/message` and `HEALTH_PATH`.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.mcp.*' &amp;&amp; JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck &amp;&amp; test -z "$(git diff --stat detekt-baseline.xml)"</automated>
  </verify>
  <done>
Every `build/test-results/test/TEST-com.six2dez.burp.aiagent.mcp.*.xml` file reports zero `<failure>`
elements — in particular `McpAccessControlPipelineTest`, `McpAccessControlExternalPipelineTest`,
`McpServerIntegrationTest` and `McpSupervisorProbeTest`, all of which drive real servers with clients
that always send `Host`, proving no legitimate client regressed.
`evaluate_treatsOriginAndRefererIdentically` passes AND is non-vacuous: at least one of its six iterations
must now yield `GateDecision.Allow` on both sides, which the executor confirms by temporarily asserting it
or by reasoning recorded in the SUMMARY. The absent-authority denial and the
loopback-authority allow are both asserted. `detekt` and `ktlintCheck` exit 0. `git diff --stat
detekt-baseline.xml` is empty.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network client → MCP gate | Every field of `RequestFacts` is attacker-controlled; the authority is the input the DNS-rebinding limb rests on |
| gate → shared authority predicate | `Host`, `Origin` and `Referer` all funnel through `isLoopbackAuthority` (D-11), so one parser defect is three control defects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07-01 | Spoofing | `evaluateLocal` host branch | mitigate | Deny `HOST_MISMATCH` when the authority is absent or not a matching loopback authority; no null-guard skip |
| T-20-07-02 | Tampering | `parseAuthority` port capture | mitigate | A numeric port outside `1..65535` makes the whole authority malformed rather than disabling the port comparison |
| T-20-07-03 | Elevation of privilege | browser-driven DNS rebinding against the local MCP port | mitigate | The rebinding limb now runs on every local-mode request instead of only those that happen to carry a parseable `Host` |
| T-20-07-04 | Denial of service | a legitimate MCP client denied by the new fail-closed branch | accept | Any client that speaks HTTP/1.1 or HTTP/2 correctly sends `Host` or `:authority`; the four real-server suites in the DONE criteria are the regression evidence |
| T-20-07-SC | Tampering | dependency supply chain | accept | No dependency is added or changed; `build.gradle.kts` is not touched by this plan and there is no npm/pip/cargo install step |
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck` exits 0.
- `git diff --stat detekt-baseline.xml` is empty.
- `parseAuthority` still contains exactly two `return` statements. Do not measure this with a whole-file
  `grep -c 'return'`: that count is 3 today (two statements plus the word "returns" in the
  `isLoopbackAuthority` KDoc) and the mandated KDoc rewrite will move it. Read the function.
- `MAX_TCP_PORT` is a top-level `private const val` and `65_535` appears nowhere inline in the file.
- No file outside this plan's `files_modified` is modified: `git status --porcelain` lists only those three.
- Out-of-plan dependency, deliberately NOT gated here: the orchestrator owns the post-merge run of the full
  `test detekt ktlintCheck` plus `test -PstoreBuild=true` on the merged tree. Each plan's gates are
  isolation-only by design.
</verification>

<success_criteria>
- A local-mode request with no authority is denied 403 `host_mismatch`, asserted at decision-core level.
- `isLoopbackAuthority("localhost:99999999999", 9876)` is false, asserted.
- All pre-existing local-mode ALLOW paths a real client can produce remain allowed, asserted through
  the four real-server suites plus the repaired unit tests.
- `detekt-baseline.xml` unchanged.
</success_criteria>

<output>
Create `.planning/phases/20-mcp-access-control-correctness/20-07-SUMMARY.md` when done. Record the exact
before/after test counts for `McpAccessControlDecisionTest`, and list the five repaired tests with the
edit applied to each.
</output>
