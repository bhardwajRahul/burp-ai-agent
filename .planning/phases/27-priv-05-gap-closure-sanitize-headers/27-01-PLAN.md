---
phase: 27-priv-05-gap-closure-sanitize-headers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt
  - src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerFilters.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeaderAdmissionTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderRuleOwnershipTest.kt
autonomous: true
requirements: [PRIV-05]

estimate:
  tokens: 102000
  raw_tokens: 102000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "EDGE `adjacency` (lifted verbatim from coverage-resolved.json): Header-name matching is name-CONTAINS-`cookie`, matching the prompt path's COOKIE_NAME_PART widening, not exact equality. Both the exactly-equal names (`cookie`, `set-cookie`) and the touching variants (`x-cookie`, `cookie2`, `set-cookie2`, `x-original-cookie`, `x-forwarded-cookie`) are redacted. Widening must not merge distinct headers: each header entry is redacted in place and keeps its own name."
    - "EDGE `empty` (lifted verbatim from coverage-resolved.json): sanitizeHeaders over an empty or absent header list returns an empty result and does not throw. A cookie-named header with an empty value is still redacted to the placeholder — an empty-but-present value is never passed through as-is."
    - "EDGE `encoding` (lifted verbatim from coverage-resolved.json): Name matching lowercases with Locale.ROOT and compares ASCII; no Unicode normalization. Acceptance: `X-COOKIE` is redacted with the JVM default locale set to `tr-TR` (the dotless-i hazard — `\"COOKIE\".lowercase()` under a Turkish default locale yields `cookıe` and would silently stop matching)."
    - "There is exactly ONE cookie-header-name rule across THE TWO REDACTION PATHS AND THE PASSIVE-SCAN ADMITTER: `Redaction.isCookieHeaderName`, owned in `Redaction.kt` alongside the two regexes it shares `COOKIE_NAME_TOKEN` with. `McpToolHelpers.sanitizeHeaders` and `PassiveAiScannerFilters.sanitizeHeadersForPrompt` both call it; neither writes its own cookie-name test. This claim is scoped to those three sites BY MEASUREMENT, not to the whole tree — other cookie-header-name matchers exist in `src/main/kotlin` and are individually classified as non-redacting (see the next truth)."
    - "Every OTHER cookie-header-name matcher surviving in `src/main/kotlin` — found by a sweep covering exact-name equality, `ignoreCase` equality, line-prefix tests and substring tests, not only the one `contains` spelling — is classified in the SUMMARY as redactor / admitter / extractor / local-analysis, with the source-verified reason it need not route through the predicate. The classification is derived by reading each site in this task, not copied from any review artifact."
    - "The classification is enforced by a repository-state test, `CookieHeaderRuleOwnershipTest`, not only by an execution-time grep: a FOURTH consumer added in a new file after this phase, WRITTEN IN ANY OF THE FIVE MEASURED SPELLING CLASSES that `MATCHER_SPELLINGS` enumerates (exact-name equality, `ignoreCase` equality, line-prefix test, Montoya `headerValue` lookup, substring test), turns that test red and forces its author to classify it or route it through the predicate. That bound is real and is stated in the test's own file header: a consumer spelled OUTSIDE those five classes — a `startsWith` on an already-lowered name, an `in` operator, a `when` branch — escapes the scan silently and the test stays GREEN. `CookieHeaderRuleOwnershipTest` is a TRIPWIRE over measured spellings, not a proof of exhaustive coverage, and stating it as unbounded would be this phase's own defect — a claim outrunning the sweep supporting it — one iteration smaller. The test is proven non-vacuous by asserting both its scanned-file floor and that its spelling patterns match a known positive fixture."
    - "The five measured variant names (`Cookie2`, `X-Cookie`, `Set-Cookie2`, `X-Original-Cookie`, `X-Forwarded-Cookie`) and the two canonical names (`Cookie`, `Set-Cookie`) all yield `[STRIPPED]` from `sanitizeHeaders` under STRICT and under BALANCED."
    - "The sentinel value of an `X-Cookie` header does NOT survive the full tool-result flow — `sanitizeHeaders` -> `toolJson.encodeToString(ParsedRequest)` -> `McpToolContext.redactIfNeeded` — asserted on the final string, so the outcome is locked independently of which layer strips it."
    - "OFF mode is unchanged: every cookie name variant passes through `sanitizeHeaders` with its original value under `PrivacyMode.OFF`."
    - "Header NAMES survive redaction with their original casing — `X-Cookie` is reported as `X-Cookie`, never renamed to `Cookie`, and the negative control `X-Request-Id` is returned byte-identical in every mode."
    - "`sanitizeHeaders`' token-header and host comparisons use `lowercase(Locale.ROOT)` too — the locale fix is applied to the whole function, not only to the cookie branch, because all three comparisons read the same lowered name."
    - "The default-locale `lowercase()` hazard is CLOSED ONLY at the two functions D-27-04 names (`McpToolHelpers.sanitizeHeaders`, `PassiveAiScannerFilters.sanitizeHeadersForPrompt`) plus inside `isCookieHeaderName`. Every other security-relevant no-argument `lowercase()` in `src/main/kotlin` is deliberately OUT of this phase's scope and is recorded in the SUMMARY as a measured backlog observation, with its observed count, so no later closure note can read as if the locale hazard is closed repo-wide."
    - "Both mutation directions are proven: reverting the predicate to an exact-name equality test turns the variant assertions red, and removing the `Locale.ROOT` argument turns the `tr-TR` assertion red. Both are recorded with their observed output."
    - "`Redaction.apply`'s existing prompt-path behaviour is byte-unchanged: `RedactionTest.cookieHeaderNameVariantsAreStripped` and `RedactionTest.strictModeStripsCookiesTokensAndHosts` stay green WITHOUT being edited."
  artifacts:
    - "src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt — new public `isCookieHeaderName(name: String): Boolean` and new private `COOKIE_NAME_TOKEN`, from which both cookie regexes are also built"
    - "src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt — `sanitizeHeaders` calls the shared predicate and lowercases with `Locale.ROOT`"
    - "src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerFilters.kt — `sanitizeHeadersForPrompt` calls the shared predicate and lowercases with `Locale.ROOT`"
    - "src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt — the tool-result path variant matrix, the end-to-end flow assertion and the locale assertion, added to the existing `SanitizeHeaders` nested class"
    - "src/test/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeaderAdmissionTest.kt — NEW file; asserts the passive-scanner admitter still admits the same header set after being routed through the shared predicate"
    - "src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderRuleOwnershipTest.kt — NEW file; the repository-state test that carries the widened sweep and the per-file classification allowlist, so the ownership invariant outlives this phase instead of expiring with an execution-time grep"
  key_links:
    - "`Redaction.COOKIE_NAME_TOKEN` -> `cookieHeaderRegex` + `setCookieHeaderRegex` + `isCookieHeaderName`: one literal feeds all three, so a rename of the token is a compile-wide change instead of three hand-kept copies"
    - "`Redaction.isCookieHeaderName` -> `McpToolHelpers.sanitizeHeaders`: the single point where the MCP tool-RESULT path decides a header is cookie-bearing. This link is the whole gap — it did not exist, and the two paths drifted"
    - "`CookieHeaderRuleOwnershipTest` -> the two redaction paths + the classified non-redacting sites: the mechanism that makes a FOURTH hand-written cookie matcher — spelled in any of the five measured `MATCHER_SPELLINGS` classes — a red test rather than a silent divergence, and the only artifact in this phase whose guarantee survives past its own execution. Bounded by that spelling set, not exhaustive"
    - "`sanitizeHeaders` -> `toolJson.encodeToString` -> `McpToolContext.redactIfNeeded`: the flow the milestone audit recorded as broken. `redactIfNeeded` CANNOT recover a missed header (line-anchored regexes vs single-line JSON, `cookie` absent from `SENSITIVE_WORDS`), so `sanitizeHeaders` is the last line of defence, not the first of two"
  prohibitions:
    - statement: "Parity must NEVER be reached by narrowing the prompt path. If a test disagreement appears between `Redaction.apply` and `sanitizeHeaders`, the tool-result path widens to meet the prompt path — the prompt path does not narrow to meet it. A green parity bought by removing redaction is the opposite of this phase."
      severity: high
    - statement: "Header NAMES must not be lost, merged or rewritten. Only the VALUE is replaced. `X-Cookie` must still be reported as `X-Cookie`; a fix that renames it to `Cookie` silently edits the analyst's view of the traffic (this is trap T-21-WA2, already paid for once on the prompt path)."
      severity: high
    - statement: "`PrivacyMode.OFF` must stay OFF. No cookie stripping, no token redaction, no host anonymisation in OFF — that mode is the user's explicit, pre-flight choice to see raw traffic, and overriding it silently is a betrayal of the privacy UI, not extra safety."
      severity: high
    - statement: "No existing test may be deleted, renamed, `@Disabled` or narrowed to make this plan green. `RedactionTest.cookieHeaderNameVariantsAreStripped` in particular is the prompt path's own guard and must pass UNCHANGED; if it needs editing, the production change is wrong."
      severity: high
    - statement: "No partial mirror. All five measured variant names plus both canonical names are covered, or the plan is not done. A fix that handles `X-Cookie` and leaves `Set-Cookie2` reproduces the exact class of miss this phase exists to repair — there is no acceptable 'v1' here."
      severity: high
    - statement: "Do NOT claim, in any artifact this plan produces, that the cookie-header-name rule is singular across the WHOLE codebase. It is singular across the two redaction paths and the passive-scan admitter; other matchers survive and are classified as non-redacting. Certifying a codebase-wide claim on the strength of a sweep is the same defect this phase exists to repair — a record wider than the control it describes — relocated one layer up."
      severity: high
    - statement: "English only in all code, comments and KDoc (AGENTS.md, non-negotiable)."
      severity: medium
    - statement: "No wall-clock threshold and no fixed `Thread.sleep` in any assertion — this repo has a recorded wall-clock flake class (`RedactionTest` under CPU load via the SafeRegex 50 ms deadline)."
      severity: medium
---

<objective>
Close the PRIV-05 gap the v0.10.0 milestone audit found: make the MCP tool-RESULT redaction path
(`McpToolHelpers.sanitizeHeaders`) recognise a cookie header by the same rule the prompt path
(`Redaction`) already uses — name-CONTAINS-`cookie`, not exact equality — and make that agreement
structural rather than a coincidence two humans have to maintain.

Purpose: PRIV-05 reads "cookie values do not reach an AI backend in STRICT or BALANCED mode **by any
path**." Phase 21 made that true on the prompt path. `sanitizeHeaders` is the second, independent
path, and it still tests exact names, so `X-Cookie`, `Cookie2`, `Set-Cookie2`, `X-Original-Cookie`
and `X-Forwarded-Cookie` reach the model VERBATIM through the `request_parse` / `response_parse` MCP
tools. Downstream cannot recover them.

Output: one shared predicate in `Redaction`, three call sites consuming it, a locale-safe compare,
and a test matrix that fails in both mutation directions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
</context>

## The defect, measured (do not re-litigate — fix it)

Confirmed against the tree at `1.0.0` before this plan was written.

| | Prompt path (Phase 21) | MCP tool-result path (Phase 26) |
|---|---|---|
| Location | `Redaction.kt:100-106` | `McpToolHelpers.kt:321` |
| Matcher | name-**contains**-`cookie`, via `COOKIE_NAME_PART = [A-Za-z0-9-]*` | exact equality against the two canonical spellings |
| `Cookie:` | stripped | stripped |
| `X-Cookie:` | stripped | **passes through** |
| `Set-Cookie2:` | stripped | **passes through** |

`McpToolContext.redactIfNeeded` (`McpToolContext.kt:59`) cannot recover a missed header, for two
independent reasons that were both re-verified in source:

1. `sanitizeHeaders` output is serialised into **single-line JSON** by `toolJson`
   (`McpToolHelpers.kt:24`), while `cookieHeaderRegex` and `setCookieHeaderRegex` are line-anchored
   `(?im)^…$`.
2. `cookie` is absent from `SENSITIVE_WORDS` (`Redaction.kt:476-477` — `access_token|api_key|apikey|
   auth|token|secret|password|pwd|session|sid`), so `jsonSecretKeyRegex` never fires on a cookie key.

So `sanitizeHeaders` is the **last** line of defence on this path, not the first of two.

## The five design decisions this plan makes

**D-27-01 — one shared predicate, promoted (assumption-delta: `promote`, not `add-alongside`).**
The cookie-name rule becomes a single public function, `Redaction.isCookieHeaderName`, and every
consumer calls it. Both cookie regexes and the predicate are built from ONE new private literal,
`COOKIE_NAME_TOKEN`. Rejected alternative: give `sanitizeHeaders` its own matcher. That reproduces
the exact structure that failed — two independently-maintained copies of one security rule — and the
audit's own remedy note asks for the sibling relationship to be made explicit. Precedent in this
codebase: `Redaction.COOKIE_SECTION_HEADER` is owned in the redactor and imported by the emitter so a
rename is a compile error, and Phase 21 W-01 coupled `COOKIES_MAX_COUNT` to
`MAX_COOKIE_SECTION_LINES` for the same reason. Rated `costly`: undoing it means unwinding a
symbol plus three call sites plus the parity test in plan 27-02.

**D-27-02 — the over-match is ACCEPTED, deliberately, for parity.** `Cookie-Consent` and
`X-Cookie-Policy` will have their values stripped. This is the same cost the prompt path already
accepts (T-21-WA3; `.planning/codebase/CONCERNS.md` W-A: *"any `*cookie*` header is cookie-bearing by
convention, and only the VALUE is removed"*). A tighter rule was rejected because it would require
BOTH paths to change together or the divergence reopens immediately — which is precisely the failure
being repaired. The acceptance is made VISIBLE as a named test rather than left as an accident.
Rated `reversible`.

**D-27-03 — the two second-order holes in `redactIfNeeded` are NOT closed here; they are recorded as
accepted residuals.** Un-anchoring the cookie regexes so they fire mid-line would change the PROMPT
path's behaviour across every body, and adding `cookie` to `SENSITIVE_WORDS` would redact every JSON
key, form parameter and query parameter containing `cookie` as a token — against a measured Phase 21
decision that REMOVED `key` and `code` from broad matching after 27 false positives over a 32-name
corpus. Both are large-blast-radius changes to a shipped redactor, undertaken to add a backstop
behind a control this plan is making correct. Instead, the deferral is made safe by an
**outcome-level** assertion: Task 1's end-to-end test asserts the sentinel is absent from the FINAL
`redactIfNeeded` output, so if anyone ever narrows `sanitizeHeaders` again, that test goes red
without caring which layer was supposed to catch it. Residuals: **AR-27-01** (line-anchored regexes
vs single-line JSON) and **AR-27-02** (`cookie` absent from `SENSITIVE_WORDS`). Rated `reversible`.

**D-27-04 — the locale fix lands for the whole function, in the same commit.** `sanitizeHeaders`
computes one lowered name and feeds it to the cookie test, the `tokenHeaders` set lookup and the host
comparison. There is no way to fix the cookie branch's locale hazard without fixing all three, and
splitting the change would be worse than useless. Under a `tr-TR` default locale, `"COOKIE"` lowered
without a locale argument yields `cookıe` (dotless i) and the control silently stops working.
`java.util.Locale.ROOT` is the established convention in this codebase (`CliBackend.kt:817/842/964`,
`AgentSupervisor.kt:1143/1199`, `Entropy.kt:154`). The same one-line hazard exists at
`PassiveAiScannerFilters.kt:169` and is fixed in Task 3. Rated `reversible`.

**D-27-05 — the record reconciliation is real work with its own plan (27-03), not a footnote.** See
plan 27-03. It is out of scope here; this plan only produces the source evidence 27-03 must cite.

## Edge lift map

The spec-less probe resolved four edges for PRIV-05, all `explicit`. Three are carried as
`must_haves.truths` in this plan (`adjacency`, `empty`, `encoding`). The fourth (`ordering`) is
carried in **plan 27-02**, where it collides with a measured constraint of the current return type
and is resolved by a checkpoint. No edge is dropped.

## The surviving cookie-header-name matchers, classified

The original invariant gate for this plan matched only the one substring spelling. That spelling is
not the only way to name a cookie header in Kotlin, so the gate could not support the claim it was
being asked to support. A sweep covering exact-name equality, `ignoreCase` equality, line-prefix
tests, Montoya `headerValue` lookups and substring tests was run against the tree at `1.0.0` and
returned **nine sites in SIX files** — the two the original gate could see, plus seven it could
not. Filtered to the redaction paths the same sweep returns exactly the two this phase fixes, so
it is wide enough to be informative and narrow enough to be a gate.

The six files, re-measured while this revision was written:
`mcp/tools/McpToolHelpers.kt` (1), `scanner/PassiveAiScannerFilters.kt` (1),
`prompts/bountyprompt/BountyPromptTagResolver.kt` (2), `scanner/PassiveAiScannerHeuristics.kt` (2),
`scanner/ActiveAiScanner.kt` (2), `scanner/PassiveAiScannerAnalysis.kt` (1) = nine sites.

**Each row was traced to its consumer in source while this plan was being revised.** Task 3 must
re-verify every row against the shipped file before recording it — this table is a starting point to
CONFIRM, never a fact to copy. If a re-read disagrees with a row, say so in the SUMMARY and act on
the re-read, not on this table.

**Read the "Kind" cell before seeding anything from this table.** The last three rows are marked
**READER NOTE, NOT A SWEEP HIT**: they return ZERO `MATCHER_SPELLINGS` hits and exist only so a
future hand-grep for `cookie` does not re-flag them. They must NOT be entered into
`CLASSIFIED_NON_REDACTING` — that map holds only files the sweep actually hits, and a key the sweep
never hits is a dead entry that fails `everyCookieHeaderNameMatcherInMainIsClassified` in its
reverse direction. Exactly SIX files are sweep hits; the classification map is seeded from the
non-redacting subset of those six, which is four files.

| Site | Kind | Routes through the predicate? | Source-verified reason |
|---|---|---|---|
| `mcp/tools/McpToolHelpers.kt:321` | **REDACTOR** | YES — Task 1 | THE gap. Decides whether a header VALUE is replaced on the MCP tool-result path. Narrow here means leak. |
| `scanner/PassiveAiScannerFilters.kt:172` | **ADMITTER** | YES — Task 3 | Decides which headers enter the passive-scan prompt. Narrow here does not leak, but parity is what makes the rule singular across both paths. |
| `scanner/PassiveAiScannerAnalysis.kt:267` | EXTRACTOR | no | Selects which request cookie-header VALUES feed `cookieSectionLines`. Fail-safe by DIRECTION: narrowing puts FEWER cookie values into the prompt, widening puts more in for zero PRIV-05 benefit. Its output reaches the model only via `buildScanMetadataText` -> `redactScanMetadata`, which calls `Redaction.apply` UNCONDITIONALLY (`PassiveAiScannerPrompts.kt:45-49`), and the `COOKIE_SECTION_HEADER` span rule (`Redaction.kt:302-330`) redacts the values inside that span. |
| `scanner/PassiveAiScannerHeuristics.kt:117` and `:102` | LOCAL-ANALYSIS | no | `:117` reduces `Set-Cookie` values to the `sameSiteSecure` boolean; `:102` reduces the `Cookie` value to a `containsMatchIn` boolean. Both feed a `LocalFinding`'s severity/suppression decision. No header value crosses the process boundary. |
| `prompts/bountyprompt/BountyPromptTagResolver.kt:144` and `:150` | EXTRACTOR over ALREADY-REDACTED text | no | `extractCookies` is called at `:106` with `requestRedacted` / `responseRedacted`, which are the outputs of `Redaction.apply` at `:79-80`. It filters lines that have ALREADY been through the wide prompt-path cookie rules. Narrowing puts fewer already-redacted lines into the tag. |
| `scanner/ActiveAiScanner.kt:936` | LOCAL-ANALYSIS | no | `hasAuthContext` reduces the value to a boolean; `stripAuthHeaders` immediately below REMOVES the header. |
| `scanner/ActiveAiScanner.kt:1411` | REQUEST MUTATOR | no | Injects an attack payload into the request sent to the TARGET, not to an AI backend. Not a redaction path at all. |
| `scanner/PassiveAiScanner.kt:189` and the request allowlist | ALLOWLIST MEMBERSHIP — **READER NOTE, NOT A SWEEP HIT** | n/a | **Re-measured: returns ZERO `MATCHER_SPELLINGS` hits — the entry is a bare `"set-cookie",` member of a `setOf(...)` literal, which no spelling class matches. MUST NOT enter `CLASSIFIED_NON_REDACTING`.** Listed only for the hand-grepping reader: set members are reached only after `Filters.kt:172` has already returned true for any `*cookie*` name, so they cannot narrow what the admitter admits. |
| `redact/Redaction.kt` | **OWNER** — **READER NOTE, NOT A SWEEP HIT** | n/a — it IS the rule | Declares `COOKIE_NAME_TOKEN`, both cookie regexes and `isCookieHeaderName`. **Re-measured: returns ZERO `MATCHER_SPELLINGS` hits both before and after Task 1, because the regexes are composed from `COOKIE_NAME_TOKEN` rather than from an inline literal in a matched spelling. It is subtracted as the owner regardless, so no dead entry can arise — but do not seed it either.** Exempt from the sweep by definition; a gate that flagged the owner would be flagging the fix. |
| `scanner/InjectionPointExtractor.kt:29` | NOT A HEADER-NAME MATCHER — **READER NOTE, NOT A SWEEP HIT** | n/a | **Re-measured: returns ZERO `MATCHER_SPELLINGS` hits — `it.type().name == "COOKIE"` is an uppercase parameter-TYPE comparison and no spelling class matches it. MUST NOT enter `CLASSIFIED_NON_REDACTING`.** Matches a Montoya parameter TYPE, not a header name. Listed so a future hand-grep does not re-flag it. |

**Consequence, and it is the point of this table:** the claim this plan may make is *"there is exactly
one cookie-header-name rule across the two redaction paths and the passive-scan admitter"* — NOT
*"across the whole codebase"*. The wider claim is false, and certifying it into a security register
would be the same class of defect the v0.10.0 audit found, one layer up.

## Artifacts this phase produces

Symbols **created by this plan** — the source-grounding pass must treat these as new, not as drift:

| Symbol | Kind | File |
|---|---|---|
| `Redaction.isCookieHeaderName` | new public function | `src/main/kotlin/.../redact/Redaction.kt` |
| `COOKIE_NAME_TOKEN` | new private const | `src/main/kotlin/.../redact/Redaction.kt` |
| `cookieVariantsAreStrippedEndToEndThroughRedactIfNeeded` | new test | `McpToolHelpersTest.kt` |
| `cookieHeaderNameVariantsAreStrippedOnTheToolResultPath` | new test | `McpToolHelpersTest.kt` |
| `headersMerelyContainingCookieAreStrippedByDesign` | new test | `McpToolHelpersTest.kt` |
| `offModePassesEveryCookieNameVariantThrough` | new test | `McpToolHelpersTest.kt` |
| `cookieNameMatchingSurvivesATurkishDefaultLocale` | new test | `McpToolHelpersTest.kt` |
| `emptyValuedCookieHeaderIsStillRedacted` | new test | `McpToolHelpersTest.kt` |
| `PassiveAiScannerHeaderAdmissionTest` | new test class + file | `src/test/kotlin/.../scanner/PassiveAiScannerHeaderAdmissionTest.kt` |
| `admitsEveryCookieNameVariantAfterRoutingThroughTheSharedPredicate` | new test | `PassiveAiScannerHeaderAdmissionTest.kt` |
| `admissionSurvivesATurkishDefaultLocale` | new test | `PassiveAiScannerHeaderAdmissionTest.kt` |
| `CookieHeaderRuleOwnershipTest` | new test class + file | `src/test/kotlin/.../redact/CookieHeaderRuleOwnershipTest.kt` |
| `noHandWrittenCookieMatcherSurvivesInTheRedactionPaths` | new test | `CookieHeaderRuleOwnershipTest.kt` |
| `everyCookieHeaderNameMatcherInMainIsClassified` | new test | `CookieHeaderRuleOwnershipTest.kt` |
| `theOwnershipScanIsNonVacuous` | new test | `CookieHeaderRuleOwnershipTest.kt` |
| `AR-27-01`, `AR-27-02` | accepted-residual IDs | `<threat_model>` below; carried into `27-SECURITY.md` |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One end-to-end path — X-Cookie stripped, proven through sanitizeHeaders, toolJson and redactIfNeeded</name>

  <precondition>`Redaction.COOKIE_NAME_PART` exists at `Redaction.kt:84` with the value `[A-Za-z0-9-]*` (a Phase 21 artifact). If it is absent or has a different value, STOP and report — the whole plan mirrors it.</precondition>

  <reversibility rating="costly">D-27-01 promotes the cookie-name rule to a single shared symbol consumed across three packages; undoing it means unwinding the symbol, three call sites and plan 27-02's parity test together.</reversibility>

  <read_first>
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt` — read the whole `sanitizeHeaders` function at lines 310-332 and the `toolJson` declaration at line 24. Do NOT work from the plan's description of the current code; read it.
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` lines 80-120 — `COOKIE_NAME_PART`, `cookieHeaderRegex`, `setCookieHeaderRegex` and the long W-A comment above them. This is the REFERENCE IMPLEMENTATION whose width the new predicate must match. Read the comment, it names two traps.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/McpToolContext.kt` lines 40-80 — `redactIfNeeded`, and confirm for yourself that `AuditLogger.emitGlobal` is null-safe (`globalEmitter?.invoke`) and `supervisor` defaults to null, so the function is safe to call headlessly.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolModels.kt` lines 138-160 — `ParsedRequest` and `ParsedResponse`; note `headers` is `Map<String, String>`.
    - `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt` — the existing `SanitizeHeaders` nested class (lines 44-190) and the `stubHeader` / `contextWith` / `newContext` helpers at lines 754-805. Extend this class; do not create a second test class for the same production file.
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt` lines 405-478 — `cookieHeaderNameVariantsAreStripped` and the comment block above it, which explains exactly why its fixture is reachable only by the cookie rules. Mirror that discipline when choosing sentinels.
  </read_first>

  <files>src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt, src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt, src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt</files>

  <behavior>
    Write the failing test FIRST, run it, record that it is red, then implement.

    `McpToolHelpersTest.SanitizeHeaders.cookieVariantsAreStrippedEndToEndThroughRedactIfNeeded`:
    - Build a header list containing `X-Cookie` with the value `sentinelxrayninezulu`, plus `Host` set to `api.example.com` and `X-Request-Id` set to `benignidcontrolvalue`.
    - Run `sanitizeHeaders(headers, contextWith(PrivacyMode.STRICT, salt))`.
    - Wrap the result in a `ParsedRequest` (method `GET`, path `/`, url `https://api.example.com/`, empty parameters, null body, bodyLength 0) and serialise it with `toolJson.encodeToString`.
    - Pass that string through the SAME `McpToolContext` instance's `redactIfNeeded`.
    - Assert 1: the final string does NOT contain `sentinelxrayninezulu`. This is the requirement, stated as an outcome.
    - Assert 2: the final string DOES contain the key `X-Cookie` — the header name survives; only its value was replaced.
    - Assert 3: the final string still contains `benignidcontrolvalue` — the negative control proves this is stripping, not blanket header loss.
    - Assert 4 (the reason the downstream cannot save us, asserted rather than asserted-in-prose). ONE assertion, run entirely under **STRICT** — the mode is named once here and nowhere is an OFF policy involved, because under OFF nothing is redacted and the assertion would be trivially true and would pin nothing:
      Build a SECOND `ParsedRequest` from the RAW, UNSANITISED header list — the same `X-Cookie: sentinelxrayninezulu`, never passed through `sanitizeHeaders` — serialise it with `toolJson.encodeToString`, and pass that string through the SAME STRICT `McpToolContext.redactIfNeeded` used for asserts 1-3. Assert the sentinel is STILL PRESENT in the output.
      That is the pin: `redactIfNeeded` under the strongest privacy mode the product offers cannot recover a header `sanitizeHeaders` missed, so `sanitizeHeaders` is the last line of defence and not the first of two. This is residual AR-27-01/AR-27-02 turned into a known, measured state rather than an assumption.
      Guard against the assertion pinning the wrong thing: in the same test, assert that this raw-path output DOES differ from its input in at least one respect attributable to STRICT — the `Host` value is anonymised — so a `redactIfNeeded` that silently no-opped (wrong policy, wrong context) cannot make assert 4 pass for the wrong reason.
  </behavior>

  <action>
    Step 1 — `Redaction.kt`. Introduce a private const carrying the single lowercase literal for the
    cookie token, named `COOKIE_NAME_TOKEN`. Rebuild `cookieHeaderRegex` (line 100) and
    `setCookieHeaderRegex` (line 105) so that every occurrence of the cookie literal and of the
    set-cookie literal inside those two expressions is composed from `COOKIE_NAME_TOKEN` instead of
    being typed inline — including the one inside `cookieHeaderRegex`'s negative lookahead. The
    compiled pattern strings must be byte-identical to what they are today; this is a refactor of
    where the literal lives, not a change to what matches.

    Step 2 — `Redaction.kt`. Add a public function `isCookieHeaderName(name: String): Boolean` on the
    `Redaction` object. It lowercases its argument with `java.util.Locale.ROOT` and returns whether
    the result contains `COOKIE_NAME_TOKEN`. No `Regex`, no compilation, no allocation beyond the
    lowercased string — this runs once per header on the MCP tool path and must not add a
    backtracking surface (same reasoning as T-26-01-03's per-character allowlist scan).

    Give it KDoc that states four things: (a) it is the single cookie-header-name rule across the
    two redaction paths and the passive-scan admitter — `McpToolHelpers.sanitizeHeaders`,
    `PassiveAiScannerFilters.sanitizeHeadersForPrompt`, and the two regexes above, which are built
    from the same `COOKIE_NAME_TOKEN` — and that this scope is measured, not rhetorical: other
    cookie-header-name matchers survive elsewhere in `src/main/kotlin` and are classified as
    non-redacting by `CookieHeaderRuleOwnershipTest`, so the sentence must stay scoped to those
    sites and must not be widened into a tree-wide singularity claim by whoever next edits it.
    (This is the plan's own high-severity prohibition applied to the artifact a maintainer reads
    FIRST — the source comment — not just to the planning records.) (b) it is
    deliberately WIDER than the two regexes — those constrain the surrounding characters to
    `COOKIE_NAME_PART` whereas this constrains nothing, and wider on the redacting side is fail-safe;
    (c) the predicate is what `McpToolHelpers.sanitizeHeaders` and
    `PassiveAiScannerFilters.sanitizeHeadersForPrompt` call, so a future widening cannot be applied
    to one path and forgotten in the other — the failure the v0.10.0 milestone audit found; (d) the
    `Locale.ROOT` argument is load-bearing, naming the Turkish dotless-i case. English only.

    Step 3 — `McpToolHelpers.kt`. In `sanitizeHeaders`, replace the exact-equality cookie test on
    line 321 with a call to `Redaction.isCookieHeaderName(name)`, passing the ORIGINAL header name
    (the predicate does its own locale-safe lowering). Keep the `policy.stripCookies` conjunct and
    the `[STRIPPED]` placeholder exactly as they are. In the same edit, change the lowered-name
    computation on line 319 to lowercase with `java.util.Locale.ROOT`, which fixes the same hazard
    for the `tokenHeaders` lookup on line 324 and the host comparison on line 327 — per D-27-04 these
    cannot be separated, they all read the one lowered value. Add the `java.util.Locale` import.

    Do NOT change the return type, the `LinkedHashMap`, the insertion of the ORIGINAL-cased name as
    the key, or the order of the three policy branches. Ordering and duplicate behaviour belong to
    plan 27-02.

    Step 4 — the test, written before steps 1-3 and re-run after. Add
    `cookieVariantsAreStrippedEndToEndThroughRedactIfNeeded` to the existing `SanitizeHeaders` nested
    class in `McpToolHelpersTest.kt`, using the existing `stubHeader` and `contextWith` helpers.
    Choose sentinels with no `=`, no quotes, no `Bearer`/`Basic`/`eyJ` prefix and no dotted segments,
    so no rule other than the cookie rule can claim them — the discipline `RedactionTest`'s comment
    block spells out. Add `Redaction.setCustomPatterns(emptyList())` in a `@BeforeEach` if the class
    does not already have one: `Redaction` is a singleton and another class in the same JVM can leave
    patterns behind (the precedent is `PassiveAiScannerPromptRedactionTest`).

    Step 5 — the red probe, both directions, recorded with observed output in the SUMMARY:
    - Probe A: revert the line-321 test to an exact-equality comparison against the two canonical
      names, run the new test, record the failure text, then restore with
      `git checkout HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt`.
    - Probe B: remove the `Locale.ROOT` argument from `isCookieHeaderName`, run
      `RedactionTest` plus the new test under a `tr-TR` default locale (Task 2 adds that assertion —
      if Task 2 has not run yet, defer Probe B to Task 2 and say so). Restore with
      `git checkout HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt`.
    NEVER `git stash` — `refs/stash` is shared across linked worktrees in this repo (Phase 24 finding).
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest'` is green.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*RedactionTest'` is green, and `git diff --quiet src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt` exits 0 — the prompt path's own guard passed WITHOUT being edited.
    - Source assertion: `grep -v '^\s*[/*]' src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt | grep -c 'lowered == "cookie"'` returns `0`.
    - Source assertion: `grep -v '^\s*[/*]' src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt | grep -c 'Redaction.isCookieHeaderName'` returns `1`.
    - Source assertion: `grep -v '^\s*[/*]' src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt | grep -c 'val lowered = name.lowercase()'` returns `0`, and `grep -v '^\s*[/*]' src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt | grep -c 'Locale.ROOT'` returns at least `1`.
    - Source assertion: `grep -v '^\s*[/*]' src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt | grep -c 'fun isCookieHeaderName'` returns `1`.
    - Source assertion, THE SCOPE GUARD on the KDoc this task writes (the plan's high-severity prohibition applies to source comments, not only to planning records): `grep -ci 'whole codebase\|entire codebase' src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` returns `0`. Baseline before this task is `0`, so any non-zero result is text this task introduced. Deliberately unfiltered by comment stripping — the whole point is what the COMMENT says.
    - Behaviour assertion, run as the test itself: after `sanitizeHeaders` under STRICT, the JSON produced by `toolJson.encodeToString` and then by `redactIfNeeded` contains `X-Cookie` and `benignidcontrolvalue`, and does not contain `sentinelxrayninezulu`.
    - RED PROBE A recorded with its observed failure output, and `git diff --quiet src/main/kotlin` exits 0 afterwards — the probe was restored.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt` green and `git diff --quiet detekt-baseline.xml` exits 0 — no baseline entry added.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck` green.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest' --tests '*RedactionTest' detekt ktlintCheck</automated>
  </verify>

  <done>
    One cookie name variant is proven stripped along the entire real flow — predicate, `sanitizeHeaders`,
    `toolJson` serialisation, `redactIfNeeded` — the header name and the negative control both survive,
    and reverting the matcher turns the assertion red. The architecture of the fix (one shared
    predicate, called from the tool-result path) is committed and working before anything is expanded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The full variant matrix, per privacy mode, plus the locale and empty-value edges</name>

  <reversibility rating="reversible">D-27-02 accepts the `Cookie-Consent` over-match for parity with the prompt path; undoing it is one predicate edit plus one test.</reversibility>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt` — the class as Task 1 left it, including `assertHeadersMatchPolicy` at lines 128-188. Extend that pattern; derive expectations from `RedactionPolicy.fromMode(mode)` rather than re-encoding the mode-to-flag table.
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` lines 13-48 — `RedactionPolicy.fromMode` and the three `PrivacyMode` values, so the OFF expectations are read from the policy, not assumed.
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt` lines 434-478 — the exact five variant names and the `X-Request-Id` negative control the prompt path uses. Use the SAME seven names so the two suites are comparable by eye.
    - `build.gradle.kts` lines 290-302 — the five test-class-name suffixes excluded from the fast PR gate. Any new class must avoid all five.
  </read_first>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt</files>

  <behavior>
    All tests written failing first, appended to the `SanitizeHeaders` nested class.

    - `cookieHeaderNameVariantsAreStrippedOnTheToolResultPath`: for `PrivacyMode.STRICT` and
      `PrivacyMode.BALANCED`, a header list of `Cookie`, `Set-Cookie`, `Cookie2`, `X-Cookie`,
      `Set-Cookie2`, `X-Original-Cookie`, `X-Forwarded-Cookie` (each with its own distinct sentinel
      value) and `X-Request-Id` returns `[STRIPPED]` for all seven cookie-named entries, keyed by
      each header's OWN original-cased name, and returns `X-Request-Id` byte-identical.
    - `offModePassesEveryCookieNameVariantThrough`: the same list under `PrivacyMode.OFF` returns
      every value unchanged, including the five variants. OFF is the user's explicit choice.
    - `headersMerelyContainingCookieAreStrippedByDesign`: `Cookie-Consent` and `X-Cookie-Policy` are
      stripped under STRICT. Name the test and its comment so the reader knows this is the ACCEPTED
      cost of D-27-02 and not an oversight, and cross-reference T-21-WA3.
    - `benignHeaderNamesWithoutTheTokenSurvive`: `X-Cook`, `Cook-ie` and `Accept` are returned
      byte-identical under STRICT — the predicate is a substring test on the whole token, so a name
      that merely shares letters is not caught.
    - `cookieNameMatchingSurvivesATurkishDefaultLocale`: with the JVM default locale set to
      `Locale.forLanguageTag("tr-TR")`, `X-COOKIE` and `SET-COOKIE` are still `[STRIPPED]` under
      STRICT, and `AUTHORIZATION` is still `[REDACTED]` and `HOST` still anonymised — proving the
      locale fix covers all three comparisons, not only the cookie branch.
    - `emptyValuedCookieHeaderIsStillRedacted`: a `Set-Cookie` header whose value is the empty string
      returns `[STRIPPED]`, not an empty string. An empty-but-present value is never passed through.
    - `emptyHeaderListYieldsEmptyMap` already exists at line 122 — do not duplicate it; reference it
      in the SUMMARY as the other half of the `empty` edge.
  </behavior>

  <action>
    Extend the existing `SanitizeHeaders` nested class in `McpToolHelpersTest.kt` with the six tests
    named above. Do not create a new test class — the production file already has one and splitting
    the assertions across two classes is how a reader stops finding them.

    Give each of the seven cookie-named fixtures a DISTINCT sentinel value (the prompt path uses
    `sentinelalphaone` … `sentinelechofive`; continue that scheme). Distinct sentinels are what make
    a partial fix visible: with one shared value, a matcher that catches six of seven still passes.

    For the locale test, save `Locale.getDefault()` into a local, set the default to
    `Locale.forLanguageTag("tr-TR")` inside a `try`, and restore it in a `finally` — restoring in
    `finally`, not at the end of the happy path, because a failed assertion must not leave a Turkish
    default locale behind for every later test class in the shared JVM. JUnit parallel execution is
    NOT enabled in this project (verified: no `junit-platform.properties` and no
    `junit.jupiter.execution.parallel.enabled` system property in `build.gradle.kts`), so a global
    default swap is safe here; add `@ResourceLock(Resources.LOCALE)` from
    `org.junit.jupiter.api.parallel` as cheap insurance against that changing, and if that annotation
    is unavailable in JUnit Jupiter 6.0.3 drop it and record the omission rather than fighting it.

    Assert the locale test is non-vacuous before trusting it: with the default locale set to `tr-TR`,
    confirm in the test (or in a scratch run recorded in the SUMMARY) that a no-argument lowercasing
    of the literal `COOKIE` does NOT equal the lowercase cookie token. If it does, the JVM is not
    exhibiting the hazard and the test proves nothing — say so in the SUMMARY instead of claiming a
    pass.

    Finish RED PROBE B from Task 1 here if it was deferred: remove the `Locale.ROOT` argument from
    `Redaction.isCookieHeaderName`, run this class, record that
    `cookieNameMatchingSurvivesATurkishDefaultLocale` turns red while the non-locale variant tests
    stay green — two mutations failing two different assertions is the evidence that neither half of
    the fix is redundant. Restore with `git checkout HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt`.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest'` is green and the run reports at least 6 more tests than the count recorded at the end of Task 1 (record both counts in the SUMMARY).
    - Behaviour assertion: under STRICT and under BALANCED, all seven of `Cookie`, `Set-Cookie`, `Cookie2`, `X-Cookie`, `Set-Cookie2`, `X-Original-Cookie`, `X-Forwarded-Cookie` map to `[STRIPPED]`, each under its own original-cased key.
    - Behaviour assertion: under OFF, all seven map to their original values.
    - Behaviour assertion: with the JVM default locale set to `tr-TR`, `X-COOKIE` maps to `[STRIPPED]`, `AUTHORIZATION` maps to `[REDACTED]`, and `HOST` maps to `Redaction.anonymizeHost("api.example.com", salt)`.
    - Behaviour assertion: a `Set-Cookie` header with an empty-string value maps to `[STRIPPED]`.
    - Behaviour assertion: `X-Cook`, `Cook-ie`, `Accept` and `X-Request-Id` are returned byte-identical under every mode.
    - `Locale.getDefault()` is unchanged after the whole class runs — assert it in an `@AfterEach` or prove it by running `*McpToolHelpersTest` and `*RedactionTest` in the same invocation and observing both green.
    - RED PROBE B recorded with its observed output, and `git diff --quiet src/main/kotlin` exits 0 afterwards.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew detekt ktlintCheck` green, `git diff --quiet detekt-baseline.xml` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest' --tests '*RedactionTest' detekt ktlintCheck</automated>
  </verify>

  <done>
    Every measured variant, both canonical names, both stripping modes, the OFF passthrough, the
    accepted over-match, the negative controls, the Turkish-locale case and the empty-value case are
    asserted on the tool-result path, and each of the two production mutations fails a different
    assertion.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route the third hand-written copy through the predicate, classify every surviving matcher, and make the ownership invariant a test rather than a grep</name>

  <reversibility rating="reversible">One conjunct in one filter lambda plus one locale argument; undo is a two-line revert. The ownership test is additive.</reversibility>

  <read_first>
    - `src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerFilters.kt` lines 160-183 — `sanitizeHeadersForPrompt`. Read the WHOLE filter lambda: `headerNoiseDenylist`, the `x-` prefix rule, the three substring tests on line 172, and the two allowlists. Note this is an ADMITTER (which headers enter the prompt) not a redactor, so widening it admits MORE headers for analysis; the redaction of their values is `Redaction.apply`'s job and is already correct.
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` — `isCookieHeaderName` as Task 1 created it.
    - **The classification table in this plan's "The surviving cookie-header-name matchers, classified" section, and then EVERY file it names, read directly.** Specifically: `scanner/PassiveAiScannerAnalysis.kt` around line 267 and its consumer chain into `scanner/PassiveAiScannerPrompts.kt` (`cookieSectionLines`, `redactScanMetadata`); `scanner/PassiveAiScannerHeuristics.kt` around lines 102 and 117; `prompts/bountyprompt/BountyPromptTagResolver.kt` lines 75-110 and 138-155 (confirm for yourself that `extractCookies` is handed the outputs of `Redaction.apply`, not the raw text); `scanner/ActiveAiScanner.kt` around lines 936 and 1411. Confirm or contradict each row. A row you cannot confirm is a row you must not certify.
    - `src/test/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerConfidenceTest.kt` lines 1-40 — how a real `PassiveAiScanner` is constructed in a unit test. Copy that construction; do not invent one.
    - `src/test/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerPromptRedactionTest.kt` lines 1-50 — the `@BeforeEach` / `@AfterEach` that clear `Redaction`'s custom patterns, and the reason. Mirror it.
    - `build.gradle.kts` lines 290-302 — the excluded suffixes; `PassiveAiScannerHeaderAdmissionTest` and `CookieHeaderRuleOwnershipTest` match none of them, which is why those names were chosen.
  </read_first>

  <files>src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerFilters.kt, src/test/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeaderAdmissionTest.kt, src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderRuleOwnershipTest.kt</files>

  <behavior>
    New file `PassiveAiScannerHeaderAdmissionTest`, tests written failing first where the change makes
    them fail (the locale test will be red before the fix; the admission-parity test should be GREEN
    before and after — say so, and treat a red there as a regression you introduced):

    - `admitsEveryCookieNameVariantAfterRoutingThroughTheSharedPredicate`: for `isRequest = true` and
      for `isRequest = false`, a header list of `Cookie`, `Set-Cookie`, `Cookie2`, `X-Cookie`,
      `Set-Cookie2`, `X-Original-Cookie`, `X-Forwarded-Cookie` yields seven admitted lines, each
      carrying its own original-cased name.
    - `admissionSurvivesATurkishDefaultLocale`: with the default locale set to `tr-TR`, `X-COOKIE`
      and `SET-COOKIE` are still admitted.
    - `nonCookieAdmissionRulesAreUnchanged`: a header in `headerNoiseDenylist` is still rejected, an
      `x-` prefixed header is still admitted, an `auth`-containing and a `token`-containing name are
      still admitted, and a plain allowlisted name is still admitted for its own direction. This is
      the regression guard on the parts of the lambda this task must NOT change.

    New file `CookieHeaderRuleOwnershipTest` in package `com.six2dez.burp.aiagent.redact` — the
    repository-state test that replaces the execution-time grep as the durable carrier of the
    ownership invariant. It scans `src/main/kotlin` from the Gradle working directory, strips
    comment-only lines before matching, and works off two declared file-private values:

    - `MATCHER_SPELLINGS`: the ways a cookie header name is named by hand in this codebase —
      `equals("Cookie"` and `equals("Set-Cookie"` (with or without `ignoreCase`), `startsWith("Cookie:"`
      and `startsWith("Set-Cookie:"`, the Montoya `headerValue("Cookie")` / `headerValue("Set-Cookie")`
      lookup, `contains("cookie")`, and equality against the two lowercase canonical spellings.
      Express them as `Regex`es, case-sensitively where the Kotlin literal is case-sensitive. These
      five classes were measured against the tree: unfiltered they return nine sites in SIX files,
      and filtered to the redaction paths they return exactly the two this phase fixes — so the
      spelling set is neither vacuous nor a rubber stamp. If your own run returns a different unfiltered
      set, that is new information: record it and classify the difference before proceeding.
      Bound this honestly in the file header (see `<action>`): these five classes are what the scan
      can see, and a matcher spelled outside them escapes it silently.
    - `CLASSIFIED_NON_REDACTING`: a map from RELATIVE FILE PATH to a one-line reason, holding exactly
      the non-redacting SWEEP-HIT rows of this plan's classification table — four files. The three
      rows marked READER NOTE, NOT A SWEEP HIT (`scanner/PassiveAiScanner.kt`,
      `scanner/InjectionPointExtractor.kt`, `redact/Redaction.kt`) return zero hits and MUST NOT be
      entered: a key the sweep never hits is a dead entry, and the reverse-direction assertion below
      fails on dead entries by design. Keyed on path, not line number, so the test does not rot the
      first time a file is reformatted.

    Tests:
    - `noHandWrittenCookieMatcherSurvivesInTheRedactionPaths`: `mcp/tools/McpToolHelpers.kt` and
      `scanner/PassiveAiScannerFilters.kt` contain ZERO `MATCHER_SPELLINGS` hits. These two files are
      NOT allowlistable — they are the redaction paths, and the whole phase is about them calling the
      shared predicate instead of writing their own rule. `redact/Redaction.kt` is exempt as the OWNER
      of the rule, and the test states that exemption in its own message.
    - `everyCookieHeaderNameMatcherInMainIsClassified`: walk every `.kt` file under `src/main/kotlin`,
      collect the set of files with at least one `MATCHER_SPELLINGS` hit, subtract the two redaction
      paths and the owner, and assert the remainder equals `CLASSIFIED_NON_REDACTING`'s key set
      EXACTLY — in both directions. A new file with a hand-written matcher fails with a message
      telling its author to route it through `Redaction.isCookieHeaderName` or classify it here with a
      reason. A classified file that no longer matches also fails, so the allowlist cannot silently
      accumulate dead entries.
    - `theOwnershipScanIsNonVacuous`: assert the walk found at least 150 `.kt` files, and assert every
      `MATCHER_SPELLINGS` regex matches a known in-test positive fixture string. A broken path, an
      empty walk or a regex that matches nothing must FAIL rather than pass — the Phase 23 lesson, and
      the same guard 27-02's corpus test applies to itself.
  </behavior>

  <action>
    In `PassiveAiScannerFilters.sanitizeHeadersForPrompt`, replace the hand-written cookie substring
    test — the third of the three `contains` conjuncts on line 172 — with a call to
    `Redaction.isCookieHeaderName(header.name())`, passing the ORIGINAL header name so the predicate
    applies its own locale-safe lowering. Leave the `auth` and `token` conjuncts exactly as they are:
    they belong to the open-ended vendor class that `.planning/codebase/CONCERNS.md` records as
    deliberately DEFERRED, and folding them into this change would quietly extend the phase's scope
    into a class the maintainer decided not to close.

    In the same edit, change the lowered-name computation on line 169 to lowercase with
    `java.util.Locale.ROOT`, which also fixes the `headerNoiseDenylist` lookup, the `x-` prefix test
    and both allowlist lookups on the same line's value. Add the `java.util.Locale` import if absent.

    Do NOT touch line 79's lowering in the same file, and do NOT touch any other default-locale
    lowering anywhere in the tree. D-27-04's scope is the two header-name functions this phase
    changes, full stop. But the blast radius must be RECORDED so no closure note in plan 27-03 can
    read as if the hazard is closed repo-wide. Run:

      `grep -rn 'lowercase()' src/main/kotlin --include=*.kt | grep -v 'Locale' | wc -l`

    and record the observed number in the SUMMARY as a measured backlog observation, together with an
    explicit named list of the security-relevant survivors you confirmed by reading:
    `scanner/PassiveAiScannerFilters.kt:79`, `mcp/McpAccessControlDecision.kt:225` and `:272`
    (`isBrowserUserAgent`), `mcp/KtorMcpServerManager.kt:348-350` (scheme and host),
    `mcp/McpTakeoverProof.kt:73` (host inside the HMAC message),
    `mcp/tools/ResponsePreprocessor.kt:106/119/123` (content-type allowlist), and
    `mcp/tools/McpToolLegacy.kt:316/323` (cookie-jar domain filter). State in one line that all of
    these are deliberately OUT of this phase's scope and belong to a future locale-hardening pass.

    Then classify. For each row of this plan's "The surviving cookie-header-name matchers,
    classified" table, open the file named in the row, read the matcher AND its consumer, and record
    in the SUMMARY: the site, the classification you arrived at, and the source evidence you arrived
    at it from. Where your read agrees with the table, say so and cite the line you read. Where it
    DISAGREES, say so plainly, act on your read, and treat the disagreement as a finding that changes
    what plan 27-03 is allowed to certify. The table is a lead to confirm; it is not evidence.

    Create `src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderRuleOwnershipTest.kt` with the
    three tests described in `<behavior>`. Seed `CLASSIFIED_NON_REDACTING` from the classification you
    just performed, not from the table — each entry carries the reason YOU verified, and ONLY files
    the sweep actually hits go in it (the three rows marked READER NOTE, NOT A SWEEP HIT are excluded
    by construction; seeding one produces a dead key that fails the reverse-direction assertion).

    Give the file a header comment stating what it is for AND what it is bounded by. What it is for:
    the v0.10.0 milestone audit found a security rule with two implementations that had drifted, and
    an execution-time grep can only prove the tree is clean on the day it runs, so this test makes a
    FOURTH implementation fail CI instead. What it is BOUNDED BY, stated in as many words in the same
    header: the scan sees only the five `MATCHER_SPELLINGS` classes, so a matcher spelled outside
    them — a `startsWith` on an already-lowered name, an `in` operator, a `when` branch — is invisible
    to it and leaves this test GREEN. Say plainly that this test is a TRIPWIRE over measured
    spellings, not a proof of exhaustive coverage, and that anyone introducing a new spelling class
    must add it to `MATCHER_SPELLINGS` or the tripwire silently stops covering their code. A reader
    who mistakes the tripwire for a proof reproduces, one iteration smaller, the defect this whole
    phase exists to repair. English only.

    If the test's directory walk cannot resolve `src/main/kotlin` from the Gradle test working
    directory, do NOT weaken it into a skip or a no-op — resolve the path from a system property set
    in `build.gradle.kts`, or report the obstacle and record the gate as remaining an execution-time
    grep with the reason. A repository-state test that silently passes when it cannot find the
    repository is worse than the grep it replaced.

    Add a one-line comment above the changed conjunct naming `Redaction.isCookieHeaderName` as the
    single cookie-header-name rule ACROSS THE TWO REDACTION PATHS AND THIS ADMITTER — scoped in the
    comment itself, the same way `isCookieHeaderName`'s own KDoc scopes it, never as an unqualified
    tree-wide singularity claim — and pointing at the v0.10.0 milestone audit as the reason all three
    sites now share it. English only.

    Create `src/test/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerHeaderAdmissionTest.kt`
    with the three tests above, building a real `PassiveAiScanner` the way
    `PassiveAiScannerConfidenceTest` does, and `HttpHeader` fixtures as mockito mocks (the Montoya
    static factory dereferences a null `ObjectFactoryLocator.FACTORY` outside the Burp runtime — the
    reason `McpToolHelpersTest` mocks its headers).

    If constructing a `PassiveAiScanner` proves to require Burp-runtime state that cannot be
    satisfied headlessly, STOP and report it rather than deleting the task: fall back to asserting
    `Redaction.isCookieHeaderName` directly over the same seven names plus a source assertion that
    the conjunct on line 172 calls it, and record explicitly in the SUMMARY that the admitter itself
    is covered structurally rather than behaviourally, with the reason.
  </action>

  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*PassiveAiScannerHeaderAdmissionTest'` is green and reports at least 3 tests.
    - Source assertion: `grep -v '^\s*[/*]' src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerFilters.kt | grep -c 'name.contains("cookie")'` returns `0`.
    - Source assertion: `grep -v '^\s*[/*]' src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerFilters.kt | grep -c 'isCookieHeaderName'` returns `1`.
    - Source assertion: `grep -v '^\s*[/*]' src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerFilters.kt | grep -c 'name.contains("auth")'` returns `1` — the deferred vendor class was NOT folded in.
    - Source assertion, THE SCOPE GUARD on the two comments this task writes: `grep -ci 'whole codebase\|entire codebase' src/main/kotlin/com/six2dez/burp/aiagent/scanner/PassiveAiScannerFilters.kt src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderRuleOwnershipTest.kt` returns `0` for both files. Deliberately unfiltered by comment stripping — the comments are exactly what is under test here.
    - Bound assertion on the ownership test's file header, verified by reading it: the header names `MATCHER_SPELLINGS` as the limit of what the scan can see, gives at least one example of a spelling that escapes it, and calls the test a tripwire rather than a proof. Quote the sentence in the SUMMARY.
    - Seeding assertion: `CLASSIFIED_NON_REDACTING` contains exactly four keys — the four non-redacting files the sweep actually hits — and contains NO key for `scanner/PassiveAiScanner.kt`, `scanner/InjectionPointExtractor.kt` or `redact/Redaction.kt`, none of which the sweep hits. Record the four keys in the SUMMARY.
    - REDACTION-PATH invariant, narrow spelling: `grep -rn 'contains("cookie")' src/main/kotlin --include=*.kt | grep -v 'isCookieHeaderName' | wc -l` returns `0`.
    - REDACTION-PATH invariant, WIDENED spelling — the gate the narrow one could not support. This command must return `0`:
      `grep -rnE 'equals\("(Set-)?Cookie"|startsWith\("(Set-)?Cookie:"|headerValue\("(Set-)?Cookie"\)|contains\("cookie"\)|== "(set-)?cookie"' src/main/kotlin --include=*.kt | grep -v 'isCookieHeaderName' | grep -vE 'PassiveAiScannerAnalysis\.kt|PassiveAiScannerHeuristics\.kt|BountyPromptTagResolver\.kt|ActiveAiScanner\.kt|Redaction\.kt' | wc -l`
      The excluded files are the classified non-redacting sites plus `Redaction.kt` as the rule's owner. Scope note, and it is load-bearing: this gate proves the claim *"one cookie-header-name rule across the two redaction paths and the passive-scan admitter"* — it does NOT prove, and must never be quoted as proving, a whole-codebase claim.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*CookieHeaderRuleOwnershipTest'` is green and reports exactly 3 tests.
    - Ownership-test falsifiability, recorded with observed output: temporarily add a hand-written cookie-name matcher to a `.kt` file under `src/main/kotlin` that is NOT in the classification, run the ownership test, record that `everyCookieHeaderNameMatcherInMainIsClassified` goes red naming that file, then restore with `git checkout HEAD -- <path>`. Never `git stash`. Without this probe the allowlist is an assumption.
    - The SUMMARY carries the per-site classification with the source evidence read in THIS task, and states for each site whether the read agreed with the plan's table or contradicted it.
    - The SUMMARY carries the measured default-locale backlog observation with its observed count and the named survivor list, and states that they are out of this phase's scope.
    - Behaviour assertion: all seven cookie name variants are admitted for both `isRequest` values, each keeping its own original-cased name.
    - Behaviour assertion: with the default locale set to `tr-TR`, `X-COOKIE` is still admitted, and `Locale.getDefault()` is restored afterwards.
    - Behaviour assertion: a denylisted header is still rejected and an allowlisted one still admitted — the untouched branches did not move.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck` all green over the WHOLE suite, `git diff --quiet detekt-baseline.xml` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck</automated>
  </verify>

  <done>
    The two redaction paths and the passive-scan admitter share exactly one cookie-header-name rule,
    proven by a sweep that covers exact-name, `ignoreCase`, line-prefix and substring spellings rather
    than one of them. Every OTHER cookie-header-name matcher in `src/main/kotlin` is classified as
    non-redacting with source evidence read in this task. The passive scanner's admission set is
    unchanged for every non-cookie rule. The classification is carried by
    `CookieHeaderRuleOwnershipTest`, proven falsifiable by a probe, so a FOURTH consumer added later
    IN ANY OF THE FIVE MEASURED SPELLING CLASSES fails a test rather than passing unnoticed — a
    tripwire bounded by `MATCHER_SPELLINGS`, stated as such in the test's own header, not a proof of
    exhaustive coverage. The surviving default-locale hazards are recorded as an out-of-scope backlog
    observation, not as closed.
  </done>
</task>

</tasks>

<assumption_delta_decision>
The detector fired `pluralization` on *"the **second** redaction path"* (`gsd_run query
assumption-delta scan 27`). It is a true positive: this codebase went from one redaction path to two
(Phase 26 added the MCP tool-result path), and nobody re-asked whether "the cookie header name rule"
still named one thing.

- **Primary noun:** the cookie-header-name RULE — not "the prompt path's regex".
- **Decision:** `promote`. `Redaction.isCookieHeaderName` becomes the primary; the two regexes and
  all three call sites are demoted to consumers of one literal and one predicate. Rejected
  `add-alongside` explicitly: giving `sanitizeHeaders` its own matcher rebuilds the structure that
  produced this gap.
- **Invariant test (accepted, planned):** plan 27-02's `CookieHeaderNameParityTest` asserts that any
  name the prompt path strips is a name the predicate matches. It goes red the instant a future phase
  widens one side and forgets the other.

The second signal, `chosen` on `custom`, is a **false positive** — it matched the JAR filename
`Custom-AI-Agent-full-1.0.0.jar` in the phase goal. No decision recorded.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Burp data → MCP tool result → AI backend | `sanitizeHeaders` is the redaction path for MCP tool RESULTS. It is independent of `Redaction.apply`'s prompt path and, for header names, is the LAST line of defence — `redactIfNeeded` provably cannot recover a header it misses. |
| Burp data → passive-scan prompt → AI backend | `sanitizeHeadersForPrompt` decides which headers enter the passive-scan prompt at all; `Redaction.apply` then redacts their values. |
| JVM default locale → security control | The default locale is ambient, host-controlled input to every no-argument `lowercase()` in a matcher. A Turkish-locale host silently disables an ASCII header-name control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01-01 | Information Disclosure | `McpToolHelpers.sanitizeHeaders` cookie matcher | high | mitigate | Exact-name equality replaced by `Redaction.isCookieHeaderName` (name-contains-`cookie`), reaching parity with the prompt path. Asserted for all five measured variants plus both canonical names, under STRICT and BALANCED, and proven falsifiable by RED PROBE A which reverts the matcher. |
| T-27-01-02 | Information Disclosure | header-name lowering under a non-ROOT JVM default locale | high | mitigate | `lowercase(Locale.ROOT)` inside `isCookieHeaderName` and at both call-site lowerings (`McpToolHelpers.kt:319`, `PassiveAiScannerFilters.kt:169`), covering the cookie, token-header and host comparisons together. Asserted with the default locale set to `tr-TR`; falsifiable by RED PROBE B. |
| T-27-01-03 | Information Disclosure | `McpToolContext.redactIfNeeded` — line-anchored cookie regexes vs single-line JSON | medium | **accept** | Residual **AR-27-01**. Un-anchoring would change the prompt path's behaviour across every body. Made safe by the outcome-level end-to-end assertion in Task 1, which locks the result regardless of which layer strips it. Severity is medium, not high, because after T-27-01-01 this is a missing BACKSTOP behind a control that is now correct and tested — not a leak path. |
| T-27-01-04 | Information Disclosure | `cookie` absent from `SENSITIVE_WORDS` | medium | **accept** | Residual **AR-27-02**. Adding it would redact every JSON key, form parameter and query parameter containing `cookie` as a token, against the measured Phase 21 decision that removed `key` and `code` from broad matching after 27 false positives on a 32-name corpus. Same backstop reasoning as AR-27-01. |
| T-27-01-05 | Tampering | parity reached by NARROWING the prompt path | high | mitigate | Prohibition (high) plus a hard acceptance criterion: `RedactionTest.cookieHeaderNameVariantsAreStripped` must pass with `git diff --quiet` on `RedactionTest.kt`. A fix that edits the prompt path's guard to agree fails the plan. |
| T-27-01-06 | Information Disclosure | a third hand-written cookie test in `sanitizeHeadersForPrompt` | medium | mitigate | Routed through the shared predicate in Task 3. The no-fourth-copy invariant is carried by `CookieHeaderRuleOwnershipTest` — a repository-state test with a per-file classification allowlist, proven falsifiable by an unclassified-matcher probe — rather than by an execution-time grep that expires the moment the phase ends. The grep survives as a secondary gate, widened to cover exact-name, `ignoreCase` and line-prefix spellings. Residual, stated rather than papered over: the test's reach is bounded by the five `MATCHER_SPELLINGS` classes, so a fourth copy spelled outside them stays invisible to it. The bound is written into the test's file header so its next reader treats it as a tripwire, not a proof. |
| T-27-01-09 | Repudiation | an ownership claim wider than the sweep that supports it | high | mitigate | The original gate matched one substring spelling and could not see four surviving matchers. Mitigated three ways: the sweep is widened to four spelling classes; every survivor is classified as redactor / admitter / extractor / local-analysis against source read in-task; and every claim in this phase — SC1, the `must_haves` truth, the Task 3 `<done>`, and everything plan 27-03 is permitted to certify — is scoped to *"the two redaction paths and the passive-scan admitter"* by a high-severity prohibition. A codebase-wide claim on a narrow gate is the v0.10.0 defect relocated one layer up. |
| T-27-01-07 | Denial of Service | predicate cost on a per-header hot path | low | mitigate | The predicate is a `lowercase` plus a `contains` on a `String` — no `Regex`, no compilation, no backtracking surface. Same reasoning as T-26-01-03's per-character allowlist scan. |
| T-27-01-08 | Repudiation | over-redaction of benign `*cookie*` names left undocumented | low | mitigate | The accepted cost (D-27-02 / T-21-WA3) is asserted by a NAMED test, `headersMerelyContainingCookieAreStrippedByDesign`, so it is a recorded decision in the suite rather than a surprise in the field. |
| T-27-01-SC | Tampering | npm/pip/cargo installs | n/a | **accept** | This plan installs no package and adds no Gradle dependency. `junit-jupiter`, `mockito-kotlin` and `montoya-api` are already on `testImplementation`. No package-legitimacy audit is required because there is no install task. |

ASVS level 1; blocking severity `high`. The register holds FOUR `high` threats — `T-27-01-01`,
`T-27-01-02`, `T-27-01-05` and `T-27-01-09` — and all four are `mitigate`, closed by this plan's
acceptance criteria. `T-27-01-03`, `T-27-01-04` and `T-27-01-06` are medium; `T-27-01-07` and
`T-27-01-08` are low. No `high` threat is `accept`: the two accepted residuals AR-27-01 and AR-27-02
are medium by the reasoning in their own rows — after `T-27-01-01` they are missing backstops behind
a control that is now correct and tested, not leak paths — and both are pinned by Task 1's
outcome-level end-to-end assertion. Counts are stated explicitly so `/gsd-secure-phase`
reconciliation has a number to check against rather than a claim to re-derive.
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck` all green over the whole suite.
- `git diff --quiet detekt-baseline.xml` exits 0 — no baseline entry added for any new code.
- `git diff --quiet src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt` exits 0 — the prompt path's guard passed unedited.
- The WIDENED sweep from Task 3's acceptance criteria returns `0`, and the narrow `contains("cookie")` sweep returns `0`. Neither is quoted anywhere as proving a whole-codebase claim.
- `CookieHeaderRuleOwnershipTest` is green with 3 tests, and its unclassified-matcher probe is recorded with observed output.
- The SUMMARY carries the per-site classification with in-task source evidence, and the default-locale backlog observation with its measured count.
- Both red probes recorded with their observed output, each failing a DIFFERENT assertion.
</verification>

<success_criteria>
- SC1: `Redaction.isCookieHeaderName` exists, is public, lowercases with `Locale.ROOT`, and is the only cookie-header-name rule across **the two redaction paths and the passive-scan admitter** — `McpToolHelpers.sanitizeHeaders`, `Redaction.apply`'s two regexes (which share its `COOKIE_NAME_TOKEN`) and `PassiveAiScannerFilters.sanitizeHeadersForPrompt`. Not a whole-codebase claim: other cookie-header-name matchers survive and are individually classified as non-redacting.
- SC1b: every surviving matcher found by the widened sweep is classified with source evidence read in-task, and the classification is enforced by `CookieHeaderRuleOwnershipTest` rather than by an execution-time grep alone — a tripwire bounded by the five `MATCHER_SPELLINGS` classes, with that bound written into the test's own file header, not a claim of exhaustive coverage.
- SC2: `McpToolHelpers.sanitizeHeaders` strips all five measured variant names plus both canonical names under STRICT and BALANCED, keeps each header's own name, and passes them all through under OFF.
- SC3: an `X-Cookie` sentinel does not survive `sanitizeHeaders` → `toolJson` → `redactIfNeeded`, asserted on the final string.
- SC4: `X-COOKIE` is still stripped with the JVM default locale set to `tr-TR`, and the default locale is restored afterwards.
- SC5: two mutations (exact-name revert; `Locale.ROOT` removal) fail two different assertions, both recorded with observed output.
- SC6: the prompt path is byte-unchanged in behaviour and its test file is unedited.
</success_criteria>

<output>
Create `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-01-SUMMARY.md` when done.
Record: the pre/post test counts, both red probes with their observed failure text, the observed
`Locale.getDefault()` behaviour under `tr-TR`, whether the passive-scanner admitter was covered
behaviourally or structurally, the per-site classification of every surviving cookie-header-name
matcher with the source evidence read in-task (and any place your read contradicted the plan's
table), the ownership test's unclassified-matcher probe with its observed output, and the measured
default-locale backlog observation with its count and named survivors.

State the ownership claim in the SUMMARY at the scope the evidence supports — the two redaction paths
and the passive-scan admitter — because plan 27-03 cites this SUMMARY when it amends a security
register, and a claim that widens between here and there is exactly the failure being repaired.
</output>
