---
phase: 27-priv-05-gap-closure-sanitize-headers
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on: ["27-04"]
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionRedactionTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionSiteInventoryTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt
autonomous: true
requirements: [PRIV-05]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The set of serialized emission sites is established BY MEASUREMENT and PINNED, so an eleventh or fifteenth site cannot be missed the way this one was: 14 call sites total, 7 in each executor file, 10 carrying a raw HTTP message across 5 tool names, 4 carrying a WebSocket payload across 2 tool names. A count that drifts fails a test rather than a review."
    - "The structural claim is asserted rather than asserted-about, over ALL THREE registration paths and not two. `src/main/kotlin` contains exactly three `addTool(` call sites: `McpTool.kt:34` and `McpTool.kt:72`, which wrap their handler output in `context.redactIfNeeded(` at `:45` and `:78` (and `mcpPaginatedTool` delegates to the first at `:82`); and `McpToolHandlers.kt:122`, whose handler does NOT wrap and is safe only because it delegates to `McpToolExecutor.executeToolResult`, which wraps at `McpToolExecutorImpl.kt:1037`. A fourth `addTool` site, or a `McpToolHandlers` handler that emits directly instead of delegating, fails a test rather than passing a review."
    - "The tripwire STATES BOTH OF ITS BOUNDS where a reader meets them, separately, because they are different bounds. The emission-site scans cover two named executor files for one measured call shape — a site added in a third file, or shaped differently, is invisible to them. The registration-path scan covers ALL of `src/main/kotlin` for `addTool(`, but proves choke-point reachability only for the delegation shape as written: a `McpToolHandlers` handler that kept the `executeToolResult` call AND emitted its own payload beside it would still pass. Tripwires over a measured inventory, not a proof."
    - "A credential-bearing auth header VALUE (`X-API-Key` and the rest of `authHeaderRegex`'s exact-name alternation) does not survive the serialized emission shape in STRICT or BALANCED. Measured before the change: it survived in both modes, un-rescued by `bearerRegex` / `basicAuthRegex` / `jwtRegex` because its value is none of those shapes."
    - "The logical-line boundary is composed into a MEASURED, STATED set of rules and no others — the two cookie rules and `authHeaderRegex`, all three built by the `logicalLineHeaderRule` composer plan 27-04 added — and `hostHeaderRegex` is deliberately excluded for a recorded reason. A source-state tripwire fails if that set changes without the record changing with it."
    - "The `Host:` header value and the sibling `url` field surviving un-anonymised under STRICT on this shape is MEASURED, quoted, and carried into plan 27-06 as open finding AR-27-04. It is recorded, never pinned by a green test, and never silently ignored."
    - "Every test added here is non-vacuous by construction: the scans fail rather than skip when the repository root cannot be resolved, enforce a source-line floor, strip comment lines before matching, and require at least one positive fixture per class scanned."
  artifacts:
    - "src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionSiteInventoryTest.kt — NEW, the emission-site inventory tripwire with pinned counts and the choke-point reachability assertion"
    - "src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt — NEW, the source-state tripwire pinning which rules the `logicalLineHeaderRule` composer is applied to"
    - "src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt — `authHeaderRegex` rebuilt through the same `logicalLineHeaderRule` composer, so it inherits plan 27-04's measured two-branch shape including the escape-pair tail that keeps a value ending in a backslash from destroying the JSON"
    - "src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionRedactionTest.kt — auth-header assertions added to the existing probe family"
  key_links:
    - "`SerializedEmissionSiteInventoryTest` -> `McpToolExecutorImpl.kt` + `McpToolLegacy.kt` -> `McpTool.kt:45` / `:78` -> `McpToolContext.redactIfNeeded`: the chain that makes the rule-level fix structural. If a future site bypasses the wrapper, this is the test that goes red."
    - "`McpToolHandlers.kt:122` (`registerToolHandler`, a bare `addTool`) -> `McpToolExecutor.executeToolResult` -> `McpToolExecutorImpl.kt:1037` `context.redactIfNeeded(output)`: the THIRD registration path, safe only by delegation. It is the one a reader of the choke-point claim would otherwise never meet, so the test asserts the delegation explicitly rather than leaving the path unnamed."
    - "`LogicalLineBoundaryScopeTest` -> `Redaction.kt` rule composition -> `26-SECURITY.md` / `CONCERNS.md` (plan 27-06): the source-state fact and the recorded claim must agree; a reader who finds them disagreeing has found a real regression."
    - "the measured `Host:` / `url` probe output -> AR-27-04 in plan 27-06: the evidence link that keeps the residual a recorded finding rather than an aside in a SUMMARY."
  prohibitions:
    - statement: "Do NOT commit a test that asserts a leaked value SURVIVES. The `Host:` and `url` residual is measured with a throwaway probe and recorded in prose; a green assertion that a host reaches a backend under STRICT is exactly the artifact a future audit misreads, and this phase exists because of one."
      severity: high
    - statement: "Do NOT write a bare zero-count gate over an unfiltered source file. Comments count, and this plan's own KDoc names the very symbols the scans look for. Strip comment lines before matching, scope each match to the property-declaration block it belongs to, and assert positives alongside every negative."
      severity: high
    - statement: "Do NOT apply `logicalLineHeaderRule` (or the boundary fragments it is built from) to `hostHeaderRegex`. Two measured reasons, both recorded: `anonymizeHost` writes into a de-anonymisation map that `RedactionHostMapBoundTest` exists to bound, and firing it on every raw message in every `site_map` result is an unmeasured load change; and `SiteMapEntry.url` (`Serialization.kt:79`) carries the same host VERBATIM with no `maybeAnonymizeUrl` in front, so anonymising only the header would produce a control that looks closed and is not."
      severity: high
    - statement: "Do NOT state the emission-site inventory as complete coverage of every path that can reach an AI backend, and do NOT let the added registration-path assertion be read as upgrading it to one. The emission scans cover two named files for one measured call shape; the registration scan covers one call name across the whole main source but only for the delegation shape as written. Both bounds belong in the test's own file header, stated separately, in the same place `CookieHeaderRuleOwnershipTest` states its five-spelling bound."
      severity: high
    - statement: "Do NOT edit `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt`. Its zero-line diff is still the proof that multi-line behaviour is unchanged, and `authHeaderRegex` has its own fixtures there."
      severity: high
    - statement: "Do NOT change `authHeaderRegex`'s NAME alternation. This plan changes the boundary it recognises, not which headers it claims. The vendor-header gap recorded in `CONCERNS.md` stays open and stays out of scope."
      severity: medium
---

<objective>
Turn plan 27-04's fix from a control that happens to be in the right place into one that cannot be
bypassed unnoticed: pin the emission-site inventory by measurement, assert the structural reason a
rule-level fix reaches every site across all three registration paths, pin which rules the new
boundary composer is applied to, and
close the one further residual on this path that measurement shows is credential-bearing.

Purpose: this phase exists because a control was verified at the site it was written for and never
compared against its sibling. The gate must make that class of miss fail a test instead of a review.

Output: two source-state tripwires with stated bounds, `authHeaderRegex` rebuilt through the same
proven composer with its inheritance verified rather than assumed, and quoted measurements — probe
source included — for the residual plan 27-06 records.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-04-SUMMARY.md
@src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderRuleOwnershipTest.kt
@src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTool.kt
</context>

<measured_facts>
Measured before this plan was written, on this checkout. Re-measure anything you rely on.

Emission-site inventory — `grep -rhE "encodeToString\(it\.(toSerializableForm|toSiteMapEntry)\(" src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/ --include=*.kt | wc -l` returns `14`, split `7` / `7` across `McpToolExecutorImpl.kt` and `McpToolLegacy.kt` and `0` across the other 20 files in that package.

| Tool name | Carrier | `McpToolExecutorImpl.kt` | `McpToolLegacy.kt` | Raw HTTP? |
|---|---|---|---|---|
| `scanner_issues` | `IssueDetails.requestResponses` | 608 | 475 | yes |
| `proxy_http_history` | `HttpRequestResponse` | 740 | 622 | yes |
| `proxy_http_history_regex` | `HttpRequestResponse` | 760 | 639 | yes |
| `proxy_ws_history` | `WebSocketMessage` | 836 | 713 | no |
| `proxy_ws_history_regex` | `WebSocketMessage` | 855 | 729 | no |
| `site_map` | `SiteMapEntry` | 873 | 744 | yes |
| `site_map_regex` | `SiteMapEntry` | 896 | 764 | yes |

7 tool names, each registered once per executor file. 5 raw-HTTP names → 10 sites; 2 WebSocket names
→ 4 sites. The verifier cited 12 sites and the UAT note cited 10; both were partial. Measure it
yourself before pinning, and pin what you measure.

Choke point — `mcpPaginatedTool` delegates to `mcpTool<I>` (`McpTool.kt:82`), whose handler wraps in
`context.redactIfNeeded(execute(input))` (`McpTool.kt:45`); the no-input overload wraps at `:78`; the
executor path wraps at `McpToolExecutorImpl.kt:1037`.

Registration paths — `grep -rn 'addTool(' src/main/kotlin --include=*.kt` returns exactly THREE hits:
`McpTool.kt:34`, `McpTool.kt:72` and `McpToolHandlers.kt:122`. The third is
`Server.registerToolHandler`, and its handler wraps NOTHING of its own: it calls
`McpToolExecutor.executeToolResult(descriptor.id, argsJson, context)` (`McpToolHandlers.kt:129`) and
is redacted only because that call reaches `context.redactIfNeeded(output)` at
`McpToolExecutorImpl.kt:1037`. A version of the choke-point claim that names only `mcpTool` /
`mcpPaginatedTool` and `redactIfNeeded` leaves this path unmentioned and unmeasured — which is the
same shape of omission as the emission-site inventory the verifier found partial. Re-measure it.

Residual measurement, run against `build/classes/kotlin/main` on JDK 21 with the shipped classes,
BEFORE plan 27-04's fix, on the serialized shape, salt `probe-salt`, `recordMapping=false`:

```
STRICT COOKIE SURVIVES        BALANCED COOKIE SURVIVES
STRICT SETCOOKIE SURVIVES     BALANCED SETCOOKIE SURVIVES
STRICT APIKEY SURVIVES        BALANCED APIKEY SURVIVES
STRICT HOST SURVIVES          BALANCED HOST SURVIVES
```

`APIKEY` is an `X-API-Key` value shaped as a plain alphanumeric token — not bearer-, basic- or
JWT-shaped — so none of the un-anchored token rules rescues it. That is a credential-bearing value
leaving the process verbatim under the strongest privacy mode, and it is the reason task 2 exists.
`HOST` is `shop.example` reaching the payload un-anonymised under STRICT.
</measured_facts>

<decisions>
**D-27-12 — `authHeaderRegex` goes through the same composer, on measured evidence rather than by analogy.**
`reversibility: reversible`. The probe above shows a credential-bearing auth header value surviving
both redacting modes on this shape. The mitigation is the identical mechanism plan 27-04 already
proved and gated, applied to an EXACT-NAME alternation whose over-redaction blast radius is smaller
than the cookie rules' name-contains predicate. A `high` Information Disclosure with a one-line,
already-tested mitigation has no legitimate reason to stay open. This is not a scope reduction and
not a requirement change: `authHeaderRegex`'s name alternation is untouched and the vendor-header gap
recorded in `CONCERNS.md` stays open and out of scope.

**D-27-13 — `hostHeaderRegex` stays excluded, and the reason is measured, not aesthetic.**
`reversibility: reversible`. Two facts: `anonymizeHost` records into a de-anonymisation map bounded by
`RedactionHostMapBoundTest`, and firing it on every raw message of every `site_map` / `proxy_http_history`
result is an unmeasured load change on that bound; and `SiteMapEntry.url` (`Serialization.kt:79`)
carries the same host VERBATIM with no `maybeAnonymizeUrl` in front, so anonymising the header alone
produces a JSON object whose `request` field is anonymised and whose `url` field is not — a control
that reads as closed and is not. Recorded as AR-27-04 by plan 27-06 with the probe output quoted.

**D-27-14 — The inventory is a pinned count, not a prose claim.** `reversibility: reversible`. The
phase's failure mode was a set of paths nobody enumerated. A pinned count converts "did we get them
all?" from a review question into a test result. Its bound — two named files, one measured call
shape — is stated in the test's own file header, exactly as `CookieHeaderRuleOwnershipTest` states its
five-spelling bound.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Pin the emission-site inventory and assert the single redacting choke point</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionSiteInventoryTest.kt</files>
  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderRuleOwnershipTest.kt` — in full. It is the pattern to follow: repository-state scan, a source-line floor, a positive fixture per class scanned, `mainSourceRoot()` throwing `AssertionError` rather than skipping, and a bound stated in the class KDoc.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpTool.kt` lines 25-95 — `mcpTool`, its `Unit` overload, the no-input overload and `mcpPaginatedTool`'s delegation, i.e. where `redactIfNeeded` actually wraps.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt` lines 600-900 and 1030-1045 — the seven emission sites and the dispatch-level `redactIfNeeded`.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolLegacy.kt` lines 465-770 — the seven sibling sites and their `toolName` literals.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHandlers.kt` lines 113-135 — `registerToolHandler`, the THIRD registration path. It calls `addTool` directly with no `redactIfNeeded` wrapper of its own; it is safe only because its handler delegates to `McpToolExecutor.executeToolResult`. Measured: `grep -rn 'addTool(' src/main/kotlin` returns exactly three hits — `McpTool.kt:34`, `McpTool.kt:72`, `McpToolHandlers.kt:122`. Re-measure before pinning.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/schema/Serialization.kt` lines 42-83 — which carrier each site uses.
  </read_first>
  <action>
Create `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionSiteInventoryTest.kt`.

Re-measure the inventory first with your own scan and reconcile it against the table in
`<measured_facts>`. If your count differs, YOUR measurement wins — pin what you measured and say in
the SUMMARY what differed and why. Do not pin the table.

Declare the measured inventory as named constants so a drift reads as a data change rather than a
logic change: total site count, raw-HTTP site count, WebSocket site count, the set of raw-HTTP tool
names, the set of WebSocket tool names, and the two executor file names.

Tests:
`theMeasuredEmissionSiteCountIsPinned` — scan both executor files for the measured call shape and
assert the total and the per-file split against the constants.
`everyEmissionToolNameAppearsInBothExecutors` — assert each tool name in both sets is registered in
both files, so a site that exists in one executor and not the other is visible.
`everyEmissionSiteIsDownstreamOfTheSingleRedactingChokePoint` — assert the structural claim: the
legacy file's emission sites all sit inside `mcpTool` / `mcpPaginatedTool` registrations, and the
executor file's tool output is wrapped by a `context.redactIfNeeded(` call. State in the assertion
message that this is why a rule-level change in `Redaction` reaches every site.
`everyToolRegistrationPathReachesTheChokePoint` — the THIRD path, which the criterion above does not
see. Scan ALL of `src/main/kotlin` for `addTool(` and assert the hit set is exactly the three measured
sites; assert that each `McpTool.kt` handler wraps in `context.redactIfNeeded(`; and assert that
`McpToolHandlers.registerToolHandler`'s handler body contains `McpToolExecutor.executeToolResult(`,
naming in the assertion message that this path carries NO wrapper of its own and is safe only by that
delegation. Without this, a future `McpToolHandlers` handler that serializes and emits directly is
invisible to the whole tripwire — which is the exact shape of miss this phase exists to close.
`theInventoryScanIsNonVacuous` — resolve the source root and throw `AssertionError` if it cannot be
resolved (fail, never skip); enforce a source-line floor on both executor files; and assert the scan
finds at least one positive for each of the two emission function names and at least one `addTool(`
hit, so an inventory of zero cannot pass as agreement.

Strip comment lines before matching in every scan. This file's own KDoc will name the symbols the
scans look for, and an unfiltered match would count that prose as evidence.

Write the class KDoc to state the bound where a reader meets it: the emission-site scans cover TWO
NAMED FILES for ONE MEASURED CALL SHAPE, and an emission site added in a third file or written in a
different shape is invisible to them. State the registration-path bound separately and honestly,
because it is a different bound: that scan covers ALL of `src/main/kotlin` for `addTool(`, but it
proves reachability of the choke point only for the delegation shape as written — a
`McpToolHandlers` handler that kept the `executeToolResult` call AND emitted its own serialized
payload beside it would still pass. It is a tripwire over a measured inventory, not a proof of
coverage. This is the same discipline `CookieHeaderRuleOwnershipTest` uses when it states its
five-spelling bound.
  </action>
  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.mcp.tools.SerializedEmissionSiteInventoryTest' --rerun` passes with 5 tests, 0 skipped, 0 failures.
    - The pinned total equals the value the executor measured in-task, and that measured command and its output are quoted in the SUMMARY.
    - The `addTool(` hit set measured in-task is quoted in the SUMMARY with file and line for each hit, and matches what `everyToolRegistrationPathReachesTheChokePoint` pins. If it is not exactly three, YOUR measurement wins — pin what you measured and say what differed.
    - Red probe, recorded: temporarily lower the pinned total by one, re-run, observe the failure, restore. A count gate that cannot fail is decoration.
    - Red probe, recorded: temporarily remove one tool name from the raw-HTTP set, re-run `everyEmissionToolNameAppearsInBothExecutors`, observe the failure, restore.
    - Red probe, recorded: temporarily drop `McpToolHandlers.kt` from the pinned registration-path set, re-run `everyToolRegistrationPathReachesTheChokePoint`, observe the failure, restore.
    - Every scan in the file filters comment lines before matching — verifiable by reading each scan helper; there is no match performed against raw file text.
    - The class KDoc states BOTH bounds separately: the two-file / one-call-shape bound on the emission-site scans, and the weaker "delegation shape as written" bound on the registration-path scan.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.mcp.tools.SerializedEmissionSiteInventoryTest' --rerun</automated>
  </verify>
  <done>The emission-site inventory is pinned to a measured count with a per-file split and a per-tool-name presence check; the choke-point claim is asserted from source rather than argued, across all three `addTool` registration paths including `McpToolHandlers.kt:122`'s delegation; all three red probes are recorded; the tripwire states both of its bounds.</done>
  <reversibility rating="reversible">New test file only.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A credential-bearing auth header value must not survive the serialized shape either</name>
  <files>src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt, src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionRedactionTest.kt</files>
  <read_first>
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` lines 60-115 — `authHeaderRegex`'s exact-name alternation and its `:\s*.+$` tail, and the fragments plus the `logicalLineHeaderRule` composer plan 27-04 added.
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` lines 1800-1815 — `apply`'s `redactTokens` branch, including the name-preserving `authHeaderRegex` lambda and the un-anchored `bearerRegex` / `basicAuthRegex` / `jwtRegex` that follow it.
    - `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionRedactionTest.kt` as plan 27-04 left it — the fixture builders, the sentinel-selection discipline and the JSON-parse gate to reuse.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-04-SUMMARY.md` — the hazards found while shaping the value terminator, so they are not rediscovered.
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt` — the existing `authHeaderRegex` fixtures. Read only; do not modify.
  </read_first>
  <behavior>
    Start RED against the probe measurement already recorded in `<measured_facts>`.

    - Test 1: an `X-API-Key` value shaped as a plain alphanumeric token — deliberately not bearer-, basic- or JWT-shaped, so no un-anchored rule can rescue it — carrying a distinct sentinel, does not survive the serialized shape under STRICT. Fails before the change.
    - Test 2: the same under BALANCED.
    - Test 3: an `Authorization` value that IS bearer-shaped is still redacted, and the header NAME survives in both cases — the name-preservation invariant `authHeaderRegex`'s lambda already holds on multi-line input.
    - Test 4: the redacted payload still parses via `toolJson.parseToJsonElement` with an unchanged key set — the same contract gate, because the same greedy-tail hazard applies to this rule.
    - Test 5: `PrivacyMode.OFF` leaves the auth-bearing payload byte-identical.
    - Test 6 (the inherited blocker): an auth header whose value ENDS IN A BACKSLASH and is the last
      content in the JSON string. `authHeaderRegex` inherits `JSON_ESCAPED_HEADER_VALUE` verbatim, so
      it inherits the shape that a "quote not preceded by a backslash" terminator would have broken.
      Assert the output PARSES as JSON. Measured on the composed shape before this plan was revised:
      the escape-pair tail strips it and the JSON stays valid; the rejected lookbehind form destroyed
      it. The inheritance is exactly why this test is required rather than assumed.
    - Test 7 (multi-line byte-identity, the other half of the inheritance): a REAL multi-line
      `X-API-Key` value containing an unescaped `"` is still stripped whole, character-identical to
      the shipped output. `RedactionTest`'s auth fixtures do not cover a quote in the value, so this
      is the assertion that proves the two-branch shape carried across to this rule too.
  </behavior>
  <action>
Add a nested class to `SerializedEmissionRedactionTest.kt` holding the seven assertions, named
`credentialBearingAuthHeaderValueDoesNotSurviveTheSerializedShapeUnderStrict`,
`credentialBearingAuthHeaderValueDoesNotSurviveTheSerializedShapeUnderBalanced`,
`bearerShapedAuthorizationIsStillRedactedAndTheHeaderNameSurvives`,
`redactedAuthBearingSerializedOutputStillParsesAsJsonWithTheSameKeySet`,
`offModeLeavesTheAuthBearingSerializedShapeByteIdentical`,
`anAuthHeaderValueEndingInABackslashAtTheEndOfThePayloadStillParsesAsJson` and
`aRealMultiLineAuthHeaderValueContainingAQuoteIsStillStrippedWhole`. Run them and record the RED
output. Add every new sentinel to the file's `ALL_SENTINELS` list so
`everySentinelInThisFileIsDistinct` (plan 27-04) covers them too — the distinctness gate must not
stop measuring the file the moment this plan grows it.

Then rebuild `authHeaderRegex` in `Redaction.kt` as
`logicalLineHeaderRule(<its existing exact-name alternation>)`, exactly as plan 27-04 rebuilt the
cookie rules. Change nothing about which names it matches.

This rule inherits plan 27-04's `JSON_ESCAPED_HEADER_VALUE` verbatim, which means it inherits BOTH
halves of the blocker that plan's revision fixed: the escape-pair tail that keeps a value ending in a
backslash from running to end-of-input and destroying the JSON, and the two-branch shape that keeps
the quote terminator off the real-multi-line branch so a quoted value is not truncated mid-redaction.
Do NOT re-derive a terminator here, and do NOT reach for a negative lookbehind on the quote —
measured, that shape fails on a value ending in one backslash and on one ending in two. Tests 6 and 7
exist to prove the inheritance rather than assume it; if either fails, the fragment is wrong for both
rules and the fix belongs in the fragment, not here.

Extend the comment above `authHeaderRegex` to record, at the width the evidence supports: the measured
before-state (a plain-token `X-API-Key` value surviving both redacting modes on the serialized shape,
un-rescued because its value is not bearer-, basic- or JWT-shaped); that only the boundary changed,
not the name alternation; that the boundary is the shared composer, so this rule carries plan 27-04's
stated over-match bound (D-27-15) as well as its fix; and that the open-ended vendor-header gap this
rule has always had is unchanged and still recorded in `CONCERNS.md`. Do not describe token redaction
as complete.

Re-run the residual probe from `<measured_facts>` against the freshly compiled classes and quote the
after-state in the SUMMARY beside the before-state, so the change is evidenced by the same instrument
that found it.
  </action>
  <acceptance_criteria>
    - The RED output for tests 1 and 2 is recorded in the SUMMARY before the `Redaction.kt` edit.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.mcp.tools.SerializedEmissionRedactionTest' --rerun` passes with at least 25 tests, 0 skipped, 0 failures, and `everySentinelInThisFileIsDistinct` is among them (proving the distinctness gate still covers the file after this plan added sentinels to it).
    - `git diff HEAD -- src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` shows no change to the header-name alternation inside `authHeaderRegex`: the 16 header names present before are all present after.
    - The inherited-blocker tests are green and named in the SUMMARY: `anAuthHeaderValueEndingInABackslashAtTheEndOfThePayloadStillParsesAsJson` and `aRealMultiLineAuthHeaderValueContainingAQuoteIsStillStrippedWhole`. State explicitly in the SUMMARY that the fragment fix carried to `authHeaderRegex` and that it was verified rather than assumed.
    - `git diff --stat HEAD -- src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt` produces no output, and `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.*' --rerun` passes — including the 4 MB newline-free fixture and the auth-header fixtures.
    - The probe is re-run after `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew compileKotlin --rerun` and its output shows `STRIPPED` / `REDACTED` for the cookie, set-cookie and api-key sentinels in both redacting modes. Both before- and after-outputs are quoted in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.mcp.tools.SerializedEmissionRedactionTest' --tests 'com.six2dez.burp.aiagent.redact.*' --rerun</automated>
  </verify>
  <done>A plain-token credential in an auth header does not survive the serialized shape in either redacting mode; the header name alternation is unchanged; the JSON contract and the OFF-mode byte-identity hold; the probe re-run confirms it with the same instrument that found the leak.</done>
  <reversibility rating="reversible">One regex recomposition using fragments already proven and gated in plan 27-04, plus tests.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Pin which rules carry the new boundary, and measure the residual that deliberately does not</name>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt</files>
  <read_first>
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` — the final state of the three fragments, the `logicalLineHeaderRule` composer, and all four boundary-relevant rules (`cookieHeaderRegex`, `setCookieHeaderRegex`, `authHeaderRegex`, `hostHeaderRegex` at `:1675`).
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/schema/Serialization.kt` lines 76-83 — `toSiteMapEntry`'s `url = req?.url()`, the sibling field that carries the host verbatim.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt` — `maybeAnonymizeUrl`, and the fact that no emission site calls it.
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionHostMapBoundTest.kt` — the bound on the de-anonymisation map that D-27-13 rests on.
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderRuleOwnershipTest.kt` — again, for the non-vacuity and stated-bound pattern.
  </read_first>
  <action>
Create `src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt`, a
source-state tripwire over `Redaction.kt`.

Isolate each rule's property-declaration block by name rather than matching the file as a whole, and
strip comment lines before matching — the KDoc in that file names every symbol involved, so an
unfiltered match would read prose as composition.

Tests:
`theBoundaryFragmentsAreComposedIntoTheMeasuredRuleSetAndNoOther` — assert that the
`logicalLineHeaderRule(` composer appears in the declaration blocks of `cookieHeaderRegex`,
`setCookieHeaderRegex` and `authHeaderRegex`, and appears zero times in the declaration block of
`hostHeaderRegex`. Assert the positives alongside the negative, so the test cannot pass because the
isolation helper returned nothing. Because plan 27-04 routes all three rules through ONE composer, a
single token carries the whole claim — a rule that stopped using it, or a fourth rule that started,
both read as a data change in this test rather than as a silent regex edit.
`theScopeScanIsNonVacuous` — fail (never skip) if the source file cannot be resolved; enforce a
source-line floor; assert the composer declaration, all three fragment declarations
(`JSON_ESCAPED_NEWLINE`, `REAL_LINE_HEADER_VALUE`, `JSON_ESCAPED_HEADER_VALUE`) and all four rule
declarations were found.
`theStatedBoundIsPresentWhereAReaderMeetsIt` — assert the KDoc region above the boundary fragments
names the rule deliberately excluded, so the code and the record cannot drift apart silently.

Class KDoc: state that this is a tripwire over four named rules in one named file. A fifth rule with
the same blind spot, or a rule in another file, is invisible to it.

Then measure the excluded residual and record it for plan 27-06. Reuse the probe technique from
`<measured_facts>`: after `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew compileKotlin --rerun`,
run a single-file Java program against `build/classes/kotlin/main` plus the Kotlin stdlib jar resolved
from the Gradle cache, calling `Redaction.INSTANCE.apply(payload, RedactionPolicy.Companion.fromMode(mode), salt, false)`
on a serialized fixture that carries a `Host:` header value AND the same host in a `url` field. Print
one verdict line per artifact per mode. Keep the probe OUT of the repository — it is a measurement,
not a committed test, precisely because a committed green assertion that a host survives STRICT is
the artifact this phase exists to stop producing.

Record in the SUMMARY, verbatim: the probe's FULL SOURCE (the single-file program, in a fenced block),
the exact command, and its output for STRICT and BALANCED, covering the `Host:` header value, the
`url` field value, and a cookie sentinel as a positive control proving the probe ran against the fixed
classes rather than stale ones.

The source goes in the SUMMARY verbatim on purpose (W-6, adopted). Keeping the probe OUT of `src/`
stops a green assertion that a host survives STRICT from ever existing; recording its source in the
planning record costs nothing, adds no test, and is what lets a future reader RE-RUN the measurement
that AR-27-04 rests on instead of taking one line of quoted output on trust. A measurement nobody can
reproduce is an assertion.
  </action>
  <acceptance_criteria>
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.LogicalLineBoundaryScopeTest' --rerun` passes with 3 tests, 0 skipped, 0 failures.
    - Red probe, recorded: temporarily route `hostHeaderRegex` through `logicalLineHeaderRule`, re-run, observe `theBoundaryFragmentsAreComposedIntoTheMeasuredRuleSetAndNoOther` fail, revert.
    - Red probe, recorded: temporarily hand-inline `authHeaderRegex` so it no longer uses the composer, re-run, observe the same test fail on the positive side, revert.
    - The probe SOURCE is quoted verbatim in the SUMMARY alongside its command, and its output contains a verdict line for each of: `Host:` header value, `url` field value, cookie sentinel — under STRICT and under BALANCED. The cookie positive control reads as stripped; if it does not, the probe ran against stale classes and the measurement is void.
    - The probe is not committed: `git status --porcelain src/` lists only the files named in this plan's `files_modified`, and no path containing `Probe` appears anywhere under `src/`.
    - Full gate: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck` succeeds. On a `RedactionTest` `SafeRegex` wall-clock failure, re-run once and record both runs.
  </acceptance_criteria>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck</automated>
  </verify>
  <done>The set of rules carrying the new boundary is pinned by a non-vacuous source-state tripwire with a stated bound and two recorded red probes; the excluded host residual is measured, and its probe SOURCE and output are both quoted so plan 27-06's AR-27-04 rests on a reproducible measurement; nothing green in the repository asserts a leak.</done>
  <reversibility rating="reversible">New test file plus an uncommitted measurement probe.</reversibility>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Burp captured traffic → MCP tool result | Raw HTTP messages carrying credentials and hostnames cross into a serialized payload |
| MCP tool result → configured AI backend / external MCP agent | The payload leaves the process |
| Source tree → security record | What the code composes and what `26-SECURITY.md` claims must stay in agreement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-05-01 | Information Disclosure | a future emission site added outside the fixed rule's reach | high | mitigate | `SerializedEmissionSiteInventoryTest` pins the measured count, the per-file split and the per-tool-name presence, and asserts every site is downstream of `redactIfNeeded`. A drift fails a test rather than a review. It also pins all three `addTool` registration paths, including `McpToolHandlers.kt:122`, which carries no wrapper of its own and is safe only by delegating to `executeToolResult`. Two bounds stated separately in the class KDoc: two named files / one measured call shape for the emission scans, and the weaker delegation-shape-as-written bound for the registration-path scan. |
| T-27-05-02 | Information Disclosure | `authHeaderRegex` on the serialized shape | high | mitigate | Measured: a plain-token `X-API-Key` value survives STRICT and BALANCED, un-rescued by the un-anchored bearer/basic/JWT rules. Rebuilt through the same `logicalLineHeaderRule` composer; gated by seven new assertions including the JSON-parse and OFF-mode controls. Two of them exist because this rule INHERITS plan 27-04's tail: a value ending in a backslash at the end of the payload must still parse as JSON, and a real multi-line value containing a quote must still be stripped whole. Inheritance is verified, not assumed. Re-measured with the same probe after the change. |
| T-27-05-03 | Tampering | the redacted JSON tool result, auth branch | high | mitigate | The same greedy-tail hazard applies to `authHeaderRegex`. `redactedAuthBearingSerializedOutputStillParsesAsJsonWithTheSameKeySet` gates it. |
| T-27-05-04 | Repudiation | a source-state tripwire that agrees with itself | high | mitigate | Every scan strips comments before matching, isolates each rule's declaration block, asserts positives alongside negatives, enforces a source-line floor, and fails rather than skips when the repository cannot be resolved. Three red probes are recorded for the inventory tripwire and two for the scope tripwire, so a decorative gate is visible as one. |
| T-27-05-05 | Information Disclosure | `hostHeaderRegex` and `SiteMapEntry.url` under STRICT on the serialized shape | medium | accept | D-27-13. Measured and quoted, deliberately not fixed here: `anonymizeHost` writes into a bounded de-anonymisation map whose load under per-message anonymisation is unmeasured, and the sibling `url` field carries the same host verbatim, so a header-only fix reads as closed and is not. Carried to plan 27-06 as open finding AR-27-04 for maintainer disposition. Below the blocking threshold; not silently dropped. |
| T-27-05-06 | Repudiation | a green test pinning the host residual | high | mitigate | Prohibition-enforced: the residual is measured with an uncommitted probe and recorded in prose. An acceptance criterion asserts nothing under `src/` claims a host, url or cookie value survives a redacting mode. |
| T-27-05-07 | Denial of Service | `authHeaderRegex` in the unbounded header stage | medium | mitigate | Same escape-pair tail with terminator lookahead measured in plan 27-04; its two alternatives are disjoint on their first character, so no nested quantifier and no backtracking surface is introduced. Plan 27-04's measurement (17 ms → 220 ms per 4 MB single-line payload, linear in size, flat across escape density) already covers all three header rules including this one, so no separate measurement is required here. CORRECTED IN REVISION: `newlineFreeOversizeBodyIsScannedNotDestroyed` is NOT an acceptance gate for this — it drives `Redaction.testWindowedBodyStage`, which bypasses the header stage. It is carried as a regression check only. |
| T-27-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package-manager install task; no dependency added or changed. |
</threat_model>

<artifacts_this_phase_produces>
**Created — `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionSiteInventoryTest.kt`**
- constants: `EXPECTED_EMISSION_SITES`, `EXPECTED_RAW_HTTP_EMISSION_SITES`, `EXPECTED_WEBSOCKET_EMISSION_SITES`, `RAW_HTTP_EMISSION_TOOL_NAMES`, `WEBSOCKET_EMISSION_TOOL_NAMES`, `EMISSION_EXECUTOR_FILES`
- `theMeasuredEmissionSiteCountIsPinned`
- `everyEmissionToolNameAppearsInBothExecutors`
- `everyEmissionSiteIsDownstreamOfTheSingleRedactingChokePoint`
- `everyToolRegistrationPathReachesTheChokePoint`
- `theInventoryScanIsNonVacuous`

**Created — `src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt`**
- `theBoundaryFragmentsAreComposedIntoTheMeasuredRuleSetAndNoOther`
- `theScopeScanIsNonVacuous`
- `theStatedBoundIsPresentWhereAReaderMeetsIt`

**Changed — `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt`**
- `authHeaderRegex` rebuilt as `logicalLineHeaderRule(<its unchanged name alternation>)`, with the measured before-state recorded in its comment

**Changed — `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionRedactionTest.kt`**
- `credentialBearingAuthHeaderValueDoesNotSurviveTheSerializedShapeUnderStrict`
- `credentialBearingAuthHeaderValueDoesNotSurviveTheSerializedShapeUnderBalanced`
- `bearerShapedAuthorizationIsStillRedactedAndTheHeaderNameSurvives`
- `redactedAuthBearingSerializedOutputStillParsesAsJsonWithTheSameKeySet`
- `offModeLeavesTheAuthBearingSerializedShapeByteIdentical`

**Not committed, by design:** the Java measurement probe for the `Host:` / `url` residual.

**Decision IDs introduced:** D-27-12, D-27-13, D-27-14
**Threat IDs introduced:** T-27-05-01 … T-27-05-07
**Finding introduced for plan 27-06 to record:** AR-27-04 (`Host:` header value and `SiteMapEntry.url` un-anonymised under STRICT on the serialized emission shape)
</artifacts_this_phase_produces>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test detekt ktlintCheck` — full gate.
- Four recorded red probes (two per tripwire), each with the observed failure quoted.
- The residual probe's before- and after-outputs quoted in the SUMMARY, with the cookie positive control confirming the classes were fresh.
- `git diff --stat HEAD -- src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt` — empty.
- Use the `Edit` tool for every source change and `--rerun` on every gradle invocation: wave 2 of this phase measured a stale `compileTestKotlin` replaying `UP-TO-DATE` results after in-place edits and masking a real defect.
</verification>

<success_criteria>
- The emission-site inventory is pinned to a measured count with per-file and per-tool-name granularity, and all three red probes prove the gate can fail.
- The choke-point claim is asserted from source across ALL THREE `addTool` registration paths, including `McpToolHandlers.kt:122`'s delegation to `executeToolResult`, so a future site that bypasses the redacting wrapper — or a handler that stops delegating — is a test failure.
- A credential-bearing auth header value no longer survives the serialized shape in either redacting mode, and the same instrument that found the leak confirms the fix.
- `authHeaderRegex`'s inheritance of plan 27-04's tail is VERIFIED, not assumed: a value ending in a backslash still parses as JSON, and a real multi-line value containing a quote is still stripped whole.
- The set of rules carrying the new boundary is pinned, non-vacuously, with both of its bounds stated where a reader meets them.
- The deliberately excluded host residual is measured, and its probe source and output are both recorded, so plan 27-06's AR-27-04 rests on a measurement a later reader can re-run — recorded, not pinned, not ignored.
</success_criteria>

<output>
Create `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-05-SUMMARY.md` when done.
</output>
