---
phase: 27-priv-05-gap-closure-sanitize-headers
plan: 13
type: execute
gap_closure: true
wave: 13
depends_on: ["27-10", "27-11", "27-12"]
autonomous: true
requirements: [PRIV-05]
files_modified:
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
  - .planning/codebase/CONCERNS.md
  - .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md
  - .planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "T-26-02-01 carries a SIXTH clause recording the FOURTH refutation — the ADMITTER-POLARITY axis and the JSON-string-open axis — and clauses (1) through (5) survive as an exact character prefix of the amended row. The threat has now been closed wrongly four times, and that fact, preserved in order, is the most instructive artifact in the register."
    - "The two residuals this round OPENS are numbered, severity-assigned, owned, and evidenced from a MEASUREMENT taken this round rather than from a prediction: AR-27-09 (the leading-whitespace / obs-fold start boundary) and AR-27-10 (the RFC 9110 tchars outside the widened cookie name class). Neither is left in a source comment only — the underscore class lived exactly there for three rounds and appeared in no security record at all."
    - "`threats_open` is RECOMPUTED from the register rows by the documented command, never hand-edited, and its POPULATION is restated. If any finding this round opens sits at or above the blocking severity and OUTSIDE that population, either it is given an id inside the population or the counter's definition is amended — and which was done is stated. A counter reading 0 beside an open high finding is the overclaim this file exists to stop."
    - "Two new standing-rule clauses are added, one per lesson this round taught that the previous four could not. Clause (v): a width claim about a shared predicate must state EACH CONSUMER'S POLARITY, because wider is fail-safe for a redactor and fail-OPEN for an admitter. Clause (vi): a green test is not a measurement, and a green test asserting a sensitive value SURVIVES a redacting policy is a defect and not coverage — with the machine check that now enforces it, and that check's stated bound, named in the same clause."
    - "The provenance of this round's two decisions is recorded and is DIFFERENT from AR-27-04's. Both were made by the MAINTAINER before planning, not auto-selected by `mode: yolo`. That contrast is itself the record: a reader comparing the two must be able to tell a human decision from a harness default."
    - "AR-27-04 is NOT relitigated. It stays OPEN, at its recorded severity, with its honestly-disclosed auto-selected provenance intact, and it still owes a human decision. The only amendment is an APPENDED note that the two green STRICT assertions pinning it were removed and the pass-through re-pointed at an OFF fixture."
    - "PHASE 27 STILL CLOSES WITH PRIV-05 NOT SATISFIED, and ROADMAP.md still says so in the phase record. AR-27-08 and InjectionPointExtractor.kt:29 remain owned by Phase 28 and are untouched by this round. REQUIREMENTS.md is UNTOUCHED and PRIV-05 stays `[ ]` — a phase that grades its own homework produces the record these four rounds have been repairing."
    - "BOUND, carried with the claim. This round closed TWO axes: the character class of a cookie header NAME, and the JSON-string-open logical-line start. THE NEXT BLIND AXES, NAMED: AR-27-09, AR-27-10, AR-27-04, AR-27-08, the CONCERNS.md vendor auth-header class, and the sweep's own vocabulary bound. Six named residuals is not a completeness claim and no sentence in any record may imply it is."
  artifacts:
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md — T-26-02-01 clause (6), AR-27-09 and AR-27-10 defined with quoted measured evidence, the AR-27-04 appended note, audit-trail row 6, standing-rule clauses (v) and (vi), recomputed threats_open with its population restated, appended sign-off items"
    - ".planning/codebase/CONCERNS.md — the fourth dated W-A amendment, scoped"
    - ".planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md — items 1-8 carried forward legible as unanswered, the two maintainer decisions recorded WITH their provenance, and the new dispositions for AR-27-09 and AR-27-10"
    - ".planning/WINDOWS.md — one entry per premise this round falsified, each carrying its observed number"
    - ".planning/ROADMAP.md — the round-4 note, the four new plan entries under waves 10-13, the six named residuals with their owners, and the unchanged not-satisfied sentence"
    - ".planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md — extended to plans 27-10 through 27-13 on the same reasoned declaration"
    - ".planning/STATE.md — position and activity"
  key_links:
    - "27-10-SUMMARY NOT_COVERED_TCHARS -> AR-27-10. The link whose absence let the underscore class live three rounds in a source comment and no register."
    - "27-11-SUMMARY's quoted indented-header measurement -> AR-27-09. Filed from a measurement, not from task 1's prediction."
    - "27-12-SUMMARY's pre-round detector run -> standing rule clause (vi). The clause cites a demonstrated gate, not an intended one."
    - "the two maintainer decisions -> 27-HUMAN-UAT.md provenance note -> the contrast with AR-27-04's yolo auto-select. A later reader must be able to tell them apart."
    - "AR-27-09 / AR-27-10 severity -> the register population -> threats_open. The link where a wrong population would hide a high finding beneath a counter reading 0."
---

<objective>
Repair the records for the FOURTH refutation of PRIV-05, at the width the evidence supports and no
wider, and record the two lessons the previous four clauses could not have carried.

T-26-02-01 has now been closed wrongly four times: on header NAMES, on emission PATHS, on field TYPES,
and now on CONSUMER POLARITY. The standing rule gained a clause after each of the first three. Rounds 1
to 3 all failed the same structural way — a rendering-keyed mechanism blind to the next rendering — and
clause (iv) named that. Round 4 failed differently, twice: a control's width was judged without asking
what its consumers DO with a true result, and a green test was allowed to stand in for a measurement in
three separate places.

Purpose: leave a record that is instructive rather than merely accurate. A threat wrongly closed four
times, with each pattern named and each residual owned, is worth more to a future auditor than a fifth
clean-looking closure.

Output: an amended register with two new findings and two new standing-rule clauses, a recomputed
counter with its population restated, a fourth CONCERNS amendment, carried-forward human items with
this round's decision provenance recorded, a broken-windows entry per falsified premise, and a ROADMAP
that still says PRIV-05 is not satisfied.

**This plan writes no code and closes no requirement.** `REQUIREMENTS.md` is under a standing
prohibition and is not touched; PRIV-05 stays `[ ]`. `AR-27-08` and `InjectionPointExtractor.kt:29`
belong to Phase 28 and are not re-decided here. `AR-27-04`'s disposition is not relitigated. The
`T-27-06-06` README and SPEC host-anonymisation overclaim stays deliberately unactioned: a change to
what SHIPS is not a record repair, and both prior record plans scoped themselves out of it for that
reason.

**NO CHECKPOINT IS RAISED, DELIBERATELY** — the reason is at the head of `27-HUMAN-UAT.md`'s
carry-forward section and it has not changed: under `mode: yolo` a checkpoint is auto-selected and then
filed as though a maintainer had weighed it. Open questions go to that file, legible as open.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-3.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-REVIEW.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-10-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-11-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-12-SUMMARY.md
</context>

<record_discipline>
These four rules bind every task in this plan. They are not style; each was learned by these files.

1. **APPEND AND AMEND. NEVER REWRITE.** Every prior narrative survives verbatim. Where a claim is now
   false, it is qualified in place by an appended sentence naming the date and the plan, never edited
   into looking correct.
2. **T-26-02-01 IS ONE PHYSICAL LINE.** Appending a clause necessarily rewrites the whole row, so the
   gate is a PREFIX check: the OLD row body must remain an exact prefix of the new one. Measure in
   CHARACTERS, not bytes — the row carries multi-byte UTF-8 and `WINDOWS.md` entry 28 records a prior
   round quoting a byte count as a character count.
3. **REMOVED LINES ARE THE DIFF SIGNAL, AND THE NAIVE FORM LIES.** Use
   `git diff HEAD --unified=0 -- <file> | grep '^-' | grep -v '^--- '`; a removed line beginning with a
   hyphen renders as a double hyphen and a naive filter excludes it. `WINDOWS.md` entry 18 records
   that. Any removed line must be a deliberate, named replacement recorded in `WINDOWS.md`.
4. **COMPUTED, NOT ASSERTED.** `threats_open` is re-derived by the command written in the frontmatter
   and the written value equals that output. A hand-written counter is how the 2026-08-24 overclaim
   survived review.
</record_discipline>

<tasks>

<task type="tracer">
  <name>Task 1: The register — clause (6), two new findings, a recomputed counter, two standing-rule clauses</name>
  <precondition>Plans 27-10, 27-11 and 27-12 are merged and their SUMMARYs exist on disk. Every number this task writes is read from those SUMMARYs; writing any of them from this plan's prose instead would file a prediction as a measurement, which is the defect WINDOWS.md entries 26 and 29 already record for this phase.</precondition>
  <reversibility rating="costly">An append-only security register kept under a preservation rule. A wrong append cannot be edited out — only corrected by a further append, which itself becomes part of the record. Take every number from the SUMMARYs, not from memory.</reversibility>
  <files>.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md</files>
  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` — the whole file. In particular: the frontmatter comment block carrying the `awk` command and the STATED POPULATION paragraph; the `## Threat Register` row `T-26-02-01` with clauses (1) through (5); the `## Accepted Risks Log` rows `AR-27-01` through `AR-27-08`; `### AR-27-04 — disposition, 2026-08-24`; `## Security Audit Trail` with its five rows and the notes beneath them; `## Sign-Off`; and `### Standing rule added 2026-08-24 (Phase 27)` with clauses (i) through (iv).
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-10-SUMMARY.md` — the red-probe output, the corpus counts, and the exact contents of `NOT_COVERED_TCHARS`. `AR-27-10` is written from that list.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-11-SUMMARY.md` — the quoted indented-header before and after strings under STRICT and BALANCED. `AR-27-09` is written from those, not from `27-VERIFICATION-3.md`'s copy of them.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-12-SUMMARY.md` — the pre-round SHA, the three-hit detector output, and the sweep's stated vocabulary bound. Standing-rule clause (vi) cites those.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-3.md` — the three failed truths and the two blockers, so clause (6) records what was refuted in the verifier's terms rather than in this plan's.
  </read_first>
  <action>
Part A — clause (6) on T-26-02-01. Append a sixth clause to the existing row. It must record: that
`Redaction.isCookieHeaderName` has three consumers and that the third,
`PassiveAiScannerFilters.sanitizeHeadersForPrompt`, is an ADMITTER where a true result puts a header on
the outbound prompt; that the KDoc's fail-safe-because-wider claim was therefore measurably false for
one consumer in three; that the underscore name class was measured leaking a cookie value to a
third-party AI backend under STRICT and BALANCED and appeared in NO security record under `.planning/`
at all; the HONEST ATTRIBUTION that the leak was PRE-EXISTING — the admitter's conjunct was already a
bare contains before plan 27-01 refactored it and the name class dates from Phase 21, so phase 27
neither introduced nor widened it, and what phase 27 did was mis-frame it and pin it green; that the
JSON-string-open axis let even the CANONICAL cookie header survive STRICT with a positive control
firing on the same run; and that both were closed on 2026-08-26 by plans 27-10 and 27-11 against red
probes, with the changed symbols named.

Before writing, capture the current row body and its CHARACTER length. After writing, assert the old
body is an exact character prefix of the new one and record both lengths. Do not alter, reorder or
soften any word of clauses (1) through (5).

Part B — define `AR-27-09` and `AR-27-10` as new rows in the Accepted Risks Log, each OPEN, each with a
severity assigned FROM THE MEASUREMENT rather than from intuition, each with a named owner, each with
its evidence quoted from the corresponding SUMMARY.

`AR-27-09` is the leading-whitespace or obs-folded header line the real-line branch cannot match. Quote
the before and after strings from `27-11-SUMMARY.md`. State the reachability honestly: no measured
emission site in this repository indents header lines, which is the mitigating fact and the reason for
the severity assigned. Write down the one-token fix — allowing leading horizontal whitespace on the
real-line branch — so a successor does not re-derive it. State that it was out of scope by the
maintainer's stated scope for this round, not by oversight.

`AR-27-10` is the residual difference set between the bare-contains predicate and the widened name
class: the RFC 9110 tchars outside it, listed verbatim from `27-10-SUMMARY.md`'s `NOT_COVERED_TCHARS`.
State the mechanism in one sentence — a name carrying one of those characters is still ADMITTED by the
predicate and still not matched by either regex, which is the same fail-open shape the underscore class
had, on a different character set. State that it is enumerated in source by `CookieHeaderNameWidthTest`,
and that this row is the record the underscore class never had.

Part C — append to `AR-27-04`, do not rewrite it. One appended, dated paragraph: the two green
`assertTrue` assertions that pinned the host residual under STRICT were REMOVED on 2026-08-26 by plan
27-12 and the pass-through they measured re-pointed at a `PrivacyMode.OFF` fixture where pass-through
is correct; the finding itself is UNCHANGED, still OPEN, still at its recorded severity, and STILL OWES
A HUMAN DECISION; and its disposition's honest provenance — auto-selected by `mode: yolo`, not
maintainer-chosen — stands as written and is not upgraded by this round's activity.

Part D — audit-trail row 6. Append a row dated 2026-08-26 naming plans 27-10 through 27-13, describing
this as the FOURTH reopening of T-26-02-01 and its re-closure on the ADMITTER-POLARITY and
JSON-STRING-OPEN axes, and stating that `threats_open` was recomputed from the rows and its population
restated. Use the ACTUAL recomputed numbers from Part E, never placeholders.

Part E — recompute `threats_open`. Run the `awk` command written in the frontmatter comment block,
verbatim, AFTER every other edit in this task. Record its raw output, the number of rows scanned and
the number closed. Write that output as the value. Then re-read the STATED POPULATION paragraph and
apply it: the population is register rows beginning with the `T-26-` prefix, and every `AR-` finding is
outside it at any severity. If either finding defined in Part B carries a severity at or above the
blocking threshold, the honest options are to give it an id inside the population or to amend the
counter's definition — pick one, do it, and state in the frontmatter which was done and why. If both
are below the threshold, say so explicitly in the same place, because "below the gate" is a conclusion
a reader must be able to check rather than infer.

Part F — standing-rule clauses (v) and (vi), in the register the four existing clauses use: the lesson,
the worked example from this file's own history, and an operative instruction.

Clause (v), on CONSUMER POLARITY: verifying a shared control's WIDTH is not sufficient without stating
what each consumer DOES with a true result. Worked example: `isCookieHeaderName` was documented
fail-safe because it is wider than the regexes, which is true for its two redacting consumers and false
for its admitting one — and the audit that closed the width question never asked the polarity question.
Operative instruction: a width claim about a shared predicate must ENUMERATE ITS CONSUMERS AND LABEL
EACH ONE redactor or admitter; wider is fail-safe only for a redactor, and for an admitter wider than
the downstream rule is fail-OPEN by construction.

Clause (vi), on GREEN TESTS AS EVIDENCE: a green test is not a measurement, and a green test asserting
that a sensitive value SURVIVES a redacting policy is a defect and not coverage. Worked example: every
blocker in round 3 was green because a test asserted it, and one of them carried a failure message
instructing the next engineer not to fix it. Operative instruction: a survival may be RECORDED here
with quoted evidence, and its pass-through may be asserted under `PrivacyMode.OFF` where pass-through
is correct, but it may not be pinned green under a redacting policy. Name
`RedactingPolicySurvivalSweepTest` as the machine check that now enforces this, cite the pre-round run
from `27-12-SUMMARY.md` as the demonstration that it fires, and — in the same clause, because this file
has been burned by exactly this omission — STATE ITS VOCABULARY BOUND and name what it cannot see.

Part G — append this round's items to `## Sign-Off`, in the shape the existing post-reopening items use.
Do not edit any existing item.
  </action>
  <verify>
    <automated>awk -F'|' '/^\| T-26-/ { sev=$5; st=$(NF-1); gsub(/[ *`]/,"",sev); gsub(/[ *`]/,"",st); if (st != "closed" && (sev == "high" || sev == "critical")) c++ } END { print c+0 }' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md; grep -c '^| AR-27-' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md; grep -c 'AR-27-09' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md; grep -c 'AR-27-10' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md; git diff HEAD --unified=0 -- .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | grep '^-' | grep -v '^--- ' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The pre-edit T-26-02-01 row body is an exact CHARACTER prefix of the post-edit row body. Both character lengths are recorded in the SUMMARY, measured as characters and explicitly not as bytes.
    - `grep -c '^| AR-27-' ...` returns `10` — the eight existing rows plus `AR-27-09` and `AR-27-10`.
    - `AR-27-09`'s row quotes the before and after strings from `27-11-SUMMARY.md` verbatim, carries a severity, carries a named owner, and names the one-token fix.
    - `AR-27-10`'s row lists the uncovered tchars verbatim from `27-10-SUMMARY.md`, carries a severity, carries a named owner, and names `CookieHeaderNameWidthTest`.
    - The `AR-27-04` row grew by an appended paragraph and lost nothing: its pre-edit body is an exact character prefix of its post-edit body, and the auto-selected-provenance sentence is present unchanged.
    - The `awk` command's raw output is recorded, the value written in the frontmatter equals it, and the rows-scanned and rows-closed counts are recorded alongside it.
    - The frontmatter states explicitly whether either new finding sits at or above the blocking severity and, if so, which remedy was applied to the counter or its population.
    - The standing-rule section contains clauses (v) and (vi), each with a lesson, a worked example from this file's history, and an operative instruction. Clause (vi) names the sweep AND states its vocabulary bound.
    - The audit trail has six rows and row 6 carries the real recomputed numbers.
    - Every removed line, counted with the robust form in the verify block, is a deliberate named replacement listed in the SUMMARY. A removed line not on that list is a failure of this task.
    - The 2026-08-24 reopening narrative is untouched: `git diff HEAD -- .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | grep -c 'Reopening — 2026-08-24'` returns `0`.
  </acceptance_criteria>
  <done>The register records the fourth wrong closure with its prior narrative intact, files this round's two residuals as numbered owned findings evidenced from measurements, recomputes its counter and restates its population, and grows two clauses naming the two lessons the first four could not have carried.</done>
</task>

<task type="auto">
  <name>Task 2: The human items, the concerns file, and one broken-windows entry per falsified premise</name>
  <reversibility rating="costly">`27-HUMAN-UAT.md` is under a zero-removed-lines rule and `WINDOWS.md` is an append-only ledger with computed counters. Both are correctable only by further appends.</reversibility>
  <files>.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md, .planning/codebase/CONCERNS.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md` — the whole file. Read the CARRY-FORWARD section head, which explains why items live there instead of at a checkpoint, and the note about the deliberately stale frontmatter. That note exists because plan 27-09's zero-removed-lines criterion made editing a frontmatter value a rule violation; the same constraint binds this task.
    - `.planning/codebase/CONCERNS.md` — the `W-A` entry and all three existing amendments, so the fourth is scoped the same way and contradicts none of them.
    - `.planning/WINDOWS.md` — the frontmatter counters, the markdown table format, and the JSON block at the foot. Both representations must stay consistent, which is why entries go in through the CLI rather than by hand.
    - All three of `27-10-SUMMARY.md`, `27-11-SUMMARY.md` and `27-12-SUMMARY.md` — specifically every place a SUMMARY records that a plan's stated premise turned out false. Each of those is one `WINDOWS.md` entry, and each entry must carry the OBSERVED number rather than the predicted one.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-3.md`, the `human_verification` block — its four items are the carry-forward source.
  </read_first>
  <action>
Part A — `27-HUMAN-UAT.md`, APPEND ONLY, ZERO removed lines under the robust diff form. Add a new dated
carry-forward section for round 4 containing four things.

First, the items still unanswered: tests 1 through 8 from the existing file, plus
`27-VERIFICATION-3.md`'s carried item, restated as still open, with a sentence stating plainly that
nothing in round 4 answered them. Do not re-open the individual test bodies; reference them by number
and state their status.

Second, the two DECISIONS THIS ROUND, recorded WITH PROVENANCE and marked MAINTAINER-MADE. Decision
one: fix the underscore class by widening the name class rather than accepting it as a residual, with
the direction specified — widen the regex side, never narrow the predicate. Decision two: the
JSON-string-open boundary is in scope for this round. Both were made by the maintainer BEFORE planning
began. Write the contrast explicitly: `AR-27-04`'s disposition is recorded in `26-SECURITY.md` as
auto-selected by `mode: yolo` and NOT maintainer-chosen, and these two are the opposite — so a later
reader must be able to tell a human decision from a harness default at a glance. That distinction is
the point of recording provenance at all.

Third, two NEW disposition items: accept `AR-27-09` at the severity task 1 assigned it, or pull its
one-token fix forward; and accept `AR-27-10` at its assigned severity, or widen the cookie name class
to the full RFC 9110 tchar set. For each, state the two things to weigh — both already measured, so
each is a judgment and not an investigation.

Fourth, restate that `AR-27-04` still owes a human decision and that removing its green pin did not
supply one.

Handle the stale frontmatter exactly as the existing file does: leave the `updated:` and `source:`
values untouched, and note in the appended section that the true state is that the file was amended on
2026-08-26 by plan 27-13 drawing on the round-4 SUMMARYs. Editing a frontmatter value would remove a
line and violate the rule the file is kept under.

Part B — `CONCERNS.md`, the fourth dated `W-A` amendment. Scope it to what round 4 changed: the cookie
name class is widened, the admitting consumer's polarity is now stated at both sites a reader meets it,
and the vendor auth-header class that `W-A` owns is UNCHANGED and still open by prohibition. Do not let
the amendment read as though the vendor class moved.

Part C — `WINDOWS.md`, one entry per falsified premise, through the CLI so both representations stay
consistent. The invocation shape is
`node ~/.claude/gsd-core/bin/gsd-tools.cjs windows append --kind <kind> --phase 27 --file <path> --description "<text>"`;
`--kind`, `--phase` and `--description` are required and `--file` is optional. Read each SUMMARY for
premises the execution falsified — a predicted line number that moved, a rationale sentence written
before the measurement that contradicted it, a floor that turned out above the measured count, a
plan-time claim about a file's contents the tree refuted. Every description must carry the OBSERVED
number or string, never the predicted one; an entry recording a prediction is the defect `WINDOWS.md`
entry 26 already records for this phase. Verify `open_count` and `total_count` both increased by exactly
the number of entries appended, and record the before and after values.
  </action>
  <verify>
    <automated>git diff HEAD --unified=0 -- .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md | grep '^-' | grep -v '^--- ' | wc -l; node ~/.claude/gsd-core/bin/gsd-tools.cjs windows status; grep -c 'AR-27-09' .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md; grep -c 'AR-27-10' .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The robust removed-line count for `27-HUMAN-UAT.md` is exactly `0`.
    - The appended section carries all nine outstanding items by number with an explicit statement that round 4 answered none of them.
    - Both round-4 decisions are recorded as MAINTAINER-MADE, with the explicit contrast against `AR-27-04`'s auto-selected provenance written out rather than implied.
    - `grep -c 'AR-27-09'` and `grep -c 'AR-27-10'` over `27-HUMAN-UAT.md` each return at least `1`, and each has a disposition item stating the two things to weigh.
    - The `27-HUMAN-UAT.md` frontmatter `updated:` and `source:` values are byte-unchanged, and the appended section says so and states the true amendment date.
    - `CONCERNS.md` carries a fourth dated `W-A` amendment, and its robust removed-line count is `0`.
    - `windows status` shows `open_count` and `total_count` each increased by exactly the number of entries appended; before and after values recorded in the SUMMARY.
    - Every appended `WINDOWS.md` description contains an observed number or an observed string quoted from a round-4 SUMMARY. Verified by reading each new entry against the SUMMARY it came from.
    - `git diff -- .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` shows only task 1's changes — this task does not touch the register.
  </acceptance_criteria>
  <done>The nine outstanding human items are carried forward legible as unanswered, this round's two decisions are on the record as human-made and distinguishable from a harness default, the concerns file is amended without widening, and every premise the execution falsified has a ledger entry carrying its observed value.</done>
</task>

<task type="auto">
  <name>Task 3: The roadmap, the coverage declaration, and the state — with PRIV-05 still open</name>
  <reversibility rating="reversible">Planning-document text. Correctable in place; none of it is under an append-only rule except the phase-record paragraphs, which are amended rather than rewritten.</reversibility>
  <files>.planning/ROADMAP.md, .planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md`, the whole `### Phase 27` section and the `### Phase 28` entry beneath it. Read the round-1 goal line, the two existing gap-closure notes, the wave list, the plans counter line, the PHASE 27 COMPLETES WITH PRIV-05 NOT SATISFIED paragraph, the WHAT PHASE 27 DID CLOSE and WHAT PHASE 27 LEAVES OPEN paragraphs, and the `REQUIREMENTS.md` tick paragraph. Every one of them is a record kept on append-and-amend terms.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md` — the whole file. It is a reasoned declaration that no external API is integrated, plus one extension covering plans 27-07 through 27-09. This task adds the same kind of extension for 27-10 through 27-13.
    - `.planning/STATE.md` — the frontmatter progress block and the Current Position section.
    - `.planning/WINDOWS.md` entry 27 — it records that the ROADMAP plans counter is an execution-tracking field the orchestrator owns and overwrites, and that a prior round left two contradictory counters in one line. Read it before touching that line.
  </read_first>
  <action>
Part A — the ROADMAP phase-27 record. Append a `**Gap closure round 4 (2026-08-26):**` note in the
register the two existing round notes use. It states what `27-VERIFICATION-3.md` scored, which three
must-haves failed and why, that the maintainer decided to FIX rather than accept on both axes, and what
plans 27-10 through 27-13 do. Do NOT rewrite the round-1 goal line: it is round-one text, deliberately
left in place, and qualified by the notes beneath it.

The four new plan entries under Wave 10 through Wave 13 headings were ALREADY ADDED at plan time, in
the same format as the existing wave headings and checkbox lines. VERIFY them rather than re-adding
them: four entries exist, the wave headings read 10 through 13 in order, each checkbox line names its
own plan file and carries its wave number, and none of the wave 1 through 9 entries was disturbed. Add
only what is genuinely missing. Re-adding an entry that is already present produces a duplicated wave
list, which is the record defect this whole plan exists to avoid producing.

Amend — by appending, not by editing — the paragraph recording what phase 27 leaves open, so that the
six named residuals and their owners are all present: `AR-27-04` (open, owed a human decision),
`AR-27-08` and `InjectionPointExtractor.kt:29` (Phase 28), `AR-27-09` and `AR-27-10` (new this round,
owners as assigned in task 1), the `CONCERNS.md` vendor auth-header class (open by prohibition), and
the sweep's own vocabulary bound. Then state plainly that six named residuals is not a completeness
claim.

Leave the PHASE 27 COMPLETES WITH PRIV-05 NOT SATISFIED paragraph in place and TRUE: append one
sentence dating it to round 4 and confirming it still holds, because `AR-27-08` is untouched and still
owned by Phase 28. Leave the `REQUIREMENTS.md` tick paragraph as it stands; PRIV-05 has since been
reverted to unticked by the milestone owner, which is the outcome that paragraph asked for, so append
one sentence recording that rather than deleting the paragraph that produced it.

The plans counter line was reduced at plan time to a SINGLE figure, deliberately, because
`WINDOWS.md` entry 27 records a prior round leaving two contradictory figures on that one line. Verify
it still carries exactly one figure and that the figure equals the number of `*-PLAN.md` files on disk.
Do not add a breakdown to that line — the round-by-round split belongs in the round notes, where it can
go stale without contradicting a counter. If the orchestrator's own value disagrees after it writes,
record the contradiction in the SUMMARY and as a `WINDOWS.md` entry rather than fighting it; that field
has two owners and entry 27 says so.

Part B — `COVERAGE.md`. Append an extension covering plans 27-10 through 27-13 on the same reasoning as
the existing extension: these four plan bodies name the passive-scan prompt path, the MCP tool result
shapes and the Montoya host API repeatedly, and every one of those is an already-running host API or an
already-exposed MCP surface. Nothing is integrated, wrapped, connected or consumed for the first time,
so the declaration stands and no matrix is fabricated to fill a gap. Name the specific symbols the
seal-time detector will re-read from these four plan bodies, so the extension is checkable rather than
assertive.

Part C — `STATE.md`. Update `last_activity`, `last_activity_desc`, the Current Position block and the
progress counters to reflect thirteen plans in phase 27. Do not change `status` to anything implying
the phase is verified — round 4 has not been verified, and the next artifact for this phase will be
`27-VERIFICATION-4.md`.
  </action>
  <verify>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs query verify.plan-structure .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-13-PLAN.md; grep -c '27-1[0123]-PLAN.md' .planning/ROADMAP.md; grep -c 'AR-27-09' .planning/ROADMAP.md; grep -c 'AR-27-10' .planning/ROADMAP.md; grep -c 'PRIV-05 NOT SATISFIED' .planning/ROADMAP.md; git diff HEAD --unified=0 -- .planning/REQUIREMENTS.md | grep '^-' | grep -v '^--- ' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '27-1[0123]-PLAN.md' .planning/ROADMAP.md` returns `4` — one entry per new plan, each under its own wave heading with wave numbers 10, 11, 12 and 13.
    - The round-1 goal line is BYTE-UNCHANGED: `git diff HEAD -- .planning/ROADMAP.md | grep '^-' | grep -c 'mirror the cookie name-variant fix'` returns `0`.
    - `grep -c 'PRIV-05 NOT SATISFIED' .planning/ROADMAP.md` returns at least `1`, and the paragraph carries an appended round-4 sentence confirming it still holds.
    - All six named residuals appear in the phase-27 open paragraph, each with an owner. Verified by reading the paragraph and by `grep -c 'AR-27-04\|AR-27-08\|AR-27-09\|AR-27-10' .planning/ROADMAP.md` returning at least `4`.
    - The plans counter line carries exactly ONE figure. Verified by reading the line; a second figure on that line is a failure of this task even if both agree.
    - `.planning/REQUIREMENTS.md` has ZERO changed lines under the robust diff form, and PRIV-05 remains `[ ]`.
    - `COVERAGE.md` carries an extension naming plans 27-10 through 27-13 and the specific symbols the seal-time detector re-reads from them.
    - `STATE.md` reflects thirteen plans in phase 27 and does not claim the phase is verified.
    - `verify.plan-structure` on this plan file exits without errors.
  </acceptance_criteria>
  <done>The roadmap records round 4 without rewriting round 1, names every residual with an owner, still says PRIV-05 is not satisfied, and carries exactly one plans counter; the coverage declaration is extended on checkable reasoning; and the requirements file is untouched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan produces records, not symbols. Every identifier it CREATES:

| Identifier | Kind | Where | What it is |
|-----------|------|-------|-----------|
| `T-26-02-01` clause (6) | register clause | `26-SECURITY.md` | The FOURTH refutation: consumer polarity and the JSON-string-open start. |
| `AR-27-09` | accepted-risk row | `26-SECURITY.md` | NEW. The leading-whitespace / obs-fold start boundary, OPEN, evidenced from `27-11-SUMMARY.md`, with its one-token fix written down. |
| `AR-27-10` | accepted-risk row | `26-SECURITY.md` | NEW. The RFC 9110 tchars outside the widened cookie name class, OPEN, evidenced from `27-10-SUMMARY.md`. The record the underscore class never had. |
| Standing rule clause (v) | rule clause | `26-SECURITY.md` | CONSUMER POLARITY: enumerate consumers and label each redactor or admitter. |
| Standing rule clause (vi) | rule clause | `26-SECURITY.md` | GREEN TESTS AS EVIDENCE, with the enforcing sweep and its vocabulary bound named in the same clause. |
| Audit-trail row 6 | register row | `26-SECURITY.md` | The fourth reopening and its re-closure, with recomputed counters. |
| Round-4 carry-forward section | record section | `27-HUMAN-UAT.md` | Nine unanswered items, two maintainer decisions with provenance, two new dispositions. |
| Fourth `W-A` amendment | record entry | `CONCERNS.md` | Scoped to what changed; the vendor class explicitly unchanged. |
| Round-4 gap-closure note + waves 10-13 | roadmap entries | `ROADMAP.md` | The phase record, appended not rewritten. |

REFERENCED BUT NOT CREATED HERE, so a reader does not look for them in this plan's diff:
`CookieHeaderNameWidthTest`, `NOT_COVERED_TCHARS`, `JSON_STRING_OPEN`, `JsonStringOpenBoundary`,
`RedactingPolicySurvivalSweepTest` — all created by plans 27-10 through 27-12.

DELIBERATELY UNTOUCHED: `REQUIREMENTS.md`, `AR-27-04`'s disposition, `AR-27-08`, Phase 28's entry,
`T-27-06-06`, and the round-1 phase goal line.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1. Blocking threshold: `high`. This plan changes no code, so its threats are threats to the
RECORD — which is the artifact three prior rounds got wrong.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Measurement -> register row | A finding filed from a prediction rather than a measurement is indistinguishable from a fact once it is written down. This phase has done it twice, recorded as `WINDOWS.md` entries 26 and 29. |
| Register row -> `threats_open` counter | The counter's population excludes every `AR-` finding at any severity. A high finding filed as an `AR-` row sits open beneath a counter reading zero. |
| Record -> future auditor | The register is what a future audit reads instead of re-measuring. A sentence wider than its evidence is a control defect with a delay fuse. |
| Harness default -> apparent human decision | Under `mode: yolo` an auto-selected checkpoint is indistinguishable from a maintainer choice unless the provenance is written down. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-13-01 | Repudiation | Prior clause text is edited or softened while appending clause (6) | high | mitigate | Task 1 Part A requires the old row body to be an exact CHARACTER prefix of the new one, with both lengths recorded, and the robust removed-line form gates the whole file. |
| T-27-13-02 | Repudiation | A residual is filed from a prediction rather than a measurement | high | mitigate | `AR-27-09` and `AR-27-10` must quote their evidence from the round-4 SUMMARYs; the task's precondition states that writing a number from this plan's prose is the defect two ledger entries already record. |
| T-27-13-03 | Information Disclosure | A high finding hides beneath a counter reading zero | high | mitigate | Task 1 Part E recomputes by the documented command, restates the population, and forces an explicit statement of whether either new finding is at or above the blocking threshold and what was done about it. |
| T-27-13-04 | Repudiation | A record sentence claims a completeness the round does not have | high | mitigate | The must-have carries the bound and names six residuals; task 3 Part A requires the phase-27 open paragraph to name all six with owners and to state that six named residuals is not a completeness claim. |
| T-27-13-05 | Spoofing | A harness-default disposition is read later as a maintainer decision | high | mitigate | Task 2 Part A records this round's two decisions as MAINTAINER-MADE with the explicit contrast against `AR-27-04`'s recorded auto-selection, so the two are distinguishable at a glance. |
| T-27-13-06 | Tampering | `27-HUMAN-UAT.md` or `REQUIREMENTS.md` loses a line | high | mitigate | Robust removed-line gates on both, at zero, in the verify blocks of tasks 2 and 3. |
| T-27-13-07 | Repudiation | The ROADMAP plans counter carries two contradictory figures again | medium | mitigate | Task 3 Part A requires exactly one figure on that line and, because the field has two owners (`WINDOWS.md` entry 27), requires any post-write disagreement to be recorded rather than silently reconciled. |
| T-27-13-08 | Information Disclosure | `T-27-06-06`, the user-facing host-anonymisation overclaim, stays live in shipped docs | medium | transfer | Deliberately unactioned: a change to what SHIPS is not a record repair, and both prior record plans scoped out of it for the same reason. Carried in `27-HUMAN-UAT.md` test 4 with its stated loss risk, and re-carried by task 2. |
| T-27-13-SC | Tampering | dependency installs | high | accept | This plan touches no build file and adds NO dependency. Gate: `git diff -- build.gradle.kts gradle/libs.versions.toml` shows zero lines at plan end. No `[ASSUMED]`/`[SUS]` package exists, so no blocking legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. The T-26-02-01 prefix check PASSES, with both character lengths recorded and explicitly measured as
   characters.
2. `threats_open` equals the raw output of the documented command, recorded alongside its rows-scanned
   and rows-closed counts, and the population statement is present and current.
3. `AR-27-09` and `AR-27-10` each exist with a severity, an owner and evidence quoted from a round-4
   SUMMARY.
4. Standing-rule clauses (v) and (vi) exist, each with a worked example from this file's own history.
5. Robust removed-line counts: `27-HUMAN-UAT.md` is `0`, `REQUIREMENTS.md` is `0`, `CONCERNS.md` is `0`.
6. `WINDOWS.md` `open_count` and `total_count` both increased by exactly the number of entries appended.
7. `ROADMAP.md` carries the four new plan entries, all six named residuals with owners, and the
   unchanged not-satisfied sentence with its appended round-4 confirmation.
8. `git diff -- src/` is EMPTY. This plan writes no code.
</verification>

<success_criteria>
- The fourth wrong closure is recorded with every prior narrative intact and its honest attribution
  stated.
- This round's two residuals are numbered, owned, severity-assigned and evidenced from measurements.
- The counter is computed and its population is restated; nothing high hides beneath it.
- Two new standing-rule clauses name the two lessons: consumer polarity, and green tests as evidence.
- This round's decisions are on the record as human-made and are distinguishable from a harness default.
- PRIV-05 remains unsatisfied and unticked, and the roadmap says so.
</success_criteria>

<output>
Create `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-13-SUMMARY.md` when done.

It MUST carry, verbatim: the pre- and post-edit character lengths of the T-26-02-01 row with the prefix
check result; the raw `awk` output with rows scanned and rows closed; the severities assigned to
`AR-27-09` and `AR-27-10` with the measurement each was derived from; the robust removed-line counts for
all four record files; the `WINDOWS.md` counter before and after; and an explicit statement that
`REQUIREMENTS.md` was not touched and PRIV-05 remains unticked.

**And it must state, in its own sentence, that phase 27 round 4 does NOT close PRIV-05.** Three carriers
plus two boundary axes are closed; `AR-27-08` is owned by Phase 28; four other residuals are named with
owners. A SUMMARY that reads as a closure would be the fifth iteration of the pattern these four rounds
exist to break.
</output>
