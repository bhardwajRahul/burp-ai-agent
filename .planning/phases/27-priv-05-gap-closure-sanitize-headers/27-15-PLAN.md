---
phase: 27-priv-05-gap-closure-sanitize-headers
plan: 15
type: execute
gap_closure: true
wave: 15
depends_on: ["27-14"]
autonomous: true
requirements: [PRIV-05]
files_modified:
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A survival pin written with a backtick-quoted function name, a same-line annotation, or a `suspend` / `public` / `override` / `protected` / `inline` modifier is FLAGGED by `RedactingPolicySurvivalSweepTest`. MEASURED: 136 of 1779 declaration lines under `src/test/kotlin` were invisible before this plan, 67 of them backtick-named `@Test` methods across 9 files including `redact/SecretTripwireHooksTest.kt`, and a synthetic pin in six declaration shapes scored 5 missed / 1 found. After this plan the same six shapes score 6 found."
    - "The claim 'it scans src/test/kotlin on every CI run and fails on the next such pin' is TRUE for the naming style 3.8% of current test methods already use and the one a future author is most likely to reach for. The claim and the gate now describe the same population."
    - "The composition `fileWalk` -> `detect` is gated in the FLAGGING direction, not only in the empty direction. Before this plan every proof that the detector can produce a hit bypassed the walk, and the one path that uses the walk expected EMPTY — so if `dropRawStringInteriors` ever began blanking real code, the tree scan returned zero and all eleven tests stayed green. One composition fixture now binds the two: a raw-string block that MUST be blanked followed by a real-code pin that MUST survive the walk and be flagged, asserting exactly one hit."
    - "The silent-blindness mode is converted into a loud one. `dropRawStringInteriors` fails with an explicit error when a file ends INSIDE a raw string, instead of returning a silently blanked tail. MEASURED before the change: 0 of 151 files end in that state and 625 lines are blanked tree-wide, so the walk is not vacuous today — which is a measurement and not a guarantee, and it is exactly the property this file exists to stop relying on."
    - "BOUND, carried with the claim, restated at BOTH places it is made. The sweep's KDoc enumeration is amended in the SAME change as the gate it describes, and `26-SECURITY.md` standing-rule clause (vi) — which cites that enumeration as the check's stated bound — is amended in the same change too. Before this plan the register carried a claim wider than its control, which is verbatim the failure clause (vi) exists to prevent."
    - "The stated axis count is MACHINE-CHECKED against the enumeration it describes, not asserted a second time. A test reads this file's own class KDoc, counts the numbered axis entries, and fails if the count and the stated constant disagree — so the number in the register can go stale only if a test goes red first."
    - "THE NEXT BLIND AXES, NAMED, because closing one axis without naming what remains is the defect one iteration smaller: a function whose opening parenthesis is not on its declaration line is still invisible to the widened regex; and `assertsPresenceAt` computes negation from the `assertTrue(` opener to the call under test, so in a compound assertion whose FIRST operand is negated every later containment inherits that negation and a real pin combined with a noise check is invisible. Both are added to the enumeration with their costs; the second's fix is written down and deliberately not applied this round."
    - "Every count this plan restates is RE-MEASURED after plan 27-14 landed, never carried forward. The benign-accessor count, the unqualified and qualified hit counts, the self-hit count, the file count and the rationale-line count are whatever the tree now produces, and any divergence from a prior projection is recorded beside the observed number rather than reconciled by narrowing anything."
    - "THIS PLAN CLOSES NO REQUIREMENT. Round 5 does NOT close PRIV-05. `REQUIREMENTS.md` is untouched and PRIV-05 stays `[ ]`; AR-27-08 and `InjectionPointExtractor.kt:29` remain owned by Phase 28. AR-27-04's disposition is NOT relitigated."
  artifacts:
    - "src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt — a widened `FUNCTION_DECLARATION` with both name spellings, the identifier taken from the correct group, a declaration-shape non-vacuity test over six shapes, a walk-to-detector composition gate in the flagging direction, a loud failure on an unbalanced file, an amended blind-axis enumeration, and a source-read pin on the stated axis count"
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md — standing-rule clause (vi)'s stated-bound paragraph amended in the same change as the control it describes, with every quoted count re-measured"
  key_links:
    - "`FUNCTION_DECLARATION` -> `detect()`'s early return -> the whole function body. A declaration line that does not match makes its ENTIRE body invisible, not merely unnamed, which is why a declaration-shape gap is not an ordinary coverage gap."
    - "`RedactingPolicySurvivalSweepTest`'s KDoc enumeration -> `26-SECURITY.md` clause (vi)'s stated bound. The link that made the register overclaim, and the one the new count pin now keeps honest."
    - "`dropRawStringInteriors` -> `fileWalk` -> `detect` -> the tree scan. The only production path in the file, and the only one that had no positive gate."
---

<objective>
Close `27-VERIFICATION-4.md` **gaps 2 and 3**, and the code review's **CR-02**, which are one defect
seen from three angles: the sweep written to stop "a stated bound wider than its control" carries one
itself, and the register inherited it.

`FUNCTION_DECLARATION = Regex("^\\s*(private |internal )?fun (\\w+)\\(")` admits exactly one optional
modifier and requires a word-character name. It matches none of: a backtick-quoted name, a same-line
annotation, or a `suspend` / `public` / `open` / `override` / `protected` / `inline` modifier.
`detect()` returns early on a non-matching declaration line, so an unmatched declaration makes its
ENTIRE body invisible. Measured over `src/test/kotlin`: **136 of 1779 declaration lines invisible, 67
of them backtick-named `@Test` methods across 9 files**, one of them in the redaction package itself.
A survival pin in the more idiomatic Kotlin style ships green today.

Separately, and confirmed independently by both the reviewer and the verifier: **nothing gates the
`fileWalk` → `detect` composition in the flagging direction.** Every proof the detector can produce a
hit bypasses the walk; the only path that uses the walk expects EMPTY. The file's own comment names
silent blanking of real code as "the dangerous direction" and then does not assert against it. It is
non-vacuous today by measurement, not by construction.

And `26-SECURITY.md` clause (vi) cites the sweep's eleven-axis KDoc as the check's STATED BOUND — so
the register carries a claim wider than its control, which is verbatim the failure clause (vi) was
written to prevent. The verifier's instruction is explicit: amend the register in the SAME change as
the sweep.

Purpose: this phase has been refuted four times by a claim outrunning its measurement. Fixing that
inside the artifact built to stop it is not housekeeping; it is the only way the next round's
evidence is worth reading.

Output: one widened regex, three new tests, one loud failure mode, an amended enumeration whose count
is source-pinned, and a register clause that matches it.

**THIS PLAN CLOSES NO REQUIREMENT.** Round 5 does not close PRIV-05. `REQUIREMENTS.md` must not be
edited by any task in this plan, and AR-27-04's disposition must not be relitigated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-4.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-REVIEW-2.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-12-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-14-SUMMARY.md
@src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt
@.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
</context>

<artifacts_this_phase_produces>
Symbols created or changed by plan 27-15.

| Symbol | File | Kind | Note |
|---|---|---|---|
| `RedactingPolicySurvivalSweepTest.FUNCTION_DECLARATION` | `redact/RedactingPolicySurvivalSweepTest.kt` | CHANGED | admits any modifier prefix, an optional same-line annotation, an optional generic parameter list, and BOTH name spellings; two capture groups |
| `RedactingPolicySurvivalSweepTest.STATED_BLIND_AXES` | same | NEW companion constant | the number of numbered axis entries in the class KDoc, machine-checked |
| `RedactingPolicySurvivalSweepTest.DECLARATION_SHAPE_FIXTURE` | same | NEW companion fixture | six declaration shapes, one survival pin each |
| `RedactingPolicySurvivalSweepTest.EXPECTED_DECLARATION_SHAPE_HITS` | same | NEW companion constant | 6 |
| `RedactingPolicySurvivalSweepTest.WALK_COMPOSITION_FIXTURE` | same | NEW companion fixture | a raw-string block that must be blanked, then a real-code pin that must survive and be flagged |
| `RedactingPolicySurvivalSweepTest.UNBALANCED_WALK_FIXTURE` | same | NEW companion fixture | an odd triple-quote count |
| `everyDeclarationShapeInUseInThisRepositoryIsVisibleToTheSweep` | same | NEW test | the gap-2 non-vacuity gate |
| `theWalkPreservesRealCodeWhileSkippingRawStringInteriors` | same | NEW test | the CR-02 composition gate |
| `theWalkFailsLoudlyWhenAFileEndsInsideARawString` | same | NEW test | the CR-02 loud-failure gate |
| `theStatedBlindAxisCountMatchesTheEnumeration` | same | NEW test | the gap-3 source-read pin |
| `dropRawStringInteriors(sourceId, lines)` | same | CHANGED signature | gains the source identifier it needs to name the file in its failure |
| `26-SECURITY.md` clause (vi) stated-bound paragraph | `.planning/phases/26-.../26-SECURITY.md` | AMENDED IN PLACE | the one place in this round where an existing paragraph is corrected rather than appended to, because it states a fact that is false |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the declaration gate to the shapes 67 live test methods already use</name>

  <precondition>Plan 27-14 is committed and `SerializedEmissionRedactionTest` is green on the working tree. This plan re-measures counts that plan 27-14's three new tests can move, and a measurement taken against a tree missing them would be stale on arrival.</precondition>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt</files>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt` lines 360-400 — `detect()`, its `return@forEachIndexed` on a non-matching declaration line, and the `Hit` construction that takes the identifier from `declaration.groupValues[2]`. That index changes with the regex and is the one place this widening can silently corrupt output.
    - The same file, lines 490-540 — `functionBodyAt`, `isolatedBodyOf` (which uses `containsMatchIn` on the same regex), `indentWidth`, `isCommentOnly`, `testSourceFiles`, `relativePath`.
    - The same file, lines 556-600 — the companion header and the `FUNCTION_DECLARATION` declaration at `:569`.
    - The same file, lines 220-300 — `theBlankLineHazardFixtureIsIsolatedWholeIncludingItsBlankLines` and `theBenignExclusionCannotSwallowARealSentinel`. The first is the precedent shape for the new non-vacuity test; the second shows how a flip-pair floors an exclusion.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-REVIEW-2.md` CR-01 — the proposed regex, the per-file counts, and the six-shape execution table.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-4.md` gap 2 — the same measurement taken independently, and the instruction to take the identifier from group 1 falling back to group 2.
  </read_first>

  <behavior>
    RED PROBE, run BEFORE the edit against the shipped detector and AFTER it, both recorded:
    a synthetic survival pin — an `assertTrue` on a value in the measured sentinel vocabulary, inside
    a body that names `PrivacyMode.STRICT` — written in six declaration shapes:
      1. a backtick-quoted function name          BEFORE: not flagged   AFTER: flagged
      2. an annotation and `fun` on the same line BEFORE: not flagged   AFTER: flagged
      3. `suspend fun`                            BEFORE: not flagged   AFTER: flagged
      4. `public fun`                             BEFORE: not flagged   AFTER: flagged
      5. `override fun`                           BEFORE: not flagged   AFTER: flagged
      6. a plain `fun`, the control               BEFORE: flagged       AFTER: flagged
    Before: 1 of 6. After: 6 of 6.

    REGRESSION PROBE, both directions, both recorded:
      - The pre-round file contents at `git show 09e9cae` for `mcp/tools/McpToolHelpersTest.kt` and
        `redact/CookieHeaderNameParityTest.kt`, run through the widened detector, must still report
        EXACTLY 3 hits — the two host pins and the underscore pin — with the same identifiers. The
        widening must not have changed what the detector says about the real artifacts it was proven
        on. This is execution-time evidence quoted in the SUMMARY, not a committed test.
      - The current tree, run through the widened detector, is expected to report 0 real pins. The
        verifier re-swept with a detector deliberately wider than the shipped one and found only the
        `Sentinel.BENIGN_CONTROL` negative controls and one benign pass-through control. If the
        widening surfaces ANYTHING beyond those, it is a real finding: report it, and do NOT narrow
        the regex or add an allowlist key to make it disappear.
  </behavior>

  <action>
Replace `FUNCTION_DECLARATION` with a regex that admits, in order from the line start: any leading
whitespace; zero or more annotations, each an at-sign, a word, and an optional parenthesised argument
list, followed by whitespace; zero or more bare word-plus-whitespace modifier tokens; the literal
`fun` followed by whitespace; an optional angle-bracketed generic parameter list followed by optional
whitespace; and then EITHER a word-character identifier as capture group 1 OR a backtick-quoted
identifier as capture group 2; then optional whitespace and an opening parenthesis. `27-REVIEW-2.md`
CR-01 gives this pattern verbatim and `27-VERIFICATION-4.md` gap 2 gives it again independently — use
it, do not re-derive it.

Change the `Hit` construction in `detect()` to take the identifier from group 1, falling back to
group 2 when group 1 is empty. This is the one place the widening can silently corrupt output: the
current code reads group 2, which under the new regex is the BACKTICK name, so leaving it unchanged
would produce empty identifiers for every plain-named hit and break the allowlist-key comparison in
`noGreenTestAssertsASensitiveValueSurvivesARedactingPolicy` without failing loudly. Verify the fix by
re-running the three-hit historical probe and checking the identifiers, not only the count.

Rewrite the constant's comment to state what the regex now admits and — the part that matters — the
shape it still does NOT admit: a function whose opening parenthesis is not on its declaration line, a
multi-line signature. That is the axis this widening creates, and task 3 adds it to the enumeration.
The comment must also state the consequence a reader needs: because `detect()` returns early on a
non-matching declaration line, an unmatched declaration makes its ENTIRE body invisible, which is why
this regex is the gate everything else in the file is downstream of.

Add `DECLARATION_SHAPE_FIXTURE` to the companion fixtures, holding the six shapes from the behavior
block as a raw string, and `EXPECTED_DECLARATION_SHAPE_HITS` with value 6. Add
`everyDeclarationShapeInUseInThisRepositoryIsVisibleToTheSweep`, which drives the fixture through
`detect` DIRECTLY — the same way every other positive fixture in this file does, because the fixture
lives inside a raw string the walk skips — and asserts exactly six hits, with the six identifiers
present. Its failure message names the measured population it is standing in for: 136 invisible
declaration lines, 67 of them backtick-named `@Test` methods across 9 files, before this change.

Scope the class KDoc's sentence "it scans `src/test/kotlin` on every CI run and fails on the next
such pin" so it says which declaration shapes it holds for and names the multi-line-signature shape
it does not. Do NOT delete the sentence: the claim is now true for the shapes named, and a scoped
true claim is the artifact this phase exists to produce.

Do not narrow `SENSITIVE_VALUE_VOCABULARY`, do not add a key to `ALLOWLIST`, do not add a second key
to `BENIGN_ACCESSORS`, and do not add a self-file exclusion. Each of those makes an inconvenient hit
set empty without making the repository safer, and the file says so in three places.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.RedactingPolicySurvivalSweepTest'</automated>
  </verify>

  <acceptance_criteria>
    - The six-shape RED PROBE scored 1 of 6 before the edit and 6 of 6 after, with both runs quoted in the SUMMARY.
    - The historical probe against `git show 09e9cae` for the two pre-round files still reports EXACTLY 3 hits through the WIDENED detector, and the three identifiers are the same strings as recorded in `27-12-SUMMARY.md`. A count that matches while identifiers are empty is a fail, not a pass — check both.
    - The widened detector over the current tree reports an empty qualified hit set, and the raw unqualified hit set is recorded in the SUMMARY with each entry classified. Anything beyond the benign controls is reported as a finding, not suppressed.
    - `grep -n 'ALLOWLIST = emptyMap' src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt` returns one line, and `grep -n 'BENIGN_ACCESSORS = setOf' src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt` shows a single-element set.
    - All existing tests in the class still pass, specifically `theTwoHostPinsRemovedThisRoundAreFlagged` (2 hits), `theBlankLineHazardFixtureIsIsolatedWholeIncludingItsBlankLines`, `theRawStringSkipIsWhyTheSelfScanIsClean`, `theSweepFileItselfYieldsNoHits` and `theTreeWalkIsNonVacuous`.
    - The gradle command above is green.
  </acceptance_criteria>

  <done>A survival pin written with a backtick name, a same-line annotation, or any modifier prefix is flagged. The detector still reports exactly the same three hits on the real pre-round artifacts it was proven against, with the same identifiers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the walk to the detector in the flagging direction, and make the silent blindness loud</name>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt</files>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt` lines 90-200 — every call site of `fileWalk` and every call site of `detect`, so the asymmetry the review tabulated is read from source rather than trusted: the two walk paths expect EMPTY, the seven direct paths expect non-empty.
    - The same file, lines 330-360 — `fileWalk`, `dropRawStringInteriors`, and the in-code comment at `:347-354` naming silent blanking of real code as "the dangerous direction" without asserting against it.
    - The same file, lines 556-575 — the companion constants including `TRIPLE_QUOTE` and `FIXTURE_ID`, which the new fixtures must be built from rather than embedding a literal triple quote inside a raw string.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-REVIEW-2.md` CR-02 — the call-site table and the proposed composition test.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-4.md` gap 3, second paragraph — the independent confirmation, plus the measured current state: 625 lines blanked tree-wide, 0 of 151 files ending INSIDE.
  </read_first>

  <behavior>
    - `theWalkPreservesRealCodeWhileSkippingRawStringInteriors`: a fixture whose first half is a
      triple-quoted block containing a survival-pin-shaped function, and whose second half is a
      real-code survival pin. Driven through `dropRawStringInteriors` and then `detect`, it must
      report EXACTLY ONE hit, and that hit's identifier must be the REAL-CODE pin's function name.
      Two hits means the raw-string skip stopped working; zero hits means the walk has started
      blanking real code, which is the direction that ships green today.
      RED PROBE: temporarily neutralise the skip so the walk returns its input unchanged — expect 2
      hits; then neutralise it the other way so it blanks everything — expect 0. Restore, expect 1.
      Record all three.
    - `theWalkFailsLoudlyWhenAFileEndsInsideARawString`: a fixture with an ODD number of triple
      quotes. The walk must raise an error naming the source identifier, instead of returning a
      silently blanked tail. `assertThrows` on `AssertionError`, with the message asserted to contain
      the source identifier.
      RED PROBE: this test fails on the pre-task tree, because the current walk returns normally.
    - MEASUREMENT, recorded not assumed: re-run the tree scan and record how many lines are blanked
      tree-wide and how many files end INSIDE. Expected 0 files INSIDE, or the new loud failure fires
      on a real file — in which case that file is the finding and this task reports it rather than
      relaxing the check.
  </behavior>

  <action>
Give `dropRawStringInteriors` a first parameter carrying the source identifier — the same string
`detect` is given — and update `fileWalk` to pass `relativePath(file)` for a tree file and the
self-path for the self-scan. At the end of the walk, if the state is still INSIDE, raise an
`AssertionError` naming the source identifier and stating the consequence plainly: the rest of that
file was blanked, so any survival pin below the unbalanced quote was invisible and the tree scan's
zero was vacuous for that file. `27-REVIEW-2.md` CR-02's closing paragraph proposes exactly this and
the verifier's gap-3 `missing` list repeats it.

Add `WALK_COMPOSITION_FIXTURE` to the companion fixtures. Build it by CONCATENATING the existing
`TRIPLE_QUOTE` constant with raw-string segments rather than embedding a literal triple quote inside
a raw string — the file already keeps `TRIPLE_QUOTE` at companion level for precisely this reason,
and embedding one would toggle the walk's state while the file scans itself. The fixture's first half
is an opening triple quote, a survival-pin-shaped function whose name is distinct from the second
half's, and a closing triple quote; its second half is a real-code survival pin function.

Add `theWalkPreservesRealCodeWhileSkippingRawStringInteriors` with the assertions from the behavior
block: exactly one hit, and that hit's identifier is the real-code pin. The failure message must
carry the reason this test exists — that zero hits means the walk has started blanking real code,
which makes the tree scan silently vacuous with every other test in this file still green, and that
this was the file's own named "dangerous direction" for a full round with nothing asserting against
it.

Add `UNBALANCED_WALK_FIXTURE` — a short raw string built the same way, with an odd triple-quote count
— and `theWalkFailsLoudlyWhenAFileEndsInsideARawString`, asserting the walk throws and that the
message names the source identifier.

Update the in-code comment at the end of `dropRawStringInteriors` so it no longer names a failure
direction it does not assert against. It must now say that the dangerous direction IS asserted, by
which two tests, and what each of them would report.

Do not add a self-file exclusion, do not weaken the loud failure into a warning, and do not exclude
any file from the tree walk to make the new check pass. If the loud failure fires on a real file
under `src/test/kotlin`, that file is a finding: record it in the SUMMARY with its path and the line
of the unbalanced quote, and fix the file, not the check.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.RedactingPolicySurvivalSweepTest'</automated>
  </verify>

  <acceptance_criteria>
    - `theWalkPreservesRealCodeWhileSkippingRawStringInteriors` reports exactly 1 hit on the shipped walk, 2 with the skip neutralised to a pass-through, and 0 with the skip neutralised to blank-everything. All three runs are quoted in the SUMMARY; a test that only ever produced 1 has measured nothing.
    - `theWalkFailsLoudlyWhenAFileEndsInsideARawString` fails on the pre-task tree and passes after, with both runs recorded.
    - The tree scan is re-run and the SUMMARY records the measured number of blanked lines tree-wide and the number of files ending INSIDE. Any file ending INSIDE is named with its path.
    - `grep -v '^[[:space:]]*[/*]' src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt | grep -c 'dropRawStringInteriors('` accounts for every call site, and each one passes a source identifier. There is no overload that omits it.
    - `theSweepFileItselfYieldsNoHits`, `theRawStringSkipIsWhyTheSelfScanIsClean` and `theTreeWalkIsNonVacuous` are still green — the self-scan must stay clean with NO self-file exclusion.
    - The gradle command above is green.
  </acceptance_criteria>

  <done>If the walk ever starts blanking real code, a test goes red instead of the tree scan going quietly empty. If a scanned file ends inside a raw string, the walk says so by name.</done>
</task>

<task type="auto">
  <name>Task 3: Restate the bound where it is made and where it is inherited, with every count re-measured</name>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt, .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md</files>

  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt` lines 20-90 — the class KDoc in full: the eleven numbered axes, the three-exclusion arithmetic, and the "TRIPWIRE OVER A MEASURED VOCABULARY" closing sentence.
    - The same file, lines 620-660 — `SENSITIVE_VALUE_VOCABULARY`, `HOST_LITERALS`, `BENIGN_ACCESSORS` with its quoted count of 7 live functions, and `ALLOWLIST` with its quoted arithmetic of 7 plus 1 plus 1.
    - The same file, lines 430-460 — `assertsPresenceAt` and its negation rule, which computes negation from the `assertTrue(` opener to the call under test. This is the compound-assertion over-fire and it is being NAMED, not fixed.
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` standing-rule clause (vi) in full, especially the paragraph beginning with the vocabulary bound and the sentence stating the axis count, plus the arithmetic paragraph that quotes the same counts.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-4.md` gap 3 — the three omissions it lists and the instruction to amend the register in the same change as the sweep.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-14-SUMMARY.md` — the re-measured benign-accessor live-function count after plan 27-14's new tests.
  </read_first>

  <action>
RE-MEASURE FIRST, EDIT SECOND. Before writing a single number, run the shipped detector — as widened
by tasks 1 and 2 — over the current tree and record: the number of `.kt` files under
`src/test/kotlin`; the unqualified hit count with the three constructed exclusions disabled, with
every hit classified as benign-control, position-rule or negation-rule; the qualified hit count with
them enabled; the self-scan hit count without the walk; and the rationale comment line count the
`MIN_RATIONALE_LINES`-style floors depend on. Every number written into either file in this task is
one of these measurements. Where a measurement disagrees with a number currently written down,
record BOTH — the observed value and the superseded one with the reason — exactly as this phase did
for the 5-versus-7 and 7-versus-9 divergences. Narrowing anything to make an old number true again is
prohibited.

Amend the sweep's class KDoc enumeration. Do NOT add the declaration-shape axis: task 1 closed it,
and an enumeration listing a closed axis is as wrong as one omitting an open axis. ADD two axes,
each stated as a COST in the register the existing eleven use:
  - a function whose opening parenthesis is NOT on its declaration line — a multi-line signature — is
    still invisible, because the declaration regex requires the parenthesis on the same line. This
    axis is CREATED by task 1's widening and naming it is the price of that widening.
  - `assertsPresenceAt` computes negation from the `assertTrue(` opener to the containment under
    test, so in a compound assertion whose FIRST operand is negated every later containment inherits
    that negation. A real pin combined with a negated noise check in one assertion is therefore
    invisible. State the fix in the same breath — scope the negation test to the operand immediately
    preceding the call, bounded by the nearest conjunction, disjunction or comma — and state that it
    is deliberately NOT applied this round, so the next engineer meets a written-down fix rather than
    a re-derivation.
Renumber the enumeration so it reads continuously, and update the sentence that introduces it to the
new count.

Add `STATED_BLIND_AXES` to the companion, and
`theStatedBlindAxisCountMatchesTheEnumeration`, which reads this file's OWN source, isolates the
class KDoc region — from its opening comment marker to the line above the class declaration — counts
the numbered axis entries in it, and asserts the count equals `STATED_BLIND_AXES`. Its failure
message states the point: a stated bound that does not match the enumeration it describes is the
exact defect this phase exists to repair, and this test exists so that the register can go stale only
if a test goes red first. RED PROBE: run it with `STATED_BLIND_AXES` deliberately off by one and
confirm it fails, before setting it to the measured value.

Amend `26-SECURITY.md` standing-rule clause (vi) IN PLACE — this is the one paragraph in round 5 that
is corrected rather than appended to, because it states a fact that is false. The sentence citing the
axis count must state the new count, must say that the count is now MACHINE-CHECKED against the
enumeration by a named test rather than transcribed, and must record — in the clause's own worked-
example register — that the previous count was cited here while the control was blind to 136
declaration lines, 67 of them backtick-named `@Test` methods, and that this is clause (vi) committing
the error clause (vi) describes. That admission is the most useful sentence in the amendment; do not
soften it. Re-state the exclusion arithmetic with the RE-MEASURED counts from the step above, keeping
the format that names each exclusion, its count and its cost.

Add a short dated paragraph to clause (vi) recording what round 5 changed about the check itself: the
declaration gate widened to both name spellings and any modifier prefix; the walk-to-detector
composition gated in the flagging direction; the unbalanced-file blindness converted into a loud
failure. Keep it factual and keep the counts measured.

Do NOT touch the `threats_open` frontmatter, the `awk` command, its population definition, the
Threat Register rows, the Accepted Risks Log, or the AR-27-04 row. All register mutation for round 5
belongs to plan 27-16, which recomputes the counter from the rows in one place. Two plans editing the
counter is how a hand-edited number gets in.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.RedactingPolicySurvivalSweepTest'</automated>
  </verify>

  <acceptance_criteria>
    - `theStatedBlindAxisCountMatchesTheEnumeration` fails when `STATED_BLIND_AXES` is set one off the true count and passes at the measured value. Both runs are quoted in the SUMMARY.
    - The number stated in the sweep's class KDoc, the value of `STATED_BLIND_AXES`, and the number cited in `26-SECURITY.md` clause (vi) are the SAME number. Verify by reading all three, and quote all three in the SUMMARY.
    - The enumeration contains no entry describing the declaration-shape blindness task 1 closed, and contains one entry each for the multi-line-signature shape and the compound-assertion negation over-fire, each stated as a cost.
    - Every count restated in either file was measured in this task after plans 27-14, and tasks 1 and 2 of this plan, landed. Each is recorded in the SUMMARY with its command. Any divergence from a previously written number appears with both values and the reason.
    - `git diff --stat` on `26-SECURITY.md` for this task shows changes confined to the clause (vi) region. `grep -n 'threats_open:' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` shows the value unchanged from before this plan, and the Accepted Risks Log rows are byte-unchanged.
    - The gradle command above is green.
  </acceptance_criteria>

  <done>The sweep states a bound it can back, the number is machine-checked against the enumeration, and the register cites the same number in the same change. Gap 3's "the register now carries a claim wider than its control" is closed at both ends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a test author's intent → the committed suite → the next audit | A green test is the most authoritative statement this repository makes about intended behaviour. A green assertion that a sensitive value survives a redacting policy converts a leak into an expectation, and the sweep is the only mechanical control on that crossing. |
| `src/test/kotlin` on disk → `fileWalk` → `detect` → CI verdict | The only production path the sweep has. Everything it claims about the repository passes through it, and until this plan nothing gated it in the direction that produces a hit. |
| `RedactingPolicySurvivalSweepTest` KDoc → `26-SECURITY.md` clause (vi) → a future auditor | A stated bound transcribed from a control into a register. Where the transcription outruns the control, the register argues for the defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-15-01 | Information Disclosure | `FUNCTION_DECLARATION` as `detect()`'s entry gate | high | mitigate | 136 declaration lines invisible, 67 of them backtick-named `@Test` methods across 9 files including one in the redaction package; an unmatched declaration hides its ENTIRE body. Task 1 widens the regex to both name spellings, any modifier prefix and an optional same-line annotation, and floors it with a six-shape non-vacuity test plus the three-hit historical re-run. |
| T-27-15-02 | Repudiation | `26-SECURITY.md` clause (vi)'s stated bound | high | mitigate | The register cites an axis count its control does not have, which is verbatim the failure clause (vi) exists to prevent. Task 3 amends the clause IN THE SAME CHANGE as the control, re-measures every quoted count, and pins the count from source so the transcription cannot go stale silently again. |
| T-27-15-03 | Tampering | `dropRawStringInteriors` → `fileWalk` → the tree scan | medium | mitigate | If the walk begins blanking real code, the tree scan returns zero and all tests stay green. Measured non-vacuous today (625 lines blanked, 0 of 151 files INSIDE) — a measurement, not a guarantee. Task 2 binds the composition in the flagging direction and converts the unbalanced-file case into a loud error naming the file. |
| T-27-15-04 | Information Disclosure | compound-assertion negation in `assertsPresenceAt` | low | accept | A real pin combined with a negated noise check inside one `assertTrue` inherits the negation and is invisible. Accepted this round and NAMED as an enumerated axis with its fix written down (scope the negation to the operand immediately preceding the call). Not fixed here because round 5 is scoped to the two blockers and touching the negation rule without its own flip-pair is how a detector gets quietly disarmed. |
| T-27-15-05 | Information Disclosure | multi-line function signature | low | accept | Created by task 1's widening: the regex still requires the opening parenthesis on the declaration line. Named as an enumerated axis in the same change that creates it, which is the discipline round 4 failed. |
| T-27-15-06 | Spoofing | `Hit` identifier taken from the wrong capture group | medium | mitigate | Under the widened regex the old group index is the backtick name, so leaving it would produce empty identifiers for plain-named hits and silently break the allowlist-key comparison without failing. Task 1 changes the extraction and the historical probe verifies IDENTIFIERS, not only the count. |
| T-27-15-SC | Tampering | npm / pip / cargo installs | low | accept | This plan adds no package-manager dependency and contains no install task, so the package-legitimacy gate has an empty population here. The Gradle dependency set is unchanged. |
</threat_model>

<verification>
- A survival pin in six declaration shapes scores 6 of 6 after the widening and scored 1 of 6 before, with both runs recorded.
- The widened detector still reports exactly 3 hits with the same three identifiers against the pre-round file contents at `git show 09e9cae`, and an empty qualified hit set on the current tree.
- The walk-to-detector composition reports exactly 1 hit, 2 with the skip disabled and 0 with the skip forced, all three recorded; a file ending inside a raw string raises a named error.
- The sweep's KDoc count, `STATED_BLIND_AXES`, and `26-SECURITY.md` clause (vi)'s count are the same number, and a deliberate off-by-one turns the suite red.
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.RedactingPolicySurvivalSweepTest' --tests 'com.six2dez.burp.aiagent.redact.CookieHeaderNameParityTest' --tests 'com.six2dez.burp.aiagent.mcp.tools.McpToolHelpersTest' --tests 'com.six2dez.burp.aiagent.mcp.tools.SerializedEmissionRedactionTest'` is green.
- `threats_open`, the `awk` command, its population paragraph, the Threat Register rows and the Accepted Risks Log are byte-unchanged by this plan.
- `REQUIREMENTS.md` shows zero changes across this plan's commit range.
</verification>

<success_criteria>
- Gap 2 is closed at the gate, proven by execution in both directions.
- CR-02 is closed: the sweep's only production path is gated in the direction that produces a hit, and its silent failure mode is loud.
- Gap 3 is closed at both ends: the enumeration is honest and the register cites a number a test enforces.
- Round 5 has not closed PRIV-05 and this plan says so.
</success_criteria>

<output>
Create `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-15-SUMMARY.md` when done.
It must carry the six-shape before/after table, the three-hit historical re-run with its identifiers,
the 1/2/0 composition runs, the deliberate off-by-one axis-count run, every re-measured count with
the command that produced it and any divergence from a previously written number, and a
`## Threat Flags` section reporting each `T-27-15-*` row above.
</output>
</content>
