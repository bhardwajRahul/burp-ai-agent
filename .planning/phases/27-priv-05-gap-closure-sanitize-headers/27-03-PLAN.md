---
phase: 27-priv-05-gap-closure-sanitize-headers
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
  - .planning/codebase/CONCERNS.md
  - .planning/v0.10.0-MILESTONE-AUDIT.md
autonomous: false
requirements: [PRIV-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`26-SECURITY.md` T-26-02-01 is `closed` again, and its closing note cites the shipped source (file + symbol), the specific test that asserts it, and the commit — not a restatement of a SUMMARY claim."
    - "The re-close was written AFTER re-reading the shipped source in the same task, and the observed `grep` output is quoted in the note. A record may not be closed on the strength of a plan saying the work was done."
    - "`26-SECURITY.md` frontmatter reads `threats_open: 0` and `status: verified`, the Security Audit Trail carries a third dated row for the Phase 27 closure, and both Sign-Off checkboxes are ticked."
    - "The reopening narrative dated 2026-08-24 is PRESERVED verbatim. Nothing in this plan deletes or rewrites the record of the miss — the register gains a closure, it does not lose its history."
    - "`26-SECURITY.md`'s 'What this audit did and did not do' section gains a durable TWO-CLAUSE process rule. Clause (i): an ASVS L1 pass that verifies a control is PRESENT must also compare its WIDTH against every sibling control implementing the same rule, and name the siblings compared. Clause (ii): a closing note may not be verified by a search NARROWER than the claim it makes — if the note says 'across X', the sweep must cover every spelling a reasonable implementer would use inside X, and the note must state the scope the sweep actually covered. Both are the generalisable lessons; without them the same class of false close recurs on the next second implementation and on the next closure note."
    - "`.planning/codebase/CONCERNS.md` W-A no longer claims the two sides agree by construction without qualification: it names the MCP tool-result path, states that it diverged for three phases, and records the shared predicate as the mechanism that now makes agreement structural — SCOPED to the two redaction paths and the passive-scan admitter, never to the whole codebase."
    - "Every claim this plan writes into a record is scoped to what the evidence supports: `Redaction.isCookieHeaderName` is the single cookie-header-name rule across THE TWO REDACTION PATHS AND THE PASSIVE-SCAN ADMITTER. Other cookie-header-name matchers survive in `src/main/kotlin`; each is named in the closure note by classification (extractor / local-analysis / request-mutator) with the reason it need not route through the predicate. No record produced here says 'the whole codebase'."
    - "The verification behind every closure note is the WIDENED sweep — exact-name, `ignoreCase`, line-prefix and substring spellings — plus a green `CookieHeaderRuleOwnershipTest`, never the single-spelling grep alone. The human checkpoint runs the widened sweep and reads the classification, not a narrower check than the claim it is being asked to approve."
    - "The closure notes state that the default-locale `lowercase()` hazard is closed ONLY at the two header-name functions this phase changed, and cite plan 27-01's measured backlog observation for the survivors. No note may read as if the locale hazard is closed repo-wide."
    - "`.planning/v0.10.0-MILESTONE-AUDIT.md` gains an append-only, dated closure note under the PRIV-05 gap narrative. Its frontmatter, its `gaps:` block and its original evidence are untouched — a future re-audit re-derives its own verdict rather than inheriting an edited one."
    - "A human has read the re-closed T-26-02-01 row against the shipped source and confirmed the record and the code now say the same thing."
  artifacts:
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md — T-26-02-01 re-closed with citations, frontmatter corrected, audit trail extended, and a new process line in the audit-scope section"
    - ".planning/codebase/CONCERNS.md — the W-A entry amended to name the third path, the shared predicate, both guarding tests and the surviving non-redacting matchers, scoped to the two redaction paths and the passive-scan admitter"
    - ".planning/v0.10.0-MILESTONE-AUDIT.md — append-only closure note under the PRIV-05 gap narrative"
  key_links:
    - "`Redaction.isCookieHeaderName` (shipped source) -> `26-SECURITY.md` T-26-02-01 closing note: the citation link that makes the record checkable rather than assertive"
    - "`CookieHeaderNameParityTest` + `CookieHeaderRuleOwnershipTest` -> `26-SECURITY.md` T-26-02-01 -> `CONCERNS.md` W-A: the places that must all agree on what is singular AND at what scope; a future reader who finds them disagreeing, or finds one of them claiming a wider scope than the tests support, has found a real regression"
    - "plan 27-01 SUMMARY classification -> the closure notes in all three records: the survivors are named in the record because a scope stated positively cannot silently widen, whereas a scope left implicit already has"
  prohibitions:
    - statement: "Do NOT mark T-26-02-01 closed on the strength of this plan, plan 27-01's SUMMARY, or any other GSD artifact saying the fix landed. Re-read the shipped source in the task, quote what you observed, and close on THAT. Closing a threat on a document's say-so is the precise failure the v0.10.0 audit found and this phase exists to repair — repeating it here would be a self-inflicted wound."
      severity: high
    - statement: "Do NOT delete, rewrite or soften the 2026-08-24 reopening narrative in `26-SECURITY.md`. A register that loses its own miss teaches nobody. The row gains a closure below the history, it does not replace it."
      severity: high
    - statement: "Do NOT edit `.planning/v0.10.0-MILESTONE-AUDIT.md`'s frontmatter, its `scores:` or its `gaps:` block, and do not tick or untick anything in `.planning/REQUIREMENTS.md`. A milestone verdict is re-derived by `/gsd-audit-milestone`, never hand-edited by the phase that closes its gap — a phase that grades its own homework produces exactly the record this phase is repairing."
      severity: high
    - statement: "Do NOT overstate. The closing note must name what is closed (the header-NAME class on both paths) and what remains accepted (AR-27-01, AR-27-02, AR-27-03). A note that reads 'cookies are now fully redacted everywhere' is the same overclaim in a new place."
      severity: high
    - statement: "Do NOT certify a whole-codebase claim about the cookie-header-name rule into ANY record, and do NOT verify any claim with a sweep narrower than the claim itself. The single-spelling grep this phase started with could not see four surviving matchers; certifying 'the whole codebase agrees by construction' on its strength would reproduce, one layer up, the exact defect this plan exists to repair — a register whose wording is wider than the control it describes. Scope every claim to the two redaction paths and the passive-scan admitter, name the classified survivors, and verify with the widened sweep plus `CookieHeaderRuleOwnershipTest`."
      severity: high
    - statement: "Do NOT let any closure note imply the default-locale `lowercase()` hazard is closed beyond the two header-name functions this phase changed. Plan 27-01's SUMMARY carries the measured survivor list; cite it as an open backlog item rather than omitting it, because omission here reads as closure to the next auditor."
      severity: high
    - statement: "English only in all documents (AGENTS.md, non-negotiable)."
      severity: medium
---

<objective>
Repair the RECORD, not just the code: reconcile `26-SECURITY.md` T-26-02-01, amend the `CONCERNS.md`
W-A entry that overclaimed, and note the closure in the milestone audit — each with citations to the
shipped source rather than to another planning document.

Purpose: the v0.10.0 audit found two defects, not one. The first was a narrow matcher. The second was
a security register that said `closed` about a control it had verified was PRESENT without ever
comparing its WIDTH to its sibling. Fixing only the first leaves the mechanism that produced the
false close fully intact, and the next second implementation of any control reproduces it.

Output: three corrected records scoped to what the evidence supports, a two-clause durable process
rule, and a human confirmation — made against a sweep wider than the claim — that the record and the
code now agree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-01-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-02-SUMMARY.md
</context>

## What is actually being repaired here

`26-SECURITY.md` T-26-02-01 read, in its own words, *"the PRIV-05 failure mode — a real-world header
spelling that the matcher misses — cannot recur unnoticed in this second redaction path"* and was
marked `closed`. It was reopened on 2026-08-24 by `/gsd-audit-milestone`, and the file's own
reopening note is unusually precise about why the L1 pass was insufficient:

> Source-level presence proved the control exists; it could not prove the control is as wide as the
> requirement.

That sentence is the reusable lesson, and it currently exists only as a post-mortem on one row. This
plan promotes it into the file's audit-scope section as a standing rule, so the next ASVS L1 pass in
this repo has to ask the width question before it can claim a close.

## Artifacts this phase produces

Symbols and records **created by this plan** — the source-grounding pass must treat these as new:

| Symbol | Kind | File |
|---|---|---|
| T-26-02-01 closure note (dated, citing source) | new register content | `26-SECURITY.md` |
| Security Audit Trail row 3 (Phase 27 closure) | new table row | `26-SECURITY.md` |
| "Width, not only presence" + "no verification narrower than its claim" process rule | new two-clause process line | `26-SECURITY.md` |
| W-A amendment naming the MCP tool-result path | new record content | `.planning/codebase/CONCERNS.md` |
| PRIV-05 closure note (append-only, dated) | new record content | `.planning/v0.10.0-MILESTONE-AUDIT.md` |
| AR-27-01, AR-27-02, AR-27-03 | accepted-residual IDs referenced from the closure notes | all three files |

<tasks>

<task type="auto">
  <name>Task 1: Re-close 26-SECURITY.md T-26-02-01 — with citations, and with the process rule that prevents the next false close</name>

  <precondition>`.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` exists and its frontmatter currently reads `threats_open: 1` and `status: gaps_found`. If it reads anything else, STOP and report — something else has already edited the record and this task's assumptions no longer hold.</precondition>

  <reversibility rating="reversible">Editing a planning record; undo is a `git revert` of one commit touching one file.</reversibility>

  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` — read the WHOLE file: the frontmatter, the trust-boundary table, the T-26-02-01 row at line 60, the Security Audit Trail table at lines 121-124, the Sign-Off block at lines 132-139, the "What this audit did and did not do" section at lines 141-168, and the "Reopening — 2026-08-24" section at lines 173-199.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt` — the SHIPPED `sanitizeHeaders`, read directly, not via a SUMMARY.
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` — the SHIPPED `isCookieHeaderName` and the two cookie regexes, read directly.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-01-SUMMARY.md` and `27-02-SUMMARY.md` — for the commit hashes and test names to cite. Use these for POINTERS only; the closure claim itself must come from the source you read above.
  </read_first>

  <files>.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md</files>

  <action>
    <!-- planner-discipline-allow: contains("cookie") -->
    Before writing anything, run these five commands and capture their literal output — the closing
    note quotes them, and without them this task is doing the thing it is meant to prevent. Commands
    3 and 4 exist because the single-spelling sweep this phase started with was NARROWER than the
    claim it was being asked to support, and a verification narrower than its claim is the defect
    being repaired, not the repair:

    1. `grep -n 'fun isCookieHeaderName' -A 6 src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt`
    2. `grep -n 'isCookieHeaderName' src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt`
    3. `grep -rn 'contains("cookie")' src/main/kotlin --include=*.kt | grep -v 'isCookieHeaderName' | wc -l`
    4. The WIDENED sweep, verbatim from plan 27-01 Task 3's acceptance criteria:
       `grep -rnE 'equals\("(Set-)?Cookie"|startsWith\("(Set-)?Cookie:"|headerValue\("(Set-)?Cookie"\)|contains\("cookie"\)|== "(set-)?cookie"' src/main/kotlin --include=*.kt | grep -v 'isCookieHeaderName' | grep -vE 'PassiveAiScannerAnalysis\.kt|PassiveAiScannerHeuristics\.kt|BountyPromptTagResolver\.kt|ActiveAiScanner\.kt|Redaction\.kt' | wc -l`
    5. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*CookieHeaderRuleOwnershipTest'`

    If command 3 or command 4 returns anything other than `0`, if command 5 is not green, or if either
    of the first two returns nothing, STOP and report — the code is not in the state this task is
    about to certify.

    Then read plan 27-01's SUMMARY for TWO things this task must carry into the record and must not
    invent: the per-site classification of every surviving cookie-header-name matcher, and the
    measured default-locale backlog observation. If 27-01's SUMMARY does not carry both, STOP — the
    closure note would otherwise be scoped by assumption rather than by measurement.

    Then edit `26-SECURITY.md`, scoped, four places:

    **(a) The T-26-02-01 row.** Change its Status cell from open to closed, and rewrite the
    Mitigation cell so it reads as a THREE-PART history rather than a replacement: the original
    narrow claim, the 2026-08-24 reopening (kept, one sentence), and the Phase 27 closure. The
    closure part must name: `Redaction.isCookieHeaderName` with the file and the fact it lowercases
    with `Locale.ROOT`; the single call site in `sanitizeHeaders`; the results of BOTH sweeps,
    commands 3 and 4, quoted as observed; the three guarding tests by name
    (`McpToolHelpersTest.cookieHeaderNameVariantsAreStrippedOnTheToolResultPath`,
    `CookieHeaderNameParityTest.everyNameThePromptPathStripsIsMatchedByTheSharedPredicate` and
    `CookieHeaderRuleOwnershipTest`); and the commit from plan 27-01.

    The closure part must be SCOPED in its own wording. Write the singularity claim as covering the
    two redaction paths and the passive-scan admitter, and in the same sentence name the surviving
    non-redacting matchers from 27-01's classification by their kind — the passive-scan cookie-section
    extractor, the local-only heuristics, the bounty-prompt extractor that runs on already-redacted
    text, and the active-scanner request mutator — with the half-sentence reason each is not a
    redaction path. A reader who takes the row at face value must end up with the true scope, not a
    wider one they would have to go and disprove.

    Also name what is NOT closed: residuals AR-27-01, AR-27-02 and AR-27-03, each in half a sentence,
    plus one line stating that the default-locale `lowercase()` hazard is closed only at the two
    header-name functions this phase changed and that 27-01's SUMMARY carries the measured survivor
    list as an open backlog item.

    **(b) Frontmatter.** `threats_open` becomes `0` and `status` becomes `verified`. Update the
    comment above `threats_open` only if it is now inaccurate; otherwise leave it.

    **(c) Security Audit Trail.** Append a third row dated with the execution date, `46` total,
    `46` closed, `0` open, run by `Phase 27 (27-03) — source re-verification`.

    **(d) Sign-Off and the process rule.** Tick both currently-unticked checkboxes, replace the
    `WITHDRAWN 2026-08-24` approval line with an approval that names Phase 27 and the date, and add
    ONE new line to the "What this audit did and did not do" section under a heading such as
    "Standing rule added 2026-08-24 (Phase 27)". That line states TWO clauses, both learned here:

    (i) Verifying that a control is PRESENT is not sufficient to close a threat about that control's
    COVERAGE; where a rule has more than one implementation, an L1 pass must compare the WIDTH of
    each implementation against the requirement's wording and against every sibling, and name the
    siblings it compared. Point at T-26-02-01 as the worked example.

    (ii) A closing note may not be verified by a search NARROWER than the claim it makes. If the note
    says "across X", the sweep must cover every spelling a reasonable implementer would use inside X,
    and the note must state the scope the sweep actually covered. Point at this phase's own first
    draft as the worked example: a single-spelling grep was about to certify a whole-codebase claim
    while four hand-written matchers survived, and the claim was scoped down to the two redaction
    paths and the passive-scan admitter once the sweep was widened. The lesson is not that the code
    was wrong — the survivors were all non-redacting — it is that the VERIFICATION was narrower than
    the SENTENCE, which is how a register drifts wider than its control in the first place.

    Do NOT delete or reword the "Reopening — 2026-08-24" section. It stays exactly as it is; the
    closure lives in the row and in the audit trail, above it.
  </action>

  <acceptance_criteria>
    - `grep -c 'threats_open: 0' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` returns `1` and `grep -c 'threats_open: 1' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` returns `0`.
    - `grep -c 'status: verified' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` returns `1` and `grep -c 'status: gaps_found' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` returns `0`.
    - `grep -c 'Reopening — 2026-08-24' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` returns `1` — the reopening narrative survived.
    - The T-26-02-01 row cites all of: `isCookieHeaderName`, `Locale.ROOT`, `McpToolHelpersTest`, `CookieHeaderNameParityTest`, `CookieHeaderRuleOwnershipTest`, `AR-27-01`, `AR-27-02`, `AR-27-03`. Verify with one grep per token against the line.
    - SCOPE assertion, verified by reading the row: the singularity claim is worded as covering the two redaction paths and the passive-scan admitter, the surviving non-redacting matchers are named by kind, and the row contains no phrase asserting the rule is singular across the whole codebase. `grep -ci 'whole codebase\|entire codebase\|everywhere in the tree' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` returns `0` for any such phrase introduced by this task — confirm the count did not rise against `git show HEAD:...`.
    - The row carries one line stating the default-locale hazard is closed only at the two header-name functions, citing 27-01's backlog observation.
    - The Security Audit Trail table has exactly 3 data rows, the third one ending in `0` open.
    - Both Sign-Off checkboxes are `[x]`; `grep -c '^- \[ \]' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` returns `0`.
    - `grep -c 'Standing rule added' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` returns `1`, and the standing rule carries BOTH clauses — the width clause and the "no verification narrower than its claim" clause.
    - The SUMMARY quotes the literal output of all five source commands run at the top of this task, including the `0` from command 3, the `0` from the WIDENED command 4, and command 5's green result with its test count.
    - `git diff --stat` shows exactly one file changed by this task.
  </acceptance_criteria>

  <verify>
    <automated>test "$(grep -c 'threats_open: 0' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md)" = "1" && test "$(grep -c 'status: verified' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md)" = "1" && test "$(grep -c '^- \[ \]' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md)" = "0" && test "$(grep -c 'Reopening — 2026-08-24' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md)" = "1" && test "$(grep -c 'CookieHeaderRuleOwnershipTest' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md)" -ge "1" && test "$(grep -rn 'contains("cookie")' src/main/kotlin --include=*.kt | grep -vc 'isCookieHeaderName')" = "0" && test "$(grep -rnE 'equals\("(Set-)?Cookie"|startsWith\("(Set-)?Cookie:"|headerValue\("(Set-)?Cookie"\)|contains\("cookie"\)|== "(set-)?cookie"' src/main/kotlin --include=*.kt | grep -v 'isCookieHeaderName' | grep -vcE 'PassiveAiScannerAnalysis\.kt|PassiveAiScannerHeuristics\.kt|BountyPromptTagResolver\.kt|ActiveAiScanner\.kt|Redaction\.kt')" = "0"</automated>
  </verify>

  <done>
    T-26-02-01 is closed on the strength of source that was read in this task, the reopening history
    is intact, the residuals are named, and the register carries a standing rule that makes the next
    L1 pass ask the width question.
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend the CONCERNS.md W-A entry that overclaimed, and note the closure in the milestone audit</name>

  <reversibility rating="reversible">Two planning-record edits; undo is a `git revert`.</reversibility>

  <read_first>
    - `.planning/codebase/CONCERNS.md` line 65 (the W-A entry) and line 66 (its `**Files:**` line). Read the whole entry — it is long, and the sentence being amended is the one claiming *"the two sides agree by construction rather than by two lists being kept in sync by hand."*
    - `.planning/v0.10.0-MILESTONE-AUDIT.md` lines 150-200 — the "The gap: PRIV-05" narrative and its "Remedy" paragraph. Note the frontmatter above it, which this task must NOT touch.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-01-SUMMARY.md` and `27-02-SUMMARY.md` — for names and commits to cite.
  </read_first>

  <files>.planning/codebase/CONCERNS.md, .planning/v0.10.0-MILESTONE-AUDIT.md</files>

  <action>
    **(a) `CONCERNS.md` W-A.** The entry's claim was true about the two rules it was describing and
    false about the codebase as a whole, because a third implementation existed. Amend it in place,
    scoped — do not rewrite the entry, which contains measured detail worth keeping.

    Add a clearly-delimited amendment inside the W-A entry, dated, that states four things:
    (1) at the time W-A was closed on 2026-08-13, a THIRD implementation of the cookie-name rule
    existed on the MCP tool-result path (`McpToolHelpers.sanitizeHeaders`) and was not considered;
    (2) it tested exact names and therefore leaked all five measured variants through the
    `request_parse` / `response_parse` MCP tools, found by the v0.10.0 milestone audit on 2026-08-24;
    (3) the "agree by construction" claim is now true of **the two redaction paths and the
    passive-scan admitter** — `Redaction.apply`'s two regexes, `McpToolHelpers.sanitizeHeaders` and
    `PassiveAiScannerFilters.sanitizeHeadersForPrompt` — because `Redaction.isCookieHeaderName` is the
    single predicate those sites call and both regexes are built from the same `COOKIE_NAME_TOKEN`.
    Write it at THAT scope and no wider. In the same breath, name the cookie-header-name matchers that
    survive elsewhere in `src/main/kotlin` and why each is not a redaction path — the passive-scan
    cookie-section extractor whose output is redacted downstream by the `COOKIE_SECTION_HEADER` span
    rule, the local-only scanner heuristics that reduce a cookie value to a boolean, the bounty-prompt
    extractor that filters text `Redaction.apply` has already processed, and the active-scanner
    request mutator that writes to the TARGET rather than to an AI backend. Take these from plan
    27-01's SUMMARY classification, which was verified against source in-task; do not restate them
    from this plan;
    (4) the enforcement is TWO tests with different jobs, and the note says which is which:
    `CookieHeaderNameParityTest` guards a narrowing of the PREDICATE — per plan 27-02's red probe 2 it
    does NOT guard a narrowing of the prompt-path REGEXES, which is guarded by
    `RedactionTest.cookieHeaderNameVariantsAreStripped` instead — and `CookieHeaderRuleOwnershipTest`
    guards the appearance of a FOURTH hand-written matcher, failing on any unclassified one.

    Update the `**Files:**` line on 66 to add `isCookieHeaderName` and `COOKIE_NAME_TOKEN` in
    `Redaction.kt`, plus `McpToolHelpers.kt` (`sanitizeHeaders`) and `PassiveAiScannerFilters.kt`
    (`sanitizeHeadersForPrompt`) as the two consumer sites.

    Keep the existing "Accepted cost" sentence about benign `*cookie*` names — it is still true and
    now applies to three sites instead of one; extend it to say so.

    **(b) `.planning/v0.10.0-MILESTONE-AUDIT.md`.** APPEND ONLY, and only in the body. Add a short
    dated subsection immediately after the "Remedy" paragraph of "The gap: PRIV-05", headed as a
    closure note, stating: which phase and plans closed it, the shipped symbol, the three guarding
    tests, and the three residuals that remain accepted (AR-27-01, AR-27-02, AR-27-03) with half a
    sentence each. Scope the singularity claim here exactly as in (a)(3) — the two redaction paths and
    the passive-scan admitter — and add one line that the default-locale hazard is closed only at the
    two header-name functions this phase changed, citing 27-01's backlog observation. State explicitly
    that the frontmatter verdict is deliberately left as audited and will be re-derived by the next
    `/gsd-audit-milestone` run rather than hand-edited here.

    Touch NOTHING above the first `---`-delimited frontmatter close: not `status`, not `scores`, not
    `gaps`, not `nyquist`, not `security`, not `tech_debt`. Touch nothing in `.planning/REQUIREMENTS.md`.
  </action>

  <acceptance_criteria>
    - `grep -c 'McpToolHelpers' .planning/codebase/CONCERNS.md` returns at least `1` — the W-A entry now names the third path.
    - `grep -c 'isCookieHeaderName' .planning/codebase/CONCERNS.md` returns at least `2` — once in the amendment, once on the `**Files:**` line.
    - `grep -c 'CookieHeaderNameParityTest' .planning/codebase/CONCERNS.md` returns at least `1`, and likewise for `CookieHeaderRuleOwnershipTest`.
    - SCOPE assertion, verified by reading the amended W-A entry and the audit closure note: neither claims the cookie-header-name rule is singular across the whole codebase, and both name the surviving non-redacting matchers by kind. Record the exact sentence used in each, in the SUMMARY, so the scope is checkable without re-reading both files.
    - `grep -c 'PassiveAiScannerAnalysis\|BountyPromptTagResolver' .planning/codebase/CONCERNS.md` returns at least `1` — the surviving extractors are named, not omitted.
    - `git diff .planning/v0.10.0-MILESTONE-AUDIT.md | grep -c '^[-+].*status: gaps_found'` returns `0`, and `git diff .planning/v0.10.0-MILESTONE-AUDIT.md | grep -c '^-'` returns `1` (the diff header only) — the change to the audit is strictly additive.
    - `git diff --quiet .planning/REQUIREMENTS.md` exits 0 — the requirements checklist was not hand-edited.
    - `grep -c 'AR-27-01' .planning/v0.10.0-MILESTONE-AUDIT.md` returns at least `1`, and likewise for `AR-27-02` and `AR-27-03`.
    - `grep -c 'The gap: PRIV-05' .planning/v0.10.0-MILESTONE-AUDIT.md` returns `1` — the original narrative heading survived.
  </acceptance_criteria>

  <verify>
    <automated>test "$(grep -c 'isCookieHeaderName' .planning/codebase/CONCERNS.md)" -ge "2" && test "$(grep -c 'CookieHeaderNameParityTest' .planning/codebase/CONCERNS.md)" -ge "1" && test "$(git diff .planning/v0.10.0-MILESTONE-AUDIT.md | grep -c '^-')" = "1" && git diff --quiet .planning/REQUIREMENTS.md</automated>
  </verify>

  <done>
    The codebase concern that overclaimed now names all three implementations and the mechanism that
    binds them, and the milestone audit carries an additive, dated closure note that does not
    pre-empt the next audit's verdict.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human confirmation that the record and the code now say the same thing</name>

  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` — the T-26-02-01 row, the frontmatter, the Security Audit Trail, the Sign-Off block and the `Reopening — 2026-08-24` section, as Task 1 left them.
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` — `isCookieHeaderName` and both cookie regexes.
    - `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt` — `sanitizeHeaders`.
    - `.planning/codebase/CONCERNS.md` — the amended W-A entry.
  </read_first>

  <what-built>
    The T-26-02-01 row in `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` has
    been re-closed with citations to source read in-task, its frontmatter set to `threats_open: 0` /
    `status: verified`, a third audit-trail row added, both sign-off boxes ticked, and a standing
    two-clause rule added to the audit-scope section — compare a control's WIDTH against its siblings,
    and never verify a claim with a search narrower than the claim. The
    `.planning/codebase/CONCERNS.md` W-A entry has been amended to name the MCP tool-result path and
    the shared predicate, SCOPED to the two redaction paths and the passive-scan admitter, and
    `.planning/v0.10.0-MILESTONE-AUDIT.md` has an additive dated closure note. The underlying code fix
    and its tests were shipped by plans 27-01 and 27-02, along with `CookieHeaderRuleOwnershipTest`,
    which classifies every surviving cookie-header-name matcher and fails on an unclassified new one.

    This checkpoint is `blocking-human` deliberately, and for two reasons now. The first: this phase
    exists because a security register said `closed` about a control that was narrower than its
    requirement, and auto-approving the re-close would reproduce that failure one layer up. The
    second: the first draft of this checkpoint handed you a single-spelling grep as the whole of its
    evidence for a whole-codebase claim, which is the same failure a third time. The sweep you are
    asked to run is now wider than the sentence you are asked to approve — check that it stays that
    way.
  </what-built>

  <how-to-verify>
    Read the record against the code — not one document against another. That comparison is exactly
    what the last ASVS L1 pass did not do.

    A note on step 2, because it is the reason this checkpoint was rewritten. The first draft of this
    phase handed you a single-spelling grep and asked you to approve a claim about the whole codebase
    on its strength. That grep could not see four surviving hand-written matchers. A verification
    narrower than the claim it certifies is the same defect this phase exists to repair, so the sweep
    below is widened and the claim below is scoped.

    1. Run `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest' --tests '*CookieHeaderNameParityTest' --tests '*CookieHeaderRuleOwnershipTest' --tests '*RedactionTest'` — expect all four classes green.
    2. Run BOTH sweeps and expect NO output from either:
       - narrow: `grep -rn 'contains("cookie")' src/main/kotlin --include=*.kt | grep -v 'isCookieHeaderName'`
       - widened: `grep -rnE 'equals\("(Set-)?Cookie"|startsWith\("(Set-)?Cookie:"|headerValue\("(Set-)?Cookie"\)|contains\("cookie"\)|== "(set-)?cookie"' src/main/kotlin --include=*.kt | grep -v 'isCookieHeaderName' | grep -vE 'PassiveAiScannerAnalysis\.kt|PassiveAiScannerHeuristics\.kt|BountyPromptTagResolver\.kt|ActiveAiScanner\.kt|Redaction\.kt'`
       Output from the narrow sweep means a hand-written substring copy survives in a redaction path. Output from the widened sweep means a matcher exists that nobody classified.
    3. Now drop the file exclusions and run the widened sweep again, unfiltered, so you see the FULL list including the classified sites. Compare it line by line against the classification table in plan 27-01's SUMMARY. Every line must be either a call to the shared predicate, the owner `Redaction.kt`, or a classified non-redacting site with a reason you find convincing. Spot-check at least two classifications by opening the file and following the value to its consumer — the classifications were verified in-task, and this is where that verification gets a second pair of eyes.
    4. Run `grep -n 'T-26-02-01' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` and read the row end to end.
    5. Open `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` and read `isCookieHeaderName` plus the two cookie regexes. Open `src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpers.kt` and read `sanitizeHeaders`.
    6. Ask the width question: is the tool-result path's rule as wide as the prompt path's? Does what the row CLAIMS is closed match what the code DOES? Are AR-27-01, AR-27-02 and AR-27-03 the residuals you would have accepted, stated at the severity you would have given them?
    7. Ask the SCOPE question, which is new and is the other half of why you are here: does the row claim the rule is singular across the whole codebase, or across the two redaction paths and the passive-scan admitter? Only the second is supported. Does it name the surviving matchers? Does it say the default-locale fix covers only the two header-name functions, rather than implying a repo-wide locale closure?
    8. Confirm the `Reopening — 2026-08-24` narrative is still present further down the file — the register kept its own history.

    If the row claims more than the code delivers, in either direction, say so and stop rather than
    approving. Naming the unsupported clause and what the source actually shows is worth more than a
    green phase.
  </how-to-verify>

  <acceptance_criteria>
    - The four-class Gradle run reports green for `McpToolHelpersTest`, `CookieHeaderNameParityTest`, `CookieHeaderRuleOwnershipTest` and `RedactionTest`, and the observed test counts are recorded in the SUMMARY.
    - Both sweeps in step 2 print no output, and both are recorded in the SUMMARY with the command and the observed result — the widened one especially, since it is the one that supports the claim.
    - The unfiltered sweep from step 3 is recorded in the SUMMARY as a list, each line paired with its classification, and the two classifications the human spot-checked are named with what they found.
    - The human's verdict is recorded verbatim in `27-03-SUMMARY.md`, including any clause they judged unsupported and the source line they judged it against, and their answer to the SCOPE question in step 7.
    - If the verdict is anything other than approval, the phase reports `## CHECKPOINT REACHED` with the named clause and does NOT seal.
  </acceptance_criteria>

  <resume-signal>Type "approved", or name the specific clause of the T-26-02-01 row that the source does not support — including any clause whose SCOPE is wider than the evidence</resume-signal>

  <done>A human has compared the re-closed register row against the shipped source and confirmed they agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning record → future reader's belief about the code | A `closed` row in a threat register is load-bearing: the next audit, the next contributor and the next release note all treat it as verified fact. A record stronger than the code is an information-integrity failure with a long half-life. |
| Phase artifact → milestone verdict | `/gsd-audit-milestone` re-derives its verdict from phase artifacts. A phase that edits the milestone's own verdict removes the independence that made the audit useful. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03-01 | Repudiation | a security record marked `closed` on a document's say-so rather than on source | high | mitigate | Task 1 runs three source commands BEFORE writing, refuses to proceed if the whole-tree grep is non-zero, and the SUMMARY must quote their literal output. The closing note cites file, symbol, test names and commit. This is the meta-threat the v0.10.0 audit exposed, mitigated by making the citation mechanical. |
| T-27-03-02 | Repudiation | the reopening history being erased by the closure | high | mitigate | Prohibition (high) plus a hard acceptance criterion that greps for the `Reopening — 2026-08-24` heading after the edit. The register gains a closure below its history; it never replaces it. |
| T-27-03-03 | Tampering | a phase editing the milestone verdict that graded it | high | mitigate | The audit edit is asserted strictly additive (`git diff` shows exactly one `^-` line, the header) and `.planning/REQUIREMENTS.md` is asserted untouched. The next `/gsd-audit-milestone` re-derives independently. |
| T-27-03-04 | Repudiation | the closure note overclaiming ("cookies now fully redacted everywhere") | medium | mitigate | The row and both notes are required to name AR-27-01, AR-27-02 and AR-27-03 explicitly, asserted by grep. An overclaim in a new place is the same defect relocated. |
| T-27-03-05 | Repudiation | the width lesson staying a one-row post-mortem instead of a standing rule | medium | mitigate | Task 1(d) promotes it into `26-SECURITY.md`'s audit-scope section as a standing rule, asserted by grep, so the next L1 pass in this repo has to ask the width question before it can claim a close. |
| T-27-03-06 | Repudiation | certifying a whole-codebase claim into a security register on the strength of a sweep narrower than the claim | high | mitigate | The precise recursion this plan was about to commit: re-closing a register that overclaimed, using a single-spelling grep that could not see four surviving matchers. Mitigated four ways. (1) Every claim is scoped by wording to the two redaction paths and the passive-scan admitter, enforced by a high-severity prohibition and a read-the-row scope assertion in Tasks 1 and 2. (2) The verification is the WIDENED sweep plus a green `CookieHeaderRuleOwnershipTest`, wired into both `<verify>` gates. (3) The surviving non-redacting matchers are NAMED in both records, so a reader is handed the true scope rather than having to disprove a wider one. (4) The human checkpoint runs the widened sweep, then the UNFILTERED sweep against the classification, and spot-checks two classifications at source. Clause (ii) of the standing rule generalises the lesson so the next closure in this repo inherits it. |
| T-27-03-SC | Tampering | npm/pip/cargo installs | n/a | **accept** | This plan touches only markdown records. No package installed, no dependency added, no install task, so no package-legitimacy audit is required. |

ASVS level 1; blocking severity `high`. The register holds FOUR `high` threats — `T-27-03-01`,
`T-27-03-02`, `T-27-03-03` and `T-27-03-06` — and all four are `mitigate`, closed by this plan's
acceptance criteria plus the Task 3 human confirmation. `T-27-03-04` and `T-27-03-05` are medium.
No `high` threat is `accept` and this plan carries no accepted residual of its own; AR-27-01,
AR-27-02 and AR-27-03 belong to plans 27-01 and 27-02 and are cited here, not re-dispositioned.
</threat_model>

<verification>
- `26-SECURITY.md`: `threats_open: 0`, `status: verified`, three audit-trail rows, zero unticked checkboxes, reopening narrative intact, standing rule present with BOTH clauses.
- `CONCERNS.md`: W-A names `McpToolHelpers`, `isCookieHeaderName`, `CookieHeaderNameParityTest` and `CookieHeaderRuleOwnershipTest`, and names the surviving non-redacting matchers.
- Scope check: no record produced by this plan claims the cookie-header-name rule is singular across the whole codebase, and no record implies the default-locale hazard is closed repo-wide.
- `.planning/v0.10.0-MILESTONE-AUDIT.md`: diff is additive only; `.planning/REQUIREMENTS.md` untouched.
- The five source commands from Task 1 quoted verbatim in the SUMMARY, including the `0` from the narrow sweep and the `0` from the WIDENED sweep.
- Task 3 human confirmation recorded, including their answer to the scope question and their two spot-checked classifications.
</verification>

<success_criteria>
- SC1: T-26-02-01 is `closed` with citations to shipped source read in-task, and the three residuals are named.
- SC2: the 2026-08-24 reopening narrative survives verbatim.
- SC3: `26-SECURITY.md` carries a standing rule with both clauses — compare a control's width against its siblings, and never verify a claim with a search narrower than the claim.
- SC4: `CONCERNS.md` W-A names the third path, the shared predicate, both guarding tests and which narrowing each guards.
- SC5: the milestone audit gains an additive dated closure note; its frontmatter, `gaps:` block and `REQUIREMENTS.md` are untouched.
- SC6: a human has read the row against the source and confirmed they agree, having run the WIDENED sweep and spot-checked two classifications.
- SC7: every claim written into a record is scoped to the two redaction paths and the passive-scan admitter, names the surviving non-redacting matchers, and states that the default-locale fix covers only the two header-name functions this phase changed. No record says "the whole codebase".
</success_criteria>

<output>
Create `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-03-SUMMARY.md` when done.
Record: the literal output of Task 1's five source commands (both sweeps and the ownership test run),
which clauses of the old T-26-02-01 claim were kept and which were corrected, the exact scope sentence
written into each of the three records, the unfiltered sweep paired line-by-line with its
classification, and the human's verdict from Task 3 including their answer to the scope question, the
two classifications they spot-checked, and any clause they found unsupported.
</output>
