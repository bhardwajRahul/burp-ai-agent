---
phase: 27-priv-05-gap-closure-sanitize-headers
plan: 06
type: execute
wave: 6
gap_closure: true
depends_on: ["27-05"]
files_modified:
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
  - .planning/codebase/CONCERNS.md
  - .planning/v0.10.0-MILESTONE-AUDIT.md
autonomous: false
requirements: [PRIV-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`AR-27-01` is RECLASSIFIED on the record: it was never a genuine residual. It was safe only where `sanitizeHeaders` ran in front of it, and on the 14 measured serialized emission sites nothing did — so between its acceptance and plan 27-04 it was a live leak of the CANONICAL `Cookie:` and `Set-Cookie:` headers, strictly broader than the five variant spellings this phase was created to close. The record states that, dated, and then states what closed it."
    - "`AR-27-02` is RE-DECIDED on evidence rather than inherited: it is SUPERSEDED, not still-deferred. It was load-bearing only because the primary cookie rule could not fire on the serialized shape; after plan 27-04 it can. The record states the bound this leaves — the cookie rules are a single point of control on that path with no independent backstop behind them — because that is the honest cost of not widening `SENSITIVE_WORDS`."
    - "`AR-27-04` is recorded as a NEW finding with its measured evidence quoted: the `Host:` header value and the sibling `SiteMapEntry.url` field reach an AI backend un-anonymised under STRICT on the serialized emission shape. Severity medium, disposition decided by the maintainer at this plan's checkpoint, with the two measured reasons it was excluded from the code change stated rather than asserted."
    - "Every record edit is APPEND-AND-AMEND. The 2026-08-24 reopening narrative in `26-SECURITY.md` survives verbatim; clause (3) of T-26-02-01 — the Phase 27 closure — survives verbatim and gains a clause (4) beside it; `CONCERNS.md`'s existing W-A amendment survives verbatim and gains a second one; `v0.10.0-MILESTONE-AUDIT.md`'s closure note survives verbatim and gains a dated correction below it. A register that loses its own miss teaches nobody, and this is the second time this phase has had one."
    - "The milestone-audit closure note's HEADING is qualified: its body was carefully scoped but the heading reads as closing the PRIV-05 requirement gap, and PRIV-05 remained refuted on the serialized emission path for the whole interval between that note and plan 27-04. The correction says so, with both dates."
    - "`26-SECURITY.md`'s `threats_open` value is COMPUTED from the register by a documented command whose output is quoted, never hand-written; and the audit trail gains a dated row for the reopening-and-reclosure of the raw-message path, so the interval during which the frontmatter overclaimed is visible in the record rather than erased from it."
    - "No claim written here is wider than the gates behind it can measure. What is closed is named as the cookie-header class and the exact-name auth-header class ON THE SERIALIZED EMISSION PATH, bounded by the pinned 14-site inventory and by two tripwires that state their own bounds. What is open is named: AR-27-04, and the vendor auth-header gap `CONCERNS.md` has always carried."
    - "`.planning/REQUIREMENTS.md` is untouched. Its PRIV-05 tick is the milestone owner's to re-derive; a phase that grades its own homework produces exactly the record this phase is repairing, and the verifier already flagged the tick for the milestone owner rather than for this phase."
  artifacts:
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md — T-26-02-01 gains clause (4); AR-27-01 reclassified, AR-27-02 superseded, AR-27-04 added; frontmatter recomputed; a fourth audit-trail row"
    - ".planning/codebase/CONCERNS.md — a second dated W-A amendment covering the serialized emission path, the boundary fragments and the excluded host residual"
    - ".planning/v0.10.0-MILESTONE-AUDIT.md — an append-only dated correction below the existing closure note, qualifying its heading"
  key_links:
    - "the pinned 14-site inventory (`SerializedEmissionSiteInventoryTest`) -> every record sentence about which paths are covered: the count is what bounds the claim, so the record cites the test rather than a prose enumeration that can drift"
    - "`LogicalLineBoundaryScopeTest` -> the record's statement of which rules carry the new boundary: the source-state fact and the written claim are held equal by a test, not by a reader's memory"
    - "the quoted probe output for `Host:` / `url` -> AR-27-04: the evidence that keeps the excluded residual a finding with a measurement behind it rather than a hedge"
  prohibitions:
    - statement: "Do NOT delete, rewrite or soften the 2026-08-24 reopening narrative in `26-SECURITY.md`, clause (3) of T-26-02-01, the existing `CONCERNS.md` W-A amendment, or the milestone-audit closure note. Each gains something beside it. This is the second reopening of the same threat; a register that overwrites its history through two of them is worse than no register."
      severity: high
    - statement: "Do NOT edit `.planning/v0.10.0-MILESTONE-AUDIT.md`'s frontmatter, `scores:` or `gaps:` blocks, and do NOT tick or untick anything in `.planning/REQUIREMENTS.md`. Verdicts are re-derived by `/gsd-audit-milestone`, never hand-edited by the phase closing their gap."
      severity: high
    - statement: "Do NOT close any record on the strength of a PLAN or a SUMMARY saying the work landed. Re-read the shipped source and re-run the named tests in the task, quote what you observed, and write the record from THAT. Closing on a document's say-so is the precise failure the v0.10.0 audit found, and this phase has now reproduced it once."
      severity: high
    - statement: "Do NOT hand-write the `threats_open` integer. Compute it from the register with a documented command, quote the command and its output in the task, and write the computed value. A hand-written counter is how the previous overclaim survived review."
      severity: high
    - statement: "Do NOT write a claim wider than the gates behind it. Bound every statement to the serialized emission path, the cookie-header class and the exact-name auth-header class, and cite the pinned inventory count that bounds it. Do not restate any of it at a scope wider than the paths this phase measured."
      severity: high
    - statement: "Do NOT record AR-27-04 as closed, moot or theoretical. It is measured, it is quoted, and its disposition is the maintainer's at this plan's checkpoint. Recording a measured leak as anything less than an open finding awaiting disposition is the overclaim class this phase exists to stop."
      severity: high
---

<objective>
Correct the records that this phase's own verification proved are overclaims, and record what plans
27-04 and 27-05 measured — all of it append-and-amend, so the register keeps both misses.

Purpose: `26-SECURITY.md`'s `threats_open: 0` / `status: verified` and the milestone-audit closure
note were restored on the strength of a threat whose parent requirement was still violated on a
sibling path. `AR-27-01` was accepted as a residual and was in fact a live leak of the canonical
cookie headers. Both facts belong in the record, dated, next to what is now true.

Output: three amended records, a reclassified AR-27-01, a superseded AR-27-02, a new AR-27-04 with
quoted evidence, a recomputed frontmatter counter, and a maintainer decision on the one residual this
phase deliberately did not close.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-04-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-05-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 26-SECURITY.md — clause (4), the AR reclassifications, and a computed counter</name>
  <files>.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md</files>
  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` in full: the frontmatter, the T-26-02-01 row's three-part structure, the Accepted Risks section, the Security Audit Trail, the Sign-Off boxes, the two-clause process rule at `:184` and the unedited `## Reopening — 2026-08-24` section at the foot.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION.md` — the two failed truths and the Record Accuracy section, which names exactly which consequence of T-26-02-01 was questionable and which clauses hold.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-04-SUMMARY.md` and `27-05-SUMMARY.md` — the shipped symbols, the pinned inventory count, the tripwire bounds and the quoted probe output.
    - The shipped source, re-read in this task rather than trusted from a SUMMARY: `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` (the three fragments and the four boundary-relevant rules) and `src/main/kotlin/com/six2dez/burp/aiagent/mcp/schema/Serialization.kt` lines 42-83.
  </read_first>
  <action>
Re-read the shipped source first and re-run the named tests, then write the record from what you
observed. Quote the observed output in the record where it supports a claim.

Amend the T-26-02-01 row by ADDING a fourth clause after clause (3), leaving clauses (1), (2) and (3)
byte-identical. Clause (4) states, dated: that when (3) was written the parent requirement PRIV-05 was
still refuted on a sibling path this phase had not compared against; that the leak on that path was
the canonical `Cookie:` and `Set-Cookie:` names rather than the five variant spellings, and so was
strictly broader than the defect that created the phase; the root cause in one sentence — a raw HTTP
message embedded in a JSON string has no real newline, and the cookie rules were line-anchored; and
what closed it — the boundary fragments in `Redaction`, cited by symbol and by the tests that gate
them, with the pinned emission-site count that bounds the claim.

Reclassify the residuals in the Accepted Risks section and wherever the row names them:
`AR-27-01` — no longer an accepted residual. Record it as a live finding for the interval between its
acceptance and plan 27-04, with the reason its acceptance was unsound (it was conditional on a
sanitizer that existed on one path only), and as CLOSED by plan 27-04. Name the repository's own green
test that pinned the leaking behaviour and record that plan 27-04 inverted it.
`AR-27-02` — SUPERSEDED, re-decided on evidence: not adopted, because it was load-bearing only while
the primary rule could not fire. State the bound this leaves: no independent backstop behind the
cookie rules on that path, and what adopting it would have cost (a broad widening re-measured against
the WR-01 corpora).
`AR-27-03` — unchanged.
`AR-27-04` — NEW. The `Host:` header value and the sibling `SiteMapEntry.url` field reach a backend
un-anonymised under STRICT on the serialized emission shape. Quote plan 27-05's probe output. State
the two measured reasons it was excluded from the code change: the bounded de-anonymisation map, and
the sibling `url` field that would leave a header-only fix reading as closed while it is not. Severity
medium; disposition left for the checkpoint in task 3 to fill in.

Also add, in the same register, the auth-header outcome plan 27-05 measured and closed, cited by
symbol and test — it is on the same path and a reader of this row must not have to reconstruct it.

Recompute the frontmatter `threats_open` from the register rather than editing the digit: define and
run a command that counts rows whose status column is not `closed` and whose severity is `high` or
`critical`, quote the command and its output in the task, and write the computed value. Set `status`
to what that computation supports. Add a fourth Security Audit Trail row, dated, naming the second
reopening and its closure, so the interval during which the frontmatter overclaimed is visible in the
record rather than erased from it.

Extend the two-clause process rule with a third clause, because this phase produced the lesson: a
control verified at the site it was written for must also be compared against every SIBLING PATH that
consumes the same downstream weakness, and the paths compared must be ENUMERATED BY MEASUREMENT with
the count recorded — an unenumerated set of paths is how a control passes at one site while its
sibling leaks.
  </action>
  <acceptance_criteria>
    - `git diff HEAD -- .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | grep -c '^-[^-]'` returns a number equal to the count of frontmatter lines and register cells deliberately replaced, and the executor enumerates each removed line in the task output with the reason it was replaced. No removed line falls inside the `## Reopening — 2026-08-24` section or inside clauses (1), (2) or (3) of T-26-02-01.
    - The first reopening's heading is never REMOVED: `git diff HEAD -- .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | grep '^-[^-]' | grep -c 'Reopening — 2026-08-24'` returns `0`. Scoped to removed lines on purpose — clause (4) is expected to CITE that heading, so a whole-diff count would forbid the very cross-reference this correction needs.
    - The `threats_open` value written equals the output of the counting command, and both the command and its output are quoted in the task output and in the SUMMARY.
    - `grep -c 'AR-27-04' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` is at least `1`, and the surrounding text contains the quoted probe output rather than a paraphrase.
    - The record cites the shipped symbols and the gating tests by name, and each citation was re-read at source in this task — the observed lines are quoted in the task output.
    - The third process-rule clause is present and states the sibling-path-enumeration lesson.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && git diff HEAD -- .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | grep -c 'Reopening — 2026-08-24'</automated>
  </verify>
  <done>T-26-02-01 carries a fourth clause beside its unedited first three; AR-27-01 is reclassified, AR-27-02 superseded, AR-27-04 added with quoted evidence; the frontmatter counter is computed rather than written; the audit trail records the second reopening; the process rule gains the sibling-path clause.</done>
  <reversibility rating="reversible">Markdown record edits under git; revertible by a revert.</reversibility>
</task>

<task type="auto">
  <name>Task 2: CONCERNS.md and the milestone audit — a second amendment and a dated correction</name>
  <files>.planning/codebase/CONCERNS.md, .planning/v0.10.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - `.planning/codebase/CONCERNS.md` — the whole "Redaction regex coverage gaps" section, including the W-A entry and its existing `AMENDMENT 2026-08-24 (Phase 27, plan 27-03)` block, and the "Redaction body-stage bounds and the unbounded header stage" section, whose unbounded-header-stage residual the new fragments run inside.
    - `.planning/v0.10.0-MILESTONE-AUDIT.md` lines 180-245 — the PRIV-05 gap narrative, the `Remedy` paragraph and the whole `### Closure note — 2026-08-24` section including its final append-only paragraph.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-05-SUMMARY.md` — the pinned inventory count, the tripwire bounds and the probe output, all of which the amendments cite.
  </read_first>
  <action>
`CONCERNS.md`: append a SECOND dated amendment to the W-A entry, below the existing one, leaving the
existing one byte-identical. Four points, in order.
(1) The 2026-08-24 amendment fixed the scope of a claim about which NAMES the cookie rules match, and
was silent about which PATHS consult them at all — so it was accurate and still left a canonical-name
leak standing on 10 raw-HTTP emission sites.
(2) What that leak was and how it was found, in one paragraph, with the root cause named.
(3) What now holds and at what scope: the boundary fragments, which rules carry them, the pinned
emission-site inventory that bounds the claim, and the two tripwires with their stated bounds.
(4) What is still open on this path: AR-27-04, and the vendor auth-header gap this entry has always
carried, which the boundary change does not touch.
Also update this entry's `- **Files:**` line to name the three new fragments, and note in the
body-stage-bounds section that the recomposed rules still run inside the unbounded, un-deadlined
header stage recorded there — the boundary change does not alter that residual.

`v0.10.0-MILESTONE-AUDIT.md`: append a dated correction BELOW the existing closure note, adding no
`-` line anywhere in the file. State: that the closure note's body was carefully scoped but its
HEADING reads as closing the PRIV-05 requirement gap; that PRIV-05 remained refuted on the serialized
emission path for the interval between that note and plan 27-04, with both dates; what the leak was
and that it was strictly broader than the variant-spelling defect the note describes; what closed it,
cited by symbol, test and pinned inventory count; and that AR-27-04 remains open on the same path.
Close with the same append-only statement the existing note ends with: the verdict, `scores:`, `gaps:`
and `REQUIREMENTS.md` are untouched and the next audit re-derives its own verdict.
  </action>
  <acceptance_criteria>
    - `git diff HEAD -- .planning/v0.10.0-MILESTONE-AUDIT.md | grep -c '^-[^-]'` returns `0` — strictly append-only, the same gate plan 27-03 met.
    - `git diff HEAD -- .planning/codebase/CONCERNS.md | grep -c '^-[^-]'` returns exactly the number of lines the `- **Files:**` line update replaces, the executor names them in the task output, and none falls inside the existing `AMENDMENT 2026-08-24` block.
    - `grep -c 'AMENDMENT' .planning/codebase/CONCERNS.md` returns at least `2`.
    - Both amendments cite the pinned emission-site count and both tripwire names, and neither restates the scope more widely than the paths this phase measured.
    - Both amendments name AR-27-04 as still open.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && git diff HEAD -- .planning/v0.10.0-MILESTONE-AUDIT.md | grep -c '^-[^-]' | grep -qx 0 && echo APPEND_ONLY_OK</automated>
  </verify>
  <done>`CONCERNS.md` carries a second W-A amendment covering paths rather than names; the milestone audit carries a dated correction that qualifies its closure heading without removing a single line; both cite the pinned count and both name AR-27-04.</done>
  <reversibility rating="reversible">Markdown record edits under git.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Maintainer decision — AR-27-04's disposition, and read the amended records against source</name>
  <files>.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md</files>
  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` as amended by task 1.
    - `.planning/v0.10.0-MILESTONE-AUDIT.md` and `.planning/codebase/CONCERNS.md` as amended by task 2.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-05-SUMMARY.md` — the quoted probe output for `Host:` and `url`.
  </read_first>
  <decision>Disposition of AR-27-04 — the `Host:` header value and the sibling `SiteMapEntry.url` field reaching an AI backend un-anonymised under STRICT on the serialized emission path — and a read-back of the three amended records against the shipped source.</decision>
  <context>
Measured by plan 27-05 against the compiled classes on JDK 21: under STRICT, the `Host:` header value
inside the raw HTTP message AND the sibling `SiteMapEntry.url` field both reach the payload verbatim
through `proxy_http_history`, `proxy_http_history_regex`, `site_map`, `site_map_regex` and
`scanner_issues`. Quote that output when presenting this.

It was excluded from this phase's code change for two measured reasons, not because it looked hard:
`anonymizeHost` writes into a de-anonymisation map that `RedactionHostMapBoundTest` exists to bound,
and firing it per raw message on every `site_map` result is an unmeasured load change on that bound;
and `SiteMapEntry.url` (`Serialization.kt:79`) carries the same host verbatim with no
`maybeAnonymizeUrl` in front, so anonymising only the header produces a control that reads as closed
and is not.

Why it matters: STRICT's stated promise to the user is host anonymisation, so this is a gap between a
shipped promise and shipped behaviour on a 1.0.0 release. It is outside PRIV-05's wording, which is
about cookie values — which is exactly why it needs a decision rather than an assumption.

**Read-back, to be confirmed item by item against the shipped source rather than any SUMMARY:**
that T-26-02-01's clauses (1) to (3) and the `## Reopening — 2026-08-24` narrative are unedited; that
clause (4) says what happened without softening it; that AR-27-01 reads as a reclassified live finding
and not as a residual; that the `threats_open` value matches the quoted computation; and that no
sentence in any of the three records claims a scope wider than the serialized emission path, the
cookie-header class and the exact-name auth-header class.
  </context>
  <options>
    <option id="accept-residual">
      <name>Accept as a recorded residual at medium severity</name>
      <pros>Keeps this gap-closure phase scoped to what it measured and gated. The finding is written down with its evidence, so the next audit inherits a measurement rather than a silence. Cheapest path to sealing the phase.</pros>
      <cons>STRICT's user-facing privacy claim stays broader than STRICT's behaviour until a later phase. Requires naming the promise-vs-behaviour gap in the user-facing privacy documentation, or the record simply relocates the overclaim into the docs.</cons>
    </option>
    <option id="follow-up-phase">
      <name>Open a follow-up phase to close it properly</name>
      <pros>Fixes the header rule and the `url` field together, so the control cannot read as closed while half of it leaks. The de-anonymisation map load question gets measured before the change rather than after.</pros>
      <cons>Leaves the gap live in the shipped release for the duration of that phase. Adds a phase to the milestone.</cons>
    </option>
    <option id="close-now">
      <name>Close it now as an in-phase addition</name>
      <pros>No interval during which a shipped STRICT promise is untrue. Reuses the same boundary fragments already proven and gated in plans 27-04 and 27-05.</pros>
      <cons>The map-load question gets measured under time pressure, and the `url` field must be fixed in the same change or the control is partial. This is plan-set revision work, not something to improvise inside a checkpoint.</cons>
    </option>
  </options>
  <action>
Present the decision, the quoted evidence and the read-back list above. Record the chosen option
verbatim beside AR-27-04 in `26-SECURITY.md`, with the maintainer's reason and the date, and repeat it
in the SUMMARY. Record each read-back item as confirmed or as a correction the maintainer asked for.

If the maintainer's answer changes AR-27-04's severity, recompute `threats_open` with the same
documented command from task 1 and rewrite the value from that output. If `close-now` is chosen, stop
and revise the plan set rather than editing source inside this checkpoint.
  </action>
  <resume-signal>Reply with `accept-residual`, `follow-up-phase` or `close-now`, plus a one-line reason and any read-back corrections.</resume-signal>
  <acceptance_criteria>
    - The chosen option is recorded verbatim beside AR-27-04 in `26-SECURITY.md`, with the maintainer's reason and the date, and repeated in the SUMMARY.
    - If option (a) is chosen, the follow-up documentation item is captured as a concrete backlog entry naming the file to change; if (b), the follow-up phase is captured with its scope named; if (c), the plan set is revised rather than the fix being improvised inside this checkpoint.
    - `threats_open` is re-derived by the documented command after any severity change, and the new output is quoted.
    - The read-back confirmations are recorded item by item, each as confirmed or as a correction the maintainer asked for.
    - The Sign-Off boxes in `26-SECURITY.md` reflect the state after this checkpoint, not before it.
  </acceptance_criteria>
  <verify>
    <human-check>Maintainer has chosen AR-27-04's disposition and confirmed each read-back item against the shipped source.</human-check>
    <automated>cd /Users/six2dez/Tools/burp-ai-agent && grep -c 'AR-27-04' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md</automated>
  </verify>
  <done>AR-27-04 carries a dated maintainer disposition with its reason; the read-back is recorded item by item; `threats_open` matches its computation; the sign-off reflects the post-checkpoint state.</done>
  <reversibility rating="costly">A recorded disposition on a security register for a shipped 1.0.0 release. Revertible as text, but it becomes the answer a future audit inherits, so getting it wrong costs a re-audit rather than a revert.</reversibility>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking severity: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shipped source → security register | What the code does and what the register claims must agree, or the register misleads the next audit |
| Security register → future auditor / maintainer | A record read as authority; an overclaim here propagates into decisions elsewhere |
| Phase artifacts → milestone verdict | A phase must not grade its own homework into a milestone-level verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-06-01 | Repudiation | `26-SECURITY.md` frontmatter `threats_open` / `status` | high | mitigate | The counter is COMPUTED by a documented command whose output is quoted, never hand-written. A hand-written counter is how the previous overclaim survived review. A fourth audit-trail row makes the overclaimed interval visible instead of erased. |
| T-27-06-02 | Tampering | the reopening narrative and clauses (1)-(3) of T-26-02-01 | high | mitigate | Append-and-amend, prohibition-enforced and gated: the diff must contain no line — added or removed — matching the reopening heading, and no removed line inside the preserved clauses. Each removed line is enumerated with its reason. |
| T-27-06-03 | Tampering | `v0.10.0-MILESTONE-AUDIT.md` | high | mitigate | Strictly append-only: the diff must show zero removed lines, the identical gate plan 27-03 met. Frontmatter, `scores:` and `gaps:` untouched; `REQUIREMENTS.md` untouched. |
| T-27-06-04 | Repudiation | a measured leak recorded as moot or theoretical | high | mitigate | AR-27-04 must be recorded as an open finding with its probe output quoted and a maintainer disposition, prohibition-enforced. Recording a measured leak as anything less is the overclaim class the phase exists to stop. |
| T-27-06-05 | Repudiation | a record closed on a SUMMARY's say-so | high | mitigate | The task re-reads the shipped source and re-runs the named tests, and quotes the observed output. Closing on a document's say-so is the precise failure the v0.10.0 audit found and this phase reproduced once. |
| T-27-06-06 | Information Disclosure | STRICT's user-facing promise vs measured behaviour | medium | transfer | If the maintainer picks option (a) at the checkpoint, the promise-vs-behaviour gap is named in the user-facing privacy documentation. An overstated at-rest/privacy claim is a control surface in this project, per the `26-SECURITY.md` precedent on `SecretCipher`. |
| T-27-06-07 | Spoofing | a checkpoint auto-approved by the configured run mode | medium | mitigate | The configured mode auto-selects blocking checkpoints, so the substantive checks here are automated gates rather than checkpoint prose. If the checkpoint is auto-approved, the diff gates, the computed counter and the quoted probe output still hold, and the SUMMARY records that the disposition was auto-selected rather than maintainer-chosen. |
| T-27-06-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package-manager install task; no dependency added or changed. |
</threat_model>

<artifacts_this_phase_produces>
No source symbols. Record artifacts and identifiers:

**`.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md`**
- T-26-02-01 clause (4) — the second reopening and its closure
- `AR-27-01` — reclassified from accepted residual to live finding, closed by plan 27-04
- `AR-27-02` — superseded, with the single-point-of-control bound stated
- `AR-27-04` — NEW open finding: `Host:` header value and `SiteMapEntry.url` un-anonymised under STRICT on the serialized emission shape
- a fourth Security Audit Trail row
- a third clause on the audit-scope process rule: sibling paths must be enumerated by measurement and the count recorded

**`.planning/codebase/CONCERNS.md`**
- a second dated W-A amendment, path-scoped rather than name-scoped
- the `- **Files:**` line extended with the three boundary fragments
- a note in the body-stage-bounds section that the recomposed rules still run inside the unbounded header stage

**`.planning/v0.10.0-MILESTONE-AUDIT.md`**
- a dated, append-only correction qualifying the closure note's heading

**Threat IDs introduced:** T-27-06-01 … T-27-06-07
</artifacts_this_phase_produces>

<verification>
- `git diff HEAD -- .planning/v0.10.0-MILESTONE-AUDIT.md | grep -c '^-[^-]'` returns `0`.
- `git diff HEAD -- .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md | grep -c 'Reopening — 2026-08-24'` returns `0`.
- `git diff --stat HEAD -- .planning/REQUIREMENTS.md` produces no output.
- Every removed line in `26-SECURITY.md` and `CONCERNS.md` is enumerated in the task output with the reason it was replaced.
- The `threats_open` computation command and its output are quoted, and the written value matches.
- The AR-27-04 probe output appears in the record verbatim, not paraphrased.
- The checkpoint outcome — maintainer-chosen or auto-selected by the run mode — is stated plainly in the SUMMARY.
</verification>

<success_criteria>
- The records say what happened, twice over, without losing either miss.
- `AR-27-01` is no longer described as a residual anywhere in the record set.
- `AR-27-02` is re-decided on evidence with its remaining bound stated.
- `AR-27-04` is an open finding with quoted evidence and a dated disposition.
- `threats_open` matches a quoted computation over the register.
- The milestone-audit heading is qualified without a single line removed from the file.
- No sentence in any record claims a scope wider than the serialized emission path, the cookie-header class and the exact-name auth-header class, and every scope claim cites the pinned inventory count that bounds it.
- `27-HUMAN-UAT.md` test 2 (re-test after the fix lands) becomes actionable against a build containing plans 27-04 and 27-05.
</success_criteria>

<output>
Create `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-06-SUMMARY.md` when done.
</output>
