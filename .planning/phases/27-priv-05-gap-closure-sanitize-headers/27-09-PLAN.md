---
phase: 27-priv-05-gap-closure-sanitize-headers
plan: 09
type: execute
gap_closure: true
wave: 3
depends_on: ["27-07", "27-08"]
autonomous: true
requirements: [PRIV-05]
files_modified:
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
  - .planning/v0.10.0-MILESTONE-AUDIT.md
  - .planning/codebase/CONCERNS.md
  - .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md
  - .planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/WINDOWS.md

estimate:
  tokens: 90000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "T-26-02-01 carries a fifth clause recording the THIRD refutation — the COOKIE-typed parameter carrier — and clauses (1)(2)(3)(4) survive as an exact byte prefix. The threat has now been closed wrongly three times and that fact is the most instructive artifact in the register."
    - "AR-27-06 is DEFINED for the first time: on both MCP parameter shapes redactIfNeeded cannot recover a missed cookie, so sanitizeParameters is the SOLE control — the exact mirror of AR-27-05's no-backstop bound on the header-map shape."
    - "threats_open is COMPUTED from the rows, and the counter's POPULATION is stated. If any finding from plans 27-07/27-08 is open at or above the blocking severity and sits outside the counter's population, the counter is amended or the finding is given an id inside it — a counter that reads 0 while a high finding is open is the overclaim this file exists to stop."
    - "Standing rule clause (iv) records what the three prior mechanisms had in common — each keyed on a RENDERING of the data — and the property that broke the pattern: a control keyed on HttpParameterType and never on a rendered string."
    - "Every prior narrative survives verbatim. Under the ROBUST removed-line form, the milestone audit and REQUIREMENTS.md carry zero removed lines, and any removed line elsewhere is a deliberate, named replacement recorded in WINDOWS.md rather than hidden."
    - "REQUIREMENTS.md is UNTOUCHED. The PRIV-05 tick remains the milestone owner's to re-derive; a phase that grades its own homework produces the record this closure is repairing."
    - "The unanswered human items are carried forward legible as unanswered, not auto-approved into the appearance of decisions."
  artifacts:
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md — clause (5), AR-27-06 (and AR-27-07/AR-27-08 as the 27-08 measurements require), audit-trail row 5, standing rule clause (iv), computed threats_open with its population stated"
    - ".planning/v0.10.0-MILESTONE-AUDIT.md — a second append-only correction, nothing above it edited"
    - ".planning/codebase/CONCERNS.md — the third W-A amendment"
    - ".planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md — the carried-forward and newly-raised human items"
    - ".planning/WINDOWS.md — one entry per falsified premise from waves 7-9"
  key_links:
    - "27-07-SUMMARY + 27-08-SUMMARY measurements -> the numbers every clause in 26-SECURITY.md is bounded by"
    - "27-08 measurement 2 severity -> AR id, register population and threats_open — the link where a wrong counter population would hide a high finding"
    - "27-VERIFICATION-2.md human_verification items 1-4 -> 27-HUMAN-UAT.md, carried forward unanswered"
---

<objective>
Repair the records for the THIRD refutation of PRIV-05, at the width the evidence supports and no wider.

T-26-02-01 has now been closed wrongly three times: on header NAMES, on emission PATHS, and on field
TYPES. The register's own standing rule gained a clause after each of the first two. This plan adds the
third, and it records something the first two could not: what all three failed mechanisms had in common.

Purpose: leave a record that is instructive rather than merely accurate. A threat wrongly closed three
times, with the pattern named, is worth more to a future auditor than a fourth clean closure.

Output: an amended threat register with a defined AR-27-06, a computed counter whose population is
stated, a fourth standing-rule clause, an append-only milestone-audit correction, a third CONCERNS
amendment, carried-forward human items, and a broken-windows entry per falsified premise.

**This plan writes no code and closes no requirement.** `REQUIREMENTS.md` is under a standing
prohibition and is not touched. The PRIV-05 tick at `REQUIREMENTS.md:23` is wrong for the third time and
remains the milestone owner's to re-derive.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-2.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-07-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-08-SUMMARY.md

The two SUMMARYs supersede anything this plan says about what was built or measured. Every number
written into a record here is READ FROM THEM, never carried over from this plan's prose.
</context>

<records_protocol>
Non-negotiable. Every one of these was learned in this repository, most of them in this phase.

- **APPEND-AND-AMEND ONLY.** All prior narratives survive verbatim. Where a row must be extended, the
  OLD row body must remain an exact BYTE PREFIX of the new one. `WINDOWS.md` entry 17 records why:
  the whole T-26-02-01 register row is ONE physical markdown line, so appending a clause necessarily
  rewrites it and it shows as a removed line. The intent is verified by the byte-prefix check, not by
  a removed-line count.
- **ROBUST removed-line form, always:**
  `git diff HEAD --unified=0 -- <file> | grep '^-' | grep -v '^--- '`.
  The naive `git diff HEAD -- <file> | grep -c '^-[^-]'` is a FALSE ZERO on markdown BULLET lines —
  a removed line beginning `- ` renders as `--`, which `[^-]` excludes (`WINDOWS.md` entry 18). Do not
  use the naive form anywhere in this plan, including in prose you write into a record.
- **`threats_open` is COMPUTED, never hand-written.** Use the `awk` command quoted verbatim in
  `26-SECURITY.md`'s own frontmatter comment. A hand-written counter is how the 2026-08-24 overclaim
  survived review.
- **`REQUIREMENTS.md` IS NOT TOUCHED.** Zero added lines, zero removed lines, asserted.
- **No claim wider than its evidence.** The `high` prohibition and the tree-wide-scope-claim
  prohibition are both in force. THE PROHIBITED PHRASE, defined once here and deliberately not spelled
  again anywhere in this plan: the two words `whole` and `codebase` adjacent, in that order. It must
  appear 0 times in every file this plan touches, and a negated or reworded form of the same claim
  fails too. This plan states the phrase by construction rather than verbatim precisely so that its own
  prose cannot become the thing the gate finds — the self-invalidating-gate defect `WINDOWS.md` records.
  Every closure sentence must instead name the carrier, shape or path it holds on.
- **Do not overstate severity.** `request_parse` / `params_extract` take CALLER-SUPPLIED content, so
  they echo a cookie the caller already holds rather than exfiltrating Burp-held traffic — the same
  mitigating property the original PRIV-05 finding had. `BountyPromptTagResolver` and the issue-detail
  route are the ones that would carry Burp-held data. State each at its measured severity.
- **English only** (AGENTS.md, non-negotiable).
</records_protocol>

<measured_baselines>
Measured on this working tree at commit `389cbbd`, 2026-08-25. Re-measure each before writing its gate.

| # | Measurement | Value TODAY | Expected AFTER |
|---|-------------|-------------|----------------|
| R1 | `26-SECURITY.md` line count | **443** | greater, by append only |
| R2 | T-26-02-01 register row, single physical line at `26-SECURITY.md:69`, character length | **11400** | greater; old body an exact byte prefix |
| R3 | Clauses present in T-26-02-01 | **(1)(2)(3)(4)** | (1)(2)(3)(4)(5) |
| R4 | `AR-27-` ids defined anywhere under `.planning/` | **AR-27-01 … AR-27-05** | AR-27-06 defined; AR-27-07 / AR-27-08 as the 27-08 measurements require |
| R5 | `threats_open` frontmatter value | **0** | COMPUTED; see task 1 criterion 4 for the population trap |
| R6 | Register rows matched by the documented `awk` population `/^\| T-26-/` | **46 rows, 46 closed** | unchanged unless a new row is added |
| R7 | Standing-rule clauses | **(i)(ii)(iii)** | (i)(ii)(iii)(iv) |
| R8 | Security Audit Trail rows | **4** | 5 |
| R9 | `v0.10.0-MILESTONE-AUDIT.md` removed lines under the robust form | **0** | 0 |
| R10 | `REQUIREMENTS.md` added + removed lines | **0 + 0** | 0 + 0 |
| R11 | `WINDOWS.md` frontmatter `open_count` / `total_count` | **18 / 18** | greater by the number of falsified premises in waves 7-9 |
| R12 | `ROADMAP.md` plan checkbox for `27-06-PLAN.md` | **`- [x]`** — ALREADY CORRECTED since `27-VERIFICATION-2.md` was written, which recorded it as unticked. The verifier's bookkeeping warning is STALE on this point; do not "fix" it back | unchanged |
| R13 | `ROADMAP.md` phase 27 plans counter | **REPAIRED at plan time** to `9 plans — 6 executed, 3 planned`. It read `6/6 plans executed (3/6 executed)` — two contradictory counters — which is itself a correction of the `5/6` the verifier recorded | verify it still matches the PLAN files on disk; re-measure, do not assume |
| R14 | `STATE.md` `state_head` | **`190735d`** (the 27-05 merge), stale by later merges | current |
| R15 | `.planning/BACKLOG.md` exists | **no** — `T-27-06-06` therefore lives only inside a phase-26 register and a phase-27 SUMMARY | unchanged, but the risk restated |

R2 and R3 together are the byte-prefix gate: they cannot both be satisfied by a rewrite.
</measured_baselines>

<tasks>

<task type="auto">
  <name>Task 1: 26-SECURITY.md — clause (5), AR-27-06, the counter's population, and standing rule (iv)</name>

  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` IN FULL (443 lines). In
      particular: the frontmatter comment carrying the `awk` counter command and its stated population;
      line 69, the T-26-02-01 row with clauses (1)-(4); lines 117-129, the Accepted Risks Log through
      AR-27-05; lines 133-271, the open-findings evidence sections whose structure AR-27-06 mirrors;
      lines 272-297, the Security Audit Trail and its two notes; lines 369-412, standing rule clauses
      (i)(ii)(iii) and their worked examples.
    - `27-VERIFICATION-2.md` — the `gaps:` block, the Failing Path trace, and truth rows 1 and 4. Row 4
      is the sentence to build clause (5) against: the header-map bound "is correct for the HEADERS
      field — it is silent about the `parameters` field beside it".
    - `27-07-SUMMARY.md` and `27-08-SUMMARY.md` — every number, name and severity written below is read
      from these, not from any PLAN.
  </read_first>

  <files>.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md</files>

  <action>
    Amend T-26-02-01 by APPENDING clause (5) to the existing single-line row. Clause (5) records the
    third refutation and must state, each bounded to its evidence: that Burp parses `Cookie:` into
    COOKIE-typed parameters and `HttpRequest.parameters()` returns them; that four MCP producers emitted
    those values with no control in front and no rule behind; that in `request_parse` the leak sat three
    lines below `sanitizeHeaders` in the same JSON object, so the control was defeated on its own output;
    the mitigating property, stated because it is real — both tools take caller-supplied content and
    therefore ECHO a cookie the caller already holds; and what closed it, cited by symbol, test and
    pinned count from the two SUMMARYs. Preserve clauses (1)(2)(3)(4) as an exact byte prefix.

    Note within clause (5) that clause (3)'s line citations are STALE — the verifier found `Redaction.kt:158`
    and `:91` now land inside comments after wave 4 moved the declarations. Clause (3) is preserved
    verbatim by design and clause (4) supplies current numbers, but nothing today tells a reader those
    numbers have rotted. One sentence fixes that without editing clause (3).

    DEFINE **AR-27-06** in the Accepted Risks Log and give it an evidence section beneath, mirroring
    AR-27-05's structure exactly. Its content: on BOTH MCP parameter shapes — the serialized
    `ParsedParam` object and the `params_extract` line — `redactIfNeeded` cannot recover a missed
    cookie. The serialized shape carries no line boundary at all, so neither branch of
    `logicalLineHeaderRule` can fire, and `jsonSecretKeyRegex` keys on the JSON key, which here is the
    literal field name `value`. The line shape carries a real newline but matches no cookie rule:
    `cookieTypedParamRegex` is keyed to the prompt path's `name=value (TYPE)` suffix. Therefore
    `sanitizeParameters` is the SOLE control on this carrier. State it as a NO-BACKSTOP BOUND and NOT a
    live leak — all four producers route through the sanitizer after plan 27-07 and the producer-ownership
    pin fails if one stops. What is open is the absence of a second control if the sanitizer is narrowed,
    bypassed at a new producer, or omitted from a future one. Severity MEDIUM, on the same reasoning
    AR-27-05 carries, and say so.

    Record the 27-08 measurements as their own ids at their MEASURED severity, reading both the result
    and the reasoning from `27-08-SUMMARY.md`. **AR-27-07** for the non-cookie parameter types
    (explicitly outside PRIV-05's cookie wording; note WR-01's measured 32 false positives as the
    recorded cost of the widening not adopted). **AR-27-08** for the transitive issue-detail carrier —
    a COOKIE-typed injection point's value reaching `scanner_issues` through `AuditIssue.detail()` — with
    its source-cited reachability statement carried over verbatim. AR-27-08 carries BURP-HELD data,
    which is strictly worse than the caller-echo property of the two MCP tools; if it is also mode-gated
    or latent, that belongs in the same sentence. If a measurement showed no leak, say that plainly and
    keep the id defined with the negative result — a measured negative is a record worth keeping.

    Add Security Audit Trail row 5, and a note beneath it in the style of the existing two: the THIRD
    reopening of T-26-02-01 and its re-closure on the parameter carrier, with the counter recomputed
    from the rows rather than asserted.

    Add standing rule clause **(iv)**. Clauses (i)(ii)(iii) were each honoured in the round that
    followed them and the threat was still closed wrongly. Clause (iv) records why: **all three prior
    mechanisms keyed on a RENDERING of the data.** The ownership test keys on how a matcher is SPELLED;
    the emission inventory keys on how an emission is SHAPED; `cookieTypedParamRegex` keys on how a
    parameter is FORMATTED. A rendering-keyed mechanism can only see renderings it already knows, so
    each was complete for its own axis and structurally incapable of seeing the next. Then record the
    property that broke the pattern, in the words plan 27-07 used because they are the accurate ones:
    the control is TYPE-KEYED, not SHAPE-KEYED — it reads `HttpParameterType` and never a rendered
    string, so a change to either MCP output format cannot defeat it. Add the operative instruction: an
    audit closing a data-disclosure threat must state whether its control keys on the data's SOURCE or
    on a RENDERING of it, and a rendering-keyed control may not be closed at a width wider than the
    renderings enumerated. Cite `CookieCarrierInventoryTest` as the source-keyed mechanism, with its
    measured accessor and site counts read from `27-08-SUMMARY.md` — and carry over its four named
    blind axes, because a clause that presents it as complete would be clause (iv) committing the very
    error clause (iv) describes.

    Recompute `threats_open` with the `awk` command quoted in the frontmatter and write the OUTPUT.
    Update the frontmatter comment to state the command's POPULATION explicitly — it matches rows
    beginning `| T-26-`, so any finding recorded only in the Accepted Risks Log is OUTSIDE the count.
    That bound exists today and is unstated, which is precisely the "gate counting the wrong population"
    anti-pattern this phase has already paid for. See acceptance criterion 4 for what to do if a
    measurement lands at or above the blocking severity.
  </action>


  <verify>
    <automated>SEC=.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md; awk -F'|' '/^[|] T-26-/ { sev=$5; st=$(NF-1); gsub(/[ *`]/,"",sev); gsub(/[ *`]/,"",st); if (st != "closed") if (sev == "high" || sev == "critical") c++ } END { print "threats_open="c+0 }' $SEC; echo "AR-27-06=$(grep -c 'AR-27-06' $SEC)"; echo "clause5=$(grep -c '[*][*](5)' $SEC)"; PH="whole"" ""codebase"; echo "prohibited_phrase=$(grep -c "$PH" $SEC)"; echo "removed=$(git diff HEAD --unified=0 -- $SEC | grep '^-' | grep -v '^--- ' | wc -l)"</automated>
  </verify>

  <acceptance_criteria>
    1. BYTE-PREFIX GATE. The OLD T-26-02-01 row body is an exact byte PREFIX of the new one. Verify by
       extracting the row from `git show HEAD:.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md`
       and from the working tree, and asserting the prefix relation in a script rather than by eye.
       Record both character lengths (baseline R2 measured **11400**) and the PASS/FAIL of the prefix
       check in the SUMMARY. `WINDOWS.md` entry 17 records why a removed-line count cannot substitute
       for this: the whole row is one physical markdown line.
    2. Clauses `(1)` `(2)` `(3)` `(4)` `(5)` are all present in the row (baseline R3 measured four), and
       the `## Reopening — 2026-08-24` section is byte-unchanged.
    3. `AR-27-06` is defined in the Accepted Risks Log AND has an evidence section beneath, mirroring
       AR-27-05. `AR-27-07` and `AR-27-08` are defined at the severity and with the reasoning read from
       `27-08-SUMMARY.md`. Baseline R4 measured AR-27-01 … AR-27-05 as the defined set.
    4. **THE COUNTER-POPULATION GATE — read this before writing `threats_open`.** Recompute with the
       documented `awk` command and write its OUTPUT. Then check its POPULATION: that command matches
       rows beginning `| T-26-` only, so a finding recorded solely in the Accepted Risks Log is OUTSIDE
       the count. If ANY finding from plans 27-07 or 27-08 is OPEN at or above the `high` blocking
       severity — the likely candidate is AR-27-08 if 27-08's measurement 2 found a live leak — then a
       counter reading `0` would be an overclaim of exactly the kind this file exists to stop. In that
       case do ONE of the following and record which and why: (a) give the finding a register ROW with
       an id inside the counter's population, or (b) amend the counter command AND its frontmatter
       comment so the population matches the claim the counter makes, then recompute. Do NOT hand-write
       a number, and do NOT leave the population unstated as it is today. If every 27-07/27-08 finding
       is below `high`, state that explicitly with each finding's severity listed, so the `0` is
       attributable rather than merely asserted.
    5. Standing rule clause `(iv)` is present (baseline R7 measured three clauses), names the
       rendering-versus-source distinction, cites `CookieCarrierInventoryTest` with its measured accessor
       and site counts read from `27-08-SUMMARY.md`, AND carries over that test's four named blind axes.
       A clause presenting the new mechanism as complete is itself the error clause (iv) describes.
    6. Security Audit Trail has 5 rows (baseline R8 measured 4) and the new row's counter equals the
       command output from criterion 4.
    7. ROBUST removed-line check on this file: every removed line is either the T-26-02-01 row (covered
       by criterion 1) or a deliberate, named replacement listed in the SUMMARY and recorded in
       `WINDOWS.md`. Use `git diff HEAD --unified=0 -- <file> | grep '^-' | grep -v '^--- '`; the naive
       `grep -c '^-[^-]'` form is prohibited.
    8. Grepping `26-SECURITY.md` for THE PROHIBITED PHRASE (constructed per `<records_protocol>`, not
       copied from this plan) returns `0`, and no negated or reworded form of that claim appears either.
       Build the pattern in the shell rather than pasting it, so this criterion cannot be satisfied or
       broken by the plan text itself. State the searches you ran and bound the claim to them.
    9. Every clause added names the carrier, shape or path it holds on. No sentence asserts that PRIV-05
       is satisfied, or that the cookie class is covered. Read the added text back in the SUMMARY and
       state, clause by clause, what each is bounded to.
  </acceptance_criteria>

  <done>
    T-26-02-01 records its third refutation with all four prior clauses byte-prefix intact; AR-27-06 is
    defined with an evidence section; the 27-08 measurements are filed at their measured severities; the
    counter is computed AND its population is stated so it cannot read zero over a high open finding;
    and standing rule (iv) names the rendering-versus-source distinction that the three prior clauses
    could not see.
  </done>

  <reversibility rating="reversible">
    Append-only record edits under `.planning/`. No code, no shipped artifact, no user-visible change.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: The documents a reader actually opens — milestone audit, CONCERNS, coverage note</name>

  <read_first>
    - `.planning/v0.10.0-MILESTONE-AUDIT.md` from the PRIV-05 gap narrative through to end of file
      (roughly `:230` onward). Read the existing closure note AND the `### Correction to the closure
      note above — 2026-08-24 (appended; nothing above is edited)` section. The second correction goes
      BENEATH that one, in the same voice and with the same append-only discipline. Note how the first
      correction distinguishes the note's BODY (carefully scoped) from its HEADING (not scoped) — the
      second correction has the same job one level out.
    - `.planning/codebase/CONCERNS.md`, the `W-A` entry and both existing amendments. `WINDOWS.md`
      entry 18 records that the previous amendment showed 2 removed lines under the robust diff — a
      genuine bullet replacement, recorded rather than hidden. If this amendment needs to replace a
      bullet, record that the same way; do not contort the text to make a counter read zero.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md` — the existing reasoned
      "No external API integration" declaration. It already satisfies the seal-time gate; it needs one
      appended sentence extending it to plans 27-07 … 27-09, not a rewrite.
    - `27-VERIFICATION-2.md`'s Requirements Coverage section — the sentence that `REQUIREMENTS.md:23`
      still carries `- [x] **PRIV-05**` and that the tick is now wrong for the third time.
    - `27-07-SUMMARY.md` and `27-08-SUMMARY.md` for every number and severity written here.
  </read_first>

  <files>
    .planning/v0.10.0-MILESTONE-AUDIT.md,
    .planning/codebase/CONCERNS.md,
    .planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md
  </files>

  <action>
    **Milestone audit — a SECOND append-only correction.** Append beneath the existing correction,
    editing nothing above it. It must record: that PRIV-05 was refuted a THIRD time, on the
    COOKIE-typed parameter carrier, after the first correction was written; the interval, dated, during
    which the record and the requirement tick were both wider than what shipped; the carrier and the
    four producers, cited by file; the mitigating property stated plainly — both MCP tools take
    caller-supplied content and echo a cookie the caller already holds, which is the same property the
    original PRIV-05 finding had and was treated as a blocker then; and what closed it, cited by
    symbol, test and pinned count from the SUMMARYs.

    Add the observation that makes this correction worth more than the first: the first correction
    distinguished a carefully-scoped BODY from an unscoped HEADING. The third refutation shows the
    same failure one level further out — every closure sentence in the register was correctly bounded,
    which is why gap 1 falsified no sentence of the record. What it falsified was the GOAL STATEMENT,
    which the records deliberately never restated. A record can be accurate clause by clause and still
    leave a requirement overstated, if nobody re-derives the requirement from the clauses.

    State explicitly that `REQUIREMENTS.md` is untouched by this phase and that the PRIV-05 tick
    remains the milestone owner's to re-derive. Do not re-derive it here. A phase that grades its own
    homework produces exactly the record this closure is repairing — the existing note says so and the
    new one must not contradict it.

    **CONCERNS.md — the third W-A amendment.** Four parts, in the voice of the existing two: what the
    third refutation was; that W-A's original reasoning (name-contains-`cookie` is a bounded and
    complete predicate) remains correct AND was never the question on this carrier, because a parameter
    has no header name to match; the new owner, cited by symbol; and the standing consequence — the
    W-A class is now "the same rule, mirrored into every path that consumes the data", and the
    mechanism that enumerates those paths is `CookieCarrierInventoryTest` with its bound carried over.
    Preserve both existing amendments verbatim.

    **COVERAGE.md — one appended sentence.** Extend the existing reasoned declaration to cover plans
    27-07 … 27-09 explicitly, since the seal-time gate re-runs its detector over the new PLAN bodies
    and those bodies name the `request_parse` / `params_extract` MCP tools repeatedly. Do not rewrite
    the declaration and do not fabricate a matrix for an integration that does not exist.
  </action>
  <verify>
    <automated>for f in .planning/v0.10.0-MILESTONE-AUDIT.md .planning/REQUIREMENTS.md; do echo "$f removed=$(git diff HEAD --unified=0 -- $f | grep '^-' | grep -v '^--- ' | wc -l | tr -d ' ')"; done</automated>
  </verify>

  <acceptance_criteria>
    1. `v0.10.0-MILESTONE-AUDIT.md` carries **zero** removed lines under the ROBUST form (baseline R9
       measured 0). The existing closure note and the existing 2026-08-24 correction are both
       byte-unchanged; the new correction sits beneath them.
    2. `REQUIREMENTS.md` has **zero** added and **zero** removed lines (baseline R10). The PRIV-05 tick
       is not altered, and the SUMMARY states that the prohibition was honoured and why.
    3. `CONCERNS.md` carries a THIRD W-A amendment with all four parts, and both existing amendments
       survive verbatim. If any line was removed, it is a deliberate bullet replacement, it is named in
       the SUMMARY, and it gets a `WINDOWS.md` entry — the same handling entry 18 gave the second
       amendment. Do not reword to make a counter read zero.
    4. `COVERAGE.md` gained a sentence naming plans 27-07 … 27-09; its existing declaration is
       byte-unchanged; no matrix is fabricated.
    5. Every severity written in these three files is read from `27-07-SUMMARY.md` or `27-08-SUMMARY.md`
       and matches it. State each severity and the SUMMARY line it came from.
    6. The caller-echo mitigating property appears wherever a severity for `request_parse` or
       `params_extract` appears, and the Burp-held-data property appears wherever
       `BountyPromptTagResolver` or the issue-detail route appears. Neither is stated without the other
       where both are relevant.
    7. No sentence added to any of these files asserts that PRIV-05 is satisfied or that the cookie
       class is covered. Read the added text back in the SUMMARY, bounded clause by clause.
  </acceptance_criteria>

  <done>
    The three documents a reader actually opens record the third refutation at the width the evidence
    supports; every prior narrative survives verbatim; the milestone audit is append-only with zero
    removed lines; and `REQUIREMENTS.md` is untouched with the tick left for its owner to re-derive.
  </done>

  <reversibility rating="reversible">
    Append-only record edits under `.planning/`. No code, no shipped artifact.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: Carry the unanswered items forward legible as unanswered</name>

  <read_first>
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md` in full (roughly 90
      lines) — its existing item format, and test 2, which is still pending.
    - `27-VERIFICATION-2.md` `human_verification:` block — four items. Item 1 (does Montoya's
      `parameters()` really yield COOKIE entries in a live Burp) and item 3 (the live end-to-end
      wave-4/5 confirmation) are both still unrun. Item 2 (the fix-or-accept disposition) has been
      ANSWERED by the maintainer's decision to fix — record the answer, do not re-raise it. Item 4
      (`T-27-06-06`: `README.md:247` and `SPEC.md:80,86` still state STRICT host anonymisation without
      AR-27-04's exclusion) is still unactioned.
    - `.planning/WINDOWS.md` frontmatter and table — the entry format, and `open_count` at 18.
    - `26-SECURITY.md`'s note that this repository has no `.planning/BACKLOG.md`, so `T-27-06-06` lives
      only in a phase-26 register and a phase-27 SUMMARY — "the two documents a docs maintainer is
      least likely to open".
    - `27-07-SUMMARY.md` and `27-08-SUMMARY.md` — every falsified premise they record becomes an entry.
  </read_first>

  <files>
    .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md,
    .planning/WINDOWS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>

  <action>
    **`27-HUMAN-UAT.md` — append, never rewrite.** Carry forward items 1, 3 and 4 unchanged in
    substance, each keeping its `why_human` reason so a reader can see why it has not been closed by
    argument. Record item 2's ANSWER — the maintainer chose to FIX rather than accept — with the date,
    and note that the fix landed in plans 27-07 and 27-08 rather than leaving the item open.

    Add the NEW human items this round produced, reading them from the two SUMMARYs. At minimum: a
    live-Burp confirmation that a cookie sentinel supplied to `params_extract` and `request_parse` now
    returns the marker in both fields rather than the value; and, if 27-08's measurement 2 found the
    issue-detail route reachable, a live confirmation of that route with the exact scan preconditions
    its reachability statement names. Each item states what to run, what is expected TODAY, what is
    expected AFTER, and why a human is needed.

    State plainly, in the file, that these items are UNANSWERED and that nothing in this phase should
    be read as having answered them. `.planning/config.json` carries `mode: yolo`, which auto-selects
    blocking checkpoints; `26-SECURITY.md` already records one disposition as auto-selected rather than
    maintainer-chosen and instructs future auditors to read it as a recorded default. This phase
    therefore raised no checkpoint that the run mode would rubber-stamp — the items live here, legible
    as open, instead. Write that reasoning into the file so a later reader does not mistake the absence
    of checkpoints for an absence of open questions.

    Re-state the `T-27-06-06` loss risk: with no `.planning/BACKLOG.md`, that item is reachable only
    from two documents a docs maintainer rarely opens. Do not silently create a backlog file; note the
    risk where the item is now also visible.

    **`WINDOWS.md` — one entry per falsified premise from waves 7-9.** Read them from the SUMMARYs; do
    not invent any and do not omit any. Every acceptance criterion that proved unsatisfiable as
    written, every red probe that failed a different assertion than its plan predicted, and every
    measured baseline that differed from this plan set's table gets an entry with its measured
    evidence. Use `gsd-tools windows add` if available so the frontmatter counters stay consistent;
    otherwise update the table, the JSON block and `open_count` / `total_count` together. Baseline R11
    measured 18 / 18.

    **`ROADMAP.md` bookkeeping**, using scoped edits only — never a whole-file rewrite, which would
    destroy phase entries outside the diff window. RE-MEASURE FIRST: two of the verifier's bookkeeping
    warnings were already fixed before this plan set was written, and a third was repaired at plan time
    (see R12 / R13). Correcting an already-correct line back to the verifier's stale observation is the
    "gate measured against the wrong tree" defect in its purest form. What remains: tick `27-07`,
    `27-08` and `27-09` as they complete; confirm the phase 27 counter still matches the PLAN files on
    disk; and correct the progress-table rows for phases 26 and 27, which both still read `In Progress`
    with no completion date.

    **`STATE.md`** — refresh `state_head`, `last_activity_desc` and `stopped_at`, which the verifier
    measured as stale by one merge and by two phases respectively (baseline R14 measured `190735d`).
  </action>

  <verify>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs query verify.plan-structure .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-09-PLAN.md; grep -c '^| [0-9]' .planning/WINDOWS.md</automated>
  </verify>

  <acceptance_criteria>
    1. `27-HUMAN-UAT.md` carries items 1, 3 and 4 forward with their `why_human` reasons intact, records
       item 2's answer with a date, and adds the new items from this round. Zero removed lines under the
       ROBUST form.
    2. The file states explicitly that the items are unanswered and explains why no checkpoint was
       raised — naming `mode: yolo` and the auto-selection precedent `26-SECURITY.md` records.
    3. `WINDOWS.md` `open_count` and `total_count` both increased by exactly the number of falsified
       premises recorded in the two SUMMARYs, and the table, JSON block and frontmatter agree. Baseline
       R11 measured 18 / 18. State the count and list each entry's id and one-line description.
    4. Every `WINDOWS.md` entry added carries its MEASURED evidence — the observed number, not the
       predicted one. An entry saying only "the plan was wrong" is not acceptable.
    5. `ROADMAP.md` was edited with scoped replacements only. Confirm no phase entry outside phase 27
       changed: `git diff HEAD --unified=0 -- .planning/ROADMAP.md | grep '^-' | grep -v '^--- '` lists
       only lines inside the phase 27 section and the progress-table rows for 26 and 27. Quote the
       removed-line set in the SUMMARY.
    6. `ROADMAP.md` has exactly ONE plans counter for phase 27 and it matches the number of PLAN files
       on disk (`ls .planning/phases/27-*/27-*-PLAN.md | wc -l`). R13 was repaired at plan time; verify
       it rather than re-deriving it from the verifier's stale reading.
    7. `STATE.md` `state_head` equals the current head at the time of the edit.
    8. `.planning/BACKLOG.md` is NOT created (baseline R15 measured it absent); the `T-27-06-06` loss
       risk is restated where the item is now visible.
  </acceptance_criteria>

  <done>
    Every unanswered question is carried forward in a document a human reads, explicitly labelled
    unanswered with the reason no checkpoint was raised; every falsified premise from this round is in
    the ledger with its measured evidence; and the roadmap and state bookkeeping the verifier flagged
    twice is finally correct.
  </done>

  <reversibility rating="reversible">
    Record edits under `.planning/`. No code, no shipped artifact.
  </reversibility>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking severity: `high`. Register origin: authored at plan time.

This plan writes records, not code. Its threats are therefore threats to the RECORD — which in this
repository is a control surface, not decoration: an overstated privacy claim causes a user to send
traffic they would otherwise have withheld, and a wrongly-closed threat causes an auditor to stop
looking. `26-SECURITY.md` already carries a worked example of each.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Record → future auditor | A closure sentence read at a width its evidence does not support stops the next investigation |
| Record → milestone owner | `REQUIREMENTS.md`'s PRIV-05 tick is derived from these records; a wider record yields a wrong tick |
| Record → user | `README.md` / `SPEC.md` state privacy guarantees a user acts on. AR-27-04's exclusion is still unstated there (`T-27-06-06`) |
| Counter → gate | `threats_open` gates the ship path; a counter whose population excludes a finding reports a false zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-09-01 | Repudiation | T-26-02-01 amendment overwriting a prior clause | high | mitigate | Byte-prefix gate on the single-line row (task 1 criterion 1); removed-line check in the ROBUST form only |
| T-27-09-02 | Repudiation | `threats_open` reading 0 while a high finding is open outside the counter's population | high | mitigate | The counter-population gate (task 1 criterion 4): either give the finding an id inside the population or amend the command AND its comment, then recompute. Never hand-write the number |
| T-27-09-03 | Repudiation | A clause claiming PRIV-05 satisfied, for the fourth time | high | mitigate | Task 1 criterion 9 and task 2 criterion 7 require every added clause to be read back bounded to its carrier; the tree-wide-scope-claim prohibition and its negated forms are gated |
| T-27-09-04 | Repudiation | Standing rule (iv) presenting the new mechanism as complete | high | mitigate | Criterion 5 requires `CookieCarrierInventoryTest`'s four blind axes to be carried over into the clause itself |
| T-27-09-05 | Tampering | A whole-file `ROADMAP.md` rewrite destroying phase entries outside the diff window | high | mitigate | Scoped edits only; task 3 criterion 5 asserts every removed line falls inside the phase 27 section or the two progress rows |
| T-27-09-06 | Repudiation | `REQUIREMENTS.md` edited by the phase that would benefit | high | mitigate | Zero added and zero removed lines asserted (task 2 criterion 2); the tick stays the milestone owner's to re-derive |
| T-27-09-07 | Repudiation | Absence of checkpoints read as absence of open questions | medium | mitigate | Task 3 criterion 2 requires the reasoning to be written into `27-HUMAN-UAT.md`, naming `mode: yolo` and the auto-selection precedent |
| T-27-09-08 | Information Disclosure | `README.md:247` / `SPEC.md:80,86` still overclaiming STRICT host anonymisation | medium | transfer | `T-27-06-06`, carried forward unactioned and restated where it is now visible. Deliberately not actioned here: a user-facing documentation change to what ships is outside a record-repair plan's scope |
| T-27-09-09 | Repudiation | A `WINDOWS.md` entry recording a prediction rather than a measurement | medium | mitigate | Task 3 criterion 4 requires the observed number in every entry |
| T-27-09-SC | Tampering | Dependency resolution | high | accept | No build runs and no dependency is resolved by this plan. If any task finds itself installing a package, STOP: the package-legitimacy gate and a blocking human checkpoint apply first |
</threat_model>

<artifacts_this_phase_produces>
From plan 27-09 specifically:

| Artifact | Kind | Purpose |
|----------|------|---------|
| T-26-02-01 clause (5) | record | The third refutation, with clauses (1)-(4) byte-prefix intact |
| AR-27-06 + evidence section | record | The no-backstop bound on both MCP parameter shapes; `sanitizeParameters` is the SOLE control |
| AR-27-07 / AR-27-08 | record | 27-08's two measurements filed at their measured severity with reasoning |
| Computed `threats_open` + stated population | record | A counter that cannot read zero over a high open finding outside its own population |
| Standing rule clause (iv) | record | Rendering-keyed versus source-keyed controls — what the three failed mechanisms had in common |
| Security Audit Trail row 5 | record | The third reopening and re-closure, counter computed from the rows |
| Second milestone-audit correction | record | Append-only; names the accurate-clauses-but-overstated-goal failure mode |
| Third W-A amendment in CONCERNS.md | record | W-A generalized from one predicate to every path that consumes the data |
| `27-HUMAN-UAT.md` carry-forward | record | Four items legible as unanswered, plus the reason no checkpoint was raised |
| `WINDOWS.md` entries for waves 7-9 | record | One per falsified premise, each with its measured evidence |
| `ROADMAP.md` / `STATE.md` bookkeeping | record | The six warnings the verifier raised twice, finally closed |
</artifacts_this_phase_produces>

<verification>
1. Byte-prefix gate PASSES on the T-26-02-01 row; both character lengths recorded (baseline R2: 11400).
2. `threats_open` is the OUTPUT of the documented `awk` command, and the counter's population is stated
   in the frontmatter comment. If any 27-07/27-08 finding is open at or above `high`, the population
   trap in task 1 criterion 4 was resolved by one of the two named routes, and the SUMMARY says which.
3. ROBUST removed-line form used everywhere; the naive `grep -c '^-[^-]'` appears nowhere.
4. `v0.10.0-MILESTONE-AUDIT.md` and `REQUIREMENTS.md`: zero removed lines; `REQUIREMENTS.md` also zero added.
5. Clause markers `**(1)` … `**(5)` all present (baseline R3 measured four).
6. Standing rule clauses (i)(ii)(iii)(iv) all present (baseline R7 measured three).
7. Security Audit Trail has 5 rows (baseline R8 measured 4).
8. THE PROHIBITED PHRASE (constructed per `<records_protocol>`) returns a count of 0 in every file
   this plan touches, and no negated or reworded form appears. State the searches run and bound the
   claim to them.
9. `WINDOWS.md` frontmatter counters agree with its table and JSON block.
10. `ROADMAP.md` removed lines fall only inside the phase 27 section and the two progress rows.
11. Every severity written matches the SUMMARY line it was read from, cited.
</verification>

<success_criteria>
- The register records a threat closed wrongly three times, with the pattern named — which is worth
  more to a future auditor than a fourth clean closure.
- AR-27-06 is defined with the same rigour AR-27-05 received, and is stated as a no-backstop BOUND
  rather than as a live leak, because that is what the measurement supports.
- The counter is computed, its population is stated, and it cannot report zero over an open high
  finding that sits outside that population.
- Standing rule (iv) explains why clauses (i)(ii)(iii) were each honoured and the threat still closed
  wrongly: all three prior mechanisms keyed on a rendering of the data. The property that broke the
  pattern is recorded in 27-07's words — the control reads `HttpParameterType` and never a rendered
  string, so a change to either MCP output format cannot defeat it.
- Every prior narrative survives verbatim; `REQUIREMENTS.md` is untouched; the PRIV-05 tick stays the
  milestone owner's to re-derive.
- No sentence written anywhere by this plan is wider than the evidence it cites.
</success_criteria>

<output>
Create `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-09-SUMMARY.md` when done.

It MUST carry, verbatim rather than paraphrased: the byte-prefix check with both character lengths;
the `awk` output and the resolution of the counter-population question with the route chosen and why;
the ROBUST removed-line set for every touched file; each severity written and the SUMMARY line it was
read from; the clause-by-clause read-back showing what each added sentence is bounded to; the list of
`WINDOWS.md` entries added with their measured evidence; and an explicit statement that PRIV-05's tick
was not re-derived and why.
</output>
