---
phase: 27-priv-05-gap-closure-sanitize-headers
plan: 12
type: execute
gap_closure: true
wave: 12
depends_on: ["27-10", "27-11"]
autonomous: true
requirements: [PRIV-05]
files_modified:
  - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No test under `src/` asserts that a sensitive value SURVIVES `PrivacyMode.STRICT` or `PrivacyMode.BALANCED`. The two host assertions plan 27-05 prohibited and this phase committed anyway are gone from the redacting fixtures; the pass-through they measured is asserted under `PrivacyMode.OFF`, where pass-through IS the correct behaviour."
    - "The claim above is MACHINE-CHECKED, not asserted a second time. `RedactingPolicySurvivalSweepTest` scans `src/test/kotlin` and fails CI on a new survival pin. Plan 27-08 made this same claim in prose and its own repository falsified it; a prose claim is what this task replaces."
    - "The sweep is proven to FIRE. Its non-vacuity tests drive the detector against verbatim copies of the three survival pins this round removed, and against the legitimate shapes it must NOT flag — a header NAME survival assertion, a benign-control survival assertion, and an OFF-mode pass-through assertion."
    - "The sweep's ISOLATION UNIT is a whole `fun` body INCLUDING blank lines — from the declaration line to the first non-blank line at or below its indent. This is asserted, not assumed: in all three artifacts the redacting-policy marker and the pin sit on opposite sides of a blank line, so the obvious reuse — `declarationBlockOf`'s blank-line-terminating `takeWhile` — would give a hit set of ZERO and a durable check silently narrower than its stated bound, which is this round's defect one iteration smaller. `BLANK_LINE_HAZARD_FIXTURE` machine-checks it."
    - "The sweep SCANS ITSELF and comes out clean, with NO self-file exclusion, and the REASON is a stated MECHANISM rather than a hope. The FILE WALK skips lines whose raw-string state is INSIDE a triple-quoted literal, BEFORE it matches function declarations. That skip is mandatory and not tidiness: every fixture is a verbatim copy of a real test function and therefore CARRIES ITS OWN `fun` DECLARATION LINE, which a line-based walk matches inside a triple-quoted block exactly as it matches one in code. Holding fixtures at companion level does NOT prevent this — the earlier draft of this plan asserted that it did, and the assertion was false for a line-based isolation. MEASURED at planning time against a mock sweep file carrying the hazard fixture and one host-pin fixture: WITHOUT the skip the sweep flags ITSELF with 2 hits (the bare `sentinel` identifier and the host literal); WITH the skip, 0 hits. That 2-to-0 pair is asserted, so the skip cannot go vacuous."
    - "The raw-string skip is proven not to have disarmed the detector. The SAME fixture text handed DIRECTLY to the detector is still FLAGGED — the positive-fixture tests take that path, the file walk takes the skipping path, and both are asserted. A skip that silenced the detector everywhere would pass the self-scan and fail this, which is the point."
    - "The sweep is proven to have fired ON THE TREE, not only on copies. Its detector is run at execution time against the pre-round file contents retrieved with `git show`, and the hit count is recorded. The in-file fixtures are the durable check; the tree run is the evidence that the durable check would have caught the real thing."
    - "The VOCABULARY IS CONSTRUCTED WITH ITS EXCLUSIONS, not allowlisted after the fact. MEASURED at planning time by running the specified detector over `src/test/kotlin` (149 `.kt` files): the vocabulary as the previous draft wrote it reports 10 hits on the current tree, of which only 3 are real survival pins. The other 7 are three named LEGITIMATE SHAPES: 5 hits on the benign control constant, 1 on a pre-redaction fixture guard, 1 on a negated containment written inside an `assertTrue`. Each is excluded BY CONSTRUCTION in the detector — `BENIGN_ACCESSORS`, a position rule, a negation rule — never by an ALLOWLIST entry. With all three, the detector reports EXACTLY 3 hits on the current tree and those 3 ARE the two host pins and the underscore pin, and EXACTLY 0 on the post-fix tree. Both numbers measured, both recorded here as bounds rather than discovered at execution time."
    - "AR-27-04's disposition is NOT relitigated. The finding stays OPEN at its recorded severity, still needs a human, and its measurement stays in `26-SECURITY.md`. What changes is only that the test suite stops teaching a future reader, in green, that a hostname surviving the strongest privacy mode is expected."
    - "BOUND, carried with the claim. The sweep sees an `assertTrue` whose `.contains(` argument is in the repository's MEASURED sentinel vocabulary, inside a test function that names a redacting policy. THE NEXT BLIND AXES, NAMED: a survival pinned with `assertEquals`, with `in`, with a regex find, through a helper in another file, with a value outside the sentinel vocabulary, or at a level above the unit suite — and three MORE that this round's three constructed exclusions each buy at a price: a survival pin whose text lives inside a triple-quoted raw string (the file walk skips those lines); a survival pin positioned textually ABOVE the line that names the policy (the position rule reads those as pre-redaction fixture guards); and a genuinely sensitive value that happens to be reached through the one accessor in `BENIGN_ACCESSORS`. It is a TRIPWIRE OVER A MEASURED VOCABULARY and its KDoc must say so before its first assertion."
    - "This plan closes no requirement. PRIV-05 stays `[ ]`."
  artifacts:
    - "src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt — NEW. The mechanical sweep, its vocabulary, its allowlist, its positive and negative fixtures, and its stated bound"
    - "src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt — the two prohibited assertions removed and re-pointed at an OFF fixture; the STRICT blocks' non-vacuity guards preserved"
  key_links:
    - "`RedactingPolicySurvivalSweepTest` -> plan 27-08's must-have. The claim moves from prose into CI, which is the only form in which it survives the phase that wrote it."
    - "the removed host assertions -> `26-SECURITY.md` AR-27-04. The measurement keeps its home; only the green pin moves. The record must still carry it, which plan 27-13 gates."
    - "the sweep's `ALLOWLIST` -> a source-verified reason per entry. An empty allowlist that silently grows entries is the tripwire failure mode `CookieCarrierInventoryTest` was warned about."
---

<!-- planner-discipline-allow: api.example.com -->
<!-- planner-discipline-allow: git show -->
<!-- planner-discipline-allow: takeWhile -->

<objective>
Stop the suite teaching a leak as expected, and replace the prose claim with a machine check.

Two failures, one root. First: this phase committed two green `assertTrue` assertions that a real
hostname survives `PrivacyMode.STRICT`, in direct violation of plan 27-05's own high-severity
prohibition — "a green assertion that a host reaches a backend under STRICT is exactly the artifact a
future audit misreads, and this phase exists because of one." Second: plan 27-08 authored a must-have
saying no such assertion exists anywhere under `src/`, and its own repository falsified it, because
nothing swept.

Purpose: every blocker in round 3 is green BECAUSE A TEST ASSERTS IT. That is the recurring failure
mode of this entire phase — a green test standing in for a measurement nobody re-scoped. A fourth
prose claim would be the fourth iteration of it. This plan makes the claim mechanical and states the
bound of the mechanism in the same breath.

Output: two assertions re-pointed at the privacy mode where their behaviour is correct, and a
repository-state sweep that fails CI on the next one.

**WHAT THIS PLAN MUST NOT DO.** It must not relitigate `AR-27-04`. That finding is legitimately on the
record with quoted evidence and honest provenance — its disposition was auto-selected by `mode: yolo`
and says so — and re-deciding it here would produce a second auto-selected answer filed as a decision.
It must not fix the host anonymisation gap: routing `hostHeaderRegex` through the composer without
also threading `maybeAnonymizeUrl` into the two serializers yields a payload whose `request` is
anonymised and whose `url` is not, which is a control that reads as closed and is not. It must not
delete the non-vacuity guards from the STRICT blocks. It must not tick `REQUIREMENTS.md`.

**NO CHECKPOINT IS RAISED, for the reason plan 27-10 states.** `AR-27-04`'s outstanding human decision
is carried to `27-HUMAN-UAT.md` by plan 27-13, legible as open.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-3.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-REVIEW.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-10-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-11-SUMMARY.md
</context>

<build_notes>
Every gradle invocation is `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew <task>`.

The sweep is a repository-state test. It resolves its scan root by walking up from the Gradle test
working directory and must FAIL rather than skip when it cannot find the repository — a
repository-state test that goes green when it cannot find the repository is worse than the grep it
replaces. Both existing precedents in this tree do exactly that; copy their resolution helper rather
than inventing a third.
</build_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Re-point the two prohibited host assertions at an OFF fixture</name>
  <precondition>Plans 27-10 and 27-11 are merged and `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew check` is green on the current tree. The sweep in task 2 is written against the post-fix tree and would report the 27-10 artifact as an outstanding hit if 27-10 has not landed.</precondition>
  <reversibility rating="reversible">Test assertions only. No production behaviour changes and no finding is closed.</reversibility>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt</files>
  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt` lines 190-300 — the whole `cookieVariantsAreStrippedEndToEndThroughRedactIfNeeded` test, both shape blocks and every comment between them. The two prohibited assertions sit at roughly `:247-250` and `:284-288`, but read the surrounding block rather than trusting the line numbers: task 1 of plans 27-10 and 27-11 may have shifted them.
    - The same file's `contextWith`-equivalent helper and its `parsedRequestOf` helper, so the OFF fixture is built the way the file already builds contexts rather than by a new pattern.
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md`, the `### AR-27-04` section and its `### AR-27-04 — disposition` subsection. You are pointing a comment at this record; read what it actually says so the pointer is accurate and so you can confirm the measurement survives there without the test.
  </read_first>
  <behavior>
    - Under `PrivacyMode.OFF`, `redactIfNeeded` returns its input BYTE-IDENTICALLY for both payload shapes — the serialized raw-message shape and the header-map shape. That is the correct behaviour for OFF and it is the right home for a pass-through assertion.
    - Under STRICT, the two blocks still prove that `redactIfNeeded` really ran: their existing `assertFalse` non-vacuity guards on the bearer-shaped sentinel are preserved untouched, so an absence assertion cannot pass because the call silently no-opped on a wrong policy or a wrong context.
    - Under STRICT, the two blocks assert NOTHING about whether a hostname survives. The measurement lives in the register.
  </behavior>
  <action>
STEP 0, THE PRE-STATE MEASUREMENT, BEFORE ANY DELETION. The gate this task is judged on must be a RED
PROBE, not decoration: it has to return a NON-ZERO count on the tree as you find it, and zero after
your edit. Run, verbatim, and record the number and the matched lines in the SUMMARY:

`grep -rn --include='*.kt' --exclude='RedactingPolicySurvivalSweepTest.kt' -A3 'assertTrue(' src/test | sed -E 's/^[^ ]*\.kt[:-][0-9]+[:-]//' | grep -v '^[[:space:]]*[/*]' | grep -c 'api\.example\.com'`

FOUR details of that command are load-bearing and none may be simplified away. `-A3` is REQUIRED
because the pins are formatted across lines — the call opener sits on one line and the hostname on the
next — so a single-line grep pairing both tokens matches nothing and would return zero with the pins
still standing. The `sed` strips the `path:lineno:` prefix that `grep -rn` prepends AND the `path-lineno-`
prefix it prepends to `-A` context lines; without it the following comment filter can never match,
because every line begins with a path. The comment filter runs AFTER the prefix strip for that reason. And `--exclude='RedactingPolicySurvivalSweepTest.kt'`
is REQUIRED because TASK 2 OF THIS SAME PLAN creates that file under `src/test` carrying VERBATIM COPIES
of the two pins you are about to delete, as its positive fixtures. Fixture lines are not comment lines,
so without the exclusion this gate reads the plan's own fixtures as survival pins and returns non-zero
again the moment task 2 lands — the plan's headline invariant failing for a reason the plan created.
MEASURED at planning time, all four numbers: on the current tree the gate returns `2` WITH the exclusion
and `2` WITHOUT it, because the sweep file does not exist yet — so the exclusion costs the red probe
NOTHING at step 0. On a simulated post-task-2 tree it returns `0` WITH the exclusion and `2` WITHOUT it,
and those two remaining lines are the two fixture copies, named here so nobody later reads them as a
regression.

Measured on the tree at planning time this returns `2`, and the two matched lines are the two
`.contains(` arguments of the pins you are about to delete. If it returns `0` at step 0, STOP: either
the pins are already gone or the command has been altered, and in both cases the rest of this task is
measuring nothing.

BOUND, and state it in the SUMMARY beside the number: this command sees a hostname within three lines
of an `assertTrue(` opener. A survival pin written with a wider gap, or with a different assertion
verb, is outside it. That is precisely why task 2 exists — this gate is a probe for THESE TWO pins, not
the durable check.

Then, the edit. Delete the two `assertTrue` assertions that assert the fixture's hostname is present in
redacted output under STRICT — one in the serialized raw-message block, one in the header-map block.
Delete them; do
not invert them. Inverting to `assertFalse` would assert a behaviour that does not ship and would turn
the suite red for a finding this plan is prohibited from fixing.

In their place, add ONE new sibling test to the same nested class that builds the SAME two payloads and
runs each through a context constructed with `PrivacyMode.OFF`, asserting with `assertEquals` that the
output is byte-identical to the input. Byte identity is a stronger and safer assertion than a
`contains`: it proves pass-through without naming any sensitive value at all, so this replacement
cannot itself become the artifact the sweep in task 2 exists to find. Name the test for what it asserts
— that OFF leaves both serialized shapes byte-identical.

Replace each deleted assertion with a comment, at the same position, that does three things. It states
that the host residual on that shape is MEASURED and OPEN; it names `AR-27-04` and
`.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` as where the measurement and its
quoted probe output live; and it states why the assertion is not here — a green assertion that a
sensitive value survives a redacting policy is a defect and not coverage, and plan 27-05's own
prohibition said so before this phase committed two of them. Do not restate the measured output in the
comment; a duplicated measurement is a second thing to keep in sync.

Leave every `assertFalse` in both blocks untouched, including the bearer-shaped non-vacuity guards and
the cookie-sentinel absence assertions. Leave the MEASURED CORRECTION comment about the header-map shape
carrying no line boundary untouched — plan 27-11 task 2 gated that it is still true.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*McpToolHelpersTest'</automated>
  </verify>
  <acceptance_criteria>
    - `McpToolHelpersTest` is green, including the new OFF byte-identity test.
    - RED PROBE, recorded as a before/after pair rather than a single green number. Command, run identically at step 0 and at task end: `grep -rn --include='*.kt' --exclude='RedactingPolicySurvivalSweepTest.kt' -A3 'assertTrue(' src/test | sed -E 's/^[^ ]*\.kt[:-][0-9]+[:-]//' | grep -v '^[[:space:]]*[/*]' | grep -c 'api\.example\.com'`. BEFORE the deletion it returns `2` — that number and the two matched lines are quoted in the SUMMARY. AFTER the deletion it returns `0`. THE EXCLUSION IS PART OF THE GATE'S STATED BOUND, not a convenience: task 2 puts verbatim copies of both deleted pins into that file as fixtures, and MEASURED on a simulated post-task-2 tree the unexcluded command returns `2` while the excluded one returns `0`. At step 0 the sweep file does not yet exist and BOTH forms return `2` (measured), so the exclusion does not blunt the red probe — record both step-0 numbers in the SUMMARY to show that it does not. A criterion that reads `0` on both sides of the edit is not a gate, and the previous formulation of this criterion was exactly that: it paired `assertTrue` and the hostname on ONE line, which no line in the tree does, and its `grep -v '^\s*//'` filter was inoperative because `grep -rn` prefixes every line with `path:lineno:`.
    - The two deleted assertions are DELETED, not inverted. Same command shape with the verb changed: `grep -rn --include='*.kt' --exclude='RedactingPolicySurvivalSweepTest.kt' -A3 'assertFalse(' src/test | sed -E 's/^[^ ]*\.kt[:-][0-9]+[:-]//' | grep -v '^[[:space:]]*[/*]' | grep -c 'api\.example\.com'` returns `0`. This one is a NEGATIVE INVARIANT, not a red probe — it reads `0` before the edit as well (measured at planning time) and its job is to stay `0`. Recorded as such in the SUMMARY so nobody later cites it as evidence that something changed.
    - The new OFF test uses `assertEquals` on byte identity and names no sensitive value in an `assertTrue`. Verified by reading it.
    - Every pre-existing `assertFalse` in `cookieVariantsAreStrippedEndToEndThroughRedactIfNeeded` survives: `git diff -- src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt | grep '^-' | grep -c 'assertFalse'` returns `0`.
    - Each replacement comment names `AR-27-04` and the register file path. `grep -c 'AR-27-04' src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt` returns at least `2`. Labelled honestly, because it is measured at `4` BEFORE the edit and expected at `4` after (two message lines leave with the deleted assertions, two comments arrive): this is a PRESENCE INVARIANT, not a red probe, and the real check is READING the two replacement comments. Record `4` and `4` in the SUMMARY so nobody later cites this number as evidence that something changed.
    - `git diff -- .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` is EMPTY — this task moves a test, it does not touch the record. Plan 27-13 owns the record.
  </acceptance_criteria>
  <done>The suite no longer asserts, in green, that a hostname survives the strongest privacy mode; the pass-through it measured is asserted where pass-through is correct; the STRICT blocks stay non-vacuous; and the finding itself is untouched and still open.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The sweep — a repository-state tripwire for green survival pins, with its bound stated first</name>
  <reversibility rating="reversible">A new test file. Reverting removes a guard; it changes no shipped behaviour.</reversibility>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt</files>
  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderRuleOwnershipTest.kt` — the whole file. This is the exact shape to follow: a class KDoc that states its bound BEFORE the first assertion, a `MATCHER_SPELLINGS` list paired one-for-one with `SPELLING_FIXTURES` so an unproven regex cannot hide, a `MIN_EXPECTED_MAIN_FILES` floor so a walk that reaches nothing fails, a `CLASSIFIED_*` allowlist keyed on path with a source-verified reason per entry, an `isCommentOnly` filter, and a root resolver that throws rather than skips.
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt` — `declarationBlockOf` and `indentWidth`. Read BOTH, and read `declarationBlockOf`'s termination condition literally. Indentation is the right AXIS to isolate on, because Kotlin string literals in this tree carry unbalanced parentheses that would derail a paren counter. But `declarationBlockOf`'s `takeWhile { it.isNotBlank() && it.indentWidth() > declarationIndent }` at `:171-175` STOPS AT THE FIRST BLANK LINE. That is correct for a `private val` regex initializer, which has none, and WRONG for a test function body, which is mostly blank-line-separated paragraphs. MEASURED, on the two files this sweep must find: in `McpToolHelpersTest.kt` the target `fun` is at `:178`, `PrivacyMode.STRICT` at `:179`, the FIRST BLANK LINE at `:187`, and the two host pins at `:249` and `:285` — `takeWhile` yields a nine-line stub containing neither pin. In `CookieHeaderNameParityTest.kt` the target `fun` is at `:178` and the first blank line is at `:181`, before both the predicate assertion and the mode loop that carries `PrivacyMode.STRICT` at `:187` — a three-line stub with no policy marker at all, so the body is not even in scope. Copying `declarationBlockOf` unchanged would give this sweep a hit set of ZERO on all three artifacts it exists to find. Reuse `indentWidth` and the comment filter; write the walk yourself to the spec in the action.
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderNameParityTest.kt` lines 178-202 AS THE PRE-ROUND TREE HAD THEM (plan 27-10 task 1 renames and inverts this test, so read it out of history). This is the blank-line hazard in its natural form and it is the mandatory fixture named in the action. RESOLVE THE PRE-ROUND SHA HERE, IN THIS TASK — the previous draft deferred that instruction to task 3, which runs afterwards, leaving this task told to copy content it had no stated way to reach. The resolution is in the action's STEP 0 and the resolved SHA is RECORDED ONCE and reused by task 3 rather than re-derived.
    - `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/SerializedEmissionRedactionTest.kt` — the `Sentinel` enum and the `Sentinel.X.value` accessor form, and `src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderNameParityTest.kt` — the `sentinelparity...` literal form and the local `sentinel` val. These three forms ARE the repository's sentinel vocabulary; the sweep's detector must be measured against them, not against a guessed convention.
    - `src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt` AS TASK 1 LEFT IT — the legitimate shapes that must NOT be flagged live here.
  </read_first>
  <behavior>
    - The sweep FLAGS a test function that names a redacting policy and contains an `assertTrue` whose `.contains(` argument is in the sentinel vocabulary. Proven against three verbatim positive fixtures: the wave-2 underscore pin, and the two host pins removed in task 1.
    - The isolation unit is a WHOLE `fun` body, blank lines included. In all three positive fixtures the redacting-policy marker and the flagged assertion sit on opposite sides of at least one blank line, so a blank-line-terminating isolation would produce a hit set of zero. This is asserted directly: the isolated body of `BLANK_LINE_HAZARD_FIXTURE` contains at least two blank lines AND both tokens.
    - The FILE WALK skips lines inside triple-quoted raw-string literals before matching function declarations, so the fixtures — which are verbatim copies and therefore carry their own `fun` lines — are invisible to the tree scan. MEASURED on a mock: without the skip the sweep flags itself with 2 hits, with it 0.
    - The skip is NON-VACUOUS AND HAS NOT DISARMED THE DETECTOR: the identical fixture text handed DIRECTLY to the detector is still FLAGGED. Both directions asserted.
    - The sweep scans its OWN file and yields zero hits. There is no self-file exclusion.
    - The sweep does NOT flag SIX legitimate shapes, all taken from the real tree: an `assertTrue` on a header NAME's survival; an `assertTrue` on the benign control value's survival; an `assertTrue` on a sentinel inside a function that names only `PrivacyMode.OFF`; any `assertFalse`; a PRE-REDACTION FIXTURE GUARD, an `assertTrue` on a sentinel positioned above the line that names the policy; and a NEGATED containment, `assertTrue(!x.contains(sentinel))`, which is an absence assertion wearing the wrong verb.
    - The benign exclusion CANNOT SWALLOW A REAL SENTINEL: an `assertTrue` on a non-benign `Sentinel.NAME.value` under STRICT is still FLAGGED, asserted against a fixture built by flipping the verb of a real `assertFalse` block.
    - The sweep FAILS rather than passes when it cannot resolve the test source root, and fails when the walk finds implausibly few files.
    - Every detector in the vocabulary is proven live against at least one positive fixture, so a regex that has stopped matching cannot make the scan vacuously clean.
    - On the post-27-10, post-task-1 tree the hit set is EMPTY. MEASURED at planning time on a simulated tree: 0 hits. On the tree as it stands BEFORE those two edits the same detector reports exactly 3 — the two host pins and the underscore pin — which is the same number task 3 requires against the pre-round contents.
  </behavior>
  <action>
STEP 0, RESOLVE THE PRE-ROUND SHA, BEFORE ANY FIXTURE IS COPIED. Both fixture sources must be read out
of history, because plan 27-10 has already renamed and inverted one of them and task 1 has already
deleted the pins in the other. Run:

`git log --format='%H %s' -- src/test/kotlin/com/six2dez/burp/aiagent/redact/CookieHeaderNameParityTest.kt src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolHelpersTest.kt | grep -v -E '^[0-9a-f]+ [a-z]+\(27-1[0-9]\)' | head -1`

That takes the newest commit touching EITHER file whose scope is not a round-4 scope, which is the
pre-round tip for both. At planning time it resolves to `09e9cae` (`test(27-04): re-pin the AR-27-01
block on the two measured shapes`), and that SHA is VERIFIED rather than assumed: measured at planning
time, `git show 09e9cae:<path>` is BYTE-IDENTICAL to the working tree for BOTH files, because nothing
has touched either file since. So the pin-ADDING commit is also the correct pre-round parent here —
which was worth checking rather than assuming, and the check is the reason it can be relied on.

CONFIRM THE RESOLVED SHA BEFORE COPYING, with a content check rather than trust: the blob for
`CookieHeaderNameParityTest.kt` must still carry the PRE-RENAME method name
`thePredicateIsDeliberatelyWiderThanTheTwoRegexes`, and the blob for `McpToolHelpersTest.kt` must still
carry BOTH host pins. If either is absent, the resolution picked a post-round commit and you must walk
further back. RECORD the resolved SHA and the command in the SUMMARY. Task 3 REUSES this SHA and does
not re-derive it.

Then create `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt` in
package `com.six2dez.burp.aiagent.redact`.

THE KDOC COMES FIRST AND IT STATES THE BOUND BEFORE ANY ASSERTION EXISTS TO BE QUOTED. It must say
what this file is for — plan 27-08 authored the claim "no green test asserting that a cookie value
survives a redacting policy is committed anywhere under `src/`" and its own repository falsified it,
because the claim was verified by no sweep at all. It must then say what the scan CAN see: an
`assertTrue` whose `.contains(` argument is in the MEASURED sentinel vocabulary, inside a test function
whose body names `PrivacyMode.STRICT`, `PrivacyMode.BALANCED` or `RedactionPolicy.fromMode(`. And it
must NAME what it cannot see: a survival pinned with `assertEquals`, with the `in` operator, with a
regex find, with `assertNotNull`, through a helper defined in another file, with a value that does not
follow the sentinel naming convention, or at any level above the unit suite. It must ALSO name the three
axes this file's own constructed exclusions buy at a price, each stated as a cost rather than a feature:
a survival pin whose text lives INSIDE a triple-quoted raw string, which the FILE WALK skips (and it
must say WHY the walk skips them — every fixture in this file is a verbatim copy carrying its own `fun`
line, so without the skip the file flags itself); a survival pin positioned textually ABOVE the line that
names the policy, which the POSITION RULE reads as a pre-redaction fixture guard; and a genuinely
sensitive value reached through the one accessor named in `BENIGN_ACCESSORS`. It must close by saying,
in the register both precedents use, that this is a TRIPWIRE OVER A MEASURED VOCABULARY and not a proof
of coverage, and that a reader who quotes it as proof reproduces the defect the phase exists to repair.

THE SCAN, IN TWO SEPARABLE PARTS, AND THE SEPARATION IS LOAD-BEARING. Part one is the FILE WALK, which
turns a path into lines. Part two is the DETECTOR, which turns lines into hits. The positive-fixture
tests call the DETECTOR directly on fixture text; the tree scan and the self-scan call the FILE WALK
first. Keeping them separable is what makes the next paragraph checkable in both directions.

THE RAW-STRING SKIP, IN THE FILE WALK ONLY, APPLIED BEFORE ANY FUNCTION-DECLARATION MATCHING. Track the
triple-quote state line by line: count the triple-quote occurrences on each line and toggle the state
once per occurrence; a line whose state was INSIDE at the START of the line is EXCLUDED from the scan
(replace it with an empty line so line numbering is preserved). That keeps the opening `val X = ` line
and drops the literal's contents and its closing line.

THIS SKIP IS MANDATORY AND THE REASON IS MEASURED, NOT ANTICIPATED. Every fixture in this file is
required to be a VERBATIM copy of a real test function, so every fixture NECESSARILY CONTAINS ITS OWN
`fun` DECLARATION LINE — that line is exactly what the isolation needs in order to isolate the fixture
at all. A line-based walk matches such a line inside a triple-quoted literal precisely as it matches one
in code, walks forward from it, consumes the blank lines by design, reaches the policy marker and the
`.contains(` argument, and FLAGS THE SWEEP'S OWN FILE. Holding the fixtures at companion level does NOT
prevent this; the previous draft of this plan claimed it did and the claim was false. MEASURED at
planning time on a mock sweep file carrying the hazard fixture and one host-pin fixture: WITHOUT the
skip, 2 self-hits; WITH the skip, 0.

THE SKIP MUST BE PROVEN NON-VACUOUS AND PROVEN NOT TO HAVE DISARMED THE DETECTOR, as two assertions in
opposite directions. First: the detector run over this file's lines WITHOUT the skip returns a NON-EMPTY
hit set, and WITH the skip returns an EMPTY one — a skip that is silently doing nothing fails the first
half, and a detector that has stopped matching passes both halves trivially, which is why the second
assertion exists. Second: the SAME fixture text handed DIRECTLY to the detector is still FLAGGED. Say in
the KDoc that raw-string contents are outside the TREE scan and why, so a reader who wonders where the
fixtures went finds the answer beside the bound rather than by reading the walk.

Comment-only lines are dropped with the same
`isCommentOnly` predicate both precedents use — prose ABOUT a survival pin, including this plan's own
replacement comments from task 1, must not be read as a survival pin — but they are dropped AFTER
function-body isolation, never before it. Dropping them first would shorten bodies and change where the
walk below terminates, which is a second way to get a silently truncated body.

THE FUNCTION-BODY ISOLATION, SPECIFIED EXPLICITLY BECAUSE THE OBVIOUS REUSE IS WRONG. Do NOT call
`LogicalLineBoundaryScopeTest.declarationBlockOf` and do NOT copy its `takeWhile`. Write this walk:

  1. Find every line matching a function declaration — `^\s*(private |internal )?fun \w+\(`.
  2. Take that line's `indentWidth()` as D.
  3. Walk FORWARD from the next line, accumulating lines. BLANK LINES ARE ACCUMULATED, NOT TERMINAL.
  4. Terminate at the first line that is BOTH non-blank AND has `indentWidth() <= D`. In Kotlin that
     line is the function's own closing brace, which is what makes this the whole body.
  5. The isolated body is the declaration line plus everything accumulated in step 3.

The single difference from `declarationBlockOf` — that a blank line is consumed rather than ending the
block — is the entire difference between this sweep finding all three artifacts and finding none of
them, because in both target files the redacting-policy marker and the pin sit on OPPOSITE SIDES of a
blank line. Apply the comment filter AFTER isolation, so dropping comment lines cannot shorten a body
and terminate it early.

State in the KDoc, in these terms: THE ISOLATION UNIT IS A WHOLE `fun` BODY, BLANK LINES INCLUDED,
from the declaration line to the closing brace at or above its indent. Say that function-scoped
isolation is the chosen bound and that a survival pin split across two functions — a marker in one, an
assertion in another — is outside it and is one of the named blind axes.

Within each isolated function body, first test whether the body names a redacting policy. If it does
not, skip it — an `assertTrue` on a sentinel under OFF is correct and must not be flagged. If it does,
normalise the body by joining its lines with a single space and collapsing whitespace runs, because
`assertTrue(` and its `.contains(` argument routinely span several lines in this tree. Then, for each
occurrence of `.contains(` in the normalised body, take the preceding window of characters; if that
window contains `assertTrue(` with no intervening `assertFalse(`, extract the argument text between
`.contains(` and its matching close parenthesis by scanning forward with a parenthesis counter, and test
that argument against the vocabulary.

THE VOCABULARY, declared as a list paired one-for-one with positive fixtures the way
`MATCHER_SPELLINGS` is paired with `SPELLING_FIXTURES`. Three measured forms occur in this repository:
a string literal matching the `sentinel` naming convention; the `Sentinel.NAME.value` enum-accessor
form used by `SerializedEmissionRedactionTest`; and a bare local identifier named `sentinel`. Add a
fourth, an explicit `HOST_LITERALS` list carrying the fixture hostname the two removed pins used, so
the detector covers a literal that does not follow the sentinel convention but is unmistakably a
sensitive fixture value. Every entry needs its own positive fixture and an assertion that the fixture
matches, or an entry that has stopped matching can hide in the list.

THE THREE EXCLUSIONS, CONSTRUCTED INTO THE DETECTOR AND NEVER INTO THE ALLOWLIST. The vocabulary above,
written without qualification, DOES NOT YIELD AN EMPTY HIT SET — the previous draft required both an
empty hit set AND an empty allowlist AND that a benign-control fixture not be flagged, and all three
cannot hold at once. MEASURED at planning time by running the detector specified in this action over
`src/test/kotlin`: the unqualified vocabulary reports 10 hits on the current tree and 7 on the simulated
post-fix tree, and only 3 of the 10 are real survival pins. The other 7 are THREE LEGITIMATE SHAPES that
genuinely live on the tree. Each is excluded BY CONSTRUCTION, with its measured count written beside it
as a stated bound. Growing the `ALLOWLIST` instead is forbidden, and so is narrowing the vocabulary:

  1. `BENIGN_ACCESSORS`, an explicitly enumerated set the enum-accessor form SUBTRACTS. It contains
     exactly ONE entry, `Sentinel.BENIGN_CONTROL` — the negative-control constant whose value is
     `benignidcontrolvalue`, declared in `SerializedEmissionRedactionTest`'s `Sentinel` enum. Asserting
     that this value SURVIVES is the whole point of a negative control: it proves a pass was not
     produced by blanket destruction of the payload. MEASURED PRE-EXISTING COUNT: 5, not 2 — five
     separate live functions in `SerializedEmissionRedactionTest` assert it under a redacting policy.
     Record 5 as the bound; a sixth appearing later is normal, a `BENIGN_ACCESSORS` that grows a SECOND
     KEY is not, and the KDoc must say that adding a key is a decision requiring the same source-verified
     reason an `ALLOWLIST` key would.
  2. THE POSITION RULE. A `.contains(` occurrence positioned BEFORE the FIRST redacting-policy marker in
     the normalised body is NOT a candidate, because nothing has been redacted yet at that point in the
     body — it is a PRE-REDACTION FIXTURE GUARD asserting the value is present in the INPUT, which is
     what makes the later absence assertion non-vacuous. MEASURED PRE-EXISTING COUNT: 1, the
     `assertTrue(truncated.contains(sentinel), "fixture guard: ...")` that precedes the redacting call in
     `theRealTruncateIfNeededOutputShapeIsStrippedAndNotLengthened`. Verified safe for all three real
     artifacts: in every one the policy marker precedes the pin.
  3. THE NEGATION RULE. If the text between the `assertTrue(` opener and the `.contains(` begins with
     `!`, the assertion is an ABSENCE assertion wearing the wrong verb and is NOT a candidate. MEASURED
     PRE-EXISTING COUNT: 1, `assertTrue(!output.contains(sentinel), ...)` in
     `RedactionTest.cookieHeaderNameVariantsAreStripped`.

WITH ALL THREE CONSTRUCTED, MEASURED: the detector reports EXACTLY 3 hits on the current tree and those
3 ARE the two host pins and the underscore pin — the three artifacts task 3 must reproduce — and
EXACTLY 0 on the simulated post-27-10, post-task-1 tree. State both numbers in the KDoc as the
measurement that justified the exclusions.

THE ANTI-SWALLOW FIXTURE, MANDATORY, because an exclusion that cannot be shown to have a floor is just a
quieter allowlist. Declare a fixture built by taking the real
`canonicalCookieDoesNotSurviveTheSerializedProxyHistoryShapeUnderStrict` block and flipping ONLY its
verb from `assertFalse` to `assertTrue`, so it asserts that `Sentinel.COOKIE_STRICT.value` SURVIVES
STRICT. Assert it IS FLAGGED. MEASURED at planning time: the real block is clean and the verb-flipped
copy is flagged on `Sentinel.COOKIE_STRICT.value`. That pair proves `BENIGN_ACCESSORS` subtracts one
named constant and not the enum-accessor form itself.

THE NEGATIVE FIXTURES, equally load-bearing, asserted NOT to be flagged. There are SIX, not four, and
the two the previous draft omitted are the two shapes that were measurably flagging on the live tree:
an `assertTrue` on a header name's stripped form; an `assertTrue` on the benign control value, taken
from `headerNameAndBenignControlSurviveTheSerializedShape`; an `assertTrue` on a sentinel inside a body
that names only `PrivacyMode.OFF`; an `assertFalse` on a sentinel inside a STRICT body; the PRE-REDACTION
FIXTURE GUARD from `theRealTruncateIfNeededOutputShapeIsStrippedAndNotLengthened`; and the NEGATED
containment from `RedactionTest.cookieHeaderNameVariantsAreStripped`. Take each from the real tree so
they are the shapes that actually exist rather than shapes you invented — each of the six was measured
against the specified detector at planning time and each came out clean.

THE BLANK-LINE HAZARD FIXTURE, MANDATORY AND NAMED. The isolation defect described above is not
avoidable by accident and must be machine-checked, or a later simplification back to `takeWhile`
reintroduces it silently with every test still green. Declare `BLANK_LINE_HAZARD_FIXTURE`: the
pre-round `CookieHeaderNameParityTest.thePredicateIsDeliberatelyWiderThanTheTwoRegexes` copied VERBATIM
from `git show`, blank lines preserved byte-for-byte. Measured, so you can check the copy is faithful:
its `fun` line is followed by two lines of setup, then a BLANK LINE, then the predicate assertion, then
a SECOND BLANK LINE, then the mode loop carrying the redacting-policy marker, and only then the
`assertTrue` on sentinel survival. Assert three things about it, as separate assertions with separate
messages:

  - the isolated body CONTAINS at least two blank lines — proving the walk consumed them rather than
    stopping at the first;
  - the isolated body contains BOTH the redacting-policy marker AND the `.contains(` argument — the two
    tokens the naive isolation separates;
  - the fixture is FLAGGED.

Do the same two-token containment assertion for the `McpToolHelpersTest` host-pin fixtures, whose
marker-to-pin distance spans many blank lines. A fixture with no blank line between marker and
assertion does NOT satisfy this requirement and must not be substituted for one that does.

THE SWEEP SCANS ITSELF, DELIBERATELY, AND MUST COME OUT CLEAN — AND THE MECHANISM THAT MAKES IT CLEAN
IS THE RAW-STRING SKIP SPECIFIED ABOVE, NOT THE COMPANION. This file lives under `src/test/kotlin` and
the walk reaches it. Its positive fixtures ARE verbatim `assertTrue(... .contains(<sentinel>))` text,
`REDACTING_POLICY_MARKERS` literally names `PrivacyMode.STRICT`, and — decisively — each fixture carries
its own copied `fun` declaration line, without which the isolation could not isolate it. So whether it
flags itself is not a hypothetical: MEASURED, without the skip it flags itself twice. The disposition is
fixed here rather than discovered at execution time and then accommodated by weakening the detector:

  - THE FILE WALK SKIPS RAW-STRING CONTENTS. That, and only that, is why the tree scan does not see the
    fixtures. This is stated as the mechanism because the previous draft stated a different one —
    that companion-level declarations are not inside a `fun` — which is true of the DECLARATION and
    false of the fixture's CONTENTS, and a line-based walk reads contents.
  - EVERY fixture — positive, negative, the anti-swallow fixture and the blank-line hazard — and every
    marker, vocabulary and literal list is held at COMPANION level as a `val` (raw triple-quoted strings
    where multi-line). Nothing is inlined inside a `@Test` body. This is still required, because it is
    what keeps every fixture INSIDE a raw string where the skip can reach it.
  - `@Test` bodies reference fixtures BY IDENTIFIER only. No `@Test` body may contain a literal
    sentinel, a literal hostname, or a literal `PrivacyMode.STRICT` / `PrivacyMode.BALANCED` /
    `RedactionPolicy.fromMode(` token.
  - There is NO self-file exclusion. Excluding this file would make the detector narrower than its
    stated scope, which is the register-wider-than-control defect one iteration smaller. The raw-string
    skip is NOT a self-file exclusion: it is scope-shaped, applies to EVERY file the walk touches, is
    declared in the KDoc as a named blind axis, and is asserted non-vacuous in both directions.
  - Add a dedicated `@Test`, `theSweepFileItselfYieldsNoHits`, that runs the FILE WALK plus detector over
    THIS FILE'S OWN PATH and asserts the hit list is EMPTY. Its failure message must say that a self-hit
    means a fixture leaked out of a raw-string literal and into real code, and that the fix is to move
    the fixture back into the companion — never to add an exclusion.
  - Add its counterpart, `theRawStringSkipIsWhyTheSelfScanIsClean`, which runs the DETECTOR over this
    file's lines WITHOUT the skip and asserts the hit set is NON-EMPTY. Without this the self-scan is
    unfalsifiable: a detector that had stopped matching would pass `theSweepFileItselfYieldsNoHits`
    exactly as a working one does. MEASURED expectation: at least 2 hits unskipped, 0 skipped.
  - State this disposition in the KDoc, in one sentence, next to the bound.

THE ALLOWLIST. A map keyed by a file-and-function identifier, valued with a reason read from source.
Expected EMPTY, and expected to STAY empty, because all three legitimate shapes measured on this tree are
excluded BY CONSTRUCTION in the detector rather than by a key here. Assert the hit set equals the allowlist's key set, with a failure
message that names the NEW hits and the STALE keys separately, the way
`everyCookieHeaderNameMatcherInMainIsClassified` does — an allowlist that accumulates dead keys is the
bookkeeping the file exists to force.

THE NON-VACUITY TEST, mandatory and separate. Assert the walk found at least a floor of `.kt` files
under the test root; measure the real count at execution time and set the floor well below it, stating
the measured number in a comment as a measurement and the floor as a floor. MEASURED at planning time:
149 `.kt` files under `src/test/kotlin`, so a floor of 100 is well below it and still fails a walk that
reaches nothing. Re-measure at execution time rather than copying 149 — task 2 adds a file. Assert every vocabulary
entry matched its positive fixture. Assert every negative fixture was NOT flagged. Assert the vocabulary
list and the fixture list have equal size. Resolve the root by walking up from `user.dir` and THROW an
`AssertionError` naming the failed resolution if it is not found — never weaken it into a skip.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests '*RedactingPolicySurvivalSweepTest' && JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt</automated>
  </verify>
  <acceptance_criteria>
    - `RedactingPolicySurvivalSweepTest` exists at the stated path and all its tests pass.
    - The hit set on the tree AS THIS TASK FINDS IT (post-27-10, post-task-1) is EMPTY and the `ALLOWLIST` is EMPTY. Both facts recorded in the SUMMARY. This is achievable ONLY with the three constructed exclusions: MEASURED at planning time, the unqualified vocabulary reports 7 hits on that same tree and 10 on the pre-round tree. Record the measured post-fix count (`0`) beside the measured unqualified count (`7`) so the exclusions are visible as a stated bound rather than invisible as a convenience.
    - THE THREE EXCLUSIONS ARE IN THE DETECTOR, NOT IN THE `ALLOWLIST`. `BENIGN_ACCESSORS` contains exactly one entry; the position rule and the negation rule are code paths. `ALLOWLIST` is empty. Verified by reading. The measured pre-existing counts each exclusion accounts for — 5 benign-accessor, 1 pre-redaction guard, 1 negated containment — are recorded in the SUMMARY as the bound each one buys.
    - THE BENIGN EXCLUSION CANNOT SWALLOW A REAL SENTINEL. The anti-swallow fixture — the real `canonicalCookieDoesNotSurvive...` block with its verb flipped to `assertTrue` — is asserted FLAGGED, and the unflipped real block is asserted clean. Both green.
    - Each of the three positive fixtures — the underscore pin and the two host pins, copied verbatim from `git show` of the pre-round file contents — is asserted FLAGGED by the detector, and the test proving that is green.
    - Each of the SIX negative fixtures is asserted NOT flagged, and the test proving that is green — including the pre-redaction fixture guard and the negated containment, which were measurably flagging under the previous draft's vocabulary and are the reason the exclusion count is three and not one.
    - THE ISOLATION IS PROVEN TO CONSUME BLANK LINES. `BLANK_LINE_HAZARD_FIXTURE` exists, is a verbatim `git show` copy of the pre-round `thePredicateIsDeliberatelyWiderThanTheTwoRegexes`, and three separate assertions pass over it: its isolated body contains at least two blank lines; that body contains both the redacting-policy marker and the `.contains(` argument; and it is FLAGGED. Verified additionally on EXECUTABLE lines only, so the KDoc may name the rejected shape without self-invalidating the gate: `grep -v '^[[:space:]]*[/*]' src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt | grep -c 'takeWhile'` returns `0`, because the blank-line-terminating `takeWhile` shape is the exact defect these assertions exist to prevent.
    - THE SELF-SCAN IS CLEAN AND UNEXCLUDED, AND ITS REASON IS ASSERTED IN BOTH DIRECTIONS. `theSweepFileItselfYieldsNoHits` exists and is green. Its counterpart `theRawStringSkipIsWhyTheSelfScanIsClean` exists and is green, asserting that the detector over this file's lines WITHOUT the raw-string skip returns a NON-EMPTY hit set — MEASURED at planning time on a mock carrying the hazard fixture and one host-pin fixture: 2 unskipped, 0 skipped. A green self-scan with no such counterpart is unfalsifiable, which is the defect class this plan exists to stop. The file contains no self-exclusion: reading the scan, the sweep's own path is walked like any other and the skip applies to every file.
    - THE SKIP HAS NOT DISARMED THE DETECTOR. Every positive fixture is FLAGGED when passed DIRECTLY to the detector — the same text the file walk skips. The positive-fixture tests and the self-scan take different code paths and both are green.
    - The class KDoc states the bound and names at least EIGHT things the scan cannot see, BEFORE the first `@Test` — the five original blind axes plus the three each constructed exclusion buys: raw-string-resident text, a pin positioned above the policy marker, and a sensitive value reached through the one `BENIGN_ACCESSORS` entry. It also records the two measured hit counts (3 on the pre-round tree, 0 on the post-fix tree) that justified the exclusions. Verified by reading the file top to bottom.
    - The file-count floor is present, is strictly below the measured count, and the measured count is recorded in a comment and in the SUMMARY.
    - The root resolver throws on failure. Verified by reading it; `grep -c 'throw AssertionError' src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt` returns at least `1`.
    - `ktlintCheck` and `detekt` exit zero.
  </acceptance_criteria>
  <done>A claim plan 27-08 made in prose is now a CI gate with a stated vocabulary, proven to fire on the three artifacts it was written for and proven not to fire on the six legitimate shapes it sits next to.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the sweep would have caught the real thing, on the real tree</name>
  <reversibility rating="reversible">Measurement only; no file under `src/` changes.</reversibility>
  <files>src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt</files>
  <read_first>
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt` AS TASK 2 LEFT IT — the detector entry point, so the execution-time run drives the SAME function the committed tests drive rather than a reimplementation of it.
    - `27-12-SUMMARY.md` as TASK 2 left it — the PRE-ROUND SHA task 2 resolved and recorded, together with the command that resolved it. REUSE that SHA; do NOT re-derive it. Task 2 owns the resolution because task 2 needs it first, and a SHA derived twice is a SHA that can differ twice. If the SUMMARY does not carry it, task 2 did not complete its step 0 and this task is blocked rather than free to guess.
  </read_first>
  <action>
Run the sweep's detector against the PRE-ROUND file contents, not against copies. Use the SHA TASK 2
RESOLVED AND RECORDED — this task does not re-derive it. Retrieve the contents with
`git show <pre-round-sha>:<path>` for `CookieHeaderNameParityTest.kt` and `McpToolHelpersTest.kt`,
write them to a scratch directory, and point the detector at that directory. It must report exactly
three hits: the underscore pin, and the two host pins. MEASURED at planning time with the detector
specified in task 2, run over the live tree while it still carried all three: EXACTLY 3 hits, and they
are `CookieHeaderNameParityTest.thePredicateIsDeliberatelyWiderThanTheTwoRegexes` (on the bare `sentinel`
identifier) plus two in `McpToolHelpersTest.cookieVariantsAreStrippedEndToEndThroughRedactIfNeeded` (on
the host literal). Anything other than 3 means the detector was built to a different spec than the one
measured, and the discrepancy is the finding — do not adjust the number to match the run.

Record in the SUMMARY: the resolved pre-round SHA and how it was resolved; the exact command run; and
the detector's raw output including the three hits with their file-and-function identifiers. This is
the RED PROBE for this task's must-have — the fixtures in task 2 prove the detector fires on that text,
and only this run proves it fires on the tree.

Then state the split honestly, in the SUMMARY and in one sentence of the file's KDoc if it is not
already there: the in-file fixtures are the DURABLE check that survives this phase and runs on every
CI run; the tree run is EXECUTION-TIME evidence that the durable check would have caught the real
artifacts, and it is not re-run by CI. Do not describe the fixtures as if they were the tree run, and
do not add a `@Test` that shells out to git — coupling the suite to repository history would make CI
fail for a reason unrelated to the code under test.

Finally, run the whole suite. Record total counts. If `RedactionTest` fails, re-run it alone before
concluding anything — the wall-clock flake through `SafeRegex`'s 50 ms deadline is a known
non-regression under CPU load.
  </action>
  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew check && JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - The detector run against the pre-round contents reports EXACTLY three hits, and the three are the underscore pin plus the two host pins. Raw output quoted in the SUMMARY.
    - The pre-round SHA is the one TASK 2 recorded, reused rather than re-derived, and it appears in this task's SUMMARY section together with task 2's resolution command so a later reader can reproduce the run rather than trust it.
    - No EXECUTABLE STATEMENT in the committed suite invokes a subprocess. Comment-filtered, because this task REQUIRES a KDoc sentence naming the tree run and the previous formulation of this criterion failed on the natural way to write that sentence: `grep -v '^[[:space:]]*[/*]' src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt | grep -c 'git show\|ProcessBuilder\|Runtime.getRuntime'` returns `0`. The comment filter drops both `//` lines and KDoc `*` lines, so prose describing the one-time run is allowed and code performing it is not.
    - `check` exits zero and `test` exits zero; total test count and failure count recorded.
    - The SUMMARY states, in its own sentence, that the tree run is execution-time evidence and is not re-run by CI, and that the in-file fixtures are the durable check.
    - `git diff -- .planning/REQUIREMENTS.md` is EMPTY.
  </acceptance_criteria>
  <done>The sweep is not merely green — it is demonstrated to have found the three real artifacts on the real tree, with the SHA, the command and the raw output recorded so the demonstration is reproducible rather than reported.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Symbol | Kind | File | What it is |
|--------|------|------|-----------|
| `RedactingPolicySurvivalSweepTest` | test class | `src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactingPolicySurvivalSweepTest.kt` | NEW. The mechanical replacement for plan 27-08's prose must-have. |
| `SENSITIVE_VALUE_VOCABULARY` | const list | same | The four MEASURED forms a sensitive fixture value takes in this repository. |
| `VOCABULARY_FIXTURES` | const list | same | One positive fixture per vocabulary entry, so an entry that stopped matching cannot hide. |
| `NEGATIVE_FIXTURES` | const list | same | The four legitimate shapes that must NOT be flagged, taken from the real tree. |
| `HOST_LITERALS` | const list | same | Sensitive fixture values that do not follow the sentinel naming convention. |
| `REDACTING_POLICY_MARKERS` | const list | same | The tokens whose presence in a function body puts that body in scope. |
| `ALLOWLIST` | const map | same | Empty on the current tree. A key requires a reason read from source. |
| `MIN_EXPECTED_TEST_FILES` | const | same | The walk floor. A scan that reaches nothing fails rather than passes. |
| `BLANK_LINE_HAZARD_FIXTURE` | const | same | The pre-round underscore pin copied verbatim, blank lines preserved. Machine-checks that the isolation consumes blank lines instead of terminating at the first one. |
| `BENIGN_ACCESSORS` | const set | same | Exactly one entry, `Sentinel.BENIGN_CONTROL`. SUBTRACTED from the enum-accessor form by construction, so a negative control is not a survival pin. Measured pre-existing count it accounts for: 5. |
| `ANTI_SWALLOW_FIXTURE` | const | same | The real `canonicalCookieDoesNotSurvive...` block with its verb flipped to `assertTrue`. Proves `BENIGN_ACCESSORS` subtracts ONE named constant and not the enum-accessor form itself. |
| the raw-string skip | file-walk step | same | Drops lines whose triple-quote state is INSIDE, BEFORE function-declaration matching. The mechanism — not the companion — that makes the self-scan clean. |
| `theSweepFileItselfYieldsNoHits` | test fn | same | The self-scan disposition, asserted rather than assumed. No self-file exclusion exists. |
| `theRawStringSkipIsWhyTheSelfScanIsClean` | test fn | same | The counterpart that makes the self-scan falsifiable: WITHOUT the skip the hit set is NON-EMPTY. Measured 2 unskipped, 0 skipped. |
| the OFF byte-identity test | test fn | `McpToolHelpersTest.kt` | NEW. Where the pass-through the two removed pins measured now lives. |

REMOVED: two `assertTrue` assertions in `McpToolHelpersTest.kt` that pinned a hostname surviving STRICT.
Removed, not inverted — the behaviour they described still ships and is still open as `AR-27-04`.

DELIBERATELY UNCHANGED: `hostHeaderRegex`, `Serialization.kt`, `maybeAnonymizeUrl`, `AR-27-04`'s
disposition, `REQUIREMENTS.md`.
</artifacts_this_phase_produces>

<threat_model>
ASVS level 1. Blocking threshold: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite -> future maintainer | A green test is the most authoritative statement a repository makes about intended behaviour. A green survival pin is a leak documented as a feature, and it is how this threat was closed wrongly three times. |
| Sweep detector -> the claim it supports | A tripwire quoted wider than the vocabulary it scans reproduces, one iteration smaller, the register-wider-than-control defect this phase exists to repair. |
| Repository history -> CI | Evidence drawn from `git show` is execution-time evidence; wiring it into a `@Test` would make CI fail on history rather than on code. |
| The sweep -> its own fixtures | A line-based scanner cannot distinguish a fixture from the code it copies. Every fixture here is a verbatim copy carrying its own `fun` line, so the sweep flags itself unless the file walk skips raw-string interiors — measured, twice, before this was written down. A scanner whose STATED mechanism does not match what it MEASURES is worse than no scanner, and that is the defect this whole phase exists to repair. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-12-01 | Repudiation | A green assertion teaches a future audit that a leak is expected | high | mitigate | Task 1 removes both; task 2 makes the absence a CI gate; task 3 proves the gate fires on the real artifacts. |
| T-27-12-02 | Repudiation | The sweep is quoted as proof of coverage it does not have | high | mitigate | The KDoc states the bound and names at least five blind axes BEFORE the first `@Test`, gated by an acceptance criterion that requires reading the file top to bottom. |
| T-27-12-03 | Tampering | The sweep goes vacuously green — walk finds nothing, or a detector stopped matching | high | mitigate | File-count floor; one positive fixture per vocabulary entry with an equal-size assertion; root resolver throws rather than skips. All three are the precedents' own mechanisms, reused rather than reinvented. |
| T-27-12-08 | Tampering | The function-body isolation terminates at the first blank line, so the sweep is silently narrower than its stated bound and its hit set on the real artifacts is zero | high | mitigate | The walk is specified explicitly in task 2 rather than by reference to `declarationBlockOf`, whose termination condition is measurably wrong for a `fun` body; `BLANK_LINE_HAZARD_FIXTURE` asserts the isolated body carries at least two blank lines AND both tokens AND is flagged; an executable-lines grep rejects the blank-line-terminating shape. |
| T-27-12-09 | Tampering | The sweep flags itself — MEASURED, twice — and the response is to weaken the detector or exclude the file | high | mitigate | Raised from medium: this is no longer a risk but a measured fact. Every fixture is a verbatim copy and therefore carries its own `fun` line, which a line-based walk matches inside a triple-quoted literal; the previous draft's stated reason (companion level) was false for the fixture CONTENTS. Disposition fixed at plan time: the FILE WALK skips raw-string interiors BEFORE declaration matching; fixtures stay at companion level so the skip can reach them; no self-file exclusion; `theSweepFileItselfYieldsNoHits` asserts 0 and `theRawStringSkipIsWhyTheSelfScanIsClean` asserts NON-ZERO without the skip, so the clean result cannot be vacuous. |
| T-27-12-10 | Tampering | The raw-string skip silently disarms the detector everywhere, and the whole sweep goes vacuously green | high | mitigate | The skip lives in the FILE WALK only; the DETECTOR is a separate entry point the positive-fixture tests drive DIRECTLY on the same text the walk skips. Both paths asserted green, in opposite directions. The skip is declared in the KDoc as a NAMED BLIND AXIS, not as a feature. |
| T-27-12-11 | Tampering | The three constructed exclusions are used to make an inconvenient hit set empty | high | mitigate | Each exclusion is enumerated in the plan with the MEASURED count it accounts for (5 benign-accessor, 1 pre-redaction guard, 1 negated containment) as a stated bound, is built into the detector rather than the `ALLOWLIST`, and each buys a NAMED blind axis in the KDoc. `ANTI_SWALLOW_FIXTURE` asserts the benign exclusion still flags a non-benign `Sentinel.NAME.value`. |
| T-27-12-04 | Tampering | The sweep is silenced by growing the allowlist instead of fixing the pin | medium | mitigate | The allowlist is asserted equal to the hit set with NEW and STALE keys reported separately, so a dead key fails as loudly as a new hit. Each key requires a reason read from source. |
| T-27-12-05 | Information Disclosure | STRICT host anonymisation is still inoperative on every serialized emission | medium | transfer | `AR-27-04`. OPEN, recorded, measured, and explicitly requiring a HUMAN decision that `mode: yolo` cannot supply. Out of scope by prohibition — half the fix ships an anonymised `request` beside a verbatim `url`. Transferred to the maintainer via `27-HUMAN-UAT.md` by plan 27-13. |
| T-27-12-06 | Denial of Service | A whole-tree source scan on every test run slows CI | low | accept | The scan is a single walk of `src/test/kotlin` with per-file string work, the same cost class as the two existing repository-state tests, both of which run in the same suite today without incident. |
| T-27-12-07 | Spoofing | The OFF replacement test itself becomes a survival pin | medium | mitigate | It asserts byte identity with `assertEquals` and names no sensitive value in an `assertTrue`, so the sweep's own vocabulary cannot match it and it cannot be misread as a redacting-mode expectation. |
| T-27-12-12 | Repudiation | Task 1's red probe reads non-zero at plan end because task 2's fixtures re-match it, and the plan's headline invariant appears to fail | medium | mitigate | Both the criterion and `<verification>` item 1 carry `--exclude='RedactingPolicySurvivalSweepTest.kt'` as part of the gate's stated bound, with all four measured boundary values recorded (`2`/`2` at step 0, `0`/`2` at plan end) and the two remaining unexcluded lines NAMED as fixture copies. |
| T-27-12-SC | Tampering | dependency installs | high | accept | This plan adds NO dependency. Gate: `git diff -- build.gradle.kts gradle/libs.versions.toml` shows zero lines at plan end. No `[ASSUMED]`/`[SUS]` package exists, so no blocking legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. `grep -rn --include='*.kt' --exclude='RedactingPolicySurvivalSweepTest.kt' -A3 'assertTrue(' src/test | sed -E 's/^[^ ]*\.kt[:-][0-9]+[:-]//' | grep -v '^[[:space:]]*[/*]' | grep -c 'api\.example\.com'` returns `2` BEFORE task 1's deletion and `0` after, AND STILL `0` AT PLAN END. The `-A3` window is required because the pins span two lines; the prefix strip is required because `grep -rn` makes a leading-whitespace comment filter unmatchable; and `--exclude='RedactingPolicySurvivalSweepTest.kt'` IS PART OF THE GATE'S STATED BOUND, because task 2 puts verbatim copies of both deleted pins into that file as its positive fixtures and fixture lines are not comment lines.

   ALL FOUR BOUNDARY VALUES, MEASURED at planning time, recorded so the two runs are never confused:
   - task-1 step 0, sweep file does not exist yet: `2` excluded, `2` unexcluded — the exclusion costs the red probe NOTHING.
   - task-1 end, sweep file still does not exist: `0` excluded, `0` unexcluded.
   - PLAN END, after task 2: `0` excluded, `2` unexcluded. The two remaining lines are NAMED as what they are — the `rawMessageFinalText` and `rawFinalText` fixture copies inside `RedactingPolicySurvivalSweepTest.kt`, not a regression. An unqualified `0` at plan end would be false; an unqualified `2` would be unreadable. The gate carries its exclusion, and the number carries its scope.
2. `RedactingPolicySurvivalSweepTest` is green with an EMPTY hit set and an EMPTY allowlist, INCLUDING over its own file, which it scans without exclusion. The clean self-scan is FALSIFIABLE: `theRawStringSkipIsWhyTheSelfScanIsClean` asserts the same detector over the same file WITHOUT the raw-string skip returns a NON-EMPTY hit set (measured 2 unskipped, 0 skipped), so a detector that had quietly stopped matching fails rather than passes.
2c. The three constructed exclusions are in the detector and the `ALLOWLIST` is empty. Measured bounds recorded: unqualified vocabulary reports 10 hits on the pre-round tree and 7 post-fix; with the exclusions, exactly 3 and exactly 0.
2b. The blank-line hazard is machine-checked: `BLANK_LINE_HAZARD_FIXTURE`'s isolated body carries at least two blank lines and both tokens, and is flagged.
3. The pre-round detector run reports exactly three hits, with SHA, command and raw output recorded.
4. `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew check` and `./gradlew test` both exit zero.
5. `git diff -- .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` is EMPTY — the
   record is plan 27-13's to amend, and a record edited from two plans is a record with two owners.
6. `git diff -- .planning/REQUIREMENTS.md` is EMPTY.
</verification>

<success_criteria>
- No green assertion under `src/` says a sensitive value survives a redacting policy.
- That statement is enforced by CI, with its vocabulary stated and its blind axes named.
- The enforcement is demonstrated against the three real artifacts, reproducibly.
- `AR-27-04` is untouched, still open, and still owed a human decision.
</success_criteria>

<output>
Create `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-12-SUMMARY.md` when done.

It MUST carry, verbatim: task 1's step-0 PRE-STATE count and the two lines it matched, beside the
post-edit count, so the gate is on the record as a red probe and not as a green number; ALL FOUR
boundary values of that gate — `2` excluded and `2` unexcluded at step 0, `0` excluded and `2`
unexcluded at plan end — with the two plan-end unexcluded lines NAMED as fixture copies rather than left
to read as a regression; the note that the `assertFalse` companion gate is a negative invariant reading
`0` on both sides, and that the `AR-27-04` count gate is a presence invariant reading `4` on both sides;
the pre-round SHA resolved ONCE in task 2 and how it was resolved, reused unchanged by task 3; the
detector's raw output over the pre-round contents showing all three hits; the measured `.kt` file count
under `src/test/kotlin` and the floor set below it; the blank-line count measured inside
`BLANK_LINE_HAZARD_FIXTURE`'s isolated body; the sweep's zero hits over its OWN file together with the
statement that no self-exclusion was added AND the NON-ZERO count the same detector returns over the
same file without the raw-string skip, which is what makes the zero meaningful; the three constructed
exclusions with the measured pre-existing count each one accounts for (5 benign-accessor, 1
pre-redaction guard, 1 negated containment) and the statement that the `ALLOWLIST` stayed empty; the
empty hit set on the post-fix tree beside the unqualified vocabulary's count on the same tree; and the
sentence distinguishing the durable in-file check from the one-time tree run, so plan 27-13 files the
standing rule from a measurement rather than from a claim.
</output>
