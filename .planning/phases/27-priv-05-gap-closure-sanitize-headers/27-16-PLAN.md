---
phase: 27-priv-05-gap-closure-sanitize-headers
plan: 16
type: execute
gap_closure: true
wave: 16
depends_on: ["27-14", "27-15"]
autonomous: true
requirements: [PRIV-05]
files_modified:
  - .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
  - src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt
  - .planning/ROADMAP.md
  - .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md
  - .planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md
  - .planning/codebase/CONCERNS.md
  - .planning/WINDOWS.md
  - .planning/STATE.md

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "T-26-02-01 carries a SEVENTH clause recording the FIFTH refutation, and clauses (1) through (6) survive as an EXACT CHARACTER PREFIX of the amended row — verified byte-for-byte, not by eye. The threat has now been closed wrongly four times and re-opened a fifth; that history, preserved in order, is the most instructive artifact in the register and no part of it may be rewritten to read better."
    - "The two residuals ROUND 4 INTRODUCED are on the record, which is exactly what round 4's own six-residual list omitted: the bare-quote logical-line start with its 1589-of-1714-character measurement quoted verbatim AND the fix that closed it, and the sweep's declaration-shape blindness with its 136-of-1779 and 67-across-9-files measurements AND the widening that closed it. A residual list that names only what a round inherited is the shape of gap 4."
    - "AR-27-11 is defined for the first time — a header at the open of a JSON ARRAY ELEMENT string is not a recognised logical-line start after the narrowing — with its severity assigned from a measurement taken this round, its reachability stated as measured or as unmeasured rather than assumed, and its owner named. It is DELIBERATELY NOT PINNED BY ANY COMMITTED TEST, on the AR-27-09 precedent and standing-rule clause (vi)."
    - "A new standing rule is added: a residual list must enumerate what the round INTRODUCED, not only what it INHERITED, and the two must be visibly separated. Its worked example is round 4's own failure, and round 5 applies it to ITSELF — naming AR-27-11 and the multi-line-signature axis as residuals THIS round created, beside the inherited ones."
    - "`threats_open` is RECOMPUTED from the register rows by the documented `awk` command, never hand-edited, and its POPULATION is restated in full rather than cross-referenced. The question that population forces is asked and answered in writing for AR-27-11: it is an `AR-` finding, so it sits outside the counter at any severity, and if it had landed at or above the blocking gate the honest options were exactly two and leaving the counter alone was not among them."
    - "AR-27-04's disposition is NOT relitigated. It stays OPEN, at MEDIUM, with its honestly-disclosed auto-selected provenance intact, and it still owes a human decision. AR-27-08 and `InjectionPointExtractor.kt:29` stay owned by Phase 28 and are untouched. `T-27-06-06` is untouched."
    - "The provenance of round 5's one decision — narrow the start rather than keep the bare quote — is recorded WITHOUT claiming what no artifact can corroborate. It was directed by the round-5 planning brief and backed by two independent measurements (`27-REVIEW-2.md` CR-03 and `27-VERIFICATION-4.md` gap 1); `.planning/config.json` still carries `mode: yolo`, so whether a maintainer personally chose it is carried as a human confirmation item exactly as round 4's equivalent was."
    - "PHASE 27 STILL CLOSES WITH PRIV-05 NOT SATISFIED. `ROADMAP.md` says so for the fifth round, `REQUIREMENTS.md` is UNTOUCHED across the whole round-5 range, and PRIV-05 stays `[ ]`. Round 5 closed two blockers and a review finding; it did not close the requirement, and every plan in this round says so."
  artifacts:
    - ".planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md — T-26-02-01 clause (7), AR-27-11 defined with quoted measured evidence, the round-4-introduced residuals recorded with their measurements and their fixes, standing-rule clause (vii), a new audit-trail row, and a recomputed `threats_open` with its population restated"
    - "src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt — `THIRD_OPEN_FINDING` pinning `AR-27-11` present in the rationale region, so the residual stays traceable from source rather than surviving as an aside"
    - ".planning/ROADMAP.md — the round-5 note, three plan entries under waves 14-16, the residual list appended with an explicit INTRODUCED-versus-INHERITED split, and the unchanged not-satisfied paragraph"
    - ".planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md — the round-4 items carried forward legible as unanswered, item 1 of round 4's `human_verification` recorded as ANSWERED BY FIX with honest provenance, and a new disposition item for AR-27-11"
    - ".planning/codebase/CONCERNS.md — a fifth dated W-A amendment, scoped, recording that the auth family inherited the bare-quote start and its repair"
    - ".planning/WINDOWS.md — one entry per premise round 5 falsified, each carrying its observed number"
    - ".planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md — extended to plans 27-14 through 27-16 on the same reasoned declaration"
    - ".planning/STATE.md — position and activity"
  key_links:
    - "`27-14-SUMMARY`'s PROBE D output -> AR-27-11. The link whose absence is the whole of gap 4: round 4 measured nothing about the residual it created, so nothing could be filed."
    - "`Redaction.kt`'s AR-27-11 citation -> `LogicalLineBoundaryScopeTest.THIRD_OPEN_FINDING` -> `26-SECURITY.md`. The three-way traceability AR-27-09 already has, applied to the residual this round creates."
    - "standing-rule clause (vii) -> round 5's own residual list. A rule whose first application is to the round that wrote it is the only kind this phase's history justifies."
---

<objective>
Close `27-VERIFICATION-4.md` **gap 4** and record round 5.

Round 4's residual list named six residuals — AR-27-09, AR-27-10, AR-27-04, AR-27-08, the
`CONCERNS.md` vendor auth-header class, and the sweep's vocabulary bound. Each is real, correctly
severity-assigned and correctly owned; the verifier re-measured two of them and both held exactly as
recorded. The failure is that **all six were INHERITED**. The list names neither residual round 4
CREATED: the bare-quote logical-line start, a shipped correctness regression in no record anywhere,
and the sweep's declaration-shape blindness, which the register affirmatively MISSTATED. A round
whose central lesson is "a stated bound wider than its control is the defect" closed with a stated
bound wider than its control in two places, and nothing in the record marked the distinction between
what a round inherits and what it introduces.

Purpose: this is the fifth round. Four of the previous four were closed wrongly. The one artifact
that has consistently held up under adversarial re-measurement is this register — and it holds up
because it appends rather than rewrites, recomputes rather than asserts, and states its population.
This plan keeps it that way while adding the one rule it was missing.

Output: clause (7), AR-27-11, standing-rule clause (vii), a recomputed counter with its population
restated, and a round-5 record that applies the new rule to itself.

**THIS PLAN CLOSES NO REQUIREMENT.** Round 5 does not close PRIV-05. `REQUIREMENTS.md` must not be
edited by any task in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-4.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-14-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-15-SUMMARY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-13-SUMMARY.md
@.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md
@.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md
</context>

<artifacts_this_phase_produces>
Symbols and record entries created by plan 27-16.

| Symbol | File | Kind | Note |
|---|---|---|---|
| `AR-27-11` | `26-SECURITY.md` Accepted Risks Log | NEW finding | the JSON-array-element logical-line start, the measured cost of round 5's narrowing. Cited forward by plan 27-14 in `Redaction.kt`, on the AR-27-09 precedent. |
| T-26-02-01 clause (7) | `26-SECURITY.md` Threat Register | APPENDED | clauses (1)-(6) preserved as an exact character prefix |
| standing-rule clause (vii) | `26-SECURITY.md` | NEW | a residual list must enumerate what the round INTRODUCED, not only what it INHERITED |
| `LogicalLineBoundaryScopeTest.THIRD_OPEN_FINDING` | `redact/LogicalLineBoundaryScopeTest.kt` | NEW companion constant | value `AR-27-11`, asserted present in the rationale region by the existing `theStatedBoundIsPresentWhereAReaderMeetsIt` |
| round-5 residual list | `ROADMAP.md` | APPENDED | with an explicit INTRODUCED-versus-INHERITED split |
| `27-HUMAN-UAT.md` round-5 section | `27-HUMAN-UAT.md` | APPENDED | round-4 item 1 recorded ANSWERED BY FIX with honest provenance; a new AR-27-11 disposition item; the carried-forward items still legible as unanswered |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Clause (7), AR-27-11, and a recomputed counter with its population restated</name>

  <precondition>Plans 27-14 and 27-15 are committed and their SUMMARYs exist. Every number this task writes is quoted from one of those two SUMMARYs or recomputed here; a record written before the measurements exist is the pattern that produced four wrong closures.</precondition>

  <files>.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md</files>

  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` frontmatter in full (lines 1-95) — the `awk` command, its POPULATION paragraph, the "question that population forces" paragraph, and the per-finding severity attribution list. This is the format the amendment must extend, not replace.
    - The same file, the T-26-02-01 row — clauses (1) through (6) in order, especially clause (6)'s opening, so clause (7) follows the same shape and the prefix stays exact.
    - The same file, the Accepted Risks Log rows AR-27-09 and AR-27-10 — the format for a NEW OPEN finding: mechanism in one sentence, evidence quoted verbatim from a SUMMARY with the harness named, severity with both aggravating and mitigating properties in one breath, what is measured versus what is inferred labelled as such, the fix or the cost of the fix written down, and a named owner.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-14-SUMMARY.md` — PROBE A, PROBE B, PROBE C and PROBE D with both columns, which are the evidence for clause (7) and for AR-27-11.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-15-SUMMARY.md` — the six-shape table, the three-hit historical re-run, and every re-measured count.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-4.md` gaps 1 and 4 in full, including the artifacts lists, which name exactly which records were missing which sentence.
  </read_first>

  <action>
Append clause (7) to the T-26-02-01 row. It opens by naming what it is — the FIFTH re-opening,
dated — and states in plain English what round 4 shipped and round 5 repaired: `JSON_STRING_OPEN` was
a bare double quote presented as a JSON string open, so all three composer-built rules treated every
double quote as a logical-line start; quote the measurement verbatim from `27-14-SUMMARY.md`, the
1589-of-1714 characters and the 40-to-0 content markers, together with the fact that the JSON stayed
structurally valid, which is why every existing shape assertion passed. State that this is the FIRST
defect in this series that failed SAFE for privacy and broke CORRECTNESS instead, and that this
codebase's own standard for that — `Redaction.kt:570-576` — is what makes it a regression rather than
a trade. Then state the repair, the narrowing to a JSON string VALUE open, and the two gates plan
27-14 added. Then state the second half: the sweep's declaration gate was blind to 136 of 1779
declaration lines, 67 of them backtick-named `@Test` methods across 9 files, and clause (vi) cited its
enumeration as the check's stated bound — so the register itself overclaimed; name the widening, the
composition gate, the loud unbalanced-file failure, and the source-pinned axis count that closed it.

Preserve clauses (1) through (6) as an EXACT CHARACTER PREFIX of the amended row. Verify it, do not
assume it: extract the old row's prose and the new row's prose and assert the old is a byte-exact
prefix of the new, the way round 4 did (18,352 characters into 29,499). Record the two lengths and
the boolean in the SUMMARY. If the prefix is not exact, the amendment is wrong and must be redone —
reformatting a prior clause to read better is the one edit this row does not permit.

Define AR-27-11 as a NEW OPEN finding in the Accepted Risks Log, in the AR-27-09 format. Its
mechanism in one sentence: after round 5's narrowing, a header at the open of a JSON ARRAY ELEMENT
string is not a recognised logical-line start, because an array element's open is a bracket-quote
sequence and the boundary now recognises a colon-quote sequence. Quote PROBE D from
`27-14-SUMMARY.md` verbatim with both columns — matching before the narrowing, byte-unchanged after
— so the row records what was measured rather than what was reasoned.

Assign AR-27-11's severity FROM A MEASUREMENT taken in this task, not from intuition. MEASURE
reachability before assigning it: read `mcp/schema/Serialization.kt` and enumerate which serialized
fields on the MCP emission path are JSON ARRAYS OF STRINGS whose contents are analyst-authored or
response-derived rather than machine-generated. If a reachable carrier exists, name it and weigh the
severity accordingly. If none is measured, say so in exactly those words — the reachability is
UNMEASURED — and bound the severity at that weight, as AR-27-09's row does for the indented-header
case. State aggravating and mitigating properties in one breath, and label the inferred half as
inferred. Name the owner. State that it is DELIBERATELY NOT PINNED BY ANY COMMITTED TEST, and why:
a green test asserting this value survives a redacting policy is the artifact class clause (vi)
prohibits and `RedactingPolicySurvivalSweepTest` now detects across both name spellings.

RECOMPUTE `threats_open` by re-running the documented `awk` command from the frontmatter comment
against the amended file. Write the raw output and the row count. Do not hand-edit the value, and do
not carry the previous run's number forward. Append a dated re-run paragraph in the format the two
previous ones use, RESTATE the population in full rather than cross-referencing it, and ask the
question that population forces for AR-27-11 explicitly: it is an `AR-` row and therefore outside the
counter at any severity, and had it landed at or above the blocking gate the honest options were
exactly two — give it an id inside the population, or amend the command and its population comment
together — and leaving the counter reading its current value was not among them.

Do NOT touch the AR-27-04 row beyond nothing at all; do NOT touch AR-27-08 or the Phase 28 deferral;
do NOT touch `T-27-06-06`; do NOT add or reclassify any `T-26-` Threat Register row; do NOT edit
`REQUIREMENTS.md`.
  </action>

  <verify>
    <automated>grep -c '^| AR-27-11 ' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md</automated>
  </verify>

  <acceptance_criteria>
    - The prose of the pre-edit T-26-02-01 row is an EXACT CHARACTER PREFIX of the amended row. Assert it programmatically, and record both character lengths and the boolean in the SUMMARY. Round 4 recorded 18352 into 29499; round 5 records its own pair.
    - `grep -c '^| AR-27-11 ' .planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` returns 1, and that row carries a severity, a status of OPEN, a named owner, quoted PROBE D evidence with both columns, and an explicit statement of what is measured versus what is inferred.
    - The reachability measurement for AR-27-11 was actually taken: the SUMMARY names which serialized fields on the MCP emission path are JSON arrays of strings and whether any carries analyst-authored or response-derived text. If none does, the row and the SUMMARY both say the reachability is UNMEASURED in those words.
    - `threats_open` is the RAW OUTPUT of re-running the documented `awk` command from the file's own frontmatter comment against the amended file. The SUMMARY quotes the command, its output and the row count. The value was not carried forward from the 2026-08-26 run.
    - The dated re-run paragraph RESTATES the population in full — rows beginning with the `T-26-` prefix, and nothing else; the whole `AR-` series outside it at any severity — rather than cross-referencing the previous statement.
    - The question that population forces is asked and answered in writing for AR-27-11, naming the two honest options had it landed at or above the blocking gate.
    - `git diff` on this file shows ZERO changes to the AR-27-04, AR-27-08 and AR-27-05 rows and zero changes to any `T-26-` row other than T-26-02-01. `.planning/REQUIREMENTS.md` shows zero changes.
  </acceptance_criteria>

  <done>The register records the FIFTH re-opening with its prior clauses byte-intact, defines the residual round 5 created from a measurement rather than a prediction, and carries a counter recomputed from its own rows with its population restated.</done>
</task>

<task type="auto">
  <name>Task 2: The rule round 4 was missing — a residual list must enumerate what the round INTRODUCED — and the source pin for AR-27-11</name>

  <files>.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md, src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt</files>

  <read_first>
    - `.planning/phases/26-coverage-static-analysis-debt-docs/26-SECURITY.md` standing-rule clauses (iv), (v) and (vi) in full — each opens with a capitalised statement of the rule, then "The lesson", then a worked example drawn from this phase's own history. Clause (vii) must follow that shape exactly; a rule stated without the failure that produced it is the kind of rule nobody reads twice.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-4.md` gap 4 in full — the sentence that names the defect precisely: the list is a residual list for what was INHERITED, not for what was INTRODUCED, and nothing in the record marks that distinction.
    - `.planning/ROADMAP.md` lines 512-545 — round 4's six-residual paragraph, so clause (vii)'s worked example quotes the real artifact rather than a paraphrase of it.
    - `src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt` lines 120-160 and 260-293 — `theStatedBoundIsPresentWhereAReaderMeetsIt`, `OPEN_FINDING`, `SECOND_OPEN_FINDING`, and the assertion shape that pins a finding id as present in the rationale region. `THIRD_OPEN_FINDING` joins that mechanism; it does not invent a new one.
    - `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` — the AR-27-11 citation plan 27-14 wrote into the rationale block, so the new constant pins a string that is actually there.
  </read_first>

  <action>
Add standing-rule clause (vii) to `26-SECURITY.md`, in the shape clauses (iv) through (vi) use. The
rule: A RESIDUAL LIST MUST ENUMERATE WHAT THE ROUND INTRODUCED, NOT ONLY WHAT IT INHERITED, AND THE
TWO MUST BE VISIBLY SEPARATED. The lesson: a round's inherited residuals are the ones it has already
thought about, so they are the ones it lists; the residuals it creates are invisible to it for the
same reason the defect was — they arrive with the change, not before it, and the author's attention
is on what the change fixes. A list of inherited residuals therefore reads as completeness while
being systematically blind in exactly the direction the round is most likely to be wrong.

The worked example is round 4's own. Quote it accurately: six residuals named, each real, each
correctly severity-assigned, two of them independently re-measured and both holding exactly as
recorded — and every one of the six INHERITED. The two round 4 CREATED appear in none of them: the
bare-quote start, which was in no source comment, no summary and no register and was gated by no
test; and the sweep's declaration-shape blindness, which the register affirmatively MISSTATED because
clause (vi) cited the sweep's enumeration as the check's stated bound. State the consequence in one
sentence: a round whose central lesson was "a stated bound wider than its control is the defect"
closed with a stated bound wider than its control in two places.

Then apply the rule to ROUND 5 IN THE SAME CLAUSE, because a rule whose first application is to some
future round is a rule that has not been tested. Round 5's INTRODUCED residuals are `AR-27-11` (the
JSON-array-element logical-line start, created by narrowing the boundary) and the
multi-line-signature declaration axis (created by widening the sweep's declaration regex, and
enumerated in that file by plan 27-15). Round 5's INHERITED residuals are the ones round 4 named,
minus anything round 5 closed, and the compound-assertion negation over-fire, which round 5 named and
deliberately did not fix. Present the two groups under separate headings so a reader cannot merge
them by accident.

Add a new dated audit-trail row for round 5 in the format the previous rows use.

In `LogicalLineBoundaryScopeTest`, add `THIRD_OPEN_FINDING` with value `AR-27-11` beside
`OPEN_FINDING` and `SECOND_OPEN_FINDING`, and extend `theStatedBoundIsPresentWhereAReaderMeetsIt`
with an assertion that the rationale region carries it. The failure message follows the existing
two: it must say that all THREE residuals have to stay traceable from source, that the boundary now
recognises a narrowed set of logical-line starts, and that a comment which stops naming what the
boundary cannot see leaves the next reader with a boundary that reads complete and is not — which is
how this requirement has been closed wrongly four times.

Do not rewrite clauses (i) through (vi). Clause (vi) was already amended by plan 27-15 in the same
change as the control it describes; do not amend it again here, and do not restate its counts. Do not
touch `threats_open`, which task 1 already recomputed.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests 'com.six2dez.burp.aiagent.redact.LogicalLineBoundaryScopeTest'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'THIRD_OPEN_FINDING' src/test/kotlin/com/six2dez/burp/aiagent/redact/LogicalLineBoundaryScopeTest.kt` shows the constant and at least one assertion using it, and `LogicalLineBoundaryScopeTest` is green.
    - RED PROBE: the new assertion was run against a temporarily reverted `Redaction.kt` rationale block with the AR-27-11 citation removed, and it FAILED; restored, it passes. Both runs are in the SUMMARY.
    - Clause (vii) exists, follows the rule / lesson / worked-example shape of clauses (iv) through (vi), quotes round 4's six-residual list accurately, and presents round 5's INTRODUCED and INHERITED residuals under separate headings.
    - Clause (vi) is byte-unchanged by this task. `git diff` on `26-SECURITY.md` for this task shows changes confined to the new clause (vii) region and the new audit-trail row.
    - `threats_open` is unchanged from the value task 1 computed, and no Accepted Risks Log row is modified by this task.
  </acceptance_criteria>

  <done>The register carries the rule whose absence is gap 4, and the round that wrote it is the first round it is applied to. AR-27-11 is traceable from source, register and roadmap alike.</done>
</task>

<task type="auto">
  <name>Task 3: The phase record — round-5 note, the split residual list, the answered decision with honest provenance, and the unchanged not-satisfied paragraph</name>

  <files>.planning/ROADMAP.md, .planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md, .planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md, .planning/codebase/CONCERNS.md, .planning/WINDOWS.md, .planning/STATE.md</files>

  <read_first>
    - `.planning/ROADMAP.md` lines 383-560 — the whole Phase 27 entry: the round-one goal line qualified in place rather than rewritten, the four gap-closure round notes, the wave-keyed plan lists, the six-residual paragraph, and the "PHASE 27 COMPLETES WITH PRIV-05 NOT SATISFIED" paragraph with its round-4 re-confirmation. This is an APPEND-AND-AMEND record; match its conventions exactly.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-HUMAN-UAT.md` — the round-4 carry-forward section, the "NINE ITEMS ARE STILL UNANSWERED" heading, the two-decisions provenance section with its written-out contrast against AR-27-04, and items 10 and 11. Items are numbered continuously; round 5's new item is 12.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-VERIFICATION-4.md` `human_verification` items 1 through 4 — item 1 is the bare-quote disposition round 5 answers by fixing; item 4 is the provenance confirmation whose shape round 5's own provenance record must follow.
    - `.planning/phases/27-priv-05-gap-closure-sanitize-headers/COVERAGE.md` — the reasoned no-integration declaration and the measured inventory table round 4 extended, including its explicit statement that the measurement disagreed with the plan instruction that requested it.
    - `.planning/codebase/CONCERNS.md` — the W-A entry and its four dated amendments.
    - `.planning/WINDOWS.md` entries 35, 36, 38 and 39 — the format: the projection, the measurement, and the cause, side by side.
    - `.planning/REQUIREMENTS.md` line 23 — PRIV-05 is unticked and MUST stay so.
  </read_first>

  <action>
Amend `.planning/ROADMAP.md`'s Phase 27 entry by APPENDING, never by rewriting. The planner already
wrote the "Gap closure round 5" note and the three wave-keyed plan entries under Waves 14, 15 and 16
— do NOT duplicate them. Tick each of the three plan entries as executed, update the plan count line
to read 16 of 16, and APPEND to the round-5 note a short closing paragraph stating what the round
actually measured, in the format the round-3 and round-4 notes use for their outcomes: the observed
before and after values for the narrowing, the observed six-shape declaration-gate result, and the
observed walk-composition result. If any number in the planner's note is contradicted by what was
measured, correct it IN PLACE and record the correction in `.planning/WINDOWS.md` rather than leaving
two numbers on the record.

Append to the residual paragraph rather than replacing it, and give it the split standing-rule clause
(vii) now requires: a heading for residuals ROUND 5 INTRODUCED — `AR-27-11` and the
multi-line-signature declaration axis — and a heading for those it INHERITED, with each inherited
entry carried forward with its owner unchanged. Record which of round 4's six are now CLOSED and
which are not: the sweep's declaration-shape blindness and the bare-quote start are closed by round
5; `AR-27-04`, `AR-27-08`, `InjectionPointExtractor.kt:29`, `AR-27-09`, `AR-27-10` and the
`CONCERNS.md` vendor auth-header class are unchanged and keep their owners. Keep the sentence that a
named-residual list is not a completeness claim, and keep the "PHASE 27 COMPLETES WITH PRIV-05 NOT
SATISFIED" paragraph intact with a dated fifth re-confirmation appended to it.

Amend `27-HUMAN-UAT.md` by appending a round-5 section. It must do three things. First, carry the
outstanding items forward and keep them legible as unanswered — round 5 answers none of items 1
through 11 and must say so in a heading a reader cannot miss. Second, record `27-VERIFICATION-4.md`
human_verification item 1 — the bare-quote disposition — as ANSWERED BY FIX, with the fix named and
the measurement quoted, AND with its provenance recorded honestly: the narrowing was directed by the
round-5 planning brief and independently measured twice, by `27-REVIEW-2.md` CR-03 and by
`27-VERIFICATION-4.md` gap 1; `.planning/config.json` still carries `mode: yolo`, so whether a
maintainer personally chose it is NOT codebase-verifiable and is carried as a confirmation item in
its own right, exactly as round 4's item 4 carried the equivalent question. Do not claim a maintainer
signature no artifact can corroborate — that claim is the one thing this file exists to keep
distinguishable from a harness default. Third, add item 12: the AR-27-11 disposition, a JUDGMENT
rather than an investigation — accept the array-element start as a residual, or widen the boundary to
recognise it — with the trade stated on both sides.

Extend `COVERAGE.md` to plans 27-14 through 27-16 on the same reasoned declaration it already
carries, and if the measured inventory disagrees with anything this plan set assumed, say so in the
file rather than reconciling it silently.

Append a fifth dated amendment to the W-A entry in `.planning/codebase/CONCERNS.md`, scoped and
short: `authHeaderRegex` is one of the three rules built from the shared composer, so it inherited
the bare-quote logical-line start and was measured mutating prose in STRICT; round 5's narrowing
repaired it by the same one-constant edit. The vendor auth-header class itself is UNCHANGED and stays
open by prohibition — do not re-decide it here.

Add one `.planning/WINDOWS.md` entry per premise round 5 falsified, each carrying the projection, the
observed number and the cause. At minimum: any count in this plan set that the re-measurement in plan
27-15 task 3 contradicted, and the AR-27-11 reachability question if it resolved differently from
this plan's expectation.

Update `.planning/STATE.md` position and activity for round 5.

Do NOT edit `.planning/REQUIREMENTS.md`. PRIV-05 stays unticked. Do not rewrite the round-one goal
line. Do not re-decide `AR-27-04`, `AR-27-08` or `T-27-06-06`.
  </action>

  <verify>
    <automated>git diff --stat -- .planning/REQUIREMENTS.md | wc -l; grep -c 'PRIV-05 NOT SATISFIED' .planning/ROADMAP.md; grep -c '27-14-PLAN.md' .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --stat -- .planning/REQUIREMENTS.md` across the whole round-5 commit range is EMPTY, and `grep -n 'PRIV-05' .planning/REQUIREMENTS.md` line 23 still shows an unticked checkbox.
    - `.planning/ROADMAP.md` carries entries for `27-14-PLAN.md`, `27-15-PLAN.md` and `27-16-PLAN.md` under waves 14, 15 and 16 — each appearing exactly once, so `grep -c '27-14-PLAN.md' .planning/ROADMAP.md` returns 1 — each ticked as executed, and the plan count line reads 16 of 16.
    - The residual paragraph presents INTRODUCED and INHERITED residuals under separate headings, names `AR-27-11` and the multi-line-signature axis under the first, and carries every inherited entry with its owner unchanged.
    - The "PHASE 27 COMPLETES WITH PRIV-05 NOT SATISFIED" paragraph is present with a dated fifth re-confirmation appended and its prior text unmodified.
    - `27-HUMAN-UAT.md` records item 1 as ANSWERED BY FIX with its provenance stated as directed-and-twice-measured rather than as maintainer-signed, carries a confirmation item for the provenance question itself, and adds item 12 for the AR-27-11 disposition. Items 1 through 11 remain legible as unanswered.
    - `.planning/codebase/CONCERNS.md` gains exactly one dated amendment; the vendor auth-header class statement is otherwise byte-unchanged.
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew check` is green on the final tree, or any failure is identified as the known `RedactionTest` `SafeRegex` wall-clock flake and confirmed by a single re-run on an idle machine.
  </acceptance_criteria>

  <done>The phase record states what round 5 fixed, what it introduced, what it inherited, what it did not answer, and — for the fifth time — that PRIV-05 is not satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a round's own account of itself → the next audit | A phase that grades its own homework is the mechanism behind four wrong closures. The register, the roadmap and the human-UAT file are the only artifacts a later reader has, and each is written by the round it describes. |
| `threats_open` → a reader concluding "nothing is open above the gate" | A counter whose population is unstated reads as a stronger claim than it is. A hand-edited value reads as a measurement. |
| a residual list → a reader concluding "this is what remains" | The gap-4 boundary. A list of inherited residuals is systematically blind in the direction the round is most likely to be wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-16-01 | Repudiation | T-26-02-01 clause history | high | mitigate | Reformatting or condensing a prior clause destroys the only ordered account of four wrong closures. Task 1 appends clause (7) and asserts PROGRAMMATICALLY that the pre-edit prose is a byte-exact character prefix of the amended row, recording both lengths and the boolean. |
| T-27-16-02 | Spoofing | `threats_open` frontmatter counter | high | mitigate | A hand-written counter is how the 2026-08-24 overclaim survived review. Task 1 re-runs the documented `awk` command against the amended file, writes its raw output, restates the population in full, and asks the question that population forces for AR-27-11 in writing. |
| T-27-16-03 | Information Disclosure | AR-27-11's severity and reachability | medium | mitigate | An authored severity reads exactly like a measured one. Task 1 MEASURES reachability by enumerating JSON-array-of-string fields on the MCP emission path before assigning severity, labels the inferred half as inferred, and says UNMEASURED in those words if nothing is found. |
| T-27-16-04 | Repudiation | provenance of round 5's narrowing decision | medium | transfer | `.planning/config.json` still carries `mode: yolo`, so no artifact can establish that a maintainer personally chose the narrowing. Task 3 records the decision as directed-and-twice-measured, does NOT claim a signature, and transfers the confirmation to a `27-HUMAN-UAT.md` item on the round-4 item-4 precedent. |
| T-27-16-05 | Elevation of Privilege | the tick on PRIV-05 | high | mitigate | Ticking a requirement a round did not satisfy is the failure this phase has repeated four times. Every plan in round 5 states that it closes no requirement; task 3's acceptance criteria assert `REQUIREMENTS.md` is byte-unchanged across the whole round-5 range and that the not-satisfied paragraph carries a fifth dated re-confirmation. |
| T-27-16-06 | Tampering | AR-27-04, AR-27-08 and `T-27-06-06` | medium | accept | Relitigating a recorded deferral inside a records plan would launder a harness default into a decision. All three are explicitly out of scope; task 1 and task 2 assert their rows are byte-unchanged. |
| T-27-16-SC | Tampering | npm / pip / cargo installs | low | accept | This plan adds no package-manager dependency and contains no install task, so the package-legitimacy gate has an empty population here. The Gradle dependency set is unchanged. |
</threat_model>

<verification>
- Clauses (1) through (6) of T-26-02-01 are an exact character prefix of the amended row, asserted programmatically with both lengths recorded.
- AR-27-11 exists exactly once, with a measured severity, a stated reachability, a named owner, quoted both-column evidence, and no committed test pinning it.
- `threats_open` is the raw output of the documented command re-run against the amended file, with the population restated in full and the question it forces answered for AR-27-11.
- Standing-rule clause (vii) exists, quotes round 4's six-residual list accurately, and is applied to round 5's own residuals under separate INTRODUCED and INHERITED headings.
- `THIRD_OPEN_FINDING` pins `AR-27-11` present in `Redaction.kt`'s rationale region and fails when that citation is removed.
- `ROADMAP.md` carries the round-5 note, three wave-keyed plan entries, the split residual list, and a fifth dated re-confirmation that PRIV-05 is not satisfied.
- `27-HUMAN-UAT.md` records round-4 item 1 as answered by fix with honest provenance, adds item 12, and keeps items 1 through 11 legible as unanswered.
- `.planning/REQUIREMENTS.md` shows zero changes across the entire round-5 commit range and PRIV-05 remains unticked.
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew check` is green, or a failure is identified as the known `RedactionTest` wall-clock flake and confirmed by a single idle re-run.
</verification>

<success_criteria>
- Gap 4 is closed: both residuals round 4 introduced are on the record with their measurements and their fixes, and the rule whose absence caused the omission is written down and applied to the round that wrote it.
- The counter is recomputed, its population restated, and the question it forces answered for the one new finding.
- Round 5 has not closed PRIV-05, and the record says so for the fifth time.
</success_criteria>

<output>
Create `.planning/phases/27-priv-05-gap-closure-sanitize-headers/27-16-SUMMARY.md` when done.
It must carry the byte-exact prefix assertion with both character lengths, the raw output and row
count of the recomputed counter, the AR-27-11 reachability measurement with the fields enumerated,
the red run of the `THIRD_OPEN_FINDING` assertion, the final `check` result, and a `## Threat Flags`
section reporting each `T-27-16-*` row above.
</output>