---
phase: 21-redaction-completeness
plan: 20
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [PRIV-05]
files_modified:
  - .planning/codebase/CONCERNS.md
  - .planning/phases/21-redaction-completeness/21-19-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "G-1 — THE ATTRIBUTION IS CORRECTED AT BOTH SITES. `CONCERNS.md:65` and `21-19-SUMMARY.md` state what plan 21-19 ACTUALLY closed — the passive-scan PROMPT carrier, for HYPHENATED name shapes — and attribute residuals (a), (b) and (c) to Phase 27 by plan id. HEAD: `CONCERNS.md:65` opens `**W-A CLOSED — the cookie header NAME class is fixed, not accepted.**`, which claims the CLASS; the class was closed by Phase 27, not by 21-19."
    - "THE THREE RESIDUALS ARE NAMED WITH THEIR MECHANISM AND THEIR CLOSER, NOT SUMMARISED. (a) `McpToolHelpers.sanitizeHeaders` compared `lowered == \"cookie\" || lowered == \"set-cookie\"` — exact names — so all five variants survived `request_parse`/`response_parse`; closed by 27-01 via `Redaction.isCookieHeaderName`. (b) `Serialization.kt` JSON-escapes every CR/LF, so the `(?im)^` anchor never landed on the tool-result carrier; closed by 27-04/27-11/27-14/27-17. (c) `COOKIE_NAME_PART` was `[A-Za-z0-9-]*`, excluding `_`, so `X_Cookie`, `my_cookie` and `session_cookie` leaked ON THE PROMPT PATH — W-A's own carrier — under STRICT and BALANCED; closed by 27-10."
    - "RESIDUAL (c) IS RECORDED AS THE SHARPEST, WITH ITS REASON. 21-19 did not fully close W-A even on the one path W-A was about, and its own `cookieHeaderNameVariantsAreStripped` test could not see it because all five fixtures use hyphens. `sanitizeHeadersForPrompt` is an ADMITTER: a name it admits that the regex cannot match is put on the outbound prompt and then not removed, so the difference set was fail-OPEN. That is the same admitter-vs-redactor asymmetry W-A itself was, reintroduced one character wide by the fix for it. A correction that omits this reduces a mechanism to a bookkeeping error."
    - "THE 6/6 IS NOT DISTURBED. `21-VERIFICATION.md` scored SC1–SC6 6/6 on re-check and the correction does not touch that. What is corrected is the claim layered on top afterwards. Both edits must say so explicitly, so a reader does not infer the phase's redaction work was unsound — the verifier's own words are that it `has survived a substantial downstream rewrite without a single guard going red`."
    - "G-2 — PLAN 21-19 EXISTS IN THE PROJECT RECORD. `.planning/ROADMAP.md` gains a `21-19-PLAN.md` entry in the phase-21 plan list immediately after the `21-18-PLAN.md` line (line 187 at HEAD), and `**Plans**: 18 plans` at line 112 becomes 19 with its wave breakdown updated to account for the third gap-closure round. HEAD: `grep -c '21-19' .planning/ROADMAP.md` is **0** and `grep -c '21-19' .planning/STATE.md` is **0**, while `21-19-PLAN.md` and `21-19-SUMMARY.md` both exist on disk."
    - "THE OMISSION IS RECORDED AS A CAUSE, NOT JUST FIXED. The entry states that 21-19 was executed, committed and summarised but never recorded as a plan and never re-verified, and that this is why an incomplete fix survived to the v0.10.0 milestone audit and became Phase 27's five rounds of rework. A silent insertion would erase the one piece of evidence that explains the cost."
    - "[edge:idempotence] NEITHER EDIT DOUBLE-APPLIES. `CONCERNS.md` already carries SIX Phase-27 amendments beneath line 65; the correction AMENDS the line-65 headline rather than appending a seventh amendment that repeats them. Verify before writing: `grep -c 'AMENDMENT' .planning/codebase/CONCERNS.md` and record the value; the count must not increase by more than 1."
    - "[edge:append-only] THE PRIOR TEXT IS AMENDED, NEVER REWRITTEN. Both records follow the phase-27/28 append-and-amend discipline already established in this repository: prior wording is preserved so a reader can see what was believed, with a dated marker naming what it supersedes and what it does NOT. `git diff --numstat` on `CONCERNS.md` must show additions strictly greater than deletions."
    - "G-3 IS RECORDED AS RESOLVED-BY-MEASUREMENT, NOT SILENTLY DROPPED. `21-VERIFICATION.md`'s G-3 called `./gradlew check` RED on redact BRANCH `0.92784` vs the `0.930` floor. Re-measured at HEAD from `build/reports/jacoco/test/jacocoTestReport.xml`: **missed 13 / covered 181 = 0.93299 — PASS**; LINE `0.97978` vs `0.975` — PASS. One branch was covered between the verification and now. The plan records this rather than leaving a BLOCKER in the record with no disposition."
    - "`.planning/REQUIREMENTS.md` IS BYTE-UNCHANGED at sha256 `9b3219662ec0d007c1c82d64eed3ef2698bd306ce69f01205ac9bbc3f42fcfb4`, PRIV-05 still `- [ ]`. The verifier's own finding is that PRIV-05 is correctly unticked because `AR-27-08` is open — this plan corrects a claim ABOUT PRIV-05 and must not touch PRIV-05 itself."
  prohibitions:
    - "MUST NOT re-score, soften or edit `21-VERIFICATION.md`. Its 8/9 and its G-1/G-2/G-3 findings are the input to this plan, not its output. A later re-verification decides whether the gaps closed; this plan does not get to mark its own homework."
    - "MUST NOT tick PRIV-05 or edit `.planning/REQUIREMENTS.md` at all. Enforced by the sha256 gate, run twice."
    - "MUST NOT claim the corrections close `AR-27-08` or make PRIV-05 'by any path' true. They do not. `AR-27-08` is open and owned by Phase 28, which accepted a further residual (`D-28-09`) rather than closing it. The corrected text must leave PRIV-05's lead sentence still stronger than what ships, because it is."
    - "MUST NOT touch any file under `src/`. This plan changes no code and adds no test. `git status --porcelain -- src/` must print nothing at every commit."
    - "MUST NOT run `./gradlew check` as a gate. It is currently RED for an ENVIRONMENTAL reason unrelated to this phase — `McpSupervisorProbeTest` fails with `java.net.BindException: Address already in use` because a live Burp MCP server holds the port. All 46 `RedactionTest` cases and every other redact test pass. Do not chase it, and do not record it as a phase-21 finding."
  artifacts:
    - ".planning/codebase/CONCERNS.md — line-65 headline amended: what 21-19 closed, what Phase 27 closed, residuals (a)/(b)/(c) attributed by plan id, with (c)'s admitter-vs-redactor mechanism stated"
    - ".planning/phases/21-redaction-completeness/21-19-SUMMARY.md — the `Closed the last open finding of the phase` claim (line 50) and the `NAME class is closed` claim (line 171) amended to their true scope, with a dated correction marker"
    - ".planning/ROADMAP.md — `21-19-PLAN.md` entry added after the 21-18 line; `**Plans**: 18 plans` (line 112) corrected to 19 with the wave breakdown updated"
    - ".planning/STATE.md — 21-19 reflected in the phase-21 plan record"
---

<objective>
Make phase 21's record of itself match what phase 21 shipped.

The verifier's judgement, which this plan does not relitigate: **phase 21's redaction work is sound.**
SC1–SC6 held 6/6 on re-check and survived a substantial downstream rewrite without a single guard
going red. What did not hold is the phase's account of itself.

Plan 21-19 closed **one of two carriers** and **five of six name shapes** on the carrier it did close,
and was recorded as a complete closure of the W-A class. It stayed that way for twelve days until a
milestone audit re-opened PRIV-05, and the missing closure became Phase 27's five rounds of rework.
21-19 itself was never added to `ROADMAP.md` or `STATE.md`, so nothing re-verified it.

Two of the verification's three gaps are live. The third is not: G-3's coverage blocker has since
been measured green and this plan records that rather than leaving an undisposed BLOCKER behind.

Output: two corrected records, one restored plan entry, and no code change of any kind.
</objective>

<context>
From `21-VERIFICATION.md` (re-verified 2026-08-27, `status: gaps_found`, `score: 8/9`).

Re-measured by the orchestrator at HEAD `69e54e1` before this plan was written:

| Gap | Recorded | At HEAD |
|---|---|---|
| G-1 record accuracy | `CONCERNS.md:65` + `21-19-SUMMARY.md` overstate | **LIVE** — the `W-A CLOSED … the cookie header NAME class is fixed` headline stands at line 65 |
| G-2 process | 21-19 absent from ROADMAP/STATE | **LIVE** — `grep -c '21-19'` returns 0 in both; `**Plans**: 18 plans` at ROADMAP:112 |
| G-3 live blocker | redact BRANCH 0.92784 < 0.930 floor | **STALE — passes.** missed 13 / covered 181 = **0.93299**; LINE 0.97978 vs 0.975 |

G-3's own text noted it "contradicts the standing build note that check is green end to end at
`0.93299`" — that is exactly the value measured now, so the build note was right and the G-3
snapshot has been overtaken.

Edit sites, pinned at HEAD:
- `.planning/codebase/CONCERNS.md:65` — the `W-A CLOSED` headline, with six Phase-27 amendments below it
- `.planning/phases/21-redaction-completeness/21-19-SUMMARY.md:50` — "Closed the last open finding of the phase…"
- `…/21-19-SUMMARY.md:171` — "**NAME** class is **closed** — recorded as fixed, not as an accepted residual"
- `.planning/ROADMAP.md:112` — `**Plans**: 18 plans — 7 original in 4 waves, 5 gap-closure in 4 waves, plus 6 second-round gap-closure in 5 waves`
- `.planning/ROADMAP.md:187` — the `21-18-PLAN.md` list entry; insert after it
</context>

<tasks>

<task id="1" name="Correct the attribution in CONCERNS.md">
<files>.planning/codebase/CONCERNS.md</files>

<action>
Capture the base SHA first: `BASE=$(git rev-parse HEAD)`. Every diff gate below pins to `$BASE`,
because `execute-plan.md` commits per task and an unpinned `git diff` measures nothing after the
first commit.

Record the pre-edit amendment count: `grep -c 'AMENDMENT' .planning/codebase/CONCERNS.md`.

Amend the line-65 headline. It currently claims the cookie header NAME **class** was closed by the
2026-08-13 maintainer decision. Correct it to state:

1. What 21-19 actually closed — the passive-scan **prompt** carrier, for **hyphenated** name shapes.
2. That the **class** was closed by Phase 27, with residuals (a), (b), (c) attributed by plan id:
   - (a) `McpToolHelpers.sanitizeHeaders` used exact-name equality, so all five variants survived
     `request_parse`/`response_parse` — a second carrier 21-19 never touched. Closed by **27-01**
     (`Redaction.isCookieHeaderName`, now shared by all three sites).
   - (b) `Serialization.kt` JSON-escapes every CR/LF, so `(?im)^` never landed on the tool-result
     carrier — even the widened regex could not fire there. Closed by **27-04 / 27-11 / 27-14 / 27-17**.
   - (c) `COOKIE_NAME_PART` was `[A-Za-z0-9-]*`, excluding `_`, so `X_Cookie`, `my_cookie` and
     `session_cookie` leaked **on the prompt path — W-A's own carrier** — under STRICT and BALANCED.
     Closed by **27-10**.
3. Why (c) is the sharpest: 21-19 did not fully close W-A even on the single path W-A was about, and
   its own `cookieHeaderNameVariantsAreStripped` test could not see it because all five fixtures use
   hyphens. `sanitizeHeadersForPrompt` is an **admitter** — a name it admits that the regex cannot
   match reaches the prompt and is then not removed — so the difference set was fail-**OPEN**. Same
   admitter-vs-redactor asymmetry W-A itself was, reintroduced one character wide by the fix for it.
4. That SC1–SC6 remain 6/6 and this corrects a claim layered on top, not the redaction work.

Follow the append-and-amend discipline already established in this file: preserve the prior wording
so a reader can see what was believed, under a dated marker naming what it supersedes and what it
does NOT. Do **not** append a seventh AMENDMENT block that restates the six below — amend the
headline itself.
</action>

<verification>
```bash
grep -c "AMENDMENT" .planning/codebase/CONCERNS.md          # must not exceed the pre-edit count + 1
grep -c "27-01" .planning/codebase/CONCERNS.md              # >= 1
grep -c "27-10" .planning/codebase/CONCERNS.md              # >= 1
grep -c "admitter" .planning/codebase/CONCERNS.md           # >= 1  (mechanism stated, not summarised)
git diff --numstat "$BASE" -- .planning/codebase/CONCERNS.md   # additions > deletions
git status --porcelain -- src/                              # empty
```
</verification>

<commit>docs(21-20): attribute the W-A class closure to Phase 27, not 21-19</commit>
</task>

<task id="2" name="Correct 21-19-SUMMARY's own closure claim">
<files>.planning/phases/21-redaction-completeness/21-19-SUMMARY.md</files>

<action>
Amend two claims to their true scope, under one dated correction marker naming
`21-VERIFICATION.md` G-1 as where the overstatement was found:

- **Line 50** — "Closed the last open finding of the phase: any header whose NAME contains `cookie`
  is now stripped under STRICT and BALANCED…". It was not the last open finding, and "any header
  whose NAME contains `cookie`" was not true at the time: `COOKIE_NAME_PART` excluded `_`.
- **Line 171** — "**NAME** class is **closed** — recorded as fixed, not as an accepted residual".
  The class was not closed here; three residuals remained and Phase 27 closed them.

State what 21-19 DID close (prompt carrier, hyphenated shapes) and name Phase 27 as the closer of the
class. Do not delete the original claims — this file is a historical record of what was believed on
2026-08-13, and the append-and-amend rule applies to it as much as to `CONCERNS.md`.

Also record, in the same marker, that 21-19 was never added to `ROADMAP.md` or `STATE.md` and was
therefore never re-verified — the process failure task 3 repairs — and that this is why the
incomplete fix survived twelve days to the milestone audit.
</action>

<verification>
```bash
S=.planning/phases/21-redaction-completeness/21-19-SUMMARY.md
grep -c "Phase 27\|27-01\|27-10" "$S"      # >= 1
grep -c "prompt carrier\|prompt path"  "$S" # >= 1  (what it actually closed, named)
grep -c "hyphen" "$S"                       # >= 1  (the shape limitation, named)
git diff --numstat "$BASE" -- "$S"          # additions > deletions
git status --porcelain -- src/              # empty
```
</verification>

<commit>docs(21-20): correct 21-19's closure claim to its measured scope</commit>
</task>

<task id="3" name="Restore 21-19 to the project record and close out">
<files>.planning/ROADMAP.md, .planning/STATE.md</files>

<action>
1. Insert a `21-19-PLAN.md` entry in the phase-21 plan list, immediately AFTER the `21-18-PLAN.md`
   line (line 187 at HEAD). Match the surrounding entries' `- [x] 21-NN-PLAN.md — <what it did>`
   shape. The description must say what 21-19 actually closed (prompt carrier, hyphenated shapes)
   and note that Phase 27 closed the remaining class — not repeat the overstatement being corrected.
2. Correct `**Plans**: 18 plans — 7 original in 4 waves, 5 gap-closure in 4 waves, plus 6
   second-round gap-closure in 5 waves` (line 112) to 19, with the wave breakdown updated to account
   for the third gap-closure round.
3. Record the omission as a cause: 21-19 was executed, committed and summarised but never recorded
   as a plan and never re-verified, which is why an incomplete fix survived to the v0.10.0 milestone
   audit and became Phase 27's five rounds of rework.
4. Reflect 21-19 in `STATE.md`'s phase-21 plan record.
5. Record G-3's disposition in the SUMMARY: re-measured at HEAD from
   `build/reports/jacoco/test/jacocoTestReport.xml` as redact BRANCH missed 13 / covered 181 =
   **0.93299** against the `0.930` floor (PASS) and LINE **0.97978** against `0.975` (PASS) — the
   blocker is resolved by measurement, not by decision, and one branch was covered between the
   verification and now.
6. Run the REQUIREMENTS gate TWICE under distinct headings, recording raw output both times — once
   in-task, once immediately before the final commit:
   ```bash
   shasum -a 256 .planning/REQUIREMENTS.md   # expect 9b3219662ec0d007c1c82d64eed3ef2698bd306ce69f01205ac9bbc3f42fcfb4
   sed -n '23p' .planning/REQUIREMENTS.md    # expect PRIV-05 still "- [ ]"
   git status --porcelain .planning/REQUIREMENTS.md   # expect empty
   ```
   The second run exists because in this project `gsd-tools query phase.complete` has silently
   ticked an unsatisfied requirement twice (`WINDOWS.md` 54 and 57), always at phase-completion time
   — after the last task. A single in-task run is structurally blind to it.
7. Write `21-20-SUMMARY.md`.

Do NOT run `./gradlew check` as a gate — see the prohibition. If you want a sanity signal, run
`--tests '*Redaction*'` only.
</action>

<verification>
```bash
grep -c "21-19" .planning/ROADMAP.md                    # >= 2  (list entry + plan count context)
grep -c "21-19" .planning/STATE.md                      # >= 1
grep -c '\*\*Plans\*\*: 18 plans' .planning/ROADMAP.md  # 0
grep -c '\*\*Plans\*\*: 19 plans' .planning/ROADMAP.md  # 1
shasum -a 256 .planning/REQUIREMENTS.md                 # 9b3219662ec0d007…
sed -n '23p' .planning/REQUIREMENTS.md | grep -c '^- \[ \] \*\*PRIV-05\*\*'   # 1
git status --porcelain -- src/                          # empty
git diff --stat "$BASE" HEAD -- .planning/phases/21-redaction-completeness/21-VERIFICATION.md  # empty
```
</verification>

<commit>docs(21-20): restore plan 21-19 to the project record</commit>
</task>

</tasks>

<success_criteria>
- [ ] `CONCERNS.md:65` attributes the W-A class closure to Phase 27, with (a)/(b)/(c) by plan id
- [ ] Residual (c)'s admitter-vs-redactor mechanism stated, not summarised
- [ ] `21-19-SUMMARY.md`'s two closure claims amended to their measured scope, originals preserved
- [ ] `21-19-PLAN.md` present in `ROADMAP.md`; plan count 18 → 19; present in `STATE.md`
- [ ] The omission recorded as a cause, not silently repaired
- [ ] G-3 recorded as resolved by measurement (0.93299 vs 0.930)
- [ ] `21-VERIFICATION.md` byte-unchanged — this plan does not mark its own homework
- [ ] `.planning/REQUIREMENTS.md` byte-unchanged, PRIV-05 still `- [ ]`, gate run twice
- [ ] Zero changes under `src/`
</success_criteria>
