---
phase: 21-redaction-completeness
plan: 07
type: execute
wave: 4
depends_on: ["21-06"]
files_modified:
  - DECISIONS.md
  - .planning/codebase/CONCERNS.md
  - .planning/phases/21-redaction-completeness/21-07-SUMMARY.md
transient_files:
  - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt
autonomous: true
requirements: [PRIV-05, PRIV-06]

must_haves:
  truths:
    - "SC4 is proven, not asserted: the rewritten oversize tests FAIL against a body stage carrying the pre-fix fail-open guard and PASS against the shipped one. A test green on both sides has not tested the defect — the same discipline Phase 20's SC4 established"
    - "The experiment leaves no trace: no red commit, no git stash, no modified file under src/ afterwards, and the transcript is machine-checkable rather than a claim that the tests are good"
    - "D-08 REFINED: ADR-14 exists in repo-root DECISIONS.md and claims only what ships — 'the redaction BODY STAGE never fails open', not the unqualified form, because the eight header-stage rules remain unbounded. It records D-01..D-04 and D-05 as one principle: when the pipeline cannot or will not fully redact, it fails closed and tells the user"
    - "D-08 REFINED: the header-stage gap is recorded in .planning/codebase/CONCERNS.md alongside the existing regex-coverage entry, together with the custom-pattern-across-window-boundary limitation and the plural-key vocabulary gap — none of them claimed as fixed"
    - "The stale Redaction.kt:56-79 line references in CONCERNS.md, which this phase's rewrite invalidates, are refreshed"
  artifacts:
    - path: "DECISIONS.md"
      provides: "ADR-14, following ADR-13's context/decision/consequences shape"
      contains: "## ADR-14"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "The header-stage and window-boundary residuals, and refreshed Redaction.kt references"
      contains: "header stage"
    - path: ".planning/phases/21-redaction-completeness/21-07-SUMMARY.md"
      provides: "The SC4 red-before-green transcript"
      contains: "SC4 — Red-Before-Green Transcript"
  key_links:
    - from: "DECISIONS.md ADR-14"
      to: ".planning/codebase/CONCERNS.md"
      via: "the Residual bullet cross-references the recorded gaps rather than overclaiming"
      pattern: "Residual:"
---

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<build_invocation>
EVERY Gradle invocation in this plan MUST be prefixed:
`JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew <tasks>`
Gradle 8.12.1 breaks on this machine's default JDK.
</build_invocation>

<objective>
Prove SC4 and record the phase's reasoning.

ROADMAP SC4 requires a test that constructs an oversized body with an embedded secret and asserts the
secret does not survive. `21-CONTEXT.md` and `21-VALIDATION.md` sharpen that into an acceptance gate:
the test must **fail against the pre-fix body stage**. `RedactionTest.oversizeBodySkippedSafely`
previously asserted the fail-open *as correct behaviour*, so a rewrite that happened to pass both
before and after would have tested nothing.

**This plan runs alone in wave 4.** Task 1 transiently mutates
`src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt`. If it ran alongside another plan, that
plan's test run would execute against the mutated redactor and fail spuriously, and its `git status`
verification would be polluted. Serialising removes the hazard rather than documenting it — the same
isolation Phase 20 gave `20-06-PLAN.md`.

Purpose: PRIV-06 / SC4 (the gate), PRIV-05 + PRIV-06 / SC5 / D-08 (ADR-14 and the honest residuals).
Output: a transcript in `21-07-SUMMARY.md`, ADR-14 in `DECISIONS.md`, and three recorded gaps in
`.planning/codebase/CONCERNS.md`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-redaction-completeness/21-CONTEXT.md
@.planning/phases/21-redaction-completeness/21-VALIDATION.md
@.planning/phases/21-redaction-completeness/21-04-SUMMARY.md
@.planning/phases/21-redaction-completeness/21-05-SUMMARY.md
@.planning/phases/21-redaction-completeness/21-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the SC4 red-before-green experiment and record the transcript</name>

  <files>.planning/phases/21-redaction-completeness/21-07-SUMMARY.md</files>

  <read_first>
    - .planning/phases/21-redaction-completeness/21-06-SUMMARY.md — the recorded line number of the single-pass guard inside `bodyStage`, which is where the mutation goes.
    - src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt — `bodyStage` in its shipped form.
    - src/test/kotlin/com/six2dez/burp/aiagent/redact/RedactionTest.kt — `oversizeBodySecretDoesNotSurvive` and `oversizeBodyFailsClosed`, the two assertions this experiment must drive red.
    - .planning/phases/20-mcp-access-control-correctness/20-06-PLAN.md — the Phase 20 precedent for the procedure, its no-`git stash` rule, and the shape of the transcript section.
    - .planning/ROADMAP.md, Phase 21 success criteria, SC4.
  </read_first>

  <action>
A verification experiment, not a code change. Run it only after plan 21-06 is committed and the full
gate is green. `$SCRATCH` is the session scratchpad directory.

**Why a whole-file rollback is NOT used, and why this matters.** Restoring `Redaction.kt` from the
pre-phase commit would not compile against the rest of the phase:
`scanner/PassiveAiScannerPrompts.kt` imports `Redaction.COOKIE_SECTION_HEADER` (plan 21-05), `App.kt`
assigns `Redaction.truncationLogger` (plan 21-06), and `RedactionTest` calls
`resetTruncationWindowForTest`. A **compile failure proves nothing about the defect** — it is not a red
test run. The experiment therefore reinstates the defect surgically, with a single-line mutation that
restores exactly the pre-fix contract documented at the old `Redaction.kt:253-257` and in the old
`Defaults.kt` comment: *"Bodies over this limit are skipped entirely."*

Procedure, from the repository root:

1. `cp src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt "$SCRATCH/Redaction.shipped.kt"` and
   confirm with `diff` that the copy is byte-identical. If it is not, stop.
2. Insert **one line** into `bodyStage`, immediately after the `if (rules.isEmpty()) return input`
   fast-path line and before the single-pass guard:
   `if (input.length > Defaults.MAX_REDACTION_BODY_CHARS) return input`
   That is the entire mutation. Do not change anything else. It reinstates the pre-Phase-21 behaviour —
   above the cap, every body rule is skipped and the original passes through.
3. Run, capturing the full output to a scratch file:
   `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests "com.six2dez.burp.aiagent.redact.*"`
   This MUST exit NON-ZERO. Record the exit code and every failed test method name.
   If it fails to **compile** rather than to assert, stop and report — the mutation was applied wrongly.
4. Restore: `cp "$SCRATCH/Redaction.shipped.kt" src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt`
5. Confirm `diff` reports the restored file byte-identical to the scratch copy, and that
   `git status --porcelain -- src/` prints nothing.
6. Re-run the same Gradle command. It MUST exit 0.

Note on shell hygiene, from project memory: this environment aliases `cp` and `rm` to their interactive
forms, which silently no-op in a non-interactive shell. Use `/bin/cp -f` and `/bin/rm -f` for every copy
and delete in this procedure. And **do not use `git stash` for any part of this** — the experiment
touches neither the git index nor any other file's working-tree state. It is a single-task local
verification step and MUST NOT be generalised into any other workflow.

Record in `21-07-SUMMARY.md` under a heading `## SC4 — Red-Before-Green Transcript`:

- the exact mutation line and where it was inserted, both exit codes, and the complete list of methods
  that failed in step 3
- a mapping table from each failed method to the SC4 property it covers: `oversizeBodySecretDoesNotSurvive`
  covers "a secret past the old cut-off does not survive", and `oversizeBodyFailsClosed` covers "a
  pathological custom pattern on an oversized input yields a marker, never passthrough"
- an explicit statement that the failure list contains **exactly** those two methods and no others —
  and that this is the expected result, because every other `RedactionTest` case uses an input at or
  below the window width and is therefore unaffected by the mutation. If any other method failed, the
  mutation reached further than intended and the transcript must say so.
- an explicit note that this experiment covers only the **body stage**. The SC1, SC2 and SC3 cookie and
  key-expression fixes are not exercised by it, because they live in the header stage and in the shared
  key expression; they are covered by their own tests in plans 21-04 and 21-05, and SC4's wording scopes
  the gate to the oversize behaviour.

If step 3 exits 0, **SC4 has FAILED**: the rewritten tests do not test the defect. Do not proceed.
Report which assertions were green under the mutation and revise them.
  </action>

  <verify>
    <automated>test -z "$(git status --porcelain -- src/)" && JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests "com.six2dez.burp.aiagent.redact.*" -q</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/21-redaction-completeness/21-07-SUMMARY.md` contains a `## SC4 — Red-Before-Green Transcript` section quoting the mutation line verbatim, a non-zero exit code for the mutated run and exit code 0 for the restored run
    - The recorded failure list contains `oversizeBodySecretDoesNotSurvive` and `oversizeBodyFailsClosed`
    - The summary states in prose that the failure list contains exactly those two methods, and names the reason every other case is unaffected
    - The summary states in prose that this gate covers the body stage only, and names where SC1/SC2/SC3 are covered instead
    - `git status --porcelain -- src/` prints nothing
    - `git log --oneline -1 -- src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` still points at the plan 21-06 commit — the mutation was never committed
    - `grep -c 'git stash' .planning/phases/21-redaction-completeness/21-07-SUMMARY.md` returns 0
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test ktlintCheck detekt -q` exits 0 as the final state
  </acceptance_criteria>

  <done>SC4 is satisfied with a machine-checkable transcript: the rewritten oversize tests fail against a body stage carrying the reinstated fail-open guard and pass against the shipped one, with no `git stash`, no red commit and a clean tree.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADR-14 in DECISIONS.md</name>

  <files>DECISIONS.md</files>

  <read_first>
    - DECISIONS.md lines 125-151 — ADR-13, the most recent entry and the closest in kind (it also amends an in-phase decision and records a residual). Copy its shape exactly: `## ADR-N: <sentence-case claim>` as the heading, then exactly three bold labels `**Context.**`, `**Decision.**` and `**Consequences.**` inline at paragraph start with the trailing period inside the bold; Context and Decision are prose paragraphs and are never bulleted; Consequences is always a bullet list; the rejected alternative is named inside the Decision paragraph; the last bullet begins `Residual:` when a gap is knowingly left open.
    - DECISIONS.md line 3 and the ADR-9 / ADR-12 headings — the requirement-id-in-heading convention.
    - .planning/phases/21-redaction-completeness/21-CONTEXT.md decisions D-01 through D-08, including D-01 AMENDED and D-08 REFINED.
    - .planning/phases/21-redaction-completeness/21-04-SUMMARY.md, 21-05-SUMMARY.md and 21-06-SUMMARY.md — what actually shipped, so the ADR describes the code rather than the plan.
    - .planning/notes/2026-08-05-code-review.md section "F5" — the finding this ADR closes.
  </read_first>

  <action>
Append a new entry at the end of `DECISIONS.md`, after ADR-13. ADR-14 is the next free number.

**Heading:** `## ADR-14: The redaction body stage never fails open (PRIV-06)`.

The qualifier "body stage" is load-bearing and is D-08 REFINED's explicit requirement. An unqualified
"redaction never fails open" would be **false the day it is written**, because the eight header-stage
rules still run unbounded on the full input. Claim only what ships.

**Context** — one prose paragraph, no bullets. It must state: the prior behaviour with a `file:line`
citation (`redact/Redaction.kt:257` before this phase) and the constant involved
(`Defaults.MAX_REDACTION_BODY_CHARS`, 1 MB); that above that cap the form, JSON **and user
custom-pattern** stages were all skipped and the original passed through untouched, so the control most
likely to matter switched itself off on exactly the inputs most likely to carry a secret (CWE-200
exposure of sensitive information); that the size of a response body is **attacker-selectable**, so
"bodies over 1 MB are rare" is not a mitigation — it is an invitation; the related emergent behaviour
that custom patterns sat inside the `redactTokens` branch and were therefore inert under
`PrivacyMode.OFF`, which was never a decision; that ADR-5 makes redaction a pre-flight, user-visible
control rather than a silent default, which is why a silent skip contradicts the project's own model;
and the source of the finding, `.planning/notes/2026-08-05-code-review.md` section F5.

**Decision** — one prose paragraph, no bullets. It must state: the body stage chunk-and-scans the whole
input in windows cut at **line boundaries**, never splitting a line, under a total wall-clock budget
(`Defaults.MAX_REDACTION_BUDGET_MS`), with each rule bounded by
`min(SafeRegex.DEFAULT_TIMEOUT_MS, remaining budget)` through a new timeout-reporting API; a window that
could not be fully scanned, and everything past an exhausted budget, is **dropped behind a visible
marker rather than passed through** — fail closed, so unscanned bytes never reach a backend — and the
user is told through a rate-limited Burp Output-tab line; and that user custom patterns now apply in
**every** privacy mode including OFF, so OFF means *"no built-in redaction"* rather than *"no redaction
at all"*, enforced structurally by deleting the caller-side OFF short-circuits at
`scanner/PassiveAiScannerAnalysis` and `mcp/McpToolContext` so the mode is expressed once, as a policy.

Name the rejected alternatives inside the same paragraph, each with its reason: truncate-and-redact
(silent capability loss on large JSON); refuse outright (turns a redaction concern into a functional
failure at five call sites, each needing its own failure story); **overlap-based windowing** (the value
side of the built-in body rules is length-unbounded — measured single matches of 200 006 characters —
so no finite overlap constant is defensible, and D-01's overlap clause is unsatisfiable as literally
written); `Matcher.region()` (its bounds semantics are correct but `Matcher.replaceAll()` silently
resets the region, so region-based replacement is not available at all); OFF-means-off for custom
patterns (a user who added a pattern for a corporate token and later flipped to OFF for a debugging
session would leak it with no warning); always-apply-plus-opt-out (a third state across the
STRICT/BALANCED/OFF matrix, on a panel whose value is that privacy is simple); a fourth
`RedactionPolicy` flag instead of deleting the short-circuits (it would have fixed the unit test, not
the leak); and renaming the constants or exposing window and budget in Privacy settings (a user who set
the budget to zero would silently disable body redaction — exactly the class of bug this ADR exists to
kill). Close with the amendment-authorisation sentence: this ADR authorises the amendment of D-01's
overlap clause to line-boundary cutting, on the measured grounds above.

**Consequences** — a bullet list. Cover at minimum: input at or below the window width is a single pass
with behaviour and cost identical to the previous implementation, so the overwhelming majority of
payloads are unaffected; the built-in body rules previously ran with **no** deadline at all and now all
run bounded, which is a real change and easy to assume was already true; oversized input costs more CPU
than before because it is now actually scanned; the marker is a constant shape plus one integer and
carries no attacker-controlled substring, so it is not an injection vector into model context; the
persistent passive-scan prompt cache is keyed on the SHA-256 of the **post**-redaction prompt, so
changing redaction causes a one-time cache-miss wave with no stored secrets and no stale leak — a
reviewer should not mistake it for a regression; `SecretTripwire` hit counts under OFF may drop slightly
because custom patterns now run first, which is correct and intended; and finally a bullet beginning
`Residual:` naming the two gaps that are deliberately not closed — the eight header-stage rules still
run unbounded on the full input, and a user custom pattern whose match spans a window boundary can be
missed because there is no principled bound on a user regex's match length — with a cross-reference to
`.planning/codebase/CONCERNS.md`.

Do not modify ADR-1 through ADR-13.
  </action>

  <verify>
    <automated>grep -c '^## ADR-14: The redaction body stage never fails open (PRIV-06)$' DECISIONS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## ADR-14' DECISIONS.md` returns 1 and the heading text contains `body stage` — the unqualified "redaction never fails open" form must NOT appear
    - `grep -c '^\*\*Context\.\*\*' DECISIONS.md` and `grep -c '^\*\*Decision\.\*\*' DECISIONS.md` and `grep -c '^\*\*Consequences\.\*\*' DECISIONS.md` each increase by exactly 1 relative to before the edit
    - The ADR-14 Consequences list contains a bullet beginning `- Residual:` that mentions the header stage and the window boundary
    - `grep -c 'PrivacyMode.OFF\|OFF' DECISIONS.md` shows ADR-14 covers the custom-patterns-under-OFF decision — SC5 requires that half explicitly
    - `git diff DECISIONS.md | grep -c '^-'` returns 1 (the diff header only) — ADR-1 through ADR-13 are untouched
    - `grep -c 'MAX_REDACTION_BUDGET_MS\|Defaults.MAX_REDACTION_BODY_CHARS' DECISIONS.md` is at least 1
  </acceptance_criteria>

  <done>ADR-14 exists in ADR-13's exact shape, claims only that the body stage never fails open, records both PRIV-06 halves as one principle with every rejected alternative named, and closes with an honest `Residual:` bullet.</done>
</task>

<task type="auto">
  <name>Task 3: Record the residual gaps and refresh the stale references in CONCERNS.md</name>

  <files>.planning/codebase/CONCERNS.md</files>

  <read_first>
    - .planning/codebase/CONCERNS.md section "Redaction regex coverage gaps" (around lines 62-67) — the entry shape: `**Issue:**`, `**Files:**`, `**Protocol for tightening:**`, `**Impact:**`. Note that it cites `Redaction.kt:56-79`, line numbers this phase's rewrite invalidates.
    - .planning/phases/21-redaction-completeness/21-04-SUMMARY.md — what the key expression now covers, and the exact list of accepted over-redactions.
    - .planning/phases/21-redaction-completeness/21-06-SUMMARY.md — the shipped body stage and its bounds.
    - DECISIONS.md ADR-14 as written in Task 2 — the `Residual:` bullet these entries expand on.
  </read_first>

  <action>
Two edits to `.planning/codebase/CONCERNS.md`.

**1. Update the existing "Redaction regex coverage gaps" entry.** Three changes:

- **Refresh the stale reference.** Replace `Redaction.kt:56-79` with the post-phase reality. Cite the
  symbols rather than a line range wherever possible (`authHeaderRegex`, `jwtRegex`,
  `SENSITIVE_KEY_EXPR`), giving current line numbers as of Phase 21 so a future reader can find them
  without the reference rotting again the next time the file is edited.
- **Correct what is no longer true.** The entry currently says `urlTokenParamRegex` "matches a fixed
  allowlist of parameter names". After Phase 21 all three consumers are driven by `SENSITIVE_KEY_EXPR`,
  which is token-boundary containment plus a vendor-name list, so the allowlist framing is obsolete.
  The gaps that remain in this entry are `jwtRegex` (still "not perfect by design") and `authHeaderRegex`
  (still an exact-name alternation, so vendor-specific headers such as `x-shopify-access-token` and
  `stripe-signature` are still missed — folding them in is **not** free under the new mechanism because
  `authHeaderRegex` would need the same treatment, which is why it stays deferred).
- **Add the plural-key gap.** Plural forms (`codes`, `tokens`, `keys`) are not matched, because the
  vocabulary holds the singular and the boundary rule requires a whole token. Record the one-character
  recipe (adding an optional trailing `s` to the vocabulary) and why it was deferred: a second widening
  axis plus six more tests, and SC3 does not require it. Note the concrete risk being accepted: a form
  field literally named `codes` carrying MFA backup codes would not be redacted.
- Keep the **Protocol for tightening** clause verbatim — add the pattern, add the test, do not loosen an
  existing pattern without documenting the reason. It is binding and was honoured this phase: the SC3
  widening was verified strictly monotone across a 60-key corpus, so no existing pattern was loosened.

**2. Add a new entry, `### Redaction body-stage bounds and the unbounded header stage`,** in the same
four-field shape, immediately after the regex-coverage entry.

- **Issue:** two deliberate residuals of Phase 21 / ADR-14. First, the body stage is now windowed,
  budgeted and fail-closed, but the eight **header-stage** rules (`authHeaderRegex`, `bearerRegex`,
  `basicAuthRegex`, `jwtRegex`, `urlTokenParamRegex`, `cookieHeaderRegex`, `setCookieHeaderRegex`,
  `hostHeaderRegex`) plus the two new cookie rules still run **unbounded on the full input**, with no
  per-pattern deadline and outside the total budget. Measured at 10 MB, each header rule costs roughly
  25-51 ms, so a 200 MB input spends seconds in the header stage before the budgeted body stage begins.
  This is a pre-existing condition rather than a Phase 21 regression, and D-01/D-02 scoped themselves
  explicitly to the body stage — which is why ADR-14's title says "body stage". Second, a **user custom
  pattern whose match spans a window boundary can be missed**: there is no principled bound on a user
  regex's match length, so no window size or overlap makes this safe. The built-in rules are unaffected
  — `formBodyParamRegex` and `urlTokenParamRegex` cannot span newlines, and the `windowEnd` JSON
  boundary-safety rule covers `jsonSecretKeyRegex`.
- **Files:** `src/main/kotlin/com/six2dez/burp/aiagent/redact/Redaction.kt` (the `apply` header stage
  and `bodyStage`/`windowEnd`) and `src/main/kotlin/com/six2dez/burp/aiagent/config/Defaults.kt`.
- **Fix approach:** extend the same budget composition over the header stage, applying each header rule
  through `SafeRegex.replaceAllSafeReporting` under `min(DEFAULT_TIMEOUT_MS, remaining budget)` and
  failing closed on timeout, exactly as the body stage does. The window-boundary limitation for custom
  patterns has no complete fix and should be documented for users rather than papered over.
- **Impact:** resource exhaustion on very large inputs (the header stage is unbudgeted), and a
  false-negative window for custom patterns at window boundaries. Neither is a new leak relative to the
  pre-Phase-21 code.

Do not modify any other CONCERNS.md entry.
  </action>

  <verify>
    <automated>grep -c 'header stage' .planning/codebase/CONCERNS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE 'Redaction\.kt:5[0-9]-79' .planning/codebase/CONCERNS.md` returns 0 — **both** stale range references are gone: `Redaction.kt:55-79` in the entry's `**Issue:**` line and `Redaction.kt:56-79` in its `**Files:**` line
    - `grep -cE '\(line 71\)|\(line 74-78\)|\(line 56-64\)' .planning/codebase/CONCERNS.md` returns 0 — the three inline line-number refs inside the entry (`jwtRegex (line 71)`, `urlTokenParamRegex (line 74-78)`, `authHeaderRegex (line 56-64)`) are also invalidated by this phase's rewrite and must be replaced with symbol-anchored references, not left behind
    - `grep -c '### Redaction body-stage bounds and the unbounded header stage' .planning/codebase/CONCERNS.md` returns 1
    - `grep -c 'SENSITIVE_KEY_EXPR' .planning/codebase/CONCERNS.md` is at least 1 — the entry describes the mechanism that actually shipped
    - `grep -c 'fixed allowlist of parameter names' .planning/codebase/CONCERNS.md` returns 0 — the obsolete framing is corrected
    - `grep -c 'codes\|plural' .planning/codebase/CONCERNS.md` is at least 1 — the plural-key gap is recorded with its recipe
    - `grep -c 'Protocol for tightening' .planning/codebase/CONCERNS.md` is unchanged from before the edit — the binding clause survives
    - `grep -c 'window boundary' .planning/codebase/CONCERNS.md` is at least 1
    - The new entry uses the same four bold field labels as its neighbours (`**Issue:**`, `**Files:**`, `**Fix approach:**`, `**Impact:**`)
  </acceptance_criteria>

  <done>CONCERNS.md describes the redaction surface as it now is: refreshed references, the corrected key-matching description, and two new honestly-scoped residuals that ADR-14's `Residual:` bullet points at.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan ships no product code. Task 1 crosses a **process** boundary — a transient mutation of a
security control inside the working tree — and Tasks 2 and 3 govern what the project claims about its
own controls, which is the boundary between the code's real guarantees and a future contributor's
belief about them.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11 | Tampering (working tree) | The transient mutation is committed, or left in place, shipping a redactor that fails open above 1 MB | mitigate | The shipped file is copied to scratch before the mutation and restored from that copy; `diff` must report byte-identity; `git status --porcelain -- src/` must print nothing; `git log` for the file must still point at the plan 21-06 commit; the plan runs alone in wave 4 so no concurrent plan can observe the mutated file |
| T-21-24 | Tampering (evidence) | The SC4 transcript is the only evidence SC4 was met, and could be hand-waved | mitigate | Both exit codes, the mutation line and the complete failed-method list are recorded verbatim; the acceptance criteria name the two methods that MUST appear and require a prose statement that no others did |
| T-21-25 | Denial of correctness | A compile failure under mutation would look like a red run and be mistaken for proof | mitigate | The mutation is a single-line insertion that compiles by construction; the whole-file rollback is explicitly ruled out with the three symbols that would break it named; step 3 requires distinguishing assertion failure from compile failure and stopping on the latter |
| T-21-26 | Repudiation | ADR-14 overclaims — an unqualified "redaction never fails open" would be false on the day it is written and would license a future contributor to skip bounding the header stage | mitigate | D-08 REFINED's wording is mandated in the heading and grep-asserted; the `Residual:` bullet and the new CONCERNS.md entry name both gaps with measurements |
| T-21-27 | Repudiation | Stale `file:line` references in CONCERNS.md send a future contributor to the wrong code and quietly invalidate the tightening protocol | mitigate | The `Redaction.kt:56-79` reference is grep-asserted removed and replaced with symbol-anchored references |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; no new Gradle dependency |
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test ktlintCheck detekt -q` exits 0
- `git status --porcelain -- src/` prints nothing
- `git diff --stat -- src/` shows no change
- `.planning/phases/21-redaction-completeness/21-07-SUMMARY.md` contains `## SC4 — Red-Before-Green Transcript`
- `grep -c 'git stash' .planning/phases/21-redaction-completeness/21-07-SUMMARY.md` returns 0
- `grep -c '^## ADR-14' DECISIONS.md` returns 1
</verification>

<success_criteria>
- The mutated run exits non-zero with assertion failures, not a compile error
- The restored run exits 0 and the tree is clean; nothing was committed and no `git stash` was used
- The failure list is exactly `oversizeBodySecretDoesNotSurvive` and `oversizeBodyFailsClosed`, and the transcript says so
- ADR-14 exists, follows ADR-13's shape, claims only the body stage, and covers both PRIV-06 halves
- CONCERNS.md records the header-stage gap, the window-boundary limitation and the plural-key gap, and its stale `Redaction.kt` references are refreshed
</success_criteria>

<output>
Create `.planning/phases/21-redaction-completeness/21-07-SUMMARY.md` carrying the
`## SC4 — Red-Before-Green Transcript` section described in Task 1, plus a short
`## Phase 21 residuals` section listing the three gaps recorded in CONCERNS.md so the phase
verification can cite them without re-reading two files.
</output>
