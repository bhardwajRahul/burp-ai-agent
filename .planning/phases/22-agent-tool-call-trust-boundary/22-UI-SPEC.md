---
phase: 22
slug: agent-tool-call-trust-boundary
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-13
extends: 09-UI-SPEC.md
---

# Phase 22 — UI Design Contract: Agent Tool-Call Trust Boundary

> Visual and interaction contract for the SEC-06 decision surface. Generated by gsd-ui-researcher.
> Pending verification by gsd-ui-checker.
>
> **Stack: Kotlin + Java Swing inside Burp Suite (ADR-2).** There is no browser, no DOM, no CSS, no
> Tailwind, no shadcn, no design-token JSON and no responsive viewport. The component specified here
> lives inside a `JScrollPane` in a Burp tab whose width the user drags. Web-specific template
> sections are explicitly replaced with their Swing equivalents — see §"Web Sections → Swing
> Equivalents".
>
> **The design system already exists and is authoritative.** Spacing, typography and color are
> inherited verbatim from `09-UI-SPEC.md` / `ui/design/DesignTokens.kt`. This phase declares **zero
> new spacing values, zero new typography roles and zero new color tokens.** Every visual claim below
> cites a token, a builder, or a `file:line` in this repository.

---

## Scope

| Aspect | Decision |
|--------|----------|
| New component | **1** — `ui/components/ToolApprovalCard.kt` |
| Modified components | **1** — `ChatPanel.SessionPanel` (one new method) + `ChatPanel.ChatSessionRenderer` (one new label) |
| New tokens | **None** — inherits `DesignTokens` entirely |
| New typography roles | **None** — uses `sectionTitle` / `body` / `caption` / `mono` from the declared set of 5 |
| New color tokens | **None** — uses `cardSurface` / `border` / `borderSubtle` / `inputBackground` / `inputForeground` / `onSurface` / `onSurfaceVariant` / `statusWarning` / `statusError` / `statusSuccess` |
| New glyphs | **1** — U+2716 (deny outcome); pairs with U+2714 already declared in `09-UI-SPEC.md` |
| Settings UI | **None** — D-09 ships no opt-out and no tier downgrade |
| Modals / dialogs | **None** — D-06 locked the inline card |

**Out of UI scope (named, not designed here):**

- **Tier resolution, the decision state machine, session memory, the denial constant, audit emission** —
  AWT-free, `mcp/` package (22-RESEARCH.md §"Architectural Responsibility Map"). This contract consumes
  the resolved tier and the resolved catalog title; it does not compute them.
- **`AUTO`-tier calls** — D-02 says they run silently. They produce **no card and no transcript row.**
- **The two user-originated call sites** (`ChatPanel.kt:928` dialog, `ChatPanel.kt:2105` `/tool`) — SC5
  says do not double-prompt. They produce **no card**.
- **The existing "Tool result: …" transcript message** (`ChatPanel.kt:2153`, `:2175`) — unchanged. Note
  that it renders through `MarkdownRenderer.toHtml` into a `JEditorPane` with
  `contentType = "text/html"` (`ChatPanel.kt:1864`, `:2070`), i.e. model text is rendered as markdown
  there. That is the **existing** behaviour and this phase does not change it; it is, however, the
  sharpest possible argument for why the approval card must not reuse that widget (see §"Trust
  Typography", Rule T-2). Recorded as an observation for Phase 26 / DOC-03, not fixed here.

---

## Web Sections → Swing Equivalents

The template's frontend sections have no meaning in Swing. Each is replaced, not left blank.

| Template section (web) | Why it does not apply | Swing replacement in this document |
|------------------------|-----------------------|-----------------------------------|
| CSS variables / design-token JSON | No stylesheet. Tokens are Kotlin computed `get` properties resolving from `UIManager` at call time (`DesignTokens.kt:20-30`) | §"Spacing", §"Typography", §"Color" — all token **names**, not values |
| Breakpoints / media queries | No viewport. Width comes from the user dragging the Burp tab divider; there is no discrete breakpoint | §"Layout & Sizing Contract" — `LayoutManager` choice + `preferredSize` / `maximumSize` rules |
| Line-height ratios | Swing exposes no line-height property (`09-UI-SPEC.md` §Typography) | §"Typography" — `lineWrap` / `wrapStyleWord` / `rows` contract |
| Font sizes in px | All sizes are multipliers of `UIManager.getFont("Label.font")` so they follow the OS accessibility setting | §"Typography" — role names + multipliers |
| Hover / focus-visible pseudo-classes | Swing has `isFocusPainted`, `MouseListener`, and L&F-supplied rollover | §"Keyboard & Focus Contract" |
| Icon library | `09-UI-SPEC.md` locked "none — Unicode glyphs only" | §"Glyphs" |
| Component registry (shadcn) | Not a React/Next/Vite project | §"Registry Safety" — n/a |

---

## Inherited Contract (from `09-UI-SPEC.md` — do NOT re-litigate)

| Property | Inherited value | Source |
|----------|-----------------|--------|
| Design system | Kotlin + Swing; `ui/design/DesignTokens.kt` + `ui/design/Components.kt` | Phase 09 (UI-01) |
| Spacing scale | `Spacing.xs/sm/md/lg/xl` = 4/8/12/16/24, all multiples of 4 | `DesignTokens.kt:40-75` |
| Typography roles | Exactly 5: `sectionTitle`, `body`, `caption`, `label`, `mono`. **No new derivation may be added** | `DesignTokens.kt:90-112`; `09-UI-SPEC.md` §Typography |
| Color 60/30/10 | 60% `surface`, 30% `cardSurface` + `borderSubtle`, 10% `primary` (reserved list) | `09-UI-SPEC.md` §Color |
| Light/dark | Tokens resolve from `UIManager` at call time, never cached; components re-apply on `updateUI()` | `DesignTokens.kt:11-14`; `SafetyIndicator.kt:50-57` |
| Icon policy | Unicode glyphs only; no icon library | `09-UI-SPEC.md` §Design System |
| Legacy shim | `UiTheme.kt` is a shim with known naming drift (`outline`→`border`, `outlineVariant`→`borderSubtle`, `statusRunning`→`statusSuccess`, `statusCrashed`→`statusError`, `statusTerminal`→`statusWarning`) | `UiTheme.kt:29-40`; STATE.md:149 |

**New code in this phase imports `DesignTokens`, never `UiTheme`.** `ActionCard.kt` imports `UiTheme`
(`ActionCard.kt:3`) — that is legacy and must not be copied. `ContextPreviewDialog.kt:5` already sets
the correct precedent by importing `DesignTokens` in a `ui/components/` file.

---

## The Hard Problem: Trust Typography

**This is the single most important section in the document.** Two classes of text share one card:

| Class | Content | Trust |
|-------|---------|-------|
| **Extension-derived** | Card heading, catalog title, tier badge, button labels, tooltips, outcome lines, captions, footers | Trustworthy. Written by this codebase. |
| **Model-supplied** | The parsed tool ID (`ParsedToolCall.tool` — "whatever string the model wrote", `22-CONTEXT.md` §Integration Points), the args JSON | **Attacker-influenceable.** An injected prompt can write "safe routine read" into either. |

A user must tell them apart **without reading carefully.** The mechanism is **containment**, not
decoration — model text is always inside a box, extension text is never inside a box — reinforced by
two further independent channels so that no single failure (a theme with low panel/field contrast, a
colorblind user) collapses the distinction.

### Rule T-1 — Containment (primary channel)

Every model-supplied string is rendered into a **bordered input-styled region** and **nothing
extension-derived ever appears inside one**.

| Region | Widget | Styling | Cites |
|--------|--------|---------|-------|
| Tool ID | `JTextField`, `isEditable = false`, `caretPosition = 0` | `applyFieldStyle(field)` | `Components.kt:484-489` |
| Args JSON | `JTextArea`, `isEditable = false`, `lineWrap = true`, `wrapStyleWord = true` | `applyAreaStyle(area)` | `Components.kt:498-505` |

Both builders set `Typography.mono` + `LineBorder(Colors.border, 1, true)` + `Colors.inputBackground`
+ `Colors.inputForeground`. That single call supplies all three channels at once — this is why the
design-system builder is used rather than hand-set properties.

**Implementation note (real Swing trap):** set `isEditable = false` **before** calling the builder, or
re-assign `background` after it. Several L&Fs paint a non-editable text component with the disabled
background and will overwrite the token value if the order is reversed.

### Rule T-2 — Widget class is a hard boundary (anti-spoofing)

Model text is rendered **only** into `JTextField` / `JTextArea`. It is **forbidden** in:

| Forbidden widget | Why |
|------------------|-----|
| `JLabel` | Swing's `JLabel` interprets a leading `<html>` tag. `"<html><b>Approved</b>"` in a tool ID would render as extension-looking chrome. |
| `AbstractButton` — `JButton`, `JCheckBox`, `JRadioButton`, `JMenuItem` | **Identical hazard to `JLabel`, and verified as identical.** Both route their text through `BasicHTML.updateRenderer`. Measured on Temurin JDK 21, headless: `JButton("<html><b>Approved</b>")` and `JLabel("<html><b>Approved</b>")` both come back with a **non-null** `getClientProperty("html")`, i.e. the HTML renderer is installed in both. This card puts computed values into button labels, so the row is stated rather than left implicit. |
| `JEditorPane` / `JTextPane` with `contentType = "text/html"` | Same, worse. This is exactly what the transcript's message body does (`ChatPanel.kt:1864`) and exactly what `SubtleNotice` does (`SubtleNotice.kt:59`). **Neither may be used for model text on this card.** |
| `setToolTipText(...)` with model content | Swing tooltips render HTML. `SafetyIndicator` documents it at `:38` — *"Swing renders HTML tooltips natively"* — and passes `tooltipHtml` straight through at `:46`. A model-authored tooltip is styled, floating, extension-looking text. |
| Any widget that also carries extension text | One widget = one trust class. No `"Tool: " + modelString` concatenations anywhere. **One sanctioned exception:** the accessible description, which is not a widget and is governed by three explicit conditions in §Accessibility. |

**Why the computed button labels on this card are nonetheless safe.** `BasicHTML.isHTMLString` fires
**only at position 0**: it tests `charAt(0) == '<'` **and** `charAt(5) == '>'`, then compares the four
characters between them to `"html"`. Nothing at any other offset can install the renderer. Measured on
Temurin JDK 21, headless:

| Probe | `getClientProperty("html")` |
|-------|------------------------------|
| `JButton("▶ Show arguments (4021 characters)")` | `null` — plain renderer |
| `JButton("x<html><b>Approved</b>")` (tag at position 1) | `null` — plain renderer |
| `JTextField("<html><b>Approved</b>")` | `null` — plain renderer |
| `JTextArea("<html><b>Approved</b>")` | `null` — plain renderer |

So `▶ Show arguments ({N} characters)`, `Show all {N} characters` and `Show first {cap} characters` are
safe on two independent grounds: the interpolated value is an extension-computed integer and never a
model string, **and** it is never at position 0. The positive allowlist — model text goes into
`JTextField` / `JTextArea` only — remains the primary control; the position-0 fact is why that allowlist
does not have to be relaxed for the toggle labels.

D-07 says "never markdown, never as extension chrome". In Swing that sentence resolves to exactly this
table.

### Rule T-3 — Font family (secondary channel, theme-independent)

Extension text is **proportional** (`Typography.sectionTitle` / `body` / `caption`); model text is
**monospace** (`Typography.mono`). This channel works in both themes, in grayscale, and at any OS font
size, because it does not depend on any color. It is the channel that still works when the other two
are weak.

### Rule T-4 — Background (third channel — honestly weak in light theme)

Card background is `Colors.cardSurface`; model regions are `Colors.inputBackground`.

- **Dark theme:** `Table.background` ≈ `#333333` vs `TextField.background` ≈ `#3C3C3C` — visible.
- **Light theme:** both resolve close to white (`#F5F5F5` vs `#FFFFFF`) — **nearly invisible.**

This is stated rather than glossed: in light theme the **1 px border (T-1) and the monospace font
(T-3) are the load-bearing signals**, and the design must not be reviewed as if the background did the
work.

### Rule T-5 — An explicit label, in extension chrome

Immediately above the first model region, a `helpLabel` (`Components.kt:331` — `Typography.caption`
+ `Colors.onSurfaceVariant`):

> `AI-supplied — this extension did not write the text below:`

It is outside every box, so by T-1 it cannot be spoofed: model text physically cannot escape into it.
The rule is **not conditional on card kind** — wherever a model region appears, including the compact
resolved row, this label appears immediately above it.

### Rule T-6 — Sanitize before display, with **two** different sanitizers

Reuse the `McpBlockedRequestReporter.sanitize` idiom (`McpBlockedRequestReporter.kt:220-233`,
Phase 20 D-07). It needs two variants, and conflating them is a real defect:

| Sanitizer | Applied to | Regex | Whitespace | Cap |
|-----------|-----------|-------|------------|-----|
| **inline** | Tool ID | `Regex("[\\p{Cntrl}\\u0080-\\u009F]")` → remove | collapse `\s+` → `" "` | 120 chars, `…` suffix |
| **block** | Args JSON | `Regex("[\\p{Cntrl}&&[^\\n\\t]]\|[\\u0080-\\u009F]")` → remove | **preserve `\n` and `\t`** | see §"Args Preview" |

Rationale for the split: `\p{Cntrl}` includes `\n` and `\t`. Applying the inline sanitizer to args
would flatten JSON into one unreadable line — the opposite of D-07's "full args are shown because the
args are where exfiltration hides". Applying the block sanitizer to the tool ID would let a name like
`"scope_check\n\n✔ Approved"` occupy several lines and imitate an outcome row.

Control characters are **removed, never replaced** (CWE-117). The regex lives at
`McpBlockedRequestReporter.kt:25` and the reasoning at `:222-223` — *"REMOVED rather than replaced, so
`"a\r\nInjected: line"` collapses to `"aInjected: line"` and a forged second log line is impossible"*. Stripping `ESC` (0x1B) also neuters ANSI sequences — Swing does not interpret them, but a
bare 0x1B renders as a missing-glyph box and shifts apparent text.

### Rule T-7 — Unknown tool names are labelled, never shown bare

D-07. When tier resolution cannot map the canonicalised ID to a catalog descriptor, the
extension-derived title line reads:

> `Not a known tool — this call will fail if you approve it.`

The model's string still appears, sanitized, inside its box (the user must see *what* was asked for),
but no catalog title is fabricated for it. On a compact resolved row, where no approve button exists,
the same rule uses a button-free wording — see §"The compact resolved row".

---

## The Hard Problem: Tier Legibility

`AUTO` / `CONFIRM` / `CONFIRM_EACH` must be distinguishable **without relying on color alone** — Burp
ships both themes and some users are colorblind.

### Decision: add a sibling, do not reuse

| Candidate | Verdict | Why |
|-----------|---------|-----|
| `SafetyIndicator` (`ui/components/SafetyIndicator.kt`) | **Reuse the pattern, not the class** | It is `JLabel("Safety: OK")` with an `OK/WARN/RISK` enum and a `setSummary(level, tooltipHtml)` API bound to the main-tab header. Mapping a SEC-06 tier onto a safety level would make one word mean two things. Its **structure** — self-describing text, opaque pill, `EmptyBorder(4,8,4,8)`, `updateUI()` re-apply with an `initialized` guard (`:23-27`, `:50-57`) — is the model. |
| `PrivacyPill` (`ui/components/PrivacyPill.kt`) | **Do not reuse** | It is bound to `PrivacyMode` and it **hardcodes `Color(0x1B9E5A)` / `Color(0xF9A825)` / `Color(0xB3261E)`** (`:11-13`), which violates the token contract. Copying it would propagate that defect. |
| `toolBadge(label, BadgeStyle)` (`Components.kt:426-472`) | **Reuse the rendering idiom** | Correct design-system pill: rounded rect via `fillRoundRect(…, 6, 6)`, `Typography.caption`, `EmptyBorder(2,6,2,6)`, and it **re-reads tokens at paint time** (`:447-451`) plus re-applies foreground in `updateUI()` (`:458-465`). But `BadgeStyle.NATIVE/FULL` means *build provenance*; adding tier variants to that enum makes it mean two things. |

**Specification:** a `private fun tierBadge(tier: SecTier): JLabel` inside `ToolApprovalCard.kt`,
structured exactly like `toolBadge` (`Components.kt:426`) with a border added. Not a public builder in
`Components.kt` — Phase 13's stance ("avoid premature generalisation") applies until a second caller
exists. Structuring it "exactly like `toolBadge`" transitively adopts that builder's internal
`EmptyBorder(2, 6, 2, 6)` padding; that is recorded in §Spacing rather than silently inherited.

### Four redundant channels

| # | Channel | `CONFIRM` | `CONFIRM_EACH` | `AUTO` |
|---|---------|-----------|----------------|--------|
| 1 | **Text** (load-bearing) | `Tier: CONFIRM` | `Tier: CONFIRM EACH` | `Tier: AUTO` |
| 2 | **Border color** (redundant) | 1 px `LineBorder(Colors.statusWarning, 1, true)` | 1 px `LineBorder(Colors.statusError, 1, true)` | 1 px `LineBorder(Colors.statusSuccess, 1, true)` |
| 3 | **Card accent strip** (redundant) | 3 px `statusWarning` | 3 px `statusError` | — |
| 4 | **Button count** (structural) | 4 buttons | 2 buttons | no card |

Channel 1 mirrors `SafetyIndicator`'s own stated rationale verbatim: *"the text spells out the state so
the indicator self-describes without relying on color alone"* (`SafetyIndicator.kt:10-11`). Channel 4
is not decoration — it **is** the semantic difference D-02 encodes, and it survives grayscale, low
vision and a screenshot.

### Badge fill: outlined, not filled — and why

- Background: `Colors.cardSurface` (the design system's declared badge-background role,
  `DesignTokens.kt:138-140`).
- Foreground: `Colors.onSurface`.
- Border: 1 px `LineBorder(tierColor, 1, true)`.

**A filled tier pill was rejected on contrast grounds, computed.** `SafetyIndicator` fills with
`statusTerminal` and sets `foreground = onPrimary` (`SafetyIndicator.kt:63`, `:66`). Against the
declared fallback values — `onPrimary` = `Color.WHITE` (`DesignTokens.kt:174`), `statusWarning` =
`#F57C00` (`DesignTokens.kt:186`) — that pairing has a WCAG relative luminance of **0.338** and a
contrast ratio of **≈2.70:1**, far below the 4.5:1 AA threshold for normal text. `PrivacyPill` avoids
the same trap for BALANCED by using `onSurface` (`PrivacyPill.kt:35`), but `onSurface` resolves to
`Label.foreground` (`DesignTokens.kt:131`), which is **light** in dark theme — so the same fix breaks in
the other theme.

An outlined badge on `cardSurface` with `onSurface` text puts badge text in exactly the same
foreground/background relationship as ordinary body text on a transcript card: `Label.foreground` over
`Table.background` (`DesignTokens.kt:131`, `:143`). **That relationship is not measured here, and
`09-UI-SPEC.md` contains no contrast measurement either** — it is supplied by Burp's Look-and-Feel and
is the same pairing every other label in the extension already depends on. The claim is therefore *not*
that a number was verified; it is that the outlined badge introduces **no new** contrast obligation,
whereas a filled badge introduces one and fails it. **Color therefore never carries text on this card**;
it is moved to the 3 px accent strip (channel 3), where contrast is irrelevant because the strip carries
no glyph.

`AUTO` is specified for completeness so the `when (tier)` is exhaustive and a future value cannot
render blank, but **`AUTO` is unreachable in this phase** — it produces no card (D-02). Claim only what
ships.

---

## The Hard Problem: The Full State Machine as Visual States

The card is a **record**, not just a prompt (`22-CONTEXT.md` §Specific Ideas). It must read
unambiguously months later.

### Invariant: at most one pending card per chat session

By construction. A pending card parks the continuation, so no second turn runs in that session; and if
the user types a new message, path 1 of the implicit-denial list resolves the pending card **before**
`sendMessage` (22-RESEARCH.md §"Pending-Decision Lifecycle"). Across sessions, N pending cards (one per
session) are possible. **The gate must enforce this invariant; the card must not be designed for a
second one.**

### All states

| # | State | Trigger | Card in transcript | Accent strip | Buttons | Args region |
|---|-------|---------|--------------------|--------------|---------|-------------|
| 1 | **Suppressed — auto** | tier = `AUTO` | **none** | — | — | — |
| 2 | **Suppressed — session-approved** | tool in `approvedForSession` | compact resolved row (**recommended**, see FLAG-22-03) — designed in §"The compact resolved row" | `borderSubtle` | none | collapsed, expandable |
| 3 | **Suppressed — session-denied** | tool in `deniedForSession` | compact resolved row (**required**) — designed in §"The compact resolved row" | `borderSubtle` | none | collapsed, expandable |
| 4 | **Pending** | tier = `CONFIRM` | full card | `statusWarning` | 4 | collapsed, expandable |
| 5 | **Pending** | tier = `CONFIRM_EACH` | full card | `statusError` | 2 | **expanded by default** |
| 6 | **Approved once** | click `Approve once` | same card, mutated | `borderSubtle` | replaced by outcome row | state preserved |
| 7 | **Approved for session** | click `Approve for session` | same card, mutated | `borderSubtle` | replaced by outcome row | state preserved |
| 8 | **Denied** | click `Deny` | same card, mutated | `borderSubtle` | replaced by outcome row | state preserved |
| 9 | **Denied for session** | click `Deny for session` | same card, mutated | `borderSubtle` | replaced by outcome row | state preserved |
| 10 | **Implicitly denied** | user sent a new message in this session | same card, mutated | `borderSubtle` | replaced by outcome row | state preserved |
| 11 | **Implicitly denied — container destroyed** | chat cleared / session deleted / project changed / unload | **card no longer exists** | — | — | — |

**State 11 is honest scope.** Four of the five implicit-denial paths (`clearCurrentChat` at
`ChatPanel.kt:971` calls `clearMessages()` at `:1699`; `deleteSession` at `:775`;
`clearInMemorySessionState` at `:1348`; `shutdown` at `:1322`) destroy the transcript or the whole
panel. There is no surviving surface to render an outcome onto. **The durable record for those four is
the SC3 audit event, not the UI.** State 10 — a new message in the same session — is the only implicit
denial with an observable visual state. Do not write a spec sentence claiming otherwise.

### Resolution: buttons are **removed**, not disabled

| Option | Verdict |
|--------|---------|
| Disable the four buttons | **Rejected.** A disabled `JButton` renders as grey text on the same chrome — months later it is indistinguishable from "unavailable", and four dead buttons occupy ~32 px of permanent noise per card in a scrolling record. |
| Remove and replace with an outcome row | **Chosen.** |

The record of *what was offered* is preserved without the widgets, by two things that stay on the card
forever: the **tier badge** (which is what determines whether 4 or 2 buttons existed) and the outcome
line, which **names the clicked action verbatim**.

**Mechanism:** `remove(buttonRow)`, `add(outcomeRow, sameGridBagConstraints)`, `revalidate()`,
`repaint()`. Replacing in the same grid cell keeps every other row from reflowing. The wrapper installed
by `SessionPanel.addComponent` recomputes from `preferredSize` on every layout pass
(`ChatPanel.kt:1681`), so the card shrinking is handled with no extra work.

### Outcome row

`BoxLayout.X_AXIS`: glyph label, `Box.createRigidArea(Dimension(Spacing.xs, 0))`, verb label,
`rigidArea(Spacing.sm)`, timestamp label, `Box.createHorizontalGlue()`.

| Element | Font | Color |
|---------|------|-------|
| Glyph (U+2714 approved / U+2716 denied) | `Typography.body` | `Colors.statusSuccess` / `Colors.statusError` |
| Verb | `Typography.label` | `Colors.onSurface` |
| Timestamp | `Typography.caption` | `Colors.onSurfaceVariant` |

**Label split — applies to every outcome string in §Copywriting Contract, full card and compact row
alike.** The glyph label carries the leading glyph and nothing else. The timestamp label carries the
trailing time token **including the separator or parentheses exactly as the copy line writes them**
(`— {time}` for the em-dash forms, `({time})` for the parenthesised forms). The verb label carries
everything in between. This fixes which of the three labels each fragment lands in; it alters no string.

Color appears on the glyph **only**. No filled background on a resolved card: a resolved card must
recede, and a denied card that shouts red forever is itself a habituation trainer.

Timestamp format: reuse whatever `ChatMessagePanel` renders at `ChatPanel.kt:1827` — do not invent a
second format in the same transcript.

### The args region stays expandable after resolution

Non-negotiable. The args are the record; collapsing them on resolution would destroy the reason the
card persists, and auto-collapsing on approve would hide the thing the user just authorised. Whatever
expand state the user left it in is preserved through resolution.

### The compact resolved row (states 2 and 3)

States 2 and 3 are **not** decisions. The gate consulted `approvedForSession` / `deniedForSession`,
applied a decision the user already made, and moved on. The row is a receipt, not a prompt, and it must
be designed as one rather than left for the executor to invent.

**Both variants are the same layout.** The required session-denied row and the optional session-approved
row (FLAG-22-03) differ in exactly one place: the outcome row's glyph, its color and its verb. Shipping
the optional variant therefore costs one `when` branch, not a second component — a fact the executor
should have when making FLAG-22-03's call.

#### Which of rows 0–10 survive

| Full-card row | On a compact row | Reason |
|---------------|------------------|--------|
| 0 — heading + tier badge | **dropped** | see below |
| 1 — catalog title | **kept**, unchanged position in reading order | the "what tool" answer; extension-derived; essential to the record |
| 2 — tier reason | **dropped** | it explains what the buttons mean; there are no buttons |
| 3 — repeat counter | **dropped** | see below |
| 4 — trust label (Rule T-5) | **kept** | T-5 is not conditional; a model region is present |
| 5 — tool-ID box | **kept** | see below |
| 6 — args toggle | **kept**, always **collapsed** by default | see below |
| 7 — args area | **kept**, `isVisible = false` until the toggle is clicked | the record of what ran (state 2) or what was attempted (state 3) |
| 8 — truncation footer | **kept, conditional** — appears only when row 7 is visible **and** truncated | identical rule to the full card |
| 9 — buttons | **replaced by the outcome row, moved to the top** | see below |
| 10 — session-scope footer | **dropped** | see below |

Result: **five visible rows collapsed**, against a resolved full card's eight or nine. The compact row
is roughly half the height, which is the whole point of the word "compact".

#### Why the heading and the tier badge are dropped

- **Heading.** `The AI asked to run a tool` is the *question* on a pending card. On a compact row the
  next line already answers it. Dropping it also gives the two card kinds visibly different first lines
  — a bold proportional `sectionTitle` heading versus a glyph plus a `Typography.label` verb — so a
  reader scanning the transcript can separate *"a decision happened here"* from *"a decision you made
  earlier was applied here"* without reading either. That distinction is semantic, not cosmetic.
- **Tier badge.** §Resolution keeps the badge on a mutated full card because it is the record of *what
  was offered* — four buttons or two. On a compact row **nothing was offered**; no button ever existed,
  so the badge has no record to carry. It is also invariant: both session sets can only be populated by
  `Approve for session` / `Deny for session`, which only a `CONFIRM` card renders (D-02 / D-11 give
  `CONFIRM_EACH` only `Approve once` and `Deny`), so every compact row is `CONFIRM` and a badge would
  read the same on every one of them. A constant badge is noise, not a channel.

#### Why the outcome row goes first here and last on a mutated full card

On a mutated full card the outcome sits last because it is dropped into the button row's grid cell —
a mechanical consequence of §Resolution's *"replace in the same cell so no other row reflows"*, not a
reading-order decision. A compact row has no button row to replace, so the constraint does not apply,
and it has no heading, so its first line does heading duty. The inconsistency is deliberate and it is
the signal described above.

#### Why the model regions are not dropped

- **State 2's call actually ran.** The args are the record of what executed against the target. D-07's
  *"the args are where exfiltration hides"* applies with full force to a call the user was never asked
  about.
- **State 3 exists precisely because a tool the user already refused was requested again.** What was
  requested is the entire content of the event.
- **The catalog title is insufficient for an unknown tool**, where the model's string is the only
  identifying content — and an unknown tool is a realistic occupant of `deniedForSession`.
- Keeping the tool-ID box **forces** the trust label to come with it (Rule T-5).

#### Why the repeat counter and the session-scope footer are dropped

- **Repeat counter.** Mechanism B is an anti-habituation device aimed at a decision, and there is no
  decision here. The signal is not lost: five compact rows for the same tool **are** the repeat count,
  visible without a caption and without spending `statusWarning` on a row whose job is to recede.
- **Session-scope footer.** Row 10 exists to reconcile D-11's button vocabulary ("session") with the
  rest of the UI ("chat"). No buttons, no mismatch — and the compact outcome copy already says "for this
  chat", which is the plain form row 10 reconciles *to*. The full card that created the session-wide
  decision carried row 10 and is still above in the same transcript.

#### Why the args are collapsed by default even on a former `CONFIRM_EACH` tool

Mechanism A expands args on `CONFIRM_EACH` in order to interrupt a run of prompts. A compact row
interrupts nothing. §"The args region stays expandable after resolution" is honoured — the region *is*
expandable; there is simply no prior user expand state to preserve, because the row was never pending.

#### Structure

```
┌ 3px MatteBorder accent strip  (borderSubtle — always; a compact row is never pending)
│ ┌ 1px LineBorder(Colors.border, 1, true)
│ │ EmptyBorder(Spacing.sm, Spacing.md, Spacing.sm, Spacing.md)   // 8, 12, 8, 12
│ │
│ │  row C0   ✖ Blocked automatically — you denied this tool …    outcome row (§Outcome row)
│ │  row C1   Send HTTP/1.1 request                               body / onSurface
│ │  row C2   AI-supplied — this extension did not write …        caption / onSurfaceVariant
│ │  row C3  ┌────────────────────────────────────────────┐       JTextField, mono, boxed
│ │          │ http1_request                              │
│ │          └────────────────────────────────────────────┘
│ │  row C4   ▶ Show arguments (412 characters)                   JButton, body, LEFT
│ │  row C5  [args area — isVisible = false until C4 clicked]     JTextArea, mono, boxed
│ │  row C6  [truncation footer — only if C5 visible + truncated] helpLabel
│ └
└
```

Same as the full card in every other respect: `GridBagLayout` single column, `isOpaque = true` with
`background = Colors.cardSurface`, no `preferredSize`, no `maximumSize` override, no `JTextArea.rows`,
and the same `updateUI()` obligation (§Light/Dark Compliance Rules).

#### Insets — no compact-specific value

| Compact rows | `GridBagConstraints.insets` | Same as full-card row |
|--------------|------------------------------|-----------------------|
| C0, C1, C2 | `Insets(0, 0, Spacing.xs, 0)` = (0, 0, 4, 0) | rows 0–4 |
| C3, C5 (model regions) | `Insets(0, 0, Spacing.sm, 0)` = (0, 0, 8, 0) | rows 5, 7 |
| C4, C6 | `Insets(0, 0, Spacing.xs, 0)` | rows 6, 8 |

#### The two variants

| Variant | Row C0 glyph | Glyph color | Verb (see §Copywriting Contract) |
|---------|--------------|-------------|----------------------------------|
| Session-denied (**required**) | ✖ U+2716 | `Colors.statusError` | `Blocked automatically — you denied this tool for this chat` |
| Session-approved (**optional**, FLAG-22-03) | ✔ U+2714 | `Colors.statusSuccess` | `Ran without asking — approved for this chat` |

Rows C1–C6 are byte-identical between the two variants.

---

## The Hard Problem: Anti-Habituation

Chains run up to `MAX_AUTO_TOOL_ITERATIONS = 8` (`ChatPanel.kt:1191`), so a user may see several cards
in a row. **Position, stated plainly:**

### Do not add friction to the approve path

Rejected: type-to-confirm, hold-to-confirm, a timed delay before `Approve` becomes enabled, a second
confirmation dialog.

1. **Constant cost trains the routine it is meant to break.** A fixed extra motion becomes part of the
   motor pattern (click, wait, click) and runs without reading. The CONTEXT's own rejection of
   "prompt-for-everything" (D-01) and its acceptance of a deliberately small `AUTO` set (D-05) rest on
   exactly this reasoning: *fewer, more meaningful prompts beat many cheap ones.* Adding cost per prompt
   reverses that.
2. **A time-dependent button contradicts D-11.** D-11 rejected the "remember" checkbox because a widget
   that silently changes what a security button means is a mis-click generator. A button whose
   availability changes over time has the same defect on a different axis: the user cannot answer "what
   does this button do right now" from looking at it.
3. **The escape hatch already exists and is better.** D-09 points at `ToolSessionState.toolsMode`
   (`ChatPanel.kt:1601`). A user who is clicking through is better served by turning tools off than by
   being slowed down.

**Answering the question directly:** friction is *cost without information* and it is automatable.
Anti-habituation is *information that varies with context*, which a motor routine cannot absorb. The
three mechanisms below are all of the second kind.

### The three anti-habituation mechanisms

| # | Mechanism | Applies to | Why it is information, not cost |
|---|-----------|-----------|----------------------------------|
| **A** | Args **expanded by default** on `CONFIRM_EACH`; collapsed on `CONFIRM` | tier | D-02/D-04's entire argument for `CONFIRM_EACH` is *"constant tool name and hostile args"* — the args **are** the decision for that tier. Showing them is not friction, it is showing the thing that must be read. Side effect: the two tiers become physically different sizes, so a `CONFIRM_EACH` card visually interrupts a run of `CONFIRM` cards. Mechanism already exists: `ActionCard(initiallyExpanded: Boolean = false)` (`ActionCard.kt:22`). |
| **B** | **Repeat counter** caption: `2nd request for this tool in this chat` | any tier, when count > 1 | The one signal that actually breaks autopilot: it says *the model is insisting*. Extension-derived (a per-canonical-ID `Map<String,Int>` on `ToolSessionState`), costs nothing, and it changes on every card so it cannot be tuned out. `Typography.caption` + `Colors.statusWarning` foreground. **Required for `CONFIRM_EACH`, recommended for `CONFIRM`.** |
| **C** | **Payload size in the collapsed toggle label**: `▶ Show arguments (4021 characters)` | any tier | A large args blob announces itself before it is opened. Varies per call. |

### Buttons carry no color, and there is no primary action

All four are plain `JButton` with `Typography.body` and default L&F chrome.

- **No `primaryButton(...)`** (`Components.kt:347`). It fills with `Colors.primary` — the 10% accent —
  whose reserved list in `09-UI-SPEC.md` §Color is *"Primary action buttons (e.g. Save Settings),
  ToggleSwitch track when ON, keyboard focus ring, store badge, link-style secondary buttons"*. A
  security affirmative is not on that list, and making `Approve once` the visually dominant control is
  the exact opposite of the goal.
- **No `statusError` on `Deny`.** `09-UI-SPEC.md` reserves the destructive color for *"Delete / Reset /
  irreversible confirmation buttons"*. Deny is neither destructive nor irreversible — it is the safe
  outcome. Colouring it red would miscolour the safe action as dangerous.
- **There is deliberately no default / recommended action.** The card must not nominate a winner. This
  also states the answer to "should approve be more deliberate than deny": approve must not be made
  *easier*, which is a different and stronger claim than making it harder.

---

## The Hard Problem: Args Preview Sizing and Overflow

### No nested `JScrollPane`

`ActionCard` puts its preview in a `JScrollPane` (`ActionCard.kt:30`). **Do not copy that here.** Three
reasons:

1. **It hides args below a fold**, which is precisely what D-07 argues against ("full args are shown
   because the args are where exfiltration hides").
2. **Nested-scroll wheel trap.** A `JScrollPane` inside the transcript's `JScrollPane`
   (`ChatPanel.kt:1652`) swallows the wheel while the pointer is over it. `ActionCard` rarely hits this
   because it appears once at the top of a session; an approval card appears repeatedly mid-transcript.
3. It removes an entire class of `preferredSize`-vs-`lineWrap`-vs-`maximumSize` interactions.

The args render into a plain `JTextArea` added directly to the card's grid. Height is whatever the
wrapped, capped content needs; the **transcript** scroll pane does the scrolling, which it already does
correctly.

### Three-stage disclosure

| Stage | Toggle label | Content shown | Footer |
|-------|--------------|---------------|--------|
| **Collapsed** (default for `CONFIRM`, and for every compact row) | `▶ Show arguments (N characters)` | args area `isVisible = false` | none |
| **Preview** (default for `CONFIRM_EACH`) | `▼ Hide arguments` | first **40 source lines** or **3200 characters**, whichever bites first | if truncated: footer + a show-all button (label below) |
| **Full**, `total ≤ 40 000` | `▼ Hide arguments` | entire sanitized args | `Showing all N characters.`; the button is removed |
| **Full**, `total > 40 000` | `▼ Hide arguments` | first 40 000 characters — the absolute ceiling | ceiling footer (below); the button is removed |

Show-all button label, chosen so the button never promises more than it delivers:

| Condition | Button label |
|-----------|--------------|
| `total ≤ 40 000` | `Show all {N} characters` |
| `total > 40 000` | `Show first {cap} characters` (`{cap}` = 40 000) |

Glyphs U+25B6 / U+25BC match `AccordionPanel.kt:103`, so the affordance reads the same as everywhere
else in the extension. `ActionCard`'s label text ("Show payload preview") is not reused because
"arguments" is the accurate noun here.

**Collapsed means `isVisible = false`, not zero height** — `ActionCard.updateExpandedState()`
(`ActionCard.kt:112`) is the correct idiom. A hidden component contributes nothing to `preferredSize`,
which keeps the `getMaximumSize` wrapper at `ChatPanel.kt:1681` correct.

### The caps and where they come from

| Cap | Value | Anchor |
|-----|-------|--------|
| Preview source lines | **40** | Tightens `ActionCard.limitLines(raw, 50)` (`ActionCard.kt:106,118`). 40 is chosen so worst-case preview height ≈ 40 rows ≈ 680 px at base 13 pt — large, but finite, and only reached by a genuinely huge payload, which D-07 wants conspicuous. |
| Preview characters | **3200** | ≈ 40 rows × 80 columns, so the two caps bite at roughly the same visual height instead of one always winning. Covers a typical POST body (500–2000 chars) without truncating. |
| Tool ID inline cap | **120** | Same class of cap as `MAX_HEADER_VALUE_LENGTH = 200` (`McpBlockedRequestReporter.kt:17`), tightened because the field is single-line and narrow. Long enough for any legitimate `ext:<server>:<tool>` triple; short enough that a model cannot use the ID as a canvas. |
| Absolute ceiling (Full stage) | **`Defaults.MAX_CONTEXT_TOTAL_CHARS` = 40 000** (`Defaults.kt:47`) | Defensive only. The args come from model output, which is requested at `Defaults.CHAT_MAX_OUTPUT_TOKENS = 4096` ≈ 16 000 chars (`Defaults.kt:48`), so in practice this can never bite. It exists because a **local** backend may ignore the output cap. **When it does bite, the Full stage says so** — the copy is below, and `Showing all N characters.` must never appear in that case. |

### Truncation must be honest

There are **two** truncation points, and each needs its own extension-derived `helpLabel`
(`Components.kt:331`) sitting directly under the args box, **outside** it.

**Preview truncated** (the common case — the preview caps bit, more is available on this card):

> `Showing the first 3200 of 41003 characters. The full arguments are sent to the tool if you approve.`

**Full stage truncated** (the 40 000 ceiling engaged — no more is available on this card):

> `Showing the first 40000 of 41003 characters. This is the maximum this card will display. The full arguments are sent to the tool if you approve.`

A bare `…` is **forbidden** in both places. On a card whose whole purpose is authorisation, an ellipsis
reads as "nothing important follows". Each sentence is load-bearing:

1. The **counts** are honest — this is exactly the case where `Showing all N characters.` would be a
   false statement, so that string is reserved for `total ≤ 40 000` and never generated otherwise.
2. The **middle sentence of the ceiling footer** explains why no show-all button is present. Without it
   the user waits for a control that does not exist.
3. The **last sentence** is the one that carries the security property: **the display cap is a display
   limit, not an execution limit** — `McpToolExecutor.executeTool` receives the full args string
   regardless of what the card renders.

### Wrap behaviour is mandatory, not cosmetic

`lineWrap = true` + `wrapStyleWord = true` (supplied by `applyAreaStyle`, `Components.kt:503-504`).
The transcript scroll pane sets `horizontalScrollBarPolicy = HORIZONTAL_SCROLLBAR_NEVER`
(`ChatPanel.kt:1662`), so a non-wrapping args area would be **clipped with no way to read the rest.**

---

## The Hard Problem: Keyboard and Focus

### Focus does not move when the card appears

**Required.** Do not call `requestFocusInWindow()` on the card or any of its buttons when it is added.

1. The card is appended from an `invokeLater` (`ChatPanel.kt:667`) after `setSendingState(false)` has
   already re-enabled `inputArea` (`ChatPanel.kt:590`, `:866`). The user may be mid-word. Async focus
   theft loses keystrokes.
2. `inputArea` binds bare `VK_ENTER` to `sendMessage` (`ChatPanel.kt:381`) with `WHEN_FOCUSED`. Moving
   focus away and back makes the destination of an in-flight Enter nondeterministic.
3. An auto-focused approve button plus a stray Enter is exactly the mis-click class D-11 rejected the
   checkbox to avoid.

### Traversal order — safe action first

Visual order **equals** focus order. Do not use a custom `FocusTraversalPolicy` to diverge them.

```
[tool ID field] → [args toggle] → [args area, when visible] →
  [Deny] → [Deny for session] → ⟵16px gap⟶ → [Approve once] → [Approve for session]
```

- Tab from `inputArea` reaches **`Deny`** first. A stray Space on the first-reached button denies —
  the fail-closed outcome, matching this codebase's stated discipline that blocked paths fail closed
  (`22-CONTEXT.md` §Established Patterns).
- **`Deny`, not `Deny for session`, is the reflex target.** Deny-for-session has no in-session undo
  (D-09 ships no settings surface); `Deny` is fully recoverable because the model may ask again.
- Scope escalates left-to-right within each group, so the two **narrowest** actions (`Deny`,
  `Approve once`) flank the gap and the two session-wide actions sit at the outer edges.
- The **16 px gap** (`Spacing.lg`) between the poles is the cheapest mis-click mitigation available: a
  mis-aimed click near the boundary lands in dead space rather than on the wrong pole.
- The non-editable tool-ID `JTextField` is focusable and is a deliberate Tab stop, so a keyboard user
  can select and copy the tool ID.

### Forbidden bindings

| Forbidden | Why |
|-----------|-----|
| `rootPane.defaultButton = <any card button>` | A default button fires on Enter from anywhere in the window. `inputArea`'s Enter binding is `WHEN_FOCUSED` (`ChatPanel.kt:375`), so the moment focus is anywhere without a local Enter binding, the default button wins. |
| `JButton.setMnemonic(...)` | Mnemonics register at `WHEN_IN_FOCUSED_WINDOW`. Alt+A firing an approve from across the panel is the same defect. |
| Escape → Deny | Escape-to-dismiss is a **modal** idiom. On an inline record it would silently resolve a security decision from a keystroke aimed at something else — and it would add a sixth implicit-denial path outside the gate's `resolvePending` set, which is Pitfall 3's exact shape. `inputArea` already owns Escape for `cancelInFlight` (`ChatPanel.kt:384`). |
| Any accelerator or global key binding on the card | Same reasoning. |

**Only a mouse click, or Space on an explicitly-focused card button, resolves the decision.** Space is
Swing's standard `BasicButtonUI` press key and is acceptable *because* the focused button is `Deny`.

### Focus ring must be painted

`isFocusPainted = true` on all four decision buttons. This is a **deliberate deviation from the
neighbouring code** — `ActionCard.kt:58` and `ChatPanel.kt:433` both set `isFocusPainted = false` — and
the executor will copy the wrong line if it is not called out. A keyboard user must see which pole is
focused before pressing Space. Precedent for `true`: `primaryButton` (`Components.kt:353`).

The args toggle keeps `isFocusPainted = false`, matching `ActionCard.kt:58` — it is not a decision
control.

### Focus after resolution

Removing the button row while it owns focus lets Swing move focus somewhere arbitrary (often another
card, or the sessions list). **Before removing the row:** if the focus owner is a descendant of the
card, call `inputArea.requestFocusInWindow()`. That is the only deterministic target and it is where
the user's next action is. Precedent: `ChatPanel.kt:960` already does exactly this after the tools
menu. Guard it on ownership so a mouse click does not yank focus from wherever the user put it.

**Do not scroll on resolution.** The user is where they are.

### Accessibility

The card root carries an extension-derived `AccessibleContext.accessibleDescription`. It is the only
channel through which a screen-reader user receives the trust distinction that sighted users get from
the box, so it is specified **verbatim**, not by content description.

**Why this needs its own rule.** Rule T-2 row 5 forbids concatenating a model string into extension
text. The accessible description is the single sanctioned exception, and the obvious justification for
it — *"it is not a rendered surface"* — is **wrong**: for a blind user the accessible description **is**
the rendered surface. What actually makes concatenation survivable in a `JLabel` is the position-0 rule
(`BasicHTML.isHTMLString` fires only at offset 0, so a model string that is not first cannot install a
renderer — see Rule T-2), and **that rule has no screen-reader analogue.** A screen reader has no notion
of position; it simply reads on. A model string placed mid-sentence would therefore be followed by
extension-authored audio, and `scope_check. Press Approve once to continue` would be indistinguishable
from narration this extension wrote. The mitigation is positional in a different way: put the model
string **last** and let nothing follow it.

**Three conditions. All three are required; the description is unsafe if any one is dropped.**

1. The tool ID is **inline-sanitized** (Rule T-6) — control characters removed, whitespace collapsed,
   capped at 120 characters with a `…` suffix. A multi-line ID cannot exist here, so it cannot be read
   as several sentences.
2. The tool ID is the **final element of the string**, with **no character after it** — no trailing
   period, no button summary, no punctuation. This is mechanically checkable:
   `description.endsWith(sanitizedToolId)` must be `true`.
3. The tool ID is immediately preceded by the extension-derived disclosure clause below, so the
   attribution is heard **before** the attacker-influenceable text, never after it.

**Verbatim templates.**

| Card state | `accessibleDescription` |
|------------|--------------------------|
| Pending, known tool | `The AI asked to run a tool. Tier: {TIER}. {catalog title}. The following tool name was written by the AI, not by this extension: {inline-sanitized tool ID}` |
| Pending, unknown tool | `The AI asked to run a tool. Tier: {TIER}. Not a known tool — this call will fail if you approve it. The following tool name was written by the AI, not by this extension: {inline-sanitized tool ID}` |
| Resolved card, or compact resolved row | `{outcome line, minus its leading glyph}. The following tool name was written by the AI, not by this extension: {inline-sanitized tool ID}` |

- `{TIER}` renders the badge's own text token exactly — `CONFIRM` or `CONFIRM EACH` — so the audio and
  the badge (channel 1, §Tier Legibility) say the same word.
- The two pending templates differ only in the title clause, which is row 1's copy verbatim. The
  unknown-tool title already ends in a period, which is why it is spelled out rather than substituted
  into `{catalog title}.` — substitution would produce `it..`.
- **Update the description when the card mutates.** A resolved card that still announces "The AI asked
  to run a tool" is a false statement made to the one user who cannot see that the buttons are gone.
- Nothing else is added. The four buttons carry their own accessible names from their labels
  (§Copywriting Contract), so restating them here would only lengthen the audio in front of the tool ID.

---

## The Hard Problem: Multiple Pending Cards

### Within the active session

Only pending cards carry a **colored accent strip** (states 4/5); every resolved card drops to
`borderSubtle`. Scanning the transcript therefore answers "is anything waiting for me" with one glance
and no reading. `SessionPanel.refreshScroll()` (`ChatPanel.kt:1711-1716`) already auto-scrolls to the
bottom on `addComponent`, so a newly created card is always in view in the active session.

### Returning to a session that has a pending card

`switchToSession` only swaps the `CardLayout` card, so the transcript is exactly as the user left it —
which means the pending card may be scrolled out of view (22-RESEARCH.md §"Pending-Decision Lifecycle",
verified). **Required:** on switching to a session with a pending decision, scroll the transcript to
the pending card.

New method on `SessionPanel`, mirroring the existing `refreshScroll` idiom (revalidate, then
`invokeLater` so it runs after layout):

```
fun scrollToComponent(c: JComponent)   // c.scrollRectToVisible(Rectangle(0, 0, c.width, c.height))
```

**Do not move focus on switch** — same reasoning as above.

### From outside the session

Without a signal, a pending decision in a background session is invisible and the chain is parked; the
user concludes the model gave up. **Required, minimal:** a third `JLabel` in `ChatSessionRenderer`'s
`textPanel` (already `BoxLayout.Y_AXIS`, `ChatPanel.kt:1625-1627`), visible only when that session has
a pending decision.

| Property | Value |
|----------|-------|
| Text | `Awaiting approval` |
| Font | `label.font.deriveFont((label.font.size - 2).toFloat())` — **not** `Typography.caption`; see below |
| Foreground | `DesignTokens.Colors.statusWarning`, **except when selected**, where it follows `list.selectionForeground` exactly as the two existing labels do (`ChatPanel.kt:1631`, `:1639`) |
| Refresh | the existing `refreshSessionList()` (`ChatPanel.kt:1248-1257`) — it re-sets every model element, which re-runs the renderer. Call it when a decision is created and when it resolves. |

**Why the font is the one place this phase does not name a `DesignTokens` role.** The marker is stacked
directly beneath two existing labels in the same `BoxLayout.Y_AXIS` column: `titleLabel` at `label.font`
(`ChatPanel.kt:1630`) and `backendLabel` at `label.font.deriveFont((label.font.size - 2).toFloat())`
(`:1638`). `Typography.caption` is `baseSize * 0.9f` (`DesignTokens.kt:104`) — at a base of 13 that is
**11.7 pt stacked against the sibling's 11 pt**. Two small sizes differing by 0.7 pt in one narrow column
read as a rendering defect, not as a hierarchy, and Rule 5 (§Light/Dark Compliance) forbids migrating the
neighbours to fix it from the other side. So the new label matches its sibling instead.

This is **not** a new typography role: no derivation is added to `DesignTokens.Typography` and the
declared maximum of 5 roles stands. `ChatSessionRenderer` is pre-existing `UiTheme`-era code
(`ChatPanel.kt:1639` reads `UiTheme.Colors.onSurfaceVariant`) whose type scale comes from the `JList`
renderer's own font, not from `DesignTokens`. The **color** still comes from `DesignTokens`: colors
resolve through the same `UIManager` keys down both paths, so there is nothing visually divergent to
match — fonts do diverge visibly, which is the entire point.

Text-first, color redundant. Cheap fallback if adding a label to the renderer is judged too invasive:
append `"  ·  Awaiting approval"` to the existing info string at `ChatPanel.kt:1636` (the `·` separator
is already in use there). The fallback raises no font question at all, because it reuses `backendLabel`.

---

## Layout & Sizing Contract

*(replaces the template's breakpoints / media-queries section)*

### Card structure

```
┌ 3px MatteBorder accent strip  (statusWarning | statusError | borderSubtle when resolved)
│ ┌ 1px LineBorder(Colors.border, 1, true)
│ │ EmptyBorder(Spacing.sm, Spacing.md, Spacing.sm, Spacing.md)   // 8, 12, 8, 12
│ │
│ │  row 0   The AI asked to run a tool          [Tier: CONFIRM EACH]     sectionTitle + badge
│ │  row 1   Send HTTP/1.1 request                                        body / onSurface
│ │  row 2   This tool is approved one call at a time. A session-wide …   caption / onSurfaceVariant
│ │  row 3   2nd request for this tool in this chat                       caption / statusWarning
│ │  row 4   AI-supplied — this extension did not write the text below:   caption / onSurfaceVariant
│ │  row 5  ┌───────────────────────────────────────────────────────┐     JTextField, mono, boxed
│ │         │ http1_request                                         │
│ │         └───────────────────────────────────────────────────────┘
│ │  row 6   ▼ Hide arguments                                             JButton, body, LEFT
│ │  row 7  ┌───────────────────────────────────────────────────────┐     JTextArea, mono, boxed
│ │         │ {                                                     │
│ │         │   "host": "evil.example",                             │
│ │         └───────────────────────────────────────────────────────┘
│ │  row 8   Showing the first 3200 of 41003 characters. …                helpLabel
│ │  row 9   [Deny] [Deny for session]      [Approve once] [Approve for session]
│ │  row 10  "Session" means this chat. Approvals are forgotten when …    helpLabel
│ └
└
```

**The diagram is illustrative, not normative.** Where a line is too wide for the box it is cut with `…`
— rows 2, 8 and 10 above are all truncated this way. **§Copywriting Contract is the single source for
every user-facing string on this card**, and where the two appear to differ, the Copywriting Contract
wins by definition.

The compact resolved row (states 2 and 3) has its own structure, insets and rationale in
§"The compact resolved row"; it reuses this card's layout manager, border, opacity and size contract
unchanged.

### Layout managers

| Element | Manager | Rationale |
|---------|---------|-----------|
| Card root | `GridBagLayout`, single column, `gridwidth = REMAINDER`, `fill = HORIZONTAL`, `weightx = 1.0`, `anchor = WEST` | Same idiom as `ActionCard.kt:65-73`, simplified to one column |
| Row 0 (title + badge) | `BoxLayout.X_AXIS`: title, `rigidArea(Spacing.sm, 0)`, badge, `horizontalGlue()` | Precedent `ChatPanel.kt:1831-1836`. Keeps the two trust signals adjacent instead of pinning the badge to a far-right edge that moves when the tab is resized. |
| Row 9 (buttons) | `BoxLayout.X_AXIS`: `Deny`, `rigidArea(Spacing.sm)`, `Deny for session`, `rigidArea(Spacing.lg)`, `Approve once`, `rigidArea(Spacing.sm)`, `Approve for session`, `horizontalGlue()` | Left-aligned: the card is a record, not a dialog footer. `Spacing.sm` is documented as "button gap in button rows" (`DesignTokens.kt:44`); rigid-area gaps follow `Components.kt:403`. |
| Outcome row | `BoxLayout.X_AXIS` (see §"Outcome row") | — |

### Row insets

All on the 4 px grid. Horizontal insets are **0** — horizontal padding comes from the card's
`EmptyBorder`, so it is declared once.

| Rows | `GridBagConstraints.insets` |
|------|------------------------------|
| 0–4 | `Insets(0, 0, Spacing.xs, 0)` = (0, 0, 4, 0) |
| 5, 7 (model regions) | `Insets(0, 0, Spacing.sm, 0)` = (0, 0, 8, 0) |
| 6, 8, 10 | `Insets(0, 0, Spacing.xs, 0)` |
| 9 (buttons) | `Insets(Spacing.sm, 0, 0, 0)` = (8, 0, 0, 0) |

`ActionCard`'s own insets (`Insets(8,10,0,10)`, `Insets(4,10,0,10)`, `Insets(6,10,10,10)` —
`ActionCard.kt:72,77,87,91`) use 6 and 10, which are **off the 4 px grid**. They are not copied.

### Size contract

| Rule | Value | Why |
|------|-------|-----|
| `preferredSize` | **never set** on the card or any child | Height must follow content; width must follow the container |
| `maximumSize` | **never overridden on the card** | `SessionPanel.addComponent` already wraps it in a panel whose `getMaximumSize()` returns `Dimension(superWidth, preferredSize.height)` (`ChatPanel.kt:1679-1685`). Overriding it on the card too would double-constrain. |
| Width | comes from the container | The user drags the Burp tab divider; a preferred width would fight them |
| `isOpaque` | **`true`**, with `background = Colors.cardSurface` | `SessionPanel.addComponent`'s wrapper sets `isOpaque = false` (`ChatPanel.kt:1683`), so the card must paint its own background or `cardSurface` never appears |
| `JTextArea.rows` | **never set** | Height is content-driven; a fixed `rows` would clip. (This is also why no fixed pixel height is used — `ContextPreviewDialog.kt:51,93` hardcodes `Dimension(740, 90)` / `Dimension(740, 340)` and renders a large empty box for short prompts. In-repo anti-example.) |

---

## Spacing

Inherited from `DesignTokens.Spacing` (`DesignTokens.kt:40-75`). **No new spacing value is declared, and
no new off-grid value is introduced.**

| Token | Value | Use in this phase |
|-------|-------|-------------------|
| `xs` | 4 | Row gaps (rows 0–4, 6, 8, 10; compact rows C0–C2, C4, C6); glyph-to-text gap in the outcome row |
| `sm` | 8 | Card border top/bottom; gap around model regions; gap between sibling buttons; title-to-badge gap |
| `md` | 12 | Card border left/right |
| `lg` | 16 | **Gap between the deny group and the approve group** |
| `xl` | 24 | not used |

### Off-grid values present on this card — both inherited, neither declared here

| Value | Where | Kind | Status |
|-------|-------|------|--------|
| **3 px** | accent strip, `MatteBorder(0, 3, 0, 0, accent)` | border weight | Matches `SubtleNotice.kt:117` exactly. A border weight is not a spacing step — the same status `09-UI-SPEC` gives `ToggleSwitch`'s 44×22 fixed size. |
| **`EmptyBorder(2, 6, 2, 6)`** | tier badge internal padding | component-internal constant | Adopted **transitively** by specifying the badge as "structured exactly like `toolBadge`" (`Components.kt:470`). Off the 4 px grid. **Kept, not corrected:** changing it would make the tier badge visually inconsistent with the build-provenance badges that already ship from the same builder. |

**Why this is not the same as the `ActionCard` insets this document refuses.** `ActionCard`'s
`Insets(8,10,0,10)` / `Insets(4,10,0,10)` / `Insets(6,10,10,10)` (`ActionCard.kt:72,77,87,91`) are
**layout spacing between rows** — precisely the quantity §"Row insets" declares, on the grid, for this
card. Copying them would put off-grid values into a table this document owns. The badge's `2, 6, 2, 6`
is **internal to a component this document does not own**; it arrives with `toolBadge`'s structure and
is listed here for completeness rather than declared. The distinction is ownership, not size.

---

## Typography

Inherited from `DesignTokens.Typography` (`DesignTokens.kt:90-112`). **No new role is derived** — the
declared maximum of 5 roles stands.

| Role | Derivation | Weight | Use in this phase |
|------|-----------|--------|-------------------|
| `sectionTitle` | `base.deriveFont(BOLD, base * 1.2f)` | Bold | Card heading (row 0) |
| `body` | `base.deriveFont(PLAIN, base)` | Regular | Catalog title (row 1 / C1); all button labels; args toggle; outcome glyph |
| `caption` | `base.deriveFont(PLAIN, base * 0.9f)` | Regular | Tier reason (row 2); repeat counter (row 3); trust label (rows 4 / C2); truncation footer (rows 8 / C6); session-scope footer (row 10); outcome timestamp; tier badge |
| `label` | `base.deriveFont(BOLD, base)` | Bold | Outcome verb |
| `mono` | `UIManager.getFont("TextArea.font")` | Regular | **All model-supplied text** — Rule T-3 |

**One deliberate exception, outside `DesignTokens`:** the session-list pending marker uses the `JList`
renderer's own sibling font — `label.font.deriveFont((label.font.size - 2).toFloat())` — and **not**
`Typography.caption`. See §"From outside the session" for the measurement (11.7 pt against a sibling's
11 pt) and the reasoning. It is a font *match* inside legacy renderer code, not a new role.

**Line height:** Swing exposes none (`09-UI-SPEC.md` §Typography). The equivalent contract for this
card is `lineWrap = true` + `wrapStyleWord = true` on the args area, height content-driven, and `rows`
never set. See §"Layout & Sizing Contract".

**Sizes are multipliers, never pixels**, so the card follows the user's OS accessibility font setting.
This is why every cap in §"Args Preview" is expressed in characters and source lines rather than pixels.

---

## Color

Inherited from `DesignTokens.Colors` (`DesignTokens.kt:126-199`). **No new token, no `Color(0x…)`
literal, no `Font(...)` constructor.**

### 60 / 30 / 10 on this card

| Share | Token | Where |
|-------|-------|-------|
| **60 % dominant** | `surface` | The transcript this card sits in (`ChatPanel.kt:1658`) — the card does not repaint it |
| **30 % secondary** | `cardSurface` | The card body; the tier badge background |
| | `inputBackground` | Model-supplied regions (rows 5, 7 / C3, C5) |
| | `border` / `borderSubtle` | Card outline; model-region outlines; resolved accent strip |
| **10 % accent** | **`primary` is NOT used on this card** | See below |

**`primary` (Burp orange) is deliberately absent.** `09-UI-SPEC.md` reserves it for primary action
buttons, the toggle-on track, the focus ring, the store badge and link-style secondaries. Spending it
on a security affirmative would both violate that list and nominate `Approve` as the recommended
action — the opposite of §"Anti-Habituation".

### Status color budget — three uses, all redundant, none carrying text

| Token | Use | Redundant with |
|-------|-----|----------------|
| `statusWarning` | 3 px accent strip on a pending `CONFIRM` card; tier badge border; repeat-counter caption; session-list pending marker | The badge **text** ("Tier: CONFIRM"), the button count, the caption text |
| `statusError` | 3 px accent strip on a pending `CONFIRM_EACH` card; tier badge border; denied outcome glyph (full card and compact row) | The badge text ("Tier: CONFIRM EACH"), the button count, the outcome verb |
| `statusSuccess` | Approved outcome glyph (full card and compact row); `AUTO` tier badge border (unreachable this phase) | The outcome verb |
| `borderSubtle` | Accent strip on every **resolved** card and every compact row | The presence of an outcome row instead of buttons |

**Every colored element has a text or structural equivalent carrying the same meaning.** Remove all
color and the card still reads correctly — which is the requirement for two themes plus color
blindness.

### Contrast

The one contrast hazard is the tier badge; it is resolved by outlining rather than filling. See
§"Tier Legibility" for the computation — `SafetyIndicator`'s white on `#F57C00` is **≈2.70:1** at a WCAG
relative luminance of **0.338** — and for why `PrivacyPill`'s `onSurface` fix inverts in dark theme. **No
contrast number is claimed for the outlined badge itself:** it reuses the L&F's own `Label.foreground`
over `Table.background` pairing and therefore adds no new obligation, which is a weaker and more honest
claim than "the design system guarantees it".

### Destructive color

`statusError` is **not** used on the `Deny` buttons. `09-UI-SPEC.md` reserves it for irreversible
actions; deny is the safe, recoverable outcome. Colouring it red would teach the wrong thing.

---

## Glyphs

Unicode only (`09-UI-SPEC.md` §Design System — no icon library).

| Glyph | Code point | Use | Precedent |
|-------|-----------|-----|-----------|
| ▶ | U+25B6 | Args collapsed | `AccordionPanel.kt:103` |
| ▼ | U+25BC | Args expanded | `AccordionPanel.kt:103` |
| ✔ | U+2714 | Approved outcome | Declared in `09-UI-SPEC.md` §Design System |
| ✖ | U+2716 | Denied outcome | **NEW.** Same Dingbats block as U+2714, so coverage is equivalent. An approve/deny pair needs both poles; a checkmark alone with color-only negation would violate the no-color-alone rule. |
| · | U+00B7 | Separator in the session-list info line | `ChatPanel.kt:1636` |

**Fallback rule:** if any glyph renders as a missing-glyph box on some platform, drop it. The text is
load-bearing; the glyph is redundant. Never the reverse.

---

## Copywriting Contract

Sentence case. English only (CLAUDE.md §Constraints, AGENTS.md — non-negotiable). Every string below
is extension-derived and appears **outside** every model region. **This section is normative**; the
ASCII diagrams elsewhere in this document truncate long strings with `…` for width and are illustrative
only.

### Buttons — labels are locked by D-11, verbatim

| Label | Tooltip (extension-derived; `{title}` is the catalog title, never the model's string) |
|-------|--------------------------------------------------------------------------------------|
| `Approve once` | `Run this tool call now. The AI must ask again next time.` |
| `Approve for session` | `Run this and any later {title} call in this chat without asking.` |
| `Deny` | `Do not run this call. The AI is told the call was not authorised and continues without it.` |
| `Deny for session` | `Do not run this call, and refuse any later {title} call in this chat without asking again.` |

`CONFIRM_EACH` shows only `Approve once` and `Deny` (D-02, D-11).

### Card body

| Element | Copy |
|---------|------|
| Heading (row 0) | `The AI asked to run a tool` |
| Catalog title (row 1) — known tool | `{catalog title}` |
| Catalog title (row 1) — unknown tool | `Not a known tool — this call will fail if you approve it.` |
| Catalog title (row C1, compact) — unknown tool | `Not a known tool — no catalog entry matches this name.` |
| Tier reason (row 2) — `CONFIRM` | `Approving for the session applies to this tool until this chat is deleted.` |
| Tier reason (row 2) — `CONFIRM_EACH` | `This tool is approved one call at a time. A session-wide approval is not offered for it.` |
| Repeat counter (row 3) | `{2nd \| 3rd \| Nth} request for this tool in this chat` |
| Trust label (rows 4 / C2) | `AI-supplied — this extension did not write the text below:` |
| Args toggle — collapsed | `▶ Show arguments ({N} characters)` |
| Args toggle — expanded | `▼ Hide arguments` |
| Show-all button, `total ≤ 40 000` | `Show all {N} characters` |
| Show-all button, `total > 40 000` | `Show first {cap} characters` |
| Preview truncation footer (rows 8 / C6) | `Showing the first {shown} of {total} characters. The full arguments are sent to the tool if you approve.` |
| Full-args footer, `total ≤ 40 000` | `Showing all {N} characters.` |
| Full-args footer, `total > 40 000` (ceiling engaged) | `Showing the first {cap} of {total} characters. This is the maximum this card will display. The full arguments are sent to the tool if you approve.` |
| Session-scope footer (row 10) | `"Session" means this chat. Approvals are forgotten when the chat is deleted, and are not restored when Burp restarts.` |

`{cap}` is `Defaults.MAX_CONTEXT_TOTAL_CHARS` (`Defaults.kt:47`), currently 40 000. It is interpolated
rather than typed literally so the copy cannot drift from the constant.

**Row 10 is load-bearing, not filler.** D-11's button labels say "session" while the rest of the UI says
"chat" ("Chat 1", "Clear Chat", "clear this chat history" — `ChatPanel.kt:976`). The locked labels are
not renamed; this one sentence reconciles the vocabulary **and** records the measured fact that
`restoreSessions()` builds a fresh `ToolSessionState` (`ChatPanel.kt:1475`), so approvals do not survive
a Burp restart (22-RESEARCH.md §7).

**The compact unknown-tool title is a separate string on purpose.** Row 1's pending wording ends
"…if you approve it", which names a button that does not exist on a compact row, and it is tense-wrong in
both compact states — state 2's call already ran and failed, state 3's was never run at all.

### Outcome lines

| State | Copy |
|-------|------|
| Approved once | `✔ Approved once — {time}` |
| Approved for session | `✔ Approved for this chat — {time}` |
| Denied | `✖ Denied — {time}` |
| Denied for session | `✖ Denied for this chat — {time}` |
| Implicitly denied (new message) | `✖ Denied automatically — you sent a new message ({time})` |
| Suppressed — session-approved (compact row C0) | `✔ Ran without asking — approved for this chat ({time})` |
| Suppressed — session-denied (compact row C0) | `✖ Blocked automatically — you denied this tool for this chat ({time})` |

Each line splits across the outcome row's three labels by the rule in §"Outcome row".

### Empty / error states

| State | Copy |
|-------|------|
| Empty state | **n/a** — the card exists only when there is a decision to make. There is no zero-state. |
| Error state | **n/a on the card.** If an approved call then fails, that is the existing transcript path `panel.addMessage("Tool result: …", "Error: …")` (`ChatPanel.kt:2153`), unchanged. The card's only error-adjacent state is the unknown-tool title above. |

### Destructive actions in this phase

The two **approve** actions are the risk-bearing ones. Confirmation approach: **the card itself is the
confirmation.** No second dialog, no type-to-confirm — see §"Anti-Habituation". Phase 15's discipline
carries forward verbatim: *the affirmative is never the default focus* (`ContextPreviewDialog.kt:112`,
`15-UI-SPEC.md` §Delta 2), which here means `Deny` is the first Tab stop and no card button is a default
button.

---

## Component / Builder Usage Map

| UI element | Component / builder | Source | Status |
|------------|---------------------|--------|--------|
| Approval card | `ToolApprovalCard` | `ui/components/ToolApprovalCard.kt` | **NEW** |
| Compact resolved row | `ToolApprovalCard` in resolved-only mode | §"The compact resolved row" | **NEW** — same class, same layout manager, no second component |
| Card insertion | `SessionPanel.addComponent(JComponent)` | `ChatPanel.kt:1677` | existing, unchanged |
| Scroll to a pending card | `SessionPanel.scrollToComponent(JComponent)` | `ChatPanel.kt` (near `:1711`) | **NEW** — one method |
| Session-list pending marker | third `JLabel` in `ChatSessionRenderer` | `ChatPanel.kt:1625-1627` | **MODIFIED** — one label |
| Model-region styling | `applyFieldStyle` / `applyAreaStyle` | `Components.kt:484`, `:498` | existing, reused |
| Captions / footers | `helpLabel(text)` | `Components.kt:331` | existing, reused |
| Tier badge | `private fun tierBadge(tier)` | inside `ToolApprovalCard.kt`, structured as `toolBadge` (`Components.kt:426`) | **NEW**, not promoted to `Components.kt` (one caller) |
| Accent strip | `BorderFactory.createMatteBorder(0, 3, 0, 0, accent)` inside a compound border | idiom from `SubtleNotice.kt:113-120` | existing idiom, reused |
| Expand/collapse | pattern from `ActionCard.updateExpandedState()` | `ActionCard.kt:97-116` | idiom reused; class not extended |
| Decision buttons | plain `JButton` + `Typography.body` + `isFocusPainted = true` | — | **not** `primaryButton` / `secondaryButton` |

### Why a new component instead of extending `ActionCard`

22-RESEARCH.md §"Alternatives Considered" already recommends a new class on API grounds (five `String`
params, no button row, one existing caller at `ChatPanel.kt:344`). **The stronger reason is trust
typography:** `ActionCard` renders all four of its text fields into `JLabel`s (`ActionCard.kt:24-27`),
and a `JLabel` interprets a leading `<html>` tag. Passing a model-supplied tool ID into
`ActionCard(actionName = …)` would be a live UI-spoofing vector (Rule T-2). `ActionCard` also uses
`UiTheme` (legacy), `Colors.surface` (blends into the transcript rather than lifting off it), off-grid
insets, a nested `JScrollPane`, and has **no `updateUI()` override** — four things this card must not
inherit.

---

## Light/Dark Compliance Rules

1. **No `Color(0x…)` literal, no `Font(...)` constructor, no hardcoded pixel font size** in
   `ToolApprovalCard.kt`. Every value comes from `DesignTokens`. (`PrivacyPill.kt:11-13` is the
   in-repo counterexample — do not follow it.)
2. **All tokens are read at call time**, never cached in a `val` at construction. `DesignTokens`
   properties are computed `get`s for exactly this reason (`DesignTokens.kt:11-14`).
3. **`ToolApprovalCard` MUST override `updateUI()`** and re-apply every token-derived color, font and
   border, using the `initialized`-flag guard idiom. `SafetyIndicator.kt:23-27` documents the hazard
   precisely: the super-constructor calls `updateUI()` **before** Kotlin field initialisers run, so an
   unguarded re-apply NPEs. Same pattern in `SubtleNotice.kt:44-46,91-98` and `AccordionPanel.kt:72-80`.
   `ActionCard` has no such override — that is an existing gap, not a precedent.
4. **The tier badge re-reads its background at paint time** in `paintComponent`, and re-applies its
   foreground in `updateUI()` — exactly `toolBadge`'s two-part handling (`Components.kt:447-451`,
   `:458-465`, flagged there as FLAG-10-01).
5. **Do not migrate neighbouring legacy code.** `ChatPanel` and `ActionCard` keep their `UiTheme`
   imports. This phase adds a component; it does not restructure `ChatPanel.kt` (2248 lines, split
   explicitly out of scope for v0.10.0). This rule is why the session-list pending marker matches its
   sibling's font instead of importing a second type scale into that column (§"From outside the
   session").

---

## States Summary

| State | Where | Presentation |
|-------|-------|--------------|
| No card | tier `AUTO`; user-originated calls (`:928`, `:2105`) | nothing added to the transcript |
| Pending `CONFIRM` | transcript | `statusWarning` strip; 4 buttons; args collapsed |
| Pending `CONFIRM_EACH` | transcript | `statusError` strip; 2 buttons; **args expanded** |
| Approved once / for session | transcript | `borderSubtle` strip; outcome row (✔) last; args state preserved |
| Denied / denied for session | transcript | `borderSubtle` strip; outcome row (✖) last; args state preserved |
| Implicitly denied (new message) | transcript | `borderSubtle` strip; outcome row naming the reason |
| Implicitly denied (4 other paths) | — | **no surviving surface**; the SC3 audit event is the record |
| Suppressed — session-denied | transcript | compact resolved row, required (§"The compact resolved row") — outcome row (✖) **first**, 5 visible rows |
| Suppressed — session-approved | transcript | compact resolved row, recommended — FLAG-22-03; identical layout, outcome row (✔) first |
| Pending, background session | sessions list | `Awaiting approval` caption on that session's row |
| Pending, on returning to the session | transcript | auto-scrolled into view; focus unchanged |

---

## Registry Safety

| Registry | Blocks used | Safety gate |
|----------|-------------|-------------|
| None | n/a | n/a — pure Kotlin/Swing, no component registry |

shadcn gate: **not applicable.** ADR-2 locks plain Swing; this is not a React/Next.js/Vite project and
`components.json` does not exist. No third-party registries are declared, so the registry vetting gate
is not run. This phase adds **zero** dependencies (22-RESEARCH.md §"Package Legitimacy Audit").

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Inline card in the transcript, not a modal | 22-CONTEXT.md **D-06** |
| Tool ID + catalog title + tier resolved by the extension; full args in an expandable preview | 22-CONTEXT.md **D-07** |
| Model text is plain text, never markdown, never extension chrome; control chars stripped; length cap | 22-CONTEXT.md **D-07**; Phase 20 D-07 |
| Unknown tool names labelled, never shown bare | 22-CONTEXT.md **D-07** |
| No timer; the card waits indefinitely | 22-CONTEXT.md **D-08** |
| Five implicit-denial paths, not three; only one leaves an observable card | 22-RESEARCH.md §"Pending-Decision Lifecycle" |
| Four labelled actions on `CONFIRM`, two on `CONFIRM_EACH`; no "remember" checkbox | 22-CONTEXT.md **D-11**, **D-02** |
| No settings UI in this phase | 22-CONTEXT.md **D-09** |
| "Session" = the chat session; memory dies with it | 22-CONTEXT.md **D-10** |
| Approvals do not survive a Burp restart | 22-RESEARCH.md §7 (`ChatPanel.kt:1475`) |
| Modals are untestable headless; `ActionCard` + `JButton.doClick()` are not | 22-RESEARCH.md §"Anti-Patterns", measured |
| Card must survive session switch and be re-findable | 22-RESEARCH.md §"Pending-Decision Lifecycle" (switch does not destroy state) |
| Spacing / typography / color / 60-30-10 / icon policy | `09-UI-SPEC.md`; `ui/design/DesignTokens.kt` |
| "Affirmative is never the default focus" | `15-UI-SPEC.md` §Delta 2; `ContextPreviewDialog.kt:99-113` |
| `SessionPanel.addComponent` wrapper caps height at `preferredSize` | `ChatPanel.kt:1677-1688` |
| Transcript disables horizontal scrolling | `ChatPanel.kt:1662` |
| `refreshSessionList()` re-runs the cell renderer | `ChatPanel.kt:1248-1257` |
| Chain cap of 8 → several cards in a row are expected | `ChatPanel.kt:1191` |
| `JLabel` and `JButton` install the HTML renderer identically; `BasicHTML.isHTMLString` fires only at position 0; `JTextField` / `JTextArea` never do | measured, Temurin JDK 21 headless (§Rule T-2) |
| White on `#F57C00` = relative luminance 0.338, contrast ≈2.70:1 | WCAG 2.x relative-luminance formula over `DesignTokens.kt:174`, `:186` |
| Session-list sibling font is `label.font.deriveFont((label.font.size - 2).toFloat())` | `ChatPanel.kt:1638` |

---

## Flags (Non-Blocking)

- **FLAG-22-01 — Repeat counter needs one new field.** Mechanism B (§Anti-Habituation) requires a
  per-canonical-ID call count on `ToolSessionState` (`ChatPanel.kt:1601`), beside D-10's approve/deny
  sets. It is AWT-free and trivial, but it is the only net-new state the card needs beyond the gate's
  own. If the planner declines it, the counter caption is dropped and mechanisms A and C still stand;
  say so explicitly rather than shipping a caption that always reads "1st".

- **FLAG-22-02 — Session-list pending marker rendering.** Preferred: a third `JLabel` in
  `ChatSessionRenderer.textPanel`. Permitted equivalent: append `"  ·  Awaiting approval"` to the
  existing info string at `ChatPanel.kt:1636`. Both satisfy the requirement (a pending decision in a
  background session must be discoverable). Do not ship neither.

- **FLAG-22-03 — Compact row for session-approved suppression.** Required for **session-denied**
  (otherwise the denial is completely invisible and the chain appears to stall). **Recommended but
  optional** for session-approved, where the existing "Tool result: …" message already shows that
  something ran. Symmetry argues for it; transcript noise argues against. Executor's call — but the
  choice must be deliberate and recorded, not defaulted by omission. §"The compact resolved row" now
  designs both variants; they differ by a single `when` branch, so the cost of shipping the optional one
  is known rather than estimated.

- **FLAG-22-04 — Preview caps (40 lines / 3200 chars) are a judgement, not a measurement.** They are
  anchored to `ActionCard`'s existing 50-line cap and to a ~80-column wrap, and they bound the worst-case
  preview at ≈40 rows. If live UAT shows real `http1_request` args routinely exceeding 3200 characters,
  raise the character cap and keep the line cap — the truncation footer and the `Show all` stage mean
  the security property does not depend on the exact number. The 40 000 absolute ceiling is a different
  quantity and is not a judgement: it is `Defaults.MAX_CONTEXT_TOTAL_CHARS`, and its copy is specified.

- **FLAG-22-05 — Tier badge stays private to `ToolApprovalCard.kt`.** Not promoted to a public builder
  in `Components.kt` while there is one caller (Phase 13's "avoid premature generalisation" stance). If
  a second surface ever renders a tier — e.g. a column in the MCP Tools tab — promote it then, and take
  the `tier → label` mapping with it so the strings cannot diverge.

- **FLAG-22-06 — U+2716 is an addition to Phase 09's declared glyph set.** Justified in §Glyphs. If the
  checker treats the glyph set as closed, drop both outcome glyphs and keep the text — the text is
  load-bearing and the design degrades cleanly.

- **FLAG-22-07 — Existing transcript renders model text as markdown.** `MarkdownRenderer.toHtml` into a
  `JEditorPane` with `contentType = "text/html"` (`ChatPanel.kt:1864`, `:2070`) escapes `<`
  (`MarkdownRenderer.kt:21`) but still applies markdown styling to model output, including tool results.
  **Out of scope for Phase 22** — recorded here so the observation is not lost. Candidate for Phase 26 /
  DOC-03.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
