---
phase: 22-agent-tool-call-trust-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt
  - build.gradle.kts
  - src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt
  - src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt
autonomous: true
requirements: [SEC-06]

must_haves:
  truths:
    - "A real ChatPanel constructs under -Djava.awt.headless=true: the single measured headless-hostile call at ChatPanel.kt:377-380 (Toolkit.getDefaultToolkit().menuShortcutKeyMaskEx) is guarded, so SC2/SC4/SC5 become assertable against the real production path instead of a modelled stand-in"
    - "ChatPanelToolGateTest drives the REAL Send button of a REAL ChatPanel and records today's SEC-06 defect: a model-emitted scope_check tool call reaches api.scope().isInScope(\"http://evil.example/\") with no user decision. This baseline records the UNGATED path and is GREEN today. scope_check is SecTier.AUTO in 22-02's locked tier table, so plan 22-07 KEEPS this assertion as the atLeastOnce() AUTO companion and adds the CONFIRM-tier gate assertion on proxy_http_history — that new assertion, not this one, is the one demonstrated red against the pre-gate commit under the Phase 20 SC4 / Phase 21 standard"
    - "No new test file in this phase carries a suffix the PR gate excludes (*IntegrationTest, *ConcurrencyTest, *BackpressureTest, *RestartPolicyTest, *SupervisionTest) — research Pitfall 7"
    - "The guard is additive: ChatPanel.kt is not restructured (its split is explicitly out of scope for v0.10.0) and no production behaviour changes outside the accelerator fallback"
  artifacts:
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt"
      provides: "Shared headless ChatPanel fixture: deep-stub MontoyaApi + AgentSupervisor, a sendChat stub emitting a caller-supplied model response, a depth-first component finder, and an EDT drain helper"
      contains: "object ChatPanelTestHarness"
      min_lines: 80
    - path: "src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt"
      provides: "The SC4 acceptance-gate file, seeded with the pre-fix characterization test"
      contains: "class ChatPanelToolGateTest"
    - path: "build.gradle.kts"
      provides: "tasks.test jvmArgs carrying -Djava.awt.headless=true so the harness behaves identically on macOS and ubuntu-latest"
      contains: "-Djava.awt.headless=true"
  key_links:
    - from: "src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt"
      to: "com.six2dez.burp.aiagent.ui.ChatPanel"
      via: "constructs the real ten-parameter ChatPanel with mockito deep stubs and TestSettings.baselineSettings()"
      pattern: "ChatPanel\\("
    - from: "src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt"
      to: "burp.api.montoya.scope.Scope"
      via: "verify(api.scope(), ...).isInScope(...) proves whether the model-emitted call reached Burp"
      pattern: "isInScope"
---

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<build_invocation>
EVERY Gradle invocation in this plan MUST be prefixed:
`JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew <tasks>`
Gradle 8.12.1 breaks on this machine's default JDK 25. A missing prefix fails for the wrong reason.
</build_invocation>

<objective>
Wave 0 for Phase 22. Land the two-line production guard that lets a real `ChatPanel` construct
headlessly, add the shared test harness, and seed `ChatPanelToolGateTest.kt` with a **characterization
test that proves the SEC-06 defect exists today**.

Research measured that `ChatPanel.kt:377-380` — `java.awt.Toolkit.getDefaultToolkit().menuShortcutKeyMaskEx`
— is the *only* headless-hostile call in the entire `ChatPanel` construction path. With it guarded, a
real panel constructs, the real Send button clicks, and a model-emitted `scope_check` was observed
reaching `api.scope().isInScope("http://evil.example/")` with no user decision. That is SEC-06, executed
rather than argued.

Without this plan, SC4's acceptance gate degrades to a modelled test that cannot go red against today's
`maybeExecuteToolCall` — the exact shape of gap that let SEC-04 ship green. Precedent for landing test
infrastructure as its own plan: Phase 20's `20-02` added the `BuildFlags.VERSION` Gradle seam purely so
`20-04`'s regression tests could exist.

Purpose: SEC-06 / SC4 — make the acceptance gate real, and land it in its own commit so CI (research
assumptions A3/A4: headless parity and Mockito inline mock-maker availability on `ubuntu-latest`) is
proven before eight plans are built on top of it.
Output: a guarded accelerator read, `-Djava.awt.headless=true` in `tasks.test`,
`ChatPanelTestHarness.kt`, and `ChatPanelToolGateTest.kt` carrying the pre-fix baseline.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-agent-tool-call-trust-boundary/22-CONTEXT.md
@.planning/phases/22-agent-tool-call-trust-boundary/22-RESEARCH.md
@.planning/phases/22-agent-tool-call-trust-boundary/22-PATTERNS.md
@.planning/phases/22-agent-tool-call-trust-boundary/22-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard the headless-hostile accelerator read and enable headless mode in tasks.test</name>

  <files>
    src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt,
    build.gradle.kts
  </files>

  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt lines 30-45 (the import block — `java.awt.event.InputEvent` is already imported at :38, so no new import is required) and lines 370-395 (`inputPanel`'s key-binding setup, where `menuMask` is computed and then consumed by the Cmd/Ctrl-T accelerator).
    - build.gradle.kts lines 150-172 — `tasks.test`. Note the existing `jvmArgs("-ea")` line carries an inline justification comment naming the requirement it serves (`REL-01 SC1 gate`); match that convention. Note also the `excludeHeavyTests` filter block at :161-171 and its five excluded suffixes.
    - .planning/phases/22-agent-tool-call-trust-boundary/22-RESEARCH.md section "2. SC4's Acceptance Gate" (Measurements A–D) and section "Pitfall 8" — Pitfall 8 states the preferred guard shape and why a broad `runCatching` is the weaker option.
    - .planning/phases/22-agent-tool-call-trust-boundary/22-PATTERNS.md section "Pattern 10 — the headless guard (Wave 0)" and section "`build.gradle.kts` (config) — MODIFIED".
  </read_first>

  <action>
Two additive edits. Do not restructure `ChatPanel.kt` — its split is explicitly out of scope for v0.10.0
and Phases 22 and 23 both touch it heavily.

**1. `ChatPanel.kt:377-380`.** The current expression assigns `menuMask` from
`java.awt.Toolkit.getDefaultToolkit().menuShortcutKeyMaskEx`. Under `-Djava.awt.headless=true` that
throws from `sun.awt.HeadlessToolkit.getMenuShortcutKeyMaskEx`, which is the single call that makes the
whole `ChatPanel` constructor unreachable from a test.

Replace it with a guarded read that falls back to `InputEvent.CTRL_DOWN_MASK`. Use the explicit form
research Pitfall 8 prefers over a broad `runCatching`: test `java.awt.GraphicsEnvironment.isHeadless()`
first and take the fallback directly when true, and wrap the non-headless read in a narrow
`try { … } catch (_: java.awt.HeadlessException) { InputEvent.CTRL_DOWN_MASK }`. `InputEvent` is already
imported at `:38`; `GraphicsEnvironment` and `HeadlessException` may be referenced fully qualified,
matching the file's existing `java.awt.Toolkit` usage style.

Attach a KDoc or `//` comment above the assignment naming the reason the guard exists: the SC4
acceptance gate in `ChatPanelToolGateTest` requires a real `ChatPanel` under
`-Djava.awt.headless=true`, and `CTRL_DOWN_MASK` is the correct accelerator on Linux and Windows so the
fallback is not a degraded UX on any platform where it can be reached. Mention SEC-06 so the guard is
not deleted later as dead code.

**2. `build.gradle.kts:153`.** Change `jvmArgs("-ea")` to `jvmArgs("-ea", "-Djava.awt.headless=true")`.
Keep the existing `-ea` justification comment and extend it (or add a second comment line) naming the
Phase 22 SC4 harness as the reason for the headless flag — so the harness behaves identically on a
developer Mac and on `ubuntu-latest`. Change nothing else in `tasks.test`; in particular leave the
`storeBuild.expected` system property and the `excludeHeavyTests` filter untouched.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test ktlintCheck detekt -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'Djava.awt.headless=true' build.gradle.kts` returns 1
    - `grep -v '^\s*//' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt | grep -c 'CTRL_DOWN_MASK'` returns at least 1 — the fallback is real code, not only a comment
    - `grep -v '^\s*//' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt | grep -c 'GraphicsEnvironment.isHeadless\|HeadlessException'` returns at least 1
    - `grep -c 'SEC-06\|SC4' src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt` returns at least 1 — the guard carries the comment naming why it exists
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test -q` exits 0 — the full existing suite still passes with headless mode on
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt -q` exits 0 and `git diff --stat -- detekt-baseline.xml` is empty (QUAL-07: the baseline must not grow)
  </acceptance_criteria>

  <done>`ChatPanel` no longer throws under `-Djava.awt.headless=true`, `tasks.test` runs headless, and the existing suite plus both static gates are green with an unchanged detekt baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChatPanelTestHarness — the shared headless ChatPanel fixture</name>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt</files>

  <read_first>
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt lines 75-95 — the ten-parameter constructor the harness must satisfy (nine of them lambdas) and the `public val root: JComponent` at `:89` that the component finder walks.
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt lines 415-470 — `sendFromInput()` and the input/Send wiring, so the finder targets the right widgets.
    - src/main/kotlin/com/six2dez/burp/aiagent/agent/AgentSupervisor.kt — grep for `fun sendChat(` and read its full signature. RESEARCH measured the response callback at argument index 7 and the error callback at index 8; VERIFY that against the real signature before writing the stub rather than trusting the index.
    - src/test/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolParityTest.kt lines 42-59 — the in-repo mockito deep-stub idiom: `mock<MontoyaApi>(defaultAnswer = Answers.RETURNS_DEEP_STUBS)` plus a `whenever(api.burpSuite().version().edition())` stub.
    - src/test/kotlin/com/six2dez/burp/aiagent/TestSettings.kt — `TestSettings.baselineSettings()`. `AgentSettings` has ~90 non-defaulted parameters; never hand-build one.
    - .planning/phases/22-agent-tool-call-trust-boundary/22-RESEARCH.md sections "2. SC4's Acceptance Gate" and "Headless `ChatPanel` harness (the SC4 enabler)" in Code Examples — the measured working construction, including `BurpSuiteEdition.COMMUNITY_EDITION` and `whenever(supervisor.requiresBurpAiAndDisabled(any())).thenReturn(false)`.
    - src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelConcurrencyTest.kt lines 55-75 — READ THIS AS A DELIBERATE ANTI-ANALOG. Its KDoc asserts that a real `ChatPanel` "requires Swing + UiTheme and throws HeadlessException in CI". Research measured that premise FALSE (the cost is the Task 1 guard). Do not copy its modelled approach and do not repeat its claim.
  </read_first>

  <action>
Create `src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt`, package
`com.six2dez.burp.aiagent.ui`. Expose an `object ChatPanelTestHarness` (a plain object, not a JUnit test
class — it must contain zero `@Test` functions so no runner picks it up).

**Naming constraint (hard).** `build.gradle.kts:161-171` excludes `*IntegrationTest`,
`*ConcurrencyTest`, `*BackpressureTest`, `*RestartPolicyTest` and `*SupervisionTest` under
`-PexcludeHeavyTests=true`, which is what `.github/workflows/build.yml` passes on the PR gate.
`ChatPanelTestHarness` ends in none of these. Do not rename it to anything that does.

Provide five members:

1. A `data class Harness(val panel: ChatPanel, val api: MontoyaApi, val supervisor: AgentSupervisor)`
   (or an equivalent holder) so a test can reach the mocks for `verify(...)` after driving the UI.

2. `fun create(modelResponse: String, settings: AgentSettings = TestSettings.baselineSettings()): Harness`
   — builds `api` as `mock(defaultAnswer = Answers.RETURNS_DEEP_STUBS)` with
   `whenever(api.burpSuite().version().edition()).thenReturn(BurpSuiteEdition.COMMUNITY_EDITION)`;
   builds `supervisor` as a deep-stub `AgentSupervisor` with
   `whenever(supervisor.requiresBurpAiAndDisabled(any())).thenReturn(false)`; stubs `sendChat` with a
   `thenAnswer` that invokes the response callback with `modelResponse` and then the completion/error
   callback with `null`, both synchronously on the calling thread (this is what makes the whole flow
   deterministic without sleeping). Construct the real `ChatPanel` with `getSettings = { settings }`,
   `applySettings = { }`, `validateBackend = { null }`, `ensureBackendReady = { true }`,
   `showError = { }`, `onStatusChanged = { }`, `onResponseReady = { }`, `passiveScanner = null`, then
   call `panel.createNewSession()`.
   Make `modelResponse` a **parameter with no default value** — a default is how a future test silently
   asserts against the wrong model output. Cite the `ContextPreviewDialog.kt:21-23` "deliberately has NO
   default value" comment shape in a `//` note above it.

3. `fun <T : JComponent> find(root: Container, type: Class<T>, predicate: (T) -> Boolean = { true }): T?`
   — a depth-first search over the component tree, used to locate the input `JTextArea` and the
   `JButton` whose `text == "Send"`. Keep it generic; later plans reuse it to find the approval card and
   its four buttons.

4. `fun sendUserMessage(h: Harness, text: String)` — sets the found input component's text to `text`,
   then `SwingUtilities.invokeAndWait { sendButton.doClick() }`.

5. `fun drainEdt(times: Int = 4)` — `repeat(times) { SwingUtilities.invokeAndWait { } }`, draining the
   nested `invokeLater` blocks the chain schedules. Four was the measured sufficient count; make it a
   parameter so a later plan can raise it without editing every call site.

Assertion/import conventions in this repo: `org.junit.jupiter.api.*` and
`org.junit.jupiter.api.Assertions.*`, mockito via `org.mockito.kotlin.{mock, whenever, any, verify, never, atLeastOnce}`
and `org.mockito.Answers`. Do not introduce `kotlin.test` assertions here.

If `sendChat`'s real signature does not place the response callback at index 7, use the correct index and
record the corrected index in a `//` comment naming the signature you read it from — do not silently
guess.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew compileTestKotlin ktlintCheck -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'object ChatPanelTestHarness' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt` returns 1
    - `grep -c '@Test' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt` returns 0 — the fixture is not itself a test class
    - `grep -c 'RETURNS_DEEP_STUBS' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt` returns at least 1
    - `grep -c 'TestSettings.baselineSettings' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt` returns at least 1 — `AgentSettings` is never hand-built
    - `grep -c 'invokeAndWait' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt` returns at least 2 — the click helper and the EDT drain
    - `ls src/test/kotlin/com/six2dez/burp/aiagent/ui/ | grep -cE 'ChatPanelTestHarness(Integration|Concurrency|Backpressure|RestartPolicy|Supervision)Test'` returns 0
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew compileTestKotlin ktlintCheck -q` exits 0
  </acceptance_criteria>

  <done>`ChatPanelTestHarness` constructs a real `ChatPanel` headlessly from deep-stub mocks, can drive the real Send button, and compiles clean under ktlint.</done>
</task>

<task type="auto">
  <name>Task 3: Seed ChatPanelToolGateTest with the pre-fix SEC-06 characterization</name>

  <files>src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt</files>

  <read_first>
    - src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelTestHarness.kt — as created in Task 2.
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/ChatPanel.kt lines 2114-2200 — `maybeExecuteToolCall` exactly as it stands today: it takes `ToolCallParser.extractFirst` output and calls `McpToolExecutor.executeTool` at `:2132` with no gate whatsoever. This is the defect being characterized.
    - src/main/kotlin/com/six2dez/burp/aiagent/ui/ToolCallParser.kt — the fenced-JSON shape `extractFirst` accepts, so the stubbed model response is parsed rather than ignored.
    - src/main/kotlin/com/six2dez/burp/aiagent/mcp/tools/McpToolExecutorImpl.kt — grep for `"scope_check"` and read that branch, to confirm it calls `api.scope().isInScope(url)` and therefore that the mockito `verify` lands on a real, reachable Burp call.
    - .planning/phases/22-agent-tool-call-trust-boundary/22-VALIDATION.md section "The SC4 acceptance gate, stated concretely".
    - .planning/phases/22-agent-tool-call-trust-boundary/22-RESEARCH.md section "2. SC4's Acceptance Gate", Measurement D — the exact measured run, including the console output `PROBE3 toolReachedBurp=YES`.
  </read_first>

  <action>
Create `src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt`, package
`com.six2dez.burp.aiagent.ui`, class `ChatPanelToolGateTest`. This file is the SC4 acceptance gate and
later plans extend it; seed it now with exactly one test plus a file-level KDoc.

**The file KDoc must state, in English, three things:** (a) this file drives a REAL `ChatPanel` through
its REAL Send button, not a model of one; (b) the single test it currently contains asserts the
**pre-fix** behaviour of the ungated path and is therefore GREEN today; (c) plan `22-07` turns this file
into the SC4 acceptance gate, and it does **not** invert this test. `scope_check` is `SecTier.AUTO` in
22-02's locked tier table, so it still runs silently after the gate lands; `22-07` therefore **keeps**
this assertion as the `atLeastOnce()` **`AUTO` companion** and adds two *new* assertions on a
`CONFIRM`-tier tool (`proxy_http_history`): a `never()` on the Burp call that tool makes, plus
`assertNotNull(findApprovalCard(...))`. Those two are red against the pre-gate commit and green after,
which is what makes this a non-vacuous acceptance gate under the Phase 20 SC4 / Phase 21 rule that
*a test that passes both before and after the change has not tested the defect*.

The test — name it `modelEmittedToolCall_reachesBurpWithNoUserDecision_preFixBaseline`:

1. Build the harness with a model response that is a fenced JSON tool call for `scope_check` with args
   `{"url":"http://evil.example/"}` — the exact payload research measured.
2. `sendUserMessage(h, "check scope please")`.
3. `drainEdt()`.
4. Assert `verify(h.api.scope(), atLeastOnce()).isInScope("http://evil.example/")`.

Put a `//` comment directly above the `verify` line reading, in substance: *SEC-06 / SC4 — this line is
the defect. It is GREEN today because `maybeExecuteToolCall` (`ChatPanel.kt:2132`) calls
`McpToolExecutor.executeTool` directly with no gate at all. After plan 22-07 this exact line still
passes, because `scope_check` is `SecTier.AUTO` and runs silently by design; 22-07 keeps it as the
`AUTO` companion and puts the `never()` acceptance assertion on the `CONFIRM`-tier `proxy_http_history`.*

Do **not** add `@Disabled`, do not assert anything about an approval card (none exists yet), and do not
assert any behaviour that plans 22-02..22-09 will legitimately change — this file must stay green from
this commit **onward**. Nothing in it is ever inverted: 22-07 **keeps** this test as the `atLeastOnce()`
`AUTO` companion and adds the `CONFIRM`-tier gate assertion (`never()` on `proxy_http_history`)
*alongside* it. That is the same statement as the KDoc mandate above; if the two ever read differently,
the KDoc mandate wins.

**Naming constraint (hard):** the class must be `ChatPanelToolGateTest`, never
`ChatPanelToolGateIntegrationTest`. Research Pitfall 7: `-PexcludeHeavyTests=true` on the PR gate would
silently skip an `*IntegrationTest`, making the SC4 gate invisible exactly where it matters most.
  </action>

  <verify>
    <automated>JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests "*ChatPanelToolGateTest*" -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'class ChatPanelToolGateTest' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` returns 1
    - `grep -c 'isInScope' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` returns at least 1
    - `grep -c 'atLeastOnce' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` returns 1 — the pre-fix assertion
    - `grep -c '@Disabled' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` returns 0
    - `grep -c '22-07' src/test/kotlin/com/six2dez/burp/aiagent/ui/ChatPanelToolGateTest.kt` returns at least 1 — the KDoc names the plan that turns this file into the acceptance gate
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test --tests "*ChatPanelToolGateTest*" -q` exits 0
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test -PexcludeHeavyTests=true --tests "*ToolGate*"` exits 0 — **no `-q` here**, because `-q` suppresses the very output the count below would otherwise come from. Then read the count from the report that run writes: `grep -c '<testcase' build/test-results/test/TEST-com.six2dez.burp.aiagent.ui.ChatPanelToolGateTest.xml` returns 1 — proving the PR gate does not skip this file
    - `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt -q` exits 0 and `git diff --stat -- detekt-baseline.xml` is empty
  </acceptance_criteria>

  <done>`ChatPanelToolGateTest` exists, runs on the PR gate, and its single test proves against the real production path that a model-emitted tool call currently reaches Burp with no user decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI backend response → `ChatPanel.maybeExecuteToolCall` | Model output is attacker-influenceable: proxy traffic sent via "Send to AI", passive-scan findings and `ext:` tool results are already in model context. The parsed tool name and args cross into the extension here. |
| `McpToolExecutor.executeTool` → Burp Montoya API | The point at which a parsed tool call becomes a real action against Burp and, for traffic tools, against the target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Elevation of Privilege | `ChatPanel.maybeExecuteToolCall:2132` → `McpToolExecutor.executeTool` | mitigate | Not fixed by this plan — this plan makes the defect *provable*. Task 3's `atLeastOnce()` assertion is the executable demonstration that no control exists today; plan 22-07 lands the control and adds the `never()` acceptance assertion on the `CONFIRM`-tier `proxy_http_history`. This `scope_check` line is **not** inverted — `scope_check` is `AUTO`, so it stays green and becomes the `AUTO` companion. |
| T-22-13 | Repudiation | The phase's own acceptance gate | mitigate | Test infrastructure lands in its own commit (this plan) so the CI headless/mock-maker assumptions A3/A4 are proven before eight plans depend on them; the test file name is checked against the `-PexcludeHeavyTests=true` PR-gate exclusion list so the gate cannot be silently skipped. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero packages — `22-RESEARCH.md` §"Package Legitimacy Audit" records the empty table and no `build.gradle.kts` dependency block changes. The `jvmArgs` edit in Task 1 adds no dependency. Any deviation requires running the Package Legitimacy Gate before the install. |
</threat_model>

<verification>
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck detekt test` exits 0
- `git diff --stat -- detekt-baseline.xml` is empty
- `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew test -PexcludeHeavyTests=true --tests "*ToolGate*"` executes the new test rather than filtering it out
</verification>

<success_criteria>
- A real `ChatPanel` is constructible and drivable under `-Djava.awt.headless=true`
- `ChatPanelToolGateTest` proves, against the real production path, that a model-emitted `scope_check`
  reaches `api.scope().isInScope("http://evil.example/")` with no user decision
- The full existing suite stays green; the detekt baseline does not grow
- No production behaviour changes other than the accelerator fallback under headless
</success_criteria>

<output>
Create `.planning/phases/22-agent-tool-call-trust-boundary/22-01-SUMMARY.md` when done
</output>
